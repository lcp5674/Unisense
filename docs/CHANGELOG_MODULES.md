| 2026-09-04 | 40518877 | frontend | **dp 血缘同步「元数据库名」改选项框——选数据源后经 /dimensions/source-databases 拉取真实库列表供选择（用户『元数据库名应支持选项框方式，通过数据源获取到数据库让用户指定』）**：上一轮暴露为手输 Input（生产库名非 dp_stable 时需手填）。修复——同步配置基础设置「元数据库名」由 Input 改 **AutoComplete 选项框**：选定 dp 数据源后自动调 `POST /dimensions/source-databases` 拉取该源真实非系统库列表作下拉选项（antd AutoComplete 可搜可选手输兜底）；**切换数据源自动清空旧库名并重拉**（换源后旧库名不适用，防误存）；拉库失败（源不可达/SSRF/私网未放行）静默降级为空列表并提示「未获取到库列表，可手动输入」，不阻塞配置。表单值/保存 payload/`^[A-Za-z0-9_]+$` 白名单校验保持。验证：LineageDpSync 20 passed（+1：切源 → `listSourceDatabases` 用新源调用 → schema 清空 → 下拉出现新源真实库 `other_db` → 点选 → 保存 payload 携带 `source_id=other_source`+`schema_name=other_db`）+ tsc 0；本地容器前端重建（chunk `LineageDpSync-BGwgMHpn.js` 含「元数据库名/选择或输入库名」）；API 对本地不可达源正确返回错误、前端 catch 静默（console 0 error）+ 下拉 loading→降级提示。说明：本地四个数据源均指向生产 IP/回环（不可达），「真实库下拉」由单测 mock 覆盖 + 生产可达源生效 | frontend 修复 lineage dp-sync schema option |
| 2026-09-04 | 33867ddf | frontend | **dp 血缘同步配置支持指定元数据库名（用户『生产环境执行 SELECT … FROM dp_stable.dispatch_task，但生产不是 dp_stable 库，需在指定数据源后可指定数据库』）**：后端（模型 `DpSyncConfig.schema_name` 默认 dp_stable + repo 白名单 + API `_ident_field_error` 标识符校验 + 扫描 SQL `_table_scope` 拼配置库名）早已支持，缺口在前端——同步配置表单只暴露「dp 数据源」下拉、未暴露「元数据库名」，生产 schema_name 停留默认 dp_stable。修复——LineageDpSync 同步配置基础设置新增「元数据库名」输入项（必填 + 仅字母/数字/下划线规则，extra 提示选完数据源后核对），默认表单值/保存 payload 均带 `schema_name`（留空回退 dp_stable）。验证：LineageDpSync 19 passed（+2：保存 payload 含 schema_name=dp_stable / 改库名 dp_ods 保存 payload 携带新值）+ tsc 0；本地容器前端重建后 chunk 含「元数据库名」，API 端到端 PUT schema_name=dp_ods → 200 落库、非法 `dp_ods.dwd` → VALIDATION_ERROR（标识符白名单生效），验证后恢复 dp_stable。影响：不同环境元库库名不同可在同步配置指定，扫描 SQL 按配置库名执行 | frontend 修复 lineage dp-sync config schema |
| 2026-09-04 | 3bb03810 | backend+frontend | **血缘表分层派生支持整库名形态字典码 + 分段码双匹配（用户『数仓分层配置了 wedw_dwd，血缘未分层表仍能看到 wedw_dwd 库的表』→ 选方案 B）**：根因——字典 code 配成整库名（`wedw_dwd`/`wedw_ods`），而派生只按库名 `_` 拆段精确命中（找 `dwd` 找不到 `wedw_dwd`）→ 1.6 万张表全部未分层。修复——`derive_dw_layer_from_catalog_name` 双匹配：**① 整库名最长优先**（`wedw_dwd.dw_order_df` 整库名 `wedw_dwd` ∈ active 码 → 直接命中 `wedw_dwd`）；**② 拆段从右往左**（原逻辑保留，`wedw_dws.dw_ord_df` → `dws`）；表名前缀兜底不变。后端 `layers.py` 与前端 `utils/dwLayer.ts` 逐行同款，治理中心/主图口径一致。验证：容器内真实字典码（混存大写 DWS/ADS/DM + 整库名形态）实测 `wedw_dwd.dw_order_df → wedw_dwd`（修复前 None）、`wedw_ods.* → wedw_ods`、`wedw_dws.* → dws`（拆段保持）、大小写不敏感；后端 test_dw_layer_derivation 15 + 前端 dwLayer.test 6 + AssetGraph/LineageView 130 全绿 + ruff/tsc 0。影响：字典可配整库名形态（如 wedw_dwd=专属层）或分段码（dwd=同层多库统一归层），两种并存按最长优先 | backend+frontend 修复 lineage layer-derivation |
| 2026-09-04 | 9049df71 | frontend | **血缘治理中心小工具分层改字典驱动（用户「治理中心小工具也要走字典驱动」）**：治理中心（健康度/路径查询/终止点/节点清理）表节点分层原为前端硬编码 ODS/DWD/DWS/ADS/DM 正则，不读 system_dict 的 dw_layer——管理员补录 dim/mid/st 等扩展层后主图 AssetGraph 泳道已自动归层，治理中心却仍归「未分层表」。落地：① 新建前端共享 `utils/dwLayer.ts`（与后端 layers.py 同款派生 `deriveDwLayerFromCatalogName`：库名按 `_` 拆段从右往左命中 active 码、回退表名前缀 + 同源配色 `dwLayerStroke` 标准 5 层/扩展层 hash 取色）；② `pathNodeVisual` 表分支改字典派生（治理中心挂载 `listDictItems("dw_layer")` 拉 active 码，未命中回退旧前缀兜底兼容字典未加载）；③ `pathsToGraphData` 给 table 节点补 `dw_layer` 字段，治理中心小血缘图（路径/终止点 graph 视图）复用 AssetGraph 泳道/层色，扩展层自动成带。验证：LineageView 78 passed（+1：字典含 dim 时库名 wedw_dim 表显示 DIM 层胶囊而非「表」）+ tsc 0；真实容器插入 dim 字典项 → `GET /dicts/dw_layer` 返回含 dim（治理中心将派生 wedw_dim.* 表为 DIM），清理后 0 残留；治理中心页面渲染无 console 错误。影响：治理中心小工具与 AssetGraph 主图分层口径一致，字典补录/停用即时生效 | frontend 修复 lineage-governance |
| 2026-09-04 | 0df53724 | backend+frontend | **软删同码重建 409 根治（用户『指标编码已存在: guahao_order_cnt（存在冲突，需协商或裁决后继续）』409）**：uq_metric_code 唯一索引不含 deleted_at，回收站软删记录仍占键位，create 前置检查（过滤软删）放行后 INSERT 撞唯一索引 → IntegrityError 409。修复——create 编码唯一性三态：① 活跃同码 409（保留）；② 仅软删同码 + 创建者=软删 owner 或平台管理员 → 接管（级联物理清除旧废弃记录释放编码，含 ES/cache 同步，审计 metric_archived_taken_over）；③ 仲裁作废记录（successor_code 置位）不接管、提示指向接替指标。restore 补撞码保护（恢复时同码活跃存在→明确报错而非撞唯一索引 500）。顺带根治 purge_metric 级联三处潜伏 NameError（QualityRule/Favorite/text 未导入，接管/回收站硬删真实执行即崩）。验证：semantic service 329 + repo 55（+6 回归）+ ruff 0；真实容器 guahao_order_cnt 软删记录重建 200 OK（id=12 级联清除、新 id=14 DRAFT）。影响接口：create/restore 对软删同码行为增强 | semantic/frontend 修复 metric-register recycle |
| 2026-09-04 | 20dd316e | backend+frontend | **指标模板/维度成员/指标编码三处放开手输与改码（用户「新建或修改主题域编码可手改」后续审查「编码自动生成不可编辑清单」→ A+B+C 全落地）**：① **A 模板编码**：新建模板弹窗补可编辑「模板编码」Input（后端早已支持 code，前端此前未暴露）——留空自动生成 tpl_{域}_{名} + extra 预览 + tpl_ 前缀校验（编辑不可改保持稳定引用）。② **B 维度成员 member_code**：DimensionMemberUpdate 增 member_code——**仅 DRAFT 可改**（发布后编码即稳定标识，改码须废弃重建），同事务级联同步子树 parent_code + 指标绑定 default_member（repository.rename_member_references 防悬空）；前端成员编辑弹窗 DRAFT 显示编码字段，api updateDimensionMember 拆 URL 旧码与新码两参。③ **C 指标 metric_code 改码**：新增 `PUT /metric-definitions/{metric_code}/rename` + service.rename_metric_code——**仅平台管理员 + DRAFT/DEPRECATED**（已发布改码须废弃重建），新码通用标识符校验 + 唯一性；**同事务级联**：血缘边节点键 metric:{old}→metric:{new}、收藏 asset_id、资产订阅、质量观测/外部基准/对账记录 metric_code、冲突仲裁/裁决 JSON（{candidate|existing} 值改写）、他指标 successor_code 反向引用、Metric 主表；**WORM 历史（审计/值快照/查询日志）保留旧码**以维持追溯；审计 metric_definition.rename detail{from,to}；前端 MetricDetail 操作区「改编码」按钮（platform_admin+DRAFT/DEPRECATED）+ Modal（级联警告 Alert + URL replaceState 跳新码）。验证：后端 test_dimension_service 110（+3 member 改码）/test_semantic_service 325（+6 rename：非 admin 拒/已发布拒/格式拒/幂等/冲突/级联）+ ruff 0（仅 1 处既存 E501）；前端 Templates 32 + Dimensions 37（+2 成员改码）+ MetricDetail（改码 Modal）+ tsc 0。影响接口：PUT /dimensions/{d}/members/{m} body 可选 member_code（DRAFT 改码）、新增 PUT /metric-definitions/{code}/rename（platform_admin）。 |
| 2026-09-04 | d382966a | backend+frontend | **注册指标编码放宽为通用标识符 + 原子选源表不再被推断翻成派生且跨步骤记忆保留（用户「注册指标不要限制 4 段式」「原子选源表跳派生要求返回上一步带记忆」）**：① 编码 4 段式强制 → **通用小写标识符**（小写字母开头/仅字母数字下划线/下划线卫生，保留词 token 检查不变）——4 段式仅保留为系统自动生成建议；后端 ConflictPrechecker.validate_code_format + 前端 metricCode.validateMetricCode 双端同口径，MetricCreateRequest 委托自动跟随。② 原子注册选「源表名（兼容旧式来源，可选）」被后端 _infer_type 恒推 derived 强制跳派生——新增 userTypeChosenRef（用户显式选过类型后推断 type 不再覆盖）+ applySuggestion 记忆保护（已手输非空字段不被推断覆盖），切类型/跨步骤返回此前填写内容保留。验证：后端 test_conflict_precheck 31（放宽短/长编码、通用标识符、下划线卫生、保留词不变）+ ruff 0；前端 metricCode 5 + MetricCreate 110（+1 回归：域默认派生→切回原子→选源表推断 derived 不覆盖，跨步骤返回名称/源表保留；已知 HEAD 既有多变体挂载用例失败经 stash 归因与本次无关）；tsc 0。影响接口：metric_code 校验口径放宽（向后兼容）。 |
| 2026-09-04 | 3f3def78 | backend+frontend | **主题域编码手动修改（用户「新建或修改主题域对应的编码要求可以手动修改调整」）**：此前创建弹窗编码为「自动生成」只读预览、编辑弹窗无编码字段（SubjectDomainUpdate 不含 code）——编码一旦生成就不可改。落地：① 后端 SubjectDomainUpdate 增 code（复用 create 格式校验），update_domain 加改码分支——**仅平台管理员**（编码为全局标识符），同事务级联更新全部按 code 引用的记录：metric.domain/reviewer_domain、dimension.domain/reviewer_domain、metric_mount/metric_template/measure_catalog/term.domain+reviewer_domain、data_source、conflict、grants、api_client.scope_domain、user.domain（Core 批量 UPDATE 15 列）+ user.domains JSON 权限域数组（Python 改写去重）；新码冲突 ConflictError、幂等提交 no-op；审计 subject_domain.update detail 补 code_renamed{from,to}。② 前端创建弹窗编码改为可编辑 Input（留空自动生成 + extra 实时预览 + 格式校验）；编辑弹窗新增编码字段（platform_admin 可编辑 + 改码级联警告 Alert；非 admin disabled）；改码成功后选中新编码刷新详情/树。验证：后端 test_subject_domain_service 50 passed（+4：非 admin 拒绝/冲突拒绝/级联 16 次 execute+JSON 改写/幂等 no-op）+ 相关 269 passed + ruff 0；前端 SubjectDomain 24 passed（+4：手输编码提交/改码警告+携带新码/幂等不携带/非 admin 禁用）+ tsc 0。影响接口：PUT /domains/{code} 请求体可选 code（向后兼容）。 |
| 2026-09-04 | c58c3eed | backend+frontend | **dp 血缘同步统计概览（用户「dp调度血缘同步中仍然没有把dp数据源里的任务总量有多少，有多少解析成功了，有多少没有解析成功，解析出来多少个表和字段」）**：新增 `GET /lineage/dp-sync/stats`——① **任务总量**：连 dp 源实时 COUNT 活跃任务/节点（按配置类型过滤，`_in_clause` 参数化 + `_safe_table_name` 防注入；源不可达返回 null + reason 不阻断统计）；② **解析成果**：最近一次成功**全量轮**（增量轮只扫变更任务不作概览口径）+ 历史成功轮累计，派生 `parse_success_total`（parsed_ok+llm_confirmed）/`parse_fail_total`（diverged+llm_fallback+unparseable+errors）/`parse_rate`；③ **待抉择存量**（未裁决单按状态计数）；④ **血缘沉淀**：dp_sql 通道活跃表级边 / distinct 表节点 / `lineage_field_mapping` 字段映射条数（即解析出的表与字段）。前端 `LineageDpSync` OpsTab 顶部新增「统计概览」卡（Statistic 四宫格 + 最近全量轮成功/失败/成功率 Progress + 历史累计 + 源不可达 Alert warning）。验证：后端 test_dp_sync_repo 9 passed（+2：聚合与派生 81.4% 断言 / dp_sql+active SQL 作用域）+ ruff 0；前端 LineageDpSync 18 passed（+2：统计卡数值断言 / 源不可达降级）+ tsc 0（既存 warns 用例失败经 stash 归因为 HEAD 既存）。影响接口：新增 GET /stats（只读）。 |
| 2026-09-04 | 01a9ae5b | backend | **可观测中心「AI 模型 未启用」误报修复（用户『生产环境下可观测中心 AI 模型显示未启用，但已在系统配置配置了本地 llm 实例且测试通过』）**：根因——周期依赖健康探针的 LLM 启用判定只读 env（settings.llm_base_url/llm_api_key），而系统配置页配置的实例存 DB llm_config 表（enabled=1）→ 探针误写 meta.enabled=false 展示「未启用」。修复——新增 _llm_probe_target()：经 LlmConfigService.get_effective() 解析生效配置（DB 启用实例 > env > 未配置，与运行时 LLM 路由同源、与 _olap_probe_target 同构），_llm_gateway_alive 改接受目标 url（探活语义不变）。验证：test_dependency_probe 8（+3：DB 实例优先/未配置/DB 异常回落 env）+ ruff 0；真实容器——dependency_health llm 行 HEALTHY + {enabled:true, last_probe:ok}（graph/es 同步正常）。影响接口：无。 |
| 2026-09-04 | 待提交 | backend+frontend | **dp 待抉择单 LLM 重试后台任务化（用户『LLM 重试不能后台运行吗？切到其他页面看不到进度与结果』）**：dp「LLM 重试」原为同步 HTTP（逐张串行调 LLM 实测 241s/批），切页即失明。改为与采集批量推断同款任务化：迁移 0140 建 `dp_ticket_retry_task`（候选快照 + 逐张进度 + 协作取消 + counts 语义计数）；`DpSyncService` 抽 `_retry_one_ticket`（单张处置，同步 retry-llm 与新任务共用）+ `collect_retry_candidates`；arq 任务 `run_dp_ticket_retry_task` 逐张独立 session 执行、进度实时落库；API `POST /tickets/retry-llm/async` 创建 + `GET /retry-tasks` 列表/详情 + `POST /cancel`（非平台管理员仅本人任务）。前端 `BatchInferCenter` **双源化**（采集批量 + dp 重试共用右下角浮条/Drawer/完成摘要/取消；dp 终态摘要展示「自动采纳/刷新意见/保留/失败」），`LineageDpSync` LLM 重试改异步提交并 `notifyBatchInferActivity(id,"dp")` 唤醒任务中心。验证：后端 test_dp_sync_resolve 22 passed（+3 单张三元组/候选快照）+ ruff 0；前端 BatchInferCenter 10（+2 dp 双源）+ Layout 13 + LineageDpSync 16 passed + tsc 0。 |
| 2026-09-04 | 待提交 | backend+frontend | **field-impact 422 根治（用户『血缘查询/治理中心字段级查询仍报 422：node=裸表名&direction=both』）**：上轮只修了治理中心入口（table: 前缀 + downstream），但**血缘查询/影响分析 Tab 的 `runFieldQuery`** 把用户输入的裸表名（经「使用输入值」兜底选项设为节点）原样透传 `lineageFieldImpact`，方向切「双向 both」时同样触发后端前缀校验 422。双管齐下根治：① **后端宽容**——`field_impact` 对无前缀 node 自动按 `table:` 起点处理（治理中心/血缘查询以表为入口最常见；字段起点仍须显式 `field:`，由 LineageNodePicker 保证），不再对裸表名 422；② **前端规范化**——`runFieldQuery` 对无 `:` 前缀的输入自动补 `table:`（与治理中心 openFieldTable 一致），track 同样记录规范节点。验证：后端 field_impact 测试 5→10 passed（+宽容用例：裸 `库.表`/纯表名按表展开、`metric:` 仍拒绝）；前端 LineageView 69 passed（+1：手动输入裸表名选「使用输入值」+ 双向 → 请求 `node:table:裸表名`）+ tsc 0（结构概览 2 用例失败仍为并行未提交 GraphTab viewMode 改 full 所致，零交集）。 |
| 2026-09-04 | 待提交 | frontend | **血缘治理中心「查看表下字段血缘」内嵌化 + 422 修复（用户『点击切字段级不希望跳血缘查询，直接在治理中心查看；field-impact 返回 422』）**：① **422 根因修复**——治理中心血缘视图点表节点此前把**裸表名**（如 `wedw_dw.wy_jzyx_rule_base_info_df`）传 `lineageFieldImpact`，而后端 `field_impact` 要求节点带 `table:`/`field:` 前缀，无前缀抛 `ValidationError(BAD_REQUEST)` → 全局映射 HTTP 422；现以 `table:{裸表名}` 前缀查询。② **治理中心内嵌查看（不跳 Tab）**——撤销此前「点表节点自动切到血缘查询/影响分析 Tab 并预填」的跨 Tab 跳转（用户明确不希望跳转）：治理中心两个体检卡血缘视图点击表节点改为**本 Tab 内 Drawer 直接展示该表下游字段级血缘**——复用血缘查询字段级能力管线（`lineageFieldImpact` + `buildFieldGraphData` + AssetGraph 字段图 + 字段映射明细表），`lanes=false`/`dimOnHover=false` 对齐影响分析字段级渲染；**`fieldColumns` 提取为模块级**供影响分析字段级视图与治理中心内嵌共用（消除重复）；主组件 `LineageView` 撤销受控 Tab/`fieldLineageRequest`/`handleOpenFieldLineage`（回非受控 Tabs），`ImpactTab` 移除 `request` prop 预填死代码。验证：前端 LineageView 68 passed（治理中心用例更新为内嵌 Drawer + `table:` 前缀断言）+ tsc 0（结构概览 2 用例失败为**并行会话未提交** GraphTab 默认改 `full` 所致，与本次零交集）。 |
| 2026-09-04 | 564c1b30 | frontend | **血缘大图字段显示修复（用户『下游健康体检/血缘视图点击显示字段没反应、不展示字段级血缘』）**：根因——AssetGraph 限流渲染 `pickVisible` 在节点 >160 时按 `nodeRank` 截断（metric=0<table=1<**field=2 最低**），血缘总览大图（1650+ 表节点）即使数据含 field 节点、用户开启「显示字段」后 field 仍被 160 截断**全部挤出可见集**→ 图上永远看不到字段（上一轮 81f07ece 只修了按钮显隐，未修截断；数据一旦含 field 按钮恢复显示、点了仍无字段）。修复——`pickVisible` 增加 `showFields` 参数：**field 节点不占用核心 MAX_RENDER_NODES=160 额度**，showFields 时按血缘度排序叠加（独立上限 FIELD_RENDER_CAP=400），字段边随两端可见进入图；调用处传 `showFieldsOn` 并纳入 useMemo 依赖。字段级血缘数据链路确认完整（后端 /path/terminals 支持 field: 起点、/nodes 支持 field 搜索、pathsToGraphData 已解析 field:/column:→type=field）。验证：AssetGraph 32 passed（+1：166 节点大图（160 表+6 字段）field 全部叠加可见 + 字段边入图）+ tsc 0。 |
| 2026-09-04 | 81f07ece | frontend | **AssetGraph 字段开关按数据自适应（用户『治理中心下游健康体检点击显示字段没反应』）**：根因——治理中心下游健康体检/链路体检的「血缘视图」mini AssetGraph 数据（`termGraph`/`pathGraph`，来自后端 path/terminals 的链路节点）**不含任何 field 节点**，而 AssetGraph 工具栏「显示字段/隐藏字段」按钮无条件渲染——数据无字段节点时点击切换无任何可见变化（无字段可显/可隐），用户感知"没反应"。修复——AssetGraph 工具栏按钮加 `(typeCounts.field ?? 0) > 0` 条件：**数据不含字段节点时隐藏该开关**（无意义控件不展示），含字段节点时（SQL 血缘解析当页图/字段钻取子图/血缘总览有字段数据时）按钮照常可用——一处修复覆盖全部使用方。验证：AssetGraph 31 passed（+1：无字段节点数据不渲染 asset-graph-show-fields）+ tsc 0。 |
| 2026-09-04 | 待提交 | frontend | **治理中心健康度维度数值展示修复（用户『血缘治理中心展示的失效率没有对应的数值』）**：根因——健康度五维卡片仅渲染 `Progress percent=维度健康分`（如失效率健康分 100），**标签是『失效率』而进度条/数值语义是健康分**（失效率 0% 却显示 100% 满条），且后端 `HealthDimension.detail` 携带的原始业务数值（`stale_edges/edge_total`、`broken_edges`、`metric_with_lineage`、`days_since_run`、`mysql_edges vs graph_edges` 等）前端完全未展示——用户看不到『失效 X / Y 边（Z%）』这类真实数值。修复——① 新增 `healthDimDetail` 明细文案 helper：每维度从 `detail` 生成中文业务数值（**失效率**：`失效 0 / 3789 边（0.0%）`；断链率：`断链 1960 / 3789 边（51.7%）`；覆盖完整度：`指标 10/11 有血缘 · N 表无下游`；采集新鲜度：`最近采集：刚刚 / N 天前 / 尚无采集运行记录`；图-库对账：`库 3789 vs 图 1466 边 · 偏差 61.3%`，图不可达/未配置给可读原因）；② 维度卡片渲染重构——进度条 `format={() => ''}` 隐藏条内百分比（避免『失效率』标签下出现语义颠倒的 100%），右侧以 `{score} 分` 彩色数字明确标注健康分（与坏率语义区分），下方展示业务明细数值。验证：前端 LineageView 60 passed（+1：评估后各维度显示得分数字与业务数值断言——失效 0/3789 边 0.0%、断链 51.7%、指标 10/11、最近采集刚刚、偏差 61.3%、100 分/48 分、综合 78）+ tsc 0。 |
| 2026-09-04 | 50399f66 | frontend | **dp 待抉择单批量 LLM 重试支持勾选驱动（用户『LLM 重试为什么没有批量选择功能』）**：此前批量「LLM 重试」为对全部 LLM 类型单一键生效（无勾选），用户无法只重试选中的部分单。修复——TicketsTab 列表加 `rowSelection`：仅 LLM 可重试行可勾选（`isLlmRetryable` 判定，已裁决/真分歧等不可重试行 checkbox 禁用），跨页保留勾选；批量按钮随勾选数显示「LLM 重试（已选 N）」并按勾选 id 提交（`{ticket_ids:[...]}`，后端仍过候选过滤安全）；无勾选点击保留全量确认兜底（弹窗注明范围）；完成/失败后清空勾选。验证：LineageDpSync 16 passed（+1：勾选 2 行→确认→retryDpSyncLlm 收 {ticket_ids:[21,23]}、真分歧行 disabled）+ tsc 0。 |
| 2026-09-04 | 8a719d7f | dp-sync | **待抉择单 LLM 重试（单条+批量，用户『llm 类型错误是否可重试、支持批量』）**：此前 LLM 关闭/异常/超时产出的单（diverged 原因含「LLM 已关闭/LLM 确认输出异常」、llm_fallback、unparseable 含「LLM 兜底输出异常」）只有「采纳 sqlglot/重跑 sqlglot」两条放弃 LLM 的处置，无「LLM 恢复后重新调 LLM」能力。新增 `DpSyncService.retry_llm_tickets`（未注入 llm_chat 时按需 `_build_llm_chat` 走 LlmConfigService.build_client——DB 实例优先含 disable_thinking）+ repo `list_retryable_llm_tickets`（未裁决 + status=llm_fallback 或 diverged/unparseable 且原因 LLM 标记）/`update_ticket_llm`（刷新意见/状态不裁决）+ `POST /tickets/retry-llm`（body ticket_ids 可选，单条传 id/批量不传，audit）+ 前端 TicketsTab「LLM 重试」批量按钮 + 详情单条按钮（`isLlmRetryable` 同后端语义）。处置语义（agree 自动消解，与 reprocess-unparseable 系统裁决一致）：diverged → 重跑 LLM confirm（agree → 自动采纳 sqlglot 入库置 accept_sqlglot；disagree → 刷新 llm_opinion/原因保留待人工）；llm_fallback/unparseable → 重跑 LLM 兜底（可提炼 → 刷新为 llm_fallback 参考；仍失败 → 保留 unparseable）；LLM 异常计 failed 单张不阻断。验证：后端 test_dp_sync_resolve 19 passed（+5 retry 回归：agree 自动消解/disagree 刷新/fallback 刷新/仍失败 kept/空内容 failed 保留）+ ruff 0；前端 tsc 0。 |
| 2026-09-04 | 1929e3bb | frontend | **治理中心下游健康体检修复（用户『提示「发现 个下游终止节点」无数字、无血缘视图效果』）**：① **前后端字段命名不一致修复**——后端 `LineageTerminalsResponse` 返回 `node`/`terminal_count`/`truncated`，前端 `LineageTerminalsResult` 误声明为 `source`/`total` 且渲染引用 `termResult.total`（恒 undefined）→ 对齐后端真实字段（node/terminal_count/truncated），「发现 N 个下游终止节点」与「链路完整」提示恢复正确数字/起点名；② `LineageTerminalItem` 补 `path`（含起点的完整节点序列，MySQL DFS 与 Neo4j shortestPath 两分支均含起点）与 `node_type`（此前前端类型缺失导致无法画图）；③ **补血缘视图效果**——下游体检结果新增 Segmented「终止节点 / 血缘视图」，把从起点到各终止节点的路径（`pathsToGraphData`）合并渲染为 AssetGraph 小血缘图（复用链路体检可视化基建，节点按前缀 table/metric/field 归类）。验证：前端 LineageView 59 passed（+「血缘视图」切换用例，mock 对齐后端真实结构）+ tsc 0；后端零改动。 |
| 2026-09-04 | 6540e6a3 | semantic+frontend | **字段级血缘钻取体验增强（用户『tracking 422 + 字段级血缘未展示表信息 + 表达式表格不直观 + 点击字段节点后其他节点暗』）**：① 后端 tracking 白名单补 `lineage_field_drill`（前端字段钻取埋点此前 422 被静默丢弃，现正常上报、未知事件仍拒绝）；② 字段钻取视图新增**表加工上下文带**——上游来源表 → 当前表 → 下游去向表（Tag 可点击切换到对应表的字段钻取，不在当前图提示），让用户一眼看清该表在加工链中的位置（此前只有标题一行 db.tbl，无参与表上下文）；③ 明细表新增**加工方式列**（表达式分类 Tag：直取/空值兜底 COALESCE·NVL/聚合加工 SUM·COUNT/条件分支 CASE/计算表达式/函数加工/常量，空表达式不再重复显示『直取』文字而是清晰分类），表达式列改『加工表达式』纯展示；④ AssetGraph 新增 **dimOnHover 开关**（默认 true 保持大图『从哪来/流向哪』压暗聚焦；字段级钻取小图传 false——hover 只高亮血缘链上节点/边、不再把其余节点压暗到 0.2 透明度，解决密集字段图上『点一个字段其他全暗看不清』）。验证：后端 test_tracking_validation 7 passed（+field_drill 接受）；前端 LineageView 58 passed（+上下文带上游表/加工方式直取断言）+ AssetGraph 30 + tsc 0；容器 tracking 白名单热更新 + 前端镜像重建 healthy（chunk 含全部中文特征串）。 |
| 2026-09-04 | dec30f9c | frontend | **血缘视图治理中心产品化重构（用户『治理中心文本框太技术化（前缀手输/容器内路径/级联删除按钮），布局不美观』）**：① 提取共享 `LineageNodePicker`（components/lineage/，可搜索节点选择器——无关键词共享缓存预加载 top-N、输入 300ms 防抖远程检索、选中返回规范 id，用户无需感知 `metric:/table:` 技术前缀）替代治理中心全部裸文本框；② 治理中心重排为仪表盘化布局——平台健康度横贯卡 + 两行两列卡组；**链路体检（A→B）**：起点/终点选择器 + 查询 → 可达路径以**分层步骤条**展示（每路径一个 Collapse 面板，节点按数仓层实底着色胶囊 DWD/DWS/指标…，悬浮显示完整 id）+ 「血缘视图」Segmented 合并路径为 AssetGraph 小图可视化；**下游健康体检（断链定位）**：起点选择器 → 终止点列表红色高亮断链（实体已删除/可到覆盖治理修复）；**节点清理（下线维护）**：节点选择器 + Modal 二次确认后软删该节点全部血缘边（说明可恢复），替代裸 Input+Popconfirm；**血缘重建（高级）**：批量解析/目录扫描/血缘资产导出收进 Collapse 默认收起（运维向不再平铺）；健康度布局紧凑化。后端零改动（7 端点复用）。验证：前端 LineageView 58 passed（+5 治理中心用例：产品化布局/分层步骤条链路/不可达提示/断链红标/Modal 确认清理）+ tsc 0；前端全量 1302 passed（3 失败全在并行 WIP 的 RoleOwnerSelect/LineageDpSync 文件，与本次零交集）。 |
| 2026-09-04 | 9ce7241f+febbd82d | semantic+frontend | **血缘图谱字段级钻取（方案 B，用户『字段级血缘挂到图谱——点击表节点展开字段节点 field:表.列、字段间连线展示』）**：此前 dp 同步的 34790 条字段映射（lineage_field_mapping 独立表）只写库无任何展示出口，图谱只有表级边。后端：DpLineageRepository.list_field_mappings_by_table（按表反查：source_table=该表 OR target_table=该表 + source_column 非空 + 未软删）+ GET /lineage/field-drill（读权限，组装 field:{表}.{列} 节点 / DERIVED_FROM 字段边 / mappings 明细含表达式/来源/置信度，降级占位行排除不伪造）。前端：血缘图谱 GraphTab 点击表节点→startFieldDrill——有逐列映射则在主图位置渲染「字段级血缘」Card（AssetGraph 字段节点图 + 下方逐列映射明细表 + 查看表详情/返回表级图谱按钮），无映射回退原表详情抽屉（既有行为零破坏）；字段信息抽屉改按节点 id 反推所属表。验证：后端 repo+API 10 passed（+SQL 过滤形态/降级排除/空表空子图）+ ruff 0；前端 LineageView 53 passed（+钻取子图/回退详情）+ tsc 0；真实容器 wedw_dwd.dp_dq_measure_df → 24 字段/12 边/12 映射、空表空子图；后端热更新 + 前端镜像重建 healthy。 |
| 2026-09-04 | 待提交 | semantic+frontend | **血缘视图数仓分层贯通（用户『未分层表是否按数据字典识别？数仓层变更血缘是否同步？』）**：此前三套『层』互不相通——字典 dw_layer 只做注册表单下拉、Metric.dw_layer 枚举（大写）后端血缘节点从不返回、前端泳道只按表名前缀写死正则猜层（指标恒归指标带/表归未分层带），注册选 DWS 的指标在血缘视图零体现。落地贯通：① 后端血缘/资产图两处 metric 节点构造（lineage `resolve_node_meta` + assetmap `_graph_metric_nodes`）查询补 `Metric.dw_layer`，节点返回**小写归一化** `dw_layer`（ODS→ods，空值 None 保持未分层语义）——血缘总览（graph_from_edges 富集）与资产地图（graph_from_mysql）同源生效；② 前端 `AssetGraphNode` 显式声明 `dw_layer`，`layerOf` 重构——**节点携带分层字段优先**（表/指标任意类型，大小写归一化兜底兼容大写枚举），表名前缀降级为兜底（表侧 DBCatalog 现无分层登记列，保持前缀推断，未来采集登记后可无缝切节点字段）——带 dw_layer 的指标节点经 layerMap 自动获得**层色描边**（layerBadges 默认开），『注册选 DWS 的指标在血缘视图按 DWS 紫色描边』即时生效；指标泳道归属不变（仍指标带，避免打散血缘方向）。效果：指标数仓层变更（改 dw_layer）→ 血缘视图层色随动；数据字典项/表名前缀规则仍与泳道无关（两机制定位不同，文档注明）。验证：lineage+assetmap repository 148 passed（+2：resolve_node_meta metric 节点含 dw_layer 小写 / graph_from_mysql 节点含 dw_layer）+ 服务层 163 passed + ruff 0；前端 AssetGraph 30 passed（+1 layerOf 大小写归一化 + 表节点携带优先 + 非法值回退前缀 + 空串视为未携带）+ tsc 0。 |
| 2026-09-04 | 6b6421ac | llm+frontend | **LLM 连通测试 chat 探测超时上限 15s→90s（用户『网关可达且鉴权通过但真实推理 ReadTimeout，明明可用』）**：根因——test 连通是两步探测：GET /models（秒回不占 slot）✅ + 真实 POST /chat（max_tokens=5）❌ ReadTimeout；本地 CPU LLM（llama.cpp 4 slot 并发架构）正在跑批量推断时 4 slot 全被长生成占满（每块 1500-2500 token ÷ 11 t/s ≈ 2-4 分钟），新 chat 请求进 llama.cpp 内部队列排在长任务后，httpx 15s 读超时先到 → 假失败（纯生成 5 token 只需 ~0.5s，非模型慢）。修复——_chat_probe 超时 `min(timeout, 15)` → `min(timeout, 90)`（新模块常量 `_CHAT_PROBE_TIMEOUT_CAP=90.0`，跟随实例 timeout 留上限防 UI 挂死），覆盖 slot 排队 + 本地 prefill 偏慢；真正不可用（连接拒绝/4xx/模型下线）仍秒级失败。验证：test_llm_config_service 59 passed + ruff 0。 |
| 2026-09-04 | 0d23d3f6+87f88856+024aea04 | semantic+frontend | **dp 血缘同步三连（用户『立即扫描显示任务 0/节点 0』+『无法解析/分歧分类分析修复增强覆盖』）**：① 立即扫描改**强制全量重扫**——scan_once 新增 force_full 参数（手动恒全量忽略水位，周期任务保持增量快扫 + 24h 自动全量），按钮改「立即全量扫描」、成功文案明示任务/节点/直入/分歧/无法解析计数（解决『点了显示 0 任务』——实为增量已最新非故障）；② 无法解析（unparseable）**分类根治 16→0**——连 dp 元库逐语句定位失败根因分两类：a) `_split_statements` 对 ";\n" join 多语句偶发产生**纯分号空元素**，`_has_parse_error` 整段解析误判失败致 14 张可解析/纯 DDL 脚本误堆工作台→过滤空分号 + 逐语句判定（与 extract 容错同源）；b) 裸**保留字列名**（`select ..., lock, ...` 精神科锁档字段）sqlglot 严格方言整条 CTAS 失败致 2 张真 failed→`_quote_reserved_column_words` 反引号保护（替换后能解析才采用，order by 等语法不误伤）；结果 16 张 unparseable 全转 ok(2 出真实边)/no_flow(14 纯 DDL 常量) 零 failed；③ 分歧（diverged 85）**分类澄清 + 批量处置**——85 张全部是 llm_enabled=false 期复杂节点按 plan §3.1 降级建单产物（无真实语义分歧、sqlglot 结果完整），新增 `POST /lineage/dp-sync/resolve-llm-disabled` 一键批量采纳 sqlglot 入库（+前端「处置 LLM 关闭期单」按钮），工作台清空只留真分歧/兜底/无法解析。验证：血缘 parser + dp 相关 243+ passed（+8 回归：空分号过滤/语义噪音不再 failed/保留字 lock 出边/order by 不误伤/批量处置三态）+ ruff 0；真实容器存量处置 resolved 85 + unparseable parsed 2/no_flow 14/kept 0 → 活跃待抉择 0；前端 LineageDpSync 11 passed + tsc 0；扫描链路真实验证全量推进（force_full 生效，此前容器漏同步 manual.py 致增量空扫已补）。提交 0d23d3f6/87f88856/024aea04。 |
| 2026-09-04 | 0607f0e6 | frontend | **血缘视图『结构概览 / 全量血缘』双模式（用户『既能完整看到全貌，视觉上又不会显得很乱』）**：方案——全貌有两义（每表名可读 vs 结构鸟瞰），1500+ 节点在固定画布物理不可全读，故双模式：① 结构概览（默认）——AssetGraph 新增 `defaultCollapsedLayers` prop，LineageView 默认把全部数仓层（ods/dwd/dws/ads/dm/table/metric/field）折叠为层聚合带、仅显示层间主干去重边，进入即『8 层结构 + 层间血缘流转』一屏清爽（复用既有 collapseLayers 聚合节点/点击层带展开/折叠提示条，仅把初值从 [] 改为全层）；② 全量血缘——展开全部节点（原 740px 行为不变）。LineageView 加 Segmented 模式切换（focusNode ?node= 聚焦时隐藏并强制 full，聚焦子图天然清晰）；切换用 key 重挂载重置折叠初值；概览高度 520/全量 740。字段级血缘：引擎/节点/详情/手动治理本就支持（SQL 解析当页展示 field:表.列 血缘边），血缘总览与聚焦子图默认 showFields=false 隐藏字段节点（刻意聚焦表/指标主干，字段全量正是『乱』的最大来源）。验证：AssetGraph 29 + LineageView 54 passed（+3：默认概览折叠提示出现 / 切全量折叠清除 / ?node 聚焦不显示切换）+ tsc 0。纯前端，props 向后兼容（defaultCollapsedLayers 可选）。 |
| 2026-09-04 | dp-meta | semantic+frontend | **dp 血缘同步同步范围类型枚举全量中文化（用户『任务类型/节点类型选项应为中文映射而非未识别』）**：连 dp 元库实测（列注释 + script 形态 + node_type 三重实据）补全内置映射——dispatch_task.type 0=同步任务/1=数据抽取（SQL 加工）/3=Shell 任务/4=混合加工任务/10=DataX 同步任务/15=接口同步任务；dispatch_task_step.task_step_type 2=DataX 同步/3=Shell 脚本/4=SQL 执行脚本/5=清表脚本（TRUNCATE）/6=Oracle SQL/PLSQL 脚本/7=Hive/Spark SQL/9=上报配置节点/15=接口同步配置（原仅内置 3 个值，其余探测值标「类型 N（未识别）」）；/meta 探测值全 known，前端兜底数组同步新映射（meta 不可达时选项不误导）。验证：test_dp_sync_meta + API 16 passed（+1 真实枚举全中文无未识别回归）+ ruff 0；前端 LineageDpSync 11 passed + tsc 0；真实容器 /meta 返回 6 task + 8 step 全中文 known、0 未识别。 |
| 2026-09-04 | 待提交 | frontend | **血缘/资产图专业降噪（第 3 层，用户『节点多时展示很混乱，选择第 3 层实现』）**：① 路径高亮替代 1 跳邻域淡化——hover 沿血缘边上下游 BFS 各 3 跳收集『血缘链』节点/路径边（上限 120 防枢纽爆炸），链上节点/边高亮、其余节点压暗 0.06/边压暗 0.05（新增 edge state active/inactive），一眼看出从哪来/流向哪；**顺带修复旧 hover 闭包陷阱**（mount effect 捕获 graphReady=false 致旧 1 跳高亮从不生效，改 graphReadyRef）；② 子图折叠（泳道折叠）——collapseLayers 把某数仓层/语义带收成聚合节点（collapsedLayer/collapsedCount 标记、层色填充白描边、内部边丢弃、外部边按源·目标·类型去重、聚合节点归位原泳道），工具栏『泳道折叠』多选 + 点击聚合节点展开该层 + 折叠提示条（已折叠 N 节点/全部展开）；③ 层间边束（边捆绑轻量版）——markLaneSpan 计算边两端泳道跨度，跨层长边（layerSpan≥2）统一细线 1.1+淡透明 0.22+实线（不再虚线分层），视觉上退为淡色底纹，缓解跨泳道长边线团。验证：AssetGraph 29 passed（+8 第 3 层：折叠纯函数/泳道归位/空折叠/层跨跨度/路径 BFS/锚定边过滤/折叠工具栏交互/hover 路径 record）+ AssetMap/LineageView/LineageImpact 155 全过 + 前端全量 1283 passed 零回归 + tsc 0。纯渲染层，props/数据结构/后端零改动。 |
| 2026-09-04 | e8c9f3c9 | frontend | **批量推断任务终态完成摘要浮条（用户『图标执行一会就不见了，统计值也没太大变化』）**：根因——任务中心浮条只在 running>0 时显示，任务进入终态（done/error/cancelled）后浮条立即消失且无任何完成反馈，用户无法感知本次批量推断是成功补全了 N 个字段还是全表失败。修复——refresh 检测「上一轮 running → 本轮终态」的任务转变，弹出右下角「完成摘要」浮条：任务号 + 完成状态 Tag（完成/失败/已取消）+ 成功/失败/取消表数 + 新增描述数，停留 10s 自动消失（用户可点关闭）；未完成不弹。验证：tsc 0 + BatchInferCenter 7 passed（+1 终态摘要：running→completed 转变展示统计 + 10s 超时消失）+ Layout 13 passed 无回归。 |
| 2026-09-04 | 67647419 | semantic+frontend | **dp 调度血缘运行记录时间差 8 小时 + 无法解析完善（用户『时间为什么差 8 小时 / 对无法解析的分析原因并完善解析场景』）**：① 时区修复——LineageDpSync 的时间渲染原用 dayjs(v).format（对后端 MySQL 返回的无偏移 naive UTC 串按浏览器本地时区解析致差 8 小时），改用全仓 formatCnTime（parseBackendTime 无偏移一律按 UTC 解析再转上海时区，与血缘模块 LineageView 同标准），运行记录/水位/待抉择全部时间列生效；② 无法解析根因分析——实测 201 张 unparseable 单中 195 张（97%）含 dp 调度宏 ${DATA_DATE}/${HIVE_DATA_DATE±N}/${PART_DATE}/${YYYY}/${HH}/${tmp_tabname}/${EXEC_TIME}（平台注入、SQL 内无 set 定义，血缘 parser 的 expand_variables 对未知占位符有意保留故无法展开）；③ 完善解析场景——dp_sync_parser 新增 _expand_schedule_macros：日期宏按基准日期（2026-01-01）±偏移推算真实日期（YYYYMMDD 无横杠、偏移互异防 tab_${DATA_DATE} 与 tab_${DATA_DATE-1} 碰撞同名）、tmp_tabname→tmp_dp_parse、EXEC_TIME→稳定时间戳、未知宏→x 字母占位，接入 parse_dp_step 预处理链（sqlglot 主解析自动覆盖宏场景）；④ 存量补扫——service 新增 reprocess_unparseable_tickets（宏可解析→入库并置 accept_sqlglot 系统裁决 / 纯 DDL→ignore / 仍失败保留）+ POST /lineage/dp-sync/reprocess-unparseable 端点。验证：真实容器补扫 201 张 → parsed 184（dp_sql 边 1033→1460 +427）、no_flow 1、kept 16（UDF 声明/方言真失败保留待人工）；parser+resolve 30 passed + dp 全量 55 passed + ruff 0；前端 tsc 0（bundle 验证 dayjs 依赖移除 + formatCnTime 上海时区 +8 输出正确）。 |
| 2026-09-04 | 66fad660 | frontend | **描述缺失治理统计卡不实时（用户『推断完成后字段覆盖率/缺失字段数/缺表描述不是实时变化』）**：根因——统计卡数值全部来自服务端聚合 coverage，只在 mount/筛选变化/任务终态 load() 时刷新；抽屉写操作（handleFieldEdit/handleFieldInfer/handleTableDescSave）成功仅 refreshDetail() 不刷新统计；任务/本地批次逐表 done 只加 coveredIds（主列表行实时消失）统计卡不动。修复——新增 silentRefreshStats（不触发整页 loading，失败静默保留）+ scheduleSilentStatsRefresh（防抖 800ms 合并连发）：① 抽屉三个写操作成功追加静默刷新；② trackServerTaskRemoval 用 seenDone 识别新增 done 表触发静默刷新；③ 本地批次 inferOneTable 逐表 ok 同样触发；④ 卸载清理防抖 timer。验证：tsc 0 + Catalogs/AssetMap 119 passed 无回归。 |
| 2026-09-03 | 35463d14+f6b507ac+6965bf12+fb26bf84 | semantic+frontend | **dp 血缘同步缺陷审查修复（第二轮，H/M/L 编号 12 项）**：H1 水位改「已扫集 max 推进」——失败/取消不越位（下轮重扫幂等安全，失败任务不再被全表 MAX 永久跳过）+ M2 同源（变更集查询直接带 gmt_modified 算 max，消除两查询窗口漏扫）；H2 字段映射软删墓碑复活（uq 不含 deleted_at 致 SQL 演进同 tuple 重插 1062——先查墓碑复活覆盖合并 provenance）；H3 dp_lineage_poll_task 加 Redis SET NX 分布式锁（cron 每分钟可重入双跑）；M1 周期自动全量观察（last_full_scan_at 超 24h 强制全量轮闭合「任务删除永不 stale」）；M4 图谱/边列表/失效队列 provenance 精确匹配改 token 匹配（provenance_contains 共享 helper，合并边 `sqlglot+dp_sql` 不再被通道过滤丢失）；M8 llm_calls/tickets_created 计数自增（_process_task 包装 llm_chat/create_ticket）；M9 _revive_or_insert tombstone provenance 覆盖改合并；M10 模型去重复索引（index=True + 显式 ix_lfm_edge 并存）；M3 写端点补审计（config/reset/scan-now/cancel/force-cancel/resolve）+ 已裁决单不同 resolution 重复裁决拒绝（同 resolution 幂等）+ manual 边表名格式校验（防脏节点污染血缘图）；L1 手动扫描 30s 最小触发间隔节流（SCAN_THROTTLED）；M5 dp_task_refs 读路径打通（EdgeResponse/Detail 返回调度来源 + 前端边详情「调度来源（DP 同步）」表）。验证：dp+lineage 相关 247+ passed + ruff 0；前端 LineageView 48 + tsc 0。 |
| 2026-09-03 | 5d750bd9+434d2ecb+10808640+66f6f261 | semantic | **dp 血缘同步缺陷审查修复（14 项全量）**：P0——tasks LLM 故障降级日志 kwargs 抛 TypeError 断裂降级链（改 %s 风格）、resolve 未知裁决 500→VALIDATION_ERROR；P1——scan per-task savepoint 事务隔离（单节点 DB 错误不再 PendingRollback 拖垮整轮）、采纳 LLM 剔除 wrong_edges（死字段打通）、scan 用配置 schema/task_table/step_table 消除硬编码、全量轮 mark_missing 删除语义闭环（seen_pairs 改实际入库路径精确收集、删重复解析）；P2——provenance 多来源合并 `hive+dp_sql` 防通道治理归属漂移（_source_l1_edges 兼容匹配）、update_config 显式 null 置 NULL、字段映射 SQL 演进软删清理、手动扫描 registry 终态裁剪、/meta 与 /exclude-preview 与 update_config 标识符白名单防注入、裁决记忆 ticket 快照 task_refs_json 还原完整任务元数据（迁移 0138）。验证：dp+lineage 相关 654+ passed + ruff 0；迁移 0138 已应用真实 DB。 |
| 2026-09-03 | 705d0970 | semantic+frontend | **dp 血缘同步取消机制 A+B+C（用户『为什么点取消不是立即停止/应支持强制终止』）+ 采集通道整合（用户『采集通道 DP 同步应统一到 dp 血缘同步模块』）**：取消改 A 检查点下沉（_process_task 在每 step 前/回填前检查，协作取消停在 step 边界不再等整任务）+ B 两段式（cancel 记录请求时刻，前端超 8s 未停显示『强制终止』→ /scan/{id}/force-cancel 子步骤立即中断，事务回滚不落半成品）+ C 文案（取消后『正在停止…』+ 黄色 Alert 明示等待当前步骤 + 当前任务号）；修复失败 run_log/ingest_run 因 rollback 静默不落库（except 重建 failed）；scan_once 每轮写 lineage_ingest_run(dp_sql) 双轨摘要，_KNOWN_CHANNELS 加 dp_sql，前端 dp_sql=『DP 同步』、dp_csv 改『DP 同步（CSV 历史）』（历史边保留）。验证：后端 66 + ruff 0，前端 59 + tsc 0。
| 2026-09-03 | bd6a1dae | semantic+frontend | **dp 血缘手动扫描改后台异步任务（用户『扫描异常无提示只显示完成、需支持取消、需实时进度』）**：scan-now 同步执行把异常包成 {skipped:failed,error} 仍 code=OK 致前端显示『扫描完成』掩盖连接失败；现改后台异步——scan_once 支持 force（手动绕过 enabled/轮询间隔）+ progress（total/processed/current_task_id/stage）+ cancel_event（当前任务处理完停止）；取消时 run_log 标 cancelled、水位不推进（下轮重扫幂等安全）、已处理保留；新增 dp_sync_manual 进程内 registry（submit/status/cancel）；API POST /scan-now 提交即返 task_id、GET /scan/status/{id} 轮询、POST /scan/{id}/cancel；前端 OpsTab 1.5s 轮询实时进度条/阶段/当前任务、结束三态 success(绿统计)/failed(红+error 全文可复制)/cancelled(黄+语义)、运行中显示取消按钮。验证：后端 dp 43 passed（+3 scan 取消/force +5 manual）+ ruff 0；前端 LineageDpSync 10（+3）+ tsc 0；真实容器 failed 含完整 Can't connect 错误、cancel 受理。 | 新增 3 端点（读+写=治理角色）；无迁移 | semantic/frontend released |
| 2026-09-03 | 149f45e0+5714003e+f6787352 | semantic+frontend | **dp 血缘同步配置增强（用户『source_id 改下拉、任务/节点类型选项覆盖全部枚举、排除规则 tips 不清、正则需校验+命中预览、延伸到数据源采集』）**：① 后端 /lineage/dp-sync/meta 枚举目录——内置已实据映射（task 1 SQL 任务；step 2 DataX/7 Hive-Spark SQL）+ 配置源可达时 DISTINCT+COUNT 探测合并（未识别值标注不编造，覆盖全部真实枚举）；② /exclude-preview 排除正则校验+命中预览（连 dp 源统计 out_table 命中/样例/非法逐条报告/不可达明确信号）；③ 修排除语义 bug——内置默认排除恒生效 + 自定义追加去重（原配置空列表会关闭默认）；④ scan 类型过滤支持空=全部（task/step IN 子句按空列表省略）；⑤ 前端 LineageDpSync 配置页——source_id 改数据源下拉（listDataSources）、类型多选用 /meta 目录（条数/未识别标注/全选·清空=全部）、排除规则 tooltip 通俗化 + 内置默认只读 Tag 展示 + 『校验并预览命中量』（命中数/样例/正则非法/源不可达）；⑥ DataSources 立即采集临时白/黑名单支持『校验并预览命中』——拉源端表清单 fnmatch 表级统计（白名单优先，预计采集数/命中数/样例）。验证：后端 dp 85 passed（+7 meta 纯函数 +5 API）+ ruff 0；前端 LineageDpSync 7（+3）+ DataSources 45（+1）+ tsc 0。 | 新增端点 /lineage/dp-sync/meta、/exclude-preview（读+写=治理角色）；无迁移 | semantic/frontend released |
| 2026-09-03 | c44977d9 | frontend | **dp 血缘同步 9 个 API 函数重复取 .data 致全部返回 undefined（根因）**：request<T>() 已返回信封 data，函数再取 r.data → 运维 Tab watermark.task 崩溃、待抉择「加载失败」、配置回显被掩盖；同步配置页布局优化为「基础设置/同步范围/LLM 与裁决」分区栅格卡片 + OpsTab 水位可选链防御 |
| 2026-09-03 | 7f32ba8c+0d5b8e31+252d4137+1b13c580 | semantic+frontend | **dp 调度血缘（用户『解析 dp 开发任务所有 SQL 节点写入血缘 + 前端可视化 + 生产实时更新』grill 后 8 模块实施）**：从 dp 数据源（mysql_uncategorized，dispatch_task/_step）增量读取开发任务 SQL 节点，sqlglot 确定性解析（L1 表级+L2 字段级）为主 + LLM 分级共识确认（简单直入 / 复杂 LLM 确认一致入库不一致仲裁 / 失败 LLM 兜底），全链路字段级映射独立成表 lineage_field_mapping；边写入走既有 upsert（同表多任务聚合 + 跨任务 provenance 集合 + 复用合并 + 全链路幂等）；任务/节点元数据（责任人链 director/created/modified/checker/项目、调度配置 cycle/cron、描述）静态落边 dp_task_refs + 增量顺刷，动态运行态（状态/最近执行）展示层实时旁路；资产 Owner 回填防孤儿（仅孤儿回填 + 平台用户查无自动建影子用户，管理员后续配置）；待抉择工作台（一节点一单三栏对照、采纳 sqlglot/LLM/手动修正/忽略四操作、sql_hash 裁决记忆自动复用）；轮询 arq 1 分钟 ticker 读 DB 配置判间隔（前端可配 1-60 分钟）+ 增量水位 + 分布式锁 + run_log；前端血缘模块内 /lineage/dp-sync 三 Tab（同步配置/待抉择/运维）+ 权限动作点 lineage:sync/resolve；迁移 0137（dp_sync_config/watermark/run_log/resolution_ticket + lineage_field_mapping + lineage_edge.dp_task_refs）；资产回填与 LLM 调用均带预算/熔断/容错。模块 8 端到端修复：首次保存配置缺 source_id 曾致 500——create_default_config 改必传 source_id + 缺省拒绝干净 VALIDATION_ERROR；真实容器验证：配置创建成功（默认值齐全）、扫描容错路径返回结构化失败（dp 源 192.168.3.226:3327 当前网络不可达，环境问题非代码缺陷）。验证：dp 同步 6 测试文件 73 passed + ruff 0。**｜ spec/dp-lineage-ingest/plan.md + decisions.md D1-D10 决策记录 | 新增端点 /lineage/dp-sync/*（config/tickets/runs/watermark/reset/scan-now）；写=platform_admin/domain_admin | semantic/frontend 新建 dp-lineage-sync released**
| 2026-09-03 | 131c4bff | collector+frontend | **治理抽屉表级/字段推断任务化（方案 B 延伸，用户『实体详情侧栏推断切页后无进度中心』）**：DescriptionCoveragePanel 治理抽屉（detail 侧栏）的「批量字段推断」与「表描述推断」由同步端点改为**提交后端批量任务**（复用 submitBatchInferTask，missing_fields/needs_table_desc 分别触发动作），进度与结果经右下角任务中心跨页可见（切页/刷新不丢）；新增共用 submitDetailTask（runInflight 防重复提交 + notifyBatchInferActivity 唤醒任务中心 + trackServerTaskRemoval 复用：终态自动把已覆盖表从治理主列表移除并 load()）；trackServerTaskRemoval 增可选 onDone 回调，任务终态刷新详情抽屉（detailIdRef 判断仍开本表，防误刷新已切换/关闭抽屉）。注意：Catalogs.tsx「采集目录」运维视图字段抽屉的批量推断仍为同步端点（终态刷新链路不同，未纳入，可后续单独任务化）。验证：tsc 0；Catalogs 118 + AssetMap（治理抽屉新用例：点「推断」→ submitBatchInferTask 收到 missing_fields=0/needs_table_desc=true、inferTableDescription/inferDescriptions 未被调用、提示任务中心）+ 12 skipped 无回归。 | 无接口变更（前端改用既有批量任务端点） | collector/frontend 保持 released |
| 2026-09-03 | 8ae2b238 | collector | **字段批量推断分块 60→40 + max_tokens 1500→2500（用户『51 字段表推断很慢』选方案 3）**：根因——51 字段一次请求输出 ~1530 token（30 token/字段）**刚好撞 max_tokens=1500 截断线** → JSON 切碎 → content 空/异常 → 2×2 重试放大（8-10 分钟）。修复：`_BATCH_CHUNK`/同步端点 batch_chunk 60→**40**（每块 ~1200 token 输出，远低于上限永不截断）+ 批量 `max_tokens` 1500→**2500**（留足余量）；同步端点（collector.py:1885）、service 默认参数、batch_infer_tasks 三路径统一。效果：单块 decode ~110s 可控、失败粒度 30 字段；总输出 token 不变故**不更慢**，但消除截断重试放大。验证：test_collector_batch_infer 28 passed + ruff 0。 | 无接口变更（内部参数） | collector 保持 released |
| 2026-09-03 | 922ce2b6 | frontend | **批量推断任务中心状态机轮询（用户『后台一直在调用 batch-llm-infer?limit=30』选定彻底方案）**：原 BatchInferCenter 无条件每 4s 轮询本人任务（无任务也空转，约 2 万次/天空查）。改为**状态机轮询 + 模块级事件唤醒**：① 无任务零请求——挂载探测一次（恢复刷新后任务可见性），发现无 running 即停止 interval（不再 4s 空转）；有 running 任务才恢复 4s 轮询；任务完成后自动停止。② 有任务立即感知——新增模块级事件总线 `notifyBatchInferActivity()`（activityListeners Set pub/sub），DescriptionCoveragePanel `submitBackendBatch` 提交成功后调用，唤醒任务中心**立即刷新**并按需恢复轮询（解决「零请求无法感知其他入口新提交任务」）。验证：单测 BatchInferCenter 5 passed（+3：无任务挂载一次后 8s 仍零请求 / 有 running 启动 4s 轮询持续拉取 / 任务完成停轮询 + 事件唤醒立即刷新并恢复）；Layout/Catalogs/AssetMap 128 passed + tsc 0；**本地真实验证三场景**（Playwright 监听 /batch-llm-infer 请求：无任务 15s=1 次探测 / DB 造 running 任务后 15s=5 次+浮条出现 / 任务改 done 后 15s 回落 1 次），测试任务已清理。 | 无接口变更（纯前端轮询策略 + 组件内事件总线） | frontend 保持 released |
| 2026-09-03 | 9f0df98f | collector | **批量推断 chunk 级协作取消（用户『取消后前端无变化』后端补强）**：取消粒度从「表」降到「块」（一次 LLM 调用）——`CollectorService.infer_catalog_columns` 新增可选 `cancel_checker: Callable[[], Awaitable[bool]]`（每块 LLM 调用**前**探测一次，返回 False 即抛 `BatchTaskCancelledError` 中断剩余块；同步端点不传=None 永不检查，行为不变）；`run_batch_llm_infer_task`（batch_infer_tasks）每张表构造 cancel_checker 闭包（独立 session 重读任务行 cancel_requested）、needs_table 表描述执行前再探测一次、worker 捕获 BatchTaskCancelledError 将本表标 `cancelled`（非 error）并停止派发剩余 pending 表（终态收敛保持既有语义）。效果：单表大任务（几十字段=多块×LLM 数十秒）点击取消的等待从「等整表跑完（分钟级）」降到「当前块结束（数十秒内）」，前端「停止中」状态及时收敛为「已结束/已取消」。验证：+3 单测（cancel_checker False→首块前中止且零 LLM 调用 / True 放行且按块调用 ceil(n/chunk) 次 / 中途取消第 2 块前抛错且第 1 块已执行），test_collector_batch_infer 11 + test_llm_description 17 = 28 passed + ruff 0。 | 无接口变更（编排方法加可选参数，向后兼容） | collector 保持 released |
| 2026-09-03 | b40c10bf | collector+frontend | **跨表批量 LLM 推断后端任务化（方案 B，用户『采集目录批量 llm 推断切页后看不到进度/结果』选定 B）**：批量推断从「前端有界并发逐表调同步端点」改为「提交后台任务（arq worker 执行）」，进度与结果落库，任意页面/刷新后可查询/取消——根治「进度存组件 state、切页组件卸载即失明」。① 迁移 0134 `batch_llm_infer_task`（actor/org、tasks_json、progress_json 逐表进度、status pending/running/completed/cancelled/failed、done/failed/cancelled/added_total、cancel_requested、started/finished_at）+ 模型 BatchLlmInferTask；② CollectorService 下沉两个编排方法 `infer_catalog_columns`（跳过已有 manual/llm 描述与有效 comment 字段、60 字段分块、按列名回填 upsert）与 `infer_catalog_table_description`（幂等短路已有 llm 描述），同步端点与 arq 任务共用单一实现；③ arq 任务 `run_batch_llm_infer_task`（worker.py 注册）：逐表**串行**执行（AsyncSession 不支持并发；跨任务并发由 arq max_jobs 提供），每表前重读任务行检测 cancel_requested 协作取消（剩余 pending 标 cancelled）、progress_json 原地改须 `flag_modified` 落库（SQLAlchemy JSON 陷阱）、终态回写 `batch_infer_history`（兼容既有历史视图）；④ API：POST /catalogs/batch-llm-infer（创建+入队，逐表校验目录存在+审计）、GET 列表/单任务（platform_admin 全量、其余仅本人，防跨用户窥探）、POST /{id}/cancel；⑤ 前端：DescriptionCoveragePanel「开始推断」改 `submitBatchInferTask`（成功提示+关面板+刷新+清勾选）；新增全局 `BatchInferCenter`（Layout 挂载，右下角浮条 4s 轮询本人任务，任意页面可见进行中进度/逐表状态/取消/最近完成，Drawer 展示）。本地端到端实证：提交→pending→running→逐表 done/cancelled→completed 落库（progress 正确）、取消路径 3 表全 cancelled、history 回写、测试数据清理。验证：后端 348+77 passed（+8 编排/进度单测）+ ruff 0；前端 tsc 0 + Catalogs 46 passed（本地执行 12 用例暂 it.skip——实现已改后端任务，新增提交 payload/失败提示 2 用例）+ AssetMap 71。 | 新增端点 /catalogs/batch-llm-infer（POST/GET/GET{id}/POST{id}/cancel）；读写角色：写=platform_admin/domain_admin、读=platform_admin/domain_admin/metric_owner，非平台管理员仅本人任务 | collector/frontend 保持 released |
| 2026-09-03 | 24e57a15 | collector+frontend | **描述缺失治理口径对齐（方案 A+C，用户『描述生成后治理面板仍在显示该表；max_tokens 是否影响其他 LLM 推断』）**：① 方案 A——治理主列表只显示「仍需治理」表（DescriptionCoveragePanel 主表格 dataSource 过滤 `missing_fields>0 || !table_desc`），单表/批量推断完成后 load() 刷新即从列表消失；全量表资产浏览保留在概览卡下钻（totalTables/fieldCoverage 明细基于全量 per_table，不受过滤影响）。② 方案 C——消除「治理计缺失、批量推断跳过有注释字段」死循环：`_column_has_desc` 将**有效源 comment**（非空/非采集占位串 `from deserializer`，is_effective_comment）视为已描述（有真实 DDL 注释的字段不再显示缺失）；汇总 `fields_with_desc` SQL 端同步 = A(manual/llm 治理记录 count) + B(带有效 comment 且无 manual/llm 记录列数，MySQL JSON_TABLE 参数化 raw SQL，治理筛选 org/source/keyword/database 同口径防注入)；`is_effective_comment` 修纯空格 bug（`"  "` 旧实现误判有效，现与 SQL 端 TRIM(cm)<>'' 语义一致）。验证：后端 collector 282 passed（stats 断言更新：ods_order 有效 comment 字段不再缺失 missing 2→1、fields_with_desc 1→2；+1 is_effective_comment 纯空格单测）+ ruff 0；前端 Catalogs 56 + AssetMap 71 passed（已覆盖表 dwd_user 不再出现在治理主列表）+ tsc 0。 | 无接口变更（前端列表过滤 + 后端统计口径，向后兼容） | collector/frontend 保持 released |
| 2026-09-03 | e294986c | llm+collector+frontend | **LLM 实例「关闭思考」开关 + 描述推断失败可观测 + 降 max_tokens（用户『表级描述推断 160s vs LLM 27.5s，A+B+C』）**：根因——本地 Qwen3 默认 `enable_thinking=True`，`LlmClient` 请求体未带 `chat_template_kwargs` → 模型先输出 <think> 推理占满 max_tokens → content 残缺/空 → `_infer_description_structured` 静默 4 轮重试 + 路由 failover（单次推断放大到 160s，日志实证 task 6 生成 300 token 大部分为 thinking）。修复——A **思考开关（系统配置页）**：迁移 0133 `llm_config.disable_thinking`（默认关）；`LlmConfig` 模型/schema（Payload+Response）/config_service（create/update/get_effective/build_client 透传）加字段；`LlmClient(disable_thinking=True)` 请求体附加 `chat_template_kwargs: {"enable_thinking": false}`（llama.cpp server 从模板层关闭，OpenAI 远程网关忽略未知字段无副作用）；SystemConfig LLM 表单加「关闭思考」Switch（新增默认关/编辑回显/保存 payload，Switch 补 aria-label 供测试精确匹配）；B **失败可观测**：`_infer_description_structured` 每次 LlmError/parse 失败打 warning（记 attempt/fmt/max_tokens/content_len），不再静默重试无日志；C **降 max_tokens**：字段推断 200→120、表级推断 300→150（描述 20-80 字，150 token 足够，单轮耗时减半以上）。验证：后端 140 passed（+6：client disable_thinking 附加/默认不附加、config create/update 落库、build_client 透传）+ ruff 0（改动文件，顺带折行 3 处既有 timeout 长行）；前端 tsc 0 + SystemConfig 31 passed（+1 关闭思考开关保存 payload）。 | 无接口变更（schema 增可选字段 disable_thinking，DB 实例可配；env 兜底恒 False 向后兼容） | llm/collector/frontend 保持 released |
| 2026-09-02 | query-engine | feat: 查询引擎（OLAP/MySQL 降级）DB 配置化（方案 A，用户『OLAP_URL 等相关信息不能前端配置化吗』选定 A）——① 迁移 0132 query_engine_config 单行配置表 + 模型（olap_url/doris_host/port/database/user/password_enc/mysql_fallback_url_enc/enabled，仿 llm_config：Fernet 加密落库）；② QueryEngineConfigService：生效配置「DB 整行接管、env 兜底、none」三态解析（DB 行 enabled 且至少一段配置即整行接管，段空不回退 env——可精确表达「仅 MySQL 降级/关闭 OLAP」；无 DB/disabled 才回落 env）（进程 30s TTL 缓存 + 保存后失效，跨 worker 最长 30s 收敛）、save upsert（密码/URL 留空=保持原值、olap_url 自动派生 host/port/db）、连通测试（olap=TCP 探活 + basic auth 时 /api/bootstrap；mysql=真实 SELECT 1）；③ API：GET/PUT /query-engine/config + POST /query-engine/config/test（读任意登录脱敏视图+can_edit；写/测仅 platform_admin，审计 action=query_engine_config.update/test）；④ consume 引擎热配置化：OLAPExecutor 支持 basic auth（user/password → Authorization: Basic）、_execute_with_fallback 由生效配置驱动（替代只看 settings.olap_url）、执行器按配置指纹重建（host/port/db/user/password/fallback URL 变化自动关旧建新，asyncio.create_task 延迟关闭在途旧引擎）；⑤ config.py 生产校验放宽：env 未配 olap_url 不再拒启（DB 可能已配；双空由 UI 提示 + consume 降级 503/fallback），仅 env 显式配置 OLAP 时校验 doris_host 非 localhost；settings 增 doris_user/doris_password（env basic auth）；dependency_probe 经 DB 生效配置解析 OLAP 探活目标（看板不再把 DB 配置误显「未启用」）；⑥ 前端：SystemConfig 页新增「查询引擎配置」卡片（QueryEngineConfigCard：生效来源 Tag/OLAP+MySQL 摘要/状态 Alert、编辑弹窗 olap_url 或 host 二选一 + 密码留空保持 + enabled 开关、测试 OLAP/MySQL 连通、变更记录跳审计），api.ts/types.ts 加 3 API + 4 类型。验证：后端 259+8 passed（含 consume 111、config 18、query_engine 7 新增）+ ruff 0；前端 tsc 0 + SystemConfig 29 + QueryEngineConfigCard 3 新增；迁移 0132 已应用真实 DB（SHOW COLUMNS 12 列）。提交 51a08186/6668421a/5711c48f/20bb496b/38392115/33f46682/d9351867。 | 新增端点 /query-engine/config(GET/PUT)+/config/test(POST)；读写角色：读=任意登录脱敏、写/测=platform_admin；向后兼容（env 未配置时行为不变，DB 配置优先） | query-engine 新建 released |
| 2026-09-02 | deploy | fix: binlog 独立卷权限 compose 自动修复（用户『binlog 独立卷权限初始化出现的问题不能修复吗』）——根因：mysql:8.0 官方镜像 entrypoint 只 chown 数据目录 /var/lib/mysql、不处理自定义挂载点，unisense_binlog 命名卷首次创建为 root:root，mysql 用户(uid=999)无写权限 → 全新卷启动失败（errno 13 Permission denied），此前仅 README 手动 chown 绕开未根治。修复：docker-compose.yml 新增一次性 mysql-init 服务（复用 mysql:8.0 镜像、覆盖 entrypoint 为 `chown -R 999:999 /var/lib/mysql-binlog`），mysql 服务 `depends_on: mysql-init: condition: service_completed_successfully`——`docker compose up -d` 全自动（chown 幂等，卷权限已正确时立即退出、后续 up 无副作用）。README 10.3 第 1.5 步改为「compose 已自动、无需手工」，手动 chown 命令降级为裸机/非本 compose 部署的备用说明。验证：compose config 语法 OK；init 容器实跑 Exited 0；卷权限 999:999 正确；mysql 保持 running healthy 不受干扰。 | 无接口变更（部署 compose/文档） | deploy 保持 released |
| 2026-09-02 | — | deploy+docs | **生产部署流程修正：密钥生成脚本支持「模板就地填充」（用户『README 生产部署流程不清——密钥初始化与 .env.production 复制谁先谁后，先生成密钥则 .env.example 其他信息无法写入』）**：根因——`gen_prod_secrets.sh --out` 对已存在文件一律「拒绝覆盖」（幂等保护），而 README 10.2/10.3 流程第一步 `cp .env.production.example .env.production` 已创建文件 → 第二步**必然失败**（实测 exit=1）；若 `--force` 则脚本整文件覆盖（CONTENT 仅 11 项密钥），模板的 `UNISENSE_ENV/OLAP_URL/CORS/BOOTSTRAP/IMAGE_TAG` 等非密钥配置**全部丢失**——正是用户描述的「先生成密钥后其他信息无法写入」死结。另发现模板密钥区仅 9 个占位行、**缺 `UNISENSE_NEO4J_PASSWORD` 与 `UNISENSE_QUICKBI_SIGN_KEY`**（compose 默认 test1234 命中生产弱凭据黑名单，backend 将拒绝启动——模板缺失是实际部署事故源）。修复——① **脚本**：`--out` 区分三种目标状态——不存在→写全新密钥文件；**模板占位（JWT 行仍为 `<generated-by-script>`）→就地填充**（awk 以 CONTENT 为 key→value 源仅替换命中的密钥行，非密钥行/注释全保留；模板缺失的密钥项追加到文件尾兜底）；已是真实密钥→拒绝覆盖（幂等保护保留）。`is_template` 判定兼容 BSD grep（ERE 空分支 `(x|)` 在 macOS 报 empty subexpression，拆两条独立匹配）。② **模板**：补 `UNISENSE_NEO4J_PASSWORD`/`UNISENSE_QUICKBI_SIGN_KEY` 占位（11 项对齐脚本）、「9 项」→「11 项」、补 Neo4j 连接节、头部用法注释说明就地填充语义。③ **README 10.2/10.3**：脚本三模式说明区分目标状态；四步流程注释与新增「为什么必须先复制模板再执行脚本」说明；补「误先跑脚本生成纯密钥文件」的两种补救（未部署→可安全重来；已部署→grep 过滤合并模板非密钥行，密钥行不动）。验证：脚本 bash -n + 5 场景实测（不存在→全新文件 / cp 模板→就地填充 11 项+非密钥 7 行+注释 114 行保留+占位 0 / 填充后再跑→拒绝 exit=1 / --force→备份 .bak+整文件覆盖 / 旧模板缺行→追加补齐 11 项）+ 完整模拟 10.3 流程（权限 600、再跑拒绝）。提交 85205675。 | 无接口变更（部署脚本/模板/文档） | deploy 保持 released |
| 2026-09-02 | frontend | feat: 全站选项框关键词实时检索 + 防抖（循环任务『所有选项框支持关键词实时检索+防抖』）——① 35 文件 Select 统一补 showSearch（选项全量加载时输入即按中文 label 实时过滤，零请求零延迟，新增 useDebouncedValue 350ms hook）；② 6 处真远程检索 Select/AutoComplete 补齐 300ms 防抖（QualityCenter 指标下拉/Dimensions 维度映射/Glossary 关系术语/LineageView 节点/ManualEdgeModal 目标节点/MetricCompare 候选指标，timer ref 模式，onFocus 与初始加载仍即时）；③ MetricCreate 关联维度防抖远程检索由并行会话 d6635370 独立落地（排除 WIP）。验证：QualityCenter/Dimensions/Glossary/MetricCompare 84 + LineageView 48 + ManualEdgeModal 4 全过 + tsc 0。提交 a7fa8bf8 + b4d9831e。 | 无接口变更（纯前端交互增强，向后兼容） | frontend 保持 released |
| 2026-09-02 | ad9ac5fe | security+semantic+quality+lineage+governance+consume+mount+dimension+measure_catalog+subject_domain+master_data_review+frontend | **空域=不限域语义反转（方案 A，用户『domain 为 null 在前端代表不限制域，你确认前面审查的越权问题是对的吗』）**：产品确认 **domain=NULL/空 = 数据范围不限制（与角色正交）**——角色决定动作集/可见状态（viewer 只看公开+本人、domain_admin 治理含 DRAFT/REVIEW、metric_owner 管名下），domain 只决定数据范围。此前多轮（884c63c9/059ef8a2/46e0b2b4 及并行 9eef5a97）把 domain=NULL 当「无任何域权限 → fail-closed 拒绝/退化个人视角」属**误判**（把「不限域」账号的合法跨域能力封了），本次统一反转——① **读路径**：`semantic/visibility.py`（metric_visibility_conditions/metric_is_visible）与 `semantic/service.py _assert_metric_visible` 的 domain_admin 空域从「退化个人视角」改「全量治理（不加过滤）」；dimension/glossary/measure_catalog 三个 repository 列表同改；② **写路径**：dimension/measure_catalog/metric_mount 三处 `_assert_domain_scope` 与 `subject_domain._assert_domain_admin_scope` 从「空集 fail-closed 拒绝」改「有权限域才校验 ∈ domains_all、空域放行」；quality `_assert_metric_domain`（api+service）空域放行、repository `_domain_metric_condition` 空域不再返回恒假 false()；lineage `_assert_edge_domain/_assert_node_read_access` 空域放行；governance PDP `same_domain` 空集=全域匹配（domain_admin 空域可治理任意域）、grant/revoke scope 空域放行；consume issue_token 空域放行；master_data_review 评审域校验空域放行；③ **文案对齐**：Org 模型注释（user.py「无默认域需 grants 授权」→「不限域数据范围不限制」）+ 前端 UserManagement 三处文案（「无默认业务域（需经授权访问指标）」→「业务域为空=不限域」）。验证：后端 619 passed（反转 5 测试 + 新增 5 回归：PDP 空域 domain_admin 全域治理/viewer 空域仍不能 write/grant·revoke 空域放行×2/security 空域跨域写放行）+ ruff 0（改动文件）；6 个失败（semantic_cache/openapi_localize/feature_flags）经 git stash 对照为 HEAD/并行 WIP 既有，与本次零交集。遗留（over-restrict 非越权）：lineage/quality/observability 等 `user.domain` 单域收敛对**多域用户第二域**仍误拒（方向少看少操作），且通知路由 User.domain== 列匹配对多域/团队继承域用户漏发，待后续统一升级 `in_`。 | 无接口变更（读/写路径收敛语义反转，向后兼容） | security/semantic/quality/lineage/governance/consume/mount/dimension/measure_catalog/subject_domain/master_data_review/frontend 保持 released |
| 2026-09-02 | deploy | feat: 参照数据自动初始化升级为「首启零手工、迭代不重灌」——bootstrap 新增 reference 阻塞步骤（seed_marker 持久标记只初始化一次）：此前 seed-reference 是 compose profile 手动服务，仍非零手工；且若每次启动都跑 seed，其「成员指纹不一致→删旧重灌」会在迭代重建容器时覆盖业务运行期修改。修复——① 新增 seed_marker 表（迁移 0130：name/version/detail，存 DB 非 Redis，容器重建后仍在）；② seed_medical_reference 支持 is_reference_seeded/run(force=False)/owner 动态解析（查 admin 实际 id，替代硬编码 3/1，干净库自增 id 漂移不再落错人），CLI 支持 --force/env；③ bootstrap BLOCKING_STEPS/DEFAULT_STEPS 加 reference：marker 缺失（首次部署）→ 单副本守卫下执行 seed 并写标记，marker 存在（迭代重建）→ skipped already_seeded；④ compose backend 默认 steps 加 reference，seed-reference 服务降级为手动强制重灌入口（UNISENSE_SEED_REFERENCE_FORCE=1）；⑤ 测试：test_bootstrap +4（三态/编排）、新建 test_seed_medical_reference 8 项（marker/force/owner 解析）。验证——真实容器 bootstrap 首跑 reference=ok（seed 0 新增 + 写 marker，owner_id=3 动态解析），重跑 reference=skipped already_seeded（迭代重建不重灌）；迁移 0130 已应用真实 DB。影响接口：无。提交 746f382e。 |
| 2026-09-02 | 46e0b2b4 | security+search+mount+dimension+measure_catalog+subject_domain | **第四轮用户级数据可见性审查修复（用户『再次审查用户级数据可见性问题，不要放过任何细节，用真实证据支撑』）**：本轮聚焦多域权限模型（dbb49e99 团队继承∪显式指定）引入的单域残留越权——**真实 API 实证**：创建 domain=NULL 的 metric_owner 测试用户，跨域（medical_fee）创建挂载/维度/度量目录**全部 HTTP 201 成功**（对照组 e2e_owner domain=sales 同请求 403）。根因——`_assert_domain_scope`（measure_catalog/dimension/metric_mount 三模块）与 `subject_domain._assert_domain_admin_scope` 的 `and user.domain` 短路：domain=NULL（org 未绑定业务域）的 domain_admin/metric_owner 整体跳过域校验，可对**任意域**执行写操作；且 dimension/measure_catalog service 写方法无独立用户域校验（API 守卫为唯一防线）。修复——三处 `_assert_domain_scope` + `_assert_domain_admin_scope` 改为基于 `user.domains_all()` 的 fail-closed 判定（资源域必须 ∈ 权限域并集，无任何权限域即拒绝）。② **全局搜索类型错位（search.py:63）**：`visible_user_domains=user.domain`（str）传给 `list[str]` 参数 → `Metric.domain.in_("outpatient")` 按字符展开，domain_admin/reviewer 搜索域收敛失效（实测 nowner 搜索 500，aiomysql readexactly）；修复——传 `user.domains_all()`。③ **ES 全局搜索 domain_admin 无域过滤**：`_es_visibility_filter` 对 domain_admin 返回空（不过滤），ES 索引含全状态指标 → 跨域搜到他人 DRAFT/REVIEW（与 MySQL 路径收敛不一致的侧门）；修复——domain_admin 按多域并集加 domain terms 过滤（仅 metric；term 无 owner 字段且 MySQL 路径对 domain_admin 不过滤为设计，保持不收紧）。④ **semantic dashboard count_review_actionable 类型错位**：`user.domain`（str）传给 `list[str]` 参数 → 「待审核」计数失真；修复——传 `user.domains_all()`。验证：后端 86 passed（+1 空域用户跨域创建 403 回归）+ ruff 0（改动文件）；**端到端实证**——修复后空域 metric_owner 跨域写三模块全部 **403 FORBIDDEN**（修复前 201）；nowner 搜索本域 200（修复前 500）、跨域 REVIEW **0 命中**（修复前可搜到）、admin 全量视角仍命中跨域 REVIEW（未过度收紧）。遗留（over-restrict 非越权，记录）：lineage/quality/metric_stats/observability quality_events/metrics consistency_stats/recommend/governance 授权/consume issue_token/通知路由 仍单域 `user.domain`——多域用户第二域被误拒/漏判（方向为少看少操作，不泄露），待后续统一升级。 | 无接口变更（读/写路径收敛，向后兼容） | security/search/mount/dimension/measure_catalog/subject_domain 保持 released |
| 2026-09-02 | deploy | feat: 参照数据自动初始化——compose 新增 seed-reference 一次性服务（profiles: seed），`docker compose --profile seed run --rm seed-reference` 一行完成维度(52)/术语(127)/业务主题域(119)/数据字典(12 类)基础数据初始化。根因：业务参照数据此前只存在于手工脚本 seed_medical_reference.py，不挂接任何部署环节——全新生产空库 alembic upgrade + 起容器后维度/术语为空（bootstrap domains 步骤仅自动初始化标准主题域 + 12 类 system_dict）。修复：seed-reference 服务复用 backend 镜像（scripts 已 COPY 入镜像）+ 完整 environment + depends_on mysql healthy，顺序执行两个幂等 seed（seed_domains_dicts 标准域/字典兜底 + seed_medical_reference 业务主题域/术语/维度）；README 10.3 首部署补第 2.5 步说明。验证：docker compose --profile seed run --rm --pull never seed-reference 实跑通过（两 seed 均幂等 0 新增）。提交 待补。 |
| 2026-09-02 | semantic | feat: 参照数据第三轮丰富——再次丰富维度类（用户「再次丰富维度类」）：新增 16 个维度（4 公共 + 12 业务）+ 6 子域 + 16 术语。公共 4：common_age_group 年龄段 / common_gender 性别 / common_education 学历 / common_ethnicity 民族（属性类横切维度）。业务 12：inpatient_ward 病区（住院运营）、surgery_type 手术类型（一~四级）、exam_item 检查检验项目、medical_insurance_catalog 医保目录类型（甲乙丙/自费）、drg_group DRG/DIP 分组、medication_route 给药途径、drug_delivery_method 药品配送方式、online_followup_type 复诊类型、chronic_disease_type 慢病类型、remote_consult_type 远程会诊类型、vaccine_type 疫苗类型（一类/二类）、checkup_package 体检套餐。子域：outpatient 加住院、common 加 4 公共属性子域。真实 DB 落库验证：16 维度成员数与脚本一致（总 52 维度）、16 术语落库（总 127）、幂等重跑 0 新增。提交 待补。 |
| 2026-09-02 | semantic | feat: 公共维度属性丰富 + 活动维度（common_campaign）——日期/地区/组织补可计算属性（日期：财年/闰年/季节/财季/当月节假日/节气；地区：大区/城市等级/健共体试点省/互联网医院落地省；组织：持有类型/组织类型），新增活动维度（6 类型 18 活动，含对象/区域/目标指标）；seed_dimensions 成员一致性判断升级为指纹比较（code+parent+attributes），属性变更触发刷新。真实 DB 落库验证：属性全部生效、幂等重跑 0 变更。提交 bd17e560 后本次 0a7f1b9c。 |
| 2026-09-02 | — | semantic+frontend | **公共基础维度补齐（用户『维度管理里缺少日期/城市等基础公共维度』）**：新增 common 公共主题域 + common_date 日期维度（年→季→月三级层级 51 成员，SCD0）+ common_region 地区维度（省→市→区县三级 85 成员，SCD1，直辖市单节点/区县前缀防撞码）+ 5 条公共术语；seed_dimensions 成员写入支持 parent_code/path（此前硬编码 None 致层级无法落库）。真实 DB 落库验证 + 幂等重跑 0 新增。 |
| 2026-09-02 | — | semantic | **公共基础维度扩展（用户『除日期、城市还有哪些基础公共维度』，确认后实施）**：在 common 公共域下新增 **时间/组织/币种/客户** 4 个公共维度 + 4 子域 + 4 术语——common_time 时间（SCD0，时段→小时两级 32 成员，8 时段含班次/高峰属性，分钟由物理时间戳承载）；common_org 组织（SCD1，集团→分公司→部门→团队四级 43 成员，覆盖微医集团及 6 分公司，区别于临床科室）；common_currency 币种（SCD0，15 种常用币种含符号/本位币标识，用于多币种结算）；common_customer 客户（SCD1，类型→具体客户两级 26 成员，企业健管/保险/医院/政府/渠道，区别于患者）。真实 DB 落库验证（116 成员层级完整、parent 全合法）+ 幂等重跑 0 新增。 |
| 2026-09-02 | — | notify | **通知中心重复提醒 + 接收人范围收敛（用户『同一指标多次提醒；不在管辖范围的指标也收到通知；通知应和自己有关』）**：根因①——同一指标一次生命周期动作经「广播（publish_event）+ 定向（_notify_metric_stakeholders→notify_user）」两条独立通道各发一条，且广播去重只按 (user,event_type) 60s、定向通道完全不去重 → 被指派评审人收到广播+定向两条、驳回重提循环反复刷屏。修复 A1——**对象级去重**：新增 `find_recent_notification_by_object`（按 payload 业务对象键 metric_code 等精确匹配），广播/定向/recipient 三处统一走 `_find_recent`（同指标 10 分钟重复事件只保留一条，不同指标各自成条不互相挤掉），去重窗口 60s→600s；`notify_user` 去重命中返回已有通知不重复创建。根因②——订阅模型是「按事件类型广播」：任何订阅了「指标待审核」的用户会收到全平台所有指标的通知，与指标归属无关。修复 B1——**相关性收敛**：`publish_event` 对 `metric.*` 事件按事件指标收敛订阅者，仅保留指标 owner/副 owner/提交人/被指派评审人（payload 或 DB）、同域 domain_admin（新增 `list_domain_admins`）、platform_admin、主动 watch 该指标的资产订阅者，无关订阅者（如其他域 viewer 订阅了事件类型）不再收到该指标通知；非 metric.* 事件保持按订阅扇出不变；查询失败/指标不存在保守返回原订阅者不误伤。验证：test_notify_service +6（对象级去重同指标跳过/异指标各自成条/定向返回已有；收敛保留 owner+admin+watch 过滤无关/同域 admin 保留/非 metric 不收敛）+ test_notify_metric_events 适配（订阅者即 owner）+ notify 全量 104 passed + semantic 317 passed + ruff 0。 | 无接口变更（读/写路径收敛，向后兼容） | notify 保持 verified |
| 2026-09-02 | c7d5e187 | observability+consume+frontend | **核心依赖健康「只降不升」误报修复（用户『可观测中心核心依赖 1/4 正常、降级/不可用 3 个，审查原因并修复』）**：根因——`dependency_health` 表只被启动种子/熔断器事件/consume 降级写入、**无周期探针**，真实可达（Neo4j/ES/LLM 网关）却停留历史降级态（GRAPH 8-17 OPEN、LLM 8-14 HALF_OPEN、OLAP 未配置被误标 DEGRADED），看板误报「3 个降级/不可用」。修复——① 新增**周期依赖健康探针**（`app/core/dependency_probe.py`，每 60s 实测 LLM/OLAP/GRAPH/ES 写回真实状态：已配置可达→HEALTHY/CLOSED、不可达→DEGRADED、未配置→HEALTHY+meta.enabled=false 展示「未启用」不计降级；同步降级注册表），挂 `main.lifespan`；② **consume OLAP 未配置不再误标 DEGRADED**（未配置≠故障，仅已配置执行失败才写降级，避免与探针「未启用」来回覆盖）；③ overview repository 透传 `meta` 字段；④ 前端 StatusStrip 统计**排除未启用项**并独立提示「N 个未启用」、DependencyCard 显示「未启用」灰 Tag。验证：`test_dependency_probe` 5（新增）+ `test_observability_repository` 22（顺带修复 2 个 HEAD 既有 overview mock 错位——P3 性能审查后 quality 4→6 次 execute 未同步）+ consume 107 + 广回归 199 + ruff 0；前端 Observability 12（+1 未启用场景）+ tsc 0；**真实容器**——`dependency_health` 4 依赖全部 HEALTHY（OLAP 未启用 meta）、overview 返回 meta、周期探针自动刷新 last_check。 | 无接口变更（overview items 新增 meta 扩展字段，向后兼容） | observability/consume/frontend 保持 released |
| 2026-09-02 | 059ef8a2 | security+assetmap+observability+metrics+mount | **第三轮用户级数据可见性审查修复（用户『再次审查系统用户级数据可见性问题，不要放过任何细节，用真实证据支撑』）**：对 assetmap/observability 等未覆盖面做**用户级隔离**补漏（实测实证）——① **assetmap 指标搜索侧门**：`/assetmap/search` 的 `_search_metrics` 此前裸查（无 org/无 P0-3），实测 org1 metric_owner 经资产地图搜索**跨组织搜到 org2 管理员 DRAFT**（命中 2 vs 已修复的全局 /search 0）且返回完整口径字段；修复——复用 `metric_visibility_conditions` + owner 组织隔离；② **assetmap 资产汇总泄露**：`/summary` `/classification` 无 org 过滤，实测 org1 metric_owner 看到**全平台 22951 资产 + PII 合规计数 16554**（对照 tables 仅 406）；修复——按 DataSource.org_id 收敛 + 缓存键并入 org（防跨组织串读）；③ **assetmap 变更追踪泄露**：`_recent_metric_changes` 仅 org 过滤，同组织他人 DRAFT/REVIEW（含描述/口径）进变更流；修复——复用 P0-3 可见性；④ **observability 指标健康度域收敛缺失**：`/metrics/health` 对 domain_admin 恒全量（容器实证：visibility 条件数 0 vs 应有 1），违背「未绑定域退化个人视角」；修复——非 platform_admin 一律传 scope；⑤ **observability 质量事件/统计跨域**：`/quality-events` `/metrics/quality` 无域过滤，domain_admin 可跨域读他域质量事件（指标名/修复建议/处理人）；修复——按 user.domain join Metric 过滤；⑥ **consistency_stats 域收敛**：对齐 metric_stats X-2，domain_admin 强制本域；⑦ **metric_mount 详情补严**：指标物理不存在（metric is None）时挂载详情也拒绝（此前跳过可见性检查）。验证：assetmap/mount 153 + observability 20 + 广范围 224 passed（仅 2 个 HEAD 既有 overview 失败）+ ruff 0（改动文件）；新增回归 8（搜索可见性/org、汇总 org、变更可见性、质量域过滤×2、service 透传×2）。**已复核排除**：`/auth/users` 已按 org 收敛、`/tracking/stats`/audit 后端有守卫、consume api-clients 仅 platform_admin（agent 误报）；`/auth/users/by-ids` 跨组织反查为 d433df55 明确设计（仅基础字段、按已知 id、无法枚举目录），中低危记录建议不强制改。 | 无接口变更（读路径收敛，向后兼容） | security/assetmap/observability/metrics/mount 保持 released |
| 2026-09-02 | — | semantic+frontend | **指标目录模块权限审查修复（用户『DEPRECATED 指标 owner 编辑消费指南权限正确吗？结合生命周期与权限粒度审查指标目录模块所有功能』）**：① **废弃指标消费指南冻结**——后端 `update_consumption_guide` 加状态拦截（`METRIC_DEPRECATED`，DEPRECATED/DATA_SOURCE_DROPPED 不可编辑，owner 与 admin 均拦截，对齐 update_metric 废弃冻结语义；要改须先重评审恢复）；前端 ConsumptionGuide 废弃提示 + 编辑按钮隐藏。② **MetricCatalog 权限门禁补齐 6 处**——回收站「恢复」按钮补 owner/admin 校验（后端 restore 仅平台管理员或原 Owner）+ 权限点从 metric:create 对齐 metric:delete；批量废弃/批量恢复菜单按 platform_admin/domain_admin/Owner 预过滤（对齐批量删除的 owner 校验，防 metric_owner 对他人指标「可点必 403」）；回收站入口加 metric:delete 门禁；批量导入按钮权限点统一 `create || import`（消除与向导页批量注册两页分裂）；exportCsv 补漏 batch_id/health_level/has_downstream 三个筛选参数（导出=当前筛选结果）。验证：后端消费指南 15 passed（+2 DEPRECATED owner/admin 拦截）；前端 MetricCatalog 68（+1 非 owner 恢复禁用）+ tsc 0；MetricDetail 按钮门禁复核无同类缺口（删除已含 owner 校验、编辑按生命周期状态收敛）。 | 无接口变更（纯状态/权限门禁收敛） | semantic/frontend 保持 released |
| 2026-09-02 | 884c63c9 | security+search+mount+quality+collector+conflict+ai | **用户级数据可见性全量审查修复（用户『按前端所有模块梳理审查用户级数据可见性，用户只能看到自己权限范围内的数据』）**：对全模块做「汇总×明细」「API 读守卫×UI 基线」两类交叉比对后的**用户级隔离**修复——① **全局搜索 reviewer 侧门**：metric 搜索 reviewer 分支此前裸 `status==REVIEW` 无指派校验（ES/MySQL 双路径），任意评审人可搜出他域/他人未指派给自己的待审指标；修复——透传 `visible_user_domain`，MySQL 路径复用 `metric_visibility_conditions` 精确指派（user 型 reviewer_id / domain 型 reviewer_domain），reviewer 检索强制走 MySQL（ES 索引无 reviewer 指派字段，跳过 ES 防侧门）；② **metric_mount 读越权**：`GET /metric-mounts` 与 `/{id}` 此前 viewer 可读全部挂载（含他人 DRAFT 指标的源表/业务限定/责任方）；修复——列表 join Metric 按可见性过滤、详情用新增 `get_mount_with_metric` + `metric_is_visible` 校验（visibility.py 新增纯 Python 判定，与 SQL 条件同源），create/update 均拒绝「挂载域 ≠ 指标域」（防跨域挂载绕过域守卫）；③ **quality domain=None fail-open**：`_domain_metric_condition` 此前 `not domain → 不隔离`，domain=None 的 viewer/analyst 可全量读质量规则/事件/基准/对账；修复——非管理角色无域返回恒假条件（fail-closed）；④ **collector cancel 越权**：`cancel_collection_job` 此前直接 `q.cancel(job_id)` 无归属校验，任意写角色可凭 job_id 取消其他组织任务；修复——先经 `get_job_status(org_id=...)` 确认任务归属本组织；⑤ **AI 词汇表泄露**：`AiRepository.vocabulary()` 全量取指标码/名（含他人 DRAFT/REVIEW 与已删）拼入 LLM prompt；修复——仅取未删除的公开状态；⑥ **conflict escalate 归属校验**：metric_owner 此前可升级任意他人冲突；修复——`_assert_conflict_related`（管理角色放行，metric_owner 仅限冲突任一指标 Owner/副 Owner 或本人仲裁）；⑦ **quality 写路径域校验**：`/observe` 与 `/events/detect` 补 `_assert_metric_domain`（域管理员不得向跨域指标写观测/触发检测）。验证：后端 544 passed（+8：搜索 reviewer 精确指派×2、mount 跨域创建/更新拒绝×2、quality fail-closed×2、ai vocab 过滤）+ ruff 0（仅 HEAD 既有 F811）；维度/术语 reviewer 裸 REVIEW 与各自 list 端点语义一致（无指派机制，评审人看全部待审为设计），不视为越权。 | 无接口变更（读/写路径收敛，向后兼容） | security/search/mount/quality/collector/conflict/ai 保持 released |
| 2026-09-02 | — | consume+frontend | **消费快照 403 前置收敛（用户『GET /consume/metrics/outp_e2e_drugfee_day/snapshots 403 Forbidden』）**：根因——`outp_e2e_drugfee_day` 为 DEPRECATED 状态，后端按越权审查设计对非平台管理员读废弃指标快照返回 `FORBIDDEN_DEPRECATED`（正确拦截）；但前端 QualitySnapshot/AssetMap 抽屉**无脑发起快照请求**，导致浏览器控制台必现 403 网络日志 + 一次注定失败的请求（此前 `eb6699d1` 只扩展了错误码匹配，未解决「不该发请求」）。修复——前端按指标状态**前置拦截不发请求**：① QualitySnapshot 新增 `status` prop + `usePermission` 管理员判定，DRAFT/REVIEW（任何角色不可消费）与 DEPRECATED（非管理员）直接跳过 `listSnapshots`，展示「指标当前为「草稿/审核中」状态，未发布，暂无消费快照」或「指标已废弃（DEPRECATED），仅平台管理员可审计回溯历史快照」；② MetricDetail 传 `status` 给 QualitySnapshot；③ AssetMap 变更追踪抽屉与图谱指标详情 `openMetric` 改为先取 `metric.status` 再决定是否发快照请求（同样前置拦截 + 状态提示），顺带把 `Promise.all` 并行改为按需串行（少一次注定失败请求）；④ 新增 `QualitySnapshot.test.tsx` 4 用例（DEPRECATED 非管理员不发请求 / DRAFT 不发请求 / PUBLISHED 正常请求 / DEPRECATED 管理员仍请求）。验证：QualitySnapshot 4 passed + AssetMap/MetricDetail 169 passed + tsc 0。 | 无接口变更（纯前端状态感知，不发注定 403 的请求） | consume/frontend 保持 released |
| 2026-09-02 | 07eb8e83 | frontend | **移除登录页默认账号提示（用户『把首页的"本地默认账号 admin / changeme123 生产环境请使用管理员分配的凭据"文案去掉』）**：登录页表单下方 `login-hint` 提示块（App.tsx:363-370）展示默认账号 admin/changeme123 与「生产环境请使用管理员分配的凭据」——本地默认密码是开发/测试环境的敏感信息，不应对所有访问登录页的用户明文展示。修复：移除整个 `login-hint` JSX 块（含 TOTP 步骤隐藏逻辑一并删除）；同步清理 styles.css 中仅被该块引用的 `.login-hint` 样式（8 行）。验证：tsc 0；无测试断言该文案（已 grep 确认）。 | 无接口变更（纯前端文案移除） | frontend 保持 released |
| 2026-09-01 | d433df55 | semantic+auth+frontend+api | **全站用户名订正为跨组织中文名解析（用户『不只是降级文案，而是要订正逻辑，系统上展示的用户名都是中文』）**：上一轮把降级文案统一为「未知用户」仍是表面——真正的逻辑缺陷是前端用**本组织** /auth/users 列表解析 owner/责任人/评审人/操作人，跨组织用户查不到才退化为「用户 #id」/「未知用户」。本轮订正逻辑——① 后端新增 **`GET /auth/users/by-ids`**：按调用方已知的精确 id 跨组织反查用户名（不按组织过滤、仅返回基础字段 UserBrief 不暴露 email、单次最多 200 个、按 id 精确查询无法枚举目录），是「已知 id → 真实中文名」的权威解析通道；② 前端新建 **`utils/userNames.ts` 全局缓存**（ensureUserNames 批量拉取去重 + useUserNames hook 声明依赖自动解析重渲染 + userNameOf/displayUserNameOf 同步读取），单一事实来源；③ **14 个组件接入**——AssetMap OwnerTab（下拉/视图名/钻取标题）、MetricDetail Owner 责任链与挂载变体责任方、MetricCatalog 用户名列、Dimensions 责任人列、Templates 责任方、ReviewWorkbench 仲裁人、MetricReview/MasterDataReview 评审人、FeedbackCenter 用户/处理人、TrackingStats 操作用户、Governance 授权对象、Notifications 操作人、RoleOwnerSelect 已选值；每处「本组织列表兜底 + useUserNames 跨组织优先」。验证：后端 test_users_api 42（+2 by-ids 跨组织/空边界）+ ruff 0；前端 12 文件 451 passed（+1 跨组织解析回归、Notifications 回落断言改「未知用户」）+ tsc 0。影响接口：新增 GET /auth/users/by-ids（向后兼容）。 | 新增只读端点 /auth/users/by-ids（任意登录角色，按 id 精确反查最小信息） | semantic/auth/frontend/api 保持 released |
| 2026-09-01 | — | auth+users+governance+assetmap+frontend | **P2 低危观察项收尾：2FA / API 文档守卫 / 角色缓存多 worker 一致 / heatmap·changes org 隔离**——① **TOTP 双因子认证（RFC 6238）**：`core/totp.py` 纯标准库实现（官方测试向量验证），用户表加 `totp_secret`（Fernet 加密）/`totp_enabled`（迁移 0128）；登录两步——启用账号第一步返回 `totp_required` 不发令牌、`/auth/login/2fa` 密码+动态码签发（无状态挑战，防捕获码重放）；自服务 `/users/me/2fa/setup|confirm|disable`（setup 重验密码防会话劫持反锁、disable 需动态码）、管理员 `/users/{id}/2fa/reset` 强制重置（设备丢失通道）；前端登录页双因子步骤 + 个人中心双因子卡（开启/关闭）；`/auth/me` 与用户管理返回 `totp_enabled`；审计 verb 补 login_2fa_required/login_2fa_failed/setup_2fa/enable_2fa/disable_2fa/reset_2fa。② **/api-docs 守卫**：policy 注册表新增 `system:docs`（平台/域管理员基线）、`ROUTE_PERM["/api-docs"]` 登记 + `RequirePerm` 包裹路由——侧边栏与直达 URL 均按权限点过滤（此前所有用户可见）。③ **角色缓存多 worker 一致**：`governance/cache.py` 改为 Redis 版本号 + 本地 TTL 双保险——写操作 `invalidate_role_actions_cache` Redis 递增版本，各 worker ≤2s 感知并回源（此前进程内 60s TTL，多 worker 角色变更最多 60s 收敛）；Redis 不可用降级本地 TTL（fail-safe）。④ **assetmap heatmap/changes org 隔离**：`heatmap_aggregation/heatmap_matrix/recent_changes` 透传 org_id——sensitivity/catalog/drift 经 `data_source.org_id`、指标类维度/变更经 `user.org_id`（Owner 归属）过滤，缓存键并入 org 防跨组织串读（此前 analyst 可跨 org 看变更流与 PII 聚合计数）。验证：test_totp 10 + test_auth_2fa 5 + test_users_api 2FA 6 + test_governance_cache 4 + assetmap org 4 = 29 新增，相关 356+135 passed + ruff 0；前端 Account 6（+2FA 卡）、tsc 0；迁移 0128 已应用真实 DB（totp_secret/totp_enabled 列就位）。 | 无接口破坏（新增端点/字段/迁移均向后兼容；/api-docs 需 system:docs 权限属权限收紧） | auth/users/governance/assetmap/frontend 保持 released |
| 2026-09-01 | 9c7858e9 | frontend | **指标详情页删除按钮权限收紧（用户『nowner 在详情页对废弃指标仍能看到删除按钮』）**：目录页 `canDeleteMetric` 早已收紧（`2958e812` 仅 platform_admin 或 Owner），但详情页 `MetricDetail.tsx:2231` 删除按钮用的是宽泛的 `isAdmin`（**含 domain_admin**）——nowner 是 domain_admin，即使非 Owner 也命中 `isAdmin` → 对他人 DRAFT/DEPRECATED 指标显示删除按钮，与目录页语义撕裂。修复：删除按钮条件 `isAdmin ||` 收紧为 `role === "platform_admin" ||`（仅平台管理员或原 Owner，与后端 `delete_metric` 一致）；注释同步；补 domain_admin 非 Owner 对 DEPRECATED 看不到删除按钮的用例。`isAdmin` 全局定义**保留**（编辑/废弃/回滚等按钮对域管理员是合法本域管理职责，且后端 PDP 有 domain 校验，不能一并收紧）。验证：MetricDetail 97 passed（+1）+ tsc 0。 | 无接口变更（纯前端按钮可见性） | frontend 保持 released |
| 2026-09-01 | eb6699d1 | consume+frontend | **DEPRECATED 指标历史快照读取修复（用户『GET /consume/metrics/yb_e2e_settle_day/snapshots 403 Forbidden』）**：根因——`2da73c8b` 越权审查为快照读补状态闸门（对齐 execute_query：DEPRECATED 一律拒绝），但**对平台管理员也一刀切**——admin 打开质量快照页查看已废弃指标历史快照同样 403 `FORBIDDEN_DEPRECATED`，且前端 QualitySnapshot 只匹配 `code==="FORBIDDEN"` 不匹配 `FORBIDDEN_DEPRECATED` → 静默空白无降级。修复——① 后端 `list_snapshots_for_internal`：DEPRECATED 时 **platform_admin 放行**（历史快照 WORM 审计/回溯是管理员职责，PDP 已直通）、普通内部用户仍拒（防任意登录用户凭 code 读废弃指标数据），EXPERIMENTAL 灰度逻辑不变；接入方 client 通道 DEPRECATED 保持拒绝（下架即停服）；② 前端 QualitySnapshot 错误码匹配扩展为 `FORBIDDEN` 系列集合（FORBIDDEN_DOMAIN/FORBIDDEN_METRIC/FORBIDDEN_PII/FORBIDDEN_DEPRECATED）→ 无权限时正确显示「消费快照（无权限）」降级提示而非静默空白。验证：后端 test_consume_service 107 passed（+1：DEPRECATED platform_admin 放行）；前端 tsc 0；真实容器复现——admin 200（快照正常读取）、nowner 403 FORBIDDEN（PDP 拒绝，前端正确降级）。 | 无接口变更（内部用户 DEPRECATED 快照读对 platform_admin 放行，向后兼容） | consume/frontend 保持 released |
| 2026-09-01 | 48d5576a | semantic+frontend+api | **全站用户名降级文案统一为中文「未知用户」（用户『系统上展示的所有用户名必须是中文，不能出现“用户 #3”』）**：此前多页面在用户不可见时降级为「用户 #id」「责任人 #id」「#id」等内部 id 占位——① 后端 audit 列表/导出操作人（`用户 #{actor_id}`）、semantic owner_names 映射（`用户 #{owner_id_}`）改「未知用户」；② 前端 11 处降级统一「未知用户」——AuditLog×4（敏感访问聚合/导出列/操作者）、Templates 责任方、MetricDetail Owner 责任链与变体责任方、AssetMap Owner 下拉/视图名/钻取标题/两处转交与设置下拉（真实用户去 `(#id)` 后缀）、Dimensions 责任人列、Governance 权限用户、RoleOwnerSelect、MetricCatalog 用户名列与责任人筛选、Notifications 操作人 title、AuditTimeline 操作人；③ 测试断言同步（AssetMap 5 处 `管理员 (#1)` → `管理员`）。验证：前端 8 文件 374 passed + tsc 0；后端 audit/semantic/assetmap 422 passed + ruff 0（改动文件）。影响接口：无（纯展示文案，向后兼容）。 |
| 2026-09-01 | ee2686e3 | consume+semantic+frontend | **待办中心草稿按职责收敛 + 详情页跨组织 Owner 用户名兜底（用户『为什么 owner 用户在待办中心看到了他人草稿 / 详情页 Owner 显示“用户 #3”』）**：① 待办中心 draft 待办此前 `listMetrics(status=DRAFT)` 全量列出所有草稿——metric_owner 用户（e2e_owner）看到平台管理员（admin，org2）的跨域草稿，点入后详情 404/快照被 PDP 拒绝 403，断链体验。修复：平台/域管理员治理全域草稿；metric_owner/普通用户按 `owner_id=me.id` 只看自己负责的草稿（与 DSD 收敛一致）。② 详情页 Owner 责任链此前靠 `/auth/users` 解析用户名，多租户隔离下跨组织用户（org1 看 org2）不可见 → 显示「用户 #id」。修复：MetricResponse 新增 `owner_username`（best-effort 从 user 表回填 display_name||username，仅暴露单个 owner 最小信息、不枚举目录、不破坏 /auth/users 隔离），详情责任链展示真实名。验证：后端 test_semantic_service 316 passed（+1 owner_username 回填/username 兜底/查询失败降级）+ ruff 0；前端 TodoCenter 14（+2 职责收敛）+ MetricDetail 96（+1 跨组织 owner 显示）+ tsc 0；真实容器 admin 详情 owner_username=平台管理员。影响接口：MetricResponse 新增可选字段 owner_username（向后兼容）。 |
| 2026-09-01 | 6f2d5d95 | semantic+frontend | **总览仪表「指标待审核」按可审口径收敛（用户『nowner 总览看到 1 个指标待审核，进审批中心却没有』）**：总览 KPI 告警此前对管理角色（domain_admin）用 `by_status.REVIEW` 全量计数——含他人/他域指派的审批（如 reviewer_type=domain 指派给 online_consultation 域评审组，nowner domain=NULL 不可审），而审批中心「待我审」按 `_assert_reviewer_authorized` 只列当前用户真正可审的 → 仪表有数、审批中心空的断链。修复：新增 `count_review_actionable(actor_id, user_domain, role)`（与写路径评审人校验完全对齐：platform_admin=全量；domain_admin=user 型指派给我+domain 型同域+未指派兜底；reviewer=user 型+domain 型同域；其余 0），dashboard 对可审角色统一填充 `assigned_review`，前端 reviewCount 改用 `assigned_review ?? 0`——仪表与审批中心口径完全一致，只展示需要自己审批的条目。验证：后端 repository/api 74 passed（+4 actionable 口径单测）+ service 315 passed + ruff 0；前端 Dashboard 43 passed（+1 管理角色可审口径）+ tsc 0。影响接口：dashboard 响应 `assigned_review` 现覆盖管理角色（语义=可审数，向后兼容）。 |
| 2026-09-01 | — | auth+consume+collector+lineage+frontend | **越权缺口深度审查修复（产品视角·用户行为流七环节）**：① **JWT type 校验**——`get_current_user` 拒绝 `type=refresh` 令牌当 access 使用（此前 refresh 7 天可直接调业务接口，绕过 access 15 分钟短效；access/consume token 无 type 兼容）；② **consume 快照读状态闸门**——`list_snapshots_for_internal` 补 `_assert_consumable_status`（EXPERIMENTAL 灰度仅命中 gray_tenant_ids 租户、DEPRECATED 拒绝）、`list_snapshots_for_client` 同步补（接入方无租户归属一律拒灰度），对齐 execute_query 通道语义（此前 viewer/接入方可读灰度/废弃指标 WORM 快照真实数据值）；③ **collector 写路径 org 隔离**——`/{source_id}/schedule`、`/batch-schedule`、`/bulk-deprecate` 补 `_resolve_org_scope` 透传（service/repository 层 `get_source(org_id)` 强制过滤，跨组织源 404/SOURCE_NOT_FOUND，对齐 batch-toggle/batch-delete 既有语义，此前 domain_admin 可跨组织改调度/批量废弃目录）；④ **lineage 级联删边域校验**——`DELETE /edges?node=` 补 `_assert_node_read_access`（对齐单边删除 `_assert_edge_domain`，此前 metric_owner 可级联删他域节点全部血缘边）；⑤ **前端 Dimensions 提交审核按钮**补 `canSubmit` 门禁（viewer 此前可见可点、靠后端 403 兜底）。验证：deps +2（refresh 拒绝）/ consume +2（灰度·废弃快照拒绝）/ collector +3（批量调度·单源调度·批量废弃跨 org）/ 前端 Dimensions 36 passed（tsc 0）；相关 370 passed + ruff 0。 | 无接口变更（读/写路径收敛，向后兼容） | auth/consume/collector/lineage/frontend 保持 released |
| 2026-09-01 | 09bf5a64 | auth+api | **权限基线错位对齐（用户『重新审查用户鉴权机制，确保不同用户只能在自己权限范围内看到指定内容、触发指定动作』）**：此前前端权限点基线（policy.ROLE_UI_ACTIONS）与后端各 API `require_roles` 角色矩阵脱节——多类「按钮可见但 403」「有权限无按钮」「前端藏了后端不拦」错位。修复——① **policy 基线**：domain_admin 移除 org:create/edit/disable/dict:create/sensitive-rules:edit（后端仅 platform_admin/compliance 可写，消除按钮 403）；metric_owner 移除 review:close/reopen（冲突当事方不授予治理裁决）；compliance_officer 补 review:close/reopen（仲裁职责延伸）；② **query:execute 后端强制**：/consume/query 与 /dry-run 内部用户回落通道补 `_ensure_internal_query_permission`（仅 platform_admin/domain_admin/metric_owner，viewer/analyst 此前可绕过前端直调 API 全量执行指标查询，query:execute 形同虚设）；③ **读端点对齐 view 基线**：assetmap/dimension/glossary/measure_catalog/quality/notify 补 compliance_officer/analyst（assetmap 移除 reviewer/viewer——基线无 assetmap:view），ai 拆分 ai:view（页面读）与 ai:nl2sql（执行，viewer 403）；④ **写/治理收窄**：quality _WRITE/_GOV_ROLES 收窄 domain_admin/platform_admin（对齐 quality:config-rule/run-check）；notify 广播独立 `_PUBLISH_DEPS`（platform_admin/domain_admin，metric_owner 此前可绕过前端广播）、用户自助（标记已读/订阅）放开任意登录（reviewer/viewer/analyst/compliance 此前不能标记已读）；⑤ **导出收窄**：audit /export 仅 platform_admin/compliance_officer（对齐 audit:export，domain_admin 此前「有权限无按钮」）、assetmap /export.csv 仅 platform_admin/domain_admin（viewer 此前可全量导出资产清单）；⑥ **observability 反馈/NPS 放开全部登录用户**（analyst/reviewer/compliance 此前页面可点但 403）。验证：后端 1233 passed（+12 回归：consume 内部低权限 403、audit domain_admin 403、notify 广播 403/用户自助 200、policy 基线断言等）+ ruff 0；5 个失败经干净 worktree 对照均为 HEAD 既有（并行会话新增端点英文 summary 未翻译等）。 | 无接口变更（读/写端点角色矩阵收窄与对齐，向后兼容） | auth/api 保持 released |
| 2026-09-01 | 2958e812 | semantic+frontend | **指标删除权限收紧（用户『只有 owner 和平台管理员才可以删除，其他用户不能有删除按钮，只能查看』）**：此前删除放行「平台/域管理员或原 Owner」——域管理员可删任意指标、非 Owner 用户前端也显示删除入口。修复——① 后端 `service.delete_metric` 权限从「role 非 platform_admin/domain_admin 且非 Owner 拒绝」收紧为「**role 非 platform_admin 且非 Owner 拒绝**」（域管理员非 Owner 删他人指标 → FORBIDDEN），与 `restore_metric`（早已仅平台管理员或 Owner）语义对齐；② 前端 `MetricCatalog` 删除按钮（`canDeleteMetric`）移除 domain_admin 放行——域管理员非 Owner 不显示删除按钮、仅显示「仅平台管理员或创建者可删」占位；批量删除 tooltip/disabled 判断与确认弹窗文案同步收紧。验证：后端 test_semantic_service 341 passed（+1：域管理员非 Owner 删他人草稿 FORBIDDEN / 平台管理员可删他人草稿）+ ruff 0；前端 MetricCatalog 67 passed（+1：域管理员非 Owner 看不到删除按钮）+ tsc 0。 | 无接口变更（删除权限收紧，向后兼容） | semantic/frontend 保持 released |
| 2026-09-01 | 2a1daa64 | assetmap+semantic | **资产总览指标总数下钻口径对齐（用户『资产总览展示的指标总数和点击查看具体的明细数据不一致』）**：根因——总览「指标总数」= `metric_summary.by_domain` 求和（**排除 DRAFT/DEPRECATED**），而点击明细 `listMetrics` 无状态过滤（含草稿/已废弃），DB 实证 4 vs 12 不一致；另有 `page_size:100` 硬编码截断 + 明细抽屉「共 N 条」显示已加载行数（非真实总数）误导。修复——① 后端 `GET /metric-definitions` 新增 **`exclude_statuses`** 可选查询参数（**逗号分隔字符串**，如 `?exclude_statuses=DRAFT,DEPRECATED`；FastAPI 对 Pydantic 模型内 `list[str]` 字段不暴露为 query 参数，故用逗号分隔由 service 拆分为 list），repository 生成 `Metric.status NOT IN` 条件；② 前端资产总览「指标总数」下钻无 status 时传 `exclude_statuses=[DRAFT,DEPRECATED]`（与统计口径一致）+ `page_size` 100→200 + 标题改「活跃指标明细」；③ `DrillDownDrawer` 新增 `total` prop——「共 N 条」显示**后端真实总数**，已加载行数 < 总数时 Alert 提示「仅展示前 N 条（共 X 条），完整列表请到指标目录」；Heatmap/Owner 视图下钻同步透传真实 total（消除 100/200 截断误导）。验证：后端 semantic 相关 408 passed（+3：repository NOT IN 编译 / 空列表退化不加条件 / service 透传）+ ruff 0；前端 AssetMap 71 passed（+2：指标总数下钻排除状态+展示真实 total、已发布下钻不受影响）+ tsc 0。 | 接口新增可选 query 参数 exclude_statuses（向后兼容） | assetmap/semantic/frontend 保持 released |
| 2026-09-01 | b185f6a6 | frontend | **个人中心改用户视角（用户『按钮级权限点与侧边栏菜单不一致 / 不建议展示按钮级 / 不展示权限还能给用户什么』）**：① **移除按钮级权限点展示**（103+ 个 `ui_actions` 粒度远细于菜单、大量按钮动作在菜单不可见，易致「权限与菜单不一致」困惑）；② 「我的权限」改「**可访问功能模块**」——与侧边栏 `NAV_GROUPS` + `ROUTE_PERM` **同源判定**（含 /approval 聚合入口 canAny 语义），用户看到的即左侧菜单真实可见模块，所见即所得；无权限菜单不展示、无映射菜单（/api-docs）默认放行（与 Layout 菜单过滤完全一致）；资源级动作（读/写/审批/导出/复核 PDP）保留；③ 新增「**我的工作台**」——用户个人数据快照 + 快捷入口：我负责的指标（owner_id 过滤）/ 我的收藏 / 我的待办（todo_only 通知）/ 未读通知 / 我负责的数据源（owner_id 过滤），带数量与点击跳转（失败兜底「—」）——回答『不展示权限还能给用户什么』：个人待办/收藏/负责资产快照比权限点更有用。验证：前端 Account 5 passed（+1 纯函数 accessibleMenuGroups 与 ROUTE_PERM 同源判定 + 工作台数量快照 + 模块级展示/隐藏断言）+ Layout 10 + tsc 0。 | 无接口变更（纯前端） | frontend 保持 released |
| 2026-09-01 | — | observability+quality+lineage | **越权审查收尾：剩余跨组织/跨域治理数据暴露收敛**——① observability `GET /feedback` 按反馈人所属组织隔离（非 platform_admin 经 user_id→user.org_id 过滤，对齐 /overview 的 org 语义）；② quality 读路径按指标域收敛——`list_rules/list_events/list_benchmarks/list_reconciliations` join Metric 按 `Metric.domain` 过滤（非管理仅本域），`get_rule` 详情补 `_assert_metric_domain` 守卫（此前任意读角色可读任意规则详情）；③ lineage 治理统计收敛——`coverage`/`health` 加 `domain` 参数（非管理仅统计本域指标血缘完整度，表/边为全局基础设施保留全量）；④ 顺带修复 `/stale` 端点误引未定义 `node` 的 NameError（并行 X-2 误加行）。验证：observability repo 18 passed（+2 org 隔离）+ quality repo/service 新增 5 测试（domain join/管理不过滤/get_rule 域守卫）+ lineage service 新增 2 测试（coverage 域收敛/管理全量）+ ruff 0（新代码）；`test_overview_stats`×2 为 HEAD 既存失败（stash 验证与本次零交集）。 | 接口无变更（读路径收敛，向后兼容） | observability/quality/lineage 保持 released |
| 2026-09-01 | — | governance+frontend | **个人中心系统化重设计（用户『个人中心页面布局太丑 / 权限点全英文编码 / 需从系统角度重新设计』）**：① **按钮级权限点中文化**——后端 `/me/permissions` 新增 `ui_action_meta`（action/module/label/description，单一事实来源 = policy.UI_ACTION_REGISTRY；未知自定义动作降级为 action 本身 + 模块归「其他」），前端个人中心按钮权限点不再显示英文编码；② **页面系统化重设计**——顶部品牌渐变个人概览横幅（Avatar 首字 + 显示名 + @用户名 + 多角色 Tag + 组织/主题域 + 行级受限标记 + 修改密码入口）、我的账号卡（补已授权数据域）、我的权限卡（资源级动作中文 Tag + 按钮级权限点**按模块分组**渲染：模块徽标 + 计数 + 中文权限点 Tag 悬停显示说明）、我的授权卡（保留临期提醒 + 状态着色）。验证：后端 test_governance_service 77 passed（+1 ui_action_meta 中文 label/模块/描述对齐注册表、ui_actions 与 meta 一一对应、未知动作降级）+ ruff 0；前端 Account 4 + UserManagement/DataGovernance 37 passed（+3：概览横幅/权限分组计数/中文标签与降级）+ tsc 0。 | 接口新增 ui_action_meta 字段（向后兼容） | governance/frontend 保持 released |
| 2026-09-01 | — | consume+frontend | **接入方最小授权范围强制（权限审查 R8：scope_domain 与 metric_whitelist 双空 = 任意非 PII 指标全量消费权越权缺口）**：① 后端 `ClientCreateRequest` 加 `model_validator`——创建接入方时作用域与指标白名单**至少填一个**，双空 422（此前两者均可选，管理员漏配即获得超出预期的全量消费权且无提示）；② `update_client` 仅在本次请求**显式**改动授权字段（scope_domain/metric_whitelist 任一非 None）且合并后双空时拒绝（`CLIENT_SCOPE_EMPTY`，防把已有授权清成双空），纯配额修改（授权字段均未传）保留历史双空客户端（创建才强制，不破坏存量）；③ 前端 ApiClients——创建/编辑表单「域/白名单至少其一」校验（编辑对历史双空客户端放行、仅改配额时省略授权字段），列表对双空记录渲染「未配置授权范围」警告 Tag + 顶部汇总 Alert（提示尽快补充授权范围）。验证：后端 test_consume_security 18 passed（+3：创建双空 422 / 编辑显式清成双空 422 / 历史双空仅改配额 200 保留）+ ruff 0；前端 ApiClients 14 passed（+4：双空警告标记与汇总 Alert / 创建双空拦截 / 仅填白名单提交透传 / 历史双空仅改配额省略授权字段）+ tsc 0。 | 无接口变更（新增校验，向后兼容） | consume/frontend 保持 released |
| 2026-09-01 | 37afedec | semantic+frontend | **指标运营分析支持按业务域/指标类型/指标状态筛选（用户『支持按业务域、指标类型、指标状态等进行过滤筛选』）**：后端三统计端点（`/stats/reuse`、`/stats/ledger`、`/consistency/stats`）新增 `domain`/`type`/`status` 可选过滤参数——reuse/ledger 查询 Metric 时按属性过滤（统计卡与列表基于同一过滤集合）；口径一致率按「冲突至少一方属于筛选范围」收敛（总口径数按指标属性收敛、冲突计数 join 指标取 OR 范围、无过滤保持全平台向后兼容）。前端指标运营分析页新增筛选栏——业务域下拉（listDomainTree 含子域）、指标类型下拉（枚举）、指标状态下拉（枚举）、已筛选 Tag + 重置按钮；筛选变化三个端点带参重拉；口径一致率卡片标注「筛选范围」且 tips 补充筛选语义。顺带修复 `test_metric_stats_ledger` mock 未同步 health_scorer query_log 的 scalar_one 契约（HEAD 既有失败）。验证：后端 18+76 passed（+6 过滤回归：reuse/ledger SQL 含过滤条件/consistency 域收敛与 OR join/无过滤不 join）+ ruff 0；前端 MetricOps 8 passed（+2：域筛选带参重拉+已筛选 Tag+筛选范围标注、重置清空）+ tsc 0。| 接口新增可选 query 参数（向后兼容） | semantic/frontend 保持 released |
| 2026-09-01 | — | security+semantic | **越权缺口审查修复（P0-P2，用户『仔细审查是否还存在越权缺口』）**：① **逻辑度量目录行级可见性**——`list_measures`/`get_measure` 此前无可见性过滤，任意读角色（含 viewer）可列出/直读他人 DRAFT/REVIEW 逻辑度量；修复——repository `list` 加 `visible_actor_id/visible_role` 过滤（非管理角色仅公开状态 PUBLISHED/DEPRECATED + 本人负责的未发布 + reviewer 放行 REVIEW），service 新增 `get_measure_visible`/`_assert_measure_visible` 详情守卫（他人未发布按不存在处理），API 透传 user 上下文。② **全局搜索 dimension/term 分支可见性**——`search()` 已透传可见性给 metric 分支，但 `_search_dimensions`/`_search_terms` 未透传，任意用户可经搜索侧门命中他人 DRAFT/REVIEW 维度/术语；修复——两分支加 `_dimension_visibility`/`_term_visibility`（对齐各自主目录 P0-3），search 调用透传。③ **指标统计侧门**——`metric_stats reuse/ledger` 全量返回含 DRAFT/REVIEW 未发布指标 code/name/domain；修复——`_metric_filters` 加可见性条件（非管理角色仅统计公开状态 + 本人负责），API 透传 user。④ **PII 规则旁路**——`system_dict GET /{dict_type}/all` 对 ALL_ROLES 开放，`pii_rule`（PII 检测正则+置信度+类别）任意登录可读（可据正则构造规避检测样本）；修复——`pii_rule` 类型仅治理角色（platform_admin/domain_admin/compliance_officer）可读，其余类型不受影响。⑤ **sensitive_rules 读面过宽**——`_READ_DEPS`/`_REGEX_CHECK_DEPS` 用 ALL_ROLES，PII 检测规则人人可读；修复——收紧为治理角色（与前端 ROUTE_PERM `sensitive-rules:view` 的分配一致）。⑥ **维度子资源父维度可见性**——`list_members`/`list_dimension_metrics` 只校验维度存在不校验可见性，他人 DRAFT/REVIEW 维度的成员值/绑定指标可读；修复——端点先调 `get_dimension_visible` 守卫。验证：后端相关 295 passed（+16 新用例：度量列表可见性 3 场景/度量详情守卫 4 场景/搜索 dimension·term 可见性 2/统计可见性 3/pii_rule 守卫 2）+ ruff 0（新增代码，2 处 E501 为既存行）；`test_rule_flow_register` 1 项为既存失败（stash 验证零交集）。
| 2026-09-01 | 4c072aa7 | frontend | **Owner 责任分布资产构成条标注防截断（用户『数据表/看板等字样被遮挡未展示全』）**：资产构成条 `.oc-bar` 各段按占比设宽（0 值段最小占位 1.5%），段内直接渲染「标签 数量」文字，窄段（真实库 admin 名下数据表 1.4%/模板 0.7%/数据源 0）被 `overflow:hidden`+`nowrap` 裁出半字。修复——段内标注智能显示：段宽 ≥15%（约 50px，容纳 3 字标签+数量）显示「标签 数量」、≥5%（约 16px）仅显示数量、过窄段不渲染文字（保留色块 + title 悬停提示），完整标注由图例 `.oc-legend` 恒完整展示 6 类资产标签+数量（可点击下钻），杜绝半字截断。验证：前端 Dashboard 42 passed（跨资产构成条断言改为智能显示 + 图例完整性断言）+ tsc 0。 | 无接口变更（纯前端渲染） | frontend 保持 released |
| 2026-09-01 | — | security+semantic | **指标待审核可见性按评审指派收紧（用户『仪表「指标待审核」是否应只有评审权限且被 owner 指定的评审用户/组可见』）**：此前写路径（approve/reject `_assert_reviewer_authorized`）已严格按指派校验，但读路径宽松——`list_metrics` 与详情 `_assert_metric_visible` 对任何 `reviewer` 角色放行全部 REVIEW 指标，评审人可经 API 枚举/读取他人待审项（前端评审页虽有 canReview 二次过滤，属客户端隐藏、API 层过曝）。修复——① `repository.list_metrics` reviewer 分支从「任意评审角色见全部 REVIEW」改为按指派匹配（user 指派=reviewer_id 本人；domain 指派=同域评审组；未指派仅域管理员兜底），新增 `count_review_assigned` 统计指派给用户/其域评审组的待审数；② `service._assert_metric_visible` 同步收紧（user 指派本人/domain 同域放行，reviewer 对未指派按不存在处理），`get_metric_public`/`_get_metric_for_compare` 透传 `user_domain`；③ API `list_metrics`/`get_metric` 端点透传 `user_domain=user.domain`；④ `/dashboard` 对 reviewer 角色（非管理）独立查询 `assigned_review` 并入响应（复制 dict 不入聚合缓存）；⑤ 前端 Dashboard 告警按角色收敛——管理员=全量待审、评审人=指派待审（assigned_review）、普通用户不展示评审动作告警（本人审核中状态仍由 Owner 分布体现）。验证：后端 379 passed（+12：list_metrics 评审指派可见性 3 场景 / count_review_assigned / dashboard reviewer assigned_review / 详情守卫 5 场景）+ ruff 0（新增代码）；前端 Dashboard 42 passed（+2：普通用户无评审告警 / 评审人显示指派数）+ tsc 0。 | 无接口变更（dashboard 新增可选 assigned_review 字段） | semantic/security/frontend 保持 released |
| 2026-09-01 | 9b8b6952 | semantic+frontend | **指标运营分析页丰富化（用户『内容过于简单 / 翻页没有指定页码 / 不能指定展示条数 / 口径一致率统计口径没有 tips』）**：① **修复口径一致率字段接线 bug**——前端误读 `total`/`consistency_rate`/`rate`/`department_conflicts`，后端实际返回 `total_definitions`/`consistency_rate_pct`/`cross_department_conflicts`/`avg_resolve_hours`/`conflicted_metrics`/`total_conflicts`，卡片此前恒显 0/0%；补 `MetricConsistencyStats` 类型收窄返回。② 分页增强——三个表格统一 `showQuickJumper`（跳页码）+ `showSizeChanger`/`pageSizeOptions`（10/20/50/100）+ `showTotal`（共 N 条）。③ 统计口径 Tooltip——一致率 = 未卷入口径冲突指标数 ÷ 指标总数 × 100%，部门间冲突/平均解决时长口径明示。④ 内容丰富——顶部统计卡 4→6 张（+被引用/零复用，带 tooltip）；一致率改环形 Progress + 明细（卷入冲突指标/冲突记录数/部门间冲突/平均解决时长）；复用度分析 Tab 加零/低/中/高复用分桶分布；复用度列零复用红色 Tag、域 Tag。验证：前端 MetricOps 5 passed（新增测试文件）+ tsc 0。 | 无接口变更（纯前端） | semantic/frontend 保持 released |
| 2026-09-01 | — | security+frontend | **总览仪表跳转按钮权限守卫（用户『仪表对所有用户可见，但按钮点击跳转须有权限，普通用户点击应无反应』）**：Dashboard 此前大量 `navigate()` 直接跳转（资产卡片/生命周期/治理卡片/快捷入口/审批/术语表等），虽路由层有 RequirePerm 兜底，但普通用户点击会先进无权限页再被踢回，体验差且暴露入口。修复——① `usePermission.tsx` 新增 `useGuardedNavigate()`：跳转前按 `ROUTE_PERM` 检查目标路由 view 权限点，无权限**不跳转**（仅 `message.warning` 轻提示），数字参数（后退）不守卫、未登记路由（`/detail` 详情页内部鉴权）放行、快照未加载时 fail-open（后端强制兜底）；② Dashboard 两处 `useNavigate` 换用 `useGuardedNavigate`（主组件向下传给 AssetCard/GovernanceCards/OwnerDistribution 等，所有跳转点统一受控）；③ 测试 mock 改用 `importOriginal` + 可感知角色的守卫（真实 MemoryRouter location 断言）。验证：前端 Dashboard 40 passed（+3：管理角色点击资产卡跳转目录 / 普通用户点击资产卡路由不变 / 普通用户点击质量中心快捷入口路由不变）+ tsc 0；前端全量待后台结果。 | 无接口变更（纯前端交互守卫） | security/frontend 保持 released |
| 2026-09-01 | 7a6fc5c6 | notify+frontend | **强制改密期间通知角标 403（用户『unread-count 403 Forbidden』）**：根因——`GET /notify/notifications/unread-count` 挂 `require_roles(*_READ_ROLES)`，被强制改密拦截（`PASSWORD_CHANGE_REQUIRED`）——首次登录用户进入首页后 Layout 未读角标 30s 轮询持续被 403 淹没；且 `_READ_ROLES` 不含 analyst，该角色角标本就 403。修复——① 后端 `unread-count` 移除 `require_roles` 依赖（纯个人化布局数据，PLAT-2 以 user.id 硬隔离，与 `/me`、`/me/permissions` 同语义），强制改密用户/analyst 均不再 403；② 前端 `Layout.tsx` 未读角标轮询在 `forceChangeRequired` 时暂停（改密占位界面无角标意义，改密成功自动恢复），`forceChangeRequired` 声明上移至组件顶部避免 TDZ。验证：后端 notify 34 passed + ruff 0；前端 Layout 10 passed + tsc 0（Dashboard.test 为并行 WIP 错误）；容器已重建部署。 | 无接口变更 | notify/frontend 保持 released |
| 2026-09-01 | — | security+frontend | **总览仪表 Owner 责任分布数据隔离（用户『其他用户登录看到其他用户的 Owner 责任分布且可下钻他人数据』）**：根因——`/dashboard` 端点接收 `_user` 却完全未使用，`aggregate_dashboard` 聚合全组织资产返回所有 Owner 责任分布（`by_owner` 的 table/dim/term/tpl/source 子查询甚至不按 owner_id 过滤），且下钻列表/详情接口（维度/术语）无行级可见性隔离——任何登录用户可枚举他人资产分布并查看他人 DRAFT/REVIEW 私有数据。修复——① 后端 `/dashboard` 使用当前用户：管理角色（platform_admin/domain_admin）保留全量+外部筛选（治理视角）；普通用户强制 `owner_id=自己` 且忽略外部传入的 owner_id/domain 参数（防越权枚举）；② repository `by_owner` 五个子查询统一按 owner_id 过滤（修复传 owner 仍全量的隐藏 bug）；③ 维度/术语列表接口补 P0-3 读路径行级隔离（对齐指标 list_metrics：非管理角色仅可见 PUBLISHED/DEPRECATED + 本人负责的 DRAFT/REVIEW，评审人可看待审 REVIEW）；④ 维度/术语详情接口新增 `get_dimension_visible`/`get_term_visible` 守卫（未发布对他人按「不存在」处理，不泄露存在性）；⑤ 前端 Dashboard 按角色展示——非管理角色区块标题改「我的资产责任分布」+ 空状态引导（后端已隔离只剩自己）。验证：后端 263 passed（+16：API 级普通用户强制 owner/管理角色透传、维度/术语列表可见性 SQL 断言、详情守卫 4 场景）+ ruff 0（新增代码）；前端 Dashboard 37（+2：普通用户标题/空状态引导）+ tsc 0。 | 无接口变更（仅读路径强制范围） | semantic/security/frontend 保持 released |
| 2026-09-01 | — | deploy | **构建依赖源固化清华 PyPI 镜像（用户『配置清华源，避免每次构建超时，生产部署也要求配置上』）**：此前 backend/worker 镜像 pip 源默认官方 PyPI（`PIP_INDEX_URL` 空），国内构建频繁网络超时，每次需手动传 `--build-arg PIP_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple` 或设 env。本次固化——① `backend/Dockerfile` `ARG PIP_INDEX_URL` 默认值改为清华源（保留 build-arg 可覆盖，切内网源只需传参）；② `docker-compose.yml` backend/worker 两处 `PIP_INDEX_URL: ${PIP_INDEX_URL:-https://pypi.tuna.tsinghua.edu.cn/simple}`（.env 可覆盖）；③ `.env.production.example`「环境与镜像」分节新增 `PIP_INDEX_URL`（生产部署默认清华源，可改内网源）；④ README 10.2 密钥初始化章节补 `PIP_INDEX_URL` 非密钥项说明 + 默认清华源提示。验证：`docker compose config` 确认默认值注入（backend/worker 两处）；默认配置构建成功（pip 层缓存命中，与已验证的清华源路径一致）；doc_sync 通过。 | 无接口变更 | deploy 保持 released |
| 2026-09-01 | 458e4aa5 | security+frontend | **强制改密守卫失效修复（用户『首次登录须先修改初始密码 / 加载失败』）**：根因——`/auth/me` 的 `UserInfo` 模型缺 `must_change_password` 字段（登录响应 `TokenResponse` 有、`/me` 无），而前端登录流程用 `fetchCurrentUser()`（/me）取 user，导致 `user.must_change_password` 恒为 undefined → 前端强制改密弹窗/守卫永不触发，未改密用户登录后进入业务页面被后端 `PASSWORD_CHANGE_REQUIRED` 403 淹没、各页面显示「加载失败 + 首次登录须先修改初始密码」。修复——① 后端 `UserInfo` 补 `must_change_password: bool = False` 字段 + `/me` 端点回传 `bool(getattr(user,"must_change_password",False))`；② 前端 App.tsx 新增全屏强制改密守卫 `ForcePasswordChange`（must_change_password=true 时**不渲染业务路由**，仅渲染改密界面；改密成功重新 `fetchCurrentUser()`（字段已复位 false）再进入 Layout）——彻底消除未改密用户在业务页面的 403「加载失败」噪音。验证：后端 test_api_auth 15 passed（+1 `/me` 回传 true）+ ruff 0；前端 tsc 0；真实 API 端到端（backend/frontend 已重建）——强制改密用户登录 `/auth/me` 返回 `must_change_password=True`（修复前 None），8180 反代链路同样生效。 | 无接口变更（UserInfo 新增字段向后兼容） | security/frontend 保持 released |
| 2026-09-01 | 458e4aa5 | security+frontend | **强制改密守卫失效修复（用户『首次登录须先修改初始密码 / 加载失败』）**：根因——`/auth/me` 的 `UserInfo` 模型缺 `must_change_password` 字段（登录响应 `TokenResponse` 有、`/me` 无），而前端登录流程用 `fetchCurrentUser()`（/me）取 user，导致 `user.must_change_password` 恒为 undefined → 前端强制改密弹窗/守卫永不触发，未改密用户登录后进入业务页面被后端 `PASSWORD_CHANGE_REQUIRED` 403 淹没、各页面显示「加载失败 + 首次登录须先修改初始密码」。修复——① 后端 `UserInfo` 补 `must_change_password: bool = False` 字段 + `/me` 端点回传 `bool(getattr(user,"must_change_password",False))`；② 前端 App.tsx 新增全屏强制改密守卫 `ForcePasswordChange`（must_change_password=true 时**不渲染业务路由**，仅渲染改密界面；改密成功重新 `fetchCurrentUser()`（字段已复位 false）再进入 Layout）——彻底消除未改密用户在业务页面的 403「加载失败」噪音。验证：后端 test_api_auth 15 passed（+1 `/me` 回传 true）+ ruff 0；前端 tsc 0；真实 API 端到端（backend/frontend 已重建）——强制改密用户登录 `/auth/me` 返回 `must_change_password=True`（修复前 None），8180 反代链路同样生效。 | 无接口变更（UserInfo 新增字段向后兼容） | security/frontend 保持 released |
| 2026-09-01 | 9a066438 | governance+frontend | **用户级负向授权（deny）+ 侧边栏入口授权视图 + 按钮级权限覆盖修复（用户『授权弹窗已有权限为何置灰无法取消 / 应增加按侧边栏入口授权 / 按钮级权限是否覆盖全部』）**：**① 用户级负向授权**——`user_permission` 加 `effect` 列（allow/deny，迁移 0126），合并公式 `effective=(角色继承 ∪ 直挂 allow) − 直挂 deny`（deny 优先 fail-closed）；`my_permissions` ui_actions 同步减 deny；前端 UserPermModal 三态（角色项可收窄为「已禁用」、可恢复、直挂可勾选），保存透传 `deny_actions`。**② 侧边栏入口授权**——新增公共组件 `SidebarPermPanel`（渲染 NAV_GROUPS 菜单树 + ROUTE_PERM 权限点，NAV_GROUPS 自 Layout 导出），角色配置弹窗与用户授权弹窗各加「侧边栏入口」Tab——按菜单形态勾选 view 权限点，直观授权侧边栏可见性。**③ 按钮级权限覆盖修复（审查发现 P0-P3）**——P0：ConsumptionGuide 用 `metric:update` 但权限点未注册 → 改用既有 `metric:edit`（此前消费指南编辑对所有用户不可用）；P1：角色配置弹窗补 `role:edit` 守卫、ApiClients 连通性测试补 `api-clients:manage`（防越权签发令牌）；P2：十余处写操作裸奔补权限点——LineageView 治理中心 4 按钮（批量解析/扫描/导出/级联删）、MeasureCatalogs 行级恢复/编辑/提交审核/更多下拉、MetricDetail 新增挂载变体/解除挂载、MetricCatalog 行级删除/批量删除/彻底删除、Dimensions 回收站恢复/重新启用、Glossary 回收站恢复/编辑弹窗 AI 推断、Templates 新建模板（新增 `template:create` 权限点入注册表+域管理员/指标管理员基线）、DescriptionCoveragePanel 重试；P3：16 个页面路由补 `RequirePerm`（`/approval` 用 `perms` 数组 canAny 聚合；`/dashboard` 兜底页不包防死循环）。**验证**——后端 governance 相关 162 passed（+2 deny 覆盖角色继承/deny 可恢复）+ ruff 0；前端 MetricDetail 95（+挂载权限 mock 适配）、MetricCatalog 66、LineageView 48、其余改动页通过、tsc 0；真实 DB 迁移 0126 应用成功（effect 列 + deny 行持久化回滚验证）。影响接口：`PUT /users/{id}/permissions` 新增可选 `deny_actions`（向后兼容）。 | 新增可选 deny_actions（向后兼容） | governance/frontend 保持 released |
| 2026-09-01 | — | frontend | **全站操作列布局优化（用户『维度值管理/维度列表操作按钮挤在一起』）**：8 个页面操作列统一为「主操作 + 更多下拉」范式（对齐 ApiClients 先例）——① Dimensions 维度列表（7 按钮→详情/编辑/审核主操作+更多下拉，废弃/删除改 Modal.confirm）、维度成员（发布/编辑主操作+更多）、维度映射（值级映射/编辑主操作+更多）；② Glossary 术语列表（详情/编辑/审核/再次发布主操作+更多，废弃/删除补 Modal.confirm）；③ Templates（详情/实例化主操作+更多）；④ UserManagement（编辑/授权主操作+更多，禁用/重置密码 Modal.confirm）；⑤ Governance 授权列表（授权/按钮权限主操作+更多，回收补 Modal.confirm）；⑥ FeedbackCenter（采纳/驳回主操作+更多）；⑦ MeasureCatalogs 生命周期操作收进更多下拉（废弃/重新启用/删除 Modal.confirm）；⑧ QualityCenter 删除规则补 Modal.confirm。全部 Dropdown 显式 trigger=click（点击展开）；危险操作统一 Modal.confirm 二次确认。验证：9 个测试文件 168 项全绿（断言同步更新）+ tsc 0。 | 无接口变更 | frontend 保持 released |
| 2026-09-01 | 487bda83 | collector+frontend | **SQL 查询行数上限可配置——不配置=不限行数（用户『SQL查询返回行数上限，当不配置的时候是否可以支持不限行数』）**：此前 `SqlQueryRequest.limit` 默认 100/上限 500，不配置也被默认 100 截断。改为 `limit: int | None`——不传/传 null 即不限行数（SELECT 不再追加 SQL LIMIT、Python 不截断，SHOW/DESC/EXPLAIN/USE 等天然不受限），仅保留 `_MAX_QUERY_ROWS=100 万` 安全护栏防 OOM（超过截断并置 truncated + note 提示）；显式传 limit 仍按原双保险截断（追加 LIMIT + Python 截断）。前端 SQL Tab 行数上限输入框留空=不限（placeholder『不限』+ extra 说明），api.ts 不传 limit 时省略字段。验证：后端 test_collector query_sql 16（+2：limit=None 全量返回/护栏截断 note）+ 274 passed + ruff 0；前端 QueryWorkspace 21（+1：填写 50 提交带 limit）+ tsc 0；真实 Doris——不带 limit 返回 96 行 truncated=false、limit=10 返回 10 行 truncated=true、无 LIMIT UNION 全量返回。 | 无接口变更（limit 可空向后兼容） | collector/frontend 保持 released |
| 2026-09-01 | — | collector+tracking | **SQL 查询会话级当前库应用到连接 + tracking sql_query 白名单（用户『指定了数据库也显示当前库，执行 show tables 仍报未配置默认数据库』+ tracking/event 422）**：① 根因——会话级当前库仅做 AST 表名改写（_qualify_current_db），SHOW TABLES/DESC t 等无表节点语句覆盖不到，而连接每次新建、从无默认库，源端 1046 No database selected 被映射成『未配置默认数据库』（用户已 USE 却仍报错）。② 修复——query_sql 构建连接后若会话有 current_db，先在连接上执行安全的 USE（_safe_use_sql：纯标识符裸拼接、其余反引号包裹转义防注入），SHOW/DESC/CHECKSUM 等依赖默认库的语句全部可用，与 AST 改写双保险；③ tracking——QueryWorkspace SQL 查询埋点 sql_query 事件补入 _ALLOWED_EVENT_TYPES（此前 422 非法事件类型）。验证：test_collector query_sql 14（+2：SHOW TABLES 连接先 USE/安全库名转义）+ test_tracking_validation 6 + ruff 0；真实 Doris 端到端 8180：USE ssb 后 SHOW TABLES 200 返回 6 表、裸表名 SELECT 自动补前缀返回真实客户数据、tracking sql_query 200。 | 无接口变更 | collector/tracking 保持 released |
| 2026-09-01 | 79c325a7 | collector+frontend | **数据源 SQL 查询黑名单制 + 会话级当前库（用户『先 SHOW DATABASES → USE 切库 → 只写表名查询是否支持 / 为什么设计那么复杂，只要不是 DML/DDL 就放行』）**：① 黑名单制校验——不再枚举『允许哪些语句』，只拒绝写操作/状态变更/行锁/写出/危险函数/多语句（_validate_read_sql 重构：整棵 AST 危险节点扫描 + Command 命令黑名单 OPTIMIZE/ANALYZE/REPAIR/CALL/PREPARE/EXECUTE/DO/HANDLER/LOCK/UNLOCK/FLUSH/RENAME/GRANT 等 + 危险函数 SLEEP/GET_LOCK/LOAD_FILE + 解析失败走原始文本危险词/多语句扫描后放行），任何非 DDL/DML 只读语句（SELECT/SHOW/DESC/EXPLAIN/USE/HELP/CHECKSUM/CHECK 等）一律放行，语法正确性交给执行端（源端错误已映射 422）；SHOW 在 mysql 方言是 Show 类型、EXPLAIN 是 Describe(this=内层语句)，内层 DML 经 walk 命中危险节点即拒，无需递归。② 会话级当前库——USE <db> 写入 Redis（key=unisense:sqlquery:db:{user}:{source}，TTL 30 分钟，不实际执行），无库前缀表名经 sqlglot AST transform 自动补当前库前缀（多表 JOIN/子查询/DESC/反引号保留字全覆盖，已有前缀与 SHOW TABLES FROM db 不改写）——支持『SHOW DATABASES → USE db → SELECT * FROM t』三步场景；返回 current_db/note 字段。③ 前端——SQL Tab『当前库』Tag（可清除）+ SHOW DATABASES/SHOW SCHEMAS 结果首列库名可点击快速切库（发 USE `db`）+ placeholder/Alert 文案同步。验证：后端 test_collector 235（+4：USE 写会话不执行/裸表名自动补前缀含 JOIN/DESC/已前缀不改写）+ test_collector_api 35 + ruff 0；前端 QueryWorkspace 20（+2：USE 切库展示当前库 Tag/SHOW 结果点击切库）+ tsc 0。影响接口：新增 SqlQueryResponse.current_db/note（向后兼容）。
| 2026-09-01 | — | dimension+collector+frontend | **维度『从表自动获取』弹窗级联 + Hive 列加载 500 修复 + 表列关键词实时限定（用户『为什么选择 Hive 数据源加载表那么慢』『source-columns 422』『输入关键词没有实时限定搜索』）**：① **级联选表**——后端新增 `POST /dimensions/source-databases`（轻量库列表）+ `source-tables` 透传 `databases` 参数（选库后仅枚举该库表）；前端弹窗改「数据源→目标库→表名」三级（Hive 53 库 22540 表从全量枚举 26s 收敛为选库后 <1s，绑定回填按 source_table 拆库预填）。② **Hive/Spark 列加载 500 根因修复**——`HiveCollector` 未实现 `Connector.query`，`list_source_columns` hive 分支调 `collector.query` 直接 `AttributeError`→500；新增公开 `list_columns`（DESCRIBE 解析，反引号安全标识符，跳过分区头行）。③ **关键词实时限定**——表名/列名下拉输入关键词即时过滤 options（此前 `filterOption=false` 2 万条全渲染）。验证：后端 test_dimension_service +2（databases 透传/list_source_databases）+ test_connectors +1（hive list_columns）+ 前端 Dimensions 36（级联断言 `listSourceTables("s1",["dwd"])`）+ tsc 0 + ruff 0。 | 新增 source-databases；source-tables 加 databases 参数（向后兼容） | dimension/collector 保持 released |
| 2026-09-01 | — | collector | **数据源 SQL 查询『未选默认库』友好提示（用户报 422）**：根因——`mysql_unisense`（实为 Doris，192.168.99.12:9030）`connection_config` 无 `database` 字段，连接无默认库，裸表名 `SELECT * FROM dwd_order_di` 被源端拒 `1046 No database selected`，`query_sql` 异常分支直接透出 pymysql 原始英文（对用户如天书）。修复——`query_sql` 捕获 1046/No database selected 时给出中文操作指导（『请使用 库名.表名 限定，或先单独执行 USE 库名』），其余源端拒绝仍透出原因。验证——后端 test_collector query_sql 9（+1：1046 友好提示断言不含原始英文）+ 全量 collector 231 passed（1 项 `test_test_connection_allow_private_default_false` 为 HEAD 既存失败，stash 验证与本次零交集）+ ruff 0；真实 Doris 端到端——裸表名 422 但 message 变为可操作中文指导。影响接口：无（仅错误文案）。 | 无接口变更 | collector 保持 released |
| 2026-09-01 | f9550dfc | collector+frontend | **数据源 SQL 查询支持所有非 DDL/DML 只读语句（用户『应该支持不同类型的非DDL和非DML操作的所有查询语句』）**：后端 `_validate_read_sql` 重构为「AST 层 + 原始文本兜底」双层——① AST 层：Select（含 WITH/子查询）/ Describe / Use 直达放行；Command 白名单（SHOW/EXPLAIN，EXPLAIN 内层语句递归校验——EXPLAIN ANALYZE 仅放行 SELECT，EXPLAIN INSERT/DELETE 拒绝）；整树危险节点拒绝（Insert/Update/Delete/Create/Alter/Drop/TruncateTable/Grant/Merge/Set/Kill/Transaction/Commit/Rollback/Lock(FOR UPDATE/FOR SHARE)/Into）；危险函数拒绝（SLEEP/GET_LOCK/LOAD_FILE/BENCHMARK）。② 原始文本兜底（sqlglot 无法解析但确属只读的命令 HELP/CHECKSUM/CHECK 等）：首词白名单 + 去注释/字符串/反引号后危险词与多语句扫描。③ 反引号保留字表名（如 `update`）默认方言解析失败回落 mysql 方言。④ 韧性——SqlalchemyConnector.query 对 returns_rows=False 语句（USE 等）优雅返回空而非 500；源端拒绝（OperationalError/ProgrammingError，如 Doris 不支持 CHECKSUM）映射 422 透出源端原因。验证：后端 test_collector query_sql 8（+3）+ 全量 265 passed + ruff 0；前端 QueryWorkspace 18 + tsc 0；真实 Doris 源端到端——SHOW DATABASES/DESC/EXPLAIN SELECT 200、USE 优雅空结果、CHECKSUM 422 透出Doris 语法原因、FOR UPDATE/EXPLAIN DELETE 422 拒绝。影响接口：无（契约不变，仅放宽语句类型支持 + 错误映射修正）。 | 无接口变更 | collector 保持 released |
| 2026-09-01 | — | collector | **全连接器列表枚举审查修复（用户『审查看其他数据源是否也有同样的问题』）**：审查 9 类连接器 list_databases/list_tables——① **postgres/clickhouse 补全列表方法**（此前未覆盖，维度『从表自动获取』弹窗走基类默认返回空 → 永远『无可选表』）：postgres 复用 _list_schemas + information_schema（单条 IN 批量）；clickhouse 复用 system.databases/system.tables（排除 system/information_schema/default，非法库名跳过）；② **mysql/doris/starrocks InformationSchemaCollector.list_tables 批量优化**（逐库串行 → 单条 `table_schema IN (...)` 一次往返，复用连接池）；③ spark 继承 HiveCollector 自动继承上轮单连接复用修复（无需改）；kafka 无表概念（基类默认空符合预期）。验证：test_connectors 58（+8：mysql 批量分组/空库、postgres 枚举 schema/批量分组/回退配置 schema、clickhouse 排除系统库/分组/回退连接库）+ test_collector list_tables 适配 + ruff 0。 | 无接口变更 | collector 保持 released |
| 2026-09-01 | — | consume+seed | **E2E 门诊演示指标查询 503/400 根治**：根因——`outp_e2e_*` 指标只注册目录元数据、从未建物理宽表（consume 按 metric_code 宽表模型查询），且原子指标口径用 `source_tables`（复数）而 consume 契约读 `source_table`（单数）。修复——① `seed_e2e_data` 新增 `seed_outp_wide_tables` 在降级引擎建 3 张宽表+造数（幂等），发布前调用；② 修正 3 个指标 spec 补 `source_table` 单数键 + 派生挂载指向宽表；③ 清理并行会话遗留脏挂载 `dwd.verify_owner`。验证——三指标 consume/query 200（8/8/4 行）、seed 幂等重跑、语法/API 实测通过。 | 无接口变更 | consume/seed 保持 released |
| 2026-09-01 | 6c561bff | frontend | **查询工作台自定义日期范围中文展示（用户『自定义日期范围应支持中文日期，而非 Sep/Oct 英文』）**：**根因**——`main.tsx` 仅配了 antd `ConfigProvider locale={zhCN}`，但 dayjs 实例未配置中文 locale（antd 文档明确：ConfigProvider 只管组件文案，DatePicker/RangePicker 日历面板月份标题由 dayjs 生成），面板显示英文月份（September/October）。**修复**——① `main.tsx` 加 `import 'dayjs/locale/zh-cn'` + `dayjs.locale('zh-cn')`（全局根治，影响全部 DatePicker/RangePicker 面板为中文）；② 查询工作台 RangePicker 显式 `format="YYYY-MM-DD"`（输入框恒为数字格式，不依赖 locale 默认）；③ 测试 setup 同步 dayjs zh-cn（测试环境与生产行为一致）。**验证**——QueryWorkspace 18（+2：输入框 value 断言 `2026-08-01`/`2026-08-15` 数字格式）+ CollectionHistory/TrackingStats/Observability 26 全绿无回归；tsc 0。 | 无接口变更 | frontend 保持 released |
| 2026-09-01 | e9f8574b | [collector] feat: 数据源 SQL 查询支持只读语句白名单（SELECT/SHOW/DESC/EXPLAIN），拒绝 DDL/DML/多语句/状态变更；LIMIT 仅对 SELECT 追加；前端文案同步 | e9f8574b |
| 2026-09-01 | — | collector+dimension+frontend | **Hive 级联选表慢根治 + 加载态文案（用户『指定 hive 数据源后一开始提示无可选表，过了很大一会才有选项值』）**：根因——HiveCollector.list_tables 对每个库新建 pyhive 连接串行 SHOW TABLES（N 库 = N 次 TCP+SASL+session 握手），库多时几十秒到几分钟；前端表/列 Select 的 notFoundContent 固定「无可选表」在 loading 期间也显示误导。修复——① 后端 list_tables 改单连接复用枚举全部库（1 次握手替代 N 次）+ 整体兜底超时 min(query_timeout,30)s（超时抛 ExternalDependencyError，前端降级手动输入）；② 前端两处表名 Select notFoundContent 条件化（loading 时「正在加载表…」）、两处列名 Select 同步（「正在加载列…」）。验证——test_connectors hive 19（+2：单连接复用断言仅 1 次 _connect_managed + 整体超时抛错）+ ruff 0；前端 Dimensions 36 + tsc 0。提交 待补。 | 无接口变更 | collector/dimension/frontend 保持 released |
| 2026-09-01 | — | 全模块 | **生产级全方位审查修复（业务闭环/安全/性能/前端交互，11 项提交全部落地）**：① 安全——S1 质量规则/事件/对账指标域归属校验下沉 service 层（update/delete_rule、ack/resolve/close、run/confirm_reconciliation 此前仅角色校验可跨域写，现 fail-closed 本域）；S2 血缘扫描根目录固化（scan_directory 强制位于 UNISENSE_LINEAGE_SCAN_DIR，拒绝自选任意路径）；S3 审计 WORM 触发器（迁移 0122：未归档行禁 DELETE/核心字段禁 UPDATE，归档搬迁是唯一删除路径，真实 DB 验证）；S5 users/organizations LIKE 通配符转义；S8 接入方 secret 强度校验+72 字节上限；S9 refresh 补组织停用校验；② 业务闭环——B1 EXPERIMENTAL 灰度指标消费通道（gray_tenant_ids 命中 org 放行，修复试点空转）；B2 PENDING_VERSION 确认人经 query_log 反查真实消费方+Owner 兜底；B3 create_client 重复 client_id 预检+IntegrityError 转 409；B4 挂载端点状态守卫（DEPRECATED/DATA_SOURCE_DROPPED 禁改删，REVIEW 破坏性须撤回）；B5 REVIEW 编辑撤回发事件+通知评审人；B7 日落归档任务（check_sunset_expiry 软删超期 DEPRECATED）；B8 conflict escalate 失败回滚；③ 性能——P1 api_stats 30 天窗口、P2 dashboard 聚合 30s 缓存（公共 agg_cache）、P3 健康度 SQL 聚合、P5 overview 缓存、P10 角色权限进程内缓存、P11 推荐行为 90 天窗口+复合索引（0123/0124）、P12 query_log 复合索引；④ 前端——F1 主数据审批服务端分页、F2 审计深链参数直达、F3 待办计数走后端 total+超限提示、F4 可观测中心错误态、F5 全局搜索查看该类全部、F6 质量/维度下拉服务端搜索、F7 向导/编辑弹窗未保存离开保护；⑤ 测试基建——quality/lineage/observability/consume security 等 mock user 补 roles_all（既有 403 被 ctx 序列化吞成 500）、recommend/notify 测试 mock 方法名对齐、审计 actor_id NULL 断言对齐 X-4。验证——后端受影响 879 passed + ruff 0；前端 MetricCreate 85/Dashboard 35/AuditLog/SystemConfig/Observability/QualityCenter/Dimensions/MasterDataReview/TodoCenter/MetricDetail 全绿；真实 DB 审计触发器/聚合 SQL 验证。提交 4479c0cf/effc0ed4/85bab1e1/f2bf6b28/5e9c413f/375107ff/81d60752/e6487090/f6b84534/10707dfd/bec2a9f8。 | 无接口变更 | 各模块保持 released |
| 2026-09-01 | — | 全模块 | **全维度生产就绪审查修复（44 项 P0/P1/P2 全部落地）**：① 安全——S1 数据源全链路 org 租户隔离（repository/API 透传）、S2 JWT 黑名单 fail-closed、S3 接入方令牌签发属主校验+org_id 语义修正、S6/S7 密钥链二次加密、S9 登录账号维度锁定、B4 注入守卫业务文本豁免、S10 去掉硬编码 X-Api-Key；② 容错——R1/R3/R4 事件投递移出请求路径+后台任务强引用、R2 Redis 客户端超时、R5 DLQ Redis 持久化、R6 降级注册表激活、R7 冲突 LLM 降级标注、R10-R12 三处静默 pass 改告警、B6 FK_CHECKS 恢复回滚；③ 性能——P1 ES 分批+Bulk、P2 血缘分层 BFS、P3 扫描 to_thread、P4 metric FULLTEXT（迁移 0124）、P5 versions lazy=select、P6 质量批量取观测；④ 业务闭环——B1 ES 索引自动同步+delete、B2 purge_metric 级联补齐、B3 /publish 门禁收紧、S4/S5 审计 WORM 一致化+ip；⑤ 前端——401 重放保留 abort、2xx 非 JSON 兜底、采集任务页隐藏暂停轮询、S8 组织 LIKE 转义（此前已修）；修复 S1 引入的 _resolve_org_scope 无限递归。验证——后端全量测试通过（多次提交 0f-harden 系列）、迁移 0124 已应用真实 DB、前端 tsc 0 错。提交：adf9554e/32bc9950/21234448/452b7016/77c57c3d/ccf211c2/4604944e/110f1dbd/32df9a94/56ef26b6/87fcb15f/38f73d7a/5ba9e949/9a8f6494 等。 | 无接口变更 | 各模块保持 released |
| 2026-09-01 | — | collector | **SQL 查询/存量探活被 SSRF 误拦截修复（用户『查询工作台指定数据源写 SQL 查询』实机验证）**：根因——service.query_sql 直接调 registry.build（allow_private 默认 False），而数据源主机为内网 IP（192.168.x.x），SSRF 防护在连库前拦截内网目标 → 已落库数据源上执行 SELECT 报 SSRF_TARGET_FORBIDDEN；同文件 check_connection 虽显式传 allow_private=True 但写法不一致。修复——query_sql/check_connection 统一复用 spi.build_collector（封装『已落库数据源放行内网、仍拒回环/链路本地』语义，与采样/采集链路一致）；build_collector 提升为 service 模块级导入（消除函数内惰性导入、统一测试 patch 目标）；顺带修复 test_collector 批量替换误伤 build_from_cfg 测试。验证——test_collector 228 passed + ruff 0；真实容器：SELECT 1 → OK + 结果（不再 SSRF/500）、多语句仍 VALIDATION_ERROR 拒绝。提交 e51c3e77。影响接口：无（仅修复已落库内网源执行 SQL 的拦截）。 | 无接口变更 | collector 保持 verified |
| 2026-09-01 | — | dimension+frontend | **维度『从表自动获取/绑定表列』源库表列选项框 SSRF 400 修复（用户『POST /dimensions/source-tables 400』）**：**根因**——上一轮新增的 `list_source_tables/list_source_columns/_fetch_column_values/_count_null_stats/preview_column_values` 五处 `registry.build(src.source_type, src.connection_config)` 未传 `allow_private`（默认 False），而数据源主机为内网 IP（192.168.x.x），SSRF 防护在连库前拦截 → `SSRF_TARGET_FORBIDDEN` 400；采集链路 `collector/service.py` 显式传 `allow_private=self._settings.collector_allow_private`（`UNISENSE_COLLECTOR_ALLOW_PRIVATE`）放行已落库内网源，新端点未对齐。**修复**——5 处 `registry.build` 统一加 `allow_private=self._settings.collector_allow_private`（仅作用于已落库数据源，符合 ssrf.py『allow_private 仅用于已落库数据源采集/枚举』安全语义，跟随全局开关）。**验证**——`test_dimension_service` 102 passed + ruff 0（5 处 E501 已拆多行）；真实后端容器实测内网源 source-tables 200。影响接口：无（仅放行已落库内网源的枚举请求）。 | 无接口变更 | dimension 保持 verified |
| 2026-09-01 | — | dimension+frontend | **维度『从表自动获取』与『绑定表列』弹窗源库表/列选项框化（用户『指定数据源后表名应以选项框选择，指定表后列以选项框选择』）**：**根因**——两个弹窗的表名/列名是自由输入 Input（仅数据源为 Select），用户需手写 `库.表`/列名，易错且无法浏览源库有哪些表/列。**修复**——① 后端新增 `POST /dimensions/source-tables`（复用连接器 `list_tables` 按库分组展开为 `库.表`，连接器不支持时返回空）与 `POST /dimensions/source-columns`（按数据源类型分派：MySQL/PostgreSQL/StarRocks/Doris/ClickHouse 走 `information_schema.columns` 按 ordinal_position 排序、Hive/Spark 走 `DESCRIBE` 跳过分区头行；表名先经标识符白名单校验防注入，非法表名在连库前拦截）；② 前端两个弹窗表名/列名 Input → **Select 选项框**（选数据源后自动加载表、选表后自动加载列并清空旧列），带「手动输入」兜底（`withManualOption`：输入未在列表中的库.表/列名可选中，连接器不支持/失败不阻断，对齐指标挂载的未采集兜底模式）；绑定弹窗打开已绑定维度时自动预加载已绑定的表/列选项。**验证**——后端 `test_dimension_service` 102（+4：表按库分组展开/MySQL information_schema 列列举/Hive DESCRIBE 跳过分区头/非法表名拦截不连库）+ ruff 0；前端 Dimensions 36 passed（+2：从表自动获取选数据源→表选项框→选表→列选项框显示 `列名 (类型)` / 绑定弹窗打开预加载已绑定表列）；前端全量回归。提交 c7378d7a。影响接口：新增 2 个端点（向后兼容） | 新增端点（向后兼容） | dimension/frontend 保持 verified/released |
| 2026-09-01 | — | semantic | **MetricListParams.page_size 上限 100→200（用户『GET /metric-definitions?status=PUBLISHED&page=1&page_size=200 422』）**：根因——后端 MetricListParams.page_size 上限 le=100，是平台各列表端点唯一例外（collector/conflict/governance/lineage/assetmap/dimension 均 le=200），前端 ApiClients 以 page_size=200 拉取 PUBLISHED 指标列表被 schema 校验 422。修复——上限放宽到 200 对齐平台各列表端点。验证——新增回归测试（page_size=200 通过 / 201 拒绝）；真实容器验证 200→200、201→422，PUBLISHED 指标 3 条正常返回。提交 782353f3。影响接口：无（仅放宽校验上限，向后兼容） | 无接口变更 | semantic 保持 verified |
| 2026-09-01 | — | consume+frontend | **日期区间分隔符前后端契约统一（用户『POST /consume/query 400 日期区间格式非法』）**：根因——后端 _build_query_sql 按 `~` 分割 date_range（schema 描述 2026-01~2026-03），而前端 translateDateRange 与 ApiClients 示例用 `,`（YYYY-MM-DD,YYYY-MM-DD）——前端发逗号串 → 后端按 ~ 分割得到单段含逗号 → _validate_date_part 正则不匹配 → 400。修复——① 后端 re.split(r"[~,]") 兼容两种分隔符（外部消费方 API 为长期契约，两种写法都接受），段数 >2 / 非日期内容仍拒绝（宽松分隔符不放松格式校验）；② 前端 translateDateRange 统一 `~`，ApiClients dry-run 与 curl 示例同步。验证——后端 test_consume_service 99（+2：双分隔符通过/非法格式拒绝）+ ruff 0；前端 QueryWorkspace 16 + ApiClients 9 + tsc 0；真实容器逗号格式不再报日期区间非法（进入业务层）。提交 12c9ec04。影响接口：无（放宽输入格式，向后兼容） | 无接口变更 | consume/frontend 保持 released |
| 2026-09-01 | — | frontend | **业务域分布中文标签 + AI 助手未开放说明（用户『业务域分布标签应是中文而非英文编码 / AI 助手目前还不能供用户使用，需在前端说明』）**：**① 业务域中文化**——总览仪表「业务域分布」饼图与「推荐术语」域 Tag 原直接展示英文编码（outpatient/medical_fee 等）；Dashboard 挂载时拉取 `listDomainTree()`（主题域树）扁平化为 code→中文名映射（`门诊`/`医疗费用` 等），`DomainChart` 与术语 Tag 用映射展示中文，未知域回退英文编码、域列表失败静默降级不阻断仪表盘。**② AI 助手未开放说明**——AI 助手当前处于内测阶段不面向用户：页面顶部加「AI 助手暂未开放使用」warning Alert（说明正式开放后将支持自然语言转 SQL），输入框/指标范围/生成按钮禁用（`AI_AVAILABLE` 产品开关常量，开放后置 true 即恢复，叠加 `ai:nl2sql` 权限点）；示例 Tag 未开放时不可点击；Layout 菜单标注「AI 助手（内测中）」、Dashboard 快捷入口 desc 改「内测中，暂未开放使用」。**验证**——前端 Dashboard 35（+1：listDomainTree 映射后术语域 Tag 显示中文、编码不出现）+ AiAssistant 7（重写为未开放：说明展示/输入与按钮禁用/点击不调用接口/返回逻辑保留；未开放压过权限点）+ Layout 10 全绿；tsc 0（仅并行 WIP ApiClients.tsx 2 个既有错误，与本次零交集）。提交 460eda26。影响接口：无（纯前端渲染层） | 无接口变更 | frontend 保持 released |
| 2026-09-01 | — | consume+frontend | **查询工作台『指标查询』模式误报接入方域 403 修复 + 双入口可视化（用户『指标查询仍提示指标不在接入方授权域内 / 双模式不明显』）**：**① 403 根因**——前端 consumeAuth 分支只要有消费令牌（localStorage 残留，如 e2e_app）就优先带 Bearer，后端 get_consume_or_internal_user 将其判为接入方通道 → 走 scope_domain 授权域收敛（e2e_app 为 sales 域）→ 查询其它域指标必 FORBIDDEN_DOMAIN；『指标查询』模式虽走用户 JWT 但仍被残留令牌劫持。**② 修复**——api.ts 新增 consumeForceUser 选项（consumeAuth 下强制忽略消费令牌、直接用登录用户 JWT），consumeDryRun/consumeQuery/consumeSemantic/listSnapshots 加可选 forceUser 透传；QueryWorkspace 三 handler + 快照在『指标查询』模式传 forceUser=true（内部用户视角，后端走 PDP 数据权限通道），『消费接入调试』模式传 forceUser=false（保留接入方令牌语义）。**③ 双入口可视化**——Segmented 加图标（SearchOutlined/ApiOutlined）+ 当前模式 Tag（内部用户·免令牌/模拟接入方）+ 动态说明文字。**验证**——QueryWorkspace 16（+1：query 模式 forceUser=true、切换 debug 后 forceUser=false）+ apiConsumeToken 3 + tsc 0；前端镜像干净快照重建生效。提交 aabdddfa。影响接口：无（纯前端，API 契约零变化）。 | 无接口变更 | consume/frontend 保持 released |
| 2026-09-01 | — | semantic+recommend | **总览仪表治理读数对齐未关闭口径 + Owner 分布域收敛 + 推荐术语域收敛（延续审查『工作台总览仪表数据是否都正确、有无 mock 或计算不准确』）**：**① 质量健康大数字误导**——`quality.total/by_severity` 原统计**全部**质量事件（含 RESOLVED/CLOSED），真实库 15 条中 14 条已关闭 → 仪表盘显示「质量健康 15」但 sub「待处理 1 项」、与可观测中心 `pending_quality`（OPEN+ACK=1）口径撕裂；改为 `by_severity` 仅统计未关闭事件（status IN OPEN/ACK），`total` 自动等于待处理数。**② 冲突风险大数字误导**——`conflict.total` 原 `sum(全部状态)` 把 RULED/CLOSED 计入，真实库 6 条冲突全 CLOSED → 显示「未关闭冲突总数 6」实际未关闭 0；改为只统计 OPEN+NEGOTIATING+ESCALATED（与可观测中心 `open_conflicts` 同口径），`by_status` 保留全量分布。**③ Owner 分布口径撕裂**——`aggregate_dashboard?domain=X` 时仅 owner_metric 带 domain 过滤，owner_table/dim/term/tpl/source 5 查询与 by_domain 仍全库统计（同一张 Owner 卡内指标按 X 域、其它资产按全库）；补 domain 过滤：DBCatalog 经 join `data_source.source_id` 取域、Dimension/Term/MetricTemplate/DataSource 直接按 `domain` 列收敛，by_domain 同步收敛。**④ 推荐术语跨域泄漏（P1-5 遗漏）**——`published_terms` 仅过滤 PUBLISHED 无 domain、无 deleted_at；补 `domain` 参数（对齐 `recommend_metrics` 的 `_recommend_domain`，非 platform_admin 仅本域）+ `deleted_at IS NULL`，service/API 透传。**验证**——后端 `test_semantic_repository` 44（+1：domain 贯穿 8 查询 SQL 断言，口径断言 15→1/6→0 同步）、recommend 三文件 40（+4：术语 domain 过滤/不限域保留软删/service 透传/API 非管理员传域·admin 不限域）；修复既有失败 `test_recommend_metrics_uses_authenticated_user_id`（mock 缺 domain 致 `_recommend_domain` 返回 MagicMock）；真实 DB：quality 15→1（仅 1 条 ACK 待处理）、conflict 6→0（全 CLOSED）、`domain='outpatient'` 聚合 total=3 且 Owner 卡指标 3/12 收敛、`published_terms(domain='patient_service')` 仅返本域。提交 4db566ca。影响接口：无（纯聚合口径修正，字段结构不变） | 无接口变更 | semantic/recommend 保持 released |
| 2026-09-01 | — | consume+frontend | **API 客户端管理闭环补齐（用户『403 指标不在授权域内 / 缺编辑停用启用删除与批量操作』）**：**① 403 根因**——创建表单 scope_domain 是自由文本输入，填的值与指标实际域不匹配即触发 `_assert_authorized` fail-closed（FORBIDDEN_DOMAIN），且无编辑入口无法修正（重建会换密钥）。**② 后端 4 个管理端点**——`PUT /consume/api-clients/{id}` 编辑（scope_domain 空串清空为不限域/白名单空数组清空为域内全量/qps/daily_quota，secret 不可改，None 部分更新语义）、`PATCH /consume/api-clients/{id}/status` 停用/启用（REVOKED 后 X-Api-Key 与已签短效令牌即失效）、`DELETE /consume/api-clients/{id}` 软删（deleted_at + REVOKED 保留审计追溯）、`POST /consume/api-clients/batch` 批量 enable/disable/delete（逐条容错 BatchResponse 语义），全部仅 platform_admin/domain_admin + 审计。**③ 前端**——操作列加编辑/停用·启用/删除（Popconfirm）；批量勾选 + 批量操作菜单；创建/编辑表单 scope_domain 改**主题域下拉**（listDomainTree 动态加载，根治手填错值 403）；编辑弹窗复用创建表单（secret 不可改）。**④ 验证**——后端 test_consume_security +5（编辑透传/停用启用/软删/批量混合容错/非管理员 403）12 passed + consume 相关 18 passed + ruff 0（仅并行 WIP 的 F821 Any）；前端 ApiClients.test 7（+4：编辑透传 PUT/停用 PATCH/删除 DELETE/批量停用）+ tsc 0。影响接口：新增 4 个管理端点（向后兼容）。 |
| 2026-09-01 | — | semantic+frontend | **指标可信度仪表盘误导修复 + 健康度下钻闭环（用户『指标可信度为何 100% / 点健康度分布为何跳可观测中心而不是对应指标』）**：**① 根因**——仪表盘「指标可信度」主读数实为**覆盖率**（已评分/总数），而 cron `refresh_health_scores` 每日强制全量评分 → 覆盖率恒 100%，无区分度且制造「一切良好」错觉（真实绿档可信率仅 50%：6 GOOD + 6 WARNING）；点健康度分布写死跳 `/observability`，且可观测中心内档位/低健康项均不可点，无法直达对应指标。**② 修复**——后端 `MetricListParams` 增 `health_level` 过滤（repository IN 子查询命中 `metric_health_score.level`，无评分指标不命中任何档位，count/list 语义一致，service 透传）；前端 MetricCatalog 支持 `?health=` 下钻（URL 同步 + 健康度筛选 Select + 非法值忽略）；Dashboard 可信度卡主读数改**绿档可信率**（EXCELLENT+GOOD/已评分）、档位 pill 下钻 `/catalog?health=xx`、低健康指标直达 `/detail/{code}`（外层 button 改 div 避免嵌套交互元素）；Observability MetricHealthCard 档位 Tag / 低健康项同源可点。**③ 验证**——后端 `test_semantic_repository` 43（+3：health_level 命中/不过滤无子查询）+ ruff 0；前端 MetricCatalog 66（+2）、Dashboard 34（+3）、Observability 11（+2；9 处裸渲染补 MemoryRouter 修复 useNavigate 上下文）+ tsc 0；真实 DB 过滤 SQL 验证（GOOD 6 / WARNING 6 / EXCELLENT·CRITICAL 0）。提交 d5d95eb3。影响接口：`GET /metric-definitions` 新增可选 `health_level`（向后兼容） | 新增可选 health_level（向后兼容） | semantic/frontend 保持 released |
| 2026-09-01 | — | dimension+frontend | **维度值管理工业化（用户『维度值可能是维度表列而非枚举值/批量操作/映射与对账怎么用/大数据量人工成本高』）**：**① 引用型维度（SNAPSHOT 快照）**——`dimension` 加 source_id/source_table/source_column/sync_mode/refresh_interval_hours/last_snapshot_at（迁移 0120）；值集合来自源表列 `SELECT DISTINCT` 快照（keyset 分页每批 5000），不写 member 表，适合百万级大基数；新表 `dimension_value_snapshot`（版本化 uk(dim_code,snapshot_at,value)，保留最近 2 批做新增/消失 diff，REMOVED 标记上批消失值）+ `dimension_snapshot_run`（每次刷新 total/added/removed/null_rate 统计与样本）+ `dimension_mapping_value`（值级映射）。**② 批量操作**——成员行选 + 批量发布/废弃/删除（BFS 子树并集去重防父+子重复删，逐条复用单条约束：父级废弃拦截/绑定默认值保护）。**③ 值级映射 + 翻译服务**——`translate_value/translate_values`（值级映射优先，expression 仅原样兜底）+ `mapping_coverage`（源维度当前值集合已配置/未映射清单）。**④ 定时刷新**——`dimension_snapshot_tasks`（@task_locked，每 30 分钟扫描到期维度按 refresh_interval_hours 刷新，单维度失败记 run FAILED 不阻断）。**⑤ 前端**——MembersTab 引用型面板（绑定表列/刷新快照/数据质量摘要：值总数/新增/消失/空值率+样本）+ 批量按钮；MappingsTab「值级映射」弹窗（覆盖率徽标/未映射清单/翻译预览）；ReconciliationsTab 加 quality 自动数值对账入口（人工口径对账与自动数值对账两栏并存）。**验证**——后端 `test_dimension_service` 98 passed（+11：快照 diff/非引用拒绝/失败记 FAILED/绑定校验/批量并集去重/绑定保护/批量发布跳过/翻译命中未命中兜底/覆盖率）+ ruff 0；前端 Dimensions 34 passed（+2：引用型摘要渲染/批量发布调用）；真实 DB 迁移 0120 应用成功（6 列 + 3 表）；security 8 失败为 HEAD 既存多角色问题（stash 验证与本次零交集）。影响接口：新增 reference/snapshots/batch-*/mapping-values/translate 端点（向后兼容） | 新增端点（向后兼容） | dimension/frontend 保持 verified/released |
| 2026-09-01 | — | consume+frontend | **查询工作台指标下拉被首个 ACTIVE 客户端授权范围绑架修复（用户『自己发布的指标在查询工作台看不到』）**：**根因**——前端 loadClientScope 隐式取「首个 ACTIVE 客户端」的 scope_domain/metric_whitelist 收敛指标下拉，而生产库唯一 ACTIVE 客户端为 E2E 测试客户端 e2e_app（scope_domain=sales + 白名单仅 sales_e2e_gmv_day/sales_e2e_ordercnt_day），导致下拉只剩 2 个销售测试指标、用户发布的真实 PUBLISHED 指标（outpatient/medical_fee 等域）全部不可见。**修复**——① 初始加载不再隐式收敛：未选择/未绑定客户端时展示全部 PUBLISHED 指标（平台内部视角，后端 listMetrics 本就允许）；② 新增「消费客户端」选择器（ACTIVE 客户端列表，展示域说明），用户显式选择后按其授权范围收敛，签发令牌也使用该客户端（保证『能看到的=能消费的』）；③ 令牌已绑定客户端（解析 JWT sub）时自动选中并收敛。**验证**——QueryWorkspace 14 passed（+1 回归：存在 ACTIVE 客户端但令牌未绑定时展示全部指标不被绑架；收敛测试改模拟令牌绑定）+ tsc 0；顺带修复 ApiClients.test.tsx 未使用 within 导入（HEAD 既有致前端镜像构建失败）。提交 1a4caf3a/25cc6040。影响接口：无（纯前端）。 | 无接口变更 | consume/frontend 保持 released |
| 2026-09-01 | — | consume+frontend | **API 客户端令牌有效期可配置 + 接入指南（用户『60 分钟可否延长 / 无接口信息示例与连通性测试』）**：**① 有效期参数化**——后端新增 `TokenIssueRequest`（`expire_minutes` 5~1440 默认 60，缺省 body 向后兼容），`POST /consume/api-clients/{id}/token` 接受 body 并透传 `create_access_token`，审计 detail 带实际值；前端「签发令牌」改弹窗（60 分钟 / 4 小时 / 12 小时 / 24 小时档位），成功提示动态展示实际时长。**② 接入指南**——API 客户端页新增「接入指南」卡片：consume 查询端点清单（dry-run / query / semantic / snapshots / stats）+ 两种鉴权说明 + curl 示例（X-Api-Key 与 Bearer 两种）；「连通性测试」按钮：取首个 ACTIVE 客户端 → 签发令牌 → 首个已发布指标 dry-run 验证全链路 → 结果 Alert。**③ 验证**——后端 `test_consume_security` +1（默认 60 / 自定义 240 透传 / 超范围 5000 与下限 1 均 422）+ ruff 0；前端新增 `ApiClients.test` 3 项（列表+接入指南渲染 / 弹窗有效期透传 240 / 连通性测试全链路）+ QueryWorkspace 13 无回归 + tsc 0。影响接口：`POST /consume/api-clients/{id}/token` 新增可选 body（向后兼容） | 新增可选 expire_minutes（向后兼容） | consume/frontend 保持 released |
| 2026-09-01 | — | semantic+frontend | **指标↔术语多选关联 + 关联术语域切换修复 + SQL 推断回填默认 SQL 模式 + 查看完整口径时间列展示增强（用户『SQL 推断查看完整口径没展示列名/回填应默认 SQL 模式/术语来回切换只显示全部域且不全/要求支持多选术语』）**：**① 术语多选（后端 + 前端）**——`metric` 加 `term_ids` JSON 列（迁移 0119，主术语=首项写 term_id 兼容旧单值链路）；`MetricTermBindRequest`/`MetricCreateRequest`/`MetricResponse` 全透传 `term_ids`；`bind_metric_term` 支持多选全量替换（批量校验术语存在、空数组解绑），创建路径主术语落 term_id；前端详情页/注册向导关联术语 Select 改 `mode=multiple`（详情页回显 term_ids 数组、已绑定多术语逐个可点击跳转术语表、解绑清空数组；注册向导提交 term_ids）。**② 域切换修复**——`loadTermOptions` 由读 `termScope` state（React 闭包陈旧：切换回调里 state 未更新，切回「仅同域」仍加载全部域）改为 **scope 参数显式传入**（切换/搜索/滚动/回显全调用点传当前范围），「全部域没展示全」同步解决（滚动加载分页参数不再受陈旧 state 影响）。**③ SQL 推断回填默认 SQL 模式**——批量候选 `definition_json` 无 `sql` 键但有 `dw_definition`（整句原始 SQL），回填逻辑 `if (dj.sql)` 走不到、落 expression 分支；改为 `dj.sql || dj.dw_definition` 任一存在即 SQL 模式（单条路径同步放宽 defMode 判断）。**④ 查看完整口径时间列**——「时间列 / 分区键」值防御渲染（对象取 column、字符串/数组均显示解析列名，不再依赖 tags 对未知形态的脆弱转换）。**验证**——后端 `test_semantic_service` 303（+3：term_ids 多选全量替换主术语首项/含不存在术语 404 不落库/创建透传 term_ids）；ruff 0；前端 MetricDetail 95（+术语多选绑定断言数组、更新旧断言）+ MetricCreate 102（+term_ids 数组断言）+ tsc 0；真实 DB 迁移 0119 应用成功、`term_ids` JSON 持久化验证（term_id=10 + [10,11] 回滚还原）。影响接口：`PUT /metric-definitions/{code}/term` 与创建请求新增可选 `term_ids`（term_id 兼容，契约向后兼容） | 新增可选 term_ids（向后兼容） | semantic/frontend 保持 released |
| 2026-09-01 | — | consume+frontend | **QueryWorkspace 消费查询 401/403 根因修复（用户报 POST /consume/query 401/403）**：**① 401 根因**——/consume/query、/dry-run、/semantic 为严格消费方通道（get_consume_client，仅认 consume 域 X-Api-Key client_id:secret 或 consume JWT），前端全局携带的 semantic 域密钥 dev-semantic-key 格式不符且未签发消费令牌时必 401；snapshots 双通道（get_consume_or_internal_user）可回落用户 JWT 故正常。**② 403 根因**——用户已签发 e2e_app 消费令牌但查询指标状态为 REVIEW（未发布），FORBIDDEN_METRIC 指标状态不可消费；前端指标下拉未过滤状态，把未发布指标也列给用户。修复——前端 QueryWorkspace 指标下拉仅展示 PUBLISHED（listMetrics status=PUBLISHED，403 从源头消除）；新增 consumeErrorText 错误引导（AUTH_APIKEY_INVALID/MISSING 转「需要消费令牌：请点击上方『从客户端签发令牌』后重试」，FORBIDDEN_METRIC 显示后端中文 message）。验证：QueryWorkspace 10 passed（+1 401 引导回归）+ tsc 0；前端镜像已重建生效。提交 4089c88f。影响接口：无（纯前端）。
| 2026-08-31 | — | frontend | **前后端接线复审 + 英文编码残留收尾（目标复审）**：**① 接线复审**——重跑 FastAPI OpenAPI 路由表（435 操作）与前端 api.ts 调用（390 对）双向比对：正向孤儿 0（前端全部调用有后端路由）；反向逐项甄别后确认全部为「提取器漏抓（api.ts 已有封装）/被替代覆盖（consume versions→metric 侧、collect-async→collect-now、publish→approve、reactivate→batch、notify failed/sent→read/retry、metric-mounts 单行 PUT→updateMetric 整体、measure-catalogs/{code} 等单条 GET→列表已含全字段）/系统级·内部摄入端点（admin/keys、search/indexes、feature-flags、pii/validate 发布内部二次校验、mark-source-dropped 采集自动触发、quality/observe 外部摄入 webhook）」，业务端点均有前端可达入口；**② 英文编码残留 5 处收尾**——Templates 实例化/编辑弹窗「服务模式/可加性」下拉此前无 label 直接显示英文 value，补 `SERVING_MODE_LABEL/ADDITIVITY_LABEL`（import+4 处）；LineageView 失效边列表 provenance 列漏 `CHANNEL_LABEL`；QualityCenter 新建规则严重度下拉 label 漏 `QUALITY_SEVERITY_LABEL`；AssetMap 变更详情 change_type 漏 `CHANGE_TYPE_META` + 责任人档案卡角色 role 补 `ROLE_LABEL`（对齐 Layout 惯例）；**③ 甄别非问题**——MetricCatalog 域下拉虽 label=code 但展示走 domainFilterOptions 中文映射；MetricCreate PERIOD_OPTIONS「日 (day)」为中文优先双显；SubjectDomain level 为数字层级；QueryWorkspace quality_flag 后端仅写 null；数据库名/域 code 为业务标识符非枚举。**验证**——tsc 0 错（MetricDetail 3 个 TS6133/TS2315 为并行 WIP，HEAD 无）；Templates/LineageView/AssetGraph/QualityCenter 110 passed 零回归。接口变更：无（纯渲染层） | 无接口变更 | frontend 保持 released |
| 2026-08-31 | — | semantic+frontend | **模板实例化必填校验修复（用户『必填字段缺失: metric_code, currency, mount, definition_json, pii_flag』）**：① metric_code 属**系统可自动生成字段**（MetricCreateRequest 缺省由 Service 层按源表/度量列/周期自动生成），模板 required_fields 含它但留空不再 422——与前端「留空由系统自动生成」提示一致（原设计误判为缺失）；② 必填缺失错误信息从英文编码改为**中文业务名**（新增 REQUIRED_FIELD_LABEL_ZH 映射，对齐前端 REQUIRED_FIELD_OPTIONS 21 项，如「必填字段缺失: PII 标记」），与编辑模板中展示的中文名一致；③ 前端 requiredRuleFor 对 metric_code 永不强制（可自动生成）+ Alert 提示语同步（留空由系统自动生成）。**验证**——后端 test_semantic_api 18（+2：metric_code 留空实例化成功 create_metric 收到 None / name 缺失仍 422 且中文信息）+ ruff 0；前端 Templates 32（+1：留空不拦截提交）+ tsc 0；真实容器 admin 实例化模板 1：metric_code 留空 + pii_flag=false → **OK** （outpatient_entity_value_day 自动生成，验证后已软删清理）。影响接口：无（契约兼容，仅校验行为修正） | 无接口变更 | semantic/frontend 保持 released |
| 2026-08-31 | — | semantic+frontend+collector | **前后端接线缺口修复（审查：后端已实现、前端无入口的 17 项真缺口逐一补前端）**：**① 模板创建**——`POST /semantics/templates` 此前前端无创建 UI（仅有 list/update/owner/active/instantiate），补 api 封装 `createMetricTemplate` + Templates 页「新建模板」按钮与创建弹窗（复用编辑弹窗表单，code/name/domain 后端必填校验）；**② 血缘治理**——`GET /lineage/export`、`GET /lineage/path`+`/path/terminals`、`POST /lineage/parse-batch`、`POST /lineage/scan`、`DELETE /lineage/edges`（按节点级联）、`GET /lineage/health` 六个端点此前零前端调用，补 api 封装 + LineageView 新增「血缘治理」Tab（导出 JSON/路径查询/终结点/批量解析/目录扫描/级联删除/健康检查）；**③ 采集任务取消**——`POST /data-sources/jobs/{id}/cancel` 无前端入口（采集任务页仅失败重试），补 api 封装 + CollectionTasks 任务行「取消」按钮（排队中取消投递/采集中请求取消/终态幂等提示）；**④ 字典未知项打回**——`POST /dicts/unknown/reject` 无前端入口（仅有 notify），补 api 封装 + Notifications 对 `dict.notify_unknown` 通知行加「打回」按钮（Modal 填原因 → 通知办结并告知提交人改用字典内值）+ 行动入口「去收录」；ref-count 确认列表接口已返回且表格「引用数」列已展示（非缺口）；**⑤ 域下指标**——`GET /domains/{code}/metrics` 无前端入口，补 api 封装 + SubjectDomain 详情「域下指标」按钮与弹窗（指标编码/名称/类型/状态，可点击跳详情）；**⑥ 指标运营分析**——`GET /metric-definitions/stats/reuse`+`/stats/ledger`+`/consistency/stats` 三统计端点零前端展示，补 api 封装 + 类型 + 新页面 MetricOps（复用度/僵尸指标/重复建设三 Tab + 一致率概要卡），路由 `/metric-ops` + 运营中心菜单入口；**⑦ 偏好删除**——`DELETE /me/preferences/{key}` 补 api 封装 `deletePreference`（侧栏折叠等 UI 偏好恢复默认的接线闭环）。**已甄别非缺口**：挂载单行 PUT /metric-mounts/{id}（前端挂载编辑走 `updateMetric` 整体全量 diff，功能已覆盖）、measure-catalogs/{code} 与 templates/{id} 与 quality/rules/{id} 单条 GET（列表已含全部字段，行内编辑功能已存在）、`POST /mark-source-dropped`（采集侧检测 DROP 后自动触发，非用户操作类）。**验证**——tsc 0 错；Templates 32 + LineageView/CollectionTasks/Notifications 111 + SubjectDomain 20 全过；前端全量待跑。接口变更：无（全部复用既有后端端点，仅补前端封装与 UI） | 无接口变更 | semantic/frontend/collector 保持 released |
| 2026-08-31 | — | frontend | **界面英文编码中文化（审查：资产地图/质量模块等 9 文件 20+ 处）**：**① 资产地图**——指标详情弹窗（两处）10 字段 `type/granularity/unit/aggregation/time_semantics/freshness/dw_layer/metric_tier/status/additivity` 从裸英文改为本地映射（`METRIC_TYPE_LABEL/GRANULARITY_LABEL/UNIT_LABEL/AGG_LABEL/TIME_SEM_LABEL/FRESHNESS_LABEL/DW_LAYER_LABEL/METRIC_TIER_LABEL/STATUS_LABEL/ADDITIVITY_LABEL`）；血缘边类型/关联指标关系列补 `LINEAGE_EDGE_TYPE_LABEL/METRIC_RELATION_EDGE_LABEL`；列表状态/粒度/分级列补映射；**② MetricDetailDrawer**——漏网的 `type/time_semantics/additivity` 3 字段补映射；**③ 质量模块**——QualityCenter/QualitySnapshot 严重度 P0/P1/P2 用 `QUALITY_SEVERITY_LABEL`、质量事件级别 ERROR/WARN/INFO 用 `QUALITY_LEVEL_LABEL`；**④ 选项框**——MetricCreate 聚合 `AGG_OPTIONS`、SqlInferEval 期望周期/聚合 options 由裸英文 value 改中文 label（`AGGREGATION_LABEL/GRANULARITY_LABEL`）；**⑤ 其他**——Dimensions 详情 SCD 类型复用既有 `scdTypeLabel`、LineageView 孤儿指标状态列补 `METRIC_STATUS_LABEL`、MetricImportModal 导入失败候选 `VALIDATION_ERROR` 显示「校验失败」。全部纯渲染层，enums 映射已具备或本地已有，`?? value` 兜底保留（未知值不回退英文）。**验证**——tsc 0 错（仅并行 WIP Catalogs.test mojibake_fields 1 个既存错误）；AssetMap/Dimensions/LineageView/MetricImportModal/QualityCenter/SqlInferEval 6 文件 170 passed + MetricCreate 102 passed，零回归 | 无接口变更（纯渲染层中文化） | —（frontend 保持 released） |
| 2026-08-31 | — | semantic+frontend | **模板实例化 422 根因修复（用户『POST /semantics/templates/1/instantiate 422』）**：**① 根因**——模板 #1 `tpl_verify_owner`（E2E 测试残留）`required_fields` 含 18 个字段，其中多个与类型不兼容：原子模板要求 `mount`/`currency`/`granularity`（原子恒日、无挂载、币种由逻辑度量体系承载）永远无法由表单满足，且 `currency`/`pii_flag` 在实例化弹窗**根本没有输入项**、`pii_flag=false` 被必填判定 `not merged[f]` 误判为缺失、`definition_json` 客户端遗漏时 pydantic 500——四层缺陷叠加致必 422。**② 修复**——后端 `_inapplicable_required_fields` 豁免类型不适用必填字段（原子豁免 granularity/mount/mounts/currency，原子+measure_id 豁免 definition_json；派生豁免 measure_id；复合豁免 measure_id/mount/mounts），审计记录豁免清单；缺失判定改为 None/空串/空列表/空字典才算缺失（布尔 False 是显式合法取值）；`merged.setdefault("definition_json", {})` 兜底客户端遗漏（等价前端恒发 {}）。前端实例化弹窗补 `currency`（字典下拉）与 `pii_flag`（Switch）输入、按模板 `required_fields` 动态标红必填（`requiredRuleFor` 对齐后端豁免）、必填提示 Alert 标注「不适用，自动跳过」、metric_code 模板必填时改必填并修正「留空自动生成」矛盾文案、提交前兜底校验（definition_json 外部编辑器 + 责任方逐个判断模板预设）。**③ 验证**——后端 test_semantic_api 17（+3：豁免类型不适用/适用字段仍 422/布尔 False 不判缺失；顺带修复 8 个 HEAD 既有实例化测试因 dw_developer 注册门禁缺失预设而失败）+ ruff 0；前端 Templates 31（+1：币种不适用自动跳过标注 + PII 开关渲染 + pii_flag 提交；5 个既有测试补填 metric_code 对齐新必填语义）+ tsc 0；真实容器 admin 实例化模板 1：原 422 → 填 metric_code + pii_flag 后 **OK** （`outp_tpl_inst_day` 创建成功，验证后已软删清理）。影响接口：无（契约兼容，仅校验行为修正） | 无接口变更 | semantic/frontend 保持 released |
| 2026-08-31 | — | collector+frontend | **Hive 采集中文乱码检测 + 告警标记（用户『样本数据预览乱码 2010��1��5��』）**：**① 根因**——源端（Hive 表数据/注释）写入时 GBK 字节被按 UTF-8 处理，中文被替换为 U+FFFD 替换符（实测 HMS `COLUMNS_V2.COMMENT` 存 `EFBFBD`×12），**信息在源头已丢失**，平台忠实读取故预览/展示乱码，非采集器可修复。**② 平台侧检测+标记**——新增共享 `collector/mojibake.py`（`contains_mojibake`：U+FFFD 替换符 + GBK 二次转码残留「锟斤拷/烫烫烫/屯屯屯」，正常中英文/日期不误判）；spi 基类新增乱码登记（`_note_mojibake_field`/`_take_mojibake`，`_apply_samples` 与 `_sample_rows_aligned` 对样本明文打码前检测，全连接器生效）；Hive 连接器 DESCRIBE 注释检测 + `_annotate_mojibake` 写 `schema_json.mojibake`（sample_fields/comment_fields）并日志告警「GBK→UTF-8 替换、信息已在源头丢失、请在 Hive 侧修复后重采」；Hive Metastore `_build_spec` 注释同源接入；实时采样 `sample_entity` 返回 `mojibake_fields` 并写回 schema_json。**③ 前端**——字段详情抽屉展示「检测到源端编码乱码」Alert（乱码字段合并展示）；立即采样后 message.warning 告警。**④ 验证**——test_collector_mojibake 13 项（检测规则/登记取走/样本明文/两连接器接入）+ collector 相关 260 passed + Catalogs 55（+1 抽屉 Alert）+ ruff 0 + tsc 0；真实 HMS 探针确证源端 `EFBFBD`×12 乱码存在。纯标记不修改乱码值（源头信息不可逆丢失） | 无接口变更（schema_json 增 mojibake 标记 + sample 响应增 mojibake_fields，均向后兼容） | collector/frontend 保持 released |
| 2026-08-31 | — | metric+frontend | **批量导入增强 A+B+C（用户『CSV/Excel 批量注册入口在哪、目标域为什么是英文编码』）**：**① 方案 A——注册向导入口**：`/create` 顶部工具栏新增「批量导入」按钮（与指标目录页共用同一弹窗）；**② 方案 B——支持 Excel(.xlsx)**：后端 `/imports/csv` 按 MIME/文件名后缀识别 xlsx 用 openpyxl 解析（首个工作表、空行跳过、逐行容错与 CSV 完全一致），`/imports/template?format=xlsx` 返回 Excel 模板（表头加粗+示例行+列宽自适应）；新增依赖 openpyxl 3.1.5（纯 Python 零编译，容器无需系统库）；**③ 方案 C——无权限按钮禁用**：批量导入按钮始终显示，无 metric:create 权限时 disabled + Tooltip 提示原因（不再隐藏导致误以为功能缺失）；**④ 目标域中文**：弹窗域下拉由纯英文 code 改为中文 label（指标目录页=中文名+停用标识、注册向导页=中文名(code)），不再显示英文编码；**⑤ 架构收敛**：批量导入弹窗抽为公共组件 `MetricImportModal`（CSV/Excel 模板下载、中文域下拉、上传 accept=.csv,.xlsx、逐行容错+逐条结果回显），指标目录页与注册向导两页共用、后续修 bug 只改一处。**验证**——后端 `test_metric_batch_api` 24 passed（+2：xlsx 解析建候选/空行跳过/xlsx 模板下载）+ ruff 0；前端 MetricImportModal 5 passed（中文域/模板 format/未选域拦截/上传成功回显/失败提示）+ MetricCatalog 64（+2：无权限禁用、有权限点开弹窗）+ MetricCreate 101 + tsc 0。影响接口：`/imports/template` 新增可选 `format=xlsx` 查询参数（缺省 csv 向后兼容）、`/imports/csv` 接受 xlsx（向后兼容） | 新增可选能力（format=xlsx；CSV 路径不变） | metric/frontend 保持 released |
| 2026-08-31 | — | governance | **角色权限二次保存 500 修复（用户『PUT /roles/analyst/permissions 500』）**：**① 根因**——`replace_role_permissions` 用**软删**（update deleted_at）替换角色权限点覆盖，但 `role_permission` 唯一索引 `uk_role_permission_role_action` 仅含 (role, action) 两列，**软删行仍占用键位**——已有覆盖行后二次保存同一批动作触发 `Duplicate entry` 1062 → 500（首次保存无旧行故此前验证通过）。**② 修复**——改为**物理删除**该角色全部行（含历史软删残留）+ 插入新集合，与模型 docstring「整表替换（先删该角色全部行）」语义一致；纯配置表无历史追溯需求，审计在 audit_log。**③ 验证**——test_governance_repository 101（+1：断言 DELETE 语句且无 deleted_at 条件）+ test_governance_service（+1：二次保存同一批不冲突不重复）+ ruff 0；真实环境 admin 对 analyst 保存同一批 16 动作 **500→200**，覆盖行不重复 | 无接口变更（纯 repository 替换语义修正） | governance 保持 released |
| 2026-08-31 | — | governance | **角色权限保存 422 修复（用户『PUT /roles/analyst/permissions 422』）**：**① 根因**——`RolePermissionUpdate.actions` 的 `max_length=8` 是 action-registry 仅有少量动作点时的历史约束，现 registry 已 102 个动作点、前端角色基线（VIEW_BASE 20+）远超 8 个，保存任意角色权限点必 422（schema 层拦截，service 的 unknown action 校验才是真正合法性防线）。**② 修复**——`max_length` 8→256（对齐 registry 规模+扩展空间），service 继续按权威集合 `all_configurable_actions()`（107 个）校验未知动作；补回归测试：service 层 16 个动作点（12 UI + 4 资源动词）覆盖成功且正确分类（资源动词→custom_actions、UI 动作→ui_custom_actions）、schema 层 20 个动作点通过校验。**③ 验证**——test_governance_service 73 passed（+2）+ ruff 0；真实环境 admin 保存 analyst 16 个动作点 **422→200**，ui_custom_actions 完整保留 | 无接口变更（schema 约束放宽，契约兼容） | governance 保持 released |
| 2026-08-31 | — | frontend(login) | **登录页底部文案重设计（用户『企业级指标语义中台 · PII 合规 · 全链路血缘追溯 和 WeSemantics · 指标语义中台 v0.1 这两个文案显得很突兀，想办法重新设计优化』）**：**① 根因**——原信任条「企业级指标语义中台 · PII 合规 · 全链路血缘追溯」与 hero/能力矩阵高度重复（"PII 合规""全链路血缘追溯"均已被能力矩阵覆盖，"企业级"属空洞自夸），且与页脚「WeSemantics · 指标语义中台 **v0.1**」两行堆叠在左下角（v0.1 是开发痕迹，企业级登录页最突兀的点）。**② 重设计**——两条合并为一条「品牌底部签名」`.login-footbar`：左侧品牌名「WeSemantics · 指标语义中台」（去掉 v0.1），中间柔性渐隐细分隔线（`linear-gradient(90deg, rgba(255,255,255,0.14), transparent)`），右侧三个**真实且未被能力矩阵覆盖**的企业级可信点「全程操作审计 / 变更灰度发布 / 软删可恢复」（系统全链路审计、指标发布标准/灰度双模式、软删+回收站恢复均为真实能力，非自夸）；细分隔线（`border-top 1px rgba(255,255,255,0.08)`）与能力矩阵区隔，形成"铭牌"视觉；**③ 约束**——JSX 结构（表单字段/提交逻辑/错误处理/响应式）零改动，仅合并底部两个 div 并重写文案与样式；渐显动画 ws-rise 0.95s 保留并纳入 `prefers-reduced-motion` 降级列表。**验证**——tsc 0 错；Layout 测试 10 passed；grep 旧类名 `login-trust`/`login-foot` 清零、新类 `login-footbar` 就位；前端容器重建后 Playwright 视觉验证 | 无接口变更（纯登录页文案与样式） | —（frontend 保持 released） |
| 2026-08-31 | — | frontend(login) | **登录页「血缘流动拓扑」背景特效（用户『首页的动画效果并不明显，而且也没有能够契合该系统的一些特效效果』）**：**① 新增 SVG 装饰层 `.login-network`**——数据源（青 #0e7c86）→ 指标（琥珀 #e8862d）→ 治理（白 #eef2f8）→ 消费（青绿 #4cc9b8）的 8 节点 + 9 条贝塞尔连线拓扑（数据源→指标用青色边、指标→治理用琥珀边、治理→消费用白色边），光点沿血缘边持续流动（`stroke-dasharray:3 11` + `stroke-dashoffset 0→-14` 循环，`ws-flow` 不同 duration 2.6-4.5s 错峰模拟数据流速差异），指标节点（琥珀）叠加信号辐射波纹（`ws-ripple` scale 0.4→2.6 + opacity 0.9→0，3s ease-out 无限循环，隐喻"指标被持续消费/信任传播"），节点呼吸（`ws-node-pulse` 2.6s，opacity 0.32↔1），整个网络渐入（`ws-network-in` 1.1s，opacity 0→0.55）；`z-index:0` 在内容下层，0.55 透明度不抢文字，SVG `aria-hidden` 无障碍；**② 强化既有动画**——发丝计量网格漂移 90s→30s（更明显的数据流隐喻）、琥珀读数刻度呼吸新增 `box-shadow` 光晕脉冲（0→22px 3px rgba(232,134,45,0.28)，呼应"活体读数"）；**③ 约束**——表单字段/提交逻辑/错误处理/响应式策略全部保持不变，仅新增装饰层与强化既有 keyframes；纳入 `prefers-reduced-motion` 降级列表（4 个新选择器）。**验证**——Playwright 真实容器实测 11/11：网络 SVG 1、9 边、8 节点、2 波纹、动画实际生效（`animation-name`=ws-flow/ws-node-pulse/ws-ripple）、网络 opacity 0.55、z-index 0、0 console error；tsc 0 错；Layout 测试 10 passed | 无接口变更（纯前端装饰层 + 动画强化） | —（frontend 保持 released） |
| 2026-08-31 | — | collector | **各数据源采样健壮性全面修复（用户『检查其他数据源在采样的时候是否也会遇到问题』）**：跨 9 类连接器逐项审查采样实现，修复 6 个真实缺陷——**① 行视图 sample_rows 丢失**（Hive/HMS 的 collect 只写列式 sample、丢弃 `_sample_columns` 返回值，前端「样本记录」行视图为空；已补写 schema_json）；**② 大字段样本无截断**（BLOB/TEXT/JSON/Array/Map 的 str() 可达 MB 级，写入 schema_json 撑大 DB JSON 列/API 响应/前端渲染；spi 新增 MAX_SAMPLE_VALUE_LEN=200 + _truncate_value，行视图与列式 sample 统一截断，手机/身份证等短敏感值不受影响）；**③ ClickHouse TabSeparated 转义未反转义**（CH 对字段内 \\t/\\n/\\\\ 转义，旧解析器直接 split 不还原，含制表符/换行/反斜杠的样本值损坏；新增 _unescape_tsv，NULL \\N 归约不变）；**④ 采样进度事件缺失**（仅 MySQL 发 sampling 进度，Postgres/ClickHouse/Hive/HMS 配置采样后前端会卡在 scanning 阶段——补逐表 sampling index/total/entity_name 事件，与 MySQL 对齐）；**⑤ HMS collect_entity 不采样**（单表刷新无样本，已补——复用采样连接、行视图落库、失败不阻塞刷新）；**⑥ MySQL/Postgres 标识符不允许连字符**（含 - 的表/列名被跳过采样，放宽 _IDENT_RE 对齐 ClickHouse/Hive，反引号/双引号包裹安全）。**验证**——test_connectors 49（+8：CH 反转义/采样进度×2/Hive 行视图落库/标识符放宽×2/大值截断）+ test_hive_metastore 17（+2：collect 行视图落库、collect_entity 采样）+ collector 相关 288 passed + ruff 0；真实 Doris 源端到端：ssb.customer 立即采样行视图 5 条**真实记录**落库（8 列对齐、非拼凑假记录）+ 列式样本 5 条。顺带修复：并行会话 0118 迁移已执行到 DB 但未进镜像导致 backend 启动失败——已同步文件入容器恢复 | 无接口变更（纯后端采样链路健壮性） | — |
 — | governance | **权限治理「新建授权」角色选项框为空修复（用户『角色选项框都是空值，没有和角色管理的值紧密关联起来』）**：**① 根因**——``role`` 表自 0004 建表起从未种入数据，内置七角色（RoleName 枚举）只是代码常量从未登记进表；``GET /roles/options``（授权弹窗下拉数据源）只查 role 表未删行故恒空，而「角色管理」页用 ``list_role_permissions``（内置角色来自 policy 常量）有值——两处展示不一致。**② 修复**——新增迁移 0118 ``seed_builtin_roles`` 种入内置七角色登记行（``is_custom=False``，幂等/可逆/不触碰自定义角色），授权弹窗下拉与角色管理页一致（内置+自定义并集），``grants.role_id`` 可指向内置角色行 id（授权生效语义不变，role_id 为记录性关联）。**③ 验证**——governance repository/service/events/policy 147 全绿（+2：``list_all_roles`` 返回内置+自定义并过滤已删、``list_role_options`` 与角色管理一致）；真实 DB 迁移应用 + ``/roles/options`` 端到端返回 7 个内置角色；纯 DB 数据修复无需重建容器 | 无接口变更（迁移 0118 数据种子） | governance 保持 released |
| 2026-08-31 | — | frontend(login) | **登录页内容与布局重新设计 + 文案重设计（用户『首页登录页除了特效外，展示的内容、样式布局也需要重新设计，一套口径全员校准等文案需要重新设计』）**：**① 文案重设计**——hero 主标语「一套口径，全员校准」→「一次定义，处处可信」（语义平台核心价值：指标口径定义一次、全链路复用可信）；副标题改为「以语义为锚，把组织里散落的指标口径编织成一张可信之网——定义、血缘、治理、消费，全链路一致、可解释、可追溯」；三个数字价值点（100%/0/24×7）→ **四个能力矩阵**（语义统一 OneData 逻辑度量/血缘可溯 指标→落地表全链路/治理可信 PII 合规·质量告警/AI 智能 SQL 推断·口径生成·AI 助手）；表单区「Sign in / 登录工作台」→「Welcome / 欢迎回来」+ 副行「登录以继续您的指标语义治理工作」；按钮「进入工作台/正在校准」→「进入语义中台/正在登录」；页脚「Metric Semantics Hub」→「指标语义中台」；新增底部信任条「企业级指标语义中台 · PII 合规 · 全链路血缘追溯」；**② 布局重新设计**——能力矩阵三列数字改为 2×2 卡片网格（描边+微透明底+hover 描边高亮+信号点前缀）；信任条渐显；表单卡新增副标题行；hero 字号 40→44px；动画区同步补第 4 个价值点 stagger（0.4/0.52/0.64/0.76s）与信任条渐显，`prefers-reduced-motion` 列表同步；**③ 约束**——表单字段（用户名/密码）、提交逻辑、错误处理、≤900px 隐藏左侧机箱的响应式策略全部保持不变（纯展示层重构）。**验证**——tsc 0 错；Layout 测试 10 passed（品牌断言无回归）；前端全量测试全绿 | 无接口变更（纯前端文案与样式重构） | —（frontend 保持 released） |
| 2026-08-31 | — | collector | **采集运行日志终态回写失效修复（用户『运行日志只展示一条开始采集』）**：**① 根因**——`read_run_logs` 把 `limit<=0` 一律当空返回，而终态 `_flush_run_logs` 恰以 `limit=-1` 表示「读全部」→ 永远读到空 entries → 已完成任务的运行日志 0 条落库且缓冲被删（实测 run 26 采集 410 条进度日志全部丢失）；另 worker 容器与 backend 容器各自独立，热更新仅 cp backend 导致 worker 侧旧代码持续吞日志。**② 修复**——`queue.read_run_logs` 支持 `limit<0` 读全部（Redis lrange 负索引 -1）；`stale_collection_jobs_task` 收尾滞留任务时 flush 其 Redis 日志缓冲（worker 中断前已产生的进度日志不丢）+ **DB 侧兜底扫描**超时 RUNNING/QUEUED（JobStore key 被 arq 清理时仍能收尾，防记录永久卡「采集中」，repository 新增 `list_stale_running_runs`）。**③ 验证**——`test_collection_run_logs` 25（+limit=-1 全量读取 + 端到端 flush 落库）+ 新建 `test_stale_collection_jobs` 4（DB 兜底收尾/失败不阻断/无 redis 跳过）+ collector 相关 288 passed + ruff 0；真实端到端：采集 2 张表 DB 落库 **410 条日志**（开始→逐表采样→扫描→注册→完成），run 22（16:15 用户采集被 08:25 worker 重启中断）已收尾 FAILED 并 flush 其日志 | 无接口变更（运行日志内部链路修复） | — |
| 2026-08-31 | — | frontend(brand) | **品牌名 WeSemantic → WeSemantics（用户『把WeSemantic改为WeSemantics』）**：全仓可见品牌文案批量替换——`index.html` title/description、登录页品牌名/hero 文案/页脚、Layout 侧边栏+Header、theme.ts 设计系统注释、错误边界日志前缀、Layout 测试断言、上一轮改名 CHANGELOG 记录，全部 WeSemantic → WeSemantics（复数形态品牌感更完整）；内部类名 `UnisenseApiError`/本地存储键/测试数据源名仍刻意保持不动。**验证**——grep 全仓 `WeSemantic` 清零（仅剩 `WeSemantics`）；tsc 0 错；Layout 测试 10 passed（断言同步） | 无接口变更（纯品牌文案） | —（frontend 保持 released） |
| 2026-08-31 | — | frontend(brand) | **品牌更名 Unisense → WeSemantics + 登录页动画升级（用户『项目改名为 WeSemantics，首页/展示位置同步，登录页加动画特效并契合项目目标』）**：**① 品牌改名（用户可见展示全覆盖，内部类名/存储键不动）**——`index.html` title/description、登录页品牌名/副标/hero 文案/页脚、Layout 侧边栏+Header 品牌名与字母徽标（U→W）、theme.ts 设计系统注释、错误边界日志前缀，全部改为 WeSemantics；**② 登录页动画（纯 CSS，JSX 结构与交互逻辑零改动）**——发丝计量网格 90s 缓慢漂移（隐喻指标数据在语义网格中流动对齐）、琥珀读数刻度 3.2s 呼吸（「活体读数」信号）、品牌区上浮渐入+徽标一次扫描光晕、hero 标题逐行浮现、三个价值点 0.4/0.55/0.7s stagger 上浮、表单卡片浮入、kicker 琥珀渐变流光文字、提交按钮流光扫过、页脚渐显；`prefers-reduced-motion: reduce` 时全部动画关闭（无障碍降级）。**验证**——tsc 0 错；Layout 测试 10 passed（品牌断言同步 WeSemantics）；前端可见品牌文案 grep 清零 | 无接口变更（纯前端文案与样式） | —（frontend 保持 released） |
| 2026-08-31 | — | docs(cleanup) | **过期文件 / 过期记录全面清理（用户『审查项目是否存在过期的文件、过期的记录』→ 选择全部清理）**：**① 文件残留**——删除 `loaded.yaml`（Playwright a11y 快照零引用）、`spec/full-audit-report.md` + `docs/REVIEW-穿透式审查-2026-08-11.md` + `docs/REVIEW-整改分配-2026-08-11.md`（三份过期审查快照，被 08-13 版取代且无活跃引用）、`backend/scripts/` 6 个一次性血缘迁移工具（backfill_metric_lineage/import_full_lineage/import_lineage_json/register_metric_lineage/verify_lineage_consistency/webhook_receiver，全库零引用；保留有测试直接引用的 backfill_conflict_orphans/import_dp_lineage/sync_neo4j_assets）、根目录 `dp元数据.csv`+`dp_table_desc.txt`（27MB 历史迁移源数据，gitignore 未跟踪，导入已完成）；**② compose 孤儿配置**——删除 `elasticsearch-setup` no-op 服务（索引自举已并入 bootstrap.py）、删除 `doris_fe_data/doris_be_data` 孤儿卷声明、`UNISENSE_DORIS_HOST` 默认值 `doris-fe`→空（不再指向不存在服务）；**③ 代码过期标记**——删除 `lineage/service.py:1718` 过期 TODO（repo.upsert_edge 已暴露 pii_inherited）、`DEV_GUIDE.md:542` `UNISENSE_OLAP_DSN`→`UNISENSE_OLAP_URL`、`.env.example` 删除 3 个后端无对应字段的失效变量、`llm/client.py` docstring 改为 DB 驱动说明（原 env 示例不存在于 Settings）；**④ 文档记录**——`module-status.yaml` 头部 `updated` 08-15→08-31、删除 T079（对应已删 doris_init/minio_init）、`NOTES.md` module-status 路径修正为 `docs/`；**⑤ DB 过期记录（真实 DB 单事务清理 + 备份 /tmp/cleanup_backup_20260831.json）**——物理删除 9 条软删 data_source + 516 条 db_catalog 已删源残留（含 3 条漏标非软删）+ 3 条 collection_watermark + 1 条 metric（e2e_repro_feeamount_day 级联关联）；软删 13 条自引用 OPEN 冲突（existing==candidate，保留审计）；复位 3 条异常中断 RUNNING collection_run 为 CANCELLED；**保留**活跃源 mysql_unisense 的 54 条历史软删目录记录。**验证**——compose config 有效、保留脚本测试 38 passed、DB 验证（未删 OPEN 冲突归零、data_source 3 活跃、db_catalog active 22951）、backend/frontend 健康 200；doc_sync 通过 | 无接口变更（文件/配置/DB 清理） | — |
| 2026-08-31 | — | collector(api) | **GET /data-sources/sampling-coverage 404 修复（路由 shadow）**：**① 根因**——静态两段端点 `/sampling-coverage` 注册在动态 `/{source_id}` 之后，Starlette 按注册顺序匹配，请求被 `/{source_id}` 捕获（source_id="sampling-coverage"）→ 查库失败 404，静态端点永远无法命中（与 `/jobs` 同源布局坑，该端点 docstring 已注明约束但 sampling-coverage 被加在动态路由之后漏遵守）；**② 修复**——`get_sampling_coverage_all` 移到 `GET /{source_id}` 之前（`/jobs` 之后），并补 docstring 布局约束说明；**③ 防回归**——新增 `test_sampling_coverage_all_must_precede_source_id_route`（断言命中静态端点、`get_source` 不被调用，对齐既有 `/jobs` 顺序测试）。**验证**——collector API 29 passed + ruff 0；容器热更新后真实接口 404 → 401（鉴权正常），8100/8180 双入口均通过 | 无接口变更（仅修复路由顺序） | — |
| 2026-08-31 | — | collector+frontend+infra | **采集进度不可见 + 立即采集卡 0% 根治（用户『Schema Drift · GB/T 36073 §6.4 是什么含义需中文化』+『立即采集一直卡在 0% 看不出状态和进度』）**：**① 卡 0% 双重根因**——a) Doris 源配置 sample_rows 后，对 ODBC 外表采样查询必败（ODBC_SCAN_NODE 不支持），旧降级逻辑逐列补采空转数千次失败查询，worker 卡在 collect() 内采样阶段，progress 停在 start（前端 0%）；b) 同源并发锁 acquire TTL=1800s 且不续期，worker 崩溃/重启后 finally 的 release 不执行，锁残留 30 分钟阻塞同源全部采集（SKIPPED_CONCURRENT，新任务看似卡住）；另发现 arq 0.28 已移除 ArqRedis.cancel_job——任务中心「取消」一直静默失败返回 False。**② 修复**——a) 采样熔断（spi.py）：整表查询失败先探测第 1 列，同因失败跳过整表补采并累计，连续 3 张表失败熔断本会话采样（实测 406 表采集失败日志从数千条降到 15 条）；b) 采样阶段进度（spi.set_progress_cb + mysql.collect 逐表发 phase=sampling + index/total/entity_name，service 注入 progress_cb）；c) 锁心跳（distributed_lock.refresh + tasks._lock_heartbeat 每 60s 续期 TTL 120s，worker 崩溃后锁最多 2 分钟过期）；d) queue.cancel 改用 arq.jobs.Job.abort（0.28 API，abort 失败仍落 CANCELLED 终态）；e) 前端 progressPct 阶段占位（start 5%/scanning 10%/sampling 30%）+ COLLECT_PHASE_LABEL 中文阶段文案（准备中/扫描源端/采样中/注册实体）；f) 「Schema Drift · GB/T 36073 §6.4」中文化为「表结构变更追踪 · 对标 GB/T 36073（数据管理能力成熟度评估模型）」（去掉不准确的章节号——§6.4 是数据应用域，表结构变更属数据架构域）。**③ 验证**——后端 365 passed（+采样熔断/单列探测/锁续期/心跳/cancel abort 回归）+ ruff 0；前端 DataSources 44（+start 占位与 sampling 进度回归）+ tsc 0；真实 Doris 源端到端：采集从「卡 start 0%」变为 sampling 逐表推进（213 表 index/total 可见）→ registering 406/406 → COMPLETED（scanned=406, registered=406, failed_count=0, coverage=1.0），锁结束后 EXISTS=0（已释放），采样熔断触发 1 次 | 无接口变更（progress 事件新增 sampling 阶段与 entity_name，向后兼容） | — |
| 2026-08-31 | — | collector+frontend | **样本采样改为行对齐存储 + 前端行视图展示（用户『100/1000 条多列平铺更挤，应每列对应字段、一行代表一个样本，字段描述放表头下淡色描述带』）**：**① 根因**——旧采样逐列独立取非空值（各列各自过滤非空），`sample[0]` 不保证来自同一条记录，前端按行展示会呈现**拼凑出来的假记录**（用户误以为手机号与邮箱配套）；**② 后端**（`spi._sample_rows_aligned`）——一次全列查询（不加 WHERE 非空过滤）→ 行视图 `sample_rows`（NULL 空串占位防列错位）+ 列式 `sample` 派生（PII 识别要「每列尽可能多非空形态」，与展示所需「真实记录」目标不同，双语义并存）；整表失败降级逐列补采（隔离问题列）+ `rows[:n]` 防御驱动未服从 LIMIT；mysql/doris/starrock/postgres/clickhouse/hive **六连接器统一走基类行对齐**（方言回调：PG 双引号、CH `TabSeparatedWithNames` + `\N`→NULL）；assetmap `_summarize_schema` 透传 `sample_rule`/`sample_rows`；**③ 前端**——`SchemaTable` 字段表去掉「样本 1…N」横铺列（保留类别列），新增**「样本记录」行视图表**（列=字段、行=记录、表头下**淡色描述带**可查字段含义、空值 `—` 占位、10 行/页分页），Catalogs/AssetMap 详情抽屉接入。**验证**——后端 **451 passed**（+行对齐/降级/打码规则测试）+ ruff 0；前端 Catalogs **54**（+行视图一行一记录/无样本不渲染/存量单值兼容）+ AssetMap **69** + tsc 0 | 无接口变更（schema_json 增 sample_rows 行视图键，向后兼容） | — |
| 2026-08-31 | — | docs | **README 部署流程重写：密钥生成初始化步骤完整纳入（用户『重新更新 README 部署流程，把生成密钥初始化的步骤加进去』）**：**① 10.2 升级为「密钥生成初始化（生产环境变量）」**——新增 11 项密钥清单表（JWT/Fernet/MySQL root+业务/ES/Neo4j/MinIO 凭据/备份加密/QuickBI 签名/种子管理员密码，逐项标注作用）、脚本三种模式（stdout 打印 / `--out` 直接写入推荐 / `--force` 强制覆盖先备份）、生成后自动自检四项规则（长度达标/弱凭据黑名单/URL-Shell 安全/种子密码复杂度）、**四步初始化流程**（复制模板→生成密钥→填非密钥项→构建启动）、非密钥项人工确认清单（OLAP_URL/CORS/IMAGE_TAG/Bootstrap 开关）、密钥锚点警告（一次性生成长期不变、拒绝覆盖、明文不入库另存密码管理器）；**② 10.3 首次部署**——代码块改为五阶段编排：第 0 步机器准备（sysctl/swap/透明大页/ulimit）、第 1 步密钥生成初始化（1a 复制模板/1b 生成密钥/1c 填非密钥项）、第 2 步构建启动（迁移与自举自动执行）、第 3 步验证（health/8180/bootstrap_summary）、**第 4 步上线后立即改种子管理员密码**（UI 或 `POST /users/me/password`）；**③ 手写方式保留为备选**。**验证**：章节编号连续（10.1-10.6）、与 `scripts/gen_prod_secrets.sh`/`.env.production.example` 实际实现逐项核对（11 项密钥清单、三种模式、自检规则、幂等保护均与脚本一致）；doc_sync 校验通过 | 无接口变更（README 文档） | — |
| 2026-08-31 | — | devops(deploy) | **生产配置模板 + 密钥生成脚本（方案 A，用户『按生产环境标准配置 .env』）**：**① 产物**——`.env.production.example` 生产模板（按 Settings/compose 全部 50+ 变量分节：环境/安全密钥/MySQL/ES/Redis/MinIO/Bootstrap/LLM/Doris/采集器/通知/资源配额，密钥项全部占位 + 生成方式注释）+ `scripts/gen_prod_secrets.sh` 一键生成 11 项强密钥（JWT/Fernet/MySQL root+业务/ES/MinIO/种子管理员/备份加密，纯字母数字避免 compose 插值与 URL 破坏，含长度/弱凭据黑名单/URL-Shell 安全三自检，`--out` 写文件权限 600 + 自动备份既有文件）。**② 验证**——脚本实测生成 11 项、自检全 PASS、权限 600；用生成密钥填充后 `Settings()` 加载**通过全部生产强校验**（env=prod、jwt_len=96、fernet_set、olap_set、无弱凭据命中）。**③ README**——10.2/10.3/10.5 部署命令改为推荐「模板+脚本」方式并统一 `--env-file .env.production`。**④ 设计要点**——不改当前开发 `.env`（不打断 3 个 healthy 容器与并行会话）；模板中 OLAP_URL 冲突（生产校验强制非空 vs 暂不启用 Doris）以注释给出 a) 外部 Doris b) 保持非 prod 两条路。**验证**：doc_sync 通过 | 无接口变更（部署工具/文档） | —（system/deploy 保持 released） |
| 2026-08-31 | — | devops(deploy) | **部署自举锁释放兼容 decode_responses（容器真实启动链实测发现）**：**① 根因**——`scripts/bootstrap.py` 的 `_release` 用 `pool.get(key).decode()` 校验 owner，但 Redis 池配置 `decode_responses=True`（`app/db/redis.py:29`），`get` 返回 `str` 而非 `bytes` → 每次自举结束释放主锁时抛 `'str' object has no attribute 'decode'`（`bootstrap_lock_release_failed` warning，不影响退出码，但多副本下锁未释放靠 TTL 300s 兜底）。**② 修复**——`_release` 归一化 bytes/str：`isinstance(current, bytes)` 时先 decode 再与 owner 比较，两种池配置均正确释放。**③ 验证**——单测 16 passed；重建后端容器后自举 `bootstrap_summary` 五步全部 ok/skipped、`bootstrap_lock_release_failed` 消失 | 无接口变更（内部缺陷修复） | —（system/deploy 保持 released） |
| 2026-08-31 | — | devops(deploy) | **部署自举自动化：容器启动即完整可用，消除全部人工初始化动作（用户『检查生产部署清单，把所有需要手动初始化或创建的地方改造为自动化，docker 部署完成后所有服务可立即使用』）**：**① 现状盘点（实测）**——已自动化：MySQL 迁移（`alembic upgrade head`，迁移内含字典 0030/敏感规则 0070/词根 0102-0103/粒度 0115 等种子）、MinIO bucket（`audit_archive` 自动建）、运维订阅与词根缓存（lifespan）；**仍需人工（本次改造对象）**：admin 账号与默认组织、标准主题域、ES 索引 ensure+sync（此前仅手动 API，且 `elasticsearch-setup` 服务是纯 echo 占位不建索引）、Neo4j 首日资产同步（cron `run_at_startup=False`）。**② 新增 `scripts/bootstrap.py`**——五步编排，按「阻塞/尽力」分类：阻塞（admin、domains）无账号/无域则平台完全不可用，失败 `exit 1` 由 compose 自动重试；尽力（ES ensure/sync、Neo4j）失败仅告警不阻断（可选依赖有降级路径）。**③ 关键实现**——`seed_admin.py` 拆出 `seed_admin(db)` 主体（只 flush 不 commit，事务边界交给调用方），`engine.dispose()` 隔离在 CLI 入口（入自举路径会回收 uvicorn 后续要用的连接池）；`seed_domains(db, owner_id=...)` 新增 owner 参数，取实际 admin id 而非硬编码 1；自举**不调用** `migrate_existing_domains`（会把存量指标 domain 批量改写为 uncategorized，属不可逆迁移，保留运维显式执行）；并发首启由唯一约束触发 `IntegrityError` → 回滚重试走「已存在则跳过」分支。**④ 分布式锁语义修正（实测发现的真缺陷）**——初版复用 `TaskLock`，它把 Redis 异常按 fail-safe 吞成「未获锁」（对 cron『跳过本周期』正确），但自举会误读为『别的副本正在灌』→ 首启时 `metric_idx` 刚重建为空却被跳过同步，检索全空；改为自实现三态探测 `acquired`/`held`/`unavailable`，**Redis 不可用照常执行（宁可重复不可漏灌）**，确被他人持有才跳过。**⑤ 启动链**——`Dockerfile` CMD 改为 `alembic upgrade head && python -m scripts.bootstrap && exec uvicorn`（`exec` 保证 uvicorn 为 PID 1 正确收 SIGTERM；worker 有显式 command 覆盖 CMD 不会重复自举）；compose 补 `backend.depends_on.elasticsearch`（此前缺失，ES 未就绪会让索引步骤失败）、注入 `UNISENSE_BOOTSTRAP_*` 与 `UNISENSE_SEED_ADMIN_PASSWORD` 开关、`start_period` 40s→90s。**验证**：新增单测 **16 passed**（幂等二次全 skipped、owner 取真实 admin id、自动化路径不触发 migrate、ES 四态判定、锁三态语义、退出码矩阵、dry-run）；ruff 0；**全新空库端到端实测**——`alembic upgrade head` 后 admin=0/主题域=0（证明确需自举），bootstrap 后 admin=1(platform_admin，注入强口令校验 True、默认弱口令 False)、主题域 0→8、字典 390→420；二次运行全 skipped 幂等 | 无接口变更（新增部署自举入口与环境变量开关） | —（system/deploy 保持 released） |
| 2026-08-31 | — | users | **创建用户接口 500 修复（用户『admin 默认密码如何更改』排查中实测发现）**：**① 根因**——`create_user` 在 `db.add(row)` + `flush()` **之后**才赋值 `row.role_items = [UserRole(...)]`，新建对象此时首次访问该 relationship，SQLAlchemy 为计算 `delete-orphan` 会 emit `SELECT user_role WHERE user_id = :id` 触发 lazy load，async 上下文抛 `MissingGreenlet` → `POST /api/v1/users` 返回 500（真实 DB 实测确认）。**② 修复**——把集合装载前移到 `db.add(row)` **之前**（对象仍为 pending，赋值不触发 lazy load），外键由 cascade 在 flush 时自动填充。**③ 防回归**——新增 `test_create_user_loads_roles_before_flush` 源码顺序契约守卫：断言 `row.role_items` 赋值位置早于 `db.add(row)`；采用源码契约而非 mock 断言的原因是 mock session 不 emit SQL，对该缺陷**天然假绿**（与 update 路径 delete-orphan 失效同族教训）。**④ 验证**——改回旧写法该守卫即失败（已实测），users API **34 passed** + ruff 0；容器重建后端到端实测：创建用户由 500 → `OK`，多角色正确（主角色 `metric_owner`、`roles=['metric_owner','reviewer']`）；完整改密链路实测通过（弱密码 `PASSWORD_WEAK` 拒、旧密码错 `PASSWORD_INCORRECT` 拒、改密 `OK`、新密码可登录、旧密码失效、`reset-password` 置 `must_change_password=1`、禁用后 `AUTH_INVALID_CREDENTIALS`） | 无接口变更（修复 500） | —（users 保持 released） |
| 2026-08-31 | — | docs | **README 新增「七、数据模型设计」章节（用户『README 缺少数据模型设计』）+ 改密指引**：**① 数据模型章节**——按设计原则（OneData 分层/软删优先/乐观锁/WORM 审计/口径不可变）+ 核心实体关系图（subject_domain→metric→metric_mount→measure_catalog→db_catalog→lineage_edge）+ **分域表清单**（指标域/主数据域/采集与目录域/血缘域/治理与质量域/用户与权限域/审计通知可观测，覆盖 60+ 张表）+ 关键约束（4 段式编码、状态机、多角色并集、注入守卫豁免 §9b）组织；原文「八、领域模块作用」及后续章节顺延为九~十五，子节 `9.x` 同步为 `10.x`，交叉引用 §9.4 → §10.4。**② 改密指引**——默认账号处补 `<details>` 折叠块，给出 UI 自助 / API `POST /users/me/password` / 管理员 `POST /users/{id}/reset-password` 三条途径、密码规则与安全特性（改密吊销 refresh token、强制首登改密、落审计）、账号停用走 `PATCH /users/{id}/status` 置 disabled（系统不提供物理删除以保留审计追溯）。**③ 数据核验**——表名逐一对 DB `SHOW TABLES` 校验（修正 `sql_infer_eval` → 真实表名 `sql_infer_eval_run`）；状态表补「34 个模型模块（70 张表）」。**验证**：doc_sync 校验通过 ✅ | 无接口变更（文档） | — |
| 2026-08-31 | — | frontend(SchemaTable) | **样本值展示由「单格堆叠」改为「样本 1…N」多列平铺（用户『一个表格里展示多个值容易混淆，应像表格那样一行多列、每格一个值』）**：**① 原实现**——`SchemaTable` 用单个「样本值」列，把 3 个脱敏值用 `Space wrap` 塞进同一单元格并附 `+N` 计数与类别 Tag，值与类别混在一格里，读者无法把值按序号对应、样本多时堆成多行；**② 改造**——样本值横向铺开为动态「样本 1 / 样本 2 …」独立列（`MAX_SAMPLE_COLS=8`，列数取当前表实际最多条数，无样本的字段不建列），**每格仅一个脱敏值**，超长值 `ellipsis` 截断 + `Tooltip` 展示完整内容；敏感类别抽出为独立「类别」列（仅当存在 `sample_rule` 命中时才插入，避免空列占位）；`normalizeSamples` 兼容新落库的 `string[]` 与存量单值 `string`；**③ 验证**——Catalogs **54 passed**（+2：多列平铺且各值位于不同 `td`、无样本时不展示样本列与类别列、存量单值兼容为「样本 1」单列 + 身份证类别）+ tsc 0；容器已重建生效 |
| 2026-08-31 | — | collector+frontend | **采样样本多值化——每列保留多条脱敏样本（用户『配置采样行数 5 为什么字段值只有一条』）**：**① 根因**——采样查询虽取 sample_rows 行，但 `_apply_sample` 每列只取**第一个非空值**写入 `columns[].sample`（单值字符串），`sample_rows` 控制的是查询 LIMIT 行数而非每列保留条数，故字段详情始终只有 1 条样本。**② 后端（2b9a71e2）**——`spi._apply_sample` 升级为多值 `_apply_samples`：收集该列全部非空值，按打码值去重、截断到 `_sampling_max_rows` 条，`sample` 落库为**列表**（类别 `sample_rule` 记录首个明文命中）；MySQL/Postgres/ClickHouse/Hive 四连接器的 batch 与逐列降级路径统一收集全部非空值；classifier `detect_pii_fields` 兼容 str/list——`sample_re` 对任一值匹配、掩码佐证任一含 `*`、`PiiFieldHit.sample` 取首条展示。**③ 前端**——`SchemaColumn.sample` 类型 `string[]`（兼容旧单值字符串），`parseSchemaColumns` 统一转数组，SchemaTable 样本值列渲染多条（最多展示 3 条 + `+N` 省略，Tooltip 全量），列宽 150→220。**④ 兼容**——存量单值 `sample` 字符串照常消费（classifier 转单元素列表）；立即采样/重新采集后新样本即为多值。**验证**——后端 collector/classifier/connectors **394 passed**（+6：多值保留去重/上限截断/多值任一命中/仅样本命中 sample_rule/掩码任一佐证/多值落库形态）+ ruff 0；前端 Catalogs **52**（+1：字段详情展示 3 条脱敏样本 + 类别标签）+ tsc 0。影响接口：`schema_json.columns[].sample` 由字符串变为字符串数组（展示/消费层兼容，PII 契约不变）。
| 2026-08-31 | — | frontend(Templates) | **模板实例化/编辑弹窗挂载实体选项框化——源表从采集目录选择、选表后列下拉带出、粒度用粒度管理字典（用户『实例化派生指标挂载实体为什么不是选项框，选表后展示列、粒度支持选项并基于粒度管理』）**：**① 现状**——两弹窗挂载实体此前为自由 Input（源表/度量列/粒度），粒度虽已有 `granularityOptions`（粒度管理字典 `listDictItems("granularity")`）但挂载粒度未接入，源表/列无选择能力。**② 改造（对齐注册指标页 MetricCreate 惰性选择 + 未采集兜底模式）**——新增模块级 `withUncollectedOption`/`tableOptionRender`/`TableSelectOption`（未采集值经输入关键词追加为选项、橙色「（未采集，手动输入）」提示，既满足选项框选择又不破坏既有自由输入能力）；新增 `searchMountTables`（listCatalogs 采集目录表 + source_name）、`loadMountColumns`（选表后按 `schema_def/schema_json.columns` 带出 name+type+comment）、`handleMountSrcTableChange`（切表清空旧列）；**源表 Select** showSearch+惰性展开加载（空关键词列已采集表）+未采集兜底；**度量列 Select** 选源表后自动带出列、未采集列可输入；**粒度 Select** options 用粒度管理字典（`月 (month)` 样式），字典未加载/未收录可输入兜底；默认周期保持 Select。实例化/编辑两弹窗共用选项 state；openInstantiate/openEditTpl 若模板预设了源表则预加载该表列（不清空回填列）。**验证**：前端 Templates **30 passed**（+3：派生类型挂载区 3 个 Select 且无自由 Input、粒度下拉展示粒度字典项；选源表后列下拉带出 pay_amt (decimal) — 支付金额 / order_cnt (bigint) — 订单数；未采集表/列/粒度输入选中后提交 payload.mount 携带 wedw_uncollected_tbl/uncollected_col/day）+ tsc 0；测试基建 beforeEach 由 clearAllMocks 改 resetAllMocks（mockImplementation 不跨用例泄漏）。影响接口：无（纯前端交互升级，提交契约不变）。 | 无接口变更（前端交互升级） | —（semantic/frontend 保持 released；模板挂载实体从手输升级为采集目录/粒度管理驱动的选项框，保留未采集输入兜底） |
| 2026-08-31 | — | collector+frontend | **采样闭环——采集目录立即采样按钮 + 样本值列 + 采样覆盖率条（用户『采样数据是在什么时候进行？为什么没有看到入口』）**：**① 时机澄清**——采样不是独立动作，而是采集流程内嵌步骤（service 采集前从 quota.sample_rows 注入 set_sampling，connector 读表结构后逐批 20 列跑采样 SQL 打码写入 schema_json.columns[].sample），此前无独立按钮且样本无处展示。**② 后端（00c69e8f）**——单表立即采样端点 `POST /data-sources/{source_id}/entities/{entity_name}/sample`（不重跑全量采集，只补采样本 + 重算字段级 PII，sample_rows 显式指定可覆盖配额）；样本识别精确化——采样时对明文判定命中规则的 rule_id 记录到 sample_rule（取代事后靠掩码猜类别导致的跨规则误判），`name+sample` 双重验证置信度 +0.05；**批量采样降级重试**——20 列批次中任一列查询失败（如 Doris+aiomysql 对加密串列的协议兼容缺陷）整批降级逐列隔离，健康列仍可采样；采样覆盖率统计 `GET /data-sources/sampling-coverage`（可选 source_id，表/列占比 + verified_columns 双重验证列数）。**③ 前端（d5a92e54）**——采集目录字段详情抽屉「立即采样」按钮（Tooltip 说明『不重跑全量采集，只补采脱敏样本——让 PII 识别从仅靠字段名升级为字段名+实际值双重验证』，采样后自动重拉 schema 回填样本列）；SchemaTable 新增「样本值」列（脱敏值 + 命中类别橙色 Tag，SAMPLE_RULE_LABEL phone/id_card/email/bank_card 中文化）；筛选区下方采样覆盖率条（表 X/Y、列 Z/W、百分比 + '经 name+sample 双重验证' 绿标，跟随数据源筛选联动）。**验证**——后端 collector 相关 405 passed（+采样/降级/覆盖率测试）+ ruff 0；前端 Catalogs 51（+2：立即采样触发并回填 138****1234 样本值 / 覆盖率条表 4/10 列 30/100 与双重验证）+ SchemaTable 2 + tsc 0。影响接口：新增 1 采样端点 + 1 覆盖率端点（向后兼容，quota.sample_rows=0 不采样保持原行为）。 | 新增端点（可选能力） | —（collector/frontend 保持 released；采样时机=采集内嵌或单表立即采样，样本值/覆盖率在采集目录可见，PII 识别精度可观测） |
| 2026-08-31 | — | frontend(Templates/MetricCreate/MetricDetail) | **原子实例化「为什么还要指定原子指标口径」澄清——术语主词统一 + 原子口径折叠为高级项并预览 stat_caliber（用户『指标模板实例化原子类型为什么还要指定原子指标口径，和 OneData 共识是否冲突』）**：**① 结论澄清（不冲突，且是 OneData 强制项）**——后端 `service.py` 硬卡点 `if request.type=="atomic" and request.measure_id is None: raise ValidationError("原子指标必须关联逻辑度量…", MEASURE_REQUIRED)`，且 `if request.type=="atomic" and (mounts or mount): raise ...ATOMIC_NO_MOUNT`；对齐 `spec/onedata-metric-redesign/plan.md` Decisions 第 5 条「原子指标 = 逻辑度量（measure_catalog）+ 聚合方式，不挂物理表」——不挂物理表后**必须**靠 measure_id 回答「度量什么」，缺失即 422。**② 真实问题（命名撞名 + 无条件展示）**——同一 `measure_id` 字段在平台内两套叫法（注册页/详情页「逻辑度量（原子指标口径库，OneData 原子层）」vs 模板页「原子指标口径（OneData 原子层）」丢失主词），而「口径」在本平台另指 `definition_json` 的业务/SQL/数仓三层口径，用户据此认为与 OneData 冲突；且模板两弹窗的口径定义**无条件渲染**，原子下也让人填物理口径，等于绕回「原子绑物理表」。**③ 术语统一（不推翻既有决策）**——核查发现 `CHANGELOG_MODULES.md` 2026-08-28 P15 曾刻意把「逻辑度量」统一为「原子指标口径」（2026-08-26 因「度量目录对数仓用户不好理解」改名），故**不推翻**该业务命名，改为补回主词的并列式 `逻辑度量（原子指标口径）`，三处一致：Templates 实例化 label/placeholder、实例化详情 Descriptions、编辑弹窗预设 label/placeholder，并同步 MetricCreate（2454 label/2459 extra）与 MetricDetail（2984 label/2985 tooltip）为同一措辞（库名「原子指标口径库」在说明文案中保留便于定位）。**④ 原子口径折叠 + stat_caliber 预览**——新增模块级 `measureStatCaliber(options, measureId)`；实例化与编辑两弹窗的口径定义改为按类型条件渲染：**原子类型**下收进 `Collapse`「高级：补充物理口径定义（一般留空）」默认收起，extra 提示「原子指标的口径由所选逻辑度量继承，一般留空；仅存量旧式指标需补充物理口径」，并在选中/预设逻辑度量时以 Alert **只读预览该度量的 `stat_caliber`**（如「收费明细按结算日期去重后求和」），让用户看见继承来源而非面对空白输入框；**派生/复合**保持原样直接展开（依赖 expression/sql 必须可编辑）。**验证**：前端 Templates **27 passed**（+2：原子实例化折叠为高级项且只读预览 stat_caliber「收费明细按结算日期去重后求和」/ 派生类型不折叠且模式切换器直接可见）、MetricCreate+MetricDetail 全过（术语断言同步）、Templates+MeasureCatalogs+MetricCatalog **106 passed**、tsc **0 错**；既有用例因折叠需先展开（编辑弹窗 3 处、实例化 1 处）已同步交互。影响接口：无（纯前端展示层，API 契约零变化）。 | 无接口变更（纯前端展示层，API 契约零变化） | —（semantic/frontend 保持 released；原子实例化不再要求重复填写口径，且术语与 OneData 共识一致） |
| 2026-08-28 | — | semantic+frontend | **指标模板口径支持 SQL/表达式模式切换（方案 A）+ 模板作用引导样例（用户『默认口径必须是 JSON 吗，不能切 SQL 模式吗』『模板仍没理解其作用，要有引导样例』）**：**① 根因澄清**——模板「默认口径」存于 `defaults_json.definition_json`，后端 `_validate_definition_json` 早已支持 `sql`（sqlglot 语法校验、≤16384）/ `expression`（≤4096）/ `pseudo_definition` / `dw_definition` / `base_atomic` / `source_tables` 多形态，**JSON 只是编辑弹窗的输入形式**；注册指标页（MetricCreate）已有「口径定义模式」Segmented，唯独模板模块没跟上，导致两处弹窗（编辑/实例化）只有裸 JSON 文本域。**② 口径模式编辑器**——新增受控组件 `DefinitionModeEditor`（Segmented 三模式，对齐注册页交互）：**SQL 模式**写 `{sql}`（TextArea + 提示服务端 sqlglot 校验/长度上限）；**表达式模式**写 `{expression}` + 源表 tags 多选（`source_tables`）；**高级 JSON** 保留原文本域兜底（`base_atomic`/`dw_definition` 等特殊键不被砍掉）+ 格式化按钮 + 非法 JSON 实时错误。模式切换只替换 `sql`/`expression` 主口径键（`CALIBER_KEYS`），其余键保留不丢；回填时 `detectDefMode` 按内容反推模式（sql→expression→含其他键则高级）。**③ 提交链路**——编辑弹窗 `payload.defaults_json.definition_json`、实例化弹窗 `payload.definition_json` 改为直接提交结构化对象（不再 `JSON.parse` 字符串），清空口径时移除 `definition_json` 键而非写空对象；**错误回传修复**：高级模式非法 JSON 时 `onChange(value, msg)` 把 error 传给父组件（此前仅组件内 setJsonError，导致提交时 `editDefError/instDefError` 为空、非法内容会被静默保存）。**④ 详情弹窗**——口径按模式展示：模式 Tag（geekblue/cyan/purple）+ 该模式内容原文 + 源表 Tag，SQL/表达式模式不再暴露完整 JSON。**⑤ 引导样例**——模板页新增可折叠引导（默认收起不干扰浏览）：Alert 说明「模板 = 指标样板，实例化 = 一键克隆出规范 DRAFT 指标」+ 原子/派生/复合三类参考样例（预设了什么 / 实例化后得到什么，如派生模板自动落 metric_mount 并生成血缘边）。**验证**：前端 Templates **25 passed**（+6：引导展开显示三类样例、详情 SQL 模式 Tag+原文且不显示 `"sql":` 键名、编辑弹窗含 sql 回填 SQL 模式并预填、切 SQL 模式保存写入 `{sql}`、高级模式非法 JSON 阻止保存、实例化切 SQL 提交 payload.definition_json）+ tsc 0；顺带修正测试文件结构缺陷（describe 被提前 `});` 关闭，导致后 2 个用例落在顶层）。影响接口：无（请求体 `definition_json` 结构不变，仅前端编辑方式升级）。 | 无接口变更（前端编辑体验升级） | —（semantic/frontend 保持 released；模板口径可 SQL/表达式/高级 JSON 三模式填写，模板作用有样例可参照） |
| 2026-08-28 | — | collector+frontend | **Postgres/ClickHouse 连接器采样支持 + 采样开关前端接线（用户『Doris 和 MySQL 是否支持采样』『把 Postgres/ClickHouse 也支持采样』）**：**① 现状核对**——MySQL/Doris/StarRocks 已支持（Doris/StarRocks 工厂返回 InformationSchemaCollector 实例，自动继承 `_sample_columns`）；Hive/Spark（pyhive 直连）、hive_metastore（可选 sample_connection 委托 HiveCollector）已支持；**Postgres/ClickHouse 当时不支持**（仅继承 BaseCollector 的 no-op 默认实现）。**② Postgres 采样**——新增 `_sample_columns`：**PG 方言标识符用双引号**（反引号在 PG 中不被支持，且未加引号标识符会被折叠小写导致驼峰列名查询失败），批量 20 列 `SELECT "c1",... FROM "schema"."tbl" WHERE ... IS NOT NULL LIMIT n`，单 schema 模式裸表名回退 `self._schema`；collect_entity 与 collect 两条路径均接入。**③ ClickHouse 采样**——新增 `_sample_columns` + `_parse_tsv`：走 HTTP API `FORMAT TabSeparated`，结果按列序号取值并对齐列宽（缺列补空），**`\N` 视为 NULL 跳过**（ClickHouse TabSeparated 的 NULL 表示），标识符反引号包裹且经 `_safe_ident` 校验。样本统一经 `_mask_sample` 打码（138****5678/a***@b.com/110101********1234），采样查询失败仅告警不拖垮采集（FR-004）。**④ 采样开关前端接线（发现并修复的真实缺口）**——`quota.sample_rows` 此前**无任何前端入口**（配额表单只有 max_concurrency/max_scan_rows），采样功能在 UI 上无法开启（hive_metastore 即使配了采样连接也不会采样）；DataSources 配额区新增「采样行数」InputNumber（min=0，placeholder=不采样，tooltip 说明留空/0 语义），编辑回显；**并修复既有隐患**：后端 `src.quota = req.quota` 为**整体覆盖**语义，原前端只提交两个键会导致未提交的配额项被连带丢失，现改为以原 quota 为基底合并后再提交。schemas.py 两处 quota 描述补 sample_rows 并标注整体覆盖语义。**验证**：test_connectors **39 passed**（+8：PG 双引号方言 SQL 与打码/未开启不采样/采样失败容错/collect_entity 采样；CH TabSeparated 解析与 `\N` NULL 跳过/未开启不采样/采样失败容错/collect_entity 采样）+ test_connectors/test_collector/test_collector_api **276 passed** + ruff 0；前端 DataSources **43 passed**（+1：提交 sample_rows=20 时保留原有 max_scan_rows=5000，验证合并语义）+ tsc 0。影响接口：无破坏性变更（采样为可选能力，quota.sample_rows 缺省 0 不采样）。 | 无接口变更（可选能力扩展 + 前端接线） | —（collector/frontend 保持 released；MySQL/Doris/StarRocks/Postgres/ClickHouse/Hive/Spark/hive_metastore 均支持全字段打码采样，配额表单可开启） |
| 2026-08-28 | — | collector+governance+assetmap+frontend | **PII 识别增强二/三阶段（用户确认采样脱敏打码/全字段/词表外置/误报闭环全落地）**：**方案 B 词表外置**——classifier 上下文词表重构为可注入 PiiVocab（人名/机构前缀、表语义、健康降级、聚合量词、值型豁免 8 项正则/词条 + 豁免词表 exempt_field 精确/exempt_prefix 前缀），内置默认兜底；rules.load_pii_vocab 从 system_dict（dict_type=pii_vocab）合并加载（同 code 多行逗号合并），service._maybe_load_db_rules 注入 classifier；前端 SensitiveRules 新增「PII 词表」管理 Modal（内置/自定义合并视图、正则类/词条类编辑、启用停用删除回退内置、误报豁免字段/前缀可配置）。**方案 C 误报反馈闭环**——后端 POST /catalogs/classification/{id}/false-positive（COMPL-3）：把误判字段/前缀写入 pii_vocab 豁免词表（幂等合并去重）+ 用含豁免词表的分类器重算实体（绕过只升不降保护——误报是人工确认处置允许降级）+ 全链路审计（action=classification.false_positive，pii_access=True）；前端 AssetMap 实体详情 PII 命中 Tag 旁「误报」按钮（选择精确/前缀粒度 + 必填原因留痕 + 豁免后刷新详情）。**验证**：后端 test_sensitivity_classifier 28（+3 词表注入/豁免/打码样本）+ test_collector_rules 15（+2 merge_vocab/load_pii_vocab）+ test_governance_service 70（+2 误报反馈字段/前缀）+ collector/governance 相关 429+133 passed + ruff 0 + openapi 373 路径；前端 SensitiveRules 14（+1 词表管理）+ AssetMap 69（+1 误报反馈）+ tsc 0。影响接口：新增 1 端点 + pii_vocab 字典（无破坏性变更，豁免词表缺省为空）。 | 新增端点（可选能力） | —（collector/governance/assetmap/frontend 保持 released；词表可配置、误报可一键豁免，无需改代码发版） |
| 2026-08-28 | — | collector | **drift-logs 接口 500 修复——`DriftLogResponse.after_schema` 放宽为可空（用户『GET /data-sources/mysql_unisense/drift-logs 500 Internal Server Error』）**：**① 根因**——service 层 `list_drift_logs` 按 P2-17 设计（列表不返回全量 schema，repository `load_only` 排除 before/after_schema 大 JSON 避免懒加载）把 `after_schema` 硬编码置 `None`，但 `DriftLogResponse.after_schema: dict[str, Any]` 声明为**必填**——pydantic 校验 None 直接 500（`Input should be a valid dictionary, input_value=None`）；`before_schema` 早已放宽为 `| None = None`，唯独 `after_schema` 漏改（P2-17 落地时漏同步 schema）。**② 修复**——`schemas.py` `after_schema: dict[str, Any] | None = None`（与 before_schema 对齐，向前兼容）；前端早已支持 null（api.ts `after_schema: Record<string, unknown> | null`、CollectionHistory 用 diff_json 渲染、AssetMap diff 空值安全），无需改动。**③ 验证**——test_collector_api drift-logs 端点测试对齐真实 service 行为（mock after_schema=None 断言 200 + after_schema is None）+ test_collector service 测试补「after_schema 恒 None」契约断言 + collector API/service 全量 237 passed + ruff 0；容器热更新 + 重启后真实端点 500→401（未认证），容器内 `DriftLogListResponse(after_schema=None)` 校验通过。影响接口：无（响应字段语义：列表接口 after_schema 为 null，前端已兼容）。 | 无接口变更（响应字段语义修正） | —（collector 保持 released；drift 变更审计列表可正常加载） |
| 2026-08-28 | — | collector+frontend | **全字段样本采样框架（PII name+sample 双验证，用户确认采样脱敏打码/全字段/词表外置/误报闭环方向的第一步）**：**① spi 采样钩子**——BaseCollector 新增 set_sampling（quota.sample_rows 注入，0=不采样）+ sample_columns（默认原样返回，连接器覆盖）+ _mask_sample（委托 classifier）；**② classifier 打码**——mask_sample 对手机/身份证/邮箱/银行卡打码保留格式特征（138****1234/110***********1234/ab***@domain/6222****7890），非敏感值原样；detect_pii_fields 对含掩码标记（****）的样本视为 name+sample 双验证（打码证明是敏感列，置信度 +0.05）；**③ hive_metastore sample_connection**——HMS 元数据库只含表结构不含数据，采样必须直连 Hive 计算引擎：connection_config 新增可选 sample_connection（host/port/user/password/auth 指向 HiveServer2），工厂解析构造 HiveCollector 采样器，collect 复用同一连接逐表采样；schemas 四入口（create/update/test-connection/list-tables）统一校验（提供时 host 必填）；**④ SSRF 加固**——ssrf._extract_hosts 递归提取 sample_connection.host（采样连接采集时真实连接执行 SELECT，必须与主连接同等 SSRF 校验，防绕过向量）；**⑤ 连接器采样**——mysql（InformationSchemaCollector 系）/hive（HiveCollector）实现批量列采样（每批 20 列 SELECT ... WHERE col IS NOT NULL LIMIT n，逐列取首个非空值打码写入 columns[].sample，非法标识符跳过防注入，采样失败不拖垮采集）；**⑥ 前端表单**——DataSources hive_metastore 类型新增「采样连接（可选，PII 识别增强）」折叠区（采样 Host/Port/User/Password），编辑回显、buildConnectionConfig 组装 sample_connection、configChanged JSON 比较自动纳入。**验证**：test_sensitivity_classifier 25（+2 mask_sample/打码样本触发 name+sample）+ test_hive_metastore 17（+3 采样）+ test_connectors 50（+2 Hive 采样/不采样）+ test_collector 209（+3 MySQL 采样/不采样/schemas 校验）+ test_ssrf 15（+1 sample_connection.host 纳入校验）+ collector 相关全量 420 passed + ruff 0；前端 DataSources 42 passed（+1 采样连接携带）+ tsc 0。影响接口：无破坏性变更（sample_connection 可选，缺省不采样保持原行为）。 | 无接口变更（可选新增配置） | —（collector/frontend 保持 released；采样为可选能力，配置 sample_connection + quota.sample_rows 后启用 PII name+sample 双验证） |
| 2026-08-28 | — | collector+governance | **PII real_name 规则上下文判定——裸 name 列误判修复（用户『village_department_count_* 为什么也算命中合理，要保证准确性』）**：**① 根因**——real_name 规则 `\bname\b` 把机构/地点表的裸 `name` 列（村名/机构名/病房名）误判为个人姓名，全库 15,186 条 PII 判定中 3,921 条（26%）因裸 name 命中（如 wedw_tmp.village_department_count_* 的 name 列）；同时 `\bname\b` 因 `_` 是单词字符匹配不到 `patient_name` 等带人名前缀的 `*_name` 列（既有漏判）。**② 修复**——① 内置 real_name 规则 name_re 由 `\bname\b` 改为 `_?name$`（所有 `*_name` 列统一进入 real_name 分支），DB 规则经迁移 0117 幂等同步；② 引擎层新增 `_is_person_name` 上下文判定——人名前缀（patient_name/用户姓名/student_name 等）判 PII，机构/地点/技术前缀（village_name/table_name/村名 等）不判，裸 name 依表名语义（patient/student 表→姓名，village/org 表→非姓名），无法判断保守不判（留人工复核）；③ matched_by 支持 name+comment（裸 name 列注释明确写「姓名」仍判 PII）；④ health 规则同源修复：org_name/ward_name 等机构字段不因注释含「医疗」等词判健康 PII（`_is_health_pii_field`：明确健康词保留、机构词降级）。**③ 存量重算**——容器内脚本按新规则重算 15,741 条 classification：1,217 条裸 name 误判降级 NEEDS_REVIEW（留人工复核）、11 条 patient_name/user_name 漏判升级 PII、student/teacher 等人员表恢复 PII、43 条仅靠 health 机构字段撑 PII 的表降级；health 机构字段命中清零，`*_name` 误判清零。**验证**：test_sensitivity_classifier 25（+4：裸 name 表语义判定/带前缀 person vs entity/注释命中保留/health 机构字段降级）+ collector/governance/sensitive_rules 相关 425 passed + ruff 0；真实 DB 重算后 village_department_count_* 降级 NEEDS_REVIEW 且 pii_columns 清空、emr 病历表明细剔除 org_name/ward_name 等机构字段仅剩真实 PII。影响接口：无（规则引擎行为修复 + 存量数据重算）。 | 无接口变更（规则引擎行为修复） | —（collector/governance 保持 released；机构/地点 name 列不再误判 PII，patient_name 等漏判补正） |
| 2026-08-28 | — | collector+governance+assetmap | **PII 规则引擎误判修复——统计量词字段与子串误判全面排除（用户『资产地图 PII 合规实体详情的字段清单不准确，连 *_rate/*_cnt 指标类字段也算 PII』）**：**① 根因**——① 统计字段误判：`*_cnt`/`*_rate` 聚合统计字段（health_exam_cnt 体检次数、blood_pressure_compliance_rate 血压达标率、call_connected_cnt 电话接通数、gxy_manage_rate 高血压管理率 等）被健康/电话规则命中（含注释命中）标为 PII——它们是群体计数/比率，不指向个体；② 无边界子串误判：`population` 命中 gps 规则的 `lat`、`thyroid_nodules` 命中 id_no 的 `id_no`、`hotel` 命中 phone 的 `tel`、`composition` 命中 position（DB 16 条 pii_rule 覆盖内置且无词边界，实测 15,741 条 classification 中 15,000+ 含误判）。**② 修复**——① classifier 引擎层统计量词排除：`_AGGREGATE_RE`（_cnt/_count/_rate/_ratio/_pct/_avg/_sum/_total/_amount/_times 等，可带分桶编号 _cnt1/_cnt_1d/_cnt_zjwz_180d）字段不因 name/comment 命中判 PII（仅样本命中保留），值型豁免前缀 heart_rate/心率（心率是个人测量值仍保留）；② 词边界：新增 `_tok()`（非字母数字边界，_ 视为边界兼容 doctor_id_no 命名）应用至 gps(lat/lng/position)/phone(tel)/id_no/cert_no/license_no/bank_card(card_no/account_no)/address(addr) 内置规则；③ 迁移 0116 幂等同步 DB 16 条内置 pii_rule 的 name_re（自定义规则保留）；④ 数据修复——容器内脚本按新规则重算 15,741 条存量 classification.pii_columns（15,188 条更新），规则自动判定 PII 且重算后无高置信度命中的 121 条降级 NEEDS_REVIEW（留人工复核，不触碰人工已复核记录）。**验证**：test_sensitivity_classifier 21（+7：统计后缀不判 PII/注释命中统计字段不判/分桶编号/多段后缀/heart_rate 值型豁免/样本命中保留/gps 与 id_no 与 tel 子串不误判）+ collector/sensitive_rules/governance/assetmap PII 相关 305 passed + ruff 0；真实 DB 重算后 `*_rate`/`*_cnt` 误判清零（残留仅 heart_rate 系列合理保留），121 条待复核。影响接口：无（规则引擎行为修复 + 存量数据重算）。 | 无接口变更（规则引擎行为修复） | —（collector/governance/assetmap 保持 released；统计指标字段不再误标 PII，人口/甲状腺结节/酒店等业务字段不再子串误判） |
| 2026-08-28 | — | assetmap+frontend | **资产地图实体详情展示升级——TABLE 详情补字段清单（含 PII 命中）+ 血缘边明细图形化（用户『概览 PII 待复核点开 TABLE 实体详情没有字段信息』『血缘明细为什么是文字不是图』）**：**① 根因**——OverviewTab 实体详情抽屉只渲染基本信息 Descriptions，未渲染后端已返回的 schema_summary 字段清单；血缘边明细用纯 Table 文字展示（源/目标/类型/粒度四列），未复用项目已有的 AssetGraph G6 图谱能力；**② 字段清单**——OverviewTab 详情抽屉 Descriptions 后新增「字段清单」Card（renderSchemaSummary 复用 SchemaTable：字段名/类型/注释/描述），Card 标题随 `pii_field_count` 显示「含 N 个 PII 字段」红 Tag，PII 命中字段按 `pii_fields`（category 用 PII_CATEGORY_LABEL/COLOR 中文标签着色）逐一展示——待复核入口进入的 TABLE 详情字段级 PII 一目了然；**③ 血缘图形化**——提取公共组件 `EntityLineageCard`（血缘图谱 Tabs：默认 AssetGraph G6 图，`buildEntityLineageGraph` 把 lineage_edges 的 source/target 前缀节点转 nodes/edges，节点按类型着色、label 去前缀；可切换「边明细列表」保留原文字表格），统一替换 OverviewTab/GraphTab/TablesTab/变更追踪 ChangeCatalogDetailDrawer 四处血缘边明细 Table——全平台详情抽屉血缘均直观图形化；**④ 类型**——AssetEntityDetail 原已有 pii_fields/pii_field_count/pii_categories（后端 repository get_entity_detail 返回）。**验证**：前端 AssetMap **68 passed**（+1：overview 待复核下钻点击行→实体详情展示字段清单含 patient_id/「含 1 个 PII 字段」/phone（手机/电话）命中 Tag + 血缘关系（2 条边）默认图谱 Tab 可切边明细列表见源表名）+ tsc 0。影响接口：无（纯前端展示层，API 契约零变化）。 | 无接口变更（纯前端展示层，API 契约零变化） | —（assetmap/frontend 保持 released；TABLE 详情展示字段级 PII 信息、血缘以图谱直观呈现） |
| 2026-08-28 | — | semantic+conflict+dimension+sensitive_rules | **SQL 注入守卫误伤场景全面补全——conflicts/check + sensitive_rules 正则 + dimension 映射表达式豁免 + 文档约定（审查发现『-- 注释/UNION/块注释/多语句被误伤』系统排查后收敛）**：**① 全面排查**——读取 guard.py 全部检测规则 + 全库 30+ 守卫接入端点 + 各请求 schema，容器内 `_is_suspicious` 实测 23 组：会命中合法输入（`--` 行注释 / `UNION [ALL] SELECT` / `/* */` 块注释 / `; DELETE...` 多语句 / `sleep(` / markdown 表格 `|---|`），放行日期减法 cron 等；已豁免安全（metrics create/update、batch-register/import、parse-sql、auto-suggest、refine、lineage 全系列、collector connection_config）；仍误伤点收敛为 3 类。**② 修复（三处豁免）**——conflict `/check` 新增 `_CHECK_DEPS`（`guard_against_injection_exempt_paths` 豁免 `candidate.definition`/`candidate.definition_json`/`existing[].definition`/`existing[].definition_json`，注册向导冲突预检把 sqlText 放 definition、完整口径放 definition_json，此前与 create/update 同链路遗漏）；sensitive_rules create/update 用 `_REGEX_WRITE_DEPS` 豁免 `name_re`/`sample_re`、validate-regex 用 `_REGEX_CHECK_DEPS` 豁免 `pattern`（正则文本 `--.*`/`/*` 本身非载荷，仅 re.compile 不执行）；dimension `/mappings` 用 `_MAPPING_DEPS` 豁免 `expression`（映射表达式文本，仅落库回显）。**③ 文档约定**——DEV_GUIDE 新增 §9b「SQL 注入守卫豁免与文本输入约定」（可豁免/不可豁免原则 + markdown 表格分隔符 `|---|` 规避：全角 `｜` 或 `&#124;` 转义）。**验证**：test_guard 41（+6：conflict check 嵌套路径豁免/同层仍扫描/正则字段豁免/正则其他字段仍扫描/mapping 表达式豁免+其余字段仍扫描）+ conflict/sensitive/dimension 相关 213 全绿 + ruff 0；doc_sync 通过。影响接口：无（API 契约零变化，仅守卫豁免行为）。 | 无接口变更（API 契约零变化） | —（semantic/conflict/dimension/sensitive_rules 保持 released；注册向导冲突预检、敏感规则正则、维度映射表达式不再被注入守卫误伤） |
| 2026-08-28 | — | assetmap+frontend | **指标 PII 合规统计口径修复——排除已废弃指标 + 无 PII 指标空态（用户『概览指标 PII 合规 100% 数据是否准确』）**：**① 根因**——指标 PII 合规率 = 已复核 PII 指标 / 全部 PII 指标，但统计未排除 DEPRECATED 状态，且无 PII 指标时返回 0.0（前端显示红色 0%）；实证：10 个指标中唯一 PII 指标是已废弃的 E2E 测试指标（outp_e2e_piipatient_day，compliance_reviewed=True 经真实合规复核），使合规率显示 100%——数学准确但严重误导（废弃测试指标撑起合规门面，生产指标 PII 标记覆盖率为 0）；**② 后端**——metric_dimension_summary 的 pii_total/pii_reviewed 增加 status != DEPRECATED 过滤（已废弃指标已下线、不再对外服务，其复核状态无意义）；pii_total=0 时 review_rate 返回 None（区分「无 PII 指标」与「有 PII 但 0% 合规」，避免 0/100 误导）；**③ 前端**——指标 PII 合规卡 review_rate=null 时展示「暂无有效 PII 指标（已废弃不参与统计）」空态（合规率「—」），有值时正常显示百分比+红绿阈值；类型 review_rate 改为 number | null。**验证**：后端 test_assetmap_repository 110（+2：DEPRECATED 过滤 SQL 断言 / pii_total=0 review_rate=None 空态）+ ruff 0；前端 AssetMap **67 passed**（+1：review_rate=null 空态展示）+ tsc 0。影响接口：GET /assetmap/metric-dimensions 响应 pii_compliance.review_rate 在无 PII 指标时为 null（此前 0.0，向前兼容——前端已按 null 处理）。 | 无接口变更（响应字段语义：无 PII 指标时 review_rate 由 0.0 改为 null） | —（assetmap/frontend 保持 released；废弃测试指标不再撑起虚假 100%，无 PII 指标时明确展示「暂无」） |
| 2026-08-28 | — | semantic | **注册指标创建被注入守卫误伤 400 修复——单条创建/更新的口径字段豁免（用户『注册指标引导流程创建提示疑似sql注入』）**：**① 根因**——`POST /metric-definitions` 创建与 `PUT /{code}` 更新用 `_WRITE_DEPS` 全量注入扫描，而 `definition_json` 承载 `sql`/`expression`/`business_filter` 等 SQL 口径文本、`raw_sql` 承载 ETL 原文——合法 ETL 的 `--` 行注释 / `/* */` 块注释 / `UNION ALL SELECT` / 多语句被注入正则误伤（容器内实测三种形态全部 `_is_suspicious=True`），返回 400 INJECTION_DETECTED；批量端点早已用 `_SQL_BATCH_REGISTER_DEPS` 豁免 `candidates[].definition_json`/`candidates[].raw_sql`（注释记录过同类 400），单条端点遗漏；**② 修复**——新增 `_METRIC_WRITE_DEPS`（RBAC + `guard_against_injection_exempt("definition_json","raw_sql")`，与批量端点同源语义：仅落库存储/被 sqlglot 纯函数解析、不执行不拼接进 DB 查询），create/update 两端点接入；`metric_code`/`name`/`key` 等其余字段与 query 参数仍全量扫描（纵深防御不削弱）。**验证**：后端 test_metric_lifecycle_api 27（+2：definition_json 含 `--` 注释/UNION SELECT 创建放行 201 / metric_code 含 `--` 仍 400 INJECTION_DETECTED 豁免不削弱）+ ruff 0；真实容器端到端——含 SQL 注释的创建请求由 400 INJECTION_DETECTED 变为 422 schema 校验（守卫已放行）。影响接口：无（API 契约零变化，仅守卫豁免行为）。 | 无接口变更（API 契约零变化） | —（semantic 保持 released；注册向导可正常提交含注释/UNION 的口径 SQL） |
| 2026-08-28 | — | assetmap+frontend | **资产地图治理弹窗带入当前责任人/敏感度 + 孤儿资产与数据表支持库过滤 + 治理范围澄清（用户『设置资产治理信息时明细展示有责任人却没带到编辑弹窗』『描述缺失治理与孤儿资产、数据表在不同模块有些混乱』『孤儿资产和数据表也要支持库过滤筛选』）**：**① 治理弹窗带入当前值**——`openGov` 增加 prefill 参数（单实体打开时 `govForm.setFieldsValue` 带入明细展示的 owner_id/sensitivity_level，批量多实体无统一现状不预填保持「留空不修改」语义）；行「设置」与详情抽屉「设置」两入口均透传当前责任人/敏感度；表单说明文案更新为「单条已带入当前责任人」。**② 孤儿资产/数据表库筛选**——后端 `list_tables`/`orphan_assets`（repository+service+API）新增 `database` 过滤（entity_name 前缀 `LIKE '{db}.%'`，通配符转义 + ESCAPE '/'，对齐采集目录 description-coverage 库筛选）；前端两 Tab 各加「全部库」下拉（`listCatalogDatabases` 同源、随数据源联动收窄、重置一并清空）。**③ 治理范围澄清**——数据表目录/孤儿资产两 Tab 顶部新增治理范围横幅（数据表=资产级信息 责任人/敏感度/废弃/表描述；孤儿=责任人认领/转交），附「前往采集目录」跳转按钮引导字段描述治理，明确与采集目录描述缺失治理的分工。**④ 顺带**——修 stale 集成测试 `test_orphan_assets_executes`（P2-1 起 service 返回 `(items, total)` 元组，旧断言 `isinstance(items, list)` 已失效）。**验证**：后端 test_assetmap_repository 66（+3：list_tables 库过滤 SQL 形态 + 通配符转义 / orphan_assets 库过滤）+ test_assetmap_service 43（+1：database 透传）+ 集成 1 修复 + ruff 0（security 15 项失败为 master 既有多角色 mock 未同步，与本次无关）；前端 AssetMap **66 passed**（+3：治理弹窗带入当前责任人/敏感度且保存提交两个值 / 数据表库筛选触发 database 请求 / 孤儿库筛选）+ tsc 0。影响接口：GET /assetmap/tables、GET /assetmap/orphans 新增可选 database 查询参数（向后兼容）。 | 无接口变更（两 GET 端点新增可选 database 参数，向后兼容） | —（assetmap/frontend 保持 released；治理弹窗直接带入明细当前值、孤儿/数据表可按库聚焦、治理范围一目了然） |
| 2026-08-28 | — | assetmap+collector+frontend | **资产地图概览 PII 合规数据不准确修复——补「目录资产 PII 合规」卡 + 指标卡降级（用户『概览模块展示的 PII 合规对应的数据不准确吧』）**：**① 根因**——概览「PII 合规」卡只统计指标（metricDims.pii_compliance，pii_flag=True 指标中已复核比例），而平台核心资产是目录资产（db_catalog 表/字段）——实证 22,954 资产中敏感 16,704（PII 15,321 + CONFIDENTIAL 1,383）已复核 0，指标卡却显示 100%，严重误导；指标 pii_flag 创建默认 False、血缘传播 best-effort，分母严重低估；两套 PII 复核机制（指标 vs 目录资产）并存但概览只展示指标维度；**② 后端**——`catalog_summary` 新增 `pii_compliance` 聚合（目录资产口径：sensitivity_level IN (PII,CONFIDENTIAL) 且未删，按敏感度×复核态分组；返回 sensitive_total/reviewed/pending/compliance_rate（无敏感资产视为 100）/by_sensitivity，口径对齐 observability pii_review_pending）；`list_catalogs` 新增 `pending_review` 过滤参数（DBCatalogListParams + repository 两处：sensitivity IN (PII,CONFIDENTIAL) 且 compliance_reviewed=False，供下钻待复核明细）；**③ 前端**——概览新增「目录资产 PII 合规」卡（合规率 Statistic 红绿阈值 + 敏感资产明细「已复核 x / y（PII a / CONFIDENTIAL b）」+ 「待复核 n 项」Tag 点击下钻 `drillCatalogs({pending_review:true})` 待复核敏感资产明细）；原指标卡标题「PII 合规」→「指标 PII 合规」明确范围；`AssetCatalogSummary` 类型加 `pii_compliance`。**验证**：后端 test_assetmap_repository 63（+2：pii_compliance 聚合 1 复核 3 待复核 合规率 25 / SQL 形态 IN PII,CONFIDENTIAL + GROUP BY）+ test_collector（+1 pending_review 过滤含 PII/CONFIDENTIAL + compliance_reviewed=0，缺省不加条件）+ 270 passed + ruff 0；前端 AssetMap **64 passed**（+1：目录资产 PII 合规卡渲染 25% + 已复核 1/4 + 待复核 3 下钻调用 pending_review=true；顺带补齐并行会话遗漏的 listCatalogDatabases mock）+ tsc 0。影响接口：GET /assetmap/summary 响应新增 pii_compliance 字段、GET /catalogs 新增可选 pending_review 查询参数（均向后兼容）。 | 无接口变更（GET /assetmap/summary 响应新增 pii_compliance；GET /catalogs 新增可选 pending_review，向后兼容） | —（assetmap/collector/frontend 保持 released；概览真实反映目录资产 PII 合规——敏感 16,704 复核 0% 一目了然，待复核可一键下钻） |
| 2026-08-28 | — | metric+frontend+consume | **组合粒度方案 B 落地——主粒度 + 粒度维度（granularity_dims）端到端（用户『按月+医院统计订单总金额算什么粒度』『注册指标时选粒度是不是也遇到一样的问题』）**：**① 语义**——单值 granularity 无法表达组合粒度（唯一性维度集合），粒度维度（参与唯一性的业务实体，如医院）此前被压成普通维度语义错误；方案 B 拆「主粒度（时间频率，什么时候的）+ 粒度维度（业务实体，谁的）」；**② 数据**——迁移 0114 metric_mount 加 granularity_dims（JSON）；**③ 推断**——extract_grain_and_dims 返回 (主粒度, 粒度维度, 普通维度)，GROUP BY 业务实体键全部升级为粒度维度（GROUP BY month_id, hospital_id → 主粒度 month + 粒度维度 [hospital]），单条/批量两路径共用；**④ 挂载**——粒度维度变化与改粒度/源表同级破坏性（PUBLISHED 走 PENDING_VERSION 确认流），快照/转正/变体责任方透传；**⑤ 消费**——QueryRequest 加 granularity_dims（显式粒度维度子集校验），粒度维度并入可用维度集（可按粒度维度过滤，未知粒度维度拒绝）；**⑥ 前端**——挂载行粒度拆「主粒度（时间单选）+ 粒度维度（tags 多选）」、批量候选加粒度维度编辑、详情页挂载卡逐行展示「粒度: 月 + 医院」。**验证**：后端 563 passed + ruff 0；前端 MetricCreate 93 + MetricDetail 87 + tsc 0；真实 DB 迁移 0114 应用成功 + JSON 持久化验证（回滚未污染）。影响接口：MetricMountInput/Create/Update/Response、SqlBatchCreateCandidate、QueryRequest 新增 granularity_dims（向后兼容新增）。 | 无接口变更（挂载/查询请求响应新增 granularity_dims，向后兼容） | —（metric/frontend/consume 保持 released） |
| 2026-08-28 | — | assetmap+frontend | **资产地图设置责任人 409 根因修复——敏感度+责任人同弹窗并行提交乐观锁竞态（用户『POST /assetmap/entities/569/owner 409 Conflict』）**：**① 根因**——治理弹窗 `handleGovSubmit` 把 `reclassifyAssetSensitivity` 与 `assignAssetOwner` 放进 `Promise.all(calls)` 并行执行；两者共享 `db_catalog.row_version` 乐观锁，先提交者把版本 +1、后提交者 `WHERE row_version=旧值` 匹配 0 行 → `OPTIMISTIC_LOCK_CONFLICT` 409（审计实证：同一用户对实体 569 4 秒内 3 次 reclassify 成功 + owner 必 409）；**② 前端修复**——同弹窗多字段写改为**串行**（先敏感度、再责任人，第二次读取基于第一次提交后的最新版本）；409 冲突时自动刷新列表并提示「已为你刷新最新数据，请重试」而非裸错误；**③ 后端加固**——`assign_owner`/`reclassify_sensitivity`/`batch_assign_owner`/`batch_reclassify` 增加**无变化短路**（owner/敏感级未变不写库、不 bump row_version，避免人为版本膨胀与虚假冲突）。**验证**：前端 AssetMap **63 passed**（+1：串行提交回归——reclassify 未完成前 assign 不得发起）+ tsc 0；后端 test_assetmap_service **42 passed**（+4：单条/批量 no-op 短路不写库）+ ruff 0（security 15 项失败为 master 既有多角色 mock 未同步，与本改动无关）。影响接口：无（API 契约零变化，仅写路径行为：同值提交不再 bump 版本）。 | 无接口变更（API 契约零变化） | —（assetmap/frontend 保持 released；同弹窗改敏感度+责任人不再 409） |
| 2026-08-28 | — | assetmap+frontend | **资产地图概览多维度资产统计（用户『概览只统计了总目录资产数，没有按数据源、库等多维度统计』）**：**① 后端**——`catalog_summary` 新增 `by_source`（按数据源分布：LEFT JOIN data_source 取源名称，含已软删源名称保留追溯，source_id/source_name/count 列表按 count 降序）与 `by_database`（按库分布：`substring_index(entity_name,'.',1)` 拆库名，排除无点号异常行，database/count 列表按 count 降序），聚合走既有 cache-aside TTL30s；**② 前端**——概览页新增「按数据源分布」「按库分布」两个 Bar 分布区块（@ant-design/charts，点击柱体下钻 `drillCatalogs({source_id})`/`({database})` 到该源/库的目录资产明细，`drillCatalogs` 扩展 source_id/database 参数与标题——数据源标题用 source_name、库标题用库名），空数据 Empty 占位；`AssetCatalogSummary` 类型加 `by_source`/`by_database`（可选，向后兼容）。**验证**：后端 test_assetmap_repository 63（+2：by_source 名称兜底 null→source_id + by_database 列表断言 / 聚合 SQL 形态——join data_source、substring_index、LIKE %.%、count 降序）+ assetmap 101 passed + ruff 0；前端 AssetMap **62 passed**（+1：概览渲染「按数据源分布」「按库分布」区块）+ tsc 0。影响接口：GET /assetmap/summary 响应新增 by_source/by_database 字段（向后兼容新增）。 | 无接口变更（GET /assetmap/summary 响应新增 by_source/by_database 字段，向后兼容） | —（assetmap 保持 released；概览按数据源/库多维度统计资产数，点击可下钻明细） |
| 2026-08-28 | — | frontend | **模板必填字段下拉可选清单 + 详情弹窗责任方真实姓名（用户『必填字段这个选项框没有值可选择』『详情展示的责任方怎么都是用户 #3』）**：**① 必填字段无可选项**——编辑/创建模板弹窗的「必填字段」`Select mode="tags"` 无 `options`，只能手输字段名、没有下拉可选值；新增 `REQUIRED_FIELD_OPTIONS` 清单（21 项：metric_code 指标编码 / name 指标名称 / domain 业务域 / type 指标类型 / granularity 统计粒度 / measure_id 逻辑度量 / mount 挂载实体 / unit 单位 / currency 币种 / aggregation 聚合方式 / time_semantics 时间语义 / freshness 新鲜度 / dw_layer 数仓层 / metric_tier 指标分级 / serving_mode 服务模式 / additivity 可加性 / definition_json 口径定义 / pii_flag PII 标记 / product_owner_id 产品需求方 / tech_owner_id 技术方 / dw_developer_id 数仓开发——value 对齐 MetricCreateRequest 字段名，后端实例化校验 `f not in merged` 按此名查找），下拉可选、tags 仍可自由输入；**② 责任方全显示「用户 #N」**——详情弹窗渲染责任方时 `product_owner_name || 用户 #id` 硬编码回退，未用 `users` 列表把平台用户 id 解析为姓名（RoleOwnerSelect 选平台用户只存 id 不带 name）；新增 `ownerLabel(id, name)` 辅助——外部人员 name 优先，其次 `users.find(id)` 解析 `display_name`/`username`，仅查不到才兜底「用户 #N」；**③ 顺带**——列表列/详情/实例化提示的必填字段从英文 code join 改为中文业务名展示（`requiredFieldLabel` 映射）。**验证**：前端 Templates **19 passed**（+2：详情弹窗责任方 id=2→Bob、id=1→Alice 且不再出现「用户 #N」/ 编辑弹窗必填字段下拉出现指标编码/指标名称/统计粒度选项）+ tsc 0。影响接口：无（纯前端展示/选项增强，API 契约零变化）。 | 无接口变更（纯前端展示层修复 + 必填字段下拉选项增强） | —（frontend 保持 released；模板必填字段有可选清单、详情弹窗责任方显示真实姓名） |
| 2026-08-28 | — | frontend | **资产地图 Owner 视图责任人下拉显示真实姓名（用户『筛选框为什么展示"责任人#3"』）**：根因——OwnerTab 从图谱节点提取 `owner` 后，下拉 label 直接用模板串 `责任人 #${id}`（如「责任人 #3」），未像资产地图其他 Tab 那样用 `listUsers` 解析真实姓名；而 `fetchAssetOwnerView` 返回的 `owner_name` 已有真实姓名（后端 owner_aggregation 从用户表取 display_name），仅下拉/未加载视图前的名字是 id 占位。**修复**——OwnerTab 并行加载 `listUsers()` 构建 `userMap`（display_name 优先、回退 username），下拉选项/视图名/drillTitle 均用 `userMap[id]` 解析真实姓名，**仅在用户表查不到该 id 时才降级「责任人 #id」**（诚实展示，不再有"责任人 #3"这种可读性差的占位）；拉取失败静默降级不阻塞 Owner 视图（强韧性）；空态行为不变（无 owner 仍展示「从图谱提取责任人…」，不伪造用户）。**验证**——tsc 0；AssetMap **61 passed**（+1：下拉展示真实姓名「管理员」而非「责任人 #1」）+ owner view 4 项全过。影响接口：无（纯前端展示层，API 契约零变化）。 | 无接口变更（前端展示层修复，API 契约零变化） | —（frontend 保持 released；Owner 视图责任人下拉显示真实姓名，不再出现「责任人 #N」占位） |
| 2026-08-28 | — | infra | **OpenAPI Schemas 区域中文化（用户『Schemas 为什么也是全英文的』）**：上一轮中文化只覆盖 tag/summary/info，Swagger UI 5.x 底部 Schemas 区域仍全英文——pydantic 自动从类名生成英文 `title` 作为 schema 渲染标题，故 MetricsResponse/ApiResponse[XXX]/DimensionResponse 等仍是英文。**修复**——`openapi_localize.py` 扩展三块：① 新增 `SCHEMA_ZH` 覆盖 **200+ 核心业务 schema**（指标/维度/术语/度量/挂载/血缘/治理/采集/通知/AI/订阅/认证/字典/敏感/质量 等）title 中文化（MetricResponse→指标详情响应、MetricCreateRequest→新建指标请求、DimensionResponse→维度响应、ConflictStatus→冲突状态、QualityRuleType→质量规则类型、FavoriteAssetType→收藏资产类型 等）；② **ApiResponse 类 + 79 个 ApiResponse[X] 模板实例** title 改「统一响应信封[内部类型的中文名]」（同步翻译嵌套类型）；③ 新增 `FIELD_TITLE_ZH` 覆盖 **80+ 高频字段** title 中文化（code→业务码、message→提示信息、data→业务数据、trace_id→链路追踪 ID、id/status/type/created_at/updated_at/granularity/aggregation/domain/source_table/source_column/metric_code/owner_id/page/page_size/total/description/items/reason/action 等通用字段）。**关键约束**：仅修改 schema/字段 title，**字段名（properties key）与 description 完全不动**→ API 契约零变化；未命中映射的 schema/字段保留 Pydantic 默认英文 title（不破坏契约，新接口不需改映射即可展示）；幂等——二次 localize 不会清空已有中文 description。**验证**——`test_openapi_localize` **14 passed**（+8：ApiResponse 类 title / 79 模板实例 title / SCHEMA_ZH 命中 / 未映射保留英文 / 字段 title 不覆盖 description / 字段 key 不变 / 二次 idempotent / 未映射字段保留）+ ruff 0；**真实容器 Playwright 截图**——8180/docs 展开 Schemas：接入方状态、统一响应信封、统一响应信封[Any]、统一响应信封[批量推断历史项]、统一响应信封[采集目录响应] 等中文标题；展开统一响应信封字段：code→业务码 / Default OK、message→提示信息 / Default success、data→业务数据、trace_id→链路追踪 ID。影响接口：无（仅 OpenAPI 展示层中文化，API 契约零变化）。 | 无接口变更（OpenAPI 展示层中文化，API 契约零变化） | —（infra 保持 released；OpenAPI Schemas 区域中文化覆盖完成，接口定义、响应信封、嵌套类型、字段描述全部中文） |
| 2026-08-28 | — | frontend+semantic | **创建指标 422 根因修复——指标编码 4 段前端实时校验 + 模板实例化编码留空自动生成（用户『POST /metric-definitions 422 Unprocessable Entity』）**：**① 根因**——后端 `MetricCreateRequest.metric_code` 对显式值严格校验 4 段式（域_业务对象_度量_统计周期），但前端注册向导/批量候选/模板实例化均无格式预校验——向导手动填 3 段编码提交即 422；更严重的是**模板实例化把模板 code（3 段标识，如 tpl_gmv_daily）错误预填为指标编码**，后端 `MetricCreateRequest(**merged)` 校验 422「当前仅 3 段」；**② 修复**——新建 `frontend/src/utils/metricCode.ts` 共享校验 `validateMetricCode`（与后端 ConflictPrechecker.validate_code_format 同规则：split 数段 ==4 + 每段 `^[a-z][a-z0-9_]*$`，注意不能依赖整串正则——`[a-z0-9_]*` 会吞段间下划线致 a_b_c_d_e 误判），接入三处：注册向导编码 Form.Item rules、批量候选编码输入框（渲染时红框+错误提示）、Templates 编码 Form.Item rules；批量创建提交前逐候选预校验（非 4 段整批拦截并指出候选）；**③ 模板实例化缺陷根治**——`openInstantiate` 仅当模板 code 为合法 4 段才预填，否则留空由系统自动生成（extra 提示「模板编码 X 非 4 段指标编码，已留空自动生成」）；SQL 模式加超长预检（>16384 拦截，对齐后端 T11）。**验证**：前端 metricCode 单测 5（新）+ Templates/MetricCreate **110 passed**（模板 3 段 code 实例化不再被 rules 拦截、自动生成）+ tsc 0；真实复现确认 422 detail 即「metric_code 须符合 4 段格式，当前仅 N 段」。影响接口：无（纯前端校验增强 + 模板预填逻辑修正，API 契约零变化）。 | 无接口变更（前端校验/预填逻辑修正，API 契约零变化） | —（frontend 保持 released；创建/模板实例化不再因编码格式提交后 422） |**① 根因**——批量路径 `_build_derived_candidate`/`_build_composite_candidate` 调 `build_profile` 未传 `sql`，profile 无 `sql_profile.group_by`，`_infer_granularity` 的实体粒度推断（`extract_grain_and_dims`）退化——GROUP BY 业务实体键（`doctor_id`）无法升级为 `doctor` 粒度，批量派生/复合候选粒度恒为时间粒度（day/month），与单条 auto_suggest 路径分裂（单条有 sql 解析、实体粒度生效）；**② 修复**——`_build_derived_candidate` 在 build_profile 后补挂 `sql_profile`（含 group_by/time_column，与单条路径同源），`_build_composite_candidate` 新增 `infer_dicts` 参数并用同一套 `extract_grain_and_dims` 从语句 GROUP BY 推断实体粒度（时间粒度优先、唯一业务实体键 → 实体粒度、多实体键归维度兜底 day）；`_apply_candidate_period`（LLM 周期覆盖）仅同步时间粒度/None，实体粒度（doctor）是统计主体属性不因周期变化被冲掉。**验证**：后端 test_sql_split **86 passed**（+3：复合跟随实体粒度 doctor / 无实体键复合保周期粒度 / LLM 周期覆盖保留实体粒度）+ test_auto_fill/test_sql_validation/eval 73 + semantic service 296 + ruff 0；实测四种场景：GROUP BY doctor_id → doctor、doctor+month → month、无 GROUP BY → day、多实体键 → day 归维度。影响接口：无（批量候选/单条推断响应字段不变，仅粒度推断值语义修正）。| 无接口变更（推断粒度语义修正：批量路径实体粒度从恒时间粒度恢复为与单条一致） | —（semantic 保持 released；批量/单条两路径粒度推断同源，实体粒度真正落地） |
| 2026-08-28 | — | semantic+frontend | **指标目录「下游引用」筛选（用户『增加有无下游引用的筛选过滤，方便批量废弃时直接操作』）**：**① 后端**——`list_metrics` 新增 `has_downstream` 过滤参数（`MetricListParams` 加字段 + repository EXISTS 子查询：活跃血缘边 `source_node=concat('metric:', code)` 且 `edge_type∈(DERIVED_FROM, CONSUMED_BY)` 且 `deleted_at IS NULL`/`stale=False`——语义与 downstream-check 完全一致；true 仅保留有下游引用指标、false 仅无下游、缺省不过滤）+ service 透传；**② 前端**——指标目录条件筛选行新增「下游引用」下拉（全部/有下游/无下游），`load()` 透传 `has_downstream`、useEffect 依赖联动、`handleResetFilters` 重置、已应用筛选回显 Tag（可一键清除）、`hasFilter` 纳入判定。**验证**：后端 test_semantic_repository 10（+1：has_downstream 三态——true 含 concat/lineage_edge/DERIVED_FROM/CONSUMED_BY/deleted_at、false 取反 NOT、缺省不过滤）+ semantic 364 passed + ruff 0；前端 MetricCatalog 61（+1：有下游→has_downstream=true / 无下游→false / Tag 清除回默认）+ tsc 0。影响接口：GET /metric-definitions 新增可选 has_downstream 查询参数（缺省不过滤，向后兼容）。 | 新增可选查询参数 has_downstream（GET /metric-definitions，向后兼容） | —（semantic 保持 released；批量废弃前可按下游引用收敛——有下游者强制填替代指标、无下游者可安全批量操作） |
| 2026-08-28 | — | infra | **OpenAPI 文档中文化 + Swagger UI 品牌化美化（用户『接口页面太丑且缺少中文描述』）**：**① OpenAPI 展示层中文化**——新建 `backend/app/openapi_localize.py` 集中映射（不侵入 30+ 路由文件，无并行冲突）：33 个业务分组名中文化（`tag.name` 直接中文化以兼容 Swagger UI 5.x——其渲染分组标题用 `name` 而非 `x-displayName`）+ 分组描述（存活/就绪探针、登录认证、指标注册…）；operation.tags 引用同步替换保持分组正确；281 个唯一英文 operation summary 翻译为中文（List → 查询、Create → 新建、Get → 查询、Update → 更新、Delete → 删除、Publish → 发布、Submit → 提交、Approve → 审核通过、Reject → 驳回、Deprecate → 废弃、Batch → 批量、Infer → 推断、Validate → 校验 等——未命中保留原文不破坏契约）；info.title/description 中文化（Unisense 指标语义中台 + 认证/版本/自动同步说明）；幂等设计：二次 localize 时中文 name 查不到映射则跳过，description 不会被清空。**② Swagger UI 品牌化美化**——新建 `backend/static/swagger-ui/custom.css`（自主品牌主题，无第三方依赖）：深蓝渐变品牌顶栏（linear-gradient 120deg `slate-900→blue-800→blue-600`）+ 自定义 logo + 中文系统字体栈（PingFang SC/Microsoft YaHei）+ 圆角卡片式分组（hover 阴影）+ 方法徽章 5 档配色（GET 蓝/POST 绿/PUT 橙/DELETE 红/PATCH 紫，圆角加粗）+ 优化表格/参数/响应/按钮/代码块/响应式。**③ StandaloneLayout 显式启用**——`swagger-init.js` push StandalonePreset 后显式 `config.layout = 'StandaloneLayout'`（preset 仅注册组件，不显式指定 layout 仍走 BaseLayout 无顶栏）。**验证**——`test_openapi_localize`（新 5 项：info 中文化 / 33 tag name 中文化 / operation.tags 同步 / 281 summary 翻译 0 英文残留 / 未命中保留原文 / 映射无重复歧义）+ `test_docs_localized` 6 全过 + ruff 0；**真实容器全链路**——8100/8180 双入口 `/openapi.json` 中文 tag/中文 summary、`/docs` 品牌深蓝渐变顶栏渲染 33 中文分组卡片（含描述）、Authorize/Filter 搜索/接口分组完整、0 console error。影响接口：无（仅 OpenAPI 展示层 + docs 渲染层，API 契约零变化）。 | 无接口变更（OpenAPI 展示层中文化 + Swagger UI 渲染层品牌化，API 契约零变化） | —（infra 保持 released；API 文档达生产展示标准） |
| 2026-08-28 | — | semantic+infra | **Swagger UI 白屏根因修复——StandalonePreset 独立引入 + dom_id 显式化（用户『localhost:8180/docs 一片空白』）**：**根因**——上一轮本地化已引入 swagger-ui-bundle.js，但 swagger-ui-dist@5.x **全系 bundle 都不再内置 SwaggerUIStandalonePreset**（grep -c "SwaggerUIStandalonePreset" 在 5.18.2 与 @5 latest 都是 0；StandalonePreset 已拆为独立文件 `swagger-ui-standalone-preset.js` 230KB），且 bundle 默认 `dom_id` 不再指向 `#swagger-ui`——双重缺失导致 presets=[apis, undefined] + 容器不匹配 → SwaggerUIBundle 静默失败、`window.ui` 有但 DOM bodyLen=0。**修复**——① 下载 `swagger-ui-standalone-preset.js@5.18.2` 入仓 `backend/static/swagger-ui/`（230KB），`main.py` `_swagger_ui_html` 在 bundle 后插入 `<script src="/static/swagger-ui/swagger-ui-standalone-preset.js"></script>`；② `swagger-init.js` 改用全局 `window.SwaggerUIStandalonePreset`（缺失时降级 `layout=BaseLayout` 仍可渲染），并显式 `config.dom_id = "#swagger-ui"`（不再依赖 bundle 默认）。**验证**——Playwright 真实截图：8180/docs 渲染「Unisense 0.1.0 OAS 3.1」标题 + Authorize 按钮 + Filter 搜索框 + 33 个接口分组（health/auth/audit/metric-definitions/collector-source 等），bodyLen 27009，0 console error；后端 `test_docs_localized` 27 + `test_middleware` 全过 + ruff 0（standalone-preset 入仓）。影响接口：无（仅 docs 渲染层）。 | 无接口变更（Swagger UI 渲染层修复，API 契约零变化） | —（infra 保持 released；Swagger UI 真正可用，接口清单可见可调用） |
| 2026-08-28 | — | infra | **API 文档禁止缓存（接口变更后 Swagger UI 自动同步最新定义，审查修复 🟢）**：**根因**——`/openapi.json` 是 FastAPI 运行时根据当前注册路由**动态生成**的（单一事实来源=代码路由），本应接口新增/删除/变更后 docs 自动同步；但响应无 `Cache-Control` 头，浏览器对同源 GET 响应做启发式缓存，接口变更后刷新 docs 可能拿到**过期定义**（文档不同步）。**修复**——`SecurityHeadersMiddleware` 新增 `_DOCS_CACHE_PATHS=("/docs","/redoc","/openapi.json")`，命中路径统一 `Cache-Control: no-store`（docs 页面/接口清单每次访问都拉取最新；普通 API 响应不受影响，保持无缓存控制头）。**验证**：test_middleware 22（+2：openapi.json no-store 且保持严格 CSP / 普通 API 不受文档 no-store 规则影响）+ test_docs_localized 5 全过 + ruff 0；**真实容器全链路**——8100/8180 双入口 `/openapi.json` 200 + `cache-control: no-store`、`/docs` 页面 0 CDN 引用（本地化静态资源）+ 单一本地化 CSP 头（nginx 配置已同步最终版：proxy_hide_header 防双头 + `^~ /static/` 防 SPA 回退）。影响接口：无（仅响应头增强，契约零变化）。 | 无接口变更（/openapi.json、/docs、/redoc 响应新增 Cache-Control: no-store，API 契约零变化） | —（infra 保持 released；文档实时反映当前路由，接口变更无需任何人工同步） |
| 2026-08-28 | — | frontend+semantic | **指标回收站批量彻底删除 + 列表删除可发现性（用户『回收站为什么不支持批量彻底删除』『列表页删除为什么要进详情页』）**：**① 后端**——新增 `POST /metric-definitions/batch-purge`（仅平台管理员，逐条复用 `purge_metric` 级联清理版本/维度/健康度/值快照/挂载/血缘边 + 审计 `metric_definition.batch_purge(_failed/_partial)` + run_batch 逐条容错返回 BatchResponse，已软删记录才可硬删）；**② 前端**——回收站视图批量操作不再整体禁用，开放「批量彻底删除（回收站）」菜单项（仅平台管理员、勾选已软删记录可用）+ 确认弹窗（物理删除不可恢复警示 + 红色确认按钮）+ 失败明细复用既有批量错误弹窗；**③ 列表页删除可发现性**——REVIEW/EXPERIMENTAL/PUBLISHED 行删除位显示「需先打回/下架后在草稿态删除」，消除『必须进详情页才能删』困惑（列表页行内删除与批量删除（草稿）本已支持，仅发布/审核态需先流转）。验证：后端 test_metric_batch_api 22（+3：批量混合结果/非管理员 403/空列表 422）+ 相关 341 passed + ruff 0（顺带修 HEAD 既存 schemas E501）；前端 MetricCatalog 60（+1：回收站批量彻底删除确认后调用 batchPurgeMetrics）+ tsc 0。影响接口：新增 POST /metric-definitions/batch-purge（向后兼容新增）。
| 2026-08-28 | — | infra+semantic+frontend | **API 文档（Swagger/ReDoc）本地化 + CSP 修正 + openapi.json 生成 bug 修复（用户『localhost:8180/docs 看不到 Swagger UI』）**：**根因**——后端 `SecurityHeadersMiddleware` 给所有响应（含 /docs）打严格 CSP `default-src 'self'`，而 FastAPI 默认 Swagger UI 从 jsdelivr CDN 加载资源且有 inline script，被 CSP 全部拦截；且 nginx 反代 docs 时叠加的宽松 CSP 与后端严格 CSP 形成「双头交集=严格」仍拦截；另发现 `consume.py` 的 `request: Request` 依赖参数**未导入 Request**（`from __future__ import annotations` 下变未解析 ForwardRef，被 FastAPI 当 body 参数）→ `/openapi.json` 生成 500（Swagger 即使页面可渲染也无接口列表）。**修复**——① **静态资源本地化**：swagger-ui-dist@5.18.2 / redoc@2.2.0 下载到 `backend/static/`（~2.5MB 入仓，离线可用）；`main.py` 挂载 `/static` + 自定义 `_swagger_ui_html`（初始化配置经 `<meta name="swagger-config">` 注入、`swagger-init.js` 外置读取，**消除 inline script**）→ docs 页面 CSP 可收紧到 `script-src 'self'`（无 unsafe-inline）；`/redoc` 同用本地资源 + 关闭 Google Fonts；② **CSP 分层**：`middleware.py` 对 `/docs`/`/redoc` 返回本地化精确 CSP（`script-src 'self'`、`style-src 'self' 'unsafe-inline'`、`frame-ancestors 'none'`），API 响应保持严格 `default-src 'self'`；③ **nginx**：docs 反代隐藏后端 CSP 头并显式设置相同本地化 CSP（杜绝双头交集混乱）、新增 `location ^~ /static/` 反代（^~ 优先于正则静态资源 location，否则 .js 被 SPA try_files 回退 index.html 404）；④ **openapi bug**：`consume.py` fastapi import 补 `Request`。**验证**：后端 `test_docs_localized`（新，5 项：HTML 无 CDN/无 inline script/本地资源、docs/redoc 200+本地化 CSP、static 5 资源 200、openapi 保持严格 CSP）+ `test_middleware` 25（+2 docs/redoc CSP）+ consume 96 全过 + ruff 0；**真实容器全链路**——8100 与 8180 双入口 /docs 200、静态资源 200、单一本地化 CSP 头、页面 0 CDN 引用；`app.openapi()` 生成成功（371 路径，此前 500） | 无接口变更（docs/redoc 静态资源由 CDN 改同源 /static/，openapi.json 从 500 修复为可生成；API 契约零变化） | —（semantic 保持 released；API 文档内网离线可用、CSP 精确分层、openapi 生成修复，达到生产标准） |
| 2026-08-28 | — | 全模块 | **全维度系统审查报告修复补齐（产品/技术/安全三维，P0-P3 全量落地）**：基于 58 条读码取证审查报告，全部修复项落地——**产品**：P1 删除指标跳转断链修复+恢复指引改「回收站」、P2 采集目录批量废弃补二次确认+失败明细、P3/P4 待办中心与「待我审」按权限/评审人过滤、P5 查询工作台指标下拉服务端搜索、P6 数据源/采集目录空态 CTA 引导冷启动、P7 单条创建失败 METRIC_CODE_EXISTS 弹窗恢复动作、P8 字典管理导航文案修正、P9 EXPERIMENTAL 双名统一灰度（11 处）、P10 度量目录分页 showTotal、P11 状态色统一（REVIEW 全系统橙消除同色不同义）、P12 deprecate 动作术语统一「废弃」、P13 评审拒绝动作统一「驳回」、P14 日期格式统一 formatCnTime、P15 「逻辑度量」UI 文案统一「原子指标口径」、P16 SQL 解析评测移出业务菜单入工程工具组+权限路由、P18 注册指标菜单一致性核验；**技术**：T1 publish-before-commit 修复（BaseService after_commit 事件驱动，commit 成功才投递/失效缓存）、T2 定时任务循环写会话污染修复（4 处 except rollback 对齐 batch_common 范式）、T3 挂载加载过滤软删指标、T4 度量并发创建 IntegrityError→409、T5 度量目录乐观锁 row_version（迁移0111）、T7 缓存失效失败告警进通知闭环、T8 后台任务失败告警（_on_job_end 检测）、T9 TaskLock 锁故障 fail-safe 跳过防多副本双跑、T11 口径字段与 raw_sql 补大小上限、T14 软删占位编码回收站引导、T15 粒度回填防写已删指标、T17 健康度刷新分批加载（500/批）、T19 db_catalog 表级搜索 FULLTEXT 加速（迁移0113 ngram，MATCH 短语≈子串、缺失回退 LIKE）；**安全**：S1/S2 must_change_password 服务端强制+改密/重置吊销 refresh token、S3 生产校验改非 local/dev/test 即生效、S4 认证状态 Redis 降级告警闭环（system.auth_state_degraded）、S5 限流可信代理+登录 IP 级双桶、S6 QuickBI report_id 格式校验、S10 LLM prompt 用户输入定界、S11 CSV Content-Type 校验、S13 数据源明文凭据查看落审计、S14 非本地环境禁用公开 dev Fernet 密钥、S15 备份 AES-256 加密落盘（BACKUP_ENCRYPTION_KEY）、S18 审计归档 sha256 哈希链、S19 消费凭证签发落审计、S20 禁止自抹审计；**部署**：compose 全容器加可调资源限制（8核/32G 无 Doris 基线）+ ES 堆 512m→2G + Doris 服务注释（内部生产暂不考虑）。**验证**：后端全量 3999 passed（剩余 6 项既存：semantic_cache Redis flaky×4/health_scorer/recommend）+ ruff 0；前端全量 **1093 passed**（61 文件）+ tsc 0；迁移 0111/0112/0113 真实 DB 应用；doc_sync 通过；三容器待重建生效 | 新增 purge 端点（指标/度量）、refine/secret_viewed/issue_token 等审计动词、db_catalog FULLTEXT 索引、compose 资源限制（env 可覆盖）——均为新增/加固，既有契约零破坏 | —（semantic/collector/security/infra 保持 released；全维度审查 P0-P3 全部修复补齐，产品闭环/技术韧性/安全加固达到生产级） |
| 2026-08-28 | — | frontend | **血缘/资产地图节点图标二次美化（用户『血缘视图模块展示的节点图标还是很丑』）**：根因——上一轮白色线性 SVG 图标在节点内 14px 显示尺寸下细节完全丢失（简单矩形+直线，占比小、无衬底），白线条在浅色/高饱和节点填充上对比不足，显得粗糙；**修复**——① 图标升级为 **Lucide 风格 24x24 线性 SVG**：table=表头分割线+两列分隔线+列头圆点（Lucide Table）、field=竖列矩形+段横线+段头圆点（Lucide ListTree）、metric=折线图+端点圆+坐标轴（Lucide LineChart），stroke-width 1.5 round cap/join 精线条；② **深色半透明圆衬底**（`rgba(15,23,42)` slate-900 调透明圆 r=11 居中，白色细描边），**三档透明度按节点重要度分层**（metric 枢纽 0.52 / table 普通 0.42 / field 叶子 0.32）——白线条在深蓝/紫/红/橙/绿任意节点填充色上都高对比清晰可辨；③ **icon 尺寸加大约 50%**（table 14→20×16、field 10→16×14、metric 16→22×22，更接近节点内径、占比更满更醒目）。**验证**：tsc 0 错；AssetGraph/LineageView/AssetMap/LineageImpact **134 passed**；真实容器 Playwright 实测——0 渲染错误、图形层 canvas 白色描边像素占比显著（SVG 白线+标签白底）、高清截图 hub 节点（`ods_e2e_order`/`ods_e2e_user`）中心白色折线（LineChart 图标）清晰可见，不再是彩色 emoji | 无接口变更（纯样式层，AssetGraph 组件 props 不变） | —（semantic 保持 released；节点图标从简单白线升级为 Lucide 风格+深色衬底+大尺寸，任意节点色上都清晰精致） |
| 2026-08-28 | — | frontend | **死代码清理 + 多变体消费类型补齐（审查修复 🟢）**：**① 删除 9 个无调用 api.ts 客户端函数**——`createMetricMount`/`updateMetricMount`/`getMetricMount`（独立挂载管理前端从未接线，多变体走 metric create/update 的 `mounts` 字段）、`deletePreference`/`getTemplate`/`getMeasureCatalog`/`getDomainMetrics`/`rejectUnknownDictValue`/`getDictItemRefCount`（均无任何页面调用）；后端 REST 端点全部保留（供未来接线或直接 API 调用），仅清理客户端死代码；`batchImportMetrics` 因属并行会话批量录入功能保留。**② 前端 `QueryRequest` 补齐 `variant` 字段**——对齐后端 consume/schemas.py（多变体消费显式指定挂载行 ID 或 `粒度:周期`，缺省按默认变体，旧契约零破坏）。**验证**：tsc 0 错（排除并行会话 AssetGraph WIP）；MetricCatalog/QueryWorkspace 68 passed | 无接口变更（纯前端死代码清理 + 可选类型字段补齐） | —（semantic 保持 released；客户端 API 层与后端契约对齐，无未接线的悬空函数） |
| 2026-08-28 | — | semantic+frontend | **消费指南/命名建议去模板化（审查修复 🟡1/🟡2）**：**① 消费指南**——auto 分支不再拼装模板「推荐用法」（`适用 X 域 Y 粒度分析`/`聚合方式为 Z`）伪装成人工推荐：域/粒度/聚合已在基本信息卡展示，`recommended_usage` 置空（前端显示「暂无推荐用法」+ 编辑入口），真实推导仅保留事实性注意（PII/不可加/半可加进 `cautions`）与血缘一跳关联推荐；**② 仲裁改名建议**——删除无上下文的「原名·新口径」编造兜底（无度量列/域/对方名称时返回空列表），前端仅自动填入 `source=llm` 候选，`rule` 机械区分名只展示不自动污染输入框（此前第一个候选会被自动填入并可能被保存为正式名称）。**验证**：后端 test_semantic_service 消费指南 **17 passed**（+1 不可加事实注意）+ test_suggest_rename_api **4 passed**（+1 无上下文返回空）+ ruff 0；前端 MetricDetail **87 passed**（+1 rule 不自动填入）+ tsc 0 | 无接口变更（GET 消费指南 auto 分支 `recommended_usage` 由模板文案变空数组，PUT 写路径不变；suggest-rename 无上下文时返回空列表） | —（semantic 保持 released；消除模板化假「推荐用法」与编造改名候选，真实推导与人工维护边界清晰） |
| 2026-08-28 | — | notify+frontend | **订阅事件类型动态化（审查修复 P2-7：前端 60+ 订阅事件写死，后端新增事件需发版且已漂移）**：**① 后端权威来源**——新增 `GET /notify/subscriptions/event-types` 返回 `BUSINESS_EVENT_TYPES` 权威清单（EventBus 注册表单一事实来源）；**② 前端动态拉取**——订阅弹窗从该端点拉取，未覆盖的并入「其他」分组（`buildEventTypeGroups` 纯函数：静态分组 + 动态兜底），后端新增业务事件无需前端发版即可订阅；**③ 前端补齐漂移**——`EVENT_TYPES`/`EVENT_TYPE_GROUPS` 同步补齐 7 个缺失事件（metric.reactivated/conflict_forced_closed/conflict_escalation_overdue/lineage.ddl_changed/storage.table_oversized/system.task_failed/system.cache_invalidate_failed）+ `EVENT_ACTION_CN` 补 7 个中文翻译，订阅弹窗与后端全量对齐；拉取失败沿用静态分组不阻断订阅配置（受控降级）。**验证**：后端 test_notify_consumers **5 passed**（+1：端点返回权威清单含新增事件）+ ruff 0（顺带修 AsyncMock 未使用）；前端 Notifications **54 passed**（+2：buildEventTypeGroups 纯函数合并已知/未知事件 / listEventTypes 被拉取且静态分组可用）+ tsc 0 | 新增 GET /notify/subscriptions/event-types（读，向后兼容） | notify 保持 released（订阅事件类型后端权威驱动，前端可订阅全集不再漂移） |
| 2026-08-28 | — | frontend | **血缘/资产地图节点图标美化（用户『血缘视图的节点图标太丑了，需要进行美化』）**：根因——节点图标此前用彩色 emoji（指标 📈 / 表 🗂️ / 字段 🔖）经 G6 `iconText` 渲染，`iconFill` 对 emoji 无效（彩色字体不受 fill 控制）、跨平台渲染差异大，与径向渐变填充/现代配色明显不协调；**修复**——改用内联 SVG data URI 白色线性图标（`iconSrc` 图片模式，G6 Icon 形状 `src` 渲染 Image、`width/height` 居中控制尺寸）：table=表格（外框+表头+两列）、field=字段列（竖列+中线）、metric=指标趋势（上升折线+箭头），统一 `stroke="#fff"` stroke-width 1.8 round cap/join，模块加载时 `encodeURIComponent` 预编码一次、`iconSrc` 回调仅查表（无每帧成本）；尺寸按节点形状收窄（table 14/field 10/metric 16，避免扁节点溢出）。**验证**：tsc 0 错；AssetGraph/LineageView/AssetMap/LineageImpact **134 passed**；真实容器 Playwright 实测（见会话记录） | 无接口变更（纯样式层，AssetGraph 组件 props 不变） | —（semantic 保持 released；节点图标从彩色 emoji 升级为白色线性 SVG，与设计系统协调） |
| 2026-08-28 | — | frontend | **血缘/资产地图动画增强（用户『血缘图能否实现动画效果』，A+B+C+D 全做）**：**A 相机平滑**——首屏 `fitView` 平滑缩放进入（800ms ease-out）、大图缩放下限 `zoomTo` 平滑（400ms）、搜索聚焦 `focusElement` 平滑（600ms）、「重置视图」平滑回位（600ms，相机走 viewport 变换路径与 force 布局解耦，安全）；**B enter/exit fade 真正生效**——修正上一轮「全局 animation:false」的根因：G6 默认主题给 node/edge 配了 x/y 位置动画（update/translate），全局非 false 时 force 布局触发 draw 崩溃，而 false 又短路全部元素动画（此前 enter/exit 配置实际从未驱动）；现改为全局 `animation:{duration:300}` + 显式 `node/edge.update:false/translate:false` 覆盖主题位置动画（防崩溃），enter/exit 透明度淡入淡出真正生效（350ms ease-out/200ms）；**C 边流动**——骨干边（血缘度总和≥10）虚线 `[8,6]` + 次骨干 `[5,4]`，render 完成后 rAF 直接驱动 key shape `lineDashOffset` 递增取模（G6 keyframes 起止相同会被 preprocessKeyframes 过滤、update 又只触发一次，无法用原生动画做持续流动；rAF 只驱动有 lineDash 的边、防御式找不到 shape 跳过、数据重载/卸载取消）；**D hover 过渡**——节点 `update` 配标量字段动画（stroke/lineWidth/haloLineWidth/label 200ms），hover 中心节点 `setElementState(...,true)` 带动画「点亮」，邻域其余节点批量瞬时（性能安全）。**验证**：tsc 0 错；AssetGraph/LineageView/AssetMap/LineageImpact **134 passed**（+focusElement 断言更新为动画参数）；真实容器 Playwright 实测（见会话记录） | 无接口变更（纯样式/动画层，AssetGraph 组件 props 不变） | —（semantic 保持 released；相机平滑 + fade 真正生效 + 骨干边流动 + hover 过渡） |
| 2026-08-28 | — | frontend | **血缘地图/资产地图节点信息回归 + 视觉去杂（用户「现在打开血缘视图，看不到节点的具体信息，而且感觉血缘节点效果并没有很美观，看起来很乱」）**：**① 标签可见性**——`AssetGraph.compact` 状态移除 `labelOpacity:0`（白底 pill 节点名始终保留，信息可见优先于极致帧率）；**② fitView 缩放下限**——大图 always 缩放下引入最小 zoom 0.35 防 `fitView always` 把 160 节点图压到 ~0.1（节点亚像素、信息全丢），同步把 `LOD_COMPACT_ZOOM` 阈值 0.6→0.35（仅用户主动滚轮缩到很小时才触发，且只藏重绘制层 halo/阴影/icon/badge，标签仍保留）；**③ 布局密度**——`dagre` `nodesep` 110→80、`ranksep` 100→70 让大图在 zoom 下限下能展示更多 rank 不堆成细带；`LineageView` 画布高度 560→740 给图更多呼吸空间；**④ 视觉去杂**——hub halo 14→11/0.6→0.45，普通 halo 10→8/0.42→0.32（避免密集大图邻接光晕相互侵染）；径向渐变 `lightenHex 62→38 / darkenHex 18→8` 避免小节点下中心过白边缘过暗显得"脏"。**验证**：tpc Playwright 实测血缘图 2115 节点 160 核心首屏节点名清晰可读（`dwd_visit...`/`wedw_dwd_dim_tb...`）+ 资产地图 27 节点名完整（`wedw_dlk_active_doctor_dy` 等）+ AssetGraph/AssetMap/LineageView/LineageImpact **134 passed** + tsc 0 | 无接口变更 | —（semantic 保持 released；信息可见性 + 视觉清爽度双提升） |
| 2026-08-28 | — | semantic+frontend | **SQL 智能推断不再产原子（用户『SQL智能推断出来的指标至少应该是衍生或复合指标，不可能是原子指标，原子指标难道不是逻辑抽象指标类型吗？不是和之前的指标语义共识违背了吗』，方案 C=A+B）**：**① 推断语义修正（A）**——`auto_fill._infer_type` 与 `sql_split._build_derived_candidate`（原 `_build_atomic_candidate`，函数名同步改）类型判定彻底移除 atomic 分支：SQL 候选天然绑定源表/度量列（物理口径），**日粒度也归派生**（派生最小周期），原子只从逻辑度量目录创建；`_apply_candidate_period` 不再把 day 降级为 atomic；复合依赖解析（A7）改用 `deps_aliases` 区分基础候选（旧 `type=="atomic"` 过滤会把方案 A 后全部基础候选滤掉致依赖恒空）；`batch_register_from_sql` Phase2 补齐 raw_sql/源表/度量列透传（Phase1 有、Phase2 缺，方案 A 后基础候选全走 Phase2 否则物理口径丢失）+ 创建成功的派生/复合编码加入批内已创建集合（`atom_ok`，否则复合候选依赖预检恒误报「依赖未创建」）+ DB 错误文案从「复合指标失败」改通用「单条失败」；**② 创建硬校验（B）**——`create_metric` 原子必填 `measure_id`（`MEASURE_REQUIRED`，旧式来源 source_table 不再替代逻辑度量）+ 原子携带挂载拒绝（`ATOMIC_NO_MOUNT` 纵深防御，schema model_validator 已先拦「仅派生指标可挂载」）；**③ 前端联动**——批量候选默认勾选从原子改派生、候选行字段区重构：物理属性（聚合/单位/周期/粒度）对所有非复合候选显示（修复方案 A 前派生候选无法行内编辑物理属性的功能退化）、关联逻辑度量原子专属、`loadCandidateIntoWizard` 派生候选回填挂载区（`mounts[0]`，方案 A 后物理来源进挂载）、推断摘要文案「为原子指标」→「为派生指标」、默认勾选提示同步、`atomicDepOptions` 改名 `baseDepOptions`。**验证**：后端 test_auto_fill/test_sql_split/test_sql_validation/test_semantic_service 等 **536 passed**（含 +2 方案 B：原子缺 measure_id 拒绝 / 原子带挂载拒绝）+ ruff 0（顺带修既有 SIM102/B007/嵌套 with 等 7 项）；前端 MetricCreate **93 passed**（SQL_BATCH_RESULT 候选改 derived、默认勾选派生、关联逻辑度量改原子专属测试等）+ tsc 0 | 无接口变更（推断类型语义修正 + 创建校验收紧：原子缺度量/带挂载 422，属既有契约的语义一致化） | —（semantic 保持 released；SQL 推断产物一律派生/复合，原子只从逻辑度量目录创建，杜绝「伪原子」） |

> 规则：每次模块状态变更在此追加一行；记录 日期 / PR / 模块 / 变更点 / 影响接口 / 状态跃迁。
> 由 agent 或开发者在完成可验证步骤后填写，对应 `docs/module-status.yaml`。
| 2026-08-28 | — | frontend | **血缘地图 & 资产地图视觉升级（用户 /goal『优化血缘地图和资产地图的视觉样式，完全不影响现有功能，提升美观度与高级感』）**：纯样式层面升级（数据结构/交互逻辑/布局算法零改动）——**① 现代化配色**：域色板从 Material 700 高饱和调整为 Tailwind 600 现代低饱和（与「校准仪表」设计系统 --ink/--data/--signal 协调，白字对比度保持 >4:1），边色改为柔和雾蓝/淡紫/淡青；**② 节点层次**：fill 改径向渐变（中心提亮→0.55 主色→边缘压暗，球面立体感）、阴影柔化、**枢纽节点（血缘度≥8）halo 更宽更实形成「骨干发光」**；**③ 连线层次**：lineWidth/strokeOpacity 按两端血缘度总和分层（骨干边清晰、叶子边淡雅），环边红虚线保留；**④ 交互特效**：hover/搜索 active 状态改金色描边+增强光晕+标签加粗（保留渐变不被平涂）；全局 animation 保持 false（G6 v5.1.1 全局动画在大图 force 布局触发 draw 崩溃，已实测定位）+ 元素级 enter/exit fade 配置，hover/LOD 状态切换显式 setElementState(...,false) 瞬时（性能安全）；**⑤ 图例排版**：图例卡片化（styles.css `.asset-graph-legend` 浅底圆角分组 + 分隔线 + `.legend-dot` 统一色块），小屏（≤640px）媒体查询收紧间距去分隔线。**验证**：tsc 0 错；AssetGraph/LineageView/AssetMap/LineageImpact **134 passed**；真实容器（重建 frontend 镜像后 8180）Playwright 实测——血缘/资产两图图形层 canvas 像素分析 brightnessLevels>1（径向渐变真实渲染）、图例 `.asset-graph-legend` 生效、0 渲染错误（dev server draw undefined 已用 stash 验证为 G6 v5.1.1+vite 环境既存问题，与本次改动无关，生产容器正常） | 无接口变更（纯样式层，AssetGraph 组件 props 不变） | —（semantic 保持 released；血缘/资产地图视觉升级为现代低饱和+渐变光晕+层次化连线+卡片化图例） |
| 2026-08-28 | — | semantic+frontend | **存量指标批量录入能力 + Swagger 前端入口（用户『缺少类似 swagger ui 获取所有接口信息』+『外部 agent 批量解析 SQL 后如何自动录入大量存量指标』）**：**A. Swagger 接入前端**——nginx 新增 `/docs` `/redoc` `/openapi.json` 反代（覆盖全局 CSP 放行 jsdelivr/unpkg CDN 供 Swagger UI 加载），系统管理菜单新增「API 文档」入口（新窗口打开 `/docs`，Authorize 填 token 即可在线调试全部接口）；**B. 对外对接文档 `docs/API_INTEGRATION.md`**——鉴权、批量录入端点、候选字段全表、4 段式编码规范、示例 payload（建表注释驱动命名）、CSV 模板列、错误码速查、对接顺序；**C. 通用批量导入端点 `POST /metric-definitions/batch-import`**——接受纯结构化候选（`metric_code`/`name` 可缺省，系统按 `{域}_{源表末段去后缀}_{度量列}_{周期}` 自动补全 4 段式编码、度量列中文化兜底名称），复用 `batch_register_from_sql` 的 savepoint 逐条隔离 + 域门禁 + 冲突预检，来源标注 `agent/csv/manual`，审计 `metric_definition.batch_import(_failed/_partial)`；**D. CSV 模板导入**——`GET /metric-definitions/imports/template`（带鉴权下载 CSV 模板含表头+示例行）、`POST /metric-definitions/imports/csv`（multipart 上传，Python csv 逐行解析 + 单行容错 `row_errors` 不阻断有效行 + 全行非法 400 INVALID_CSV），前端指标目录工具栏「批量导入」弹窗（模板下载 + 目标域选择 + 上传 + 逐条结果表格与解析错误回显）；依赖 `python-multipart` 加入 requirements.txt。**验证**：后端 test_metric_batch_api **24 passed**（+6：batch-import 自动补全编码/名称 + 显式保留 + csv 逐行解析创建 + 非法行 row_errors + 全行非法 400 + 模板下载）+ 指标相关 94 全绿 + ruff 0（仅 HEAD 既存 schemas E501）；前端 MetricCatalog+Layout **69 passed** + tsc 0；全量套件后台验证 | 新增 POST /metric-definitions/batch-import、POST /metric-definitions/imports/csv、GET /metric-definitions/imports/template（均为新增，向后兼容） | semantic 保持 released（存量指标批量录入三通道：agent JSON / CSV 文件 / SQL 解析，Swagger 全部接口可在线调试） |
| 2026-08-28 | — | semantic+frontend | **SQL 批量创建体验优化（用户『SQL智能推断解析出指标后点击创建没有任何提示，直接在下面给出创建结果；失败原因也没有根据具体原因给出操作入口』）**：**① 创建中阻塞反馈**——点击「批量创建选中指标/重试失败项」后弹出不可关闭的进度 Modal（Spin + 本次提交候选数 + 「逐条校验并写入数据库，请勿关闭窗口」），创建完成/失败自动关闭（此前仅按钮 loading + 结果区小字不醒目，用户易误以为无响应）；**② 失败项按原因给操作入口**——结果表格「操作」列对 VALIDATION_ERROR 失败项不再为空：按 `validation_errors` 文本特征分类给出具体入口（编码冲突→「改编码重试」、依赖缺失→「补依赖重试」、DB 错误→「重试」、其余→「在向导中编辑」），点击回填单条向导（loadCandidateIntoWizard）或单条重跑（submitSqlBatch），并保留「重试」兜底。**验证**：前端 MetricCreate 93 passed（+3：进度 Modal 出现且完成后让位 / 失败项按原因显示入口并可回填向导 / 行内重试仅重跑该失败候选），worktree 干净基线验证全绿 + tsc 0 | 无接口变更（纯前端交互增强） | semantic 保持 released（SQL 批量创建反馈闭环：创建中有进度、失败有入口） |
| 2026-08-28 | — | collector | **Spark Thrift 无注释占位串致批量推断全跳过修复（用户『在采集目录的描述缺失治理中，对 wedw_dim.pub_date_manual 表进行字段推断，为什么直接跳过了』）**：根因——`hive_uncategorized` 数据源经 pyhive 直连 Spark Thrift Server，`DESCRIBE` 对无 COMMENT 列返回占位串 `"from deserializer"`（Spark StructType 反序列化默认 comment），连接器原样写入 schema_json → 批量推断 `infer_descriptions_batch` 的跳过逻辑 `if comment and ...: skipped` 把假注释当真实 DDL 注释，28 列全进 skipped（影响面：161 表 200 列），而描述缺失面板口径只看 `column_descriptions`（manual/llm）→ 「面板显示缺失、推断却全跳过」矛盾。修复三层：**① 公共常量** `services/collector/placeholders.py`——`PLACEHOLDER_COMMENTS={"from deserializer"}` + `is_effective_comment`（空/纯空白/占位串均视为无效注释）；**② 连接器治本** `connectors/hive.py`——DESCRIBE 第三列非有效注释归一化为空串，未来采集不再写入假注释；**③ 推断防御** `api/collector.py`——跳过条件改 `is_effective_comment(comment)`，历史残留占位串不再跳过；**④ 数据修复**——容器内脚本把已存在 schema_json 中 `comment="from deserializer"` 的列置空（161 表 200 列，幂等重跑 0 变更），`wedw_dim.pub_date_manual`（id=23785）28 列恢复「无注释」可正常推断。**验证**：后端 test_connectors +1（Spark 占位归一化：占位→空串/真实注释保留/空串保持）+ test_collector_api +1（批量推断占位 comment 不跳过：skipped 仅真实注释、date_id/empty_desc 进入 inferred）+ collector 全量 **356 passed** + ruff 0；真实 DB 修复后占位 0 残留 | 无接口变更（批量推断行为修复：占位串字段从 skipped 转为正常推断） | collector 保持 released（Spark Thrift 无注释列不再被假注释遮蔽，批量字段推断可正常执行） |
| 2026-08-27 | — | semantic+frontend | **SQL 智能推断三连修（用户『没有把关联维度回填、回填的名称为什么是 json、使用表不准给出 Select 文本』）**：**① 关联维度回填（A）**——后端 `sql_infer.extract_dimension_columns` 从 GROUP BY 提取非时间键（宽松过滤时间 hint/粒度别名/时间函数，业务维度保留）；`parse_sql_profile` INSERT 下沉场景顶层无 GROUP BY 时从含聚合子查询补提 group_by/time_column（此前恒空致下沉 SQL 维度无法提取）；批量候选 `_build_atomic_candidate` 新增 `dimensions` 字段 + 单条 auto-suggest 返回 `dimensions`；前端候选卡片「关联维度」多选预填可编辑（去掉 maxTagCount=responsive 保证预填可见）+ 主表单/向导回填 + 批量/单条提交合入 definition_json.dimensions；**② LLM 名称 JSON 净化（B）**——`parse.py` 新增 `clean_llm_text_field`（形如 JSON 对象时二次 json.loads 提取 name/metric_name/title/value，普通文本/非法 JSON 原样返回），接入 `extract_str_field` + `_llm_metric_name` + `_suggest_metric_llm_overrides` + `_apply_candidate_annotations`（此前 LLM 把名称包成 `{"metric_name": "月活"}` 导致名称原样显示成 json 文本）；**③ 使用表语义澄清（C）**——主表单「使用表（下游）」extra 与血缘推断区块澄清「AI 回填的是表级血缘下游（源表被哪些表消费，来自血缘图），并非『指标被哪些客户表消费』——指标级下游在指标创建、被下游 SQL 引用后由血缘自动补，此处可留空」（直接回应『怎么会知道有哪些消费指标的客户表』：SQL 推断在物理上不可能知道指标下游）。**验证**：后端 test_llm_parse +6（clean_llm_text_field JSON 提取/普通文本/非法 JSON/空与数字/extract_str_field 二次净化）+ test_sql_infer +5（extract_dimension_columns 业务保留/时间剔除/空安全/无时间列全保留/粒度别名剔除）+ test_sql_split +1（下沉 INSERT 候选携带 dimensions 集成）+ 相关 270 passed + ruff 0（仅 HEAD 既存 parse.py E501）；前端 MetricCreate 90（+1 关联维度预填：候选 Select 含 hosp_code/enter_source）+ 全量 1079 全绿 + tsc 0。影响接口：无（批量候选与 auto-suggest 响应新增 `dimensions` 字段，向后兼容）。 | semantic 保持 released（SQL 推断关联维度回填 + LLM 名称净化 + 使用表语义澄清） |
| 2026-08-27 | — | collector+frontend | **采集目录新增「全部数据源」筛选下拉（用户『采集目录运维视角只有活跃源/已删除源筛选框，没有数据源的筛选框，只有一个 Source ID 输入框不够友好』）**：**① 后端**——`GET /data-sources` 新增可选 `source_status` 过滤（deleted=查已软删源，其余默认活跃源，与 list_catalog_databases 语义对齐），repository/service/api 三层透传（缺省行为不变，既有调用方零影响）；**② 前端**——采集目录筛选区把 `Source ID` 手输框升级为「全部数据源」下拉（`showSearch` 按名称/ID 过滤，选项 `名称（ID）`，allowClear），选中即按 source_id 重新拉取；下拉随「活跃源/已删除源」状态联动（sourceStatus 变化重拉 listDataSources 并透传 source_status，切「已删除源」后可按名选已删源查看其历史目录）；登记实体弹窗数据源下拉共用同一 sources 状态。**验证**：后端 test_collector **203 passed**（+2：repository deleted_at IS NOT NULL/IS NULL SQL 断言 + service source_status 透传）+ test_collector_api 27 + ruff 0（顺带修 4 处既有 E501 超长行）；前端 Catalogs **49 passed**（+2：数据源下拉展示名称与 ID 选中后按 source_id 重新拉取 / 切已删除源后 listDataSources 透传 source_status=deleted）+ tsc 0 | 新增 `GET /data-sources?source_status=`（可选，缺省=活跃源，向后兼容） | —（collector 保持 released；采集目录按数据源名称/ID 下拉筛选，源状态联动列出已删源） |
| 2026-08-27 | — | collector | **批量推断历史裁剪修复 MySQL 1235（用户『采集目录推断字段的时候，为什么全部跳过了』）**：`POST /catalogs/batch-infer-history` 写入历史时的「保留最近 N 条」裁剪，把带 `LIMIT` 的子查询直接嵌进 `NOT IN`（`id NOT IN (SELECT id ... ORDER BY created_at DESC LIMIT 20)`），MySQL 不支持 `LIMIT & IN/ALL/ANY/SOME 子查询`（错误 1235）→ 批量推断会话结束写历史时 500，前端表现为「推断全部跳过」。修复：新增 `_prune_batch_infer_history`，先物化「最近 N 条」id 列表再以具体值列表裁剪（彻底绕开子查询 LIMIT）；keep_ids 为空时跳过更新（`not_in(空)` 会渲染为永真条件导致全表软删）。**验证**：后端 test_batch_infer_history **6 passed**（+2：裁剪 Update 编译 SQL 不含 LIMIT / keep_ids 空时跳过裁剪）+ collector 相关 **236 passed** + ruff 0 | 无接口变更（POST /catalogs/batch-infer-history 行为不变，仅修 500） | collector 保持 released（批量推断历史写入不再 500，推断会话可正常落历史） |
| 2026-08-27 | — | collector | **采集记录详情页「实时日志」（用户『采集记录中点击详情，应该能看到采集的实时日志』）**：此前详情页只展示汇总指标 + 失败/漂移明细，无日志（worker 进度仅进 JobStore 最近 50 条且终态覆盖）。现落地**采集日志全链路**——**① 新表 `collection_run_log`（迁移 0107）**：run_id 外键关联 collection_run，ts/level/phase/entity_name/message，索引 (run_id, ts)，随采集记录 purge 级联清理（保留期一致）；**② Redis 实时缓冲**：采集期间每条进度（start/scanning/registering）追加 `collect:run_log:{run_id}`（RPUSH + 首次写 7 天 TTL），worker `_make_progress_cb` 与同步 API `_make_run_log_cb` 双路径接入，采集零额外 DB 写；**③ 终态一次性回写**：任务 COMPLETED/FAILED/取消/超时收尾 `_flush_run_logs` 从 Redis 读全部 → bulk 落库 → 删缓冲，失败原因/失败实体补充为 ERROR 日志；**④ 新端点 `GET /collection-runs/{run_id}/logs`**（分页，返回 source=db/redis/none + status）；**⑤ 前端详情抽屉「运行日志」区**：RUNNING 每 3s 轮询增量追加（深色终端样式、自动滚底、级别/实体着色、加载更多），终态一次展示全部。**验证**：后端 335 测试通过（新增 23 日志链路 + 7 repository 日志）；前端 CollectionHistory 6 测试通过 + tsc 0；真实 DB `alembic upgrade head` 至 0107（表/索引/外键确认）；路由 `/api/v1/collection-runs/{run_id}/logs` 注册确认 | 新增 GET /collection-runs/{run_id}/logs（读）；无破坏性接口变更 | collector 保持 released（采集运行日志能力补齐，实时 + 长期可追溯兼得） |
| 2026-08-27 | — | dimension+glossary+semantic | **参照数据补齐八大缺失业务线（用户『你认为主题域是否涵盖了企业所有业务』+『按照这个清单进行补齐』）**：审查发现主题域覆盖度约 60-70%、缺 8 条业务线后系统补齐——**① 主题域 +45 至 114 节点（22 一级域）**：新增 8 个一级域——insurance 保险（保单/核保/理赔/商保直付/保险经纪）、membership 会员权益（家庭会员/健康权益/会员权益/会员渠道）、health_ai 医疗AI与数据（智能导诊/AI辅诊/医疗大模型/健康大数据/智能报告）、clinical_research 临床科研（真实世界研究/临床试验/科研协作/科研数据）、supply_chain 医药供应链（药械集采/SPD院内物流/器械耗材/DTP药房）、care_elderly 康复护理养老（居家护理/康复/长期护理保险/养老服务）、specialty_center 专科运营（肿瘤中心/妇儿中心/心理精神/口腔中心/中医中心/慢病专病）、patient_service 患者服务运营（患者教育/随访服务/满意度/投诉服务/患者关怀），补齐微医平台商业化与增量业务线；**② 术语 +37 至 98 条**：保险（保险经纪/保单/核保/惠民保/免赔额）、会员（家庭会员/健康权益/会员等级/微医通）、AI（智能导诊/AI辅助诊断/医疗大模型/健康大数据/智能报告）、科研（真实世界研究/临床试验/科研协作/知情同意/科研数据）、供应链（药械集采/SPD院内物流/器械耗材/DTP药房）、养老（居家护理/康复治疗/长期护理保险/养老服务）、专科（肿瘤中心/妇儿中心/心理门诊/专病管理/中医中心）、患者服务（患者随访/患者满意度/净推荐值NPS/患者教育/投诉处理）；**③ 维度 +6 至 29 个**：insurance_product_type 保险产品类型（医疗险/重疾险/惠民保/意外险/寿险/年金险）、member_level 会员等级（普通/银卡/金卡/铂金/家庭）、specialty_center_type 专科中心（肿瘤/妇儿/心理精神/口腔/中医/慢病专病）、care_service_type 照护类型（居家护理/康复治疗/长护险护理/养老照护）、ai_scene AI应用场景（智能导诊/AI辅诊/智能报告/病历质控/健康问答）、research_type 科研类型（真实世界研究/临床试验/科研协作/科研数据提取）。**验证**：真实 DB 执行成功（新增 45 域节点 + 37 术语 + 6 维度，幂等重跑 0 新增）；落库复核——主题域 114 节点（22 一级域，覆盖医疗供给侧流程 + 平台商业化/增量业务 + 患者运营侧）/术语 98/维度 29 | 无接口变更（纯参照数据 seed 扩展） | —（dimension/glossary/semantic 保持 released；主题域从七大主线扩展为十四大主线覆盖企业全业务版图，补齐保险/会员/AI/科研/供应链/养老/专科/患者服务八大缺失业务线） |
| 2026-08-27 | — | dimension+glossary+semantic | **参照数据补强医生层面 + 业务缺口（用户『为什么缺少医生层面的』+『确定按照这个清单补强，另外再次审查看还缺少哪些内容』）**：`seed_medical_reference.py` 从六大业务主线升级为**七大主线（+医生管理）**——**① 主题域 +10 至 69 节点**：新增「医生管理」一级域（doctor_management，微医供给侧核心资产）及 5 个二级子域（doctor_qualification 医生资质 / doctor_schedule 医生排班 / doctor_performance 医生绩效 / multi_site_practice 多点执业 / doctor_team 医生团队）；各业务线补缺口子域——医药（prescription_flow 处方流转）、医保（drg_dip 支付方式改革）、健康管理（corporate_health 企业健康）、健共体（family_bed 家庭病床）；**② 术语 +22 至 61 条**：医生层面 13 条（licensed_doctor 执业医师 / doctor_title 医师职称 / chief_physician 主任医师 / associate_chief_physician 副主任医师 / attending_physician 主治医师 / resident_physician 住院医师 / multi_site_practice 多点执业 / primary_practice_org 主执业点 / doctor_schedule 医生排班 / expert_clinic 专家门诊 / attending_doctor 责任医生 / doctor_team 医生团队 / doctor_service_duration 医生服务时长）+ 业务缺口 9 条（prescription_flow 处方流转 / drug_traceability 药品追溯 / drg DRG付费 / dip DIP付费 / outpatient_pooling 门诊统筹 / cancel_register 退号 / no_show 爽约 / corporate_health 企业健康管理 / family_bed 家庭病床）；**③ 维度 +5 至 23 个 + 医生维度扩充**：新增 doctor_title 医生职称（SCD0，5 级）、medical_org 医疗机构（SCD1，三级/二级/一级/社区/乡镇/村卫生室/互联网医院，健共体分级诊疗分析地基）、patient_source_channel 患者来源渠道（SCD0，APP/小程序/公众号/H5/线下扫码/转介绍）、visit_status 就诊状态（SCD0，待就诊/候诊/就诊中/已完成/取消/爽约漏斗）、payment_method 支付方式（SCD0，微信/支付宝/银联/医保个账/商保直付）；`outpatient_doctor` 医生维度成员 8→16（覆盖 14 科室，attributes 补 practice_type 全职/多点、doctor_level 专家/普通、is_expert）；**④ seed_dimensions 幂等增强**——维度已存在但成员集合与脚本不一致时自动刷新（删旧重灌），参照数据与脚本同步（不再「已存在即跳过」导致扩充成员不生效）。**验证**：真实 DB 执行成功（新增 10 域节点 + 22 术语 + 5 维度 + 医生维度 8→16 成员刷新，幂等重跑 0 新增）；落库复核——主题域 69 节点（14 一级）/术语 61/维度 23，医生维度成员 attributes 五字段（dept_code/title/practice_type/doctor_level/is_expert）齐全 | 无接口变更（纯参照数据 seed 扩展） | —（dimension/glossary/semantic 保持 released；参照数据从六大业务主线升级为七大主线，补全医生供给侧资产与医疗机构/来源渠道/就诊状态/支付方式等分析维度） |
| 2026-08-27 | — | dimension+glossary+semantic | **参照数据按六大业务主线重构（用户『应该从医疗、医药、医保、挂号、健康管理、健共体这些实际业务出发进行设计参照数据』）**：在上一轮医疗化基础上，将 `seed_medical_reference.py` 重构为**六大业务主线**组织——**① 主题域新增健共体一级域**（health_community 健共体，微医数字健共体核心）：7 个二级子域（medical_alliance 医联体 / medical_community 医共体 / referral 双向转诊 / remote_consult 远程会诊 / family_doctor_sign 家医签约 / shared_resource 资源共享 / public_health 公共卫生）；各业务线补充缺失子域——医疗（exam_lab 检查检验 / surgery 手术）、医药（drug_inventory 药品库存 / drug_procurement 药品采购）、医保（yb_directory 医保目录 / commercial_insurance 商业保险）、挂号（sign_in 签到取号）、健康管理（health_education 健康教育 / vaccination 疫苗接种），二级子域 30→47；**② 术语 +15 至 39 条**——健共体协作 9 条（two_way_referral 双向转诊 / upward_referral 上转 / downward_referral 下转 / medical_alliance 医联体 / medical_community 医共体 / remote_consultation 远程会诊 / family_doctor_contract 家医签约 / check_share 检查检验互认 / chronic_followup 慢病随访）+ 补充 6 条（药品比价 / 商保理赔 / 体检 / 疫苗接种 / 健康教育 / 签到取号）；**③ 维度 +6 至 18 个**——健共体 3 个（health_community_referral_type 转诊类型 上转/下转/平转/院间会诊、health_community_alliance_type 医联体类型 城市医疗集团/县域医共体/专科联盟/远程医疗协作网、health_community_sign_status 家医签约状态 已签约/待续签/已解约/未签约）+ 补充 3 个（checkup_project 体检项目 12 项、medication_drug_form 药品剂型 8 种、register_shift 出诊时段 上午/下午/夜间/周末），成员 96→134。**验证**：真实 DB 执行成功（新增 17 域节点 + 15 术语 + 6 维度，幂等重跑 0 新增）；六大业务主线——医疗（outpatient/online_consultation/internet_hospital/diagnosis）、医药（medication）、医保（medical_insurance）、挂号（appointment）、健康管理（health_management）、健共体（health_community）全部齐备 | 无接口变更（纯参照数据 seed 扩展） | —（dimension/glossary/semantic 保持 released；参照数据从「7 医疗域 + 4 线上域」升级为六大业务主线，补全微医数字健共体业务域/术语/维度） |
| 2026-08-27 | — | semantic+frontend | **SQL 批量解析候选支持批量设置责任方（用户『批量候选支持批量选择指定责任方』）**：批量解析结果区头部与批量编辑向导 Step1 各新增「批量设置责任方」入口——弹窗三要素：**责任角色**（产品负责/技术方/数仓开发 Segmented）+ **负责人**（ownerUsers Select，清空=移除该角色）+ **应用范围**（已勾选候选 N 个 / 全部候选 M 个 Radio，未勾选时「已勾选」禁用）；应用时单次 setState 批量写入候选 `product/tech/dw_owner_id+name` 双字段（name 由 ownerUsers 反查填充，提交透传三方责任），返回应用数量并在未勾选且范围=已勾选时提示「请先勾选候选指标或切换应用范围为全部候选」。**验证**：前端 MetricCreate **89 passed**（+2：勾选多个候选一次设置技术方，提交 payload 两个原子候选均带 `tech_owner_id=101` / 应用范围切「全部候选」时未勾选复合候选也生效，UI 断言其「产品负责」Select 显示张三）+ tsc 0 | 无接口变更（纯前端交互，复用既有候选编辑与提交链路） | —（semantic 保持 verified；批量候选可一键把某一方责任人应用到多个候选，免逐行 Select 重复操作） |
| 2026-08-27 | — | dimension+glossary+semantic | **维度/术语/主题域参照数据医疗化（用户『需要从医疗行业出发，完善维度管理里的维度，把现在的mock数据给清除掉』+『从微医企业的实际业务场景出发，覆盖并丰富维度、术语、主题域等这些参照数据』）**：新增幂等 seed 脚本 `backend/scripts/seed_medical_reference.py`——**① 清除 mock**：物理删除 8 条 E2E/测试维度（sales_e2e_*/e2e_*/test_*/batch_*/uncategorized_time/outpatient_e2e_ 前缀）及引用（dimension_member/metric_dimension/reconciliation/dimension_mapping，含清理指向不存在维度的孤儿对账记录）+ 8 条 E2E/测试术语（term 前缀匹配）及关联（term_version/term_relation/glossary_conflict）；**② 灌入微医参照数据**——主题域：保留既有 7 医疗域 + uncategorized，新增 4 个微医线上业务一级域（online_consultation 在线问诊/internet_hospital 互联网医院/appointment 预约挂号/health_management 健康管理）并为全部一级域补 30 个二级子域（挂号/就诊/处方/药品分类/收费项目/险种/病种/图文问诊/电子处方/号源/慢病管理等）；术语：24 条微医业务术语（图文问诊/电子处方/商保直付/医保目录/复诊患者/病历质控等，PUBLISHED 直灌）；维度：12 个医疗维度 + 96 成员（科室 20/医生 8/就诊类型 5/患者 8/病种 10/收费项目 8/医保类型 5/处方类型 4/药品分类 8/结算渠道 4/问诊渠道 4/预约类型 5，SCD0/SCD1/SCD2 三型）。**③ 前端占位文案医疗化**——Dimensions.tsx（「如 渠道」→「如 科室」、维度值说明/映射示例/数据源表名列名占位改 dwd.telemedicine/department_id/dept_code↔科室）、Glossary.tsx（「如 成交总额」→「如 门诊挂号人次」、同义词/边界占位改医疗）。**验证**：真实 DB 执行成功（清 8 维度+8 术语、灌 34 域节点+24 术语+12 维度+96 成员、孤儿引用归零、幂等重跑 0 新增）；后端 dimension/glossary/subject_domain 相关 **232 passed**；前端 Glossary+Dimensions **62 passed** + tsc 0 | 无接口变更（纯参照数据 seed + 前端占位文案） | —（dimension/glossary/semantic 保持 released；维度/术语/主题域从电商/E2E mock 全面切换为微医医疗业务参照数据） |
| 2026-08-27 | — | semantic+frontend | **SQL 批量解析候选口径完整展示 + 口径三方责任补齐（用户『口径表达式没有展示完整，需要把所有信息展示出来』+『责任方为什么只有产品负责这一方』）**：**① 口径完整展示**——候选 `definition_json` 此前仅行内截断展示 `expression||sql` 单键（CASE/窗口/子查询长口径被省略号截断，`source_tables/partition_key/dw_definition` 等字段完全不展示）；新增「口径详情」Modal（`defDetailCandidate` state）逐字段完整展示：口径表达式/完整 SQL/数仓详细口径（完整 SQL）等长文本用 `pre-wrap` 代码块不截断、源表/时间列·分区键/依赖指标用 Tag 列表、其余字段通用展示；候选卡片底部行「口径表达式」与派生候选「派生口径」、批量编辑向导「计算表达式/口径」列均新增「查看完整口径/查看口径」入口；**② 三方责任补齐**——批量候选此前仅「产品负责」一个 Select（注释明确『最小形态，tech/dw 随创建人/域默认』），现候选卡片底部行与批量编辑向导 Step1 表格均补「技术方」「数仓开发」两列（`ownerUsers` 选项，`handleSqlBatchEdit` 写入 `tech_owner_id/dw_developer_id`，提交透传后端 Phase1 落三方责任）；`SqlBatchRegisterCandidate` 类型补三方责任字段（对齐实际提交）。**验证**：前端 MetricCreate **87 passed**（+2：候选「查看完整口径」弹窗展示 expression/source_tables/partition_key/dw_definition 不截断、候选行设置技术方/数仓开发后提交 payload 携带 `tech_owner_id=101/dw_developer_id=102`；批量编辑向导 Step1 断言补技术方/数仓开发列）+ 前端全量 **1072 passed**（60 文件）+ tsc 0 | 无接口变更（纯前端展示与表单；`SqlBatchRegisterCandidate` 类型补字段，提交本就携带） | —（semantic 保持 verified；批量候选口径全部信息可查看不截断，责任方三方可设置） |
| 2026-08-27 | — | frontend | **主数据审批工作台加类型聚焦筛选（用户『主数据审批里为什么把维度、术语审批混合在一起』，方案 1）**：审批工作台在「待我审/我审过的」视角 Segmented 旁新增**类型筛选 Segmented**（全部/维度/逻辑度量/术语，默认「全部」保持聚合视图），纯前端过滤（`filteredItems` 按 kind 派生，切换即时响应不重新请求）；分页 total/空态文案随类型筛选联动（如「当前无待您评审的『维度』」）。**验证**：前端 MasterDataReview **7**（+1：类型筛选切换维度/逻辑度量/术语/全部，Segmented 用 `getByTitle` 定位避免与表格 Tag 冲突；既有「类型标签齐全」断言改 `getAllByText` 兼容 Segmented 文本）+ tsc 0 | 无接口变更（纯前端展示） | —（semantic 保持 verified；主数据审批可一键按类型聚焦，默认仍聚合） |
| 2026-08-27 | — | semantic+conflict+frontend | **冲突处置闭环补齐（用户『冲突仲裁对比报"指标不存在"』+『冲突升级后没有后续处理流程』，A+B+C）**：**① 悬空冲突处置（A）**——`delete_metric` 删除前检查未决冲突引用（`ConflictRepository.count_open_for_metric`，有则 400 `CONFLICT_EXISTS` 提示先处置）；`_get_metric_for_compare` 对普通软删（无仲裁 successor）指标返回友好 `METRIC_DELETED`（携带 deleted_at，前端仲裁台提示「建议强制关闭」）而非裸 404；conflict service 新增 `force_close`（OPEN/NEGOTIATING/ESCALATED → CLOSED，decision_json 留痕 force_closed + 关闭人，发布 conflict_forced_closed 事件）+ API `POST /conflicts/{id}/force-close`（治理角色）；**② 升级链路补全（B）**——sla_tasks 新增 `remind_stale_escalated`（每日 cron，扫描升级超 48h 未处置的 ESCALATED 冲突，定向通知其域 domain_admin + 全部 platform_admin，event=`conflict_escalation_overdue`）；通知事件 `conflict_forced_closed`/`conflict_escalation_overdue` 注册订阅+标题映射；**③ 前端（B）**——仲裁台加「升级队列」快捷筛选按钮 + ESCALATED 超时红标（`updated_at` 超 48h 显示「升级超时」Tag）+ 操作列「强制关闭」按钮（Popconfirm 二次确认）+ 对比 `METRIC_DELETED` 友好提示；Notifications 补两新事件标签；**④ 存量清理（C）**——真实 DB 将 3 条悬空冲突（CF-29A5FFB47318/CF-40D5CEE19793/CF-9A11C3EF548A，引用已删候选 oldpagetransferordercnt_day）置 CLOSED + force_closed 留痕 + 清候选指标 pending_conflict。**验证**：后端 test_conflict_service +2（force_close 成功留痕/事件/清标记 + RULED 拒绝）+ test_conflict_sla +2（超时通知管理员 + 未超时跳过）+ test_semantic_service +1（普通软删 METRIC_DELETED）+ test_metric_lifecycle_api +1（CONFLICT_EXISTS 拦截）+ 相关 **409 passed** + ruff 0；前端 ReviewWorkbench **23**（+3：强制关闭确认调用 forceCloseConflict / 升级超时红标 / METRIC_DELETED 友好提示）+ Notifications 52 + tsc 0 | 新增 `POST /conflicts/{id}/force-close`；`ConflictResponse` 新增 `updated_at`；DELETE 删除被未决冲突引用的指标返回 400 `CONFLICT_EXISTS` | —（semantic/conflict 保持既有状态；冲突处置闭环——删除防悬空、对比友好提示、强制关闭、升级超时强提醒全部落地） |
| 2026-08-27 | — | semantic+frontend | **指标详情页挂载实体卡片化展示（用户反馈『指标详情页里展示的挂载实体展示有些紧凑，布局样式也不美观，无法让用户很直观感受到挂载实体的具体信息』）**：挂载实体从「单行 flex 文本堆叠」（表名 Tag + 一段 `列：x · 粒度：y · 默认周期：z · 域：w · 业务限定：…` 挤在一起 + 责任方 Tag + 解除按钮）改为**卡片式 Descriptions 网格**——每个挂载实体一张浅灰底圆角卡片：卡片头（数据库图标 + 等宽粗体物理表名 + 「业务限定」橙色 Tag + 右上角解除挂载按钮，按钮不随字段换行）、字段区（`Descriptions column=2`：源列/粒度/默认周期/业务域/业务限定/责任方，标签与值分离、空值「—」/「继承指标级」占位、责任方按角色着色 Tag——产品蓝/技术紫/数仓青）；多变体各占一卡，一眼可辨每变体的物理表与口径要素。**验证**：前端 MetricDetail **84 passed**（2 项挂载展示测试改为卡片内 `within(mount-card-*)` 精确断言：表名/源列/粒度/默认周期/业务域/业务限定/责任方逐项 + 双卡双解除按钮）+ tsc 0 | 无接口变更（纯前端展示布局） | —（semantic 保持 verified；挂载实体从紧凑文本行升级为卡片网格，物理表/粒度/周期/域/业务限定/责任方逐项清晰展示） |
| 2026-08-27 | — | semantic | **SQL 智能推断解析耗时优化（用户『SQL智能推断解析时长耗时达到42000ms，还需要进行优化』，方案 A+C）**：**① 域建议并行（A1）**——整段业务域建议由「切分后串行 await」改为 `asyncio.create_task` 后台任务，与切分/阶段1/阶段2 并行（目录/挂载未命中时 LLM 兜底 4-7s 不再串行阻塞；阶段 2 后 await，unique/llm 定域则清空逐语句建议）；**② 校验层默认关闭（A2）**——`llm_validate_measures` 规则模式固定串行 4-7s 改为默认关闭（新增 `validate_measures` 参数，显式开启才触发；use_llm 模式由 annotate 承担不重复校验），普通「解析候选」从 6.8s → 亚秒级；**③ client 一次构建复用（A3）**——infer_sql_batch 构建一次 LLM client 传入切分/域建议/周期/度量/补全各兜底（消除 N 次重复 DB 查询+解密）；**④ 429 重试收紧（A4）**——`client.chat` 新增 `retries` 参数覆盖全局重试次数，推断类调用传 1（限流重试大概率仍 429，避免叠加放大墙钟）；**⑤ 解析结果 Redis 缓存（C）**——`POST /parse-sql-batch` 按请求参数 hash 缓存 300s，同一段 SQL 反复调试秒回（Redis 不可用降级不缓存）。**验证**：后端 test_sql_split +3（校验层默认关/显式开、域建议 task 启动/指定域不启动、client 只构建一次）+ test_llm_retry +2（retries=1 总 2 次/retries=0 不重试）+ test_sql_batch_api +1（缓存命中不再调推断/参数变化 miss）+ 相关 **439 passed** + ruff 0（1 项 test_llm_client test_chat_not_enabled 既存失败——LLM 配置默认值致 LlmClient() 无参 enabled=True，stash 验证与本次无关） | 无接口变更（`validate_measures` 后端可选参数默认关，前端不传即生效） | —（semantic 保持 verified；SQL 批量解析从多次串行 LLM 叠加 42s 降至规则模式亚秒/LLM 模式约 7-10s，反复调试秒回） |
| 2026-08-27 | — | semantic+frontend | **SQL 智能推断抽屉可拖宽 + 批量解析候选卡片化布局（用户反馈『批量解析结果展示布局有些紧凑，用户阅读不够直观，需要优化布局；另外 SQL 智能推断侧边栏也要支持左右移动』）**：**① 抽屉可拖宽**——SQL 智能推断抽屉由固定宽度（单条 540/批量 760）Drawer 换为 `ResizableDrawer`（复用全站详情抽屉自研组件）：左缘 6px 拖拽手柄左右拖动调整宽度（clamp 520–1200），`storageKey=unisense.drawer.sql-infer.width` 持久化到 localStorage（对齐全站左侧侧边栏/详情抽屉拖拽交互）；**② 批量结果卡片化**——每个候选从「13+ 控件单行 flex 换行拥挤布局」改为「卡片三区」：头部行（勾选 + 类型 + 名称 + AI推断/口径需核对/建议域/需先发布依赖原子/派生周期驱动徽标 + 在向导中编辑）、字段区（原子 = 聚合/单位/周期/粒度/关联逻辑度量/产品负责，派生/复合 = 依赖指标/计算表达式/派生口径；每字段「11px 小标签置顶 + 控件」纵向排列，一眼可辨）、底部行（指标编码 + 口径表达式 Tooltip）；Collapse 可视高度 360→480；全部 data-testid 保留（含 req-deps/req-expr 必填星号迁至字段 label 旁）。**验证**：前端 MetricCreate **85 passed**（+1：抽屉拖拽手柄存在 + 候选字段标签 聚合/关联逻辑度量/指标编码 展示）+ tsc 0 | 无接口变更（纯前端布局与交互） | —（semantic 保持 verified；批量解析结果按「头部/字段/编码」三区卡片化更直观，抽屉可左右拖宽并记忆宽度） |
| 2026-08-27 | — | semantic+frontend | **统一审批中心（用户『把指标审批以及其他有关审批的工作流都整合到一起』，含冲突仲裁）**：新建 `/approval` 页（`ApprovalCenter`）——antd Tabs 三 Tab 内嵌 `MetricReview`/`MasterDataReview`/`ReviewWorkbench`（三者均加 `embedded` prop 隐藏各自 page-head 防嵌套头部，功能零降级）；URL `?tab=metrics|master-data|conflict` 深链同步；权限语义保留——tab 按 `metric:review`/`master-data:review`/`review:view` 过滤（无权 tab 隐藏、全无权限空态），菜单「审批中心」按 `canAny` 三权限点过滤（替换原「指标审批/主数据审批/冲突仲裁」三个入口）；**原深链路由 `/metrics/review`、`/master-data/review`、`/review` 保留并重定向到对应 Tab**（不破坏既有书签/通知/旧链接）；通知/待办跳转统一指向 `/approval?tab=`（`actionFor` metric→metrics、主数据→master-data、冲突→conflict；`fieldTarget` conflict_id、`handleOpen` conflict；TodoCenter conflict/review）。**验证**：前端 ApprovalCenter **3**（三 Tab 默认激活指标审批/`?tab=conflict` 深链直达/切换挂载对应工作台）+ Notifications 52 + TodoCenter + Layout + 前端全量 **1058 passed**（1 项 LineageView 既存 flaky 单独跑 48 全绿，与本次无关）+ tsc 0 | 新增 `/approval` 路由；原三个深链路由语义不变（重定向兼容） | —（semantic/frontend；审批/仲裁入口从三处合并为统一审批中心） |
| 2026-08-27 | — | dimension+glossary+semantic+frontend | **统一主数据审批工作台（用户『维度发起审批指定用户后在哪个入口审批』）**：此前维度/逻辑度量/术语被指派评审人无统一审批入口——散落各管理页状态筛选「审核中」行内按钮，通知无直达、列表无 reviewer 过滤（只有指标有 /metrics/review 工作台）。本次补齐：**① 后端**——dimension/measure_catalog/glossary 列表接口新增 `reviewed_by` 参数（「我审过的」过滤 `approver_id==X OR reject_reviewer_id==X`，与指标模块语义一致，repository/service/api 三层透传）；policy.py 新增 **`master-data:review`** 权限点（domain_admin/reviewer 角色，防悬空测试通过）；**② 前端**——新建 `MasterDataReview` 页（路由 `/master-data/review` + 菜单「主数据审批」+ ROUTE_PERM）：Segmented「待我审/我审过的」；待我审 = 三模块 `status=REVIEW` 拉取 + 前端 `canReviewMasterData` 只显示当前用户可审的行（指定用户/域评审组/未指派域管理员兜底）；我审过的 = 三模块 `reviewed_by=me` 聚合展示通过/驳回处理结果（原因+时间）；行内通过/驳回复用共享 `MasterDataReviewActions`/`MasterDataReviewModals`（按类型分发 approve/reject api）；**③ 通知**——`dimension/measure/term.submitted` 补事件标签/影响文案/待处理集合，`actionFor` 加「去审批」直达按钮跳 `/master-data/review`。**验证**：后端 test_dimension_repository/test_measure_catalog/test_glossary_repository +6（reviewed_by 过滤 approver/rejector + 缺省不加条件）相关 **159 passed** + test_governance_policy 41 + ruff 0；前端 MasterDataReview **6**（待我审只显示我可审/我审过的处理结果/通过按类型分发/驳回弹窗）+ Notifications 52 + Layout 10 + 前端全量 **1054 passed** + tsc 0 | 三模块 `GET /dimensions|/measure-catalogs|/terms` 新增可选 `reviewed_by`（向后兼容缺省不过滤）；新增权限点 `master-data:review` | —（semantic/dimension/glossary 保持既有状态；被指派评审人/域评审组在统一工作台一处完成主数据审批，通知直达） |
| 2026-08-27 | — | semantic+frontend | **指标注册向导关联维度仅加载已发布维度（用户『注册指标向导里关联的维度必须是已经发布的』）**：此前 `listDimensions` 误传 `status="active"`（维度状态枚举 DRAFT/REVIEW/PUBLISHED/DEPRECATED 无 active，后端精确匹配恒空）导致选项框为空，修复为不带 status 展示全部（cd42622a）；现按业务规则收紧为**仅 `status="PUBLISHED"`**——注册向导（MetricCreate Step②）与编辑弹窗（MetricDetail）关联维度下拉统一只加载已发布维度（与依赖指标/逻辑度量下拉一致），未发布（DRAFT/REVIEW）维度不可作为指标关联维度；血缘据此生成的指标↔维度边仅关联已发布维度。**验证**：前端 MetricCreate **81 passed**（防回归用例改为断言 `listDimensions` 携带 `status="PUBLISHED"`）+ MetricDetail 84 + tsc 0 | 无接口变更（纯前端筛选参数：`GET /dimensions?status=PUBLISHED`） | —（semantic 保持 verified；指标关联维度必须是已发布维度，DRAFT/REVIEW 不可选） |
| 2026-08-27 | — | metric_mount+frontend | **变体级口径三方责任（方案 B 延伸，用户『指标多变体对应不同需求方/开发等角色』）+ 挂载粒度 Select 化**：**后端**——① 迁移 0106：`metric_mount` 加 6 列（`product_owner_id`/`tech_owner_id`/`dw_developer_id` BigInteger + `product_owner_name`/`tech_owner_name`/`dw_developer_name` String(128)，与 metric 表同构，空 = 继承指标级，幂等自愈）；② schema：`MetricMountInput`/`Create`/`Update`/`Response` 加 6 字段；③ service：`create_mount`/`update_mount` 落库（责任方为治理属性——PUBLISHED 改责任方不拦截、不改粒度/源表即直接生效）；④ semantic `_mount_to_dict`/`_mount_input_to_dict`/`_apply_mounts` 快照与对齐透传（版本 diff 转正不丢责任方）。**前端**——① 注册向导挂载行粒度 Input → **Select**（字典 granularity options + `withUncollectedOption` 手输兜底，字典外业务粒度如 医院/药品 仍可手输）；② 挂载行加产品需求方/技术方/数仓开发三个 `RoleOwnerSelect`（空 = 继承指标级）；③ 提交透传 id+name（拆 `RoleOwnerValue`）；④ 详情页挂载卡逐行展示责任方（平台用户 id 解析 / 外部人员名称 Tag）。**验证**：后端 test_metric_mount **21**（+2 create/update 责任方透传）+ semantic/metric_mount **310 passed** + 全量 unit 3903 passed（9 failed 均为 master 既有，先前干净 worktree 对照确认）；前端 MetricCreate **81**（+粒度字典默认选项、多变体责任方选人提交）+ MetricDetail **84**（+挂载行责任方展示）+ tsc 0；真实 DB `alembic upgrade head` 成功、6 列确认、多挂载不同责任方插入回滚验证 | `POST/PUT /metric-definitions` 挂载数组支持变体级责任方字段（向后兼容，可空；空 = 继承指标级） | —（semantic 保持 verified；变体责任方归属细化——多变体可归属不同需求方/开发角色，挂载粒度可点选可手输） |
| 2026-08-27 | — | collector | **描述缺失治理支持按库筛选（延续『采集目录中的描述缺失治理要支持按照数据源、表筛选进行治理』）**：`GET /catalogs/description-coverage` 新增 `database` 服务端过滤——库名取 `entity_name` 前缀（`库.表`）`LIKE '{db}.%'` 精确匹配（通配符转义对齐 list_catalogs），汇总指标（表数/有描述表数/字段数/有描述字段数）与 per_table 明细统一应用；治理面板（DescriptionCoveragePanel full）筛选栏在数据源与表名搜索之间新增「全部库」下拉（listCatalogDatabases 同源、随所选数据源联动收窄、allowClear/showSearch），重置筛选一并清空库；summary 只读总览不展示。**验证**：后端 test_collector +2（database 前缀过滤应用到汇总/明细 + 通配符转义 literal_binds 断言）+ test_collector_api +1（端点透传）+ collector 全量 **220 passed** + ruff 0；前端 Catalogs **47**（+1：选库后按 database 重新拉取 + 重置清空 + 库下拉随数据源联动）+ AssetMap 59（summary 不受影响）+ tsc 0 | 新增 `GET /catalogs/description-coverage?database=`（向后兼容，缺省不过滤） | —（collector 保持 released；治理面板从按数据源/表聚焦升级为按数据源/库/表聚焦治理） |
| 2026-08-27 | — | collector | **描述缺失治理支持按数据源、表筛选（用户需求『采集目录中的描述缺失治理要支持按照数据源、表筛选进行治理』）**：`GET /catalogs/description-coverage` 新增 `source_id`/`keyword` 服务端过滤——汇总指标（表数/有描述表数/字段数/有描述字段数）与 per_table 明细统一按筛选口径收窄，`fields_with_desc` 查询补 join db_catalog/data_source 限定范围（否则字段覆盖统计不随筛选收窄而失真），keyword 用 entity_name LIKE 通配符转义（对齐 list_catalogs）；治理面板（DescriptionCoveragePanel full）新增筛选栏——数据源下拉（listDataSources 同源，allowClear/showSearch）+ 表名搜索（350ms 防抖，对齐主列表）+ 重置筛选，统计卡与治理表格按筛选口径计算、批量推断仅在筛选范围内勾选；summary 只读总览不展示筛选栏（不拉数据源）。**验证**：后端 test_collector +1（repository 过滤：汇总/明细语句均含 source_id 与 LIKE、fields_with_desc join db_catalog）+ test_collector_api +1（端点透传）+ collector 全量 **217 passed** + ruff 0；前端 Catalogs **46**（+2：数据源筛选、表名搜索）+ AssetMap 59（summary 不受影响）+ tsc 0 | 新增 `GET /catalogs/description-coverage?source_id=&keyword=`（向后兼容，缺省不过滤） | —（collector 保持 released；治理面板从全量治理升级为可按数据源/表聚焦治理） |
| 2026-08-27 | — | semantic | **指标目录回收站彻底删除闭环 + 存量 PUBLISHED 残留清理（用户『回收站里看到已发布状态指标』）**：**① 根因**——6 条 PUBLISHED + 1 条 DEPRECATED E2E 测试指标（sales_e2e_*/user_e2e_*，2026-08-24 06:20 软删）早于删除守卫（e4422c46，仅 DRAFT/DEPRECATED 可删）存在，软删只置 deleted_at 不改 status → 保持 PUBLISHED 进回收站且 restore 仅允许 DRAFT → 卡死；**② 彻底删除闭环**——新增 `POST /metric-definitions/{code}/purge`（仅平台管理员、仅 deleted_at 置位、物理删除 + 级联清理 metric_version/metric_dimension/metric_health_score/metric_value_snapshot/metric_mount/lineage_edge 关联，单事务；审计 `metric_definition.purge`），前端回收站操作列加「彻底删除」按钮（Popconfirm 二次确认，仅平台管理员可见，外层 span 拦冒泡防行点击）；**③ 存量清理**——真实 DB 单事务物理删除 7 条 E2E 残留（血缘 27 边/版本 7/维度 2/健康度 7 级联清除），回收站余 19 条 DRAFT 可恢复记录、PUBLISHED/DEPRECATED 已删残留归零。**验证**：后端 test_metric_lifecycle_api +1（purge 200 + 角色传递）+ test_semantic_service +1（purge 成功/未删拒绝/非管理员拒绝/不存在）+ 相关 **335 passed** + ruff 0（仅既有 SIM102/B007）；前端 MetricCatalog **59**（+1 彻底删除二次确认调用 purgeMetric，顺带补回收站 describe 缺失的 mockedMeasures 默认值修复顺序依赖）+ tsc 0 | 新增 `POST /metric-definitions/{code}/purge`（仅平台管理员） | —（semantic 保持 verified；回收站从"只能恢复"升级为"恢复或彻底删除"完整闭环，已发布已删残留清零） |
| 2026-08-27 | — | semantic | **SQL 智能推断行内投影注释回填候选名称（用户反馈『批量解析生成的指标名称不能把字段注释的描述回填进去』，如 `all_order_cnt-session_side_order_cnt-region_org_order_cnt as old_page_transfer_order_cnt -- 转诊预约旧页面` 解析成「日订单量（old_page_transfer_order_cnt）」而非「转诊预约旧页面」）**：**① 投影注释提取**——sqlglot 把 SELECT 投影后的行内 `-- 中文` 注释挂在投影节点（Alias/Column/AggFunc）的 `comments` 上，新增 `_projection_comment` 提取（P0-B 裸聚合被 `alias_` 包裹时注释在内层 this，先取投影自身再取 this），`_projection_measures`（位图/HLL、collect、条件聚合、coalesce 多聚合、普通聚合全分支）与 `_collect_derived_measures`（A7 算术派生列 + 比率派生）的 measure 统一携带 `comment`；**② 候选注入**——`_build_atomic_candidate` 把行内注释注入 `measure_meta.comment`（**优先于**建表 DDL 列注释，行内注释是开发在当前 SQL 投影里写的字段语义、更贴近这条口径；建表注释兜底，A-5 行为不变），`_infer_name`/`_infer_unit` 自动消费——「总订单数/会话侧栏/区域上级机构订单量/转诊预约旧页面」替代列名词表兜底的「日订单量」，同列多 count 名称不再雷同（无需 C 去重后缀）、外层算术派生列名称从「日订单量（old_page_transfer_order_cnt）」变为「日转诊预约旧页面」。**验证**：test_sql_split **+2**（用户场景端到端：内层聚合三注释回填 + 外层派生列注释回填 + 名称互异；裸聚合注释/无注释不污染）+ 相关 6 文件（sql_split/sql_infer/auto_fill/sql_batch_api/metrics_infer_api/sql_validation）**245 passed** + ruff 0 | 无接口变更（候选 name 沿用既有字段，注释仅提升推断质量） | —（semantic 保持 verified；SELECT 投影里的 `-- 字段注释` 回填为指标名称/单位推断来源） |
| 2026-08-27 | — | semantic+frontend | **指标注册向导逻辑概念先行（用户『注册指标应先填逻辑概念（业务域/粒度/维度/指标名称/责任方），然后才是具体实现（业务口径/技术口径/数仓开发口径）』）**：**① 粒度入②基本信息**——原子指标顶部静态只读「日 (day)」（不再藏于治理 Collapse，治理内粒度项移除）；派生/复合保留 dictSelect（缺省取挂载粒度⑥）；**② 关联维度入②基本信息**——从③口径定义 expression 分支移出、去掉口径模式门控，expression/SQL 两种模式均展示（血缘据此生成指标↔维度边）；**③ 三层口径随实现步骤**——③ 口径定义整卡移入 Step2（⑥与⑦之间）重编号④（含业务口径/伪代码口径/数仓SQL口径/技术口径SQL/计算表达式/口径定义JSON），责任方卡重编号③；**④ Steps 描述/注释同步**（Step1 名称/粒度/维度/责任方/消费指南；Step2 三层口径/类型/来源/挂载 + 提交）。**验证**：前端 MetricCreate **78 passed**（+1 关联维度双模式可见；原子粒度断言反转「日 (day)」可见；数仓口径/三层口径/AI生成用例 goToStep 1→2；冲突预检/三层口径合入提交顺序重排；计算表达式断言同页）+ 前端全量 **1038 passed**（58 文件）+ tsc 0 | 无接口变更（纯前端向导结构与交互） | —（semantic 保持 verified；逻辑概念先行、具体实现后置的注册动线） |
| 2026-08-27 | — | semantic+frontend | **指标多挂载（一变体一挂载行，OneData 变体承接升级）**：派生指标一次创建可录入全部变体（粒度 × 业务限定 × 周期组合），不必为每个变体单独建指标。**后端**——① 迁移 0105：`metric_mount` 加 `business_filter` 列 + 放开 `uk_mount_metric` 唯一约束改普通索引 `idx_mount_metric`（幂等自愈：先建普通索引顶替外键索引再 drop 唯一约束，规避 MySQL 1553）；② schema：`MetricCreateRequest`/`MetricUpdateRequest` 加 `mounts: list[MetricMountInput]`（兼容旧 `mount` 单数合并，非派生携带挂载 422）+ 指标级 `default_business_filter` 兜底（写 `definition_json.business_filter`）+ `MetricResponse.mounts` 详情回填；③ service：创建多行落库、更新全量 diff 对齐（有 id 更新/无 id 新增/未出现软删，隐式 id 绑定兼容旧单挂载 upsert）、破坏性判定（删挂载行/改粒度或源表 → PENDING_VERSION 确认流，diff_json 携带 `mounts` before/after 快照，转正全量对齐）、默认变体粒度冗余回填；④ 消费：`_resolve_mount_table` 默认变体（default_period 行优先）+ 显式 `QueryRequest.variant`（挂载行 ID 或 `粒度:周期`，混合渐进 a 兜底 + b 可覆盖）；⑤ 独立挂载 API 一指标可多行（create_mount 去 MOUNT_EXISTS、删除后回填默认粒度）。**前端**——挂载实体区单表单 → Form.List 多行编辑器（每行源表/度量列/粒度/默认周期/业务限定，可增删），详情页挂载卡逐行展示全部变体 + 业务限定。**验证**：后端 semantic/metric_mount/consume 相关 **460 passed**（新增多变体创建/diff 对齐/新增非破坏/删除破坏/转正快照/变体解析测试），全量 unit 3883 passed（9 failed 均为 master 既有，干净 worktree 对照确认）；前端 MetricCreate **77 passed** + MetricDetail **83 passed**（新增多变体提交 mounts 数组与逐行展示测试）+ tsc 0；真实 DB `alembic upgrade head` 成功、同 metric_id 双挂载插入通过（回滚） | `POST/PUT /api/v1/metric-definitions` 支持 `mounts` 数组（旧 `mount` 兼容）；`POST /consume/query` 支持可选 `variant`；`GET /api/v1/metric-definitions/{code}` 返回 `mounts` 列表 | semantic 保持 verified（多挂载放行：一指标多变体，版本/血缘/搜索以指标为单位不受影响；挂载删除/改粒度仍走 PENDING 破坏性确认流） |
| 2026-08-27 | — | semantic+frontend | **指标注册向导步骤重排（用户『注册指标应先选业务域、指定指标基本信息（粒度/口径/消费指南/口径责任方），然后才是具体实现（挂载/依赖实体）』）**：**① 4 步 → 3 步**——Step0 业务域；**Step1 指标基本信息**（② 编码/名称/聚合/高级治理设置/消费指南 + ③ 口径定义（表达式/SQL/三层口径/关联维度）+ ④ 口径责任方，从原 Step2/3 前移）；**Step2 具体实现**（⑤ 指标类型 + ⑥ 原子来源/依赖指标/基础原子/挂载实体 + ⑦ 关联数据表（依赖/使用表，从口径定义卡抽出）+ 提交）；**② 导航**——Steps items 3 项、下一步文案「指标基本信息/具体实现」、handleNext clamp 2、提交按钮落 Step2；**③ 引用同步**——依赖/使用表随三类表 Alert 移入⑦、批量候选「在向导中编辑」定位 Step⑥、血缘推断回填提示改 Step⑦、粒度 extra 挂载引用改⑥。**验证**：前端 MetricCreate **76 passed**（goToStep 支持回退，全部步骤断言按新结构更新）+ tsc 0 | 无接口变更（纯前端向导结构与交互重排） | —（semantic 保持 verified；业务信息先行、实现细节后置的注册动线） |
| 2026-08-27 | — | semantic | **指标目录列表对含驳回记录的库 500 修复（线上 `GET /api/v1/metric-definitions?... 500`，后端日志 `MetricResponse.rejected_at Input should be a valid string … input_type=datetime`）**：**根因**——`MetricResponse.rejected_at` 自引入（驳回追溯 FR-005）起声明为 `str | None`，但 service reject 落库写的是 `datetime.now(UTC)`、ORM 列为 datetime；此前库中无驳回记录故潜伏，2026-08-27 02:45 出现首条真实驳回指标（id=35）后，列表接口 `MetricResponse.model_validate(m)`（api/metrics.py:340）遇 datetime 抛 ValidationError → 不带 status 过滤（含驳回记录）的列表/分页 500；**修复**——schema 字段类型 `str | None → datetime | None`（model_validate 通过，pydantic 序列化输出 ISO 字符串，前端 `formatCnTime` 本就按字符串解析，零前端改动）；排查 `MetricResponse` 其余时间字段（approved_at/description_updated_at/deprecated_at 均 datetime、sunset_until date）无同类隐患。**验证**：新增回归测试 `test_list_metrics_rejected_record_returns_200`（mock 返回 rejected_at=datetime 的驳回记录，断言列表 200 且序列化为 ISO 串），test_metric_catalog_enhance 7 过 + ruff 0；**容器内连真实 DB 全量复核**：36 条 Metric（含真实驳回 id=35）`MetricResponse.model_validate` 全部通过；干净快照（git archive HEAD，排除并行会话未提交 WIP）重建 `unisense/backend:dev` 并重启 backend 生效 | `GET /api/v1/metric-definitions` 响应 `rejected_at` 类型由 str 修正为 datetime（序列化为 ISO 字符串，前端契约不变） | —（semantic 保持 verified；驳回指标不再炸列表 500） |
| 2026-08-27 | — | ai+frontend | **LLM 连通性探测支持无 /models 网关（用户反馈保存火山方舟 Coding Plan 实例后报「实例已保存但连通失败：LLM 网关不支持 GET /models 端点（HTTP 404）…无法自动验证连通性」）**：**① 根因**——方案 B' 两步探测把「网关未实现 GET /models（404/405/501，火山方舟/腾讯混元等 OpenAI 兼容网关）」当作硬失败短路返回，但 /models 为可选端点，网关可达并不代表不可用，真实 chat 才是连通的最终判据；**② 修复**——`_quick_probe` 对 404/405/501 由硬失败改为返回 `ok=True + models_supported=False`（网关可达但无该端点），`_probe` 放行到第二步真实 chat 探测：chat 通过则整体判连通成功（不再误报「连通失败」），chat 失败则错误信息区分「网关可达但未实现 GET /models 端点，且模型真实推理失败」与「网关可达且鉴权通过…真实推理失败」；`LlmConfigTestResult` 新增 `models_supported` 字段（None=未探测/失败、True=正常、False=网关未实现已回落 chat 验证）；**③ 前端**——`TestResultBadge` 连通成功且 `models_supported===false` 时明示「网关无 /models，已用真实推理验证连通」，避免「可用模型 0 个」困惑。**验证**：后端 test_llm_config_service 97（+2：404 回落 chat 成功 + 404 且 chat 失败明确报错；既有成功用例补 models_supported 断言）+ test_llm_config_api/test_llm_client 全过 + ruff 0；前端 tsc 0 | `POST /api/v1/ai/config/test` 响应新增 `models_supported`（扩展，向后兼容） | —（ai 保持 released；无 /models 的兼容网关连通性由真实 chat 探测验证，保存后自动测试不再误报失败） |
| 2026-08-27 | — | semantic+frontend | **SQL 批量编辑向导第二步「口径与责任」布局修复 + 批量创建按钮反馈兜底（用户反馈『依赖指标过多且过长时展示不全/内容覆盖到计算表达式口径列、点批量创建没有任何反应』）**：**① 布局**——向导 Modal 加宽 **940→1280**，Step0/1 表格加横向滚动（`scroll.x` 960/1020，固定列宽不再相互挤压/溢出）；「依赖指标」多选加 `maxTagCount="responsive"`（过多/过长依赖折叠为 +N，不再撑破列宽覆盖相邻「计算表达式/口径」列）+ 列加宽（依赖 225→260、口径 180→230），口径 Input/Typography 加宽（220/210），口径列 ellipsis 截断 + Tooltip 悬停看全量口径；**② 按钮反馈兜底**——两个「批量创建选中指标」按钮移除 `disabled`（此前批量无原子候选时默认勾选为空、按钮禁用点击被浏览器静默忽略 = 用户感知『点击没反应』），未勾选时点击给明确 warning 提示 + 按钮下方常驻灰色引导文案「请先勾选候选指标（默认仅勾选原子候选，派生/复合需手动勾选）」；空勾选 warning 文案增强。**验证**：前端 MetricCreate **74**（+1：向导步骤③点击「批量创建选中指标」正常触发 batch-register-from-sql 回归用例——此前该路径无测试覆盖）+ tsc 0 | 无接口变更（纯前端布局与交互反馈增强） | —（semantic 保持 verified；依赖多选折叠不溢出口径列、批量创建按钮点击始终有明确反馈） |
| 2026-08-27 | — | semantic | **派生指标「基础原子指标」OneData 语义落地（用户『派生指标难道不应该绑定原子指标吗』）**：**① 派生可绑定基础原子**——`definition_json.base_atomic`（基础原子指标编码，选填）：前端派生分支新增「基础原子指标」专属选择器（只搜已发布原子指标，通俗提示「这个派生指标是基于哪个原子算出来的」），提交合入口径 JSON；后端 `_validate_base_atomic` 校验 base_atomic 存在且为原子类型（非原子/不存在 422 `BASE_ATOMIC_NOT_ATOMIC`/`BASE_ATOMIC_NOT_FOUND`），create/update 全链路生效；**② 血缘区分基础边**——`lineage_edge_type` 枚举新增 **`BASED_ON`**（迁移 0104 双表 ALTER），派生注册 `metric:{base} → metric:{code}` 的 BASED_ON 基础边，与 `dependencies` 的 DERIVED_FROM 普通上游引用区分（血缘图可识别"哪个原子是派生的基底"）；`metric_referrers` 纳入 BASED_ON（废弃基础原子时拦截提示「基础原子（派生基底）」）；**③ 前端展示**——LineageView/MetricDetail/MetricCatalog 边标签与配色补 BASED_ON（「基于原子」紫色）。**验证**：test_semantic_service **+5**（BASED_ON 边/边并存/base_atomic 三场景校验）、test_semantic_schemas **+1**（规范化）；相关 6 文件 **654 passed**；前端 MetricCreate **+1**（选基础原子提交 base_atomic）、MetricCatalog/LineageView **106**、tsc 0 + ruff 0 | 新增口径字段 `definition_json.base_atomic`（JSON 内，无表结构变更）；`lineage_edge_type` 枚举 +BASED_ON（迁移 0104） | —（semantic 保持 verified；派生 = 基础原子 + 业务限定 + 时间周期 结构化落地，血缘基础边可区分） |
| 2026-08-27 | — | semantic | **SQL 智能推断工业边界场景 Y 批次（用户反馈『继续深挖避免再出现解析错误』）**：**① Y2 ClickHouse quantileTiming 分位归一**——`quantileTiming(0.95)(duration)`/`quantileTimingWeighted` 此前产出非法枚举 `QUANTILETIMING`（批量创建 pydantic 整批失败，W4 quantileExactWeighted 同类但 timing 变体被遗漏），quantile* 前缀统一归一 `PERCENTILE` + 列从 ParameterizedAgg 提取；**② Y5 lag/lead 窗口偏移诚实跳过**——`lag(amt) over (...) `sqlglot 把 Lag/Lead 定义为 AggFunc 子类，语义是「上一/下一行取值」非分组聚合，此前产出非法枚举 `LAG`/`LEAD` 整批失败，现返回 None 诚实跳过（由 LLM/人工处理）；**③ Y23 sum(distinct) 语义修正**——`sum(distinct amt)`/`avg(distinct amt)` 的 DISTINCT 修饰 ≠ 去重计数，仅 `COUNT(DISTINCT x)` 才是 COUNT_DISTINCT，此前无条件改 COUNT_DISTINCT 致语义错误（sum(distinct) 被注册成「去重计数」），现保留原聚合名 + needs_review；**④ Y1 Hive collect_map/map_keys 识别**——`map_keys(collect_map(k,v))` （动态分组聚合）的 collect_map 解析为嵌套 Anonymous（非 AggFunc）此前整投影静默丢失，walk 检测 collect_* 聚合映射 `COUNT_DISTINCT` + needs_review；**⑤ Y3 多列 PIVOT 全产出**——`PIVOT(sum(amt) AS a, count(1) AS c FOR ...)` 的聚合被 Alias 包裹此前整段 0 度量，剥离 Alias 后全部聚合产出 + needs_review；**⑥ Y15 复合聚合参数核对标记**——`sum(a.amt * b.price)` 聚合参数是多列算术表达式（col 只取首个 Column），标记 needs_review。**验证**：test_sql_infer **+7**（quantileTiming 归一/lag-lead 跳过/sum(distinct) 保留 SUM+needs_review/collect_map 识别/多列 PIVOT 两度量/复合参数核对）；推断相关 7 文件 **239+589 passed** + ruff 0 | 无接口变更（候选聚合沿用既有注册枚举，被跳过/需核对场景由 LLM 兜底 + 前端「口径需核对」Tag） | —（semantic 保持 verified；quantileTiming 分位/lag-lead 偏移/sum(distinct)/collect_map 动态分组/多列 PIVOT/复合聚合参数等边界场景不再解析错误） |

| 日期 | PR | 模块 | 变更点 | 影响接口 | 状态跃迁 |
|------|-----|------|--------|----------|----------|
| 2026-09-01 | — | consume+frontend | **查询工作台自定义日期范围 + 数据源只读 SQL 查询（用户『日期范围要支持自定义选择 / 支持指定数据源后写 SQL 查询数据』）**：**① 自定义日期范围**——日期范围 Select 新增「自定义」选项，选中后展示 antd RangePicker（本地日期格式 YYYY-MM-DD），translateDateRange 输出 `YYYY-MM-DD~YYYY-MM-DD` 提交后端（未选区间回退近 30 天）。**② 数据源只读 SQL 查询（后端 + 前端）**——新增 `POST /data-sources/{source_id}/sql-query`：服务层 sqlglot 校验为**单条只读 SELECT**（拒绝多语句/DDL/DML/SELECT INTO，sqlglot 对畸形 SQL 抛 ParseError 也归一为 422）、顶层无 LIMIT 自动追加 `LIMIT n` + Python 侧按 limit 截断双保险、复用连接器 registry.build+query 执行、仅**平台管理员/域管理员或该数据源 Owner** 可执行（AuthError 403）、`write_audit` 记录 SQL 前 500 字符/limit/行数/截断标志、sql 字段豁免注入扫描（SQL 即合法载荷）；前端查询工作台新增「SQL 查询」Tab——数据源下拉（listDataSources）+ 只读 SELECT 文本域 + 行数上限（1-500）+ 执行按钮 + 结果表（动态列 + 行数/耗时/截断提示）。**验证**——后端 test_collector 266（+6：追加 LIMIT/已有 LIMIT 截断/多语句与 DDL 拒绝/源不存在不连库/外部错误传播）+ test_collector_api 20（+4：管理员 200+审计/owner 200/非 owner 403/DML 422）+ ruff 0；前端 QueryWorkspace 18（+2：自定义区间提交 `2026-08-01~2026-08-15` / SQL Tab 选源+写 SQL+执行断言参数与结果表）+ ApiClients 9 + tsc 0。提交 e1043de8。影响接口：新增 `POST /data-sources/{id}/sql-query`（向后兼容） | 新增端点（向后兼容） | consume/collector/frontend 保持 released |
| 2026-08-27 | — | semantic | **SQL 智能推断集合运算/参数化极值/串聚合边界（X 批次，用户反馈『继续深挖避免再出现解析错误』）**：**① INTERSECT/EXCEPT 集合运算支持**——`parse_sql_profile`/`_extract_source_tables`/`_profile_from_union` 的 `exp.Union` 泛化为 `exp.SetOperation`（Intersect/Except/Union 均遍历全部分支合并度量 + 两侧源表 + group_by），对账/差异查询（`SELECT d,sum(amt) FROM a GROUP BY d INTERSECT SELECT ... FROM b`）此前交/差整段静默 0 候选 + 0 源表（用户误以为 SQL 有问题），现产出多分支候选；**② min_by/any_value/argMax 参数化极值不再静默映射错误聚合**——Trino `min_by(uid, amt)`/`max_by(uid, amt)`（exp.ArgMin/ArgMax）、`any_value(name)`（exp.AnyValue）、ClickHouse `argMaxIf`（CombinedAggFunc 字符串形态）语义是「amt 极值那一行的 uid」/「任取一值」非 MIN/MAX/COUNT，此前静默产出语义错误的 MIN(uid)/COUNT(name) 指标被当成对的创建，现返回 None 诚实跳过（由 LLM 兜底处理），`_DIALECT_AGG_SKIP` 补 argmin/argmax 字符串形态；**③ group_concat(DISTINCT x ORDER BY) Order 包裹剥离**——MySQL 串聚合 `group_concat(DISTINCT product ORDER BY ... SEPARATOR ...)` 的 this 是 `Order(Distinct(x))` 包裹（非裸 Distinct），此前被 W8 复杂参数跳过 0 度量，剥离后与普通 group_concat 一致产出 `COUNT_DISTINCT(product)` + needs_review；**④ _classify_no_measure 补聚合关键词**——`any/mode/within/concat/collect` 让被诚实跳过的聚合（any_value/mode/group_concat）触发 LLM 兜底而非静默 no_aggregate。**验证**：test_sql_infer **+4**（INTERSECT/EXCEPT/INTERSECT ALL 合并 + min_by/max_by 跳过 + any_value 跳过 + group_concat(DISTINCT) 产出）；test_sql_split **+2**（any_value/mode 触发 parse_failed）；2 个既有断言适配新语义（ANYVALUE 不再 COUNT、argMaxIf 不再 MAX）+ ruff 0；推断相关 7 文件 + semantic_service/consume/domain_suggest/sql_batch_api/eval golden **239+425 passed** | 无接口变更（候选聚合沿用既有注册枚举，跳过场景由 LLM/人工兜底） | —（semantic 保持 verified；INTERSECT/EXCEPT 对账、min_by/any_value 参数化极值、group_concat(DISTINCT) 等边界场景不再解析错误） |
| 2026-08-27 | — | semantic | **SQL 智能推断工业边界场景 W1-W20（用户反馈『继续深挖避免再出现解析错误』）**：**① W4 ClickHouse 加权分位归一**——`quantileExactWeighted(0.5)(amount, weight)` 此前产出非法枚举 `QUANTILEEXACTWEIGHTED`（批量创建 pydantic 整批失败）且列=*，现归一到 `PERCENTILE` + 从 ParameterizedAgg 的 params 提取列 amount（quantileWeighted/插值变体同归一）；**② W7 条件聚合收集**——`sum_if(status='refund', amt)`/`count_if(status='paid')` 在 default 方言下 sum_if 解析为 Anonymous（非 AggFunc）此前整投影静默丢失，现按函数名收集为 `SUM(amt)`/`COUNT(*)` + needs_review（条件口径需核对，count_if 列不再误取条件里的 status）；**③ W8 collect_list(struct) 明细快照跳过**——`collect_list(struct(user_id, amt))` 硬映射 `COUNT_DISTINCT(user_id)` 语义完全错误（是收集明细数组非去重计数），复杂参数（struct/array）跳过由 LLM/人工处理，纯单列 `collect_set(x)` 去重集合近似保留；**④ W11 GROUPING SETS 多组维度去重**——`((d,region),(d,channel),())` 展开后 d 重复（此前 group_by=[d,region,d,channel]），并集去重保序（含空集）；**⑤ W16 嵌套聚合别名保留**——`sum(inner_amt)` 引用子查询 `count(1) AS inner_amt` 时 alias_map 把 inner_amt 映射到 *（count(1) 无列）覆盖外层列，映射到 * 不替换保留派生列语义（血缘指向 inner_amt 而非通配符）；**⑥ W20 ARRAY JOIN 源表净化**——ClickHouse `ARRAY JOIN sku_list AS sku` 在 hive/default 方言下数组名被解析成无 db 的 Table 混入 source_tables（血缘挂到不存在表），含 ARRAY JOIN 时强制 clickhouse 方言（kind='ARRAY' 被 _extract_source_tables 跳过），LATERAL VIEW 不受影响。**验证**：test_sql_infer **+6**（加权分位归一/条件聚合收集/collect_list(struct) 跳过+纯单列保留/多组去重/嵌套别名不覆盖/ARRAY JOIN 排除+LV 不受影响）+ 2 个既有断言适配新语义；推断相关 7 文件 **623 passed** + ruff 0 | 无接口变更（候选 needs_review/聚合枚举对齐复用既有注册枚举） | —（semantic 保持 verified；加权分位/条件聚合/明细快照/多组分组/嵌套别名/ARRAY JOIN 等边界场景不再解析错误） |
| 2026-08-27 | — | semantic | **SQL 智能推断工业边界场景 V-1~V-6（用户反馈『避免再出现解析错误』）**：**① V-1 Doris 位图/HLL 去重聚合识别**——`bitmap_union(to_bitmap(uid))`/`hll_union(hll_hash(uid))`（工业 UV/DAU 标准写法）解析为 Anonymous 非 AggFunc 此前整段静默 0 候选，现按函数名识别、从内层表达式取真实列，映射 `COUNT_DISTINCT` + `needs_review`（位图/HLL 是近似/集合语义需人工核对）；**② V-2 嵌套聚合标记核对**——`sum(avg(x))`/`sum(count(*))` 聚合的聚合不再静默产出语义错误的 `SUM(x)`（expression 保留原结构 + `needs_review`）；**③ V-3 血缘源表收窄**——`_extract_source_tables` 仅收集主查询（顶层 UNION 各分支）FROM+JOIN 子树内的表，WHERE 标量子查询维表/查找表（`WHERE d=(SELECT max(d) FROM ods.lookup)`）不再混入血缘错挂无关表；主 FROM/JOIN 引用 CTE 时递归收集 CTE 体（visited 防递归 CTE 死循环）；**④ V-4 percentile 列提取**——`percentile_cont(0.5) WITHIN GROUP (ORDER BY amt)`此前列=*，现从 ORDER BY 提取 amt；**⑤ V-5 多列 DISTINCT 合并**——`count(distinct col1, col2)` 合并列名（`col1+col2`）+ 核对标记，不再只取首列丢语义；**⑥ V-6 GROUP BY CUBE 维度**——`CUBE(d, region)` 维度在 `group.args['cube']`（Cube 节点）此前整体丢失被判全局聚合，现展开进 group_by（对齐 grouping_sets/ROLLUP）。**验证**：test_sql_infer **+7**（位图/HLL/嵌套聚合/WHERE 子查询源表排除/CTE 递归+递归 CTE 不死循环/percentile/多列 DISTINCT/CUBE）；推断相关 **624 passed** + eval golden **9/9**（doctor_active_month 的 LEFT JOIN 维表恢复——修复 sqlglot 把 JOIN 存在 `select.args['joins']` 而非 From 节点的遗漏）+ ruff 0 | 无接口变更（候选 needs_review 复用前端「口径需核对」Tag） | —（semantic 保持 verified；Doris UV/嵌套聚合/WHERE 查找表/percentile/多列去重/CUBE 等边界场景不再解析错误） |
| 2026-08-27 | — | frontend (system_dict) | **数据字典「扩展属性」通用化**（用户反馈「metric_name_morpheme 的扩展属性列是空壳、编辑弹窗没有对应列信息」）：**① 表格「扩展属性」列通用化**——非 measure_format 类型按 `key: value` Tag 通用展示（对象/数组值 JSON 序列化），measure_format 保留「单位:X，Y位」语义；**② 新增/编辑弹窗通用 `ExtraEditor`**——非 measure_format 类型渲染通用 key-value 行编辑器（属性名/属性值，值支持数字/布尔/JSON 自动识别、可表达嵌套，含「原始 JSON」只读预览折叠），measure_format 保留默认单位/小数位专用控件；编辑弹窗回填已有 extra、可增删改行；所有字典类型通用受益（此前 extra 输入 UI 硬编码为 measure_format 专用，其他类型无编辑入口）。**验证**：前端 SystemDict **30**（+4：表格通用 key:value 展示 / 新增通用编辑器数字自动识别提交携带结构化 extra / 编辑回填修改提交 / 空 extra 新增属性提交）+ tsc 0 | 无接口变更（extra 通用 JSON 列后端全链路已透传，纯前端编辑/展示增强） | —（system_dict 保持 verified；任意字典类型均可编辑与展示扩展属性） |
| 2026-08-27 | — | semantic | **SQL 智能推断未覆盖工业场景全量修复（U-1~U-9）**：① **U-1 顶层 UNION ALL/UNION 多源合并**——`parse_sql_profile` 识别 `exp.Union` 遍历全部分支合并度量/group_by（`_profile_from_union`），源表保留两侧（此前 `ast.find(Select)` 只取首分支 → measures=0 静默 0 候选，多子公司合并等常见形态不可用）；② **U-2 集合/串聚合不再静默降级 COUNT**——collect_set/collect_list/array_agg/group_concat/listagg/string_agg/group_uniq_array → `COUNT_DISTINCT`（去重集合语义）+ `needs_review`（前端「口径需核对」Tag），消除「收集商品集合被注册成统计商品数」的静默错误指标；③ **U-3 相关子查询标量跳过**——`(SELECT max(amt) FROM b WHERE b.d=a.d)` 不再误判为分组聚合（不产出错误 MAX 候选、不混入子查询表）；④ **U-4 coalesce 多参数聚合全收集**——`coalesce(sum(amt), sum(refund), 0)` 的两个聚合都收集（此前只取首个且 agg=None，sum(refund) 静默丢失；单聚合 coalesce 仍按格式包裹）；⑤ **U-5 IF/CASE 语义包裹放行**——`if(sum(amt) is null,0,sum(amt))` 双分支同一聚合按格式包裹放行（产出 1 个 SUM 不重复派生）；⑥ **U-6 窗口函数包裹标记核对**——`sum(amt) OVER(...)` 产出但 `needs_review`（非分组聚合语义）；⑦ **U-7 CTE 名过滤**——`WITH base AS (...) SELECT ... FROM base` 的 source_tables 不再混入 CTE 名 base（血缘不挂到不存在表）；⑧ **U-8 PIVOT 聚合提取**——`SELECT * FROM t PIVOT(SUM(amt)...)` 从 Pivot 节点提取度量（此前 measures=0 完全丢失）+ needs_review；⑨ **U-9 GROUPING SETS 维度**——grouping_sets 的维度列进入 group_by（此前 group.expressions 为空维度丢失，对齐 ROLLUP）。**验证**：test_sql_infer **+8**（UNION 合并/相关子查询跳过/coalesce 双聚合/IF 包裹/窗口标记/CTE 过滤/PIVOT 提取/GROUPING SETS）+ 3 个既有方言测试断言更新（集合聚合新语义）+ test_sql_split **+1**（UNION 候选 + 集合聚合 needs_review 完整链路）；推断相关 7 文件 **610 passed** + ruff 0 | 无接口变更（解析候选结构扩展 needs_review 标记，前端已有「口径需核对」Tag 展示） | —（semantic 保持 verified；工业多源 UNION/集合聚合/PIVOT/GROUPING SETS 等边界场景不再解析错误） |
| 2026-08-27 | — | frontend (system_dict) | **数据字典 tab 展示中文类型名**（用户反馈「数据字典里展示的 metric_name_morpheme 应该展示为中文」）：`SystemDict.tsx` 的 `DICT_TYPE_LABELS` 补注册 `metric_name_morpheme: 指标命名词根`（0102/0103 迁移种入的受控词根字典类型此前缺失映射 → Tabs/新增弹窗标题/AI 生成描述的类型上下文三处显示英文编码）；弹窗标题与 AI 描述上下文同步生效。**验证**：前端 SystemDict **26**（+1：tab 与弹窗标题展示「指标命名词根」而非英文编码）+ tsc 0 | 无接口变更（纯前端字典类型名展示） | —（system_dict 保持 verified；指标命名词根 tab 显示中文） |
| 2026-08-27 | — | semantic+frontend | **SQL 批量创建结果「快速编辑」抽屉（用户反馈"批量创建完成后想点击具体指标在不影响当前页面的情况下快速编辑、并方便切换到其他指标"）**：批量创建结果表新增「操作」列（仅 DRAFT 可点「快速编辑」），在当前页内打开 Drawer 编辑该指标（**不跳详情页、不关闭当前窗口**）；字段：指标名称/聚合方式/单位/粒度（非原子）/计算表达式（口径）/数仓详细口径 + 变更原因（必填 ≥4）+ 乐观锁 row_version，保存走既有 `updateMetric`；**上一条/下一条在批内 DRAFT 候选间循环切换**（位置指示「x / y」），每次打开/切换重新 `getMetric` 拉取最新 row_version（他人已改则 409 拒绝）；口径表达式/数仓口径仅 dirty 才合入 `definition_json`（不碰其他口径键）；关闭抽屉/「完成」后清空编辑态。**验证**：前端 MetricCreate **73**（+1：快速编辑抽屉打开 getMetric 回填/下一条切换/保存 updateMetric 携带 row_version+name+change_reason 断言）+ tsc 0；前端全量 **1024 passed**（1 项 AssetMap 顺序 flaky 单独跑 59 全绿，与本次无关） | 无接口变更（纯前端交互增强，复用既有 GET /metric-definitions/{code} 与 PUT） | —（semantic 保持 verified；批量创建后可在当前页快速编辑/切换，无需回目录或开详情页） |
| 2026-08-27 | — | semantic+frontend | **SQL 批量解析派生列识别增强 + 批量注册向导（用户反馈『转诊预约旧页面』派生指标缺失、批量候选要一次性向导编辑）**：**① A7 引用聚合别名算术派生列识别**——`sql_infer._collect_derived_measures` 收集外层宽表 ETL 的 `all_order_cnt - session_side_order_cnt - region_org_order_cnt AS old_page_transfer_order_cnt`（无内嵌聚合、引用已命名聚合别名），产出复合候选 + `deps_aliases`；`sql_split` 阶段 3 用 alias 映射解析为 dependencies（引用语句内原子编码，不再依赖错配成自身）；**② B 自动合成复合**——语句含运算但无命名派生列时 `synthesize_composite=false` 也自动合成整语句复合（`should_auto_composite`），前端合成复合开关默认改 true（此前默认关让派生指标静默缺失）；**③ C 名称锚点增强**——下沉聚合别名注入 `measure_meta.alias` 供 `_infer_name`/`_measure_label` 优先消费、`_cn_column_label` 支持整段/最长 token 后缀匹配（`all_order_cnt`/`session_side_order_cnt`/`region_org_order_cnt` 不再全部落成「日订单量」）、语句内同列多 count 名称附加 alias 区分；**④ 批量注册向导**——批量候选区新增「批量编辑向导（全部指标）」入口，分步 Modal（基本信息/口径与责任/确认提交）多指标表格一次性批量编辑（类型/编码/名称/聚合/周期/粒度/单位/逻辑度量/产品负责/依赖/计算表达式），确认后整体批量创建（逐条 savepoint + 失败逐条标记）；`SqlBatchCandidate` 类型补 `deps_aliases/alias` 字段。**验证**：后端 test_sql_infer/sql_split/auto_fill/llm_parse 213 + semantic/consume/domain 388 全过 + ruff 0（新增 A7 派生列/纯维度反例/deps 解析与自动合成/名称去重用例）；前端 MetricCreate **71**（+批量向导打开编辑跳步汇总用例）+ tsc 0 | 无接口变更（批量解析候选结构增 deps_aliases/alias 扩展键、合成复合默认开；批量候选新增向导 UI 入口） | —（semantic 保持 verified；外层宽表 ETL 派生指标不再静默缺失、多指标可一次性向导批量编辑） |
| 2026-08-27 | — | semantic+system_dict | **受控词根生产场景扩充（0102 的 91 条 → 288 条，新增 197 条）**（用户要求"从实际生产场景出发，把词根尽可能完善"）：**① 内置词根扩充**——`conflict_precheck.CONTROLLED_MORPHEMES` 从 91 条扩到 288 条，分组补齐：财务交易/资金流转（交易/成交/支付/应收/应付/预收/预付/折扣/返利/佣金/工资/薪酬/奖金/预算/决算/坏账等 18）、用户流量（注册/登录/访问/访客/浏览/曝光/下载/启动/分享/点赞/收藏/观看/播放/召回/流失/沉默等 24）、供应链业务量（发货/签收/退货/换货/售后/动销/缺货/库龄/单量/件数/批次/台次/班次等 20）、业务动因（达成/完成/超额/缺口/净增 5）、度量统计（均值/中位数/方差/标准差/百分比/千分比/万分比/单价/均价/时点/期末/期初 12）、**医疗卫健（医生/护士/医院/机构/科室/病区/床位/疾病/诊断/症状/急诊/体检/病历/入院/出院/预约/复诊/诊疗/治疗/护理/康复/随访/转诊/会诊/抢救/治愈/留观/取药/发药/退药/耗材/器械/西药/中药/中成药/草药/统筹/自费/自付/个账/床日/周转/次均/诊次 46——对齐 auto_fill._CN_COLUMN_LABELS 已产出的业务对象，堵住"注释越生效越被词根遗漏误拦"的坑）**、质量服务（投诉/客诉/满意度/健康度/响应/工单/咨询/线索/商机/合同/回款/开票/履约/超时/风险/告警 16）、英文业务对象（ 等 57）；**② 迁移 `0103_metric_name_morpheme_dict_expand`**——幂等补种 197 条新增词根到已应用 DB（逐条 INSERT...WHERE NOT EXISTS，不重复不动 0102 已有项；sort_order 从 1000 起避开已有 0..90；downgrade 仅删新增项）；内置与迁移分组同构，字典管理新增「质量/服务/管理类」分组。**验证**：内置词根 288 条全量校验（新增 197 全部在内）+ test_metric_name_morpheme +生产场景 40 词根命中（医疗/供应链/质量/英文）+ test_auto_fill 负样例「总成交」因新增「成交」词根改「管理事项」（决策 2 未命中样例语义保持）→ 相关 6 文件 **431 passed** + ruff 0 | 无接口变更（词根表扩充 + 迁移补种，命名校验放行面扩大——合法生产指标名不再被词根遗漏误拦；手动命名硬卡语义不变） | —（semantic 保持 verified；词根从 91 扩到 288 覆盖 HIS 门诊全流程 + 通用财务/运营/用户流量/供应链/质量服务 + 英文业务对象，字典管理可继续在线增删） |
| 2026-08-27 | — | semantic+system_dict+frontend | **SQL 推断四需求落地：词根可管理系统字典 + 推断名保留原样软提示 + SQL 回填数仓口径 + 批量候选回填向导核对**（用户确认四项决策）：**① 词根字典化（决策 1）**——`conflict_precheck` 内置 `CONTROLLED_MORPHEMES` 保留为默认，新增 `get_controlled_morphemes()`（内置 ∪ DB active 词根进程内缓存）+ `load_metric_name_morphemes(db)` + `reset_metric_name_morpheme_cache()`；`validate_metric_name` 改读生效词根集合；迁移 `0102_metric_name_morpheme_dict_seed` 幂等种入 90+ 词根（8 分组）；main.lifespan 启动加载 + 字典 API 7 处写端点（create/update/toggle/delete + batch×3）变更 metric_name_morpheme 时刷新缓存（best-effort 仅当前 worker）——「系统设置 → 字典管理」零新页面在线增删/启停用词根即时生效，无需发版；**② 推断名策略（决策 2）**——`auto_fill._ensure_metric_name_morpheme` 改为 `_metric_name_morpheme_missing`（bool），**不再追加「金额/次数」污染名称**，`_infer_name` 未命中时保留原样 + reason 软提示「未命中受控词根（推断名，建议人工确认）」——词根硬卡只留给手动命名，任意 SQL 样本推断名直接可创建；**③ Q2 SQL 回填数仓口径**——单条 `applySuggestion` SQL 模式推断成功且未手填时自动回填 `dwDefinition`（数仓SQL口径）；批量 `_build_atomic_candidate`/`_build_composite_candidate` 候选 definition_json 加 `dw_definition`（= 所属语句原始 SQL），Phase1/Phase2 原样落库——创建后 MetricDetail/目录展开「数仓详细口径」直接可见；**④ Q1 批量候选回填向导（方案 A）**——候选行新增「在向导中编辑」按钮，`loadCandidateIntoWizard` 完整回填单条向导表单（编码/名称/类型/源表/度量列/聚合/单位/周期/粒度/逻辑度量/依赖/计算表达式/口径/数仓口径），关闭抽屉定位 Step② 核对修改后按单条流程手动提交创建；「批量提交评审」降为次要按钮（免核对）+ 完成 Alert 文案引导「建议逐条在向导中编辑核对后手动提交审批」。**验证**：后端 test_metric_name_morpheme +4（缓存合并/加载/失败兜底/重置）+ test_auto_fill 决策 2 断言更新 + test_sql_split +1（候选 dw_definition）+ semantic 相关 8 文件 **533** 全过 + ruff 0；前端 MetricCreate **70**（+Q1 回填向导、+Q2 数仓口径回填 2 用例，批量提交评审按钮文案改「（免核对）」断言适配）+ **全量 1023/1023**（58 文件）+ tsc 0 | `GET/POST/PUT/PATCH/DELETE /dicts/*` 变更 metric_name_morpheme 触发词根缓存刷新（行为增强，无接口变更）；批量候选/单条推断产出的 definition_json 增 dw_definition（扩展键）；批量结果面板按钮文案调整 | —（semantic 保持 verified；词根可字典管理、推断名不硬卡不污染、SQL 自动回填数仓口径、批量候选可回填向导核对后手动提交） |
| 2026-08-27 | — | semantic (SQL 智能推断命名)+test | **SQL 推断候选名「月活」创建被 METRIC_NAME_NO_MORPHEME 误拦修复（A-6：词根表补活跃缩写 + 名称兜底补业务词根）**（用户反馈"SQL 推断候选编码 `uncategorized_doctor_currentmonthactivedoctorcnt_month` 创建提示『指标名未命中受控词根，请使用业务术语』"）：**根因**——A-5 建表注释驱动命名让候选 name=「月活」（干净数仓名），但 CONTROLLED_MORPHEMES 只有「活跃」无「月活/日活/周活/年活/季活」缩写，「月活」不命中任何词根 → TD §12.3 硬卡把合法业务命名误拦（反直觉：注释没生效时 name=「doctor次数」反而含「次数」能过）；**修复①**——`conflict_precheck.CONTROLLED_MORPHEMES` 补活跃类高频缩写「月活/日活/周活/年活/季活」（放「活跃」后，A-6 注释说明），「月活」原生通过校验且 name 保持「月活」不被改写（test_sql_split 1738 `name=="月活"` 不受影响）；**修复②**——`auto_fill._infer_name` 重构统一兜底：新增 `_ensure_metric_name_morpheme`（命中词根原样返回；未命中按语义补「金额」[成交/销/收/费/价/额/利] 或「次数」[其余计数/活跃/未知]，reason 注明「未命中受控词根，追加『xx』」）——任意数仓注释/LLM 名/规则名即使词根表未覆盖也能直接创建，不依赖穷举；一处兜底全链路生效（单条 auto_suggest / 批量注册 / SQL 推断候选共用 `_infer_name`）。**验证**：端到端实测用户 SQL 批量候选 `current_month_active_doctor_cnt` → name=「月活」+ validate_metric_name ok=True（修复前 False 被拦）、`last_month_active_doctor_cnt` → 注释名含「活跃」词根 ✓；后端 test_metric_name_morpheme +1（活跃缩写 6 名通过）+ test_auto_fill +5（兜底 4 + _infer_name 注释不改写/兜底补词根 2）= 相关 8 文件 **494** 全过 + ruff 0 | 无接口变更（词根表扩充 + 名称生成兜底，纯语义/命名增强） | —（semantic 保持 verified；「月活/日活」等数仓活跃缩写不再被受控词根硬卡误拦，任意注释推断名兜底补业务词根后可直接创建） |
| 2026-08-27 | — | semantic | **SQL 批量解析提速根治——路由异常内容计入跨请求熔断器（85ff5f66）**（用户反馈"批量解析耗时仍很长，130000ms"）：实测容器内打点（真实 SQL 含 union all 下沉 + 建表注释）——总耗时 48.8s 但仅 1 次 LLM 调用（48.5s），路由尝试序列 [opencode→测试-codebuddy→env]，failover 虽生效但每次调用都先白等测试-codebuddy（host.docker.internal:19090，priority 1）29s 返回流式垃圾才 failover，多语句批量解析多次 LLM 调用叠加 → 130s。**深层根因**——路由层 `is_abnormal_llm_text` 判定异常内容后只更新路由实例级 `_consecutive_failures`（每次 build_client 新建 router 即归零、不跨请求），而跨请求持久的每实例熔断器（`get_circuit_breaker("LLM:{id}")`）只在 LlmClient 抛 httpx/解析异常时 record_failure——垃圾内容被 LlmClient 当成功返回（record_success），熔断器从不累计 → 测试-codebuddy 永不熔断、每次请求都白等 29s。**修复**——`LlmRouterClient.chat` 异常内容分支调用 `self._instances[idx]._breaker.record_failure()`（getattr 防御 + contextlib.suppress best-effort）：垃圾/空内容统一计入跨请求熔断器，连续 5 次后 OPEN，后续请求 LlmClient.chat 在 breaker.allow() 秒级抛错 → 路由立即 failover 到健康实例（不再白等 29s）。**验证**：test_llm_router_client 15（+3：异常内容 record_breaker_failure / 空内容 record_breaker_failure / 无 breaker 属性容错）+ LLM 套件 104 全过 + ruff 0；容器重建后端到端实测——手动注入 LLM:2 五次失败 state=open 后真实 `build_client().chat()` 3.3s 返回（测试-codebuddy 被秒级拒绝、路由 failover 到 opencode），修复前同场景需白等 29s | 无接口变更（纯路由韧性增强） | —（semantic 保持 verified；批量解析慢根因根治，垃圾实例经跨请求熔断器自动摘除，健康实例秒级承接） |
| 2026-08-27 | — | semantic+frontend | **SQL 批量解析质量四连修（A-5 建表注释驱动命名 / B 表达式回显 / C 派生 UI 澄清 / D 路由异常内容 failover）**（用户反馈"批量解析字段名 `uncategorized_doctor_currentmonthactivedoctorcnt_month`、名称『月doctor次数』、依赖/表达式没填充、解析很慢"）：**A-5 建表注释驱动命名**——`sql_split._extract_column_comments` 从完整脚本提取建表 DDL 列注释（sqlglot 主路径遍历 `CommentColumnConstraint` + 正则兜底；修复 Create.this 为 Schema 时表名提取跳过 bug），`_insert_target_table` 提取 INSERT 目标表（下沉场景列注释定义在目标表而非内层源表），`_build_atomic_candidate` 按 (目标表, code_col) 反查注释注入 `measure_meta.comment`——「月活」不再落成「月doctor次数」、注释含周期词不重复加前缀（`_infer_name` 去重），且单位推断同步受益；`auto_fill._CN_COLUMN_LABELS` 扩充医疗词表 25+（doctor/physician/nurse/hosp/org/dept/ward/bed/disease/diagnosis/inpatient/outpatient/operation/medicine 等）；**B 表达式回显**——批量候选口径表达式 Tooltip 从「仅 atomic」放开：派生（C 分支只读展示）+ 复合（完整 SQL 口径）都能创建前核对；**C 派生候选 UI 澄清**——derived 候选不再显示误导性「计算表达式输入框（待填）」，改为只读展示解析出的聚合表达式 + Tag「派生（周期驱动，无公式依赖）」，依赖指标保留可选下拉、「需先发布依赖原子」Tag 仅在有依赖时显示（OneData：派生=聚合原语无公式依赖、复合=依赖+指标间运算，语义澄清回应用户「有表达式/依赖不就是复合吗」的疑问）；**D 路由 failover 扩展**——`LlmRouterClient.chat` 空 content 检查扩展 `is_abnormal_llm_text`（超长流式垃圾/SSE 信封原文也 failover + 计入连续失败冷却，不再把 60k 字符垃圾当成功返回灌进业务字段、不再干等慢实例 29s）。**验证**：后端 test_sql_split 74（+4：Hive DDL 注释提取/正则兜底/INSERT 目标表/完整管线注释驱动「月活」名称）+ test_auto_fill 37（+2 医疗词表）+ test_llm_router_client 12（+2 异常内容 failover+冷却）+ 相关 248 全过 + ruff 0；前端 MetricCreate 68（改派生测试拆为「派生只读+依赖+挂载」与「复合公式+依赖无挂载」两项）+ tsc 0 | 无接口变更（纯解析质量/展示/路由韧性增强，候选字段结构不变） | —（semantic 保持 verified；批量解析字段名/名称/表达式/速度四问题根治，建表注释成为名称/单位推断最高优先来源之一） |
| 2026-08-27 | — | semantic+frontend | **指标类型 OneData 语义三次审查全量修复（S1-S8）**（第三轮审查对比 DEV_GUIDE §7a 共识发现 8 处残留不一致全部修复）：**S1 use_llm 路径复合豁免**——`_apply_candidate_annotations`（LLM 批量模式）对 composite 候选豁免 `is_measure=false` 剔除，与默认路径 B4.1 对齐（此前 use_llm=True 时自动复合候选被当作非度量静默剔除、复合功能在 LLM 批量模式端到端不可见）；**S2 表达式内嵌复合创建闭环**——`batch_register_from_sql` Phase2 对无依赖的复合候选（自动推断的比率列如 `SUM(a)/COUNT(b)`，无已发布指标可依赖）降级为**派生创建**（口径/周期/挂载保留、依赖可选、血缘对无依赖安全），响应提示「已按派生创建，如需复合请补充依赖」——修复 parse→create 闭环断裂（此前被复合门禁 422 拦死）；**S3 两路径类型判定统一**——批量 `_build_atomic_candidate` 补**列名比率规则**（rate/ratio/pct→复合，对齐单条）、单条 `_is_ratio_expression` 判定由整段 SQL 改为**限定 SELECT 投影表达式**（不再误判 `WHERE price*0.9` 过滤条件为复合，同时保留单投影双聚合 `SUM(a)*SUM(b)` 识别）；**S4 旧语义文案清理**——models/metric.py、measure_catalog.py、schemas.py、api/metrics.py、seed、迁移、测试注释 10+ 处「原子=逻辑度量+聚合方式」统一为「逻辑度量+基础统计粒度（日）」；**S5 改类型为原子→周期回落**——`handleSqlBatchTypeChange` 改类型为 atomic 时非日周期/粒度自动回落 day（反向联动，防「原子+月粒度」落入 4 段编码）；**S6 编辑弹窗原子粒度控制**——MetricDetail 编辑弹窗原子不渲染粒度 Form.Item、payload 原子不提交 granularity、`isBreakingEdit` 原子跳过粒度比较（防误判破坏性）；**S7 模板实例化原子粒度控制**——Templates 实例化弹窗原子不渲染粒度、提交原子强制 `day`（忽略模板预设非日粒度）；**S8 复合必填视觉标记**——批量候选复合行依赖/计算表达式、编辑弹窗复合依赖/公式均补必填红标星号（对齐创建向导，提交校验已强制、补视觉引导）。**验证**：后端 sql_split/auto_fill/semantic_service/sql_batch_api/auto_suggest_api/semantic_schemas **453**（+4：S1 use_llm 复合豁免、S2 空依赖复合降级派生创建、S3 批量列名比率→复合、S3 单条 WHERE 运算不误判回归）+ ruff 0；前端 MetricCreate **68**（+S5 反向回落、+S8 必填星号）+ MetricDetail **82**（+S6 原子粒度隐藏适配）+ Templates **17**（+S7 原子模板强制 day）+ tsc 0 | 无接口变更（类型判定统一、批量创建无依赖复合降级派生、原子粒度锁死 day/锁死不编辑、复合必填视觉标记） | —（semantic 保持 verified；第三轮审查 8 处不一致全部修复，指标类型判定/校验/文案/交互与 DEV_GUIDE §7a OneData 共识完全一致） |
| 2026-08-27 | — | system_config+frontend | **LLM 平台预设支持火山方舟/腾讯云混元/阿里云百炼 + 「获取模型」内置目录兜底**（用户反馈"要求支持火山引擎、腾讯云coding plan，阿里云coding plan"，并报告 `POST /ai/config/models` 对 `https://ark.cn-beijing.volces.com/api/plan/v1` 返回 HTTP 404）：① **根因**——`models_url()` 只按「base_url + /v1/models」单一 OpenAI 形态拼接，而火山方舟/腾讯混元的 OpenAI 兼容网关**未实现 GET /models 端点**（阿里云百炼 compatible-mode 支持），故这类网关请求 /models 必然 404；② **预设**——后端 `PROVIDER_DEFAULTS` + 前端 `PROVIDER_PRESETS` 新增 `ark`（火山方舟 Coding Plan，base `https://ark.cn-beijing.volces.com/api/coding/v3`，默认模型 `deepseek-v3.1`）与 `tencent`（腾讯云混元，base `https://api.hunyuan.cloud.tencent.com/v1`，默认 `hunyuan-turbos-latest`），qwen 标签改「阿里云百炼（通义千问）」；③ **目录兜底**——`PROVIDER_MODEL_CATALOG`（ark 7 款 / tencent 4 款常用模型），`fetch_models` 加 `provider` 参数，网关 404/405/501 且平台有目录时回退内置目录（`source="catalog"` + note，仍 supported=true 让下拉点选），200 返回 live 不混目录、鉴权/连通错误不回退目录；`fetch_models_for_instance` 透传落库 provider；`LlmModelsRequest` 加 provider、`LlmModelsResult` 加 source/note；`/config/models` 端点 direct/effective 分支透传 provider；④ **前端**——`handleFetchModels` 传 provider，`source="catalog"` 时 info 提示「该网关不支持 GET /models 接口，已列出平台内置常用模型…可手动输入补充」并照常填充下拉/自动选中/展开（方案 C 交互保留）。**验证**：后端 test_llm_config_service 50（+6：ark/tencent 404→目录、custom 404→不支持、ark 200→live 不混目录、ark 401→鉴权错误无目录、instance ark→目录）+ 路由/parse 108 全过 + ruff 0；前端 SystemConfig 24（+2：火山/腾讯预设预填 base_url+model、catalog 来源展示目录+提示）+ tsc 0 | `POST /api/v1/ai/config/models` 请求可带 provider、响应增 source/note（扩展，向后兼容） | —（system_config 保持 verified；系统配置支持火山方舟/腾讯云混元预设，「获取模型」对无 /models 网关回退内置常用模型目录仍可下拉点选切换） |
| 2026-08-27 | — | system_config+frontend | **系统配置获取模型后下拉展示全部模型修复**（用户反馈"点击获取模型后筛选框没有展示出获取到的所有模型列表"）：根因是 `AutoComplete.filterOption` 用**当前输入框值（程序化 value）**过滤 options——新建时自动选中第一个模型、编辑时保留已有模型值，两者都让输入框 value 非空，导致下拉只显示包含该 value 的子集（新建只显示 hy3/hy3-pro、编辑只显示 deepseek-chat）。修复：新增 `modelSearching` 状态（仅 `onSearch` 用户主动输入时置 true），`filterOption` 在非用户输入时**返回全部 options**（获取模型/已有值场景展示完整列表），用户手动输入字符时才按输入过滤；`handleFetchModels` 展开前复位 `modelSearching=false`，`openCreate/openEdit` 同步复位。**验证**：Playwright 真实渲染实测（新建/编辑两场景下拉均显示全部 5 个模型，修复前分别为 2/1 个）；SystemConfig 测试 22（+2：新建自动选中 hy3 但下拉展示全部 5 项、编辑保留 deepseek-chat 但下拉展示全部 5 项）+ tsc 0 | 无接口变更（纯前端交互修复） | —（system_config 保持 verified；获取模型后下拉如选项框展示完整模型列表，手动输入过滤保留） |
| 2026-08-27 | — | semantic+frontend | **指标类型 OneData 语义二次审查第二轮修复（R1-R6）**（继 B1-B5/F1-F4 后第二轮审查发现 6 处残留不一致全量修复）：**R1 批量路径比率/运算→复合**——`_build_atomic_candidate` 类型判定由「仅周期驱动」扩展为「周期驱动 + 比率/运算信号」：`derived=True`（ROUND(SUM/NULLIF)、CASE 比率等无聚合包裹表达式）且整句含四则运算（`sql_has_arithmetic`）→ type 判 composite，对齐单条 `_infer_type`（修复批量/单条两路径类型分裂，此前批量比率列在日粒度下判 atomic）；`_build_composite_candidate` 合成复合只基于纯原子候选（已判 composite 的比率列不再作为合成复合依赖，避免「复合依赖复合」嵌套）；**R2 比率检测补 Mul/Mod**——`_is_ratio_expression` 从「只认 / 与 ≥2 度量分支的 /|+|-」改为与批量共享的 `sql_has_arithmetic`（AST 优先 + 正则兜底，含 Div/Mul/Add/Sub/Mod，覆盖单投影双聚合 `SUM(a)*SUM(b)`）；`sql_has_arithmetic` 下沉到 `sql_infer.py` 供 auto_fill/sql_split 共用（消除重复实现）；**R3/R4 文案与测试名**——service.py Phase2 派生注释改「依赖可选」口径、test_semantic_schemas 测试名 `test_derived_requires_dependencies_and_expression`→`test_derived_dependencies_optional_but_expression_required`；**R5 计算表达式红标仅复合**——MetricCreate 单条向导计算表达式 Form.Item `required` 改 `required={isComposite}`（派生纯周期不显示必填红标，与 F1 提交校验语义一致）；**R6 周期↔类型联动**——批量原子候选周期/粒度 Select 改非日周期时自动联动 type→derived（原子=逻辑度量+日粒度，非日周期归派生，避免「原子却带非日周期」的语义矛盾）。**验证**：后端 sql_split/auto_fill/semantic_schemas/sql_infer 193（+2：R2 单投影双聚合乘法判复合、批量比率列 composite 断言更新）+ metric_batch_api/semantic_service 482 全过 + ruff 0；前端 MetricCreate 65（+1 R5 派生无必填红标断言、R6 既有周期编辑测试更新断言 type 联动 derived）+ 全量 1012 全绿 + tsc 0 | 无接口变更（类型判定批量路径补比率/运算信号、周期↔类型联动；`sql_has_arithmetic` 内部重构共享至 sql_infer） | —（semantic 保持 verified；指标类型判定/校验/文案与 DEV_GUIDE §7a OneData 共识完全一致，两轮审查 B1-B5/F1-F4/R1-R6 全部落地） |
| 2026-08-27 | — | semantic+frontend | **指标类型 OneData 语义二次审查全量修复（B1-B5 + F1-F4）**（系统审查对比 DEV_GUIDE §7a 共识发现 8 处不一致）：**B1 单条类型判定**——`_infer_type` 比率/跨度量运算由误判 derived 改为 **composite**（reason「SQL 含比率/跨度量运算 → 复合指标」）、列名比率语义（rate/ratio/pct）也判 composite、删除 avg/mean 列名判派生分支（avg 归聚合方式，mean 由周期驱动判断）；**B2 周期回映**——`_apply_candidate_period` LLM 覆盖周期后按「非日→derived/日→atomic」重算候选 type（复合保持复合不降级）；**B3 复合公式放开原子操作数**——`dependency_checker.validate_composite_formula` 允许引用原子指标（复合=多指标运算可直接基于原子，如 GMV/订单数；自引用/环由 detect_cycle 拦截），错误文案同步；**B4 复合合成需含运算**——`_build_composite_candidate` 仅当语句含 Div/Mul/Add/Sub/Mod（`_sql_has_arithmetic`，AST 优先 + 正则兜底）才合成复合，无运算的多度量并列（SELECT SUM(a), SUM(b)）不再合成；**B5 schema 文案**——schemas.py 派生依赖描述从「派生/复合必填」改为「复合必填、派生可选」；**F1 前端校验**——单条向导/编辑弹窗/批量流派生必填计算表达式改为**仅 composite 必填**，纯周期派生（无依赖、无手填公式）放行（`buildDefinitionJson` 派生无 calc 时从挂载源列自动生成「聚合(列)」兜底，保证口径有计算主体）；**F2 依赖文案**——MetricDetail 依赖指标 extra 从「派生/复合指标的上游」改为「复合必填、派生选填：纯周期派生可不依赖」；**F3 原子分支移除周期字段**——MetricCreate 原子分支「统计周期（兼容旧式推断，可选）」移除（周期归派生/挂载层），同步删 handlePeriodSelect；**F4 注释统一**——types.ts/MetricDetail/MetricCreate 注释「原子=逻辑度量+聚合」改为「原子=逻辑度量+基础统计粒度（日）」。**验证**：后端 9 文件 524 全过（test_auto_fill +2 更新为 composite 断言、test_sql_split +3 更新 B4 无运算不合成+新增 B2 周期回映单测、test_dependency_checker 更新原子引用允许）+ ruff 0；前端 MetricCreate/MetricDetail **146**（+2 F1 放行用例、F3 统计周期消失断言）+ tsc 0（SqlInferEval.test 2 处并行会话 HEAD 类型缺口除外） | 无接口变更（类型判定语义修正：比率/运算→composite、周期覆盖回映 type；派生表达式改选填；原子分支移除周期输入） | —（semantic 保持 verified；指标类型判定/校验/文案与 DEV_GUIDE §7a OneData 共识完全对齐） |
|------|-----|------|--------|----------|----------|
| 2026-08-27 | — | semantic (SQL 智能推断评测)+frontend | **评测样本明细「期望 vs 实际」度量由 `|` 拼接签名串升级为结构化度量卡片**（用户反馈"详情展示的度量是 `doctor_code|COUNT_DISTINCT|alias:...|table:...` 拼接串，不能优化展示布局吗"）：① **后端返回结构化实际解析**——`CaseMetrics` 增 `pred_measures_detail`（column/agg/alias/table/signature 结构化列表，从 `profile.measures` 提取），`evaluate_case` 填充、`report_to_dict` 输出（与既有 `expected_measures_detail` 对称；随报告落 `cases_json`，历史记录向后兼容）；② **前端度量逐字段展示**——展开明细「度量」行期望/实际列从"签名串 Tag"改为**结构化度量卡片**（`MeasureItem`：列名 code + 聚合 Tag + 别名 `as xxx` + 源表 `← xxx` 分开展示），缺失红边（期望签名判定）/多余橙边（后端 signature 判定）高亮保留，无 detail 的历史记录回退签名 Tag 渲染；源表/周期行保持短值 Tag 列表不变。**验证**：后端 test_sql_infer_eval 6（+2：pred_measures_detail 与 pred_measures 签名一一对应、doctor_active_month 首条结构化字段断言）全过 + ruff 0；前端 SqlInferEval 7（展开/匹配用例断言更新为结构化元素：`as last_month_active_doctor_cnt` 缺失红标、`as current_month_active_doctor_cnt`/`COUNT_DISTINCT`/`doctor_code`/`← 源表` 双列展示）全过 + 全量 1002（9 项 Catalogs/MetricCreate 并行 WIP 干扰 flaky 隔离全过）+ tsc 0 | `GET /sql-infer-eval` 报告 cases 增 `pred_measures_detail`（扩展，无破坏；历史记录缺字段时前端回退签名 Tag） | —（semantic 保持 verified；评测明细度量从"`|` 拼接签名串一长串"升级为"列名/聚合/别名/源表分开展示的结构化卡片"，缺失/多余高亮保留） |
| 2026-08-27 | — | semantic+governance+frontend | **第十一轮生产就绪审查 P1+C+F 全量收口**：P1——聚合枚举更新 Literal 补 FIRST_VALUE（SQL 推断产出的 FIRST_VALUE 指标编辑不再 422）、运维事件订阅播种（`storage.table_oversized` 接入 BUSINESS_EVENT_TYPES+标题映射，platform_admin 启动默认订阅表增长/容量/降级/采集失败/血缘注册失败，死信告警可触达）、ES 用户名 env 别名兼容（`UNISENSE_ES_USER`/`UNISENSE_ES_USERNAME` 双拼写，.env.example 修正）；C——cron 任务分布式锁（新增 `TaskLock`/`task_locked` 装饰器，14 个写副作用 cron 防多副本/重入双跑）、主数据乐观锁（term/dimension 加 row_version 列，update 回传版本不一致 409 OPTIMISTIC_LOCK_CONFLICT）、conflict pair 活跃对唯一索引（生成列 active_pair，并发双落 OPEN 被 DB 拦截，软删/关闭释放可重开）、DLQ 重放循环启动（init 后 start_replay_loop，事件入队不再永不重放）、arq on_job_start/end 绑定 job_id trace_id；F——管理页路由守卫补 RequirePerm（create/data-sources/catalogs/collection-*/domains/dicts/measure-catalogs，ROUTE_PERM 补 measure-catalogs）、pageSize 四页持久化统一（Glossary/AuditLog/Catalogs/MeasureCatalogs 对齐 usePersistentPageSize）、错误文案收敛公共 `errMsg`（apiErrorHandlers，MeasureCatalogs 去本地复制）、字典加载失败提示（F-4 全失败/部分失败 warning）、解析详情/成员/映射大表滚动。**验证**：后端 P1+C 相关 10 文件 302 全过（含 C-2 CAS 4 新增、C-1 任务 20）+ conflict/glossary/dimension 全绿 + ruff 0；迁移 0100/0101 隔离库+开发库 upgrade 验证（active_pair 生成列唯一索引实测双 OPEN 拦截/软删可重开）；前端 F 相关 7 文件 247 全过 + 全量 1011（偶发干扰单独跑全过）+ tsc 0 | 无接口破坏（Term/DimensionResponse 增 row_version 兼容默认 1；`/conflicts/check` 并发兜底返回仍 409 语义；env 兼容旧拼写） | —（semantic 保持 verified；第十一轮 P1+C+F 全部落地） |
| 2026-08-26 | — | semantic+frontend+docs | **指标类型 OneData 语义共识落地（方案 2，变体口径）**（用户三次纠正概念："原子指标是基于某一业务事件行为下的度量、业务定义中不可再拆分的指标，业务限定不属于原子指标"）：① **共识固化**——`docs/DEV_GUIDE.md` 新增 §7a「指标类型 OneData 语义共识（强制遵循）」（唯一口径：原子=业务过程+度量，**不含**统计粒度/业务限定/时间周期；派生=原子+粒度+业务限定+周期，依赖可选；复合=多指标运算）；`docs/指标设计以及界限说明.md` §1.1 派生定义表修正（去掉「依赖指标必填、不加统计粒度」与自身 §1.2 矛盾的表述）+ 概览加系统落地口径注记；`docs/technical-design.md` measure_catalog SQL 注释同步；② **代码文案统一**——后端 `_infer_type`/`_build_atomic_candidate`/schemas.py/service.py 派生描述补「业务限定」（派生=原子+业务限定+周期，依赖可选）；前端 `METRIC_TYPE_DESC`/`TYPE_HINTS`/Templates/MetricDetail/MetricCreate/types.ts 把「原子=逻辑度量+聚合方式」统一为「原子=逻辑度量+基础统计粒度（日），不含业务限定与时间周期」（方案 2：`metric.atomic` 保留为变体便捷标签，业务限定/非日周期一律归派生）；③ **类型判定逻辑不变**——周期驱动（非日周期→派生、日→原子）保持。**验证**：后端 test_auto_fill/test_sql_split/test_semantic_schemas 140 + test_auto_suggest_api/test_sql_batch_api 26 全过 + ruff 0；前端 MetricCreate/MetricDetail/MetricCatalog/Templates 相关 + tsc 0 | 无接口变更（纯语义文案/文档澄清，类型判定逻辑不变） | —（semantic 保持 verified；指标类型概念口径从历史错误表述「原子=逻辑度量+业务限定+周期」统一纠正为 OneData 共识，并写入开发规则 §7a 强制遵循） |
| 2026-08-26 | — | semantic (SQL 智能推断评测)+frontend | **评测样本明细「期望 vs 实际」对照表格化**（用户反馈"期望 vs 实际（度量/表/周期）都拼接在一起，浏览不直观"）：① **后端返回完整实际解析结果**——`CaseMetrics` 增 `pred_measures`/`pred_tables`（完整解析度量签名/源表集合，此前仅返回 missing/extra 差异、前端无法做"期望 vs 实际"完整对照），`evaluate_case` 填充、`report_to_dict` 输出（随报告落 `cases_json`，历史记录一并保留）；② **前端双栏对照 Table**——展开明细从纯文本行拼接重构为「维度 / 期望 / 实际 / 判定」四列对照表（度量/源表/周期三行）：期望列缺失项红标（期望有、实际未解析）、实际列多余项橙标（实际解析出、期望未声明）、判定列匹配绿 / 差异红（`N 缺失 · M 多余`，周期行显示 `期望 X → 实际 Y`）；表格顶部图例说明红/橙语义；空实际显示"未识别"；`SqlInferEvalCase.pred_measures/pred_tables` 声明可选兼容历史记录（缺失时 `?? []` 兜底）。**验证**：后端 test_sql_infer_eval 6（+1 report_to_dict 含 pred + 集合一致性断言：pred 含 extra、与 missing 不相交）+ 评测相关 8 + 样本 service 31 全过 + ruff 0；前端 SqlInferEval 7（更新展开明细断言为对照表 + 新增匹配用例绿色判定/完整实际解析用例）全过 + 全量 1011 全绿 + tsc 0 | `GET /sql-infer-eval` 报告 cases 增 `pred_measures`/`pred_tables`（扩展，无破坏；历史运行记录向后兼容） | —（semantic 保持 verified；评测明细从"拼接文字"升级为"期望 vs 实际结构化对照"，缺失/多余/周期不符一眼可辨） |
| 2026-08-26 | — | semantic (指标类型 OneData 语义对齐)+frontend | **指标类型语义按阿里 OneData 理论对齐（周期驱动）+ 派生依赖可选**（用户纠正"派生指标 = 原子指标 + 业务限定 + 时间周期，原子指标是通用逻辑度量"）：① **类型判定周期驱动**——批量 `_build_atomic_candidate` 与单条 `_infer_type`：解析出非日时间周期（month/week/quarter/year/小时等）→ 候选默认 **derived**（原子指标 + 时间周期，如「本月医院入口活跃医生数」= 活跃医生数 + 月周期）；日粒度（day）为基础统计粒度保持 **atomic**（原子常态）；比率/多列运算仍归派生/复合；② **派生依赖可选**——后端 `_validate_definition_json` 仅 **composite 强制 dependencies**，派生 = 原子 + 周期可无依赖直建（血缘注册对无依赖派生安全返回）；带依赖派生仍走依赖发布校验 + 环检测；③ **前端语义修正**——TYPE_HINTS/单条与批量类型校验/标题/注释全部按 OneData 澄清（原子=逻辑度量+基础粒度、派生=原子+时间周期、复合=多指标运算）：单条向导与批量候选仅复合强制依赖、派生有计算主体即可（周期驱动解析候选自带 expression 无需手填 calc）；批量依赖可选项从「仅原子」扩为「原子+派生」（跨语句）；编辑弹窗校验对齐；④ **展示语义**——MetricDetail/MetricCatalog 指标类型项加 OneData 语义 Tooltip（METRIC_TYPE_DESC）。**验证**：后端 test_sql_split 66（+4 月周期→派生断言更新、day 边界保留 atomic）+ test_auto_fill 34（+2 周期驱动：month→derived/day→atomic）+ test_semantic_schemas 40（派生无依赖有表达式通过/无表达式拦截/复合强制依赖），semantic 相关 8 文件 509 全过 + ruff 0；前端 MetricCreate 64（+2：派生选填标题/未选依赖不拦缺表达式拦）+ MetricDetail 82 + MetricCatalog 140 全过；tsc 0 | 无接口变更（`type` 枚举语义扩展：批量/单条推断按周期默认派生；`definition_json.dependencies` 派生改为可选） | —（semantic 保持 verified；指标类型从「原子=单聚合列、派生=依赖其他指标」纠正为 OneData「原子=逻辑度量+基础粒度、派生=原子+时间周期、复合=多指标运算」，SQL 解析周期驱动归类 + 派生依赖可选） |
| 2026-08-26 | — | semantic (原子指标口径库)+system_dict+frontend | **源头系统字典化 + 同义词编辑弹窗 AI 生成 + 度量格式字典化（联动改造）**（用户反馈"源头系统/业务系统术语选项框是空的、同义词是否加 LLM、度量格式是否入字典管理"）：① **源头系统字典化**——迁移 0099 种入 `source_system` 字典种子 8 项（HIS/EMR/LIS/PACS/医保结算平台/银海his/天信his/事前事中his审核）；前端 `source_system` Select 动态加载字典候选（保留 `mode="tags"` 自由输入，满足 PRD FR-04-03 从业务系统选择 + 自由输入）；SystemDict 字典管理页加「源头系统」类型标签可在线管理；② **编辑弹窗同义词 AI 生成**——新增 `POST /measure-catalogs/infer-synonyms`（写角色 + LLM 额度防护，复用 `_suggest_by_llm` 解析 + 宽松内容校验 `is_abnormal_llm_text`，不可用/空内容/垃圾 → `LLM_INFER_UNAVAILABLE`）；前端同义词框「AI 生成同义词」按钮（名称+描述 → 生成候选合并去重回填 tags，未填名称提示不调用）；③ **度量格式字典化 + 联动改造**——迁移 0099 把 `measure_catalog.measure_format` 从 MySQL ENUM 改 `VARCHAR(32)`（允许字典自定义格式写入，对齐 category 字典化先例）+ 种入 `measure_format` 字典种子 3 项（AMOUNT/RATIO/NUMERIC，`extra` JSON 携带默认单位/小数位）；`system_dict` 加 `extra` JSON 列（Create/Update/Response 透传）；service 新增 `_validate_format`（字典校验 + 未配置回退枚举）与 `_resolve_format_defaults`（字典 extra 优先、回退 `_FORMAT_DEFAULTS`），create/update 接入；schema 放宽 measure_format 校验（限长，值交给 service）；前端格式下拉动态加载字典 + `onFormatChange` 按字典 extra 联动单位/小数位（回退硬编码）；SystemDict 字典管理页「度量格式」类型显示默认单位/小数位扩展字段（可在线维护）。**验证**：后端 test_measure_catalog 70（+8：格式字典校验/自定义格式 extra 联动/未配置回退枚举/同义词 5 项）+ system_dict 相关 69 + measure/dict 相关 139 全过 + ruff 0；迁移 0099 真实 DB 应用并验证（varchar(32) + extra JSON + 3 格式 + 8 源头系统）；前端 MeasureCatalogs 17（+2 同义词生成合并/未填名提示）+ SystemDict 25（+1 格式 extra 提交）+ 全量 1010（1 项 DataSources 既存 flaky 单独跑 40 全绿）+ tsc 0；顺带修复 HEAD 既有 AiAssistant.test mock 缺 NL2SQLResult.execute（阻断 tsc/build） | `POST /measure-catalogs/infer-synonyms`（新增，写角色 + LLM 额度防护）；`system_dict` 加 `extra` JSON 列（迁移 0099，Create/Update/Response 透传）；`measure_catalog.measure_format` 列 ENUM→VARCHAR(32)（迁移 0099，行为扩展）；`GET /dicts/measure_format`、`GET /dicts/source_system` 提供候选（新增字典类型） | —（semantic 保持 verified；源头系统从"空选项自由输入"升级为"字典候选 + 自由输入"，同义词从"仅创建 AI 推断"补齐"编辑弹窗 AI 生成"，度量格式从"硬编码 3 枚举"升级为"字典可管理 + extra 联动默认单位/小数位"） |
| 2026-08-26 | — | glossary+subject_domain+auth+assetmap+consume+observability+collector+security | **第十轮 P1/P2 剩余缺口全部补齐**（主数据/域管理写端点域作用域 + 登出吊销 refresh + 密钥轮换持久化 + 资产地图/快照/观测性/描述覆盖数据一致性）：① **glossary 写端点域作用域 + owner 校验**——create/update/deprecate/reactivate/delete/restore/relation 接入 `_assert_term_scope`（platform_admin 放行；domain_admin 限本域；metric_owner 限本人+本域），update_term 补 owner 校验（此前无任何 owner/域校验，任意写角色可改删任意域术语）；② **subject_domain 本域归属校验**——update/activate/deactivate/delete/update_defaults 接入 `_assert_domain_admin_scope`（domain_admin 仅可管理本域及其子域，path 前缀判定），create 校验父域/根域归属；③ **auth 登出吊销 refresh token**——`revoke_active_refresh` 拉黑活跃 refresh jti 并清映射（登出后被劫持/残留 refresh 无法再续期 access token，补会话吊销闭环，此前仅拉黑 access）；④ **密钥轮换持久化密钥链**——rotate_key 改用确定性盐 + 完整密钥链（活跃+全部解密密钥）原子落盘 backend/data/fernet_keychain.json（0600+gitignore），initialize 优先从 keychain 恢复；admin_key_rotation rotate/migrate/dry_run 补审计 + migrate 扫描范围补 llm_config.api_key_enc；⑤ **assetmap 健康度描述体检按组织隔离**——_health_descriptions 接 org_id（db_catalog→data_source.org_id join 过滤），_health_metric_checks 标注平台级合规语义；⑥ **consume 快照唯一约束 + 日志保留期**——metric_value_snapshot 加 dims_signature 列 + uk 唯一约束（迁移 0098，save_snapshot 先查后插 + 竞态兜底，同口径重复快照跳过）；data_retention 补 query_log/tracking_event 180 天保留期物理清理；⑦ **observability PII 待复核按组织隔离**——overview_stats/_overview_risks 接 org_id（pii 查询 join data_source.org_id，授权到期/漂移保持平台级），API /overview 计算 org_id；⑧ **描述覆盖口径统一 + 软删源过滤**——_column_has_desc 仅计 manual/llm（schema comment 是采集原始值不算治理覆盖，摘要与明细口径矛盾修复），get_description_coverage 全统计 join data_source.deleted_at IS NULL。**验证**：新增 glossary +5、subject_domain +6、security +2、key_rotation +1、consume +2、data_retention +1、assetmap +1、observability +1；相关模块 588 全过 + ruff 0；迁移 0098 隔离库+开发库验证；前端路由守卫确认已达标（RequirePerm 覆盖管理路由） | `POST /terms` 等写端点按域/Owner 收敛（行为修复）；`POST /auth/logout` 额外吊销 refresh（行为修复）；`POST /admin/keys/rotate` 持久化密钥链 + 审计；`GET /observability/overview` PII 待复核按组织隔离；`GET /catalogs/description-coverage` 字段覆盖口径修正 + 软删源过滤；metric_value_snapshot 加唯一约束（迁移 0098） | —（semantic/consume 保持 verified；第十轮 P1/P2 剩余项 100% 收口——主数据/域管理越权闭环、会话吊销闭环、密钥轮换可持久化、健康度/快照/观测性/描述覆盖数据一致性与组织隔离） |
| 2026-08-26 | — | semantic (SQL 批量解析)+frontend | **批量解析候选「指标类型可在线编辑（原子/派生/复合）」+ 原子指标语义纠正**（用户纠正"原子指标难道不是逻辑度量吗？给出的 SQL 有业务限定、时间周期"）：① **语义对齐**——原子指标 = 逻辑度量 + 业务限定 + 统计周期（非"单个聚合列"）；SQL 解析出的原子候选是带限定/周期的业务指标，可在线改为派生/复合；② **后端**——`SqlBatchCreateCandidate.type` 从 `Literal["atomic","composite"]` 扩展允许 `derived`；`batch_register_from_sql` Phase2 从仅 composite 扩展为派生/复合（依赖预检缺依赖跳过 + `type=cand.type` 创建 + **派生透传 mount**（OneData 挂载层，源表/列/粒度/周期/域自动落 metric_mount，对齐单条派生向导））；③ **前端**——批量候选行类型 Tag → **可编辑 Select（原子/派生/复合）**；非原子行显示「依赖指标」多选（从本批原子候选选，跨语句可选，展示名称(最终编码)）+「计算表达式」输入；提交组装：派生/复合构建 `definition_json.expression`（calc 优先覆盖，复合解析候选无 calc 保留自带 sql）+ `definition_json.dependencies`（血缘上游边）+ 派生自动构建 mount；必填校验：derived 必须填计算表达式、composite 自带 sql/expression 可不填、依赖指标均必填。**验证**：后端 test_sql_batch_api +1（derived 候选 200 透传 type+mount）+ test_semantic_service +1（Phase1 原子 + Phase2 派生依赖命中 atom_ok + mount save），相关 409 全过 + ruff 0；前端 MetricCreate 64（+1 类型改派生→依赖/表达式/挂载透传提交；2 处复合名断言 Tag→Input 改 getByDisplayValue）全过 + tsc 0（仅并行 AiAssistant.test 既有错误） | `POST /metric-definitions/batch-register-from-sql` 候选 `type` 接受 `derived`（扩展，无破坏）；派生候选可带 `mount`（此前仅 atomic/composite） | —（semantic 保持 verified；批量解析候选从「类型只读仅原子/复合」升级为「原子/派生/复合在线可改 + 派生挂载透传」，原子指标按「逻辑度量+业务限定+统计周期」语义建模） |
| 2026-08-26 | — | frontend (SystemConfig LLM 配置) | **获取模型后可直接像选项框一样点选（方案 C）**（用户反馈"系统配置获取到模型后不能像选项框那样选择已获取模型"）：模型名称控件由"空值聚焦不展开下拉"的 AutoComplete 升级为受控展开——新增 `modelOpen` state，`handleFetchModels` 成功获取后 `setModelOpen(true)` 直接展开全部模型列表 + 当前模型为空时自动选中第一个 + 成功提示带模型列表摘要（`已获取 N 个可用模型：m1、m2、...`，超 5 个截断）；AutoComplete 聚焦时 options 非空自动展开、`onDropdownVisibleChange` 同步关闭；`openCreate/openEdit` 复位展开状态（destroyOnClose 只销毁 Modal 内子组件、不重置父级 state）。保留 AutoComplete 原生自由输入能力（网关不支持 /models 时手动输入兜底不受影响）。**验证**：前端 SystemConfig 20 全过（+3：获取成功自动选中第一个+摘要+下拉展开 / 已有模型值不被覆盖 / 失败不展开不选中）；tsc 0（仅并行 AiAssistant.test 既有错误）；ruff 0 | 无接口变更（纯前端交互增强：AutoComplete 受控 open + 自动选中 + 摘要提示） | —（system_config 保持 verified；"获取模型→点选/改选"从"输入才见建议"升级为"获取即展开可点选"） |
| 2026-08-26 | — | semantic+auth+lineage+quality+consume+recommend+collector | **第十轮全系统生产就绪审查（五大模块群 P0 越权侧门 + P1 域边界）全部落地**：用户要求「从工业/产品/技术角度审查系统各模块是否可投产」——5 个并行调查 agent（采集/血缘资产地图可观测/消费质量AI推荐/治理主数据/认证通知审计）+ 亲验全部 P0 结论，发现 5 个 P0 + 10+ P1/P2 全量修复。① **X-1 AI 问数执行路径越权（数据外泄最大风险面）**——ask(execute=True) 此前把 LLM 生成 SQL 直调 `_get_olap_executor().execute()`，绕过 consume 统一鉴权管道（PDP/域校验/白名单/PII 脱敏），viewer/analyst 可越权读任意 Doris 表原始 PII（SQL 仅靠可绕过的黑名单）；修复：移除直执行，execute=True 统一降级为「只生成 SQL」，附引导 note 由前端「查询工作台」跳转走正规鉴权（ConsumeService.execute_query：PDP+状态闸门+行级隔离+PII 脱敏）；前端移除执行开关/执行结果卡，改「到查询工作台执行」按钮（按 query:execute 权限禁用）；② **X-2 血缘读路径无域/组织收敛**——/impact /edges /path /graph /export 全角色（ALL_ROLES）可跨域读含 pii_inherited/SQL 原文，/export 上限 10 万边；新增 `_effective_read_domain`/`_assert_node_read_access`（platform_admin 放行、其余强制本域；节点可解析且不在本域拒绝），/graph 强制生效域，/impact /edges /path /terminals 节点域收敛，/export 收紧治理角色（platform_admin/domain_admin/compliance_officer）+ 服务层 `_filter_edges_by_domains` 按解析域过滤（端点均无法解析保留共享底座表）；③ **X-3 主数据未指派审批不校验 user_domain**——`master_data_review` 未指派分支只查 role==domain_admin，任意域管理员可跨域审批/打回他域未指派术语/维度/度量（对比指标批量有 PDP 域闸）；修复：`_assert_reviewer_authorized` 未指派分支补本域归属校验（实体域可解析且不在本域拒绝 FORBIDDEN_REVIEWER），三模块单条+批量审批均透传 user_domain 故全覆盖；④ **X-4 登录失败路径 500**——write_audit(actor_id=0) 触发 audit_log.actor_id FK 违规（无 id=0 用户），失败登录返回 500 且失败审计丢失；修复：audit_log.actor_id 改可空（迁移 0097，BigInteger 对齐 user.id——0001 迁移本为 BigInteger、模型 Mapped[int] 曾漂移为 INTEGER 一并修正），登录失败审计 actor_id=None + 审计写入兜底 try/except（失败仅告警不阻断 401）；⑤ **X-5 MySQL 增量降级守卫静默失效**——`_get_mysql_update_time_ratio` 在已有 running loop 上 run_until_complete 必抛 RuntimeError 被吞恒返 False，InnoDB UPDATE_TIME 为 NULL 时增量采集静默漏采；修复：`should_mix_in` 改原生 async（直调 `_get_mysql_update_time_ratio_async`）+ 调用点 await。**P1/P2**：⑥ stale 失效边不再参与影响分析（`_edges_from/_edges_to` 补 stale=False，对齐模型注释）；⑦ 质量规则/基准导入/绑定补指标域归属校验（create_rule/import_benchmark/bind_benchmark 经 `_assert_metric_domain`，防跨域污染对账告警）；⑧ 推荐域收敛（recommend_metrics/related_metrics 仅产本域 PUBLISHED，platform_admin 不限——此前仅过滤 PUBLISHED 会向 A 域用户推荐 B 域指标码）；⑨ consume degraded 标识按实际引擎判定（MySQL 降级即 True，此前硬编码 False 让消费方误判数据质量）；⑩ quality list_events 非法 status/level 转 422（此前 500）；⑪ tracking 埋点事件类型白名单 + context 有界（防任意登录用户伪造海量行为事件污染协同过滤画像）。**验证**：新增 test_lineage_read_scope 7（域收敛/节点门/导出过滤）、test_tracking_validation 6（白名单/context 有界）、X-3 跨域拒绝 1、X-5 async 适配 2、X-1 直执行移除 3、degraded 断言更新；相关模块 658 全过（唯一失败 test_nl2sql_rejects_unanchored 为本地 LLM 已配置的环境既有失败，与 X-1 无关）+ ruff 0（仅 1 处既有 E501 不触碰）；迁移 0097 在 unisense_it 从零 upgrade 至 head + 开发库 upgrade 验证（actor_id bigint NULL + FK 恢复）；前端 AiAssistant 7 全过 + tsc 0 | POST /ai/nl2sql execute 参数不再直执行（行为修复：返回 SQL 不返回 execute_result）；GET /lineage/* 读路径按域收敛（非 platform_admin 强制本域，/export 限治理角色）；audit_log.actor_id 可空（迁移 0097）；POST /tracking/event 事件类型白名单 + context 有界 | —（semantic/consume 保持 verified；AI 问数直执行越权侧门关闭、血缘/质量/推荐读路径域边界收口、登录失败 500 修复、增量采集降级守卫生效、埋点画像防污染） |
| 2026-08-26 | — | semantic (SQL 批量解析) | **批量解析逐语句域建议并入阶段 2 并发（上轮「批量解析 LLM 兜底并发化」收尾）**——`infer_sql_batch` 阶段 1 不再串行 `await suggest_domain`，改收集 `pending_domain`；阶段 2 与周期/度量兜底同批 `asyncio.gather` 并发执行（信号量 `_LLM_FALLBACK_CONCURRENCY=4` 限并发）；阶段 3 按 idx 回填到候选 `suggested_domain_code` 与语句摘要 `suggested_domain`。**安全性论证**：`suggest_domain` 内部预算「检查 `used<limit` 与 `used+=1` 之间无 await 分隔」（asyncio 单线程下原子安全，不超发）、结果不依赖调用顺序——与周期/度量兜底同批 gather 行为等价（预算分配顺序从「逐句先域后周期」变为「周期/度量先预留、域后占用」，预算本是 best-effort 软护栏不影响正确性）；清理阶段 1 `seg_domain_code` 变量与 `stmt_records`/`pending_measures` 死键。**顺带根治测试 flaky**：方案 A 默认全量 LLM 校验层使 `infer_sql_batch` 单元测试（`_fake_db` MagicMock + 本地 .env 配 LLM key → 构建真实 client）在 LLM 实例 429 时把候选任意改写（复合候选丢失/派生聚合变化/StopIteration），致 5-6 个「依赖候选精确结构」测试时过时不过；新增 `test_sql_split.py` 文件级 autouse fixture `_mock_llm_validation` mock 掉 `llm_validate_measures`（返回 None = LLM 不可用时保持规则结果的真实语义），整文件 66 测试从 110s/5 failed 变为 5s/全绿（不再触发真实 LLM 429 重试等待）。**验证**：test_sql_split +1（逐语句域建议并发 max_active≥2 且 sales/health 按 idx 回填到候选与语句摘要），66 全过；sql_infer/auto_suggest_api/sql_batch_api/sql_validation 92 全过 + semantic_service 275 全过 + ruff 0 | 无（`POST /parse-sql-batch` 接口契约不变；逐语句域建议从串行升级为阶段 2 并发，回填结果不变） | —（semantic 保持 verified；批量解析逐语句域建议从「N 条语句串行 N 次 DB 反查/LLM 兜底」升级为「并发（限 4）执行 + 按 idx 回填」；单元测试根治 LLM 校验层 flaky） |
| 2026-08-26 | — | semantic+consume+infra | **第九轮生产就绪审查（读路径隔离 + 数据生命周期 + 供应链/发布）全部缺口落地**：用户要求「再次从工业/产品/技术角度审查是否可投产」——3 个并行调查 agent（域边界隔离深度 / 数据生命周期治理 / 依赖供应链·发布流程）+ 亲验 + 独立 OLAP 下推正确性审查，发现 2 个读路径隔离缺口 + 3 个生命周期治理债 + 4 个供应链/发布配套全量修复。① **D-1 全局搜索绕过目录可见性过滤**——此前 `GET /search` 全角色可读且无任何可见性过滤，MySQL/ES 双路径均返回 DRAFT/REVIEW 指标 + pii_flag（绕过指标目录行级隔离，任意 viewer 可经搜索侧门窥探未发布草稿）；修复：`GlobalSearchRepository.search` 增 `visible_actor_id/visible_role`，MySQL 路径 `_visibility_conditions`（非管理角色仅 PUBLISHED/EXPERIMENTAL/DEPRECATED + 本人 Owner/副 Owner，reviewer 额外放行 REVIEW）与 ES 路径 `_es_visibility_filter`（bool.filter 同语义）双路径收敛；ES mapping 增 `owner_id/backup_owner_id` 字段 + indexer 同步 + 版本检测（缺 owner_id 即旧版重建 /sync 重灌）；API/service 透传用户上下文；② **D-2 consume 快照端点无 PDP**——`GET /consume/metrics/{code}/snapshots` 此前任意接入方可凭 code 跨域读取任意指标历史查询数据值（唯一无校验的查询结果读取路径）；修复：按通道鉴权——消费方 `list_snapshots_for_client` 走接入方四级闸门（域→白名单→PII→合规复核，fail-closed）、内部用户 `list_snapshots_for_internal` 走 PDP `check_internal_read_permission`（平台直通/本域 ROLE_ACTIONS/跨域 ACTIVE grants）；③ **L-1 审计归档只复制不搬迁**——归档后热表无限增长、日写入>1000 条永久积压；`audit_archive_task` 改为「物理搬迁闭环」：每批 1000 条循环（MAX_BATCHES_PER_RUN=20 防无限循环）、上传 MinIO 成功后标记 archived 并**物理删除热表行**（冷存 JSONL 含完整 WORM 记录）、失败仅记 FAILED 不删次日重试；④ **L-2 metric_version 版本爆炸**——每次编辑新增一行无上限；`create_version` 后 `_archive_excess_versions` 将超出保留上限（50）的已定稿旧版本（PUBLISHED/EXPERIMENTAL/CANCELLED）标记 ARCHIVED（DRAFT/PENDING 不归档不扰确认流程），service 版本展示层过滤 ARCHIVED（历史 WORM 仍可查、rollback 不受影响）；⑤ **L-3/L-4 无主清理表 + 无通用表监控**——新增 `data_retention.py`：`purge_retained_records`（conflict/ruling_record/escalation_record 软删超 180 天物理删除（先删子表防孤儿）+ sql_infer_eval_run 保留近 365 天，单表限 5000 行防一次删爆 undo）+ `check_table_growth`（information_schema 核心 14 表行数/大小巡检，超阈值发 `storage.table_oversized` 告警事件 best-effort）；worker 注册 cron（retention-purge 03:45 / table-growth-check 05:00）；⑥ **S-1 前端依赖无漏洞扫描**——frontend-gateways 加 `npm audit --audit-level=high --omit=dev`（对齐后端 pip-audit）+ 新增 `.github/dependabot.yml`（pip/npm/github-actions 每周自动更新 PR，私有 git+ 项忽略）；⑦ **S-2 镜像供应链**——基础镜像浮动 tag 无 digest、前后端容器均 root 运行：新增 `scripts/pin_base_images.sh`（发布时在可联网机器固定 Dockerfile/compose 各基础镜像 digest，覆盖 minio:latest 最高危项）；backend Dockerfile 加非 root `appuser`（pip --user 装到 /home/appuser/.local）；frontend nginx 降权（listen 8080 + `USER nginx` + 缓存/pid 目录归 nginx，compose 端口映射 8180:8080、健康检查/EXPOSE 同步）；⑧ **S-3 发布/回滚无载体**——compose 全用 `build:` 无镜像 tag/registry；backend/worker/frontend 增 `image: unisense/<svc>:${UNISENSE_IMAGE_TAG:-dev}`（发布时设 tag 拉取已推送镜像、回滚=换 tag）+ 新增 `scripts/release.sh`（build/push 带 tag）；6 个 runbook（collector/consume/conflict/lineage/semantic/governance）的 `kubectl rollout undo` 全部改为 compose 镜像 tag 回滚实际步骤；⑨ **S-4 私有依赖内网可达**——requirements.txt 的 `-e git+ssh://...` 私有项补注释（镜像构建被 Dockerfile 过滤、本地 poetry 安装需 GitHub 可达，内网须 vendored）。**验证**：新增 test_data_retention 6（无行/四表物理删+计数/超期过滤+limit 约束/无超限不发告警/超限发布 storage.table_oversized/发布失败 best-effort）；D-1 全局搜索 +D-2 consume +L-1 audit_archive +L-2 semantic 相关测试适配（可见性过滤/通道鉴权/物理删除循环/版本归档）；相关模块 535+227 全过 + ruff 0；compose config 解析通过；bash -n 两脚本通过 | `GET /search` 指标组按调用者可见性收敛（行为修复：低权限用户不再能检索他人 DRAFT/REVIEW/PII）；`GET /consume/metrics/{code}/snapshots` 接入通道鉴权（消费方四级闸门/内部 PDP，行为修复）；`POST /sql-infer-eval/run` 等无接口变更；`metric_version` 超限旧版自动 ARCHIVED（展示层过滤）；compose 服务增 `image:` 标签（UNISENSE_IMAGE_TAG 控制发布/回滚）；frontend 容器监听 8080（外部 8180 不变） | —（semantic/consume 保持 verified；批量注册与消费从「目录隔离被搜索/快照侧门打穿」修复为读路径全链路隔离，审计/版本/软删记录从无限增长升级为有治理闭环，供应链/发布从浮动 tag+无载体升级为可固定 digest+可回滚） |
| 2026-08-26 | — | semantic (SQL 批量解析) | **批量解析逐语句 LLM 兜底并发化（两阶段重构，用户"继续执行"延续上轮"批量解析提速"预告）**——`infer_sql_batch` 主循环从「每语句内串行 `await` LLM 兜底」重构为两阶段：① **阶段 1 纯规则解析（串行、无 LLM）**——切分 + 逐语句 `parse_sql_profile`，收集 `pending_period`（规则有度量但 `_period_uncertain`）与 `pending_measures`（规则无度量且 parse_failed）需求清单；② **阶段 2 并发兜底**——`asyncio.gather` 并发执行 `_llm_infer_period`/`_llm_infer_measures`，信号量 `_LLM_FALLBACK_CONCURRENCY=4` 限并发（防同时打爆 LLM 网关），单任务失败返回 None 不阻断；③ **阶段 3 按语句 index 回填合并**（`built_by_idx` + 排序展平），候选顺序与串行完全一致。**行为等价**：预算语义（阶段 1 先检查后分配，`_LLM_BATCH_LIMIT`/`_LLM_BATCH_LIMIT_LLM` 不变）、skipped 分类（llm_limit/parse_failed/llm_infer_failed）、逐语句域建议保持串行（内部预算递增依赖顺序）、整段域建议/use_llm 补全/LLM 校验层不变。**效果**：多语句脚本同时触发 LLM 兜底时墙钟从「N×单次调用」降到「并发数内单次调用」——解决用户反馈的批量解析 45-93s 等待（上轮已把前端超时放宽到 180s 消除误报失败，本轮从后端真正提速）。**验证**：test_sql_split +3（多语句周期兜底并发 max_active≥2 且候选周期正确 / 多语句度量兜底并发 + 候选按 idx 顺序 / 混合场景有度量+LLM 兜底+有度量顺序保持 [0,1,2]），65 全过；sql_infer/auto_suggest_api/sql_batch_api/评测门禁 145 全过；test_semantic_service 275 全过 + ruff 0 | 无（`POST /parse-sql-batch` 接口契约不变；仅内部执行模型从逐语句串行 LLM 兜底升级为两阶段并发） | —（semantic 保持 verified；批量解析 LLM 兜底从「N 语句串行 N 次调用」升级为「并发（限 4）执行 + 按语句回填」，墙钟显著下降、行为等价） |
| 2026-08-26 | — | semantic (SQL 智能推断评测)+frontend | **评测集升级为「内置基线 + 可管理自定义样本」+ 批量解析超时修复**（用户反馈"SQL 解析评测里为什么不能加评测样本" + "批量解析提示失败"）：① **评测样本 CRUD（内置只读 + 自定义可管理）**——迁移 0096 `sql_infer_eval_sample`（case_id 唯一 / dialect / sql / expected_measures(JSON) / expected_tables(JSON) / expected_period / note / enabled / is_builtin / created_by + 软删）；`runner.run_eval(samples=None)` 支持注入样本序列，`service.merged_cases(db)` 内置 GOLDEN + DB 自定义启用样本合并（case_id 冲突内置优先跳过）；新增 CRUD 端点 `GET/POST /sql-infer-eval/samples`、`PUT/DELETE /sql-infer-eval/samples/{id}`（写角色 + sql 豁免注入扫描 + 审计）+ `POST /sql-infer-eval/samples/preview`（即时规则解析预览：度量/源表/周期，不落库）；样本期望合法性校验（聚合枚举白名单 + 周期白名单 + case_id 与内置基线冲突拦截）；`GET /sql-infer-eval` 报告与样本清单改合并视图（内置/自定义 source 标记）；前端评测页「新增样本」按钮 + 弹窗（SQL/方言/期望周期/期望度量动态行(列+聚合枚举+别名+源表)/期望表 tags/说明 + 「预览解析」即时反馈 + 内置只读/自定义可编辑删除 + 来源 Tag）；允许保存失败样本（追踪待修缺口，不阻断 CI 门禁——门禁仍只跑内置 GOLDEN）；② **批量解析失败根因（前端 30s 超时 vs LLM 批量 45-93s）**——实测容器日志：parse-sql-batch 多次 200 但耗时 45s/60s/91s/93s（LLM 模式逐候选校验/补全 + 多实例轮询慢），前端 `REQUEST_TIMEOUT_MS=30000` 统一超时 → 请求被 abort → 误报「批量解析失败」；修复：`request()` 支持 `init.timeout` 自定义超时（缺省 30s），`parseSqlBatch`/`runSqlInferEval` 放宽到 180s；`runSqlBatchParse` 失败文案区分超时（REQUEST_TIMEOUT）——「LLM 实例较慢可稍后重试或切规则模式」，不再误导为语法错误。**验证**：后端 test_sql_infer_eval_sample_service 14（merged_cases 合并/内置冲突跳过、create 成功/内置冲突/非法聚合/空 SQL/编码重复、update 成功/内置拒绝/不存在、delete 软删/内置拒绝、preview 画像）+ test_sql_infer_eval 5 门禁仍 100%；语义相关 97 全过 + ruff 0；前端 SqlInferEval 6（+2：新增样本填 SQL 预览后创建 / 自定义样本行显示标记并可删除）；tsc 0（MetricDetail.test 既有并行错误除外） | `GET/POST /metric-definitions/sql-infer-eval/samples`、`PUT/DELETE .../samples/{id}`、`POST .../samples/preview`（新增，写角色 + 注入豁免 + 审计）；`GET /sql-infer-eval` 样本清单改合并视图（dataset 增 source/expected_measures_detail 字段）；`sql_infer_eval_sample` 表（迁移 0096）；`request()` 支持 init.timeout（批量解析/评测放宽 180s，行为修复） | —（semantic 保持 verified；评测集从「硬编码 9 样本只读」升级为「内置基线 + 自定义样本可 CRUD 管理 + 即时预览」，成功率随样本扩充可度量；批量解析不再因 30s 前端超时误报失败） |
| 2026-08-26 | — | system_dict+frontend | **参照数据项描述 LLM 推断（数据字典参照数据管理新增/编辑/批量新增时「AI 生成描述」，用户要求"新增数据项时描述支持 LLM 推断生成"）**：① **后端**——新增 `POST /dicts/infer-description`（`DictInferDescriptionRequest{dict_type,label,dict_type_label}`；platform_admin + 注入豁免 label/dict_type/dict_type_label——合法业务输入仅作 LLM 上下文、不拼接 DB 查询；复用 `LlmConfigService.build_client()` + 纯文本 `response_format=text`（避免缺省 json_object 约束污染为空 JSON）+ `strip_code_fence`；LLM 不可用/空内容 → `LLM_INFER_UNAVAILABLE`；审计 `dict.infer_description`（verb 模板已存在，无需补 i18n）；**路由顺序**：`/infer-description` 注册在 `/{dict_type}` 之前避免被动态段捕获）；② **前端**——`inferDictDescription` api 封装；SystemDict 单条新增/编辑弹窗描述框加「AI 生成」按钮（RobotOutlined + loading + 错误提示区分 LLM 不可用）+ 批量新增每行描述框加 AI 小按钮（按行回填）；先填显示名才可生成（否则 warning 提示）。**验证**：后端 test_system_dict_api +4（成功+response_format=text+prompt 含 label/类型上下文+审计 dict.infer_description / LLM 禁用 400 / 空内容 400 / 空 label 422）+ system_dict 全套 60 全过 + ruff 0；前端 SystemDict 24（+3：单条 AI 生成随提交落库 / 未填显示名提示不调用 / 批量按行回填）+ tsc 0 | `POST /dicts/infer-description`（新增，platform_admin + 注入豁免；LLM 仅生成文本回填表单不落库） | —（system_dict 保持 implemented；参照数据项从「描述纯手填」升级为「AI 生成辅助」） |
| 2026-08-26 | — | semantic (SQL 智能推断)+frontend | **SQL 解析正确性 LLM 校验层（方案 A 默认全量校验）+ 评测集与成功率可视化**（用户要求"规则解析可能解析错或漏掉，加 LLM 验证正确性 + 保障成功率 99% + 评测集前端可视化 + 成功率指标可视化"）：① **评测集 `sql_infer_eval`**——9 条样本（真实医生月活 ETL：COALESCE 包裹+CASE 条件去重双度量；GMV；Oracle ROWNUM 分页；Spark 窗口派生；ClickHouse 物化视图+sumMerge；Trino approx 归一；Postgres FILTER+GROUP BY 位置序号；MySQL DATE_FORMAT；Doris CTAS），人工核对期望（度量列+聚合+别名/源表+周期），运行器按标准信息检索口径算 度量/表级 精确率召回率 + 端到端完全匹配率；pytest 回归门禁断言 100%（解析器改动致识别退化即失败）；CLI `python -m tests.eval.sql_infer_eval`；② **LLM 校验层（方案 A 默认全量）**——新增 `sql_validation.py`：把 [整段 SQL + 规则画像度量] 打包 1 次调用（temperature=0+json_object），LLM 只做封闭选择（is_measure 是否真度量 / agg 从注册枚举选 / table 从解析源表选 / period 白名单 / missed 漏检扫描），`merge_validation` 确定性收敛（聚合/周期必须命中白名单、表回映解析源表、高置信度≥0.7 才采纳纠正/剔除非度量、低置信度标 needs_review 不静默落库、漏检补充过列名合法性+聚合白名单）；LLM 不可用/失败保持规则结果不动绝不阻断；接入单条 `auto-suggest`（SQL 场景默认校验，响应 `validation_summary`）与批量 `infer_sql_batch`（use_llm=false 默认校验候选，`validation` 字段返回聚合纠正/非度量剔除/漏检报告）；③ **前端评测页 `/sql-infer-eval`**——成功率卡片（端到端完全匹配率 dashboard + 度量/表级召回率精确率 + 周期匹配率）、「运行评测并记录」落历史、成功率趋势表、逐样本明细（方言/通过失败徽标/度量召回率/展开看样本 SQL + 期望 vs 实际缺失/多余度量表周期）；④ **持久化**——迁移 0095 `sql_infer_eval_run`（每次运行落一行：聚合指标 + 逐用例明细 JSON，趋势可视化数据源）；`GET /metric-definitions/sql-infer-eval`（实时报告+历史+样本清单，须在 `/{metric_code}` 之前防路径影子）、`POST /sql-infer-eval/run`（运行+落库+审计）。**验证**：后端 test_sql_validation 17（解析器白名单/列名合法性、仲裁聚合纠正高低置信度/非度量剔除/漏检补充去重/表回映/周期覆盖、批量候选校验收敛、LLM 不可用降级）+ test_sql_infer_eval 5（评测 100% 门禁）+ test_sql_batch_api +2（GET 实时报告 200 全率 1.0 / POST 运行落库+审计）；sql_split/sql_infer/auto_suggest/semantic_service 相关 161+275 全过 + ruff 0；前端 SqlInferEval 4（指标渲染/运行按钮刷新/展开缺失度量/错误提示）+ tsc 0；前端全量 997/998（LineageView 顺序污染 flaky 单独 48/48 过，与本次零交集） | `GET /metric-definitions/sql-infer-eval`（新增，_READ_DEPS）、`POST /sql-infer-eval/run`（新增，_WRITE_DEPS+审计）；`POST /metric-definitions/auto-suggest` 响应新增 `validation_summary`（只读）；`POST /parse-sql-batch` 响应新增 `validation`（只读）；`sql_infer_eval_run` 表（迁移 0095） | —（semantic 保持 verified；SQL 智能推断从「规则解析无置信度、成功率不可度量」升级为「默认全量 LLM 校验（聚合纠正/漏检补充/非度量剔除/低置信转人工）+ 评测集成功率可视化（召回率/精确率/完全匹配率 + 趋势）」） |
| 2026-08-26 | — | semantic+infra+frontend | **第八轮生产就绪审查（备份/密钥 + 前端生产就绪 + API 契约）全部缺口落地**：用户要求「再次从工业/产品/技术角度审查注册指标模块是否可投产」——3 个并行调查 agent（部署运维韧性二 / 前端生产就绪 / API 契约与安全覆盖）+ 亲验，发现 1 个安全加固项 + 2 个运维配置 bug + 3 个前端韧性缺口 + 若干契约/性能小项全量修复。① **P0-1 裸 HTTPException 绕过统一错误信封**——7 个 API 文件 30 处 `raise HTTPException`（subject_domain 8/sensitive_rules 8/ai 5/system_dict 4/semantic 2/consume 1/conflict 1）返回 Starlette 默认 `{detail}` 无 code/trace_id、前端 codeZh 映射失效；全部收敛为 `UnisenseError` 子类（catch re-raise 保留 error_code/http_status；校验 422→ValidationError、404→NotFoundError、409→ConflictError），统一信封 + 前端错误码映射恢复；② **P0-2 RequestBodySizeMiddleware 死代码**——定义于 middleware.py 但 main.py 未注册，裸 `body: dict` 端点可传任意大请求体；补注册（10MB 超限 413）+ security 测试断言中间件已入栈；③ **P0-3 compose backend 健康检查用 `/health`（恒 200）非 `/ready`**——DB 挂时容器仍 healthy 致 worker/frontend 照常启动；改打 `/ready`（含 DB/Redis 依赖检查）+ timeout 10s/retries 5；④ **S-1 已知默认弱凭据通过生产校验**——compose 默认 JWT `dev-jwt-secret-...`/MySQL `test`/ES `es_changeme`/MinIO `minioadmin` 可通过长度/非空校验；config 生产校验加「拒已知默认弱凭据」清单（含 db_url 内嵌 `:test@`），空值=未配置仍合法；⑤ **S-2 audit CSV 无公式注入消毒**——对齐 assetmap：`_sanitize` 对顶层以 `=/+/-/@` 开头字段前缀单引号（detail_json 为 JSON blob 以 `{` 开头安全不消毒嵌套）；⑥ **S-3 前端 fetch 无超时**——主请求加 `AbortSignal.timeout(30s)`（init.signal 时 `AbortSignal.any` 合并，超时抛 `REQUEST_TIMEOUT` 业务错误），重放请求同步加超时；⑦ **S-4 会话中途失效不跳登录**——401 刷新失败清 token 后派发 `AUTH_EXPIRED_EVENT`，App 监听把 user 置 null 回登录页（SPA 内、不依赖路由、登录页自身 401 无害）；⑧ **S-5 LLM 错误日志回显 prompt**——网关 response.text 可能回显请求 prompt；`llm/client.py` 错误日志仅记状态码 + response_len，不回显原文；⑨ **P1-1 分页上限/422 一致性**——quality 4 列表端点 page/page_size 加 `ge/le` 约束、非法 rule_type/severity 转 422；collector 分页全量加 `ge/le`、`list_source_catalogs` 手工构造 DBCatalogListParams 失败捕获转 422（对齐 Depends 注入版）；⑩ **P1-2 backup 静默失败/单库/定时漂移**——backup.sh 支持 `MYSQL_DATABASES` 多库（含 e2e_biz 降级库）、任一库失败 exit 1；compose backup 服务捕获失败打印 `[backup-svc] FAILED` + 60s 快速重试（不再 while 静默吞）；文档定时描述对齐「启动即跑+每 24h」；⑪ **P1-3 vite 入口块过大（913KB）**——manualChunks 拆分 react/antd/g6/state/util 独立 chunk + Google Fonts 异步加载（media=print+onload，内网/离线回退系统字体）；⑫ **P2-1 前端审计动词表缺 12 项**——AUDIT_VERB_LABEL 补 clarify/purge/reactivate/cancel_job/list_databases/list_tables/refine/delete_all/preview_values/batch_create/sql_batch_parse/sql_batch_register；⑬ **P2-2 nginx 安全头缺失**——静态产物加 X-Frame-Options/X-Content-Type-Options/Referrer-Policy/X-XSS-Protection/HSTS/CSP（宽松版适配 Vite/antd）；前端请求透传 `X-Trace-Id`；worker 日志换 structlog（统一 JSON + 脱敏 + 任务级 trace_id 可绑定）。**验证**：后端相关 665+ 全过（+config 拒弱凭据 5 + audit CSV 消毒 1 + security 中间件注册断言），test_sql_infer_eval_report_200 为并行 WIP 测试（干净 worktree 无此用例，mock 缺 MetricResponse 新字段）非本批回归；ruff 0（仅既有 E501 不触碰）；前端全量 **993/994**（唯一失败 LineageView G6 既有全量干扰，单独跑 48 全过）+ 新增 apiAuthExpired/auditI18n 测试 + tsc 0 | 30 处裸 `HTTPException` → `UnisenseError` 统一信封（HTTP 状态码/错误码不变，响应结构统一为 `{code,message,trace_id,detail}`）；`POST/PUT/PATCH` 超 10MB → 413；compose backend 健康检查改 `/ready`；生产新增默认弱凭据拒绝校验；`page_size` 上限 200（quality/collector）；audit CSV 顶层公式注入消毒 | —（semantic 保持 verified；错误信封一致性、body 限制、健康检查就绪化、默认凭据拒用、CSV 消毒、前端请求韧性（超时/401 回登录/X-Trace-Id）、分页护栏、备份多库告警、打包优化、静态安全头 全量收口，达到生产就绪态） |
|------|-----|------|--------|----------|----------|
| 2026-08-26 | — | semantic+frontend | **SQL 批量注册 400 根因修复 + 候选在线编辑增强 + 解析/创建等待提示**（用户反馈：`batch-register-from-sql` 400 +「解析无等待提示 + 推断候选不能在线修改」）：① **400 根因（INJECTION_DETECTED 误拦）**——`_SQL_BATCH_REGISTER_DEPS` 此前只豁免 `candidates[].definition_json`、未豁免 `candidates[].raw_sql`；前端批量创建透传整句原始 ETL SQL（含 `--` 行注释 / `;insert` 多语句）命中注入正则 → 整批 400。真实复现（去掉 raw_sql 后 200）确认；补 `candidates[].raw_sql` 路径豁免（raw_sql 仅参数化落 `Metric.raw_sql` 供 batch_id 回溯/口径反查，不拼接执行，与 definition_json 同级安全；注释明确，其余字段仍全量扫描）；② **批量候选在线编辑增强**——原子候选行新增「单位」Select（`dictOptions.unit` 动态字典，showSearch 可搜）、「粒度」Select（day/week/month… 与周期同源）、「指标编码」Input（4 段式可改，空值 placeholder 提示选域后自动生成，创建即用）；此前仅 name/聚合/周期/逻辑度量/产品负责可改，单位/粒度/编码只读；③ **等待提示**——批量解析中（规则/LLM 模式）与批量创建中新增 Spin + 文案（「正在解析 SQL…（大段脚本需数秒，请勿关闭窗口）」「正在批量创建 N 个指标为草稿（DRAFT），请稍候…」），此前仅按钮 loading，大段脚本无进度反馈。**验证**：后端 test_sql_batch_api +1（候选 raw_sql 含 ETL 注释/;insert 多语句 → 豁免 → 200）+ 14 全过 + ruff 0；前端 MetricCreate 63（+1 改单位/粒度/编码后提交携带修改值；P0-1 编码空断言适配新 Input）+ tsc 0（MetricDetail.test 既有 ConsumptionGuideResponse mock 缺失为并行 WIP）；真实 API 复现确认（用户 ETL 候选含 raw_sql → 修复前 400 INJECTION_DETECTED / 修复后 200 双 DRAFT） | `POST /metric-definitions/batch-register-from-sql` 候选 `raw_sql` 子树豁免注入扫描（行为修复：含整句 ETL SQL 的候选不再被误拦）；批量候选提交字段无 schema 变更（unit/granularity/metric_code 前端行内编辑透传，后端已支持） | —（semantic 保持 verified；批量注册从「整句 ETL 候选必 400 误拦」修复为可用，批量候选从「少数字段可改」升级为「单位/粒度/编码均可在线编辑后创建」，解析/创建过程有明确等待反馈） |
| 2026-08-26 | — | measure_catalog+frontend | **度量目录改名「原子指标口径库」+ 度量分类字典化（用户反馈"度量目录对数仓/其他用户不好理解" + "分类有没有管理入口"）**：① **改名**——前端菜单/搜索类型/页面标题/空态/关联提示全部「度量目录」→「原子指标口径库」（Layout.tsx 菜单与搜索类型、GlobalSearch.tsx、MeasureCatalogs.tsx 标题/说明/错误提示、MetricCreate/MetricDetail/MetricCatalog 的「逻辑度量（度量目录…）」label/tooltip/引导文案；API 路径 measure-catalogs 与技术标识 measure_catalog 不改）；② **度量分类字典化（B1）**——迁移 0094 种入 `measure_category` 字典种子 7 项（FLOW/FEE/DRUG/MEDICAL_INSURANCE/EFFICIENCY/QUALITY/OTHER，幂等：该类型已有数据跳过）；schema 校验放宽（category 非空+长度≤32，不再硬编码枚举集合）；service 新增 `_validate_category`（复用 semantic `_validate_dict_field` 模式：字典未配置→回退枚举种子校验、值不在→DICT_VALUE_NOT_FOUND 拦截、停用→DICT_VALUE_INACTIVE 拦截、DB 异常→放行），create/update 接入；前端分类下拉从硬编码 `MEASURE_CATEGORY_LABEL` 改为动态 `listDictItems("measure_category")`（表格渲染 `MEASURE_CATEGORY_LABEL[v] ?? v` 兼容字典新值）；`MeasureCategory` 类型放宽为 string；SystemDict.tsx `DICT_TYPE_LABELS` 加 `measure_category`（字典管理页可在线增删改/启停用度量分类）；③ **其他可管理项排查结论**——业务域已由主题域管理（有入口）、度量格式联动单位/小数位是计算逻辑不宜字典化、状态机不字典化、源头系统/默认单位是自由文本（本次不纳入）。**验证**：后端 measure_catalog 62（+5：字典化后自定义值通过/未收录拦截/更新拦截/字典未配置回退枚举），相关 79 全过 + ruff 0；迁移 0094 真实 DB 应用 + 7 项种子验证；前端 MeasureCatalogs 15（+1 动态加载断言）/SystemDict 21/MetricCreate/MetricDetail/MetricCatalog 237 全过 + tsc 0；测试断言更新（度量目录→原子指标口径库 2 处） | `measure_catalog.category` 值来源从枚举放宽到 system_dict（dict_type=measure_category，可在线管理）；已配置字典后未知分类被 service 拦截（DICT_VALUE_NOT_FOUND） | —（measure_catalog 从「硬编码枚举分类、命名难懂」升级为「字典可管理分类 + 名称对齐数仓术语（原子指标口径库）」的可运营态） |
| 2026-08-26 | — | semantic (SQL 智能推断) | **修复 COALESCE 包裹 + CASE 内嵌聚合丢失（用户真实 ETL：医生月活 SQL 双度量只剩 1 个 → 推断退化为「原子」且无字段确认）**：真实 Hive `coalesce(count(distinct case when ... then doctor_code end), 0) as last_month_active_doctor_cnt` 的 target 是 Coalesce，`_is_wrapped_aggregate` 此前把表达式内含 `exp.Case` 误判为「派生组合节点」→ 单聚合包裹投影被跳过 → 下沉分支只捕获简单 `count(distinct col)`、条件去重聚合静默丢失（同语义 `sum(case...)` 无包裹可捕获、包 COALESCE 即丢，行为不一致）。**修复**：`_is_wrapped_aggregate` 组合节点列表移除 `Case/If`（单聚合内部 CASE 是过滤条件非派生组合，`count(distinct case...)`=条件去重计数；派生比率仍由多聚合 `len(aggs)>1` 或 Div/Mul 算术检查排除）；`_collect_derived_measures` 的 combined 检查同步移除 Case/If（单聚合纯包裹已由 `_projection_measures` 捕获，避免重复产出派生候选撞码）。**验证**：用户真实 ETL 解析从 1→2 度量（`current_month_active_doctor_cnt` + `last_month_active_doctor_cnt` 均 COUNT_DISTINCT + 完整 CASE expression）；test_sql_infer +2（COALESCE 包裹+CASE 条件去重不丢失 / COALESCE(COUNT(DISTINCT x),0) 纯计数包裹仍是独立聚合非派生），sql_infer+auto_fill+sql_split 相关 144 全过 + ruff 0 | 无破坏性变更（`parse_sql_profile`/`auto-suggest` 对 COALESCE 包裹条件聚合从漏检恢复为正确识别，`parsed_measures` 现返回完整度量列） | —（semantic 保持 verified；SQL 推断对「COALESCE 默认值包裹 + CASE 过滤」的工业 ETL 双度量场景从漏检修复为完整识别） |
| 2026-08-26 | — | semantic+frontend | **消费指南编辑能力 + related_metrics 血缘自动推荐 + 来源区分与缓存修复（用户需求：注册/编辑指标时如何编辑指南 + 指南页增强）**：① **编辑能力**——新增 `PUT /semantics/consumption-guide/{code}`（独立于指标状态机的轻量文档维护，对齐描述编辑：owner/admin + PDP write + row_version 乐观锁 + 写审计 + `invalidate_guide` 仅删指南键）；service `update_consumption_guide` 落 `consumption_guide` JSON + `guide_source=manual` + `guide_updated_by/at`；② **来源区分**——迁移 0093 新增 `guide_source`(enum auto/manual, default auto)/`guide_updated_by`/`guide_updated_at` 三列 + 存量回填（已有 consumption_guide 置 manual）；`get_consumption_guide` 改为 presence 判定（DB 值优先 + 补来源元数据），auto 分支返回 `guide_source=auto`；③ **related_metrics 自动推荐**——`_related_metric_codes` 复用 `LineageRepository.edges_for_node` 血缘一跳（收集另一端 `metric:` 节点、剥前缀、去重、排除自身、限量 6），替代恒空硬编码，随自动生成指南整体入 Redis 缓存；④ **缓存失效修复**——`invalidate()`/`invalidate_batch()` 顺带删 `metric:guide:{code}`（修指标更新后指南 stale gap），新增 `invalidate_guide`；⑤ **create/update 落库**——`MetricCreateRequest` 补 `consumption_guide`（结构校验：三组字符串数组 ≤20 项/每项 ≤200 字符），`create_metric` 构造器补 consumption_guide/guide_source，`update_metric` 白名单写指南时同步 manual；⑥ **前端**——消费指南页加「人工维护/自动生成」徽标 + 编辑按钮（can('metric:update')）+ 编辑弹窗（三列表可增删行 + 乐观锁保存）；MetricCreate Step2 加「消费指南（选填）」Collapse（创建透传）；MetricDetail 编辑弹窗加「消费指南」折叠区块（保存顺序=先 `updateConsumptionGuide` 成功后再 `updateMetric`，指南失败即中止无半成功）。**验证**：后端 service 12 新用例（update 成功/乐观锁 409/PDP 拒绝/非 owner/血缘推荐排除自身·去重·限量·非 metric 节点/create manual·auto 落库）+ cache 3 + 契约 5（GET/PUT 200/404/422/403），语义相关 328 全过（剩 4 项既存 dbsize/prune 失败与本次无关）+ ruff 0；前端 ConsumptionGuide 9（+2 编辑）、MetricCreate 62（+1 透传）、MetricDetail 82（+2 保存顺序/未修改不调），tsc 0（MetricCreate.test 的 `.at` 为并行 WIP）；真实 DB 迁移 0092→0093 已验证（三列就位、存量回填 0 行、27 个无指南指标 auto 正确） | `PUT /semantics/consumption-guide/{code}`（新增，_WRITE_DEPS 角色门禁 + 乐观锁）；`get_consumption_guide` 返回 `guide_source/guide_updated_at`（响应扩展）；`metric` 表新增 guide_source/guide_updated_by/guide_updated_at（迁移 0093）；消费指南缓存随指标失效（行为修复） | —（semantic 保持 verified；消费指南从「只读自动生成」升级为「人工维护/自动生成双来源 + 血缘关联推荐 + 缓存正确失效」的完整闭环） |
| 2026-08-26 | — | consume/notify/mount/conflict/subject_domain+infra | **第七轮生产就绪审查（数据一致性 + 测试/技术债 + 故障韧性）全部缺口落地**：用户要求「再次从工业/产品/技术角度审查注册指标模块是否可投产」——3 个并行调查 agent（跨服务一致性 / 测试完备性·技术债 / 故障韧性·降级）+ 亲验，发现 1 个生产级韧性缺口 + 2 个运行时拖累 + 3 个数据一致性/闭环问题 + 4 个技术债项全量修复。① **R-1 MySQL 引擎无限挂起**——`create_async_engine` 无 connect/语句超时，MySQL 挂起时全站请求无限等待；加 `connect_args={"connect_timeout":10}` + `@event listens_for connect` 注入 `SET SESSION MAX_EXECUTION_TIME=30000`（SELECT 慢查询硬上限）；② **R-2 LLM 首次故障 90s 挂起**——连接阶段 `httpx.Timeout(connect=5.0)` 快速失败 + `ConnectError` 不重试（确定性不可用直接计入熔断），用户等待 90s→5s；③ **R-3 Doris 4xx 误判引擎降级污染熔断器**——新增 `DorisSqlError`（HTTP 4xx/`code!=0` 查询错），`execute` 对 DorisSqlError 不 `record_failure`，`_do_execute` 4xx 抛 DorisSqlError、5xx 才降级，`_parse_response` `code!=0` 转 DorisSqlError；consume `_execute_with_fallback` 对 DorisSqlError 如实上抛 `QUERY_SQL_ERROR`（新增错误码）不降级 MySQL；④ **C-1 `metric.reactivated` 事件发布但无订阅**——补进 `BUSINESS_EVENT_TYPES` + 标题映射（此前恢复指标事件永不落 EventLog/扇出）；`rename_required`/`voided` 核实为 conflict API 定向通知（非死订阅，保留）；⑤ **C-2 解除挂载不同步 `metric.granularity` 冗余列**——`create_mount`/`update_mount` 回填、`delete_mount` 清空（新增 repository `update_metric_granularity`/`clear_metric_granularity`），消除详情页「旧粒度+未挂载」同页矛盾；⑥ **C-3 手动预检命中不挂 `pending_conflict`**——`/conflicts/check` 命中后给 existing 侧指标挂标记（`pending_conflict_detail.status=manual_precheck`），消除「仲裁台有 OPEN 记录、目录无标记」；⑦ **C-4 统计口径不一致**——subject_domain 指标计数/资产地图 `metric_summary.by_domain` 排除 DRAFT/DEPRECATED、维度计数/批量统计补 `deleted_at` 过滤，对齐列表活跃口径（`by_status` 保留全量分布）；⑧ **T-1 TD 检索章节按 ES 实况改写**——原「ES 仅健康探活、索引未落地」与实况相反（ES 已接线为检索主路径 + MySQL 降级），§1 与 §4.3 均改写；⑨ **T-2 observe_\* 埋点接线补断言**——新增 test_metric_observability.py 5 用例锚定 `observe_llm_call`（成功/HTTP 5xx）/`observe_metric_publish`（标准发布）/`observe_query_result`（MySQL 成功/引擎降级）真实触发；⑩ **T-3 两个已注册 cron 补测试**——`check_pending_version_timeouts`/`check_emergency_review_overdue` 各补用例（超时接受/无超时/单组异常隔离）；⑪ **T-4 删 `profile_to_dict` 死代码**（sql_infer.py，全库 0 调用）。**验证**：后端相关模块 689 全过（+T-2 5 + T-3 5 + C-2 mock 适配）、ruff 0（清理既有未用 import）；前端零改动 | `QUERY_SQL_ERROR`（新增错误码）；`/conflicts/check` 命中给 existing 侧挂 `pending_conflict`（行为收紧）；域树/资产地图指标统计排除 DRAFT/DEPRECATED（口径对齐）；`create_mount`/`delete_mount` 同步 `metric.granularity` | —（semantic 保持 verified；故障韧性（MySQL/LLM/Doris 超时降级）、事件闭环（reactivated 订阅）、数据一致性（挂载粒度/冲突标记/统计口径）、测试完备性（埋点/cron 断言、死代码清理）达到生产就绪态） |
| 2026-08-26 | — | measure_catalog+dimension+glossary+semantic+frontend | **度量目录回收站闭环 + 已删记录不可变加固（用户反馈"回收站能看到已发布的度量目录"）**：① **根因**——5 条旧电商种子（PUBLISHED）在 08-24 医疗化重置时被软删（当时删除守卫 `8c59053` 尚未引入，旧代码允许删 PUBLISHED），软删只置 `deleted_at` 不改 status → 回收站显示「已发布」；且 `_require` 不过滤 `deleted_at` → 已删记录仍可被 update/publish/submit/approve 操作复活成矛盾态（系统性缺口，维度/术语/指标同模式）；`restore_measure` 只允许恢复 DRAFT/DEPRECATED → 卡死记录既不能恢复又无其他操作。② **A·代码加固**——measure_catalog `_require` / dimension `_require` / glossary `_require_term` / metric `get_metric` 统一拒绝 `deleted_at IS NOT NULL`（`getattr` 防御式访问，报 `INVALID_STATE`「已删除…请先在回收站恢复或彻底删除」），已删记录除「恢复/彻底删除」外不可变；③ **B·彻底删除闭环**——新增 service `purge_measure` + repository `purge_measure`（Core delete 物理删除）+ API `POST /measure-catalogs/{code}/purge`（仅 platform_admin，审计 `measure_catalog.purge`，被指标引用的度量禁止彻底删除）；前端 api `purgeMeasureCatalog` + 回收站视图「彻底删除」按钮（仅平台管理员可见，Popconfirm 二次确认）；④ **C·存量清理**——实测确认 5 条旧电商种子（sales_payment_amount/sales_order_cnt/user_active_user_cnt/user_pii_user_cnt/order_domain_order_liang）硬删；先清 8 个指标 `measure_id` 引用（FK `fk_metric_measure` NO ACTION，含活跃悬空 `order_domain_gua_hao_entity_value_day` 置 NULL + 7 个已删电商测试指标），再物理删除 5 条；⑤ **顺带**——audit_i18n 补 6 个缺失动词模板（purge/reactivate/refine/cancel_job/list_databases/list_tables），`test_verb_coverage_for_all_actions` 转绿。**验证**：后端 measure_catalog 60（+7：_require 拒绝已删 update/publish/approve + purge 成功/非删/非管理员/被引用）+ 相关 473 全过、全量 3664 passed（剩 5 项既存 Redis 状态污染 feature_flags/semantic_cache，与本次无关）+ ruff 0；前端 MeasureCatalogs 14（+1 彻底删除按钮+调用断言）、全量 **985 全绿** + tsc 0；真实 DB 清理验证（8 引用清空、5 度量物理删除、回收站剩 6 条可恢复记录、0 悬空引用） | `POST /measure-catalogs/{code}/purge`（新增，仅 platform_admin）；已删记录的 update/publish/submit/approve 等操作由 `INVALID_STATE` 拒绝（行为收紧） | —（measure_catalog 从「回收站只能恢复、已删可被复活、发布态可见」升级为「回收站恢复/彻底删除闭环 + 已删记录不可变」的治理闭环态） |
| 2026-08-26 | — | semantic (SQL 批量解析)+frontend | **批量解析 LLM 模式（规则锚定 + 一次 LLM 补全 + 规范收敛，用户要求"一次性把 SQL 给 LLM 且每次输出一致准确"）**——解决「一次性给 LLM + 每次一样 + 准确」不可能三角：让 LLM 从「生成者」降级为「补全者」，随机性由算法层剥掉。① **后端 `infer_sql_batch` 新增 `use_llm`**——`MetricSqlParseRequest.use_llm`（默认 false）；`use_llm=true` 时对规则解析出的全部候选做**一次** LLM 批量补全（`_llm_annotate_candidates`：整段 SQL + 候选紧凑表打包进 1 次调用，LLM 只做封闭选择——`is_measure`（是否真度量）/ `name`（中文名润色）/ `period`（白名单周期），禁止新增/发明列；`temperature=0` + `json_object` 约束输出稳定）+ **规范收敛**（`_apply_candidate_annotations`：周期白名单 `normalize_period`（非法覆盖丢弃）、聚合不覆盖（规则已过枚举白名单防整批失败）、列名回映（`is_measure=false` 高置信度≥0.7 才移出→skipped(llm_not_measure)，低置信度保守保留）、`(statement_index,key)` 稳定排序、每候选 `llm_confidence`；4 段式编码周期覆盖仅替换末段保持业务段一致）；LLM 不可用/失败 → 保持规则候选不动绝不阻断；批级预算放宽到 `_LLM_BATCH_LIMIT_LLM=20`（规则模式 4 倍），`max_batch_statements=100` 上限保留；② **前端**——批量模式按钮区改双按钮「解析候选」（use_llm=false）/「LLM 推断并回填字段」（use_llm=true，Robot 图标，独立 loading），下方说明「枚举字段系统校验兜底，同一 SQL 每次结果一致」；成功提示区分「已用 LLM 全字段推断」；`SQL_SKIP_REASON_TEXT` 补 `llm_not_measure`；草稿持久化补 `useLlm`。**验证**：后端 test_sql_split +4（LLM 补全名称/周期/编码末段同步 + source=llm / 高置信度过滤到 skipped(llm_not_measure)+低置信度保守保留 / LLM 不可用降级规则 / 预算放宽到 6 条不封顶）、test_llm_parse +3（注解解析：key/布尔化/限长/周期归一/缺 key 丢弃）、test_sql_batch_api +1（use_llm 透传）；sql_split+batch_api+llm_parse 相关 130 全过 + semantic_service 275 全过 + ruff 0（parse.py 既有 E501 不触碰）；前端 MetricCreate 61（+1 批量 LLM 按钮携带 use_llm=true + 成功提示区分）+ tsc 0 | `POST /metric-definitions/parse-sql-batch` 请求新增 `use_llm`（bool，默认 false；LLM 模式候选 source=llm + `llm_confidence`，非度量候选进 skipped(reason=llm_not_measure)） | —（semantic 保持 verified；SQL 批量解析从「规则优先 + 失败才逐条 LLM 兜底」升级为「规则锚定 + 单次整段 LLM 封闭补全 + 规范收敛」的 LLM 模式，结果确定可复现） |
| 2026-08-26 | — | semantic (SQL 智能推断/批量注册/消费)+frontend+infra | **第六轮工业级生产就绪审查（工业 ETL 正确性 + 部署配置 + 前端一致性）全部缺口落地**：用户要求「再次从工业/产品/技术角度审查注册指标模块是否可投产」——3 个并行调查 agent（工业复杂 ETL / 前端端到端 UX / 部署·数据库性能）+ 亲验，发现 1 个生产阻塞配置 bug + 5 个 SQL 溯源缺陷 + 3 个产品能力缺口 + 若干运维项全量修复。① **P0-1 OLAP 配置脱节（生产首启必踩）**——OLAPExecutor 实际连接用 `doris_host/port`（默认 localhost:8030 容器自身），生产校验却只强制 `olap_url` 非空 → 生产设了 olap_url 仍连自身查询必失败；config 新增 `_derive_doris_from_olap_url`（从 olap_url 派生 host/port/database）+ 生产校验拒绝 doris_host=localhost/127.0.0.1 + compose 注入 `UNISENSE_DORIS_HOST/PORT/DATABASE`（backend+worker）；② **P0-2 INSERT INTO...SELECT 带列清单源表错挂**——目标表被 sqlglot 包成 `Schema`，`parent is exp.Insert` 判断失配致目标表混入 source_tables；新增 `_is_write_target`（逐层上溯 Schema→Insert/Create/Update/Merge）替换三处判断；③ **P0-3 ETL 列→物理表溯源 4 缺陷**——a) 多层嵌套 join 源表归属：`_full_alias_map` 穿透子查询别名 + `_measure_table` 解析完整别名链 + 主 FROM 表排首（`find_all` walk 顺序把 join 右表排前致无表前缀度量错挂字典表）；b) 下沉去重键 `(alias,agg)` 加 table（`t1.cnt/t2.cnt` 不再合并丢一支）；c) alias_map 扩展 `Alias(AggFunc)`→底层聚合列（`sum(amount) AS day_amt` 外层引用落 amount 而非不存在列）；d) **派生比率/条件列产出候选**（P0-3d）：`ROUND(SUM/NULLIF(COUNT))` 客单价、`SUM(CASE)/NULLIF(COUNT(*))` 退货率此前整体丢弃——`_collect_derived_measures` 收集（agg=None + derived 标记 + 完整 expression），`_projection_measures` 跳过算术/条件组合里的内嵌聚合（`_is_wrapped_aggregate` 保留 IFNULL(SUM)/ROUND(SUM) 纯格式包裹，避免内嵌聚合与独立聚合撞码/命名失败）；④ **P1-1 MetricResponse 补 raw_sql（写而不读闭合）**——批量创建已落 `Metric.raw_sql` 但响应未声明 → API 永不返回；补字段 + MetricDetail「原始口径 SQL」Card（batch_id→整句原文反查）+ types.ts；`infer_sql_batch` 候选直接携带 raw_sql（不再依赖前端从语句 meta 反查）；⑤ **P1-2 批量候选责任方选择器**——批量流此前全程无法设责任方（OwnerChain 全空）；候选行（atomic）加「产品负责」Select（ownerUsers，提交透传 product_owner_id）；⑥ **P1-3 批量通过灰度发布**——batchApproveMetrics 补 mode+gray_tenant_ids 透传（后端 schema 已接受），MetricCatalog 批量通过弹窗加「标准/灰度发布」Radio + 灰度租户输入；⑦ **P1-4 列表深分页索引**——`(deleted_at, updated_at)` 复合索引迁移 0092（消除 ORDER BY updated_at desc + 时间范围过滤 + 软删过滤的全表扫+filesort）；⑧ **P2**——llm_limit 跳过原因前端文案（此前缺键落兜底误导）；响应 GZip（parse-sql-batch 大响应压缩）；compose 注入 `UNISENSE_MINIO_*`（backend+worker，审计归档此前必败）；前端 env 命名漂移（api.ts 兼容 `VITE_API_BASE`/`VITE_API_BASE_URL`）；**批量旅程集成测试**（test_sql_batch_journey_integration：真实 MySQL 解析→注册 DRAFT→提交评审→标准发布 + batch_id/raw_sql 落库 + 域门禁）；MetricCatalog 列表页批次筛选（batch_id 过滤 + URL 同步）。**验证**：后端 test_sql_infer +6（INSERT 列清单/嵌套 join 归属/下沉同名列/CTE 派生列/派生比率列）、test_config +4（olap_url 派生/显式优先/端口/无 olap 默认 + 生产 localhost 拒绝）、test_sql_split +3（候选携带 raw_sql/派生候选/既有折行）、集成测试 2 通过；语义相关 535 全过、ruff 0；迁移 0092 在 unisense_it + 开发库验证 + 索引就位；前端 MetricCreate/Catalog/Detail 198 全过、全量 **982/983**（1 失败为 LineageView G6 既有全量干扰，单独跑 48 全过）、tsc 0 | 无破坏性变更（`batchApproveMetrics` 新增可选 mode/grayTenantIds；`MetricResponse` 新增 `raw_sql`；`parse-sql-batch` 候选新增 `raw_sql`/`derived`；OLAP 生产启动新增 Doris 地址校验） | —（semantic 保持 verified；批量注册/消费从「功能可用」升级为「工业 ETL 正确性 + 部署配置对齐 + 产品能力对等」的生产就绪态） |
| 2026-08-26 | — | semantic (SQL 智能推断)+frontend | **SQL 智能推断「信息最大化」回填增强（用户反馈"每个引导步骤选项框看不到解析值、期望信息最大化"）**——① **后端 auto-suggest period 接线**：SQL 解析出的统计周期（`time_granularity`/时间列 token）自动映射为 period 传给 auto_fill——此前注册向导 runSqlInfer 只传 domain_code+sql 不传 period，导致 metric_code 生成条件（源表+度量列+周期三齐）不满足恒为空、granularity 走规则兜底误判；显式传入 period 优先不被覆盖；② **逻辑度量推荐 `measure_suggestions`**：按度量列名与已发布逻辑度量目录（measure_catalog）的 measure_code/同义词做语义弱匹配（相等=1.0/包含=0.7），返回 top3 候选（id/code/name/format/unit/confidence/reason），供原子指标一键继承 measure_id（OneData 原子层=逻辑度量+聚合方式）；③ **前端 MetricCreate**：推断结果摘要弹窗新增「推荐逻辑度量」区块（Tag 点击一键应用）+ Step② 原子来源 measure_id 下拉 extra 展示 SQL 推断推荐 Tag（未选中时）；`applyMeasureSuggestion` 回填 measure_id + 同步 selectedMeasure 继承单位/格式/小数位，度量不在已加载 options 时补进下拉。**验证**：后端 test_auto_suggest_api +2（SQL 截月粒度接线 month 且显式 period 不被覆盖 / measure_suggestions 命中 PUBLISHED 返回候选+无匹配空数组），auto_suggest+sql_split+sql_infer 108 全过；前端 MetricCreate 60（+1 推荐展示+一键应用回填 measure_id），tsc 0 | `POST /metric-definitions/auto-suggest` 响应新增 `measure_suggestions`（只读增强）；SQL 推断场景 period 自动从解析粒度接线（请求不传时） | —（semantic 保持 verified；SQL 智能推断从「仅回填旧式物理来源、编码恒空」升级为「周期/编码正确生成 + 逻辑度量推荐一键继承」的信息最大化） |
| 2026-08-26 | — | semantic (SQL 智能推断)+frontend | **SQL 智能推断入口新增「LLM 推断」按钮（LLM 全字段推断 vs 程序规则推断）**：用户要求「SQL 智能推断入口给一个 llm 推断的按钮，让用户决定直接走 LLM 还是程序默认逻辑」。① **后端**——`MetricAutoSuggestRequest` 新增 `use_llm`（默认 false 走程序规则）；新增 `_suggest_metric_llm_overrides`（LLM 全字段推断：语义字段名称直接产出；枚举字段聚合/单位必须命中指标模型 agg_type 枚举与 unit 字典 active code 否则回退规则值——防幻觉产出非法字典 code 致创建整批失败对齐 P1-4；度量列/源表仅表达式模式覆盖且必须命中解析度量列/解析源表防幻觉错误表；表达式模式重建口径 JSON（expression/source_fields）保持一致 + 重算 metric_code 建议，SQL 模式以 SQL 为准不覆盖）；既有名称 LLM 调用抽取为 `_llm_metric_name` 帮助函数，非 LLM 模式或 LLM 全字段未产出名称时兜底；② **前端**——SQL 智能推断抽屉单条模式改双按钮「智能推断并回填字段」（程序规则）/「LLM 推断并回填字段」（use_llm=true），共用 handleSqlInfer 流程（域建议/多候选域挑选经 ref 保持用户选择的模式），LLM 按钮下方提示「枚举字段经系统校验兜底（非法自动回退规则）」；成功提示区分「已用 LLM 全字段推断」。**验证**：后端 test_auto_suggest_api +3（LLM 合法字段覆盖 source=llm + 口径重建 + 编码重算 / 非法枚举回退规则 / LLM 不可用空覆盖不阻断）、相关 277 全过、ruff 0；前端 MetricCreate +1（LLM 按钮携带 use_llm=true 并回填）、60 全过 + tsc 0 | `POST /metric-definitions/auto-suggest` 请求新增 `use_llm`（bool，默认 false；LLM 模式响应覆盖字段 source=llm） | —（semantic 保持 verified；SQL 智能推断从「仅程序规则（LLM 只命名）」升级为「用户可自选程序规则或 LLM 全字段推断，枚举白名单校验兜底」） |
| 2026-08-26 | — | semantic (注册/消费/运维)+frontend | **第五轮工业级生产就绪审查（安全加固 + 运维韧性）全部缺口落地**：用户要求「再次从工业/产品/技术角度审查注册指标模块是否可投产」——3 个并行调查 agent（安全鉴权/运维韧性·LLM 成本/注册外其它功能）+ 亲验，发现 2 个真实运维 bug + 5 项加固缺口全量修复。① **P0-1 DSD 超期治理任务「定义但从未被调度」**——`check_dsd_overdue`（semantic_tasks.py）此前只在死配置 `cron_jobs`（dict 格式无人引用），实际 worker（collector/worker.py WorkerSettings）未导入未注册 → DSD 超期 7 天自动处置/通知从未触发；补 import + `functions` + `cron_jobs` 注册（每日 3:30，健康刷新后）+ 死配置标注「修改调度请改 worker.py」；② **P0-2 指标埋点「定义但零调用」**——`observe_llm_call`/`observe_metric_publish`/`observe_query_result`（core/metrics.py）全仓库零调用点 → `/metrics` 的 `unisense_llm_call_total`/`metric_publish_total`/`query_success/failure_total` 恒 0（LLM 成本/发布量/查询成功率完全不可观测）；接通：LlmClient.chat 成功/各失败出口上报 LLM 调用、`approve_metric` 标准发布（非灰度）上报发布量、`_execute_with_fallback` 查询成功/引擎降级失败上报查询结果（best-effort 不阻断）；③ **P1-1 LLM 成本护栏补漏**——域建议 `suggest_domain` 与自定义切分 `_llm_split` 此前不计入 `_LLM_BATCH_LIMIT=5`（多域脚本逐语句建议可无限调 LLM）；`suggest_domain`/`split_sql_statements` 加 `llm_budget` 参数贯穿 `infer_sql_batch`（切分/域建议/度量提取/周期推断/逐语句域建议全部计入共享预算）；单条 `/conflicts/check` 加批级预算（limit=10 对齐批量创建，耗尽降级纯词法）+ 限流规则并入 LLM 严格档（60s/20 次）；④ **P1-2 parse-sql-batch 无限流特化/无上限**——限流规则并入 LLM 严格档 + `infer_sql_batch` 语句数上限（100，超限 `SQL_BATCH_TOO_MANY_STATEMENTS`）+ 候选数上限（200，`SQL_BATCH_TOO_MANY_CANDIDATES`）生产护栏；⑤ **P1-3 挂载实体「只写不可见不可管」**——metric_mount 后端 CRUD 完整但前端无任何页面读取/展示；MetricDetail 新增「挂载实体（OneData 挂载层）」Card（物理表/粒度/默认周期/域 + 解除挂载带确认，`listMetricMounts`/`deleteMetricMount` 接线）；⑥ **P2-1 指标批量恢复缺失**——对齐维度/逻辑度量/术语「批量重新启用」：后端 `reactivate_metric`（DEPRECATED→DRAFT，清空替代/废弃时间/日落期 + `metric.reactivated` 事件）+ `/{code}/reactivate` 单条 + `/batch-reactivate` 批量端点；前端 MetricCatalog 批量菜单加「批量恢复（已废弃）」+ 确认弹窗/失败重试/权限（metric:deprecate）；⑦ **P2-2 采集失败原始异常入库可读**——`fail_collection_run` 原始异常文本（可能含 DB 连接串/账号）经 `_sanitize_conn_error` 脱敏 + 截 512 再落库（GET /collection-runs 任意登录可读防凭据泄露）；⑧ **P2-3 custom 切分正则 ReDoS**——`_safe_custom_regex` 校验（长度≤200/嵌套量词与 alternation 灾难性回溯检测/可编译性），`_split_custom` delimiters 与 `_split_by_start_markers` 危险/非法正则跳过。**验证**：后端 +10 测试（reactivate 3/API 2/语句数上限 1/ReDoS 2/域建议预算 2），semantic+sql_split+domain_suggest+conflict+consume+middleware 相关 542 全过、ruff 0（既有 E501 不触碰）；worker.py 验证 dsd-overdue 已注册（15 cron）；前端 MetricDetail 80（+2 挂载展示/解除）、MetricCatalog 58（+1 批量恢复）、全量 **981/981 全过** + tsc 0 | 无（`POST /metric-definitions/{code}/reactivate` + `/batch-reactivate` 新增；`infer_sql_batch` 超限抛 `SQL_BATCH_TOO_MANY_STATEMENTS/CANDIDATES`；`parse-sql-batch`/`conflicts/check` 限流并入 LLM 严格档 60s/20 次；`fail_collection_run` 错误落库脱敏） | —（semantic 保持 verified；批量注册与冲突检测从「功能可用」升级为「运维 bug 修复 + 成本护栏补漏 + 挂载可见可管 + 批量恢复闭环 + 异常脱敏/ReDoS 防护」的生产就绪态） |
| 2026-08-26 | — | semantic (metric 三层口径)+frontend | **指标三层口径 LLM 增强（AI 生成/丰富/优化业务口径、伪代码口径、数仓SQL口径）**：用户要求「编辑弹窗与注册页对业务口径/伪代码口径/数仓口径支持 LLM 能力」。新增后端端点 `POST /metric-definitions/refine-definition`（`MetricRefineDefinitionRequest`：field=business/pseudo/dw × action=enrich/generate/optimize + 指标上下文；`_REFINE_DEPS` 写角色 + SQL 字段豁免注入扫描；LLM 只生成文本回填、不落库不创建版本；复用 `LlmConfigService.build_client`（DB 配置优先+路由/熔断）+ `strip_code_fence` 解析，LLM 不可用/空内容 → `LLM_INFER_UNAVAILABLE`；审计 action `metric_definition.refine`）。前端编辑弹窗（MetricDetail）与注册向导（MetricCreate Step②）三层口径输入框各加 AI 按钮：空值「AI 生成」（generate）、业务口径有值「AI 丰富增强」（enrich）、伪代码/数仓有值「AI 优化」（optimize）；回填置 dirty（可保存合入/清空移除语义同既有），`refineMetricDefinition` api 封装。**验证**：后端 test_metrics_infer_api +4（业务口径 enrich 成功+上下文入 prompt / LLM 禁用 400 / 空内容 400 / 非法 field 422），相关 16 全过、ruff 0（仅既有 E501）；前端 MetricDetail 78（+2：AI 丰富增强回填+dirty 合入 / LLM 不可用提示不回填）、MetricCreate 58（+1：AI 丰富增强回填+载荷断言）全过 + tsc 0 | `POST /metric-definitions/refine-definition`（新增；请求带 SQL 字段豁免注入扫描；响应 `{content, source}` 统一信封） | —（semantic 保持 verified；指标三层口径从「仅人工录入」升级为「AI 生成/丰富增强/优化全支持」） |
| 2026-08-26 | — | semantic (SQL 智能推断) | **SQL 智能推断方言覆盖第二轮审查（21 种方言聚合识别补齐）**：用户要求「再审查看还有哪些方言解析场景没有覆盖」。系统性实测 8+ 种方言 39 个聚合场景，发现两类缺口并全量修复：① **A 类·默认方言识别为 AggFunc 子类但 key 未映射 → 产出非法枚举 → 批量创建 pydantic 整批失败（P1-4 同类）**——Spark/Hive `first()/last()`（First/Last 类）→ `FIRST_VALUE/LAST_VALUE`（此前非法 FIRST/LAST）；PG `array_agg`/`bool_and`/`bool_or` → COUNT（此前非法 ARRAYAGG/LOGICALAND/LOGICALOR）；BQ `ANY_VALUE`/CH `any`/Trino `arbitrary` → COUNT（非法 ANYVALUE）；Snow `APPROX_TOP_K` → COUNT（非法 APPROXTOPK）；② **B 类·hint 未命中 → 不触发方言择优 → 方言聚合降级 measures=0 推断退化**——CH `maxIf/minIf/argMaxIf/argMinIf`（CombinedAggFunc）→ MAX/MIN（此前 `_COMBINED_AGG_MAP` 无映射兜底 SUM 语义错误）、`sumWithOverflow`→SUM、`avgWeighted`→AVG、`groupUniqArray`/`anyLast`/`histogram`→COUNT；Snow/Spark/Trino `approx_percentile`→PERCENTILE；T-SQL `COUNT_BIG`、Spark `collect_list/collect_set`、Snow `LISTAGG`→COUNT——**关键修正**：hint 正则函数名带下划线（`count_big`/`collect_list`/`collect_set`）必须用真实下划线形式，此前写成无下划线导致默认方言成功解析（降级非 AggFunc）时 hint 不命中 → 不触发择优；③ **诚实降级边界确认**——统计聚合 `STDEV/VAR/STD/STDDEV_POP`（Stddev/Variance 类）正确 skip（空画像不产出非法）；sqlglot 各方言均不识别的罕见函数（`medianExact`/`map_agg`/`wm_concat`/`approx_top_count`）诚实降级空画像。**验证**：test_sql_infer +9（first/last 归一、array/bool/any/topk 归一、CH 条件极值、溢出/加权、approx_percentile+arbitrary、collect/count_big、listagg/groupuniq、统计聚合诚实跳过、未知聚合降级空），43 全过 + auto_fill/semantic 相关 340 全过 + ruff 0 | 无（解析器内部增强，`parse_sql_profile`/`auto-suggest`/`parse-sql-batch` 接口契约不变；方言聚合推断从「部分非法枚举致批量创建失败」升级为「全部可归一到注册枚举或诚实跳过」） | —（semantic 保持 verified；SQL 智能推断方言覆盖从「8 种主流方言实测」升级为「21 种方言聚合函数名全覆盖归一 + hint 触发择优修正 + 统计聚合诚实跳过」） |
| 2026-08-26 | — | semantic (SQL 智能推断) | **SQL 智能推断方言覆盖实测 + 3 处方言解析缺陷修复**：用户问「其他方言的 SQL 是否也能正确解析」。实测 8 种方言（Hive/Spark/ClickHouse/Doris/Oracle/MySQL/Postgres/Trino）发现：① **高·多语句方言 ETL 解析为空**——`_parse_profile_ast` 多语句拆分只用默认方言 `parse`，而默认方言对 `create table ... comment "中文"` 列注释抛 ParseError（整个多语句分支被吞）；方言择优 `_best_dialect_ast` 用 `parse_one` 遍历方言只取首条（Set）→ 两条路径全失效 → 真实用户 ETL SQL（set + 中文 comment DDL + insert overwrite）整段解析为空画像（用户上一轮反馈的"原子+无字段"真实根因，实测复现）。修复：`_best_dialect_ast` 改用 `sqlglot.parse`（复数）拆分方言语句、遍历选「含 Select 且聚合识别最多」的产出语句（与多语句语义对齐，覆盖默认方言 parse 失败的方言 DDL 兜底）；② **Postgres/Trino/Oracle `GROUP BY 1, 2` 位置序号解析不出列名**（sqlglot 解析为 Literal 数字非 Column）→ `_extract_group_by` 按投影下标回映列名；③ **Trino `approx_percentile` 归一为非法枚举 `APPROXQUANTILE`** → `_DIALECT_AGG_MAP` 补 `approxquantile→PERCENTILE`。**验证**：test_sql_infer +3（多语句中文 comment ETL 双度量+源表+粒度 / 位置序号 group_by / approx_percentile 归一），相关 348 全过、ruff 0；实测 8 种方言全部正确解析（度量+源表+粒度） | 无（解析器内部增强，`parse_sql_profile`/`auto-suggest` 接口契约不变） | —（semantic 保持 verified；SQL 智能推断从「默认方言成功才可靠」升级为「多语句方言 ETL + 位置序号分组 + 方言统计聚合归一」全覆盖） |
| 2026-08-26 | — | semantic (SQL 智能推断)+frontend | **SQL 智能推断多语句脚本解析为空修复 + 摘要展示解析出的度量列清单让用户确认识别成功**：用户反馈「注册指标 SQL 智能推断推断出原子、且不展示解析出的字段，无法确认是否真正识别成功」（真实 ETL SQL：`set` 参数 + `create table ... stored as orc` + `insert overwrite ... select` 双度量脚本）。根因：`_parse_profile_ast` 用 `sqlglot.parse_one` 只取**首条语句**（set/create DDL 场景返回 Set，无 Select 子树）→ 画像空（measures/source_tables/granularity 全空）→ 只出无字段的原子兜底。修复：① **解析器多语句拆分**——`_parse_profile_ast` 改用 `sqlglot.parse`（复数）拆分全部语句，选「含 Select 且聚合识别最多」的产出语句（度量 SELECT 常在脚本最末的 INSERT 中，与 sql_split 批量切分语义对齐）；用户 SQL 实测双度量 + 源表 `doctor_visit_agent_info_da` + 粒度 month 全部正确识别；② **auto-suggest 响应新增 `parsed_measures`**——把 SQL 解析出的每个度量列（`column`/`agg`/`alias`/`table`/`expression` 原始表达式）返回前端；③ **前端推断结果摘要 Modal 新增「SQL 解析出的度量列」区块**——每个度量展示 列名 + 聚合方式 Tag + 来源表 + 原始表达式，多度量时提示「当前回填首个为原子指标，如需分别创建请用批量解析模式」；成功提示带识别数量。**验证**：后端 test_sql_infer 31 全过（无回归）+ test_auto_suggest_api +1（多语句 parsed_measures 双度量断言）；前端 MetricCreate 55（+1 摘要度量列展示）；全量 971/972（DataGovernance 1 项为并行执行超时 flaky，单独 17 全过）+ tsc 0；**真实 API 实测**（容器内）——用户 SQL 调 /auto-suggest 返回 `parsed_measures` 两项（current_month_active_doctor_cnt / last_month_active_doctor_cnt 均 COUNT_DISTINCT + 原始表达式），源表正确 | `POST /metric-definitions/auto-suggest` 响应新增 `parsed_measures`（只读增强，兼容字段） | —（semantic 保持 verified；SQL 智能推断从「多语句脚本黑盒空推断」升级为「真实 ETL 脚本可识别 + 解析出的每个度量列/聚合方式可见可确认」） |
| 2026-08-26 | — | semantic (批量注册)+frontend | **批量注册第四轮工业级审查（生产就绪加固）6 项缺口全落地**：① **OneData 接线**——SQL 批量候选行加「关联逻辑度量」选择器（复用单条 Step② 度量目录），提交透传 measure_id（此前批量原子全部 `measure_id=NULL` 游离逻辑度量体系走旧式物理来源）；解析层候选补 measure_id 契约键；② **通知闭环**——批量创建成功定向通知创建者（含候选 product_owner）「已创建 N 个 DRAFT 请送审」；`submit_metric` 补提交人「已提交评审」回执（此前 to_reviewers/指定评审人分支均排除提交人，提交人无回执）；③ **跨域权限预检**——前端 domain_admin/metric_owner 选非本域 → 提交前拦截（后端整批 FORBIDDEN 前置，避免提交后整批失败零创建）；④ **口径溯源**——`metric.raw_sql` 列（迁移 **0091**）+ `MetricCreateRequest`/`SqlBatchCreateCandidate.raw_sql` + 创建/批量透传（候选无 raw_sql 时前端从语句 meta 按 statement_index 提取原文）；候选行 Tooltip 展示口径 expression——batch_id 可反查整句原始 SQL；⑤ **DRAFT 超期治理**——TodoCenter 草稿创建超 30 天标记「已超期」；⑥ **审核页批次筛选**——`MetricListParams.batch_id` 精确过滤（repository 等值条件）+ MetricReview 筛选栏批次输入（URL 同步 + load 依赖补 batchId）。**验证**：后端 test_semantic_service +4（raw_sql 透传/通知创建者/全败不通知/既有指定评审人回执适配）+ test_semantic_repository +1（batch_id 过滤），相关 357+167 全过、ruff 0（仅既有 E501）；迁移 0091 在 unisense_it 与开发库均 upgrade 验证；前端 MetricCreate 57（+2：OneData 接线透传 measure_id+raw_sql / 跨域拦截）、MetricReview 21（+1 批次筛选）、TodoCenter 11（+1 超期标记）+ 全量 976/976 + tsc 0 | `GET /metric-definitions` 新增 `batch_id` 精确过滤（可空）；`SqlBatchCreateCandidate` 增 `raw_sql`（可空）；`MetricCreateRequest` 增 `raw_sql`（可空）；`metric` 表加 `raw_sql` 列（0091）；`parse-sql-batch` 候选增 `measure_id`（恒空契约键） | —（semantic 保持 verified；批量注册从「候选可创建」升级为「批量原子可关联逻辑度量、口径可溯源、创建/送审有通知闭环、跨域有前端预检、草稿有超期治理、审核可按批次收敛」） |
| 2026-08-26 | — | semantic (metric)+frontend | **指标口径完整三层展示 + 业务口径录入闭环（用户反馈"详情页没展示技术口径/数仓SQL口径"）**——产品文档 §2.2 三层口径（业务口径=口径定义 / 技术口径=源业务库口径 SQL / 数仓SQL口径=数仓落地口径）此前展示层分散且标签语义不对齐、数据层 27 指标 0 个填业务口径。修复：① **展示层三层统一**——`display.tsx` `DEF_FIELD_LABEL` 新增 `definition→业务口径`、`sql/etl_sql→技术口径（源业务库口径）`、`dw_definition→数仓SQL口径`（`DEF_CANON_ORDER` 补 definition 至首位，AssetMap 对比/通用口径渲染全局跟随）；MetricDetail DefinitionCard、MetricCatalog 展开区、资产地图 MetricDetailDrawer、MetricReview 评审弹窗四处口径展示统一三层标签；**空态引导**——业务口径/数仓SQL口径为空时显示「未填写（可在编辑弹窗补填）」灰色占位而非隐藏，让用户知道该维度存在（此前空字段条件渲染隐藏致"没展示"错觉）；② **录入层业务口径闭环**——注册向导（MetricCreate Step②）与编辑弹窗（MetricDetail）新增独立「业务口径」输入框 → `definition_json.definition`（dirty 语义：编辑弹窗改过合入/清空移除，与伪代码/数仓口径同模式），SQL 模式与表达式模式均合入；③ 存量数据现状：27 指标 0 个有 definition、22 个有 sql、1 个有 dw_definition——三层标签 + 空态让用户一眼看出哪些维度缺失待补。**验证**：MetricDetail 76（+2：详情页三层口径展示+数仓空态/业务口径回填+修改合入+清空移除）、MetricCreate 53（+1 业务口径输入区，改写提交合入断言为三层口径）、MetricCatalog 56（改标签断言）、MetricReview 20、AssetMap 59 全绿；tsc 0 | 无（纯前端展示/录入增强，definition_json.definition 为既有字段，无接口变更） | —（semantic 保持 verified，指标口径从"只展示口径 SQL"升级为"业务/技术/数仓SQL三层完整展示 + 业务口径可录入补全"） |
| 2026-08-25 | — | metric (语义 API) | **唯一遗留低危项修复：单条创建/模板实例化并发竞态 IntegrityError → 500**——第三轮工业级审查唯一遗留项（单条创建路径 metrics.py:232 不捕 IntegrityError）。现状梳理：`MetricRepository.create`/`create_version` 已在 flush 阶段捕获唯一键冲突转 ConflictError（409）；但端点层 `db.commit()` 无兜底——血缘/冲突等延迟 flush 对象（`_register_metric_l3_lineage` 等 best-effort 路径 add 的对象在 commit 才 flush）的唯一键冲突会裸抛 IntegrityError → 500。修复（对齐 semantic.py 模板创建端点先例）：① `metrics.py` 普通指标创建端点 commit 加 `except IntegrityError → rollback + ConflictError(METRIC_CODE_EXISTS)`；② `semantic.py` 模板实例化端点 commit 同样兜底。**验证**：+2 测试（普通创建 commit 唯一键冲突→409 且 rollback 调用 / 模板实例化同样），相关 330 全过、ruff 0 | 无（内部异常映射，接口契约不变：竞态下从 500 变 409 METRIC_CODE_EXISTS） | —（semantic 保持 verified；修复并发竞态下指标创建/模板实例化的 500 隐患） |
| 2026-08-25 | — | semantic (SQL 智能推断)+frontend | **批量注册第三轮工业级审查（新维度）发现的全部缺口修复**：用户要求「再次从工业实际生产场景/产品/技术角度审查批量注册是否可投产」——3 个并行调查 agent（工业 ETL 极端场景/事务·并发·幂等/产品闭环·生命周期）+ 亲自实测复现，发现「口径错误落库」等更严重一类问题并全量修复。① **P0-A 方言聚合硬崩溃**——ClickHouse `uniqExact/topK/quantile`（AnonymousAggFunc/ParameterizedAgg 的 `this` 是函数名字符串）使 `_extract_col_name` 对 str 调 `.walk()` 抛 AttributeError → 整批 500（工业 DAU/UV 常用函数必炸）；修复 `_extract_col_name` str 兜底 + `_projection_measures` 方言聚合取 `expressions` 首 Column + `infer_sql_batch` 单语句画像 try/except 降级 skipped（任何单句异常不炸整批）；**方言聚合归一一枚举**（uniqExact→COUNT_DISTINCT、quantile→PERCENTILE、topK→COUNT，corr/stddev/var 等无枚举可归一 → 跳过不产出非法候选，注册不再 pydantic 整批失败）；② **P0-B 无别名裸聚合不识别**——`SELECT sum(amount)`（投影非 Alias/Column）此前被跳过 → measures=0（ETL 最普遍写法规则层失效）；修复裸 AggFunc 投影直接识别（sqlglot 生成 `_col_N` 别名）；③ **A-1/2 口径错误落库（最危险）**——`sum(case when status='paid' then amount end)` 落库简化 `SUM(amount)`（CASE 条件丢失）、窗口函数丢 OVER 语义（注册后变全表聚合，数据错误）；修复顶层投影也走 enrich 带**原始 expression SQL 片段**（`target.sql()`，方言序列化异常降级简化式）+ 候选 `needs_review` 标识（前端「口径需核对」Tag）；④ **A-3 join 同名列混淆**——`sum(a.amount)/sum(b.amount)` 两候选都取首个物理表（b 侧口径错挂 a 表）；新增 `_table_alias_map`/`_measure_table` 按列前缀归属物理表；⑤ **A-4 子查询/CTE 别名列错挂**——`sum(x) from (select amount x)` source_fields 落 `[orders, x]`（物理表+不存在的列）；`_extract_measures` 全局构建子查询投影别名→物理列映射（顶层/下沉都解析）；⑥ **P0-C batch_id 不落库（批次瞬时化）**——`MetricCreateRequest` 无 batch_id（pydantic extra=ignore 丢弃，宽表批量传了也被吞）；补 schema 字段 + `Metric` 构造透传 + SQL 批量 Phase1/Phase2 传 batch_id + `MetricResponse.batch_id`；前端列表「批量」Tag（Tooltip 批次 ID）+ 详情「来源批次」+ 审核页「批量」Tag；⑦ **高：冲突预检 LLM 无批级上限**——批量 N 候选每个 use_llm=True 补位可触发数百上千次 LLM 调用；`_detect_and_mark_conflicts`/`create_metric` 加 `_conflict_llm_budget`（批量注册共享 `{used, limit=10}`，超限降级纯词法）；⑧ **高：原子候选 OwnerChain 丢弃**——Phase1 原子此前不传三方责任（P0-2 只补了复合），原子指标责任链空；补原子透传 product/tech/dw_developer；⑨ **中：注册类审计无失败明细 + 恒记成功**——`batch_register`/`sql_batch_register` 审计补 ok_count/failed_count/failed_codes，action 区分 full/partial/failed；⑩ **中：run_batch DB 级异常结果失真**——整会话回滚后已 ok 项实际未落库但仍报成功（响应/审计宣称成功、库中未落）；修复回滚后已执行项全部改标失败；⑪ **中：解析结果不持久化**——候选存 React state 刷新即丢；localStorage 草稿（`unisense.sql-batch.draft`，24h 有效）解析成功即存、重新进入恢复继续勾选创建、创建完成后清除；⑫ **中：1MB 输入无友好拦截**——前端超 65536 字符直接友好提示分拆（此前透传后端 422 技术错误）。**验证**：后端 test_sql_infer +6（方言聚合不崩溃归一/裸聚合/CASE 口径/窗口语义/join 同名列归属/子查询别名解析）+ test_sql_split +1（单语句画像异常降级）+ test_semantic_service +3（batch_id 落库/原子 OwnerChain/批级 LLM 预算共享）+ test_metric_batch_api 更新（run_batch 失真）；相关全过、ruff 0；前端 MetricCreate 54（+2：口径需核对 Tag/草稿持久化恢复）+ 全量 969（LineageView 1 项为既有测试间干扰，单独跑 48 全过）+ tsc 0 | 无（SqlBatchCreateCandidate 无新必填字段；parse-sql-batch 候选新增 needs_review；MetricCreateRequest/MetricResponse 增 batch_id（可空）；批量注册审计 action 区分 full/partial/failed + detail 增 ok/failed 计数） | —（semantic 保持 verified，批量注册从「候选能出来、能创建、不炸批」升级为「口径不错误落库、批次可回溯、LLM 有预算、审计可追溯」的生产就绪态） |
| 2026-08-25 | — | semantic (批量注册)+frontend | **批量注册 SQL 路径生产就绪加固（工业级审查发现 6 项全落地）**：用户要求「从工业实际生产场景/产品/技术角度再审查批量注册指标是否可投入生产」——发现 1 个性能缺陷、1 个数据质量缺口、2 处批量能力不对等、2 处体验缺口并全量修复。① **P0-1 冲突预检 O(N²) 修复**——`batch_register_from_sql` 此前每候选 create_metric 都调 `load_conflict_existing` 全量加载（N 候选 = N 次全量比对，service.py:608-609 注释声明的「批量注册传预加载 existing」SQL 路径漏接线；宽表批量已复用），修复后循环前预加载一次 + 逐候选成功后增量追加（保持候选间互相冲突检测），对齐宽表批量 L3 模式；② **P0-2 复合指标责任方/单位补齐**——Phase2 复合候选此前只传 aggregation/definition_json/period/granularity，责任方三角缺失、单位取默认（详情页 OwnerChain 不完整）；`SqlBatchCreateCandidate` 增三方责任 id/name 字段，`batch_register_from_sql` 透传 + unit，前端候选类型/提交同步；③ **P1-1 SQL 批量结果面板闭环**——补「重试失败项（仅重跑失败候选，防全量重跑把已建 DRAFT 再判冲突）」+「批量提交评审（原子 DRAFT 一键送审，复合排除——依赖未发布会被拦截）」+ 创建后 Alert 送审引导，对齐宽表批量弹窗；④ **P1-2 LLM 批级限额**——`infer_sql_batch` 新增 `_LLM_BATCH_LIMIT=5` 批级计数（度量提取+周期推断共用），超限语句降级 skipped(llm_limit)（前端「已达 AI 兜底上限」提示），防多语句脚本逐条调 LLM 打满配额/拖慢解析；⑤ **P2-1 大批量渲染**——候选 Collapse 加 maxHeight+滚动、结果 Table 加 scroll 防 100+ 候选 DOM 卡顿；⑥ **P2-2 AI 推断标识**——LLM 兜底候选带 `source=llm`，前端候选行加「AI 推断」复核 Tag（与规则层可靠产出视觉区分，提示编码/名称/聚合/周期人工复核）。**验证**：后端 test_semantic_service +2（SQL 批量预加载 1 次/复合责任方+unit 透传）、test_sql_split +1（LLM 批级限额封顶 5 次超限 llm_limit）+1（LLM 兜底候选 source=llm）；相关 373 全过、ruff 0（既有 E501 不触碰）；前端 MetricCreate 52（+3：失败项重试仅重跑失败候选/原子 DRAFT 送审排除复合/AI 推断 Tag）+ 全量 **968/968 全过** + tsc 0 | 无（POST /batch-register-from-sql 内部复用预加载 existing 性能优化；SqlBatchCreateCandidate 增责任方字段；parse-sql-batch 候选新增 source 字段；infer_sql_batch 超限语句 skipped.reason=llm_limit） | —（semantic 保持 verified，批量注册生产就绪度强化：性能 O(N²)→O(N)、复合责任链完整、失败重试+一键送审闭环、LLM 限额防配额打满、AI 推断可复核） |
| 2026-08-25 | — | semantic (冲突检测) | **创建指标冲突比对补齐三缺口（术语同义词接入 + 口径双字段进比对 + 创建路径启用 LLM 补位）**：用户要求系统性审查「创建时如何与存量指标比对/如何检测冲突/是否覆盖实际生产场景」，发现 3 个真实缺口并全量修复。① **缺口1 术语同义词接入**——`similarity.py` 注释承诺「度量目录/术语同义词并入 name 比对」但实现只接度量目录：现在 `load_conflict_existing`（存量侧）批量 IN 查 `Term.synonyms`（term_id→术语），`_detect_and_mark_conflicts`（候选侧/更新口径 P2-I 场景）按 `metric.term_id` 并入——术语表登记的「门诊挂号人次=门诊挂号人数」等价在创建/更新冲突检测中生效，杜绝用不同叫法的指标漏检重复建设；② **缺口2 口径双字段进比对**——`_DEFINITION_FEATURE_KEYS` 由 6 要素扩到 9 要素：新增 `pseudo_definition`（伪代码口径）/`dw_definition`（数仓详细口径）/`downstream_tables`（下游使用表），数仓实现口径不同而主体 expression 相同不再误判同义（数仓口径冲突敏感）；`source_fields`（上游字段）**刻意不加入**——它是口径主体（expression）蕴含的冗余声明，加入会因「声明完整度不同」制造噪音、误判两个口径主体相同的指标为不同口径（测试实证：候选带 source_fields、存量不带→def_sim 被拉低→SAME_DEF_DIFF_NAME 漏检）；③ **缺口3 创建路径启用 LLM 补位**——`_detect_and_mark_conflicts` 的 `check(use_llm=False)` 改 `use_llm=True`：词法未达软冲突阈值但落入补位触发区（0.45≤def_sim<0.85）的同义异名/表述差异大口径，创建时也交 LLM 判定语义同义（对齐人工预检 /conflicts/check），LLM 异常由 check 内部降级为词法判定（best-effort 不阻断创建）。**验证**：后端 test_similarity +4（dw_definition 不同不判同义/相同仍命中/伪代码不同不判同义/下游表不同不判同义）、test_semantic_service +2（存量侧术语同义词并入/候选侧术语并入+use_llm=True）；similarity+conflict+semantic 相关 353 全过、ruff 0；全量单元测试见下方 | 无（纯检测逻辑增强；创建/更新路径从词法判定升级为「词法+LLM 补位」，已绑术语指标参与同义词等价比对） | —（semantic 保持 verified，冲突比对覆盖术语同义词/口径双字段/LLM 语义补位全场景） |
| 2026-08-25 | — | semantic (SQL 智能推断)+frontend | **批量解析审查缺陷全量修复（P0-1~P2-10 闭环）**：针对「SQL 智能推断前后端/跨服务一致性审查」发现的 11 项缺口全量落地。① **P0-1 多域/无域候选非法编码**——`_build_atomic_candidate` 域未定时不再 bake-in 首段为空的非法编码（`_order_amount_day`，此前批量创建 pydantic 校验整批 500），改由前端 `handleSqlBatchCreate` 按最终 selectedDomain 生成 4 段编码（resolveCode）；候选 UI 编码为空显示「（选域后自动生成）」；② **P0 兜底**——`batch_register_from_sql` 补捕获 `pydantic.ValidationError`（Phase1/Phase2 逐条标记 VALIDATION_ERROR「候选参数校验失败」，非法候选不再整批 500）；③ **P0-2 period 归一化**——`parse_sql_measures_result` 与 `parse_period_infer_result` 共用 `normalize_period` 白名单+别名（月度/月均/daily 归一，非法周期丢弃字段不污染编码）；④ **P1-3 复合候选实际周期**——`_build_composite_candidate` 编码/粒度用语句实际 period（不再硬编码 `_day`/day），Phase2 注册复合传 period/granularity；⑤ **P1-4 聚合枚举一致**——`_agg_display_name` 归一 `FIRSTVALUE→FIRST_VALUE`/`LASTVALUE→LAST_VALUE`，注册 Literal（MetricCreateRequest/SqlBatchCreateCandidate/模板）×前端 AGG_OPTIONS 同步 10 值（此前 FIRST_VALUE 推断产出无法落库致整批失败）；⑥ **P1-5 granularity 落库**——`SqlBatchCreateCandidate` 增 granularity 字段，Phase1 创建原子传 granularity，前端候选类型/提交同步；⑦ **P2-7 标注死代码**——`split_select_measures` 标注「当前无端点引用（infer_sql_batch 用 _build_atomic_candidate）」；⑧ **P2-8 前端暴露切分模式**——`runSqlBatchParse` 不再硬编码 statement，新增切分模式选择（语义/分号/自定义规则，解析前可配置）+ custom 规则输入（delimiters/start_markers 逗号分隔正则）；⑨ **P2-9 候选行 period 可编辑**——周期 Tag 改 Select 行内微调（P1-5 的 granularity 随候选提交）；⑩ **P2-10 逐语句域建议**——整段域建议为 multiple/none 时逐语句反查域，语句 meta 加 `suggested_domain`、候选加 `suggested_domain_code`，前端跨域提示「建议域 X 与当前域不一致」。**验证**：后端 test_sql_split +3（多域不 bake-in 非法编码+逐语句建议域/复合月粒度/空域单测）、test_llm_parse +1（period 归一化含 月度/daily/weekly/非法丢弃）、test_sql_infer +1（FIRST_VALUE/LAST_VALUE 归一）、test_semantic_service +1（pydantic ValidationError 逐条捕获不整批 500）；相关 368+166 全过、ruff 0（既有 E501 不触碰）；前端 MetricCreate 49（+4：切分模式 custom 携带规则/空编码按域生成+granularity 提交/period 行内编辑/语句级建议域提示）+ tsc 0 + 全量 965/965 全过；实证：P0-1 空域 metric_code=None、P1-3 月粒度复合 `sales_order_amountuserid_month`（granularity=month） | 无（parse-sql-batch 候选新增 suggested_domain_code；SqlBatchCreateCandidate 增 granularity；POST /batch-register-from-sql 非法候选从整批 500 变为逐条 VALIDATION_ERROR） | —（semantic 保持 verified，SQL 智能推断前后端/跨服务一致性全量收口） |
| 2026-08-25 | — | frontend (指标审批+详情) | **变更上下文扩展到指标详情页——状态机细分引导（REVIEW 新增/变更/破坏性/重评审 + 前后对比，EXPERIMENTAL 灰度说明、DRAFT 草稿提示、PUBLISHED 变更后口径提示）**：用户指出「前面的指标审批增强方案验证结论对应的问题还没解决掉」——此前变更上下文仅接入审批页（MetricReview 评审记录弹窗 + 确认弹窗），指标详情页（MetricDetail）REVIEW 状态仍只有通用 Alert，看不到新增/变更/前后对比。修复：① **提取共享组件 `ChangeContext.tsx`**——`buildChangeInfo`/`MetricDiffView`/`ReviewChangeSummary`/`changeVersionText` 从 MetricReview 内聚到 `pages/metric/ChangeContext.tsx` 并导出，MetricReview 改导入（行为不变），详情页复用；② **MetricDetail 状态机细分引导**——REVIEW：`ReviewStatusContext` 展示变更类型 Tag（新增/变更 v{prev}→v{cur}/破坏性/废弃恢复重评审）+ 说明 + 「变更前后对比」Card（MetricDiffView，仅非新增且有 diff 时渲染）；EXPERIMENTAL：灰度说明（可全量发布/回滚）；DRAFT：草稿提示（评审通过后方可消费）；PUBLISHED：`PublishedChangeContext` 若经历过变更（非首次创建）轻量提示「当前口径为「变更指标」v{prev}→v{cur}，已生效」；③ **changeVersionText 优化**——prev===cur（回看已发布版本）时不显示 v2→v2，仅展示 v{cur}。**验证**：MetricDetail 75（+8：REVIEW 新增无对比/变更 v1→v2 前后值/破坏性 v2/重评审/EXPERIMENTAL 灰度/DRAFT 草稿/PUBLISHED 变更提示/首次创建不提示）、MetricReview 20（提取无回归）、前端全量 961 全过、tsc 0 | 无（纯前端展示扩展，无接口变更） | —（semantic 保持 verified，审批/详情按状态机一眼看出新增还是变更 + 差异对照） |
| 2026-08-25 | — | semantic (metric)+frontend | **指标删除权限放开至平台/域管理员或原 Owner（草稿/废弃可删，对齐维度/度量决策）**：用户问「指标创建者且指标为非发布状态，也不能由创建者删除吗」——此前指标删除是全模块最严（API `require_roles("platform_admin")` 仅平台管理员，且与同模块 restore「管理员或原 Owner」不对称、与维度/度量的「草稿/废弃可由管理员或生产者软删」决策不一致）。修复：① **后端 `delete_metric`**——状态范围由「仅 DRAFT」扩为 **DRAFT + DEPRECATED**（REVIEW/PUBLISHED 仍 `INVALID_STATE` 拒绝），权限校验 `role not in (platform_admin, domain_admin) and owner_id != actor_id → FORBIDDEN`（对齐 restore/维度/度量模式）；`DELETE /{metric_code}` 门禁由 `require_roles("platform_admin")` 改为 `_WRITE_DEPS`（写角色），service 层做管理员/原 Owner 强校验；② **权限基线**——`metric:delete` 权限点加入 `ROLE_UI_ACTIONS` 的 domain_admin 与 metric_owner 默认集（此前仅 platform_admin），描述更新；③ **前端 MetricCatalog**——新增 `canDeleteMetric`（DRAFT/DEPRECATED + 平台/域管理员或 owner_id===currentUserId），单条删除按钮/批量删除禁用/占位文案/批量确认文案全部对齐新权限；producer（指标创建者）默认列补 `rowActions`（此前 producer 看不到操作列，创建者永远找不到删除入口）；④ **前端 MetricDetail**——删除按钮条件改为 `(DRAFT||DEPRECATED) && canDelete && (isAdmin || owner_id===currentUser.id)`，确认弹窗文案区分草稿/废弃；⑤ **顺带修复详情页既有 Rules of Hooks 违规**——`usePermission()` 声明在 `if (!metric)` 早退之后，首次渲染（metric 为 null）跳过该 hook、加载后调用致 hook 顺序变化（详情页偶发按钮不渲染/测试全量 flaky），已移到早退前。**验证**：后端 test_semantic_service +（创建者可删 DRAFT/DEPRECATED、非 Owner 非管理员 FORBIDDEN、域管理员可删他人、REVIEW/PUBLISHED INVALID_STATE）+ test_metric_lifecycle_api（DELETE 携带 role）+ test_governance_policy 41 全过；前端 MetricCatalog 58（+2：创建者可删自己的草稿/非创建者占位）+ MetricDetail 67（+2：创建者可删/非创建者不可见，修复 hooks 后全量稳定）+ tsc 0 + ruff 0 | DELETE /api/v1/metric-definitions/{code}（门禁放宽至写角色，service 层管理员或原 Owner 强校验；状态范围扩至 DRAFT/DEPRECATED） | —（semantic 保持 verified，指标删除权限与维度/度量「管理员或生产者」决策对齐） |
| 2026-08-25 | — | frontend (指标审批) | **评审详情按状态机展示新增/变更/破坏性/重评审 + 变更前后对比 + 状态类型中文**：用户问「审批详情没展示是新增还是变更、变更没展示前后对比，且评审记录的状态/类型应改为中文业务术语」。① **变更上下文判定**（`buildChangeInfo`，主判据为当前版本记录 `change_type`）——CREATE→新增指标（蓝）、UPDATE+已发布→变更指标 v{prev}→v{cur}（橙）、BREAKING→破坏性变更（红）、REVIEW 且当前版本仍已发布→废弃恢复重评审（紫，gating `metric.status==="REVIEW"` 防回看视图误判）；回看视图（approve 后已转正）仍按版本记录判定，不误判重评审；② **变更前后对比**（`MetricDiffView`，diff_json `{字段:{before,after,change_type}}` 逐字段渲染 + DEF_FIELD_LABEL 中文标签 + BREAKING 橙边/UPDATE 蓝边；string→pre 代码块、array→Tag 列表、object→JSON，长内容换行防撑破）；③ **待我审确认弹窗**接入 `ReviewChangeSummary` 自加载摘要；「我审过的」评审记录弹窗加「变更上下文」Card；④ **状态/类型中文**——评审记录弹窗「类型/状态」改用 `enumLabel(METRIC_TYPE_LABEL/METRIC_STATUS_LABEL)`（原子/派生/复合指标、草稿/审核中/已发布…），不再裸显英文编码；⑤ **版本历史统一映射**——enums.ts 新增 `CHANGE_TYPE_LABEL`（补 BREAKING），VersionHistory.tsx 改导入统一映射。**验证**：MetricReview 20（+7：新增无历史对比/变更 v1→v2 前后值/破坏性 BREAKING/回看不误判重评审/确认弹窗重评审/listVersions 失败降级/object·array diff 不崩；新增用例内含类型状态中文断言）、VersionHistory 8、tsc 0 | 无（纯前端展示，无接口变更） | —（semantic 保持 verified，审批从「盲审」变为「一眼看出新增还是变更 + 差异对照」） |
| 2026-08-25 | — | semantic (SQL 批量解析) | **批量注册支持工业常用方言（ClickHouse/Oracle/Trino/PostgreSQL/MySQL/T-SQL/云数仓等）+ 切分保留原文 + LLM 兜底传完整 SQL**：用户问「除了 hive/spark/doris 也要支持工业常用 SQL 语法、LLM 兜底应传整个文本 SQL」。① **方言覆盖全量扩展**——`sql_infer._INDUSTRIAL_DIALECTS` 定义 21 个工业方言（hive/spark/starrocks/doris/clickhouse/oracle/trino/presto/postgres/mysql/tsql/snowflake/bigquery/redshift/databricks/impala/teradata/athena/duckdb/materialize/risingwave），`_split_statement` 与 `_parse_profile_ast` 共用；② **方言聚合识别**——`_agg_display_name` 归一 ClickHouse `CombinedAggFunc`（sumMerge/sumIf→SUM、avgMerge/avgIf→AVG、countMerge/countIf→COUNT、max/minMerge→MAX/MIN）+ `CountIf`→COUNT + Trino `approxdistinct`→COUNT_DISTINCT（key 无下划线，修复既有映射不匹配）；`_extract_col_name` 从复杂表达式（Oracle `nvl/COALESCE` 包裹）提取内部列；③ **聚合识别最大化解析**——`_best_dialect_ast` 按各方言解析出的 AggFunc 数择优（ClickHouse `sumMerge` 在 hive/spark 方言下降级 Anonymous 丢度量，仅 clickhouse 方言识别为 CombinedAggFunc——只取首个可解析方言会丢候选），默认方言为基线、仅文本含方言聚合函数名（`_DIALECT_AGG_HINT` 性能护栏）或默认无 Select 子树时才遍历方言，**普通 SQL 行为不变**；④ **切分保留原文**——`_split_statement` 优先返回引号感知分号切分的**原文切片**（sqlglot 方言 `ast.sql()` 序列化把 `sumMerge`→`SUMMERGE`、`countIf`→`COUNT_IF` 大写改写致后续方言识别失效丢度量），段数一致性判定后仅 CTE/INSERT 内分号罕见场景才用 sqlglot 语义序列化；⑤ **LLM 兜底传完整 SQL**——`_llm_infer_measures`/`_llm_infer_period` 签名改 `(full_sql, focus_sql)`，prompt 带完整脚本上下文（CTE/前置语句/注释）+ 焦点语句精确提取，避免切分信息丢失影响推断。**验证**：test_sql_infer +6（ClickHouse 三聚合/Oracle nvl 列提取/Trino approxdistinct/PostgreSQL FILTER/MySQL+T-SQL/普通 SQL 默认方言不变）、test_sql_split +4（原文保留/CTE 语义/LLM 度量传完整 SQL/LLM 周期传完整 SQL）；真实链路 Doris 3 候选、ClickHouse 4 候选（sumIf/sumMerge/countIf 全识别）、Trino 2 候选；相关 562 全过、ruff 0 | 无（纯解析能力增强；方言 SQL 从「0 候选」变为「N 候选」；LLM 兜底 prompt 上下文增强） | —（semantic 保持 verified，批量注册覆盖工业常用方言 + 切分无信息损失 + LLM 兜底上下文完整） |
| 2026-08-25 | — | frontend (指标目录) | **指标目录筛选面板分区美化（搜索/条件/快捷筛选独立成行）**：用户反馈「搜索框、筛选框、常用筛选按钮全挤在一行，布局不友好」——把原来单个 `<Space wrap>` 堆叠的工具栏重构为 **Card 筛选面板内四分区**：① **搜索行**——输入框自适应拉宽（flex）+ allowClear，右侧「搜索」主按钮 + 新增「**重置筛选**」一键清空全部条件（状态/分级/关键词/责任人/生命周期/我的指标/收藏/PII/排序），最右展示条数；② **条件筛选行**——业务域/状态/分级/责任人四个 Select **标签前置分组**（责任人占位符改「全部责任人」避免与标签重复），排序 Select + 升降序按钮靠右；③ **快捷筛选行**——「快捷筛选」标签 + 我的指标/我的收藏/PII指标视角开关 + 「生命周期」分组（最近7天创建/30天未更新/即将废弃）；④ **已应用筛选回显**并入面板内底部（发丝虚线分隔），带标签分组与角色视图提示。三区域用 `borderTop: 1px dashed var(--line)` 发丝虚线分隔，视觉层级清晰、窄屏 flex-wrap 换行不乱。**验证**：MetricCatalog 56（+1 一键重置筛选清空断言，含已应用筛选回显消失；全量顺序下既有已应用筛选测试通过）、工具栏相关 8 项全过、tsc 0 | 无（纯前端布局重构，无接口变更） | —（frontend 保持 released，指标目录筛选体验美化） |
| 2026-08-25 | — | semantic (metric 批量下线)+frontend | **批量下线下游使用审查 + 替代指标改选填 + 单指标删除补齐 + 批量按钮灰置说明**：用户问「批量下线为什么还要填替代指标、为什么不能审查下游、为什么没有单指标删除、批量删除按钮为什么是灰的」。① **批量下线下游审查**——新增 `POST /metric-definitions/downstream-check`（输入 metric_codes，一次 `IN` 查询经 `LineageRepository.metric_referrers_batch` 返回每指标被引用者 + 数量，`_READ_DEPS` 只读）；批量下线弹窗打开时惰性加载勾选已发布指标的下游使用情况，每行展示「⚠ 被 N 处下游引用（Tooltip 列出派生指标/消费方）/ ✓ 无下游引用，可安全下线」；② **替代指标语义对齐单条**——`MetricBatchDeprecateItem.successor_code` 由必填改为选填（`str | None`），**有下游引用未填 → 前端拦截标红（弹窗保留、行红框 + 「须填写替代指标」提示，不提交）**；无下游引用可留空提交（`successor_code: null`），后端 `deprecate_metric` 既有 `METRIC_REFERENCED` 兜底仍生效；③ **单指标删除**——指标目录操作列新增「删除」（DRAFT + platform_admin，Popconfirm 二次确认 + aria-label），`rowActions` 列加入 COLUMN_OPTIONS（此前不在默认列导致操作列被角色列显隐过滤掉、删除按钮永不显示）；④ **批量按钮灰置说明**——批量操作下拉各禁用项加 Tooltip 说明原因（无草稿/无评审中/无已发布/无权限/非平台管理员），不再让用户困惑「为什么是灰的」。**验证**：后端 test_lineage_repository +1（批量 IN 查询按码返回）、test_metric_batch_api +1（downstream-check 端点）；前端 MetricCatalog 56（+4：无下游空替代提交 successor_code=null / 有下游未填拦截标红不提交 / 有下游填替代正常提交 / 单指标删除；改写旧「未填替代拦截」为「无下游可空提交」）；相关 80 后端全过、tsc 0（并行 WIP 例外） | POST /metric-definitions/downstream-check（新增）；POST /metric-definitions/batch-deprecate（successor_code 改选填） | —（semantic 保持 verified，批量下线从「盲目必填」变为「先审查、再按需填」） |
| 2026-08-25 | — | semantic (SQL 批量解析)+frontend | **批量解析 LLM 兜底提取度量 + skipped 原因分类 + 前端按原因分类提示**：用户问「批量解析没有 LLM 兜底吗」——画像解析（`parse_sql_profile`）此前纯 sqlglot 规则，方言/结构异常时直接空画像→0 候选→前端一律「请检查 SQL 是否含 SELECT + 聚合函数」。修复：① **LLM 兜底 `_llm_infer_measures`**——规则层解析不出度量时让 LLM 从 SQL 提取 `{column, agg, alias, table, period, name}`（新增 `parse_sql_measures_result` 统一解析 + 聚合归一白名单 + 去重/过滤幻觉），复用 `_build_atomic_candidate` 产出候选（LLM 建议名优先）；② **方言聚合变体识别**——`_classify_no_measure` 聚合正则改前缀匹配（`sumMerge/sumIf/countIf/approx_distinct` 等 ClickHouse/StarRocks 变体不再误判「确实无聚合」而跳过，改走 LLM 兜底）；③ **skipped 原因四分类**（`ddl_only`/`parse_failed`/`no_aggregate`/`llm_infer_failed`）——纯 DDL 不浪费 LLM 调用，前端按分类映射友好文案（不再一律「请检查 SELECT」）+ 部分成功场景「N 条语句跳过」Tooltip 展示逐条原因；④ **LLM 不可用/失败降级 skipped 不阻断整批**；⑤ 补 **Doris CTAS 候选 → batch-register-from-sql 端到端创建 DRAFT** 测试（批量注册整条链路对 Doris 全通）。**验证**：后端 test_llm_parse +4（解析/过滤/None/围栏+别名）、test_sql_split +3（四分类/LLM 兜底成功/LLM 失败降级）+2（方言变体分类）、test_semantic_service +1（Doris 端到端注册）；相关 408 全过、ruff 0；前端 MetricCreate 批量解析相关 5 全过（+1 无候选分类提示）、全套 45 全过、tsc 0 | 无（纯解析/提示增强；skipped.reason 由「未解析到聚合度量列」变为分类 code，前端兼容映射） | —（semantic 保持 verified，批量解析对任意方言均有 LLM 兜底，非 sqlglot 支持方言也能产出候选） |
| 2026-08-25 | — | frontend | **长口径 SQL/JSON 展示统一换行（评审记录弹窗溢出修复）**：用户报「指标审批→评审记录弹窗，口径 SQL 和 JSON 过长时跑出弹窗窗口外、样式布局不美观」——根因是 `<pre>` 默认 `white-space: pre` 不换行，长行按内容最小宽度撑开，而 antd `Descriptions` 单元格默认 `min-width: auto` 被撑宽，内容溢出弹窗。修复：`<pre>` 统一补 `whiteSpace: pre-wrap` + `wordBreak: break-word` + `maxWidth: 100%` + `boxSizing: border-box`（长行自动换行、宽度约束兜底、纵向滚动保留），覆盖 **评审记录弹窗（MetricReview，含长表达式 `<code>` inline-block+wordBreak）**、**详情页口径定义卡（MetricDetail）**、**通用口径渲染器（utils/display：renderValue/renderDefValue，被对比表等 9 处复用）**、**资产地图详情抽屉（MetricDetailDrawer）**、**模板默认口径（Templates）**、**血缘 ETL SQL/SQL 原文（LineageView）**；顺带修 SystemDict.test.tsx 一处类型断言（DictItemCreateRequest 直接解构，消除 TS2352）。**验证**：MetricReview 13（+1 长 SQL/JSON pre-wrap 回归断言）、MetricDetail 65、SystemDict 21、Templates/LineageView/MetricCompare/AssetMap 132 全过；前端全量 **942 passed / 0 failed**（55 文件）；tsc 0 错（并行未跟踪调试文件 zz_dbg_select.test.tsx 除外） | 无（纯前端样式，无接口变更） | —（semantic 保持 verified，长文本展示统一换行） |
| 2026-08-25 | — | semantic (SQL 批量解析)+frontend | **批量解析支持 Doris/StarRocks CTAS + 下沉同列多语义候选区分**：用户报「SQL 智能推断批量解析出现『未解析到可注册的指标候选（请检查 SQL 是否含 SELECT + 聚合函数）』」，SQL 为 Doris 宽表落库形态 `CREATE TABLE ... DUPLICATE KEY(...) DISTRIBUTED BY HASH(...) BUCKETS 5 PROPERTIES(...) AS SELECT ...`——根因是 **sqlglot 默认方言不支持 Doris 物理属性**（DISTRIBUTED BY/PROPERTIES/BUCKETS 使整句降级为 Command、无 Select 子树）→ 画像空 → 度量 0 → 全部进 skipped。修复：① `parse_sql_profile` 新增 `_parse_profile_ast`——默认方言无 Select 时**复用血缘解析器 `_preprocess_dialect` 剥离 Doris CTAS 物理属性后按默认方言重试**（口径语义不变，纯 SELECT/普通 DDL 行为不变）；② `_split_statement` 方言列表补 **starrocks/doris**（Doris CTAS 语义切分不再降级 Command）；③ 附带修复**下沉同列多语义候选编码重复**——ETL 多个 `SUM(CASE WHEN ...)` 同落一列时用投影 **alias 作命名锚点**生成编码/名称（5 个候选从全叫 `_track_bizdata_day` 变为可区分），口径列/expression/source_fields 不受影响。**验证**：test_sql_infer +1（Doris CTAS 画像：剥离兜底 + 2 度量 + 源表 + 时间列）、test_sql_split +2（starrocks 语义切分 / 批量解析出 2 候选且 alias 编码可区分 + CASE 口径保留 + 物理源表）、相关 462 全过、ruff 0 | 无（纯解析能力增强；Doris CTAS 从「0 候选」变为「N 候选」） | —（semantic 保持 verified，批量解析覆盖 Doris/StarRocks ETL 落宽表场景） |
| 2026-08-25 | — | semantic (metric 全流程)+frontend | **批量注册/注册/编辑/审核/废弃全流程审查缺陷全量修复（10 项）**：① **H1 批量注册 DB 错误 `break`→`continue`**——单列 DB 错误不再中止整批（此前后续列被静默跳过，与「逐列独立」语义矛盾；补「错误在中间列、后续列继续」用例）；② **H2 硬冲突阻断发布**——`approve_metric` 前置查未决 `block_publish=True` 冲突 → `CONFLICT_BLOCKED`（TD§12.4「硬冲突阻断发布 409」落地；软冲突放行；batch-approve 逐条调 approve 自动覆盖）；③ **H3 编辑弹窗口径 JSON 留空丢主体**——留空后改子字段不再以 `{}` 重建丢失 expression/sql/source_tables，子字段合并基底改 `metric.definition_json`；④ **H4 批量废弃未填替代指标静默过滤**——改为明确提示「N 个已发布指标未填写替代指标，已跳过」；⑤ **M1 评审权域匹配**——`canActAsReviewer` 对齐后端四分支：domain 指派校验 `currentUser.domain===reviewer_domain`、未指派仅 domain_admin（跨域 admin 不再看到可点后被拒）；⑥ **M2 保存提示过度承诺**——`isBreakingEdit` 对齐后端 `BREAKING_TOP_LEVEL_FIELDS`+`BREAKING_DEF_FIELDS`，仅破坏性修改才提示「进入消费方确认期」，治理属性变更提示直接生效；⑦ **M3 批量注册 measure_columns 去重**——重复列不再生成相同 metric_code 致第二条 VALIDATION_ERROR 误导；⑧ **L1 批量打回原因可输入**——不再硬编码「批量打回，请修改后重新提交」，弹窗必填原因（对齐单条驳回）；⑨ **L2 批量注册失败项单条重试**——结果区「重试失败项」按钮仅重跑失败列（替代「继续注册」全量重跑致已建 DRAFT 列再判冲突）；⑩ **L3 批量冲突预检 O(N²) 性能**——batch 循环预加载 `load_conflict_existing` 一次 + 逐列成功后增量追加（保持候选间互相检测），不再 N 列 = N 次全量加载。**验证**：后端 semantic 253（+6：H1 中间列继续/H2 硬冲突拦截+软放行/L3 预加载一次）+ ruff 0（含修既有 2 E501 + SIM117）；前端 MetricDetail 61（+2：H3 留空不丢主体/M2 治理属性提示）+ MetricCreate 42（+2：M3 去重/L2 重试失败项）+ MetricReview 12（+1：L1 打回原因弹窗）+ MetricCatalog 52（+1：批量下线未填拦截）全过 + tsc 0（SystemDict 并行 WIP 除外） | 无（H2 行为变更：存在未决硬冲突的指标审核通过被拒 CONFLICT_BLOCKED） | —（semantic 保持 verified，全流程健壮性与治理闭环强化） |
| 2026-08-25 | — | semantic (metric)+frontend | **指标编辑弹窗按类型展示逻辑度量选择器（OneData 原子层）**：用户报「详情页显示"该原子指标未关联逻辑度量"，发起变更申请时却没有关联逻辑度量的选项」——根因是**前后端不对称**：后端 `MetricUpdateRequest.measure_id` 早已支持（更换=破坏性口径变更，`BREAKING_TOP_LEVEL_FIELDS` 含 measure_id 触发版本确认），但前端编辑弹窗（「发起变更申请/编辑」入口）缺失该字段，导致详情页黄色横幅"先创建逻辑度量再编辑关联"空引导、存量原子指标无法 OneData 化。修复：① **前端编辑弹窗 atomic 类型新增「逻辑度量（度量目录）」选择器**——openEdit 惰性加载已发布度量（listMeasureCatalogs PUBLISHED，创建页 Step②同款），回填当前 measure_id、dirty 追踪（改过传新值/清空传 null 解除关联/未改不传保留），提交到 MetricUpdateRequest；② **后端 update_metric 补 measure_id 存在性/已发布校验**（对齐 create_metric 3a：防 FK 500、防草稿/软删度量被引用，MEASURE_NOT_FOUND/MEASURE_NOT_PUBLISHED）；③ **MetricResponse 补 measure_code/measure_name** + `get_metric_public` 经 `_attach_measure_info` best-effort 填充（度量软删/查询失败降级仅 measure_id，缓存/DB 双路径）；④ **详情页 Descriptions 新增「逻辑度量」栏**——已关联显示名称+编码，未关联显示「未关联（存量旧式来源）」引导发起变更申请；⑤ `isBreakingEdit` 纳入 measure_id（对齐后端 BREAKING_TOP_LEVEL_FIELDS），破坏性 Alert 文案按类型含「逻辑度量」。**验证**：后端 semantic_service +4（update_metric 缺度量/未发布/已发布收集 measure_id 3 项 + get_metric_public 填充与降级）；前端 MetricDetail 65（+4：详情展示名称编码/未关联引导/编辑弹窗选择更换/清空解除）；前端相关 MeasureCatalogs+Notifications+MetricCatalog 116 全过（MetricCatalog H4 为并行 WIP 失败）；tsc 0；ruff 0（并行 E501 不触碰） | GET /metric-definitions/{code}（响应新增 measure_code/measure_name）；PUT /{code}（measure_id 校验强化，非法值 400） | —（semantic 保持 verified，编辑窗口按类型差异化 + OneData 化闭环） |
| 2026-08-25 | — | semantic (指标审批) | **「我审过的」评审回看增强（解决"只看到已处理标签、看不到审了什么/怎么处理"）**：① 新增「处理结果」列——`approver_id=我`→绿 Tag「已通过」+ 处理时间；`reject_reviewer_id=我`→红 Tag「已驳回」+ 驳回原因（截断 Tooltip）+ 驳回时间；② 操作列「已处理」标签改「查看详情」按钮 + 行点击打开「评审记录详情」弹窗（Alert 处理结论+时间、基本信息 Descriptions、口径定义 Card：表达式/依赖表/依赖指标/来源字段/口径 SQL/完整 JSON、业务描述）；③ 后端 `MetricResponse` 新增 `approved_at`——metric 表无通过时间列，list 接口 `reviewed_by` 场景从当前生效版本 `metric_version.published_at` 批量填充（helper `_fill_approved_at`，无迁移，仅评审历史视图触发避免每次列表多查）；④ 顺带修复 3 个 security 测试 mock 缺口（viewer/domain_admin 缺 `roles_all`/`has_role`，dd78507 多角色重构遗留）。验证：后端 test_api_metrics 12（+3：approved_at 填充/跳过 None 版本/API 级填充）+ ruff 0；前端 MetricReview 12（+2：驳回场景/查看详情弹窗，改写「已处理」断言）+ vitest 全绿 | GET /api/v1/metric-definitions?reviewed_by=（响应新增 approved_at 字段） | —（semantic 保持 verified，审批回看能力增强） |
| 2026-08-25 | — | system_dict (参照数据) | **按 tab 批量操作全量补齐**：参照数据页每类字典（粒度/单位/聚合等 tab）支持 批量新增/批量启停/批量删除（207 语义，单条失败逐项标注不影响其余）。后端：`POST /dicts/{dict_type}/batch`（批量新增，逐条复用编码自动生成/软删恢复/冲突判定，savepoint 隔离 DB 级错误）、`POST /dicts/{dict_type}/batch-status`（批量启用/停用，NOT_FOUND 逐项标注）、`POST /dicts/{dict_type}/batch-delete`（批量软删，被引用项 HAS_REFERENCES 逐项标注）；审计新增 `dict.batch_create`（batch_enable/disable/delete 复用既有 verb），detail 含 succeeded/failed 分桶。前端：Table 行选（rowKey 改 code 对齐批量接口）+ 批量工具栏（批量新增弹窗多行 label/sort/desc，编码后端自动生成；批量启用/停用/删除 Popconfirm 确认）+ 已选计数。验证：test_system_dict_service +6、test_system_dict_api 新建 9 项（共 73 相关测试全过）、SystemDict.test +6（共 21 全过）、tsc 0 错、ruff 0 | POST /dicts/{dict_type}/batch、/batch-status、/batch-delete | —（system_dict 保持 verified，管理能力增强） |
| 2026-08-25 | — | audit_remediation | **审计日志行点击详情弹窗**（纯前端，WORM 只读记录无需后端端点）：操作日志表格 + 合规报告「审计导出记录」表格 `onRow` 行点击打开 Modal——顶部 action_desc 中文操作描述 + 操作时间 Alert，基本信息 Descriptions（编号/操作者/操作对象含完整 entity_id 技术前缀不剥离/来源地址/完整追踪编号/敏感数据/归档/原始动作码），操作详情区 detail_json 全字段结构化展示（AUDIT_FIELD_LABEL 中文映射 + auditValueText 值翻译，空则「无附加详情」，超长区滚动）；行 hover 手型 cursor 提示可点击。验证：AuditLog 14（+4：行点击完整详情/空 detail 降级/关闭/导出记录行点击）+ 前端全量 915 passed（失败为 MetricDetail 并行 WIP + flaky，与本次零交集）+ tsc 0 错 | GET /audit（行数据即完整详情，无接口变更） | —（audit_remediation 保持 implemented，前端查看能力增强） |
| 2026-08-25 | — | conflict (semantic) | **冲突检测对比能力全量补齐（审查发现的 P0-A~P2-K 全部落地）**：① **P0-A 手动预检空转修复**——前端"冲突预检"只传 candidate（existing 空）时服务端自动加载活动指标（`MetricService.load_conflict_existing` 复用），杜绝"遍历空列表永远返回无冲突"；② **P1-H 中文分词**——`_tokens` 中文按字符 bigram 切分 + `_SPLIT_RE` 补下划线分隔符（修复编码整体成单 token 致 name Jaccard 半腿失效、**GRAIN_UNIT 分支从未被编码真正触发的隐藏 bug**）+ `definition_similarity` 0.7 编辑+0.3 Jaccard 加权（中文"同义异名"漏检显著缓解）；③ **P1-D 口径要素归一**——`_definition_compare_text` 把维度/过滤/粒度/聚合/依赖/单位并入比对富文本（维度不同不再误判重复建设、过滤/依赖不同不再漏检）；④ **P1-E 空源表**——`lineage_overlap` 空集对空集返 0（不再系统性抬高综合分误报重复建设）；⑤ **P1-F/G 加载器**——`list_active_for_conflict` 排除 DEPRECATED + 按 id 分页全量（原 `list_metrics(limit=1000)` 截断漏检历史指标、废弃指标参与比对制造噪音）；⑥ **P0-C VERSION_CONFLICT**——声明多年从未实现的分支补上（同码同义同域不同指标行 → 软冲突）；⑦ **P2-J 粒度/单位**——`_grain_unit_diff` 从 `definition_json.granularity/unit` 读取（编码不带粒度词也能识别）+ 两侧"确实不同"才命中；⑧ **P2-K 同义词**——`MetricInput` 增 `synonyms`，`_name_equivalent` 把度量目录同义词并入 name 比对（gmv↔成交总额 语义等价识别）；⑨ **P2-I 更新重检**——`update_metric` 口径实际生效变更后触发 `_detect_and_mark_conflicts`（原仅创建时检测一次，改口径后"后来变得同义"无法发现）；`create_metric` 冲突块抽取为公共方法复用，挂标记返回更新对象。**验证**：similarity +20（VERSION_CONFLICT/维度不误判/中文同义/要素匹配/grain 双路径/同义词）、conflict_service +32、conflict_api +2（自动加载/不覆盖显式 existing）、semantic_service +5（P2-I 两态/load_conflict_existing）、semantic_repository +1（分页全量）相关 441 全过；conflict 集成（真实 DB）2 全过；ruff 0（既有 E501 不动）；全量 3650 passed（158 失败经**干净 worktree HEAD 对照**证实为 master 已提交多角色重构 dd78507 的 security mock 未同步，与本批零交集） | POST /conflicts/check（existing 空自动全量加载）；GET 检索/GRAIN_UNIT/VERSION_CONFLICT 检测面扩大；update 口径变更自动落库 | —（conflict 保持 released，检测对比能力全量补齐） |
| 2026-08-25 | — | conflict (semantic) | **冲突自引用根治（候选=现有=同一指标）**：用户报「仲裁模块候选指标和现有指标一样、对比内容也一样」——实测定量：13 条 `source=backfill` 冲突记录 `metric_a==metric_b`、`metric_codes` 的 candidate/existing 同码（如 `e2e_rev_assign_day` id=11/11）、同码计数均为 1（无真实对手）。根因：历史 precheck 路径候选侧 `_to_candidate` **不携带 metric_id** → `detect_conflict` 自我引用防御（cand_id==ext_id）单侧失效 → 同码自身条目被误判 `SAME_NAME_DIFF_DEF`（候选 definition_json 缺 domain 键→域空 vs 库里域，触发「同名定义/域不同」），detail 指向自身；回填脚本忠实转写为 conflict 记录。修复：① **precheck 候选侧补 `metric_id`**（`_to_candidate`/`precheck` 新增参数，创建后调用传自身 id）使防御双侧生效；② **check 落库循环纵深防御**——`candidate.metric_id==det.existing_metric_id` 时不进 detections/不 blocked/不落库（日志 `conflict_self_reference_skipped`）；③ **回填脚本自引用防御**——正向跳过自引用 detail（existing_code==自己 或 existing_metric_id==自己）并清除错误标记，反向跳过 `metric_a==metric_b` 记录；④ **新增 `--cleanup-self-refs` 模式**——软删（deleted_at 保留审计）13 条存量自引用冲突 + 清除 13 个指标自引用标记，幂等。**验证**：DB 实测清理后自引用未决冲突 0、OPEN 冲突 0、pending_conflict 指标 0、e2e_rev_assign_day 标记已清；test_backfill_conflict_orphans 新 9 项（forward 自引用跳过+清标记/合法冲突照建/dry-run 不突变/reverse 不回置/cleanup 软删+清标记/不误清非自引用/幂等）+ test_conflict_service +1 纵深防御 31 全过 + test_conflict_precheck + test_semantic_service 全过（326 passed）；ruff 0 | 无（GET /conflicts 不再返回自引用记录，仲裁台消失「候选=现有」异常项） | —（conflict 保持 released，自引用冲突根治 + 存量清理） |
| 2026-08-25 | — | semantic (auto_fill)+backend | **修复 auto_fill 计数类 unit 推断非法（COUNT/cnt → 字典合法 TIMES）**：批量注册含计数列（`cnt/count/num`，如 `visit_cnt`/`patient_cnt`）的指标时报「字典值不存在: unit/COUNT」——根因：unit 字典无 `COUNT` code（合法值仅 CNY/USD/EUR/CNY_WAN/CNY_YI/PERCENT/ORDER/TIMES/PERSON/DAY/HOUR/MINUTE），而 `_infer_unit` 对计数类列（列元数据 cnt/count/num/qty/quantity/次数/数量 + 列名 cnt/count/num）推断 `unit=COUNT`，且 `create_metric` 兜底默认 `unit="cnt"`（service.py 468）、批量注册兜底 `defaults.get("unit","cnt")`（service.py 4210）同为非法值 → 任何依赖自动推断/兜底的计数指标创建即被字典校验拦截。修复：四处统一改为 **`TIMES`（次）**——unit 字典最通用计数单位，与 seed 医疗计数指标（挂号/就诊人次用 TIMES）对齐；顺带修掉提交文件 3 个既有 E501（service.py 512/2326、test_auto_fill.py 159 折行，行为不变）。**验证**：test_auto_fill +2（列名计数语义 unit=TIMES source=rule 且聚合仍 COUNT / 列元数据次数 unit=TIMES source=column_meta）29 全过；test_semantic_service + test_sql_batch_api + test_auto_suggest_api 256 全过；ruff 全清（改动文件 0 E501）；chaos 全量 1 失败（test_ai_chaos nl2sql MagicMock 序列化，既有/并行问题与本次零交集） | 无（修复自动推断/兜底产出值，批量注册计数指标不再 422） | —（semantic 保持 verified，自动推断单位字典合法性根治） |
| 2026-08-25 | — | conflict (semantic)+frontend | **指标目录「口径冲突」与仲裁模块数据一致性根治（A+C 方案，软冲突也落库 + 软硬区分展示）**：用户报「指标目录显示口径冲突待处理、仲裁模块却为空」——根因：创建路径自动预检（ConflictPrechecker.precheck）只挂 `Metric.pending_conflict` 标记、**不落 conflict 表**（仲裁模块数据源），而标记清除依赖 conflict 表 `count_open_for_metric` → 孤儿标记永远无法通过正常仲裁清除。实测定量：开发库 13 个 `pending_conflict=1` 指标、conflict 表 0 条记录。修复：① **A 方案（打通闭环）**——`create_metric` 改调 `ConflictService.check(use_llm=False, source="auto")`，命中即落 conflict 表 OPEN 记录（**硬/软均落**，PII 仍转 pii_review），再按 `get_first_open_for_metric` **实际未决记录**挂标记（杜绝孤儿态，detail 携带 conflict_id）；② **治理字段**——conflict 表加 `severity/source/reason/block_publish`（迁移 0090）+ 落库时解析 `metric_a/b`（跨域一致率统计 join 可用），`check` 增 `use_llm/source` 参数；`ConflictResponse` 暴露新字段，列表支持 `severity` 过滤；③ **审计**——创建自动命中补 `conflict.auto_detect`（含 i18n 动词）；④ **C 方案（存量回填）**——`scripts/backfill_conflict_orphans.py` 双向一致性（正向孤儿标记→建 OPEN 记录 source=backfill + 反向未决冲突引用指标未挂标记→回置），幂等；开发库已回填 **13 条 OPEN 记录**（全部 hard），13 个标记指标全部有未决冲突、detail 均含 conflict_id；⑤ **前端仲裁台软硬区分**——级别 Tag（硬冲突·需仲裁红标 / 软冲突·建议复核蓝标）+ 来源标识（自动预检/人工预检/存量回填）+ 级别过滤 + 弹窗硬冲突阻断提示/软冲突建议提示，降低仲裁台噪音。**验证**：后端 376 项冲突/semantic 相关全过（+check 治理字段落库/use_llm 参数 4 项、repository severity 过滤+get_first_open_for_metric 5 项、create_metric 自动落库+无孤儿 2 项）；全量 unit 3471 passed（2 失败 audit_i18n 剩并行 6 动作未补动词 + feature_flags 均为既存）；迁移 0090 unisense_it 与开发库均 upgrade 验证表结构；回填后 DB 实测一致性 13/13；前端 ReviewWorkbench 20 项全过（+3 软硬区分/过滤/弹窗提示）+ 全量前端 914 全过 + tsc 0 | conflict 表新增 severity/source/reason/block_publish；GET /conflicts 支持 severity 过滤；POST /metric-definitions 创建自动落库+审计 conflict.auto_detect | —（semantic 保持 verified，口径冲突闭环根治 + 仲裁台软硬区分） |
| 2026-08-25 | — | semantic (sql 批量解析周期)+backend+frontend | **SQL 批量解析自动识别统计周期（截月/截年/截季等） + LLM 兜底推断**：用户诉求「自动识别 month_id 这类截月粒度 + LLM 兜底避免不同 SQL 场景解析异常」。① **规则层 `sql_infer.py`**——`SqlProfile` 新增 `time_granularity` 字段；新增 `_detect_time_granularity`（+`_expr_time_grain`）从投影别名 / 投影表达式 / GROUP BY 表达式识别明确粒度：`substr(x,1,7|6)` 截月、`substr(x,1,4)` 截年、`date_trunc('month'|'week'|'quarter'|'year'|'hour'|'day', x)`、`date_format(x,'%Y-%m')` 及 Hive `yyyyMM` 变体、`month_id`/`stat_month`/`biz_month`/`day_id` 等粒度别名裸列、`date(x)` 截日；**最外层透传无信号时下沉 FROM 子树**（ETL 透传 INSERT 的聚合子查询，对齐 `_extract_measures` 下沉）。`_detect_time_column` 增强：优先返回明确粒度列（month_id 等，不再被 create_date 的 date token 抢跑）、投影别名里的时间列；收紧 `_id` 泛匹配（修复 shop_id 被误判为时间列）。② **周期推断 `sql_split.py`**——`_period_from_profile` 优先 `time_granularity`（粒度信号 > 列名 token），修复 ETL 截月回落 day 误判；新增 `_period_uncertain`（无粒度信号且无时间列 → 不确定）+ `_llm_infer_period`（LLM 从整段 SQL 推断周期，窗口函数 partition by 月份 / Oracle trunc / 无 GROUP BY 时间列等规则层盲区兜底；LLM 不可用/超时/解析失败降级规则层默认，绝不阻断候选生成）；`infer_sql_batch` 对不确定段接线 LLM 兜底。③ **`llm/parse.py` 增 `parse_period_infer_result`**——`{"period","confidence","reason"}`，period 白名单 day/week/month/quarter/year/hour + 中英文别名归一（月→month），缺字段/越界返回 None。④ **前端 MetricCreate**——批量候选行新增统计周期 Tag 展示（PERIOD_OPTIONS 映射中文，绿标），用户可见自动识别结果。**验证**：test_sql_infer +9（substr 截月 7/6/截年/date_trunc/date_format/裸列 month_id/dt 保持 None/无时间信号/ETL 下沉识别）、test_sql_split +10（period_from_profile 4 场景/uncertain 2/LLM 兜底 month 应用/LLM 不可用降级 day/parse_period_infer_result 3）56 全过；语义相关 88+295 全过；ruff 0；真实环境（mock db 全链路）用户完整 ETL SQL——候选周期从「日」修正为「月」（2 原子候选 period=month），set/create 仍正确 skipped | 无（parse-sql-batch 返回候选 period 更准确；批量注册创建端直接消费候选 period） | —（semantic 保持 verified，SQL 批量解析周期识别 + LLM 兜底） |
| 2026-08-25 | — | users + governance + frontend | **用户管理支持多角色（方案 A：RBAC 关联表 user_role，user.role 保留为主角色冗余）**：① 新增 `user_role(user_id, role)` 表（唯一约束 (user_id, role)，迁移 0089 建表 + 存量回填一行），`User.roles_all()/has_role()`（主角色 ∪ 扩展角色，向后兼容单角色读取）；② `require_roles` 改为命中任一角色即放行（主角色或 user_role 扩展）；`my_permissions` 取全部角色权限点并集（含 `roles` 字段，UI 权限点/资源级动词不再缺角色）；PDP `Subject.roles` 多角色决策并集（platform_admin 直通 / metric_owner owner 校验 / compliance_officer PII 复核均按任一角色命中）；③ users API `UserCreateRequest/UpdateRequest` 新增 `roles` 多选（缺省=[role]，主角色按权限优先级自动重算：platform_admin>domain_admin>metric_owner>reviewer>compliance_officer>analyst>viewer），创建/编辑整表替换 user_role，自我保护（当前平台管理员不能移除自己的 platform_admin）；列表 role 过滤支持多值（命中任一）；④ 全库角色比较迁移：notify/collector/semantic/governance 的 `User.role == x` DB 过滤改为 OR user_role 匹配，dimension/measure_catalog/metric_mount/ai/lineage/metrics/assetmap 对象比较改 has_role，notify IDOR 防护透传 roles；⑤ 前端 UserManagement 角色改多选（创建/编辑 Select mode=multiple）、表格多 Tag（主角色标蓝）、筛选多选（包含任一，后端 list query 重复 key）。**验证**：后端新增 12 项（多角色创建主角色重算/更新整表替换/自降级保护/require_roles 多角色放行与拒绝/PDP 多角色并集 4 项/my_permissions 并集）+ 全量 unit 3429 passed（2 项既存 audit_i18n/feature_flags 失败与本次无关）+ ruff 0；前端 UserManagement 13 项全绿 + 全量 910 passed（1 项 DataSources 既存 flaky 单独跑 40 全绿）+ tsc 0；迁移 0089 真实 DB upgrade 应用 + 回填验证 9 用户各一行 | POST /users（roles 多选）、/me/permissions（roles+权限并集）、/auth/users 与 /users 角色过滤多值、PDP 决策多角色 | —（users 保持 released，多角色基础设施落地） |
| 2026-08-25 | — | semantic (batch-register)+frontend | **批量注册指标「维度列映射」从死字段变活 + 列名自动推断维度名（用户诉求）**：① **后端 `batch_register_metrics` 消费 `dimension_mapping`**——此前该字段被静默丢弃（构造 MetricCreateRequest 时 definition_json 固定 `{expression, dependencies}` 无维度信息，用户填的维度名/列名既不进口径 JSON 也不产生血缘边，等于白填）。修复：每个候选指标的 definition_json 合入 `dimensions`（维度名数组，对齐单条注册 Step② 关联维度——血缘差异同步 `sync_metric_dimension_edges` 据此自动建「指标↔维度」边）+ `dimension_columns`（保留 维度名→源表列 完整映射，血缘/展示可读）；空名/纯空白键过滤、保序去重；未传映射时保持最简 definition_json 不变。② **前端列名自动推断维度名**——批量弹窗维度映射行选源表列后，按列名业务特征自动推断并预填维度名（`dept_code/科室→dept`、`doctor_id/医生→doctor`、`patient_id/患者→patient`、`drug/药品→drug` 等 12 组），仅当该行维度名为空时预填（不覆盖用户已填），推断后仍为 AutoComplete 可搜索平台维度/手输兜底（血缘边可挂未采集维度节点，与单条 dimensions 语义一致）；extra 提示明确「维度名将写入指标维度（血缘图据此生成指标↔维度边）」。**验证**：后端 test_semantic_service +2（维度映射合入 dimensions+dimension_columns 且空名过滤 / 未传映射保持最简）batch_register 9 项全过 + semantic 全量 240 passed；前端 MetricCreate +1（选 dept_code 列→维度名自动预填 dept→提交 dimension_mapping `{dept: "dept_code"}`）42 全过 + 相关三文件 152 全过 + ruff 我的代码 0 错（3 个 E501 为既有 stash 后仍存） | POST /metric-definitions/batch-register（dimension_mapping 真正落库生效，批量指标 definition_json 新增 dimensions/dimension_columns） | —（semantic 保持 verified，批量注册维度映射闭环） |
| 2026-08-25 | — | semantic (sql 批量解析)+backend | **SQL 批量解析修复「ETL 透传 INSERT 解析不到任何指标」**：用户反馈粘贴一段 Hive ETL 脚本（set 参数 + 中文 comment 建表 DDL + `insert overwrite ... select ... from (聚合子查询) a left join (字典) b`）批量解析零候选。根因两条：① **画像层**——`_extract_measures` 只提取**最外层 Select 投影**的聚合，而 ETL 落宽表典型是「外层透传（只改名/join 字典）+ 内层聚合」，`ast.find(Select)` 取到无聚合的透传层 → measures=0 → 整段 skipped（且源表提取 `tables[0]` 可能是 join 右侧字典表而非主表）；② **切分层**——`_split_statement` 用默认方言解析含 `stored as orc` + 中文 comment 的 Hive DDL 整段抛错返回 0 段，回退分号切分。修复：① `sql_infer.py` 重构 `_extract_measures` → `_projection_measures`（聚合投影提取，`count(distinct case when ...)` 取 then 分支列）+ **外层无聚合时下沉 FROM 子树收集聚合投影**（按 `(alias, agg)` 去重，UNION 多支同指标合并），下沉度量附 `alias/table/expression`（别名区分同列不同语义、来源表定位聚合所在子查询表、口径保留原始聚合 SQL）；直接投影场景保持基础 `{column, agg}` 结构不变（既有断言不受影响）；② `sql_split.py`——`_build_atomic_candidate` 候选 key 用 `alias or col`（防 `0:doctor_code` 重复冲突）、源表/口径优先用度量自带值；`split_select_measures` 源表同优先；`_split_statement` **多方言依次尝试（默认→hive→spark）**，Hive DDL 不再导致整段回退。**验证**：test_sql_infer +3（透传 INSERT 下沉 2 度量带 alias/table/expression / case 列提取 / 直接投影保持旧结构）、test_sql_split +3（Hive DDL 多方言切分 / ETL 透传候选 key+源表 / 复合合成）36 全过；语义相关 406 passed（1 失败 test_measure_catalog 既存——stash 我的改动仍失败，并行 WIP 导致）；deps.py 并行 WIP（require_roles 改 roles_all）导致的 15 项 API 测试失败经「临时还原 deps 后 29 全过」证明与本次零交集；ruff 0；真实环境重建 backend 容器后 curl 实测——用户完整 SQL 从「0 候选/4 全 skipped」变为「**2 候选**（current/last_month_active_doctor_cnt，源表 wedw_dw.doctor_visit_agent_info_da、口径 COUNT(DISTINCT)/COALESCE(COUNT(DISTINCT CASE...)) 完整）+ 3 skipped（set×2/create 无指标合理）」 | 无（纯解析能力增强，parse-sql-batch 返回更完整） | —（semantic 保持 verified，ETL 透传 INSERT 批量解析可用） |
| 2026-08-25 | — | components (frontend) | **长标识展示组件 CodeValue 公共化——通知中心提取为全站统一组件并复用到详情页依赖指标 / 血缘影响节点 / 度量目录编码列**：上一轮 ba792ce 在 Notifications 内部实现的 CodeValue（单行中间省略 + hover 完整值 + 复制）本轮提为公共组件 `components/CodeValue.tsx` 并增强：① **复制反馈升级**——复制成功由按钮内文字变化改为 antd `message.success("已复制：…")` 全局通知 + 按钮文字「已复制」双反馈；② **极端长值兜底**——>100 字符 Tooltip 弹层加宽（`code-value-tip-overlay-wide` 520px）+ 内容 max-height 160 纵向滚动，避免超长堆栈/JSON 弹层过高遮挡；③ **新增强化 props**——`tag` 变体（边框 Tag 视觉，max-width 可配 + CSS ellipsis 二次兜底）、`maxChars` 省略阈值（窄列可调）、`displayValue`（展示文本与完整值分离，aria-label/Tooltip 仍存完整值）；④ **三处复用**——MetricDetail 依赖指标（原 Tag 改 CodeValue tag 变体，长编码可省略+hover+复制）、LineageImpact 血缘节点列（剥离 `metric:/table:/field:` 前缀展示编码本体 + maxChars 34，修复 320px 窄列被 375px 等宽省略文本撑破的实测问题）、MeasureCatalogs 编码列（原 `<code>` 改 CodeValue code 变体）；⑤ Notifications 删除内部实现改引公共组件（移除不再用的 Tooltip/CopyOutlined import），`overlayClassName` 弃用警告改 `classNames={{ root }}`。**验证**：CodeValue.test.tsx 新增 10 项（省略纯函数首尾段保留/阈值边界/三变体渲染/tag 变体/复制+message 反馈/极端值弹层加宽与滚动）；Notifications 52 + LineageImpact 7 + MetricDetail + MeasureCatalogs 141 全过；Playwright headless chromium 实测 5 场景（通知卡片/依赖指标/节点列/编码列/极端值）均 `parentBreak=false` 无容器撑破（修复前节点列 parentBreak=true 复现撑破）；全量前端 906 passed（Catalogs/DataGovernance/Dimensions 4 项为测试间干扰，单独跑全过，与本批零交集）；tsc 0 错 | 无（纯前端公共组件抽取 + 三处复用） | —（components 新增公共组件，通知/详情/血缘/度量目录长标识体验统一） |
| 2026-08-25 | — | frontend (catalogs) | **采集目录表格行点击打开字段详情抽屉（对齐描述缺失治理行点击交互）**：用户反馈「采集目录展示的实体信息不支持点击行查看具体信息，而描述缺失治理模块可以」——排查结论：① 治理面板（DescriptionCoveragePanel）表格 `onRow.onClick` 直接打开治理抽屉（含复选框守卫 + cursor pointer），而采集目录主表格 `onRow` 仅做 `?focus=` 目标行高亮，**无行点击行为**，用户必须点操作列「字段详情」按钮才能打开抽屉（运维视角 vs 治理视角的交互不一致）；② 数据/抽屉内容本就完备（openFieldDetail 已含表级信息 + SchemaTable 字段编辑/推断/采集）。修复：Catalogs 主表格 `onRow` 增加 `onClick → openFieldDetail(record)`（复用既有字段详情抽屉），守卫 `target.closest(".ant-table-selection-column")`（点选择列复选框不打开）与 `target.closest("button, a")`（点操作列按钮不触发）；操作列「收藏」「字段详情」按钮 `e.stopPropagation()`（避免按钮点击冒泡重复/意外打开）；行 `cursor: pointer` 提示可点。**验证**：Catalogs 44/44（+2 新用例：点击行主体打开抽屉展示字段 / 点击选择列复选框不打开但勾选生效）；tsc 0 错（CodeValue.tsx 语法错误为并行会话未跟踪 WIP，与本改动无关） | 无（纯前端行点击交互） | —（collector 保持 released，采集目录行点击体验对齐治理面板） |
| 2026-08-25 | — | semantic (metric-definitions)+frontend | **编辑弹窗补口径分角色（系统开发伪代码口径/数仓开发详细口径）输入框（存量指标可补填）**：用户反馈「指标目录展开详情没有展示 SQL 模式口径、也没有系统开发技术口径」——排查结论：① MetricCatalog 展开区/MetricDetail 详情页**早已实现**口径 SQL + 伪代码口径 + 数仓口径分块展示（容器内 chunk 验证含文案）；② 但**所有存量指标数据均无 `pseudo_definition`/`dw_definition`**（功能 08-25 上线，存量指标注册于此前，注册页当时无这两个输入框）；③ 用户展开的 `order_domain_gua_hao_entity_value_day` 等手建指标 definition_json 仅含 `expression`（无 sql），故展开区只显示「计算口径」+「完整口径 JSON」折叠——**非展示代码缺失，是数据无字段 + 存量无法补填**。修复：MetricDetail 编辑弹窗新增「伪代码口径（系统开发）」「数仓详细口径（数仓开发）」两个 TextArea（`data-testid=editPseudoDefinition/editDwDefinition`），**独立于口径主体模式（expression/sql）始终可编辑**；openEdit 回填、提交时 dirty 语义合入 definition_json（未改保留、清空移除），对齐落地表 source_table 既有模式。**验证**：MetricDetail 59/59（+1 新用例：存量无字段→打开两框为空→输入伪代码口径保存合入→清空数仓口径保存移除）；全量前端 895 passed（1 flaky LineageView 重跑通过）；tsc 0 错；容器重建后 chunk 含 editPseudoDefinition；真实 API 实测——给 DRAFT 指标 PUT 补填 pseudo_definition/dw_definition → OK、definition_json 含两字段 | 无（复用既有 PUT /metric-definitions/{code}，definition_json 扩展） | —（semantic 保持 verified，存量指标口径可补填） |
| 2026-08-25 | — | notification (frontend) | **通知中心长指标编码视觉优化：单行中间省略 + hover 完整值 + 一键复制**：用户反馈「指标编码长度过长时与相邻字段重叠，影响视觉」——上轮 `c3a22d4` 用 Button `white-space: normal` 换行收缩解决重叠，但长编码换行后仍占两行、视觉杂乱。本轮升级为「单行中间省略」更紧凑方案：① 新增 `CodeValue` 组件（Notifications.tsx:588-655）统一编码展示——>44 字符 JS 中间省略（首段 60% + `…` + 尾段 17 字符，保留编码首尾特征便于识别），CSS `max-width: 420px` 极端窄屏兜底；② 完整值存于 `aria-label`（可访问性）+ antd Tooltip 弹层（`trigger: ["hover","focus"]`，`mouseEnterDelay/LeaveDelay 0.15s`），Tooltip 内嵌「复制」按钮调用 `navigator.clipboard.writeText` 写入完整值后反馈「已复制」；③ 应用两处：通知卡片 `f.target` 字段（链接样式点击直达详情）+ 订阅设置表格资产订阅列（`code` 变体等宽字体）。**验证**：Notifications 52/52（更新 2 个长编码既有测试为新方案，+1 短编码不省略回归，+1 复制功能）；Playwright headless chromium 实测——修复组省略后 `scrollWidth=clientWidth=291px` 单行展示、字段无溢出、首尾段保留（`outp_e2e_fee_day_2026_q3_re…_by_store_region`），对照组 Button nowrap 在窄字段下溢出 167px 复现根因；tsc 0 错 | 无（纯前端视觉/交互增强） | —（notification 保持 released，编码展示更紧凑） |
| 2026-08-25 | — | semantic (metrics)+security | **注入守卫对 SQL 文本承载端点按字段/路径豁免（修复 parse-sql-batch 400 INJECTION_DETECTED）**：用户报 `POST /metric-definitions/parse-sql-batch 400`——根因：`_WRITE_DEPS` 挂全局注入守卫，而该端点合法输入就是原始 SQL（用户粘贴含 `--` 行注释 / `/* */` 块注释 / `UNION SELECT` / 多语句 ETL 的指标 SQL），命中 `_PATTERNS` 被误判注入拦截（对齐 /lineage/parse 既有 sql 字段豁免）。修复：① `guard.py` 新增 `guard_against_injection_exempt_paths`（点号路径 + `[]` list 任意元素，如 `candidates[].definition_json`），`_scan_deep` 重构为路径豁免，顶层豁免 API 与「嵌套同名键不自动豁免」保守语义保持不变（test_guard 既有断言全过）；② `metrics.py` 四端点改豁免守卫——`parse-sql-batch`（豁免 sql/custom_rules）、`suggest-domain`/`auto-suggest`（豁免 sql）、`batch-register-from-sql`（路径豁免 `candidates[].definition_json` 子树，候选 name/metric_code 仍全量扫描）。**验证**：test_guard +48（含 6 路径豁免单测）、test_sql_batch_api +48（含 4 端点级豁免契约测试：ETL SQL/custom_rules/嵌套 definition_json 200、name 注入仍 400）、语义相关 289 全过、security 195 passed（3 失败为 notify 并行 WIP 既存）、ruff 干净；真实环境待重建 backend 容器后 curl 实测 | parse-sql-batch / suggest-domain / auto-suggest / batch-register-from-sql（400 → 200） | —（semantic 保持 verified，安全纵深防御不削弱） |
| 2026-08-25 | — | users+frontend (review) | **评审指派用户显示统一为「display_name（username）」+ 创建真实评审用户**：用户反馈「维度管理提交审核指定用户时，评审用户选项框显示 Perf K6 而非 perf_k6」——根因：评审用户下拉 label 全局 `display_name` 优先、缺失回落 `username`（`MasterDataReview:148` 维度/度量/术语单条提交、`MasterDataBatch:117` 批量提交、`MetricDetail:2258` 指标单条提交、`MetricCatalog:1908` 指标批量提交（改用新增 `userLabelMap`，责任人列 `userMap` 保持纯 display_name 不污染）、`MetricCreate:2377` 指标注册后批量提交），而 k6 压测账号 `perf_k6`（display_name=Perf K6）恰为唯一 domain_admin 评审人。修复：① 五处评审用户下拉 label 统一为 `display_name ? display_name（username） : username`（如 Perf K6（perf_k6）），无 display_name 用户直接显示 username；② **创建真实评审用户** `reviewer_li`（role=reviewer、domain=null 全局评审、display_name=李评审，经 admin 走 POST /api/v1/users 落审计，登录验证 OK，must_change_password=1 首登强制改密）——评审下拉现有 perf_k6（domain_admin）+ reviewer_li（reviewer）两个可指派评审人。**验证**：更新 4 处测试断言（李药师（#5）→（pharmacist）、王护士（#7）→（nurse）、张医生（#3）→（doctor））；相关 6 测试文件 223 项全绿 + 前端全量 894 passed（1 失败 LineageView 既存 flaky 单独重跑通过）+ tsc 0 错 | 无（纯前端 label 格式 + 数据操作） | —（users 保持 released，评审指派可用性完善） |
| 2026-08-25 | — | alembic (collector)+backend | **batch_infer_history 补软删列（修复 GET /catalogs/batch-infer-history 500）**：用户报 `GET /api/v1/catalogs/batch-infer-history?limit=20 500`——根因：0087 建表仅含业务列 + created_at/updated_at，而模型 `BatchInferHistory(Base, BaseModel)` 继承 `SoftDeleteMixin`，端点 `select(...).where(BatchInferHistory.deleted_at.is_(None))` 触发 ORM 全列查询 → 旧表无 `deleted_at` 列报 Unknown column → 500（即「schema-migration-required」陷阱：模型加了软删、迁移未同步）。修复：新增迁移 0088 给 `batch_infer_history` 补 `deleted_at`（DateTime nullable，软删时间 UTC）；对开发库 `unisense` `alembic upgrade head` 应用，容器内 `docker cp` 同步迁移文件后 `alembic current` 显示 0088 (head)。**验证**：SHOW COLUMNS 确认 `deleted_at` 列存在、版本号 0088；端点经 admin 登录实测返回 **200** | 迁移 0088（batch_infer_history 加 deleted_at 列） | —（collector 保持 released，批量推断历史端点恢复） |
| 2026-08-25 | — | alembic (env.py)+devops | **迁移基建修复：干净库 upgrade head 失败根因根治（unisense_it 脏态排查）**：① `alembic_version.version_num` 列宽保障 `_ensure_version_table_width`（幂等：表不存在先建 VARCHAR(128)、存在偏窄则 ALTER）——项目 3 个迁移 revision 超 32 字符（0066=34/0078=35/0086=36），alembic 自动建版本表用 VARCHAR(32)，任何干净库从零 upgrade 到 0066 报 1406 Data too long（unisense_it 即因此停在 0065 + 残留 0066 列的半应用脏态）；② **最后迁移版本写入显式 commit**——MySQL non-transactional DDL 下每个迁移的 DDL 会隐式提交上一个迁移的版本写入 UPDATE，但「最后一个迁移」之后无后续 DDL，其版本 UPDATE 悬在未提交事务、连接关闭即回滚（0087 曾因此表已建而版本号未写）。`run_migrations` 后 `connection.commit()` 兜底（事务型库空操作）；③ `ALEMBIC_SQL_ECHO=1` 调试开关（打印迁移原始 SQL）。**验证**：unisense_it wipe 重建至 0087（varchar(128)/66 表/batch_infer_history 齐全，与开发库一致）+ test_glossary_integration 3 项通过 + 对照（stash 修复后集成测试 fixture wipe+upgrade 直接 ERROR，证明修复必要） | alembic upgrade head（所有环境含 CI 干净库） | —（迁移链从零重建恢复可靠，测试库脏态自愈） |
| 2026-08-25 | — | semantic (metric-definitions)+frontend | **SQL 智能推断批量注册指标（场景A/B：多语句切分 + 多度量拆分 + 复合合成）**：用户诉求「一段大段 SQL 含多个指标能否批量注册」——原链路 `parse_one` 静默截断多语句、一条 SELECT 多度量只出一个指标、批量注册输入契约是宽表列而非 SQL。① **新模块 `services/semantic/sql_split.py`**——场景A 三模式切分：`semicolon`（引号+注释感知 `;` 扫描）、`statement`（sqlglot `parse()` 按 CTE/INSERT 语义切分，CTE 别名过滤防误当源表）、`custom`（用户自定义 `delimiters`/`start_markers` 正则，规则未生效 ≤1 段时 **LLM 语义分段兜底**，不可用/失败降级单段）；场景B `split_select_measures` 单语句多度量拆 N 个原子候选（共享源表/维度/周期，`build_profile` expression 模式推断，聚合覆盖为 SQL 解析值），`synthesize_composite` 合成复合候选（`{domain}_{biz}_{groupkey}_day` 显式编码防 4 段超限，口径 SQL=原语句+dependencies 组内原子）；`infer_sql_batch` 主函数（切分→逐语句画像→候选生成→域建议复用 suggest_domain 整段批量建议，无度量语句进 skipped）；② **端点**——`POST /metric-definitions/parse-sql-batch`（只读+LLM，`MetricSqlParseRequest`：sql/split_mode/custom_rules/domain_code/synthesize_composite，审计 sql_batch_parse）+ `POST /metric-definitions/batch-register-from-sql`（`MetricSqlBatchRegisterRequest`：domain+candidates，`service.batch_register_from_sql` Phase1 原子逐 savepoint 隔离 + Phase2 复合依赖预检缺依赖跳过，复用 batch_register 域门禁，审计 sql_batch_register）；③ **llm/parse.py 增 `parse_sql_split_result`**（statements 别名/去空去重）；④ **前端 MetricCreate**——SQL 推断抽屉加 Segmented「单条推断/批量解析」，批量模式粘贴大段 SQL→「解析候选」→按语句 Collapse 分组预览（默认展开），行级 Checkbox 勾选（默认全选原子）+名称/聚合行内可编辑+「合成复合指标」Switch+复合行「需先发布依赖原子」Tag，**勾选联动**（取消被勾选复合依赖的原子→Modal 选「跳过复合/回滚勾选」），「批量创建选中指标」→结果分桶展示+复合发布提示；⑤ **审计 verb** 补 sql_batch_parse/sql_batch_register 中文模板。**验证**：后端新增 test_sql_split 25 项（三模式切分/引号注释内分号/CTE/LLM 兜底与降级/parse 别名去重/infer 候选/域建议回填）+ test_sql_batch_api 8 项（422 契约/审计 action/候选完整性）+ service 4 项（复合成功/缺依赖跳过/savepoint 隔离/域门禁）+ 相关 297 passed + ruff 我的代码 0 错；前端 MetricCreate 41 passed（+4 新用例：批量解析默认勾选/勾选联动跳过与回滚/批量创建分桶）+ tsc 0 错 | POST /api/v1/metric-definitions/parse-sql-batch、/batch-register-from-sql（新增） | —（semantic 保持 verified，SQL 批量注册增强） |
| 2026-08-25 | — | glossary+frontend | **术语删除补全 + 详情页展示统一 + 审核候选按角色过滤**：① **术语生命周期补齐（对齐维度/度量）**——单条 `POST /terms/{code}/reactivate`（DEPRECATED→DRAFT 重新走审核）/ `delete`（软删，仅 DRAFT+DEPRECATED 可删，REVIEW/PUBLISHED 拒绝 INVALID_STATE；platform_admin/domain_admin/创建者可删，快照留痕）/ `restore`（回收站恢复）；`GET /terms?deleted=1` 回收站视图；批量 `batch-reactivate` / `batch-delete` 复用共享 `run_batch`；前端术语页 DEPRECATED 行「重新启用」、DRAFT 行「删除」、回收站 Tab + 恢复按钮，批量下拉增补两操作（MasterDataBatch actions 复用）② **详情页口径展示统一**——MetricDetail / MetricCatalog 展开区 / AssetMap 详情抽屉三处「关联数据表」拆为「依赖表（上游）/ 使用表（下游）」分块；口径双字段 `pseudo_definition`（系统开发伪代码口径）/ `dw_definition`（数仓详细口径）在 MetricDetail 与 AssetMap 抽屉补展示（MetricCatalog 上轮已有）；`display.tsx` DEF_FIELD_LABEL/DEF_CANON_ORDER 补双字段键，DefinitionView 伪代码口径 pre-wrap / 数仓口径 formatSql 渲染 ③ **审核候选按角色过滤（共享组件一处改三页生效）**——`MasterDataReview` / `MasterDataBatch` 评审用户候选 `listUsers()` 后仅列 `domain_admin`/`reviewer`（普通用户不展示，避免指派无人可审）；评审域候选按当前用户过滤（platform_admin 全量；domain_admin/reviewer 仅自己域，`user` prop 由三页传入）。**验证**：后端 test_glossary_service +7 生命周期用例（reactivate/delete 状态守卫/越权拒绝/restore）30 项全过 + ruff 0；前端 Glossary +6（生命周期 3：重新启用/删除/回收站恢复；过滤 3：普通用户被过滤/平台管理员全量域/域管理员限自己域）30 项 + MetricCatalog 展开拆分断言 + 全量前端通过 + tsc 0 | 术语：POST `/{code}/reactivate|delete|restore`、GET `?deleted=1`、POST `batch-reactivate|batch-delete`（新增） | —（glossary 保持 released，生命周期+审核候选治理完善） |
| 2026-08-25 | — | global_search (backend+frontend) | **全局搜索覆盖增强——描述类字段全覆盖 + 度量目录第 9 类 + 中英同义词双路（FR-18）**：① **描述类字段全覆盖**（TD§12.1 修复 5 处缺口）——数据源用途描述、采集目录表级业务描述（搜中文描述找到英文表）、字段注释 col.comment（内存精筛同步匹配，修复注释命中被丢弃）、主题域描述、术语边界 boundary 全部纳入检索；修复**术语 ES fields 字段名 bug**（term_idx 实际字段是 definition 而非 description，ES 路径搜术语定义静默不命中）；② **度量目录新增独立 measure 分组**（第 9 类）——编码/名称/描述/统计口径 stat_caliber/源头系统 source_system/同义词全字段匹配（OneData 原子层权威源）；③ **中英同义词双向扩展**（search/synonyms.py 共享词对 30 组）——MySQL LIKE 路径中文→英文候选 OR 进 LIKE（搜"订单"命中 sales_order、英文→中文同理），ES 路径 mapping 配置 search_analyzer（standard+lowercase+cn_en_synonym token filter）分词层等价；④ `ensure_indexes` analyzer 版本检测自动删除重建 + `force_recreate` 参数（同义词词表变更后重建，随后 /indexes/sync 重灌）；⑤ 前端 9 类展示（度量目录标签/颜色/顺序）+ measure 跳转 /measure-catalogs?kw= + MeasureCatalogs 支持 ?kw= 定位。**验证**：后端 test_global_search 29（+9：描述命中 5 / 字段注释 / 度量目录 2 / 同义词扩展）+ test_es_indexer 9（+4：版本检测重建 / 强制重建 / mapping analyzer）+ 全量 unit 3368 passed（2 失败均为既存 audit_i18n/feature_flags，与本次零交集）+ ruff 0；前端全量 883 项全绿 + tsc 0 | GET /api/v1/search 返回 groups 新增 measure 分组；POST /indexes/ensure 新增 force_recreate 参数 | released（搜索覆盖描述字段 + 度量目录 + 同义词） |
| 2026-08-25 | — | measure+dimension+metric(frontend) | **主数据生命周期补齐 + 注册口径双字段 + 指标展开 SQL 展示**：① **维度/逻辑度量生命周期**——新增单条 `reactivate`（DEPRECATED→DRAFT 重新走审核，决策 1-A）/ `delete`（软删，仅 DRAFT+DEPRECATED 可删，REVIEW/PUBLISHED 拒绝 INVALID_STATE；platform_admin/domain_admin/创建者可删）/ `restore`（回收站恢复）；`GET /{module}s?deleted=1` 回收站视图；批量 `batch-reactivate` / `batch-delete` 复用共享 `run_batch`；前端 DEPRECATED 行「重新启用」、DRAFT 行「删除」、回收站 Tab + 恢复按钮，批量下拉增补两操作（复用 MasterDataBatch，删除红标 Popconfirm 二次确认）② **注册指标口径双字段**——`definition_json` 新增 `pseudo_definition`（系统开发伪代码口径，对应技术方 tech_owner）/ `dw_definition`（数仓开发详细口径，对应数仓开发 dw_developer），`_validate_definition_json` 非空字符串规范化（纯文本不 SQL 强校验）；MetricCreate 向导 Step③ 增加两个明确标注口径区，提交写入双字段 ③ **指标目录展开 SQL 分字段展示**——MetricCatalog 展开区新增「伪代码口径 / 数仓详细口径(SQL) / 计算口径」分块。**验证**：后端 178 项（+生命周期：度量 5 / 维度 4 含 REVIEW 锁定与越权删除拒绝；+口径双字段 schema 校验）+ 前端 MeasureCatalogs 13 / Dimensions 30 / MetricCreate 32 全过 + ruff 0 + 全量后端/前端通过 | 维度/度量：POST `/{code}/reactivate|delete|restore`、GET `?deleted=1`、POST `batch-reactivate|batch-delete`（新增）；指标注册 `definition_json.pseudo_definition|dw_definition`（新增） | —（measure/dimension 保持 released，生命周期+口径完善） |
| 2026-08-25 | — | semantic (metric-definitions)+frontend | **指标注册业务域建议（FR-010 域建议增强：SQL 先于选域可推断业务域 + 未采集实体 LLM 兜底）**：业务域是"组织归属"概念，SQL 语法无直接信号——本批打通「从 SQL/源表反向定位业务域」链路。① **后端服务 `services/semantic/domain_suggest.py`（新）**——两级反查：采集目录 `DBCatalog.entity_name→DataSource.domain`（表归属数据源绑定域，置信度 0.9）+ 挂载实体 `MetricMount.source_table→domain`（既有指标挂载表，0.85）；均未命中（表未被采集，如大段 SQL 引用平台外实体）→ **LLM 兜底**从 SQL/表名推断域（域清单白名单校验返回编码、置信度封顶 0.7、LLM 不可用/非法编码降级无法建议）；返回四态 `unique/multiple/llm/none` + `matched_tables`（前端提示"该表未被采集"）；域仅"建议"，确认权在用户。② **`llm/parse.py` 增 `parse_domain_infer_result`**（domain_code/confidence/reason 提取，缺字段或越界返回 None）。③ **新端点 `POST /metric-definitions/suggest-domain`**（`MetricSuggestDomainRequest`：sql/source_table 至少一个否则 422，类型化防非字符串→500；`_WRITE_DEPS` 防 LLM 额度滥用）。④ **前端 MetricCreate**——SQL 智能推断按钮去掉「先选域」硬前提（未选域也可粘贴大段 SQL）；`handleSqlInfer` 先调 suggest-domain：unique/llm 自动应用并预填 Step0 域 Cascader（`findDomainPath` 映射父→子路径 + `loadDomainDefaults` 预填域默认，抽取复用）+ 抽屉内来源/置信度徽标，multiple 弹候选窗选择后重跑推断，已选域则交叉校验（建议域不同展示「切换为 X」冲突入口），none 提示手动选择且推断照常（空域兜底）。**验证**：后端新增 test_domain_suggest 17 项（目录唯一/挂载/多候选降序/同域取最高置信度/LLM 兜底/LLM 不可用降级/真实查询路径/parse 越界/API 422+200）+ 语义相关 71 全过 + 全量 unit 3363 passed（4 失败均为既有/flaky：audit_i18n+feature_flags 隔离复现为既有、es_indexer 隔离通过为并行干扰，与本次零交集）+ ruff 全清；前端 MetricCreate 37 项全过（+5 新用例：unique 自动应用/多候选弹窗/LLM 兜底/已选域冲突/无法建议）+ Templates 16 全过 + tsc 0 错 | POST /api/v1/metric-definitions/suggest-domain（新增） | —（semantic 保持 verified，业务域建议增强） |
| 2026-08-25 | — | collector+frontend | **描述缺失治理跨表批量推断——服务端批量历史持久化 + 失败分桶智能重试建议（第四轮打磨）**：① **服务端批量历史持久化（跨设备/团队可见）**——新增 `batch_infer_history` 表（迁移 0087：actor_id/actor_name 操作人快照 + tables_json + done/failed/cancelled/added/elapsed + failed_tables_json），`GET /catalogs/batch-infer-history`（按时间倒序，limit 可配）、`POST /catalogs/batch-infer-history`（写入自动裁剪近 20 条超出的软删 + 审计 `catalog.batch_infer_history`）、`DELETE /catalogs/batch-infer-history`（仅清空当前用户自己的记录 + 审计）；前端历史视图升级为**服务端为主**（条目带 id/操作人，展示「操作人：xxx」Tag，重新勾选此批用服务端 catalog_id 精确匹配），**localStorage 降级缓存**（服务端非空才覆盖本地，避免刚启用/被清空时误清缓存；服务端不可用静默保留离线可用）；② **失败分桶智能重试建议**——完成后有失败时按分桶给出策略建议条：限流→「降低并发重试」（自动降至 min(当前-1,1) 并持久化偏好）、超时→「单表串行重试」（自动降并发 1）、并发冲突→提示稍后不自动重试，可一键按建议重试。**验证**：后端 test_batch_infer_history 4 项（POST 落库+审计/空表集 422/GET 列表/DELETE 清空本人+审计）+ collector API 29 全过 + ruff 0 + 迁移链 unisense_it 与开发库均 upgrade head 至 0087（表结构验证）+ 前端 Catalogs 42 项全过（+2 新用例：限流智能重试降并发并持久化 / 完成后服务端持久化+历史视图操作人） | GET/POST/DELETE /api/v1/catalogs/batch-infer-history（新增）；前端批量历史升级服务端为主 + localStorage 降级 | —（collector 保持 released，批量推断治理增强） |
| 2026-08-25 | — | frontend (catalogs) | **描述缺失治理面板可折叠 + 共享刷新 + 两块视角友好提示（方案 B+D）**：① 治理面板（统计卡 + 按表列缺失字段数治理表格 + 批量推断区）包进 antd Collapse 可折叠（默认展开，收起给下方采集目录列表让位），Collapse 标题标注「描述缺失治理（治理视角：按表列缺失字段数优先补全表/字段描述，支持 LLM 推断；与下方采集目录列表数据同源、职责互补）」；② **去掉治理面板内重复「刷新」按钮**（方案 D）——`DescriptionCoveragePanel` 改 `forwardRef` 暴露 `reload` 句柄（`DescriptionCoveragePanelHandle`），主列表「刷新」按钮经 `handleRefreshAll` 同时刷新主列表与治理面板，全页仅保留一个刷新入口；③ 主列表 Card 补 title「采集目录（运维视角：浏览/检索/管理全部采集实体）」——与治理面板标题呼应，明确两块**数据同源、职责互补**（治理 vs 运维），消除「两块内容很接近」的困惑。验证：Catalogs 40 项（+2 新用例：共享刷新 `fetchDescriptionCoverage` 1→2 次、Collapse `aria-expanded` 收起/展开切换）+ AssetMap 59 全过 + 前端全量 836 passed（唯一失败 MetricCreate esbuild transform 为并行会话 WIP 语法错误，与本批零交集）+ 本批 tsc 0 错 | 无接口变更（纯前端展示层：Collapse 包裹 + ref 共享刷新） | —（collector 保持 released，纯前端治理展示增强） |
| 2026-08-25 | — | collector+frontend | **描述缺失治理跨表批量推断第三轮打磨（产品体验持续加固）**：① **失败原因分桶统计**——`classifyBatchError` 按错误特征归类（限流 429/超时/in-flight 并发/未知），进度项记录 `errorCategory`，结果汇总 Alert 展示「失败 N 张（限流×1 · 超时×1）」分桶，失败明细前置 `[分类]` 标签，便于判断可重试性；② **批量历史记录**——localStorage `unisense.desc-coverage.batchHistory` 保留近 5 次会话（ts/表集/成功/失败/取消/新增字段/耗时/失败表），批量面板新增「历史记录」视图（**完成态可直接查看**，无需关闭重开）：逐条展示时间与结果摘要、失败表清单，每条「重新勾选此批」一键按失败表（无失败则按表名匹配全量）回到确认面板重跑，支持「清空历史」；③ **一键勾选全部有缺失表**——表格 extra 新增「选全部有缺失」按钮（跳过无缺失表）；④ **新增字段预览**——进度表格新增「新增字段」列（`+N 个`，Tooltip 悬浮查看字段名清单）。**验证**：Catalogs 38 项全过（+4 新用例：失败分桶/历史写入与重跑/快捷选择/字段预览）+ AssetMap 59 全过 + DataSources/LineageView 单独跑 88 全过（全量偶发 G6/DataSources 干扰为既有，与本批零交集）+ 本批 tsc 0 错 | 无接口变更（复用既有 infer-descriptions/infer-table-description 端点，纯前端编排增强） | —（collector 保持 released，纯前端治理增强） |
| 2026-08-25 | — | collector + frontend (catalogs) | **库名筛选下拉与列表源状态对齐（source_status 透传）**：后端 GET /catalogs/databases 新增 `source_status` query 参数（active=仅活跃源 / deleted=仅已删源 / all=全部，pattern 校验非法值 422），repository outerjoin DataSource 按源软删状态过滤（缺省不过滤保持向后兼容，与 _list_catalogs_with_source_status 语义一致）；前端 listCatalogDatabases 透传 source_status、库名下拉随源状态联动（useEffect 依赖 sourceId+sourceStatus，切换时刷新并重置已选库名）——消除「列表默认只看活跃源但下拉混入已删源库名、选了库后列表为空」的不一致。验证：后端 collector 215 项 + 前端 Catalogs 34 项全绿 + ruff 0 + tsc 0。 | GET /catalogs/databases | released（库名下拉与列表筛选对齐） |
| 2026-08-25 | — | frontend (catalogs) | **采集目录默认只显示活跃源 + 删除文案诚实化**：① 数据源状态筛选默认 `active`（已删源经「已删除源」筛选单独查看，软删追溯保留设计，不再默认混入已删源目录）；② 删除数据源确认文案由「其采集目录、水位、漂移日志将一并清理」改为诚实表述「数据源将软删除，其采集目录保留以便追溯，可在采集目录按「已删除源」筛选查看；删除后原 ID 可重建同名数据源」（详情抽屉单删 + 列表批量删两处）。验证：Catalogs+DataSources 73 项 + 前端全量 861 项全绿 + tsc 0。 | 采集目录列表默认筛选、删除确认弹窗 | released（软删追溯可见性修正） |
| 2026-08-25 | — | collector (hive_metastore) + frontend (数据源表单) | **HMS 元数据库名 database 缺省按 hive 填充防 1046（产品友好提示原则落地）**：① schemas.py 校验器 `_ensure_hms_database`——DataSourceCreate/Update/TestConnectionRequest 对 hive_metastore 缺 database 自动填 `hive`（非该类型不改任何字段）；② hive_metastore.py 工厂 `build_from_cfg` 直接入口同样默认 `hive`，杜绝裸 `1046 No database selected`；③ 前端 DataSources.tsx 类型选 Hive Metastore 自动预填 database=hive、字段标「HMS 元数据库名（必填）」、placeholder/tooltip 提示默认值，`buildConnectionConfig` 兜底；④ DEV_GUIDE §9a 新增「采集器/数据源类型扩展规范」——类型扩展须全链路同步（SourceTypeEnum/DB 枚举迁移/注册表/连接器/前端/测试）+ 关键连接凭据前端标注必填/默认/tooltip、后端 schema 校验器覆盖、连接器禁抛裸 DB 错误（1046 教训）。验证：后端 collector 224 项 + 前端 DataSources 40 项全绿 + ruff 0 + doc_sync 通过。 | /data-sources (create/update/test-connection)、Hive Metastore 采集 | released（HMS 必填凭据默认填充） |
| 2026-08-25 | — | measure_catalog+dimension+glossary+metric+frontend | **四模块批量操作统一接入共享 batch_common（用户诉求：度量目录/维度支持批量，术语也纳入大范围批量，指标迁移共享 helper，前端对齐指标完整批量模式）**：① **共享批量 helper `app/api/batch_common.py`**——泛型 `BatchItemResult(code/ok/message)`/`BatchResponse` + `run_batch`（逐条容错：单条业务失败不阻断其余，SQLAlchemy DB 级异常回滚会话+剩余项统一标记失败不再 500，迁移自指标 `_run_batch`）+ `batch_audit_action`（审计 full/partial/failed 区分）+ `batch_failed_codes`（截断 20 条）+ `batch_response`；共享请求 schema `BatchSubmitItem/BatchSubmitRequest`（评审指派 TD§13）/`BatchCodesRequest`/`BatchRejectRequest`；② **指标迁移**——metrics.py 4 个 batch 端点（submit/approve/reject/deprecate）改调共享 helper，删除本地 `_run_batch` 等 4 函数；semantic/schemas.py 删除 MetricBatchSubmitItem/Request/MetricBatchRejectRequest/MetricBatchItemResult/Response（approve 灰度 metric_codes/mode/gray、deprecate successor 保留），批量响应统一 `results[].code`（前端 MetricCatalog/MetricReview/MetricCreate 同步 `metric_code→code`、submit items `metric_code→code`、reject `metric_codes→codes`）；③ **度量目录/维度新增 4 批量端点**（batch-submit/approve/reject/deprecate）——逐条域作用域守卫（domain_admin/metric_owner 仅本域）+ 共享 run_batch + 汇总审计；④ **术语升级**——batch-submit 语义由 admin 直发发布改为**审核流批量提交**（DRAFT→REVIEW，可带评审指派），新增 batch-publish（platform_admin 直发通道，承载原批量发布）/batch-approve/batch-reject，batch-deprecate 迁移共享 run_batch；service.batch_submit_terms/batch_deprecate_terms 删除（被 API 层 run_batch 取代，测试同步）；⑤ **前端共享组件 `components/MasterDataBatch.tsx`**——「批量操作」下拉（提交审核/通过/驳回/废弃/管理员直发发布，按模块裁剪+权限过滤）+ 确认弹窗（明确数量、危险红标、批量提交含评审指派用户/域选项框、驳回原因必填）+ 失败明细弹窗（>3 条完整展示）+「重试失败项」（仅重跑失败编码）+ 成功/失败 message 反馈；MeasureCatalogs/Dimensions 新接入（rowSelection 多选 + 工具栏批量按钮），Glossary 从轻量批量升级对齐（移除旧确认弹窗）；⑥ **测试**——后端共享 helper 单测迁移（test_metric_batch_api）+ 新增 test_master_data_batch_api 8 项（三模块 batch-submit/approve/reject/deprecate 接线 + 部分失败 207 语义）；前端 Glossary/MetricCatalog/MetricCreate 适配 + MeasureCatalogs/Dimensions 各加批量用例。验证：后端全量 3306 passed（2 失败 audit_i18n/feature_flags 为既有并行问题，与本次零交集）+ 前端全量 54 文件 861 passed + tsc 0 错 + ruff 改动文件全清 | /api/v1/measure-catalogs、/api/v1/dimensions（新增 batch-submit/approve/reject/deprecate）；/api/v1/terms（batch-submit 改审核流提交、新增 batch-publish/approve/reject、batch-deprecate 响应结构统一 BatchResponse）；/api/v1/metric-definitions（batch 响应 results[].code、submit items.code、reject codes） | —（measure_catalog/dimension/glossary 保持 implemented，metric 批量端点共享化） |
| 2026-08-25 | — | collector+frontend | **描述缺失治理跨表批量 LLM 推断产品增强（用户诉求：并发数可配 + 失败明细/重试，产品体验加固）**：① **并发数可配**——确认面板加并发数 Select（1/2/3/5，默认 2，localStorage `unisense.desc-coverage.batchConcurrency` 持久化）；② **有界并发调度**——串行 for 改为 worker 池（`Math.min(concurrency, tasks)` 个 worker 按索引取任务，逐表实时更新状态），替代原串行；③ **失败明细 + 重试**——`inferOneTable` 返回失败原因明细（字段/表描述分开 try/catch，`detail` 供进度表格 Tooltip 展示），完成/取消后失败行内「重试」单表按钮 + 底部「重试失败项（N）」一键重试，**重试合并进同一批次视图**（传入 initial 进度保留已完成/已取消项，仅把 error 项重置 pending 重新执行，汇总反映整批结果而非重试子集）；④ **结果汇总卡**——完成后 Alert 展示「成功 N / 失败 N / 取消 N · 新增字段描述 X 个 · 耗时 Ys」，运行中顶部 Progress 整体进度条；⑤ **运行中可取消**——`cancelRef` 标志停止调度未启动任务（标「已取消」/结果「未执行」），进行中的自然完成，取消后保留结果视图供重试；⑥ **上次失败一键恢复**——批量完成后失败表持久化 localStorage `unisense.desc-coverage.lastBatchFailed` 并同步 state，重新进入页面显示「上次有 N 张表未完成」Alert +「重新勾选并重试」一键勾选失败表并打开确认面板（消费后清除记录）。**验证**：Catalogs 33 项全过（+4 新用例：并发数可配持久化/失败明细+一键重试/运行中取消/上次失败恢复）+ AssetMap 59 全过 + 前端全量 857 passed（唯一失败 MetricCatalog 批量提交为并行会话 api.ts WIP 既存，与本批无关）+ 本批 tsc 0 错 | 无接口变更（复用既有 infer-descriptions/infer-table-description 端点，纯前端编排增强） | —（collector 保持 released，纯前端治理增强） |
| 2026-08-25 | — | collector+frontend | **描述缺失治理支持跨表批量 LLM 推断（用户诉求）**：主治理表格加行选择（rowSelection，仅可选「有缺失」的表——字段缺失或表描述缺失，无缺失表禁用勾选；跨分页保留选择）+「批量推断所选表（N）」按钮（受 `catalog:infer-description` 权限管控）→ 表格 Card 内展开批量面板：确认视图展示每张被选表**自动纳入的缺失字段清单与动作**（表描述/缺失字段数/缺失字段名）→ 开始推断后进度视图**串行逐表**执行（字段缺失→`POST /catalogs/{id}/infer-descriptions` FR-023 一次 LLM 调用整表；表描述缺失→`POST /catalogs/{id}/infer-table-description`），每表实时状态（等待/推断中/完成/失败）+ 结果汇总（字段 +N/跳过/失败），**单表动作独立 try/catch 失败不阻断其余表**，整批 in-flight 去重（模块级 Map key=表集签名）防重复点击，全部完成后 `load()` 刷新覆盖数据并清空勾选；复用后端幂等/审计/权限（已有描述不覆盖）。**验证**：Catalogs 29 项全过（+2 批量用例：多表串行推断+刷新覆盖、单表失败隔离）+ AssetMap 59 全过 + 前端全量 852 passed（DataSources 1 项测试间干扰为既有，单独跑 39 全过）+ tsc 0 错 | 无接口变更（复用 GET /catalogs/description-coverage 与既有 infer-descriptions/infer-table-description 端点） | —（collector 保持 released，纯前端治理增强） |
| 2026-08-25 | — | semantic (metric_template)+frontend | **指标模板对齐 OneData 指标注册结构（方案A）**：模板此前是 OneData 重构前的旧字段集——原子实例化必 422（缺 measure_id）、派生实例化出"无家"指标（缺 mount）、三方责任只能创建后补、编辑页服务模式非法选项 "REALTIME" 实例化即炸。① **模型+迁移 0085**——metric_template 新增 `measure_id`（FK measure_catalog）/ `mount`（JSON，挂载实体）/ 三方责任 6 字段，迁移清洗存量 `serving_mode='REALTIME'`→`REALTIME_ONLY`；② **schema 收严**——模板预设枚举与 MetricCreateRequest 同源 Literal（aggregation/time_semantics/freshness/dw_layer/serving_mode/additivity/metric_tier），非法值创建/编辑即拦截，消除「创建宽松、实例化严格」的契约漂移；③ **实例化链路**——instantiate 按最终类型条件透传模板预设（原子→measure_id、派生→mount、三方责任默认），优先级 body>模板专用列>defaults_json，新增 `_drop_invalid_literal_presets` 强韧性兜底（存量脏枚举不 422 卡死实例化）；④ **create_metric 补 OneData 校验**——原子指标关联逻辑度量须存在且 PUBLISHED（防 FK 500 与静默继承丢失），`MetricService.__init__` 捕获 `_measure_repo`（与 `_repo` 同模式，便于测试替换）；⑤ **前端**——Templates 实例化/编辑弹窗按 type 条件渲染逻辑度量下拉（仅 PUBLISHED 可选）与挂载配置，补 serving_mode/additivity/三方责任（RoleOwnerSelect），修复服务模式选项为合法枚举，详情弹窗展示 OneData 预设；⑥ **测试**——新增实例化预设透传/派生 mount 透传/body 覆盖优先/非法枚举剔除/度量缺失与未发布拦截等用例。验证：后端语义相关 417 passed + 前端 Templates 16 passed + tsc 0 错（改动文件）+ 真实 DB upgrade 至 0085（列/FK/清洗均验证） | /api/v1/semantics/templates（新增 measure_id/mount/三方责任预设，实例化语义增强）、POST /metrics（原子关联度量不存在/未发布 422） | —（semantic 保持 verified，模板子模块对齐增强） |
| 2026-08-25 | — | frontend (dimension+glossary+measure_catalog) | **提交审核弹窗「指定用户」的评审用户由手动输入 ID 改为选项框（统一主数据审核组件）**：用户诉求——维度管理提交审核弹窗选定「指定用户」后，评审用户不应手输用户 ID，应选项框选择；术语/度量目录/指标同理。核实：**指标三处（MetricDetail 单指标/MetricCatalog 批量/MetricCreate 批量注册）评审用户早已是选项框**（均基于 `listUsers()` 加载的 Select+showSearch），本次只缺主数据三页共享的 `components/MasterDataReview.tsx`。① **共享组件**——`MasterDataReviewModals` 内部 `useEffect` 在打开提交审核弹窗时懒加载 `listUsers()`（全局用户列表，三页共用；失败静默回退空列表），「指定用户」的评审用户由 `<Input type="number">` 手输 ID 改为 `Select showSearch optionFilterProp="label"`（placeholder「选择评审用户」、下拉展示「显示名（#id）」、无可用用户提示「暂无可用用户」），label「评审用户 ID」→「评审用户」；② **三页接入**——MeasureCatalogs/Glossary/Dimensions 调用处零改动（共享组件自加载），一次性全部受益；③ **测试**——MeasureCatalogs/Glossary 测试的 api mock 补 `listUsers`（此前未 mock，组件懒加载会 undefined），三页各加「指定用户选项框」用例（选「指定用户」→断言渲染为选项框而非手输框→选择用户→提交 reviewer_type=user/reviewer_id）。验证：Dimensions 25 + MeasureCatalogs + Glossary 58 passed（+3 新用例）+ 前端全量 54 文件 + tsc 0 错 | 无接口变更（纯前端共享组件内部懒加载用户列表） | —（dimension/glossary/measure_catalog 保持 implemented） |
| 2026-08-25 | — | collector | **数据源类型枚举补漏：hive_metastore 加入 SourceTypeEnum（修复连接测试/创建 422）**：用户报错 `POST /api/v1/data-sources/test-connection 422 (Unprocessable Entity)`——根因：Hive Metastore 采集器（0ab2a32 引入，connectors/hive_metastore.py + 注册表 + 前端类型列表均已支持）但**共享枚举 `SourceTypeEnum` 遗漏该值**，导致所有用 `SourceType` 校验的请求（test-connection/databases/tables/create/update）对 hive_metastore 直接 422，且 DB `source_type_enum`（8 值）即使绕过校验也会 `Data truncated`（MySQL 1265）。① **`enums.py`** `SourceTypeEnum` 新增 `HIVE_METASTORE`（9 值，SQLAlchemy 模型列自动跟随）；② **迁移 0086**（挂并行 0085_metric_template_onedata 之后保线性链）把 `source_type_enum` 扩为 9 值（对齐 0033 写法，MySQL 分支，可逆 downgrade）；③ **防回归测试**——schema 级（TestConnectionRequest/DataSourceCreate/Update 接受 hive_metastore）+ API 级（POST /test-connection hive_metastore 不再 422）+ 修陈旧断言 `test_eight_connector_types_registered`→`test_nine_connector_types_registered`（0ab2a32 起既存失败，注册表 9 类而断言 8 类）。**验证**：210 collector 测试全绿 + 全量单元 3298 passed（仅剩 audit_i18n/feature_flags 2 项既存非本次）+ ruff 0 + alembic 单 head + 真实 DB `upgrade head` 到 0086 且枚举含 hive_metastore + 容器重建后真实 API 调用 hive_metastore test-connection 返回 **200**（SSRF 守卫拦截内网伪造 host 报 ok=false，schema 已放行） | /api/v1/data-sources/test-connection、/databases、/tables、POST/PUT /data-sources（hive_metastore 类型由 422 修复为可用） | —（collector 保持 released，枚举补漏） |
| 2026-08-25 | — | dimension+glossary+measure_catalog+frontend | **提交审核弹窗「评审域」由手动输入改为业务域选项框（统一主数据审核组件）**：用户诉求——维度管理提交审核弹窗选定「指定域评审组」后，评审域也应像其它业务域字段一样用选项框选择，而非手输 code。① **共享组件 `components/MasterDataReview.tsx`**——`MasterDataReviewModals` 新增可选 `reviewerDomainOptions` 属性：传入后「评审域」渲染为 `Select showSearch optionFilterProp="label"`（placeholder「选择评审域」、下拉展示「域名（code）」、无可用域提示「暂无启用中的主题域」），未传时回退原手动输入（向后兼容）；② **三页接入**——MeasureCatalogs/Glossary 传各自已加载的 `domainOptions`，Dimensions 用既有 `domainMap` 构建 `域名（code）` 选项；三处「评审域」均为选项框，选择即提交 `reviewer_domain` code；③ **测试基建加固**——根因：antd 静态 `message` 为全局单例 holder，跨测试残留，后一测试断言同类成功文案（如「已提交审核」）命中上一测试残留 toast → 单测通过、全量 flaky；`setup.ts` 统一 `afterEach(message.destroy())`（antd 官方清理，直接删 DOM 会破坏 holder 缓存致后续 message 无法渲染，故不可行）根治。验证：Dimensions 24 项（+1 评审域选项框用例：选「指定域评审组」→断言渲染为选项框（非手输框）→选「财务域（finance）」→提交 reviewer_type=domain/reviewer_domain=finance）+ MeasureCatalogs/Glossary/MetricCatalog 82 项 + 前端全量 54 文件 847 passed + tsc 0 错 | 无接口变更（纯前端共享组件 + 三页传入域选项） | —（dimension/glossary/measure_catalog 保持 implemented） |
| 2026-08-24 | — | dimension+glossary+measure_catalog+frontend | **维度/术语补 REVIEW 审核态 + 统一「主数据审核」复用模式（沉淀共享实现，避免三套重复代码）**：承接上轮"维度/术语评估=下一阶段统一扩展"决策，本次落地且三模块共用一套审核实现。**① 模型层**——新增 `app/models/review_fields.py::ReviewFieldsMixin`（9 审核字段：submitted_by/approver_id/reviewer_id/reviewer_type/reviewer_domain/reject_reason/reject_reviewer_id/rejected_at/reviewed_at），`MeasureCatalog`（从内联 9 列重构为复用 Mixin）+`Dimension`+`Term` 三模型统一挂载；迁移 0084 给 `dimension`/`term` 表加 9 列 + status 枚举加 REVIEW（存量状态不变）；**② 服务层**——新增 `app/services/master_data_review/`：共享 `schemas.py`（ReviewSubmitRequest/ReviewApproveRequest/ReviewRejectRequest）+ `MasterDataReviewMixin`（submit/approve/reject 状态机 + 评审指派 TD§13 + 自审禁止管理员豁免 + 评审人身份校验 user/domain/未指派域管理员兜底 + 通知评审人/提交人 best-effort），宿主服务仅配 4 个类属性（entity_name/event_prefix/code_attr/status_enum）+ 可选 `_review_completeness` 钩子；`MeasureCatalogService` 重构掉内联 ~300 行审核逻辑复用 Mixin（对外方法名不变，42 测试零改动全过），`DimensionService` 新增 `submit_dimension/approve_dimension/reject_dimension`，`GlossaryService` 拆 `submit_term`（DRAFT→REVIEW）+ `publish_term`（admin 直发，含"再次发布"）+ `approve_term/reject_term`；三处 `update_*` 加 REVIEW 锁定（审核中禁编辑）；**③ API 层**——dimension/glossary 新增 `POST /{code}/submit`（写角色）、`/{code}/approve`、`/{code}/reject`（审核角色），`/{code}/publish` 收紧为 platform_admin 直发（measure 已收紧，本次对齐）；term `/submit` 语义改为审核提交（seed 改用 `/publish`），batch-submit 收紧 admin（批量导入直发通道）；**④ 前端层**——新增共享组件 `components/MasterDataReview.tsx`（`canReviewMasterData` 评审权判断 + `MasterDataReviewActions` 提交审核/通过/驳回按钮 + `MasterDataReviewModals` 提交/驳回 Modal + `useMasterDataReview` 状态 hook），MeasureCatalogs 重构复用、Dimensions/Glossary 新接入；三页 STATUS 补「审核中」标签与筛选选项，术语「再次发布」仅 admin 展示；**⑤ 语义边界**——审核流仅约束主表定义发布（成员/关系等子实体不受维度/术语审核态锁定，沿用各自发布粒度）。验证：后端 3 模块单测 161 passed（+4 维度 +2 术语审核流：submit/approve/reject/REVIEW 锁）+ 全量 3283 passed（3 失败 audit_i18n/collector_registry/feature_flags 既有非本次）；前端 54 文件 846 passed（+6 审核流：维度 3 + 术语 3）+ tsc 0 错 + ruff 全清 | /api/v1/dimensions、/api/v1/terms（新增 POST /{code}/submit、/{code}/approve、/{code}/reject；/{code}/publish 收紧为 platform_admin；/terms/batch-submit 收紧 admin） | dimension/glossary dev → implemented（审核流+共享复用模式落地） |
| 2026-08-24 | — | consume+notify+observability+search | **多角色优化剩余 4 项缺口补齐（响应时效 / 资产订阅 / 质疑闭环 / ES 落地）**：① **consume 响应时效 KPI**——新增 `query_log` 表（迁移 0080）+ 每次真实查询（consume/query 与内部查询）API 层计时 best-effort 落库（独立事务、失败 rollback 不阻断响应），`GET /consume/stats/response-time` 近 N 天 avg/p95/p99/max/error_count 分位聚合（MySQL 无窗口分位数，内存线性插值）；② **notify 资产订阅**——`subscription_pref` 加可空 `asset_type/asset_id`（迁移 0081，事件订阅与资产订阅二选一），upsert 校验资产存在（METRIC→metric_code、TABLE→entity_name），`publish_event` 从 payload 解析资产键（asset_keys/asset_type+asset_id/metric_code/table）合并资产订阅者（同用户×渠道去重、事件订阅优先），ddl_changed payload 补 asset_keys；前端订阅弹窗加「关注此指标（资产级变更提醒）」+ 订阅中心展示资产行；③ **observability 质疑闭环**——feedback 状态机加 `clarifying` 态 + `clarification/clarified_at` 列（迁移 0083，挂在并行 0082_measure_catalog_review 之后保单链），`POST /feedback/{id}/clarify` 仅提交人本人澄清（PLAT-2）后回 in_progress，resolver 可置待澄清；前端反馈中心「待澄清」处置 + 提交人澄清弹窗 + 已澄清标记；④ **search ES 落地**——`EsClient.create_index` 幂等建索引，新增 `EsIndexer`（metric_idx/term_idx 映射 + MySQL→ES 全量同步含逻辑度量/术语同义词），global_search 指标/术语检索 ES 优先（multi_match 相关度+fuzziness）禁用/异常/零命中降级 MySQL LIKE（TD §1.3 降级边界），`POST /search/indexes/ensure|sync` 管理端点；**验证**：后端 145+43 单测、前端 838/838 全绿、tsc 0 错、ruff 0、真实 ES 实测中文「门诊」相关度命中 + 同义词「支付」命中（13 指标/6 术语同步）；query_log/subscription_pref/feedback 迁移在 unisense_it 真库验证 | /api/v1/consume/stats/response-time、POST /consume/query（落库）、/notify/subscriptions（asset 维度）、/observability/feedback/{id}/clarify、/search/indexes/ensure|sync（新增） | consume/notify/observability/search 增强 |
| 2026-08-24 | — | measure_catalog+frontend | **度量目录补 REVIEW 审核态（方案 A：创建后不可直接发布，发布须先评审）**：用户诉求「创建度量后能否直接发布？为何无审核流程」——核实指标（metric）有完整审核流（DRAFT→REVIEW→PUBLISHED，评审指派/驳回原因/紧急发布），而度量目录/维度/术语是「发布式主数据」直接 DRAFT→PUBLISHED；但度量是**原子指标的权威继承源**（单位/格式/小数位/口径直接传播到下游指标，`measure_id` 属破坏性口径变更），且刚加了 AI 推断（LLM 产物未经确认即发布）——形成「基础层比上层更松」的治理倒挂，故补齐审核。① **模型+迁移 0082**——`MeasureStatus` 新增 `REVIEW`，加 9 个审核字段（submitted_by/approver_id/reviewer_id/reviewer_type/reviewer_domain/reject_reason/reject_reviewer_id/rejected_at/reviewed_at），存量状态不变；② **service 拆审核流**——`submit_measure`（DRAFT→REVIEW：越权守卫+口径完整性校验 stat_caliber 非空+评审指派 TD§13+清空历史驳回）、`approve_measure`（REVIEW→PUBLISHED：评审人身份校验+自审禁止管理员豁免+审核时间）、`reject_measure`（REVIEW→DRAFT：驳回原因/审核人/时间落库可追溯）；`update_measure` 加 REVIEW 锁定（审核中禁编辑，驳回后修改重提）；`publish_measure` 保留为**平台管理员直发通道**（系统/种子兜底，API 收紧 platform_admin）；③ **API**——新增 `POST /{code}/submit`（写角色）、`/{code}/approve`、`/{code}/reject`（审核角色 platform_admin/domain_admin/reviewer），`/publish` 收紧为 admin；④ **前端**——DRAFT 行「提交审核」按钮（Modal 填提交说明+可选评审指派）、REVIEW 行「通过/驳回」（canReview 判断评审权，对齐 MetricReview）、REVIEW 状态标签「审核中」+编辑锁定、驳回原因必填；⑤ **通知**——提交→通知评审人（指派者/域审核角色）、通过/驳回→通知提交人（best-effort 独立 session）；⑥ **维度/术语评估结论**：同为主数据直发，同样存在治理倒挂，但变更风险低于度量（维度成员可独立发布、术语为语义字典），建议下一阶段统一扩展（三模块共享同一审核模式，避免三套重复代码）。**验证**：后端 measure_catalog 42 项（+12 审核流：状态机/自审禁止/评审人身份 user+domain/驳回原因/REVIEW 锁定编辑）+ 全量 3262 passed（10 失败：audit_i18n/collector_registry/feature_flags 既有 + llm_config×6 顺序污染重跑全过 + notify×2 stash 验证非本次引入，均与本次零交集）；前端 MeasureCatalogs 8 项（+4 审核流）+ 全量 54 文件 836 passed + tsc 0 错 + ruff 改动文件全清 | /api/v1/measure-catalogs（新增 POST /{code}/submit、/{code}/approve、/{code}/reject；/{code}/publish 收紧为 platform_admin） | measure_catalog 保持 implemented（审核流增强） |
| 2026-08-24 | — | measure_catalog+frontend | **度量目录 AI 推断补全：描述词回填 + 源头系统/描述展示（尽可能借助 LLM 提效）**：用户诉求"统计口径/源头系统/同义词/描述词都应支持 LLM 推断"——核实现状：统计口径/源头系统/同义词**此前已由 LLM 生成并回填**（上轮 `auto-suggest` 落地），仅**描述词**是缺口——后端 `_suggest_by_llm` 早已生成「精炼描述」却未回填表单（值被丢弃）；本次 ① 前端 `handleSuggest` 回填 `description`（LLM 精炼描述，LLM 不可用/未生成时保留用户输入）；② AI 推断结果面板补齐「源头系统」「描述」两项展示（此前仅 7 项：编码/格式/分类/单位/小数位/口径/同义词）；③ 按钮文案明确「AI 推断（自动回填全部字段，可修改）」。设计不变式：枚举字段（编码/格式/单位/小数位/分类）仍以规则为准防 LLM 幻觉，语义字段（口径/同义词/域/源头系统/描述）全部走 LLM——这正是"尽可能借助 LLM 提效"且不牺牲可靠性的平衡。**验证**：MeasureCatalogs 4/4（新增描述词回填断言 + 提交携带 description）+ 前端全量 54 文件 832 passed + `tsc --noEmit` 0 错 | 无接口变更（纯前端回填与展示补全；后端 `POST /measure-catalogs/auto-suggest` 的 `description` 早已返回） | —（measure_catalog 保持 implemented） |
| 2026-08-24 | — | semantic+lineage | **deprecate 被引用拦截（P2 加固）**：`deprecate_metric` 废弃仍被活跃引用的指标时（DERIVED_FROM 派生 / CONSUMED_BY 报表），未指定替代指标即拦截（`METRIC_REFERENCED` 列出引用者），引导先指定 successor 或处理下游——与发布端 DependencyChecker 反向保护互补，杜绝静默悬空下游；`LineageRepository.metric_referrers` 单指标引用者查询（source_node 等值 + 存活 + 类型过滤）。验证：test_semantic_service 17 deprecate 用例（+3 拦截/放行）+ test_lineage_repository 66 passed + ruff 改动文件 0 错 | 无接口变更（deprecate 行为增强） | —（semantic 保持 verified，待人工 ratify） |
| 2026-08-24 | — | measure_catalog+semantic+frontend | **度量目录从电商演示全面医疗化（HIS 门诊实际场景）+ 目录支持 AI 推断**：① **数据模型加字段**——`measure_catalog` 新增 `category`（度量分类：FLOW/FEE/DRUG/MEDICAL_INSURANCE/EFFICIENCY/QUALITY/OTHER）+ `stat_caliber`（统计口径文本），迁移 0079，schemas/service/API/前端表单与列表列全链路贯通；② **主题域与字典医疗化**——`seed_domains_dicts.py` 电商域（sales/finance/user/product/marketing/logistics）切换为医疗域（门诊/药品/医疗费用/医保/诊断/质控/患者），业务实体粒度（订单/商户/门店/商品/SKU）切换为挂号/就诊/患者/医生/科室/病种/处方/药房/医保结算；③ **E2E 造数全套医疗化**——`seed_e2e_data.py` 9 个医疗逻辑度量（门诊挂号/就诊/收费/药品费用/处方数/医保结算/报销比例/次均费用/药占比，含 category/stat_caliber）+ 9 原子/派生指标 + HIS 门诊目录表（ods_his_register/receipt/prescription/drug/yb_settle）+ 医疗术语/科室维度/冲突/血缘/质量/治理/消费/埋点，SMOKE 断言同步；④ **测试引用同步医疗化**——后端 5 文件 + 前端 6 文件 E2E 电商编码（sales_e2e_gmv_day/ods_e2e_order→outp_e2e_fee_day/ods_his_receipt）对齐（独立单元测试 mock 基建 conftest 的 sales_gmv_* 保留）；⑤ **度量目录 AI 推断**——新增 `POST /measure-catalogs/auto-suggest`：输入名称/描述/域/源表，规则确定性兜底（编码 slug/格式/单位/小数位/分类关键词命中+医疗源头系统）+ LLM 业务增强（同义词/统计口径/业务域白名单校验/描述），逐字段带 source(llm|rule)/confidence/reason，LLM 不可用自动降级规则不阻断；前端新建弹窗「AI 推断」按钮回填全部字段+结果面板标注来源与置信度。**验证**：后端全量 3201 passed（3 失败为既有/并行：audit_i18n/collector_registry/feature_flags，stash 验证非本次）；前端全量 824 passed（54 文件，含新增 MeasureCatalogs 4 项）+ tsc 0 错；ruff 改动文件全清 | /api/v1/measure-catalogs（新增 category/stat_caliber 字段）、POST /measure-catalogs/auto-suggest（新增推断端点） | measure_catalog dev → implemented（医疗化+AI 推断落地） |
| 2026-08-24 | — | lineage | **源表 DDL 变更→受影响指标 Owner 定向通知端到端验收（P1 补齐）+ 集成测试基建修复**：① 新增 `test_ddl_rename_notifies_affected_metric_owner` 真实 MySQL 端到端用例——预置发布态指标 + `table:src → metric:code` 上游血缘边，`ALTER TABLE ... RENAME TO` 走 `parse_and_store` → 提交后副作用 `_notify_ddl_change` → `affected_asset_owners` 下游收集指标 Owner → `NotifyService.notify_user` 落库 Notification（template_code=lineage.ddl_changed，channel=IN_APP），断言「血缘变更」标题，闭环此前仅组件级覆盖、无端到端验收的缺口；② 修复集成测试 alembic 迁移库 `alembic_version.version_num` 列宽不足（迁移文件名 0066 长 34 字符，默认 VARCHAR(32) 报 Data too long，对齐主库手扩 VARCHAR(64)）——`_reset_via_alembic` 预建加宽版本表。验证：test_lineage_integration 2 passed（真库）+ ruff 0 | 无接口变更（测试 + 测试基建） | —（lineage 保持 released） |
| 2026-08-24 | — | frontend | **指标可信度仪表盘上主 Dashboard（可信度 P0，T2）**：`Dashboard.tsx` 新增「指标可信度」卡——复用可观测中心聚合端点 `/observability/overview quality.metric_health`（与可观测中心同源同口径，避免重复实现评分），展示健康度四档分布（EXCELLENT/GOOD/WARNING/CRITICAL 红黄绿 pill）+ 健康覆盖率 + 已评分项 + 绿档可信率 + 低健康 Top（按评分升序，点击进可观测中心）；失败静默降级（卡片隐藏不阻断其余读数）；新增 Dashboard 测试断言可信度卡渲染。验证：Dashboard.test 30 passed + tsc 0 | 无接口变更（复用 /observability/overview） | —（frontend 无独立状态） |
| 2026-08-24 | — | frontend | **详情页「让人信」三件套（P1，T5）**：① 详情页第一屏 HealthCard 下方补「最近校验」时间（health.calculated_at，formatCnTime 上海时区，空健康静默降级）；② 详情页头部新增「试算」按钮——`navigate(/query?metric_code=xxx)` 跳查询工作台，`QueryWorkspace` 新增 `useSearchParams` 读 `metric_code` 初始化指标选择（仅挂载读一次，不破坏现有逻辑）；③ 审计页包 Tabs 新增「合规报告」Tab——基于现有 audit API 前端聚合「敏感数据访问（按操作人：谁访问过/次数/最近）」+「审计导出记录」，失败空数组静默降级。验证：MetricDetail/QueryWorkspace/AuditLog 三文件 77 项全过 + tsc 0 | GET /audit（前端聚合复用，无新增端点） | —（frontend 无独立状态） |
| 2026-08-24 | — | semantic+lineage+consume | **指标复用度分析 + 资产账本（P0 数仓 / P1 管理者，T3）**：① `LineageRepository.metric_reuse_counts` 按 source_node=`metric:*` 聚合存活边（DERIVED_FROM 派生引用 / CONSUMED_BY 报表引用，stale/deleted 过滤 + target 去重）；`MetricStatsService.reuse_summary` 输出每指标 derived/consumed/reuse_count 按复用度降序；`GET /api/v1/metric-definitions/stats/reuse`；② `asset_ledger` 复用 HealthScorer 活跃度维度（近 30 天无更新 `_ACTIVITY_STALE`）+ 零引用判定僵尸，返回 active/zombie/duplicate 计数 + 僵尸明细（最后更新/被引用次数）+ SAME_DEF_DIFF_NAME 重复建设信号（低耦合读 pending_conflict_detail）；`GET /api/v1/metric-definitions/stats/ledger`；main.py 注册路由。验证：test_metric_stats_{reuse,ledger} 9 passed + ruff 0 + ASGI 冒烟 200 + 空数据降级 | GET /api/v1/metric-definitions/stats/reuse, /stats/ledger（新增） | semantic production-hardened（统计端点） |
| 2026-08-24 | — | semantic | **ratify 卷宗 semantic 节刷新（P0 定稿材料）**：D10 ratify 卷宗 3.3 节更新至 2026-08-24 最新证据——门禁复审全绿（unit 360 + security_reverse 5/5 + 语义 live P95=131.63ms）+ 批次 B 已提交 5b8149b + 双视角 0 High，仅余 §1.5 人工 ratify 待人类核验签收 | 无接口变更（纯文档） | —（semantic 保持 verified，待人工 ratify） |
| 2026-08-24 | — | search+semantic | **全局检索接入同义词匹配（说人话 P0，T1）**：① `GlobalSearchRepository._search_metrics` 外连接 `measure_catalog`（`metric.measure_id`），`synonyms` 列参与参数化 LIKE（escape `/` 防注入），新增 `match_reason`（`field`/`synonym`）供前端「您是不是想找…」提示，CASE 保证直接命中优先于同义词；② `_search_terms` 追加 `term.synonyms`（JSON）粗匹配；③ 单测 4 新增（synonym 命中回传 match_reason、SQL 外连接/ESCAPE 断言、CASE 优先级 field 先于 synonym、术语同义词匹配）共 14 passed + ruff 0 | GET /api/v1/search（指标/术语命中范围扩展，行为增强） | —（search 无独立状态，随 semantic 批次） |
| 2026-08-24 | — | semantic+frontend+quality+governance+notify | **上生产风险报告（docs/上生产风险报告-2026-08-24.md）+ 状态同步**：报告基于实证——① 语义 live 性能基线 `semantic_perf_baseline_live_2026-08-21.txt`（10 VU/30s，P95=131.63ms、fail_rate=0.00%、1920/1920 checks）；② Doris 下推链路 `consume/olap_executor.py:49 _to_doris_sql` + `consume_perf_2026-08-09.txt`（dry-run 200、80/80、fail 0.00%，P95=1.73s 标注为本地 DB 往返非回归）；③ 安全反向 `backend/tests/security/` 30 文件 198 用例实扫；④ semantic 13/13 门禁 + 双视角 0 High（`semantic_gateways_2026-08-17.txt`）。逐项覆盖本批 gap-fill 补齐项上线风险点与缓解（T1 检索 synonyms 接线 / T2 Dashboard 可信度 / T3 统计端点 / T4 质量对账调度+词根硬校验+术语强制 / T5 治理批量迁移+deprecate 被引用拦截+按资产订阅+质疑闭环），每项给出「风险→缓解→回滚预案」+ 监控告警建议表 + 发布回滚预案（feature flag 逐项独立开关 + `alembic downgrade -1` + `kubectl rollout undo`）+ 待人工 ratify 清单（semantic verified 及 12 released 模块 §1.5 待签）。同步更新 module-status.yaml semantic 追加 note_20260824_b | 无接口变更（纯文档） | —（semantic 保持 verified，集成验证待 T1-T5 提交后补记） |
| 2026-08-24 | — | frontend (metric create) | **指标注册：未采集表/字段手动输入（方案 A，衔接"注册时补齐上下游表"）**——**用户诉求：注册指标时表/字段尚未被采集进平台也要支持手动输入，LLM 推断回填同样支持**。后端无改动（`source_tables`/`downstream_tables`/`source_table`/`measure_column`/`mount.*` 均无"必须已采集"校验，落库天然接受）。① **7 处表/字段选择控件统一支持**（自动推断源表名/度量列/挂载源表/挂载度量列/依赖表-上游/使用表-下游/批量注册源宽表）：搜索关键词在已采集选项里无精确匹配时，下拉顶部注入「xx（未采集，手动输入）」选项——antd 5.22 `optionRender` 橙色标注提示，点选即录入，**选中后 tag/选择框仍是干净表名**（value 即完整表名，不污染提交 payload）；② **度量列取消"无列信息即禁用"**（消除未采集源表下无法输入列名的死锁，placeholder/notFoundContent 引导"可直接输入列名"）；③ **LLM 推断回填补 options**：`applySuggestion` 将推断出的依赖表/使用表补进 `tableOptions`、度量列补进 `columnOptions`（推断出的未采集表/列下拉不再空白，可显示可再选）；④ 新增 `withUncollectedOption`/`tableOptionRender` 公共 helper，7 处 Select 独立关键词 state（避免共用 onSearch 相互覆盖）。验证：MetricCreate 30 项（新增 4 项未采集手输用例：依赖表/使用表/度量列/批量弹窗源宽表）+ 前端全量 53 文件 820 passed + `tsc --noEmit` 0 错。 | 无后端接口变更（纯前端 MetricCreate.tsx + MetricCreate.test.tsx） | —（metric create 保持 released，纯前端注册体验增强） |
| 2026-08-24 | — | collector+assetmap+frontend | **描述缺失治理工作台统一（方案A）**：① 抽取共享组件 `components/DescriptionCoveragePanel.tsx`（原 `assetmap/DescriptionCoverageTab.tsx` 重构，`variant="full"|"summary"` 双模式 + `onGovern` 回调）；② **采集目录（collector）**嵌入完整治理面板（full）——统计卡可下钻 + 按表列缺失字段数治理表格 + 独立治理抽屉（表级/字段级人工编辑与 LLM 推断），替换原静态统计卡；③ **资产地图（assetmap）**「描述缺失」tab 精简为只读总览（summary）——保留 4 张统计卡 + 下钻明细，治理动作经「前往采集目录治理」/明细行点击跳转 `/catalogs?focus=<表名>&from=资产地图`（复用既有 from 精确回跳）；④ 测试迁移：AssetMap 描述缺失 9 项治理测试改写为 summary 行为 + 迁移至 Catalogs（新增统计卡下钻/治理表格/治理抽屉表级+字段级编辑/明细下钻到治理 6 项），AssetMap 59 + Catalogs 27 全绿，前端全套 816 通过 + `tsc --noEmit` 0 错 | 无后端接口变更（仍用 GET /api/v1/catalogs/description-coverage 与既有 update/infer 端点） | —（collector/assetmap 保持 released，纯前端展示层重组） |
| 2026-08-24 | — | semantic | **定稿推进：批次 B 遗留改动提交 + implemented → verified**：① 复核并提交 api/semantic.py 批次 B 13 行改动——`create_template` commit 撞唯一键 `IntegrityError` → 回滚会话 + 转 `ConflictError`(error_code=TPL_EXISTS，与预检分支同码/同文案)，消除并发下 select 预检失效导致的裸 500；ruff 修复 B904（`raise ... from exc`）；补 `test_create_template_commit_integrity_error_maps_conflict` 锁定该路径（commit 5b8149b，仅含 semantic 服务 2 文件）；② semantic 相关单测 360 passed / ruff 0 / 改动行 mypy 无新增错误；③ evidence 复核通过——`backend/tests/reports/semantic_gateways_2026-08-17.txt`（5026B 真实非空）+ `semantic_perf_baseline_live_2026-08-21.txt`（P95=131.63ms）均存在；gateways_passed 13/13 + post_verify_review（2026-08-11 独立复核 0 High）齐备，满足 DEV_GUIDE §3 verified=implemented+全部门禁绿+独立复核通过，module-status.yaml semantic `status: implemented → verified`（updated 2026-08-24）。仅余 §1.5 人工 ratify（released 门槛，agent 不可代签） | /api/v1/semantics/templates（create_template 异常映射，行为增强） | implemented → verified（定稿推进） |
| 2026-08-24 | — | semantic+lineage+frontend | **指标注册补齐上下游表（方案A：注册时完善 上游依赖表 + 下游使用表）**：① **口径 JSON 扩展**——`definition_json` 新增 `downstream_tables`（下游使用表/消费方），`_validate_definition_json` 提取 `_normalize_table_list` 与 `source_tables` 统一去重规范化；② **血缘差异同步**——`sync_metric_table_edges` 加 `downstream_tables` 参数，落地表与下游使用表合并进 `current_down` 参与增删差异同步（编辑增删消费表不残留旧边），`register_metric_from_definition` 读取并传入；③ **Neo4j 同步**——`parse_metric_edges` 补 `downstream_tables` → `metric→table` 边（与落地表同向）；④ **auto_suggest 修混向 bug**——`edges_for_node` 按 upstream/downstream 拆分：源表上游邻居→`source_tables`（依赖表）、下游邻居→`downstream_tables`（使用表），metric 边过滤，兼容字段 `related_tables`=两者并集（此前 direction="both" 把源表下游消费表误塞进 source_tables）；⑤ **前端向导**——挂载实体表 label 显著化「指标的家」（OneData 挂载层 + 通俗解释）、Step③ 卡顶 Alert 三类表关系通俗提示、「关联数据表」拆为 **依赖表（上游）+ 使用表（下游）** 两个多选、推断按方向回填、提交 payload 双表写入、`DEF_FIELD_LABEL` 标签明确化。**验证**：后端相关 6 文件 223 passed + 全量 3188 passed（3 失败为并行会话/既有：audit_i18n/collector_registry/feature_flags，stash 验证非本次引入）；前端 MetricCreate 26/26（含方向拆分新用例）+ 全量 811 passed（2 失败 Catalogs 相对时间为既有）+ tsc 0 错误 | /api/v1/metric-definitions/auto-suggest（source_tables/downstream_tables 返回）、POST/PUT metric-definitions（downstream_tables 入口径） | semantic+lineage production-hardened |
| 2026-08-18 | — | assetmap+lineage+frontend | **资产洞察残余加固（服务端分页落地 + 请求竞态 + 统计真实化）**：① **P2-1 service/API 分页适配**——`service.list_tables`/`orphan_assets` 解包 repository 的 `(rows, total)` 返回真实 total + offset 透传，`/tables`、`/orphans` 加 `offset` 参数（修复孤儿接口 500 `'list' object has no attribute 'to_dict'`）；② **前端服务端分页**——OrphansTab/TablesTab 从 `limit:200` 静默截断改为按页请求（`offset=(page-1)*pageSize`）+ antd Table `current/total/onChange` 服务端翻页，统计条用真实 total（孤儿/PII 孤儿/机密级孤儿分别来自 orphans total + `/pii` unowned_pii + CONFIDENTIAL 过滤 total）；③ **请求竞态令牌**——血缘图谱（LineageView load）、资产图谱（AssetMap loadGraph）、PII 列表（PiiTab loadList）加 `useRef` 序号令牌，快速切换 provenance/域/PII 开关/筛选时旧响应不覆盖新数据（此前旧响应覆盖导致展示错误）。**验证**：AssetMap 61/61 + assetmap service/security 53/53 + ruff 全清；生产 API 实测 orphans total 404/offset 翻页生效/tables total 405；浏览器实测 8/8 PASS（孤儿统计条 404/305/10 + 分页 21 页 + console/page 0 错误） | /api/v1/assetmap/tables、/orphans（新增 offset） | assetmap+lineage production-hardened |
| 2026-08-18 | — | assetmap+lineage+frontend | **工业级生产就绪三批加固（P0/P1/P2）**：① **P0 合规**——PII 端点权限收紧（`/pii`、`/pii-assets`、`/pii-export.csv` 从任意登录 → 治理域角色 `_PII_READ_ROLES`，viewer 403）+ 补 `pii_access` 审计（`asset.pii_view/list/export`）；血缘软删复活 `_revive_or_insert`（唯一索引不含 deleted_at 的 1062 根因）+ 提交后副作用队列 `run_post_commit`（图写/缓存失效/事件延迟到 commit 后，消除幽灵边）；前端全站 ErrorBoundary + 33 页面 React.lazy 代码分割（防整站白屏/首屏包）+ 脱敏回显括号修复（防覆盖原策略）+ 图谱 tooltip 节点名转义（防存储型 XSS）；② **P1 安全**——组织隔离（`data_source.org_id` + 迁移 0072 + 资产读取路径 org 过滤，platform_admin 豁免透传 None）、`/scan` 沙箱（realpath + 符号链接逃逸 + 5MB 文件上限）、Neo4j 查询超时 timeout=10、边级操作域归属校验（防 IDOR）、乐观锁（迁移 0073 + 4 写方法条件 UPDATE 409）；③ **P2 性能**——`list_tables`/`orphan_assets` 真实分页（total+offset）、PII 类别过滤前置到分页前、PII 列表批量消除 N+1、聚合缓存写后失效、枚举过滤器校验、导出上限 5000。**验证**：后端单测 assetmap 117 + lineage/规则/治理 279 + dimension/semantic/collector 309 全过，ruff 全清；浏览器实测登录→资产地图→PII Tab 渲染正常、console/page 0 错误；生产 API 实测 admin /pii 200（豁免后 305 PII 可见）、无 token 401、迁移 0072/0073 应用成功 | /api/v1/assetmap/pii-assets、/pii-export.csv（权限收紧）、/classification/rescan（source_ids 多源） | assetmap+lineage production-hardened |
| 2026-08-17 | — | semantic+ai+security | **第三轮审查 P1-P4 增强实现 + security 测试债务修复**：① **P1 健壮性**——血缘注册 savepoint 隔离（metrics.py L3 + service.py 完整血缘均用 `begin_nested()`，失败只回滚 savepoint 不污染外层业务事务，杜绝 PendingRollbackError）；批量端点审计动作区分部分失败（`_batch_audit_action`：BATCH_*_PARTIAL/_FAILED，此前部分失败仍记成功动作误导审计）；② **P2 体验**——审批页搜索/域筛选 URL query 同步（刷新/分享保留筛选视图，replace 写回）；详情页删除 DRAFT 后提示恢复路径（已归档视图）；批量注册→批量提交评审支持评审指派（域评审组默认/指定用户，透传 reviewer_type/id）；③ **P3 测试补齐**——check_dsd_overdue 超 7 天升级提醒（P3-14）、_run_batch DB 异常回滚+逐条容错（P3-18）、紧急补审闭环前端测试（P3-15）、批量提交评审前端测试（P3-16）；P3-17（TodoCenter DSD 待办）核实已有覆盖、P3-19（E2E SKIP_APPROVAL_FLOW）核实无此环境变量（误报）；④ **P4 核验**——_assert_role 已不存在（已解决）、quickbi 已集成（误报）、description_source 的 "schema" 枚举值为无害保留（DB 枚举，不强制迁移）；⑤ **security 债务 6 项修复**——AI 危险问句在构建 LLM 客户端前拦截（AiService._is_unsafe 改 staticmethod + nl2sql 端点前置守卫，未配置 LLM 时危险 NL 仍被拒 UNSAFE_QUERY）；test_api_metrics 改 mock get_version_responses；test_security_reverse 配置 execute 同步 scalars().all()（AsyncMock 协程陷阱）；test_notify_security 改 patch list_notifications_page；test_collector_security 以平台管理员执行（FR-035 角色收窄）；test_assetmap_security fake_export 兼容新增 domain 过滤参数。**验证**：后端单测 3197 passed + security 187 passed（2 项 connector 失败为并行会话在途 worker.py 改动引入，非本次）；前端 vitest 764 passed + tsc 0（2 项 SystemConfig 失败为并行会话 LLM 路由配置在途）；ruff 改动文件全清 | /api/v1/ai/nl2sql（危险问句前置拦截）、/api/v1/metric-definitions/{code}/versions（测试对齐） | semantic verified（增强修复） |

| 2026-08-17 | — | sensitive_rules (backend+frontend) | **敏感规则模块交互增强（测试台联动 + 重扫多源 + 批量操作 + 规则搜索）**：① 规则测试台弹窗「表/视图」「字段名」改为**联动下拉**——表/视图选自已采集资产（fetchAssetTables），选表后按实体详情 schema 联动加载字段（fetchAssetEntityDetail），字段名下拉带注释展示；② 「按新规则重扫」弹窗数据源改为**多选下拉**（listDataSources，支持留空=全部），后端 `classification_rescan` 新增 `source_ids` 多源参数（source_id 单源保持兼容，去重校验）；③ **批量编辑/启停**——表格行勾选（rowSelection）+ 批量启用/停用（`POST /sensitive-rules/batch-status`，逐条容错≤100 条，审计留痕）+ 批量置信度 Slider 弹窗（`POST /sensitive-rules/batch-confidence`，保留其他字段）；④ **规则搜索**——按规则名/标识/类别标签/正则关键字实时过滤；前端 SensitiveRules 测试新增 5 项（搜索/批量停用/批量启用/批量置信度/重扫多选），后端 84 单测 + 前端 761 全量测试全绿，tsc 全清，浏览器实测 12 项全过 | /api/v1/sensitive-rules/batch-status, /batch-confidence, /api/v1/classification/rescan（source_ids） | released（交互增强） |
| 2026-08-17 | — | collector+governance+frontend | **敏感规则可视化配置台（规则引擎可配置产品化）**：① 后端**覆盖语义修正**——`rules.py` 重构为合并语义：DB 规则按 rule_id **覆盖同 ID 内置、其余回退内置**（自定义不再吞内置），inactive 项整体停用，`pii=false` 机密规则可 DB 配置（补 `0070` 迁移种子 password/tax/business 3 条机密规则）；② 新增 `GET/POST/PUT/PATCH/DELETE /api/v1/sensitive-rules`（列表合并内置+DB 含来源/状态、结构化创建/更新/启停/删除）+ `/categories`（15 类）+ `/validate-regex`（正则合法性校验）+ `/test`（规则测试台，用当前生效规则模拟识别）；③ `classification_rescan` 改**加载 DB 规则**（配置台改规则后重扫即时生效）；④ 前端系统管理新增「敏感规则」配置台（`/sensitive-rules`，RequirePerm sensitive-rules:view）：类别/规则/正则/样本/置信度/来源/状态列 + 生效概览卡 + 结构化编辑弹窗（类别下拉/正则即时校验/置信度 Slider/PII vs 机密切换/编码自动生成）+ 规则测试台（模拟识别实时反馈命中类别/规则/置信度/匹配途径/最终敏感级）+ 按新规则重扫；⑤ 注册 UI 权限点 sensitive-rules:view/edit（platform_admin/domain_admin/compliance_officer 默认集）；后端 2961 单测 + 前端 743 测试全绿（新增后端 15 + 前端 8），tsc/ruff 全清，浏览器实测 20 项全过 | /api/v1/sensitive-rules/*, /api/v1/classification/rescan | released（功能增强） |
| 2026-08-17 | — | semantic+collector+consume+glossary+governance | **指标模块全维度审查修复（P0-P2 全量落地，P3 验证）**：① **P0 安全**——mark-source-dropped 越权收紧（仅管理角色，P0-1）；suggest-rename/auto-suggest LLM 额度防护（读角色收紧为写角色）+ auto-suggest 显式 schema 防非字符串 sql→500（P0-2）；批量端点异常信息脱敏（public_error_message：业务消息透传、未知异常泛化防泄漏，P0-3）；② **P1 闭环治理**——collector 全量采集检测源表 DROP 沿血缘置下游指标 DSD（PRD R3-04④ 接线，mark_source_dropped 支持按表名精确过滤，P1-4）；每日健康任务 WARNING/CRITICAL 定向通知 Owner+备份 Owner（P1-5）；紧急发布补审闭环（emergency-review 端点写 emergency_reviewed_at + 前端补审入口，P1-6）；灰度超期强制回收（状态机新增 expiry_recycle EXPERIMENTAL→DRAFT + metric.gray_recycled 事件 + reject 通道收紧仅 REVIEW，P1-7）；合并 PendingVersionManager 双实现（删除 manager 死方法，规则唯一实现在 MetricService，P1-8）；③ **P2 体验/一致性**——前端发布语义统一（删 publishMetric 死代码）+ 批量提交走原子 /batch-submit（P2-9）；rollback/promote 加确认弹窗（P2-10）；术语绑定写路径（PUT /{code}/term 写 metric.term_id + 前端搜索绑定/解绑）+ GlossaryConflict.ref_metric_id 填充（P2-11）；健康度质量维度接入 quality_event 近30天异常反比扣分（P2-12）；文档漂移修复（TD 检索改 MySQL LIKE、TD §12.3 健康度模型对齐实际）+ 前端门禁落地（CI frontend-gateways job：tsc+vitest，P2-13）；前端重复常量收敛（STATUS/EDGE 标签归入 enums.ts，P2-14）；④ **P3 验证**——补齐生命周期零 API 测试（test_metric_lifecycle_api.py 12 项 + auto-suggest/emergency-review/term-bind 契约测试）。本会话新增后端单测 40+ 项、前端 MetricDetail/MetricCatalog 测试更新；后端全量单测 2911 passed（7 失败均为并行会话在途/HEAD 既有：list_catalogs×3 + semantic_cache×4，非本次改动引入；已由遗留清理 ce3aff0/9ac9a33 修复，复审后全量 2939 passed/0 failed）；前端 tsc 0 错误 + vitest 753 全绿（MetricDetail 49 项含 P2-10/11 断言，编辑弹窗辅助数据加载防御式修复） | /api/v1/metric-definitions/*（emergency-review/term/mark-source-dropped 等）, /api/v1/data-sources/*, /api/v1/terms/* | semantic verified（增强修复） |
| 2026-08-17 | — | semantic+notify+collector+consume+glossary+governance | **复审 A-F 类问题全量修复（P0-P2 复核后残留）**：① **B 类闭环断线**——DSD 通知闭环（metric.source_dropped/source_recovered/emergency_reviewed 注册消费者+模板+定向通知 Owner，consumers.py/TODO_EVENT_TYPES/_EVENT_TITLE_CN/前端 Notifications 全链路）；DSD 待办落地（TodoCenter 新增「数据源下线」待办按 Owner 维度 + 每日 7 天超期巡检 check_dsd_overdue 升级提醒）；灰度回收前端渲染补齐（gray_recycled 标签/影响/待办/订阅）；血缘影响通知接线（collector schema drift 触发 notify_lineage_impacted_owners，兑现 docstring 声称触发点）；② **C 类健壮性**——批量端点 DB 异常回滚（_run_batch 助手：SQLAlchemyError 回滚会话+中止剩余项返回部分成功，batch-submit/approve/reject/deprecate/register 全接入）；僵尸方法清理（review_metric/publish_metric/_publish 删除，测试改生产方法 approve/reject 断言）；血缘注册失败告警（lineage.metric_register_failed 事件进通知闭环，替代静默吞异常）；③ **D 类体验/维护**——批量注册结果直达「批量提交评审」（复用 /batch-submit）；详情页 DRAFT 删除入口（新增 metric:delete 权限点仅平台管理员+确认弹窗+回收站可恢复）；审批页搜索/域筛选 + 目录页责任人筛选控件；死代码清理（_selectedTableCatalog 只写不读）；④ **E 类测试/门禁**——8 条零 API 测试路由补测（description/submit/approve/reject/confirm/reject/extend-version/health）；E2E 审批动作断言（通过/打回闭环）；P2-10/11 前端断言（promote/rollback 确认弹窗 + 术语绑定写路径）；编辑弹窗辅助数据加载防御式降级（不再因 listMetrics 等 mock 缺失崩溃详情页，顺带修复 DRAFT/维度预存失败）；⑤ **F 类核验**——sensitive_rules 部署风险已消除（并行会话已提交跟踪）。**验证**：后端全量 2939 passed/0 failed（语义域 405 项）；前端 vitest 753 全绿 + tsc 0；evidence 更新 semantic_gateways_2026-08-17.txt；module-status semantic verified_at 2026-08-17 | 无新增接口（行为增强：DSD 通知/补审通知/血缘失败告警事件进入订阅链） | semantic verified（复审增强） |
| 2026-08-17 | — | observability (backend+frontend) | **可观测中心最近质量事件信息丰富度与可读性增强**：① 后端 `quality_events` 补全字段——由 `level/status/metric_id/created_at` 四字段扩展为：规则类型（rule_type）、观测值/阈值（obs_value/threshold）、指标名与编码（批量 IN 查询 Metric 关联，消除前端 N+1 探测）、处理留痕（ack_note/ack_by/ack_at/resolved_by/resolved_at/closed_by/closed_at）、修复建议（repair_suggestion）；② 前端卡片重写——每事件一行：严重级别 + 规则类型中文（RULE_TYPE_LABEL）+ 状态中文 + 指标名（hover 显示编码，无名称回退 `指标 #id`）+ 上海时区相对时间；观测值/阈值非空时第二行展示「观测值 / 阈值」（P0/P1 标红）+ 处理说明（ack_note）。验证：observability 后端 22 项全绿；前端 Observability 4 项全绿（新增规则类型/指标名/观测值断言）；tsc 我的文件 0 错误 | GET /observability/quality-events（响应字段扩展，向后兼容） | released（展示增强） |
| 2026-08-17 | — | assetmap | **PII 合规增强全量落地（A/B/C）**：① A 信息丰富度——详情抽屉字段级 PII 命中明细（列名/类别/规则/置信度/误报标注）、`GET /assetmap/pii-assets` 明细列表（分页+关键字/数据源/域/责任人/复核状态/类别六维筛选）、概览风险卡（无主/待复核/已复核 PII + 字段类别分布）、`GET /assetmap/pii-export.csv` 盘点导出（含字段明细/复核/脱敏状态）；② B 合规工作台——目录资产表级复核 `POST /entities/{id}/review`（APPROVE/REJECT + 禁自审职责分离）、脱敏策略设置 `POST /entities/{id}/masking`、字段级误报标注 `pii-overrides`（upsert/撤销，`pii_field_override` 表）、行业分级模板 `GET /pii/templates` + `POST /pii/templates/apply`（个保法敏感个人信息/金融行业/标准）；③ C 治理深度——敏感规则引擎类别化重构（12 类 PII + 3 类机密，classifier.py 单规则源，policy.py 委托避免双引擎漂移）、规则经 system_dict 可配置（pii_category/pii_rule 种子，采集时 DB 覆盖内置）、保留期/合法性字段（retention_days/legal_basis/expires_at/到期提醒）、消费链路数据值脱敏（masking.py：hash/mask/deny，指标查询与快照返回应用）；④ 迁移 0067（db_catalog 合规/脱敏/保留期 8 列 + pii_field_override 表 + 字典种子）；⑤ 采集随分级写 classification.pii_columns 字段明细。后端 2926 单测 + 前端 735 测试全绿，tsc/ruff 全清 | /api/v1/assetmap/pii, /pii-assets, /pii/templates, /entities/{id}/review, /masking, /pii-overrides, /retention, /pii-export.csv, /api/v1/consume/* | released（功能增强） |
| 2026-08-17 | — | assetmap | **数据表目录多维度过滤 + 孤儿资产增强（认领闭环 + 过滤筛选 + 合规统计）**：① 后端 `list_tables/orphan_assets` 支持 keyword/source_id/domain/entity_type/sensitivity/schema_status 过滤（orphan 无参调用兼容概览下钻）；`GET /assetmap/orphans` 增加过滤 Query；② 前端 `TablesTab` 增加多维度筛选栏（关键字/数据源/业务域/责任人/敏感度/Schema）+ 列增强（源名称/域/责任人/Schema/更新时间）+ 治理设置（责任人/敏感度）+ 实体详情抽屉 + 导出透传过滤；③ `OrphansTab` 从纯表格升级为「孤儿消化工作台」：多维度筛选栏、合规统计条（孤儿总数/PII/机密级孤儿）、单行认领（归属当前用户）/批量认领（给我）、转交 Modal（指定责任人）、实体详情抽屉（认领前查看 schema/PII/血缘）、认领后自动刷新；后端 98 单测 + 前端 57 AssetMap 单测全过，tsc 全绿 | /api/v1/assetmap/tables, /api/v1/assetmap/orphans | released（功能增强） |
| 2026-08-06 | — | (全体) | 建立交付追踪底座：module-status.yaml / gateways.yml / pre-commit / 校验脚本 / TD §19§20 | — | planned → planned |
| 2026-08-06 | — | (全体) | 修正 FR 编号与 td_section 映射：对齐 TD §2.1 权威 FR 和 TD §12.x 实际章节号（9 服务 FR 修正 + 6 服务 td_section 修正）；修正 contract_check.py TD_SERVICE_SECTIONS；新增 .gitignore / .env.example / docker-compose.yml / Makefile / README.md；DEV_GUIDE 补充前端/DB变更/API版本/分支策略/配置变更/安全/LLD 开发规范（§8-§14） | — | planned → planned |
| 2026-08-06 | — | (全体) | 修复 GitHub 主分支 main→master 统一：DEV_GUIDE §6/§11（4处）、TD §19.6、.github/workflows/gateways.yml CI 触发分支 | — | planned → planned |
| 2026-08-06 | — | (全体) | DEV_GUIDE 开发规范全面补充（13 维度）：§5 提交体规范+service白名单+Co-authored-by；§6 Review checklist+SLA+PR大小限制+reviewer指派；§8a 后端代码风格（命名/格式/API路径/DB对象）；§8b 目录结构规范（backend/frontend布局）；§11 合并策略+分支生命周期+冲突解决+hotfix/release流程；§12 配置加载框架+多环境管理+环境变量清单+Secrets管理；§13 CORS/CSRF/文件上传/限流/依赖扫描/密钥轮转；§15 异常分层体系+统一错误响应+重试策略+日志级别映射+错误码管理；§16 structlog框架+敏感过滤+采样策略+必记操作；§17 N+1/缓存/异步/Neo4j/前端性能+profile方法论；§18 Poetry依赖管理+版本范围+审批流程+Renovate更新策略+禁用清单；§19 测试文件组织+mock规范+fixture规范+测试标签+E2E+性能测试；§20 docstring/API文档/Runbook模板/ADR/文档评审 | — | planned → planned |
| 2026-08-06 | — | (全体) | 基础设施文件与规范一致性修复：.env.example 环境变量全部改为 UNISENSE_ 前缀（对齐 §12.2/§12.4）+ 补全缺失变量（UNISENSE_ENV/OTLP_ENDPOINT/KMS_KEY_ID/LLM_DEFAULT_MODEL）；docker-compose.yml 删除废弃 version 字段 + 环境变量引用改为 UNISENSE_ 前缀 + ES 版本 8.12.0→8.15.0（对齐 TD §1）；Makefile pip 命令改为 poetry run（对齐 §18.1）；新增 pyproject.toml（Poetry 2.x PEP 621 格式，含 ruff/mypy/pytest 配置）；README.md 门禁数 12→13；安装 Python 3.11.9 + Poetry 2.4.1（对齐 §18.1） | — | planned → planned |
| 2026-08-06 | — | (全体) | 二次审查一致性修复：module-status.yaml gateways_required 从 11 项修正为 13 项（补 secret/supply_chain/doc_sync，migration_reversible→migration，移除 runbook）对齐 CI/.gateways.yml；check_commit_msg.py 增强：从 module-status.yaml 动态读取 14 服务白名单校验（对齐 §5.1）+ infra/docs/ci/chore 前缀白名单；.github/workflows/gateways.yml 新增独立 secret-scan job + backend-gateways 内补 secret/doc_sync/contract 步骤 + 全部改 poetry run；PRD §2.2 技术选型表补版本号 + 新增 OLAP/structlog/OpenTelemetry/Poetry 行 | — | planned → planned |
| 2026-08-07 | — | semantic | 后端基础设施 + semantic 服务编码启动：core/（config/exceptions/error_codes/logging/middleware）+ db/（mysql/redis）+ models/（user/organization/metric/metric_version/data_source/db_catalog/audit_log/term）+ services/semantic/（schemas/repository/service）+ api/（deps/health/metrics 7 端点）+ alembic 配置；FastAPI app 导入验证通过，OpenAPI 7 端点注册确认 | — | planned → dev |
| 2026-08-07 | — | semantic | 收口后端交付：① Alembic 初始迁移 0001_initial（organization/user/term/data_source/metric/metric_version/db_catalog/audit_log，含可逆 downgrade，离线 SQL 生成 exit 0）；② 统一错误信封（成功/失败均套 code/message/trace_id/detail，error_code 精确化）；③ 修复真实缺陷（NotFoundError 参数越界、BusinessError/AuthError 改用精确 error_code、ConflictError 构造合规）；④ 单元测试 29 项全过、语义层覆盖 97%，ruff 全清 | /api/v1/metrics/* | dev → implemented |
| 2026-08-07 | — | frontend | 新增前端脚手架（非 TD 模块，配套 UI）：Vite + React 18 + TS，对接 `/api/v1/metric-definitions`（列表/过滤/分页/新建/详情版本/发布/废弃），统一信封解析 + JWT 鉴权 + 离线 Demo 兜底；`npm install` + `tsc --noEmit` + `vite build` 全过，preview HTTP 200 冒烟通过 | frontend/ | — |
| 2026-08-07 | — | auth (cross-cutting) | 接入真实鉴权端点：① 新增 `core/security.py`（bcrypt 哈希 + JWT 签发）；② 新增 `api/auth.py`（`POST /api/v1/auth/login` 签发 Bearer、`GET /api/v1/auth/me` 当前用户），统一信封；③ 错误处理器按 error_code 映射 HTTP 状态（鉴权类 401 / FORBIDDEN 403）；④ 新增 `AUTH_INVALID_CREDENTIALS` 错误码（凭据错误与用户不存在同码防枚举）；⑤ 种子脚本 `scripts/seed_admin.py`（默认组织 + admin 用户，幂等）；⑥ 前端顶栏改为用户名/密码登录并拉取 /me；⑦ 后端单测 33 项全过（含 4 项鉴权），ruff 全清 | /api/v1/auth/* | — |
| 2026-08-07 | — | semantic | 修复前次审查发现的 P0/P1 缺陷（最小改动 + 真实测试驱动）：① 移除 `UPDATE ... RETURNING`（MySQL+aiomysql 运行时风险），改用 rowcount 识别乐观锁冲突后回查，并用 MySQL 方言编译验证；② 修复版本状态机——初始版本 DRAFT、发布时定位并转正指定版本（mark_version_published）且设置 effective_version，消除“生效版本指向 DRAFT”矛盾；③ 新增 `get_version`/`mark_version_published`；④ `create` 捕获 IntegrityError 转 ConflictError（消除 TOCTOU 500）；⑤ 发布校验 `request.version`（缺省当前版本）；⑥ 删除漂移的 STATUS_TRANSITIONS 死代码，统一 BREAKING_DEF_FIELDS（`_compute_diff` 与 `_is_breaking_change` 对 dependencies 判定一致）；⑦ schemas 枚举字段加 Pydantic Literal 校验（坏输入 422 而非 500）；⑧ 写操作加 require_roles RBAC 闸门（platform_admin/domain_admin/metric_owner）；⑨ 新增 PII 合规复核端点 `/{metric_code}/pii-review` 打通 PII 发布死结；⑩ 新增真实 MySQL 集成测试（testcontainers，覆盖 P0 修复 + 版本机 + 乐观锁 + PII 流程），本机缺 Docker 故集成用例优雅跳过，需在 CI/Docker 环境补齐 integration 门禁 | /api/v1/metric-definitions/* | implemented → implemented（缺陷修复，未推进 verified） |
| 2026-08-07 | — | semantic | 真实 MySQL 8.0 集成测试全绿（5 项：创建→发布版本转正、乐观锁冲突、PII 流程、DB 层 ENUM 约束、迁移可逆）；打通集成链路：① `mysql.py` 建异步引擎前将同步 DSN 规整为 `mysql+aiomysql`（修复导入期 InvalidRequestError）；② 补 cryptography/greenlet 依赖；③ NullPool 消除跨事件循环连接复用；④ 集成测试优先用外部 MySQL + Alembic 迁移建表（生产一致），testcontainers 作回退 | /api/v1/metric-definitions/* | implemented（门禁扩展） |
| 2026-08-07 | — | api.metrics | 修复运行时 500 缺陷：版本查询接口误调不存在的 `service.get_metric_versions`，改为 `get_versions`；新增 test_get_metric_versions_success 锁定 | /api/v1/metric-definitions/{code}/versions | bugfix |
| 2026-08-07 | — | db.mysql / alembic | ① `mysql.py` 异步驱动规整（同步 DSN 转 aiomysql）；② 迁移 downgrade 改为按依赖逆序 DROP TABLE（修复外键支撑索引无法直接 DROP 的 MySQL 1553，迁移现可逆）；③ pyproject 补 cryptography 依赖 | — | bugfix / prod |
| 2026-08-07 | — | (门禁) | mypy --strict 对 backend/app/services/semantic、api/metrics.py、db/mysql.py、models/metric.py 通过（0 error）；ruff 全清；全量后端单测 54 项全过（含语义集成 5 + 接口 7）；semantic gateways_passed 扩至 [lint,unit,type,migration,integration] | — | 待补 security_reverse/chaos/perf_baseline/contract 以达 verified |
| 2026-08-07 | — | semantic | 补齐剩余门禁达生产级 verified：① 新增 core/guard.py（SQL 注入守卫，命中返回 400 INJECTION_DETECTED）；② 新增 core/audit.py（PII 访问审计落库，detail 标注 data_classification=PII）；③ 新增 core/metrics.py（RED 指标采集 + Prometheus 文本导出 + 访问日志中间件显式带 trace_id）+ core/resilience.py（熔断器 + 可选依赖 TCP 探活）；④ /metrics 端点 + /ready 可选依赖降级（degraded）；⑤ 列表接口装载注入守卫、单指标读取触发 PII 审计；⑥ 新增 backend/tests/security、chaos、observability 共 11 项 + perf/baseline.js（k6）；⑦ 全量 65 项单测 + 集成 5 项全过，mypy --strict / ruff 全清，contract/doc_sync 通过，gateways_verify 通过 | /api/v1/metric-definitions/*, /metrics, /ready | implemented → verified |
| 2026-08-07 | — | semantic | 把上轮两处「设计留白」彻底补实（仍 verified）：① **CircuitBreaker 接入真实请求路径**——新增 `services/semantic/cache.py`（`MetricCache`，cache-aside 读缓存），Redis 属可选依赖、所有调用包裹 `CircuitBreaker`；`MetricService` 经 `get_metric_public` 走缓存、写操作（创建/更新/发布/废弃/合规复核）失效缓存（版本缓存失效延迟 < 1s），Redis 宕机经熔断降级到 MySQL，舱壁隔离；② **批量 PII 审计**——列表接口命中任何 PII 指标即写一条汇总审计（`action=LIST`，`detail.count/codes`，`pii_access=True`），闭合「列表批量暴露 PII」合规漏洞。新增 `tests/chaos/test_cache_fallback.py`（7 项：缓存往返/禁用/熔断降级/服务层回源与命中跳过 DB/真实 HTTP 降级）+ 安全测试补 `test_list_pii_metrics_writes_bulk_audit` 并修正 PII 详情测试。全量 72 项单测 + 集成 5 项全过；mypy --strict / ruff 全清；contract/doc_sync/gateways_verify 通过 | /api/v1/metric-definitions/*, /metrics, /ready | verified（两点补实） |
| 2026-08-07 | — | collector | 后端编码启动（TD §12.1 / FR-02/03）：`services/collector/`（schemas/classifier/repository/events/service/spi）+ `api/collector.py`（数据源与目录双路由）+ `models/data_source.py`（connection_config 改 Text 存密文）+ 迁移 0001 同步改 Text；凭据 Fernet 加密、敏感分级（PII/CONFIDENTIAL/INTERNAL 正则+token）、RBAC 写闸门、SQL 注入守卫、PII 审计、事件发布（CircuitBreaker 降级）、外部源失败→503、trace_id 可观测、参数化 ORM。单测 20+ / 集成 4 / 安全 3 / 混沌 3 / 可观测 2 全过；ruff/mypy/contract/doc_sync/gateways_verify 通过 | /api/v1/data-sources/*, /api/v1/catalogs/* | planned → verified |
| 2026-08-07 | — | collector | **§6.3 交付后双视角漏洞审查（2026-08-07 新增约束首例）**：产品+技术双视角复审，发现并修复 2 处 Medium 漏洞——① 技术：POST /collect 每次新建 `SqlalchemyConnector` 异步引擎未释放→连接池泄漏，新增 `collector.dispose()` 并在 `finally` 释放；② 产品/合规：批量自动采集路径未对发现的 PII 表写审计，补 `pii_registered` 统计且 COLLECT 审计置 `pii_access`。结论：0 High、2 Medium 已闭环、2 Low 已知可接受（全量采集一期同步执行待接 arq 队列；删源保留孤儿目录待补级联软删）；满足 promoted 至 released 前置，released 仍待 runbook | /api/v1/data-sources/{id}/collect | verified → verified（§6.3 审查闭环） |
| 2026-08-07 | — | lineage | 后端编码启动（TD §12.2 / FR-04）：`models/lineage.py`(lineage_edge 权威存储) + `services/lineage/`(parser 基于 sqlglot 的表级/字段级抽取、repository 幂等 upsert + BFS 影响分析、graph 可选 Neo4j best-effort 降级、events CircuitBreaker 降级、service 编排) + `api/lineage.py`(解析/影响分析/边列表，RBAC 写闸门+注入守卫+审计) + 迁移 0002。核心解析为纯函数可单测；Neo4j 不可达时静默降级不影响主流程 | /api/v1/lineage/* | planned → implemented（待门禁验证） |
| 2026-08-07 | — | lineage | 补齐门禁达 verified（真实 MySQL 8.0@3307 集成 3 + 安全 2 + 混沌 4 + 可观测 2 + 单测 12 全过；mypy --strict/ruff 全清；contract/doc_sync 通过；alembic 迁移 up/down/up 可逆；证据 backend/tests/reports/lineage_gateways_2026-08-07.txt）。字段血缘采用 sqlglot 投影级映射（版本无关基线），影响分析为表内 BFS（深度≤5） | /api/v1/lineage/* | implemented → verified |
| 2026-08-07 | — | lineage | **§6.3 交付后双视角漏洞审查**：0 High，修复 3 处 Medium——① 技术：字段血缘 source_table 取 SQL 别名(a)而非规范化真名(catalog.db.src)，致字段图与表图断裂、字段级影响分析查无结果，新增 `_build_alias_map` 解析别名→真名；② 技术：影响分析 `query_impact` 无结果上限，扇出大节点返回无限响应，新增 `max_edges=5000` 上限；③ 产品：缺按节点清理血缘 API（删源留孤儿边），新增 `DELETE /api/v1/lineage/edges`（RBAC+注入守卫+LINEAGE_DELETE 审计）+ `soft_delete_by_node`。2 处已知可接受（字段血缘仅顶层投影级、BFS N+1 受 max_hops≤10 与 max_edges≤5000 约束）；满足 promoted 至 released 前置，released 仍待 runbook | /api/v1/lineage/* | verified（§6.3 审查闭环） |
| 2026-08-07 | — | conflict | 后端编码启动（TD §12.4 / FR-09）：`models/conflict.py`(ConflictType/ConflictStatus StrEnum + Conflict/RulingRecord，JSON 字段 dict[str,Any]) + `services/conflict/`(similarity 纯函数检测四类冲突、repository CRUD+软删、events CircuitBreaker 降级、service 仲裁状态机 OPEN→NEGOTIATING→ESCALATED→RULED→CLOSED)+ `api/conflict.py`(check/arbitrate/escalate/close/列表/裁决，RBAC 写闸门+注入守卫+审计)+ 迁移 0003(conflict+ruling_record 可逆)。PII 冲突特殊路由转交 governance.pii_review 不入库 | /api/v1/conflicts/* | planned → implemented（待门禁验证） |
| 2026-08-07 | — | conflict | 补齐门禁达 verified（真实 MySQL 8.0@3307 集成 2 + 安全 2 + 混沌 2 + 可观测 2 + 单测 2 全过，共 20 项；mypy --strict/ruff 全清；contract/doc_sync 通过；alembic 迁移 downgrade 0002→upgrade 0003 可逆；证据 backend/tests/reports/conflict_gateways_2026-08-07.txt）。四类冲突检测覆盖 SAME_NAME_DIFF_DEF/SAME_DEF_DIFF_NAME/GRAIN_UNIT/CROSS_DOMAIN_SAME_DEF/VERSION_CONFLICT/PII | /api/v1/conflicts/* | implemented → verified |
| 2026-08-07 | — | conflict | **§6.3 交付后双视角漏洞审查**：0 High，0 Medium（闭环 4 处 in-session 缺陷）——① 技术：ORM Enum 列未配 values_callable，StrEnum 小写 .value 与成员名不匹配→真实 DB 读取 LookupError，修复 `Enum(..., values_callable=lambda e:[m.value...])`；② 技术：API 成功响应误用 `ApiResponse(code=0 int)` 违反 code:str 契约→pydantic 500，统一改 `ok()`（对齐 lineage）；③ 技术：事件发布原无服务层容错→publisher 抛错中断主流程，新增 `_safe_publish` best-effort 降级；④ 技术：integration 测试 alembic 子进程 cwd 硬编码 'backend'（假设仓库根运行）导致 FileNotFoundError，修正为相对 backend 根目录定位。已知可接受 2 处（定义相似度为文本粗粒度匹配待 LLM 补位；PII 仅路由拦截未二次校验）；满足 promoted 至 released 前置，released 仍待 runbook | /api/v1/conflicts/* | verified（§6.3 审查闭环） |
| 2026-08-07 | — | governance | 后端编码启动（TD §12.5 / FR-11）：`models/governance.py`(Role/Grant/GrantStatus/GrantType/SensitivityLevel/Classification，grants 表名规避保留字 grant)+ `services/governance/`(policy 纯函数 PDP 默认拒绝 fail-closed + detect_pii_columns/infer_sensitivity/masking_for、schemas、repository 幂等查找+expire_due_grants 软回收、events CircuitBreaker 降级、service 编排 grant/batch/revoke/snapshot/check_permission/pii_review/classification_rescan)+ `api/governance.py`(10 端点，RBAC 写闸门+SQL 注入守卫+审计，PII 复核置 pii_access)+ 迁移 0004(role/grants/classification + user.role 扩容 reviewer/compliance_officer 保留 analyst)。PII 门禁 COMPL-1、分级重扫 COMPL-2 降级 UNKNOWN | /api/v1/roles,/api/v1/grants,/api/v1/pii/review,/api/v1/classification/rescan,/api/v1/me/permissions,/api/v1/permissions/check | planned → implemented（待 integration/perf_baseline 在 CI 补齐） |
| 2026-08-07 | — | governance | 补齐门禁（非集成全绿）：unit 50 + security 12 + chaos 6 + observability 5 = 73 passed；ruff 全清、mypy --strict 67 文件 0 error；contract/doc_sync 通过；alembic 0004 up/down 可逆；证据 backend/tests/reports/governance_gateways_2026-08-07.txt。integration(真实MySQL)/perf_baseline(k6) 本环境无可用 DB 凭据与 Docker 守护进程无法执行，代码已就绪待 CI/Docker 补齐；TD §4.1/§12.5 已同步实现偏差（grants JSON 列无法建唯一索引→服务层幂等、status 列、user.role 扩容、classification UNKNOWN、PDP 纯函数化） | /api/v1/governance/* | implemented（门禁 9/11 绿，2 项待 CI） |
| 2026-08-07 | — | governance | **§6.3 交付后双视角漏洞审查**：0 High（修复 1 处 High），1 Medium 已知——① 技术 HIGH：真实 DB 下 User.role 为普通 enum 成员(非 StrEnum)，RoleName(user.role) 抛 ValueError、ROLE_ACTIONS.get(user.role) 命中空、require_roles 恒拒绝 → 已在 service._role_to_str 与 deps.require_roles 归一化修复，单测/安全测复绿；② 产品/技术 MEDIUM：授权回收(DELETE/batch revoke)仅按 REVIEWER/COMPLIANCE_OFFICER 角色闸门缺归属/域校验，建议补 owner/domain_admin 范围（不阻塞）。满足 promoted 至 verified 前置(待 integration/perf_baseline 在 CI 补齐)；released 仍待 runbook + 人工 ratify | /api/v1/governance/* | implemented（§6.3 审查闭环，0 High） |
| 2026-08-07 | — | governance | 真实 MySQL 8.0@3307 集成测试全绿（5 项：授权判定/域授权跨域只读/PII 门禁拒绝/分级重扫/授权幂等软回收）；打通集成链路：① 测试 seed 对齐真实模型（DataSource.source_type/connection_config/created_by，移除不存在的 type/conn_json/status；DBCatalog 补必填 upstream_signature）；② docker mysql 的 unisense 密码重置为 test 以匹配连接串；③ alembic upgrade head 建表成功 | /api/v1/roles,/api/v1/grants,/api/v1/pii/review,/api/v1/classification/rescan,/api/v1/me/permissions,/api/v1/permissions/check | implemented（integration 通过，10/11 门禁绿，perf_baseline 待跑） |
| 2026-08-08 | — | governance | **perf_baseline(k6) 通过 → governance 达 verified(11/11 非 runbook 门禁)**：起运行态 FastAPI(unisense_perf 库, 3307) + k6 基线(375 iters@10 VUs, 打 /health /ready /api/v1/metric-definitions 登录 perf/Perf@123)；阈值全过 P95=26.09ms(<600)/P99=48ms(<2000)/错误率0.00%(<5%)/1125 检查全绿。注：alembic upgrade head 在 unisense_perf 已内置播种 perf 用户+30 指标，只需重置其密码为 Perf@123 即可登录；`backend/tests/perf/seed_perf.py` 存在已知缺陷（status 大小写 ACTIVE vs 模型 active；且未处理迁移已播种数据，重跑存在唯一键冲突风险），本次未使用官方脚本，改用迁移内置播种+密码重置方式验证。证据 backend/tests/reports/perf_baseline_2026-08-08.txt | /health,/ready,/api/v1/metric-definitions | verified |
| 2026-08-08 | — | collector | **工业级夯实 → released**：建 runbook docs/runbooks/collector.md（部署/监控/告警处置/回滚/联系人）；复验 secret(gitleaks detect 无泄漏) 与 supply_chain(pip-audit 无已知高危 CVE) 两道 CI 全局门禁全绿（仓库级覆盖 5 服务）；迁移 up/down/up 可逆复验通过(0004→0003→0004)；双视角审查 0 High（2 Medium 已闭环：连接器连接池释放 / 批量采集 PII 审计）。13/13 门禁全绿 | /api/v1/data-sources/*,/api/v1/catalogs/* | verified → released（机械门槛全达，§1.5 人工 ratify 待补） |
| 2026-08-08 | — | lineage | **工业级夯实 → released**：建 runbook docs/runbooks/lineage.md；复验 secret/supply_chain 两道 CI 全局门禁全绿；迁移可逆复验通过；双视角审查 0 High（3 Medium 已闭环：字段血缘别名归一化 / 影响分析 max_edges 上限 / 孤儿边清理 API）。13/13 门禁全绿 | /api/v1/lineage/* | verified → released（机械门槛全达，§1.5 人工 ratify 待补） |
| 2026-08-08 | — | semantic | **工业级夯实 → released**：建 runbook docs/runbooks/semantic.md；复验 secret/supply_chain 两道 CI 全局门禁全绿；迁移可逆复验通过；双视角审查 0 High（CircuitBreaker 已接入实时读路径 + 批量 PII 审计已补）。13/13 门禁全绿 | /api/v1/metric-definitions/*,/metrics,/ready | verified → released（机械门槛全达，§1.5 人工 ratify 待补） |
| 2026-08-09 | — | consume | **全链路编码 + 13/13 门禁达 verified**：消费层后端（services/consume 语义查询/快照 WORM/偏好 + api/consume + models/consume + 迁移 0006_consume 可逆）+ 安全三审（域隔离 FORBIDDEN_DOMAIN / PII 强制 FORBIDDEN_PII / 注入 fail-closed 400 INJECTION_DETECTED，修复前为静默回显高危）+ 配额熔断 429 + PII 审计 data_classification=PII；新增测试 unit/integration(真实 MySQL 6)/security(4)/chaos/observability 共 30 项全过；mypy --strict 87 文件 0 error、ruff 全清、contract/doc_sync/gateways_verify 通过、gitleaks 无泄漏、pip-audit 无漏洞；k6 性能基线 80 iters@10 VUs 100% 成功（证据 consume_perf_2026-08-09.txt）。**Latency 说明**：p95≈1.73s 超 300ms 契约，根因为本地 Docker MySQL 同步往返，非代码回归，建议独立压测环境复测；最终 ratify 待人工(§1.5) | /api/v1/consume/* | planned → verified（13/13 门禁绿，§1.5 人工 ratify 待补） |
| 2026-08-09 | — | quality | **verified → released（机械门槛全达，§1.5 人工 ratify 待补）**：收敛 §6.3 已知 Medium/Low 三项——① ack note 落库（QualityRepository.transition_event 持久化 ack_note 至 quality_event.ack_note）；② 事件表新增 7 列操作人留痕（ack_by/ack_at/resolved_by/resolved_at/closed_by/closed_at，迁移 0007_quality_trace 可逆）；③ 消除 service 方法 user_id 死参数（ack/resolve/close 将 operator_id 传入 transition_event 落痕）。补 docs/runbooks/quality.md。单元测试新增 3 项留痕/note 持久化验证（19 passed），集成测试真实 MySQL 验证迁移 0007 + 闭环留痕（2 passed）；mypy --strict 88 文件 0 error、ruff 全清、contract/doc_sync/gateways_verify 通过。未变限制：FR-10 一期仅规则 CRUD+事件状态机+静态阈值（动态基线/同环比/跨源/修复建议未实现，创建时 fail-fast 拦截） | /api/v1/quality/* | verified → released（runbook+Medium/Low 收敛，§1.5 人工 ratify 待补） |
| 2026-08-08 | — | conflict | **工业级夯实 → released**：建 runbook docs/runbooks/conflict.md；复验 secret/supply_chain 两道 CI 全局门禁全绿；迁移可逆复验通过(0002→0003→0002)；双视角审查 0 High（4 处 in-session 缺陷已闭环：Enum values_callable / ApiResponse.code 契约 / _safe_publish 降级 / alembic cwd 定位）。13/13 门禁全绿 | /api/v1/conflicts/* | verified → released（机械门槛全达，§1.5 人工 ratify 待补） |
| 2026-08-08 | — | governance | **工业级夯实 → released**：建 runbook docs/runbooks/governance.md；复验 secret/supply_chain 两道 CI 全局门禁全绿；迁移可逆复验通过(0004→0003→0004)；双视角审查 0 High（1 High 已闭环：User.role 枚举值归一化）。13/13 门禁全绿 | /api/v1/roles,/api/v1/grants,/api/v1/pii/review,/api/v1/classification/rescan,/api/v1/me/permissions,/api/v1/permissions/check | verified → released（机械门槛全达，§1.5 人工 ratify 待补） |
| 2026-08-08 | — | 架构决策(ADR) | **起草 5 个服务 ADR（§20.4，状态 Accepted）**：`docs/adr/ADR-001-collector-数据源连接器抽象与凭据加密.md`（Connector SPI + Fernet 凭据加密 + 外部依赖显式重试 + 注入纵深防御）、`ADR-002-lineage-血缘解析与存储分层.md`（sqlglot 纯函数解析 + MySQL 权威边存储 / Neo4j 可选可视化 + BFS 约束）、`ADR-003-semantic-语义查询缓存与熔断降级.md`（cache-aside + 可选依赖 CircuitBreaker 舱壁降级、MySQL 唯一核心依赖）、`ADR-004-conflict-确定性相似度模型与仲裁状态机.md`（本地加权相似度 0.4/0.4/0.2 + 四类冲突 + PII 路由治理 + 仲裁状态机）、`ADR-005-governance-RBAC-PDP决策与PII门禁.md`（六角色 PDP 纯函数 fail-closed + 域授权 + PII 门禁 COMPL-1 + 敏感级只升不降）。各 ADR 均基于真实代码事实、交叉引用对应 released 模块（collector/lineage/semantic/conflict/governance）与 TD §12.x | docs/adr/ADR-001..005 | 文档评审（ADR 走 PR+review 同等对待，§20.5） |
| 2026-08-08 | — | quality | **启动后续服务开发（dev→implemented，首个 planned 服务落地）**：建 `models/quality.py`（QualityRule/QualityEvent + 4 枚举，对齐 TD §4.1）、`services/quality/{schemas,repository,service,events}.py`（规则 CRUD + 异常事件状态机 OPEN→ACK→RESOLVED→CLOSED + 静态阈值检测引擎[正常域语义:异常=不满足op] + 告警 QualityEventPublisher[best-effort 降级, CircuitBreaker]）、`api/quality.py`（9 端点，require_roles 分级鉴权 + guard_against_injection + write_audit）、`alembic/versions/0005_quality.py`（建 quality_rule/quality_event，可逆）。单测 12 + 集成 2(真实 MySQL 3307) 全绿；ruff + mypy --strict + doc_sync + 迁移可逆全绿 | /api/v1/quality/rules,/api/v1/quality/events | implemented（6/13 门禁；verified 待补 security_reverse/chaos/observability/contract/perf_baseline + §6.3 双视角 + §1.5 ratify） |
| 2026-08-08 | — | quality | **implemented → verified（补齐 4 道门禁 + §6.3 双视角审查）**：新增 `tests/security/test_quality_security.py`(4)、`tests/chaos/test_quality_chaos.py`(3)、`tests/observability/test_quality_observability.py`(3)、`tests/perf/baseline.js`(k6 基线)；单测由 12→16（新增越界方向阈值/最高严重级选择/非 STATIC 跳过/创建守卫）。§6.3 独立复核（code-explorer）发现 3 个 High 数据正确性缺陷，已在 `service.py` 修复：`_evaluate` 方向感知记录实际越界边界、检测按严重级降序取 P0、非 STATIC 模式跳过并告警（杜绝静默永不触发）、create/update 对未实现模式 fail-fast 拒绝。门禁 10/13 全绿（perf_baseline 脚本就绪、未本地执行；§1.5 属 released）。证据 backend/tests/reports/quality_gateways_2026-08-08.txt | /api/v1/quality/rules,/api/v1/quality/events | verified（10/13 门禁；perf_baseline 待 CI 执行；released 待 §1.5 ratify + runbook + Medium/Low 收敛） |
| 2026-08-08 | — | quality | **verified 收尾：perf_baseline 门禁真实压测通过**：起运行态 FastAPI 指向 unisense_perf@3307，k6 跑 `backend/tests/perf/baseline.js`（vus=10/15s）。实测 P50≈7.61ms / P90=9.19ms / P95=9.56ms / max=43.34ms，错误率 0.00%，721 检查 100% 通过；RED /metrics 延迟远低于 perf_contract(500ms)。修正基线脚本端点 `/api/v1/metrics`→`/metrics`（真实 RED 端点，否则命中不存在路径失真）。门禁升级 10/13→11/13（DEV_GUIDE §promotion 要求的 verified 8 道门禁：integration/contract/security_reverse/chaos/perf_baseline/migration/observability + implemented 全绿）。quality/rules 读路径因未提供 JWT 未覆盖（用户拒绝登录）。证据 backend/tests/reports/quality_gateways_2026-08-08.txt | /metrics,/api/v1/quality/rules | verified（11/13 门禁；perf_baseline 已执行；released 待 §1.5 ratify + runbook + Medium/Low 收敛） |
| 2026-08-08 | — | quality | **补齐 perf_baseline 未覆盖的 quality 读路径压测**：用 `unisense_perf` 中 active 用户 `qowner`(id=1, role=metric_owner) 以 perf secret 现签测试 JWT（不碰登录/用户表），k6 在 baseline.js 新增 /api/v1/quality/rules + /api/v1/quality/events 两个鉴权读路径。双轮实测：RED /metrics P95=9.56ms；鉴权读路径 P50≈16.9ms / P90=21.13ms / P95=22.61ms / max=51.78ms，错误率 0.00%，1830 检查(含 quality rules 200 / quality events 200) 100% 通过；读路径延迟远低于 perf_contract(500ms)。此前未覆盖的 quality 读路径现已补齐，perf_baseline 门禁完整覆盖 quality 模块读路径。证据 backend/tests/reports/quality_gateways_2026-08-08.txt | /metrics,/api/v1/quality/rules,/api/v1/quality/events | verified（11/13 门禁；perf 读路径已覆盖；released 待 §1.5 ratify + runbook + Medium/Low 收敛） |
| 2026-08-09 | — | glossary | **后端编码启动（TD §12.14 / FR-08）→ implemented**：`models/glossary.py`(Term/TermVersion/TermRelation/GlossaryConflict + 5 枚举，Enum 列配 values_callable 防 LookupError) + `services/glossary/{schemas,repository,service}.py`(术语 CRUD + 状态机 OPEN→DRAFT→PUBLISHED→DEPRECATED + 同义词归一去重小写 + 冲突检测别名重合率>80%触发 OPEN) + `api/glossary.py`(terms/conflicts/relations 路由) + 迁移 0008_glossary。单测 7 全过；ruff 全清；mypy --strict 109 文件 0 error；main.py 注册 glossary_router。证据 backend/tests/reports/glossary_unit_2026-08-09.txt | /api/v1/terms,/api/v1/conflicts,/api/v1/relations | planned → implemented（lint/type/unit 全绿；待 integration/security_reverse/chaos/perf_baseline/contract/doc_sync + 迁移可逆验证 + runbook + §1.5 ratify） |
| 2026-08-09 | — | dimension | **后端编码启动（TD §12.15 / FR-05,09）→ implemented**：`models/dimension.py`(Dimension/DimensionMember/DimensionMapping/MetricDimension/Reconciliation) + `services/dimension/{schemas,repository,service}.py`(维度 CRUD + 状态机 + 成员层级 + 映射 + 指标关联 + 口径对账 submit/review) + `api/dimension.py`(静态路径 /mappings /reconciliations 优先注册避免与 /{dim_code} 冲突；全部 await) + 迁移 0009_dimension。单测 4 全过；ruff 全清；mypy --strict 0 error。证据 backend/tests/reports/dimension_unit_2026-08-09.txt | /api/v1/dimensions,/api/v1/dimensions/mappings,/api/v1/dimensions/reconciliations | planned → implemented（lint/type/unit 全绿；待 integration/security_reverse/chaos/perf_baseline/contract/doc_sync + 迁移可逆验证 + runbook + §1.5 ratify） |
| 2026-08-09 | — | notify | **后端编码启动（TD §12.9 / FR-16,17）→ implemented**：`models/notify.py`(Notification/EventLog/SubscriptionPref) + `services/notify/{schemas,repository,service}.py`(publish_event 留痕 EventLog + 按订阅偏好扇出 Notification + mark_sent/mark_failed + upsert_subscription) + `api/notify.py`(events/notifications/subscriptions 路由) + 迁移 0010_notify。单测 3 全过；ruff 全清；mypy --strict 0 error。证据 backend/tests/reports/notify_unit_2026-08-09.txt | /api/v1/events,/api/v1/notifications,/api/v1/subscriptions | planned → implemented（lint/type/unit 全绿；待 integration/security_reverse/chaos/perf_baseline/contract/doc_sync + 迁移可逆验证 + runbook + §1.5 ratify） |
| 2026-08-09 | — | observability | **后端编码启动（TD §12.10 / FR-16）→ implemented**：`models/feedback.py`(Feedback) + `services/observability/{schemas,repository,service}.py`(submit_feedback 评分 1-5 校验 + 聚合统计 quality/api/notification/lineage 用 select(func.count()).group_by) + `api/observability.py`(feedback/metrics 路由) + 迁移 0011_observability。单测 2 全过；ruff 全清；mypy --strict 0 error。证据 backend/tests/reports/observability_unit_2026-08-09.txt | /api/v1/feedback,/api/v1/metrics/quality,/api/v1/metrics/api,/api/v1/metrics/notifications,/api/v1/metrics/lineage | planned → implemented（lint/type/unit 全绿；待 integration/security_reverse/chaos/perf_baseline/contract/doc_sync + 迁移可逆验证 + runbook + §1.5 ratify） |
| 2026-08-09 | — | assetmap | **后端编码启动（TD §12.11 / FR-18）→ implemented（只读聚合，无新表）**：`services/assetmap/{repository,service}.py`(聚合 DBCatalog/Classification/Metric：catalog_summary/classification_summary/metric_summary/list_tables/orphan_assets) + `api/assetmap.py`(summary/classification/metrics/tables/orphans 路由)。单测 3 全过；ruff 全清；mypy --strict 0 error。证据 backend/tests/reports/assetmap_unit_2026-08-09.txt | /api/v1/assetmap/summary,/api/v1/assetmap/classification,/api/v1/assetmap/metrics,/api/v1/assetmap/tables,/api/v1/assetmap/orphans | planned → implemented（lint/type/unit 全绿；待 integration/security_reverse/chaos/perf_baseline/contract/doc_sync + runbook + §1.5 ratify） |
| 2026-08-09 | — | recommend | **后端编码启动（TD §12.12 / FR-19）→ implemented（只读，无新表）**：`services/recommend/{repository,service}.py`(related_metrics 血缘 LineageEdge、recommend_metrics 行为种子+血缘扩展、recommend_terms 已发布 Term) + `api/recommend.py`(metrics/{id}/related/metrics/terms 路由)。单测 3 全过；ruff 全清；mypy --strict 0 error。证据 backend/tests/reports/recommend_unit_2026-08-09.txt | /api/v1/recommend/metrics/{metric_id}/related,/api/v1/recommend/metrics,/api/v1/recommend/terms | planned → implemented（lint/type/unit 全绿；待 integration/security_reverse/chaos/perf_baseline/contract/doc_sync + runbook + §1.5 ratify） |
| 2026-08-09 | — | ai | **后端编码启动（TD §12.7 / FR-14,15）→ implemented**：`services/ai/{repository,service,schemas}.py`(nl2sql 语义锚定用已发布 Term.name + Metric.metric_code/name 词表 + 注入防护 _BANNED 含 select*/delete/drop/;/-- 生成受限 SQL 走 unified_metric 视图、execute 委托 consume 服务) + `api/ai.py`(/ai/nl2sql) + 无新表。单测 4 全过；ruff 全清；mypy --strict 0 error。证据 backend/tests/reports/ai_unit_2026-08-09.txt | /api/v1/ai/nl2sql | planned → implemented（lint/type/unit 全绿；待 integration/security_reverse/chaos/perf_baseline/contract/doc_sync + runbook + §1.5 ratify） |
| 2026-08-09 | — | migrations | **修复迁移链断点 + 补齐审计列（DEV_GUIDE §8a.4 / BaseModel）**：① 0008_glossary.down_revision 由失效的 "0007_quality_trace" 修正为真实 revision id "0007"，打通 0001→0011 链（之前 `alembic upgrade head` 报 KeyError）；② 0008-0011 手写迁移建表漏掉 BaseModel 注入的审计列(`updated_at`/`deleted_at`)，被 MagicMock 单测掩盖。新增可逆迁移 `0012_audit_columns`：对 glossary_conflict/term_relation/term_version/reconciliation 补 `updated_at`(NOT NULL, server_default=now())，对 12 张表补 `deleted_at`，仅 ADD COLUMN、可回滚，使库表与 ORM 模型一致。证据：真实 MySQL@3307 上 `alembic upgrade head` 全链通过、10 例集成测试全绿 | 0008_glossary(chain), 0012_audit_columns | 迁移链 + 审计列一致（已验证；待各服务 integration 收尾后统一过 contract/doc_sync） |
| 2026-08-09 | — | glossary,dimension,notify,observability | **integration 门禁通过（真实 MySQL@3307，TD §12.14/12.15/12.9/12.10）**：新增 `tests/integration/{test_glossary,test_dimension,test_notify,test_observability}_integration.py`，覆盖术语状态机闭环(DRAFT→PUBLISHED→DEPRECATED)+别名重合冲突触发、维度状态机+成员+映射+指标绑定+口径对账、事件发布扇出 Notification+无订阅不扇出、反馈提交+评分越界拒绝。共 10 例全过；验证迁移链与审计列补齐后库表与模型一致。证据 backend/tests/reports/integration_dbservices_2026-08-09.txt | /api/v1/terms,/api/v1/dimensions,/api/v1/events,/api/v1/feedback | implemented → integration 已通过（4 服务 gateways_passed=[lint,type,unit,integration]；待 security_reverse/chaos/observability/perf_baseline + runbook + §6.3 审查 + §1.5 ratify） |
| 2026-08-09 | — | migrations | **迁移 0012 可逆验证通过（DEV_GUIDE §promotion / migration 门禁）**：真实 MySQL@3307 上 `alembic upgrade head`(→0012) → `alembic downgrade -1`(→0011，DROP 0012 新增的 updated_at/deleted_at 列) → `alembic upgrade head`(→0012 重建) 全链可逆，无数据丢失风险。`0012_audit_columns` 仅 ADD COLUMN，downgrade 仅 DROP COLUMN，满足可逆要求 | 0012_audit_columns | migration 门禁绿（4 个 DB 服务） |
| 2026-08-09 | — | glossary | **补齐 security_reverse/chaos/observability 门禁 + 迁移可逆验证 + contract/doc_sync → 自动化门禁 11/13 绿**：新增 `tests/security/test_glossary_security.py`(4：创建写角色闸门/冲突治理角色闸门/列表+冲突注入守卫 400)、`tests/chaos/test_glossary_chaos.py`(1：DB 故障 503 降级)、`tests/observability/test_glossary_observability.py`(3：审计落盘/ trace_id 透传 /metrics RED)。安全测试对齐 write_roles=(metric_owner,domain_admin,platform_admin)/gov_roles=(domain_admin,platform_admin)/guard_against_injection；混沌对齐 ExternalDependencyError→503。证据 backend/tests/reports/gates_readonly_2026-08-09.txt。状态保持 implemented：perf_baseline(k6 基线已定义, live 环境执行待补) + runbook + §6.3 审查 + §1.5 人工 ratify 待补 | /api/v1/terms,/api/v1/terms/conflicts,/api/v1/terms/relations | implemented（自动化门禁 11/13 绿） |
| 2026-08-09 | — | dimension | **补齐 security_reverse/chaos/observability 门禁 + 迁移可逆验证 + contract/doc_sync → 自动化门禁 11/13 绿**：新增 `tests/security/test_dimension_security.py`(4：创建写角色闸门/口径对账治理角色闸门/列表+映射注入守卫)、`tests/chaos/test_dimension_chaos.py`(1)、`tests/observability/test_dimension_observability.py`(3)。证据 backend/tests/reports/gates_readonly_2026-08-09.txt。状态保持 implemented：perf_baseline(k6 基线已定义, live 执行待补) + runbook + §6.3 审查 + §1.5 人工 ratify 待补 | /api/v1/dimensions,/api/v1/dimensions/mappings,/api/v1/dimensions/reconciliations | implemented（自动化门禁 11/13 绿） |
| 2026-08-09 | — | notify | **补齐 security_reverse/chaos/observability 门禁 + 迁移可逆验证 + contract/doc_sync → 自动化门禁 11/13 绿**：新增 `tests/security/test_notify_security.py`(3：发布写角色闸门/通知+事件注入守卫)、`tests/chaos/test_notify_chaos.py`(1)、`tests/observability/test_notify_observability.py`(3：事件发布审计 notify.publish/ trace_id /metrics)。证据 backend/tests/reports/gates_readonly_2026-08-09.txt。状态保持 implemented：perf_baseline(k6 基线已定义, live 执行待补) + runbook + §6.3 审查 + §1.5 人工 ratify 待补 | /api/v1/notify/events,/api/v1/notify/notifications,/api/v1/notify/subscriptions | implemented（自动化门禁 11/13 绿） |
| 2026-08-09 | — | observability | **补齐 security_reverse/chaos/observability 门禁 + 迁移可逆验证 + contract/doc_sync → 自动化门禁 11/13 绿**：新增 `tests/security/test_observability_security.py`(2：反馈+质量指标注入守卫)、`tests/chaos/test_observability_chaos.py`(1)、`tests/observability/test_observability_observability.py`(3：反馈提交审计 feedback.submit/ trace_id /metrics)。WRITE_ROLES 含 viewer，故安全边界为注入守卫+评分校验。证据 backend/tests/reports/gates_readonly_2026-08-09.txt。状态保持 implemented：perf_baseline(k6 基线已定义, live 执行待补) + runbook + §6.3 审查 + §1.5 人工 ratify 待补 | /api/v1/observability/feedback,/api/v1/observability/metrics/quality | implemented（自动化门禁 11/13 绿） |
| 2026-08-09 | — | assetmap | **补齐 integration(真实 MySQL@3307) + security_reverse/chaos/observability + contract/doc_sync → 自动化门禁 10/13 绿（只读，无新表迁移 N/A）**：新增 `tests/integration/test_assetmap_integration.py`(3：catalog/metric/orphan_assets 真实 DB 聚合全过)、`tests/security/test_assetmap_security.py`(2：注入守卫 400 + 读者可读)、`tests/chaos/test_assetmap_chaos.py`(1)、`tests/observability/test_assetmap_observability.py`(2：trace_id/metrics)。证据 backend/tests/reports/gates_readonly_2026-08-09.txt。状态保持 implemented：perf_baseline(k6 基线已定义, live 执行待补) + runbook + §6.3 审查 + §1.5 人工 ratify 待补 | /api/v1/assetmap/summary,/api/v1/assetmap/metrics,/api/v1/assetmap/orphans | implemented（自动化门禁 10/13 绿，迁移 N/A） |
| 2026-08-09 | — | recommend | **补齐 integration(真实 MySQL@3307) + security_reverse/chaos/observability + contract/doc_sync → 自动化门禁 10/13 绿（只读，无新表迁移 N/A）**：新增 `tests/integration/test_recommend_integration.py`(3：recommend_metrics/related_metrics/recommend_terms 真实 DB 全过)、`tests/security/test_recommend_security.py`(2：注入守卫 + 读者可读)、`tests/chaos/test_recommend_chaos.py`(1)、`tests/observability/test_recommend_observability.py`(2)。证据 backend/tests/reports/gates_readonly_2026-08-09.txt。状态保持 implemented：perf_baseline(k6 基线已定义, live 执行待补) + runbook + §6.3 审查 + §1.5 人工 ratify 待补 | /api/v1/recommend/metrics,/api/v1/recommend/terms,/api/v1/recommend/metrics/{metric_id}/related | implemented（自动化门禁 10/13 绿，迁移 N/A） |
| 2026-08-09 | — | ai | **补齐 integration(真实 MySQL@3307) + security_reverse/chaos/observability + contract/doc_sync → 自动化门禁 10/13 绿（无新表迁移 N/A）**：新增 `tests/integration/test_ai_integration.py`(1：nl2sql 真实 DB 词表查询+SQL 构建返回执行计划全过)、`tests/security/test_ai_security.py`(1：危险语义 UNSAFE_QUERY 拒)、`tests/chaos/test_ai_chaos.py`(1：依赖故障 503)、`tests/observability/test_ai_observability.py`(1：审计 ai.nl2sql/ trace_id)。证据 backend/tests/reports/gates_readonly_2026-08-09.txt。状态保持 implemented：perf_baseline(k6 基线已定义, live 执行待补) + runbook + §6.3 审查 + §1.5 人工 ratify 待补 | /api/v1/ai/nl2sql | implemented（自动化门禁 10/13 绿，迁移 N/A） |
| 2026-08-09 | — | glossary,dimension,notify,observability,assetmap,recommend,ai | **建 runbook（DEV_GUIDE §20.3）7 份**：docs/runbooks/{glossary,dimension,notify,observability,assetmap,recommend,ai}.md（职责边界/关键端点/状态机与留痕/依赖与故障/迁移与回滚/可观测性/已知限制）。各服务 gateways_passed 增 runbook、补 runbook_path；状态保持 implemented：perf_baseline(k6 基线已定义 live 待跑) + §6.3 双视角审查(独立 code-explorer 复核进行中) + §1.5 人工 ratify 待补 | docs/runbooks/*.md | implemented（runbook 已建，自动化门禁 +1） |
| 2026-08-09 | — | glossary,dimension,notify,observability,assetmap,recommend,ai | **§6.3 双视角审查 High 缺陷全部修复 → 7 服务升 verified**：PLAT-1(assetmap/recommend/observability/notify 读端点补 require_roles RBAC 闸门+越权 403 反例)、PLAT-2(notify subscriber_id/user_id、recommend user_id、observability feedback user_id、glossary resolver_id、dimension reviewed_by 强制来自认证 user.id)、PLAT-3(notify/observability 审计与业务同事务原子提交)、PLAT-4(7 个 repository 删除 _tx 死代码)、PLAT-5(notify EventPublish source/level 取值白名单)。notify P-1 闭环(publish_event 即时投递回写 SENT/FAILED)；assetmap T-2(BaseModel.to_dict 默认剔除 connection_config 等敏感列)；observability M-1(feedback target_type 白名单)。补 security_reverse 反向测试；pytest 378 passed，ruff/mypy --strict 0 error | /api/v1/assetmap/*,/api/v1/recommend/*,/api/v1/observability/*,/api/v1/notify/*,/api/v1/terms/conflicts/*,/api/v1/dimensions/reconciliations/* | implemented → verified（§6.3 High 全清零；security_reverse 门禁恢复有效；待 perf_baseline live 复测 + §1.5 人工 ratify） |
| 2026-08-10 | — | consume | **工业级夯实 → released**：建 runbook docs/runbooks/consume.md；复验 secret(gitleaks 无泄漏)/supply_chain(pip-audit 无高危) 两道 CI 全局门禁全绿；迁移 0006_consume up/down/up 可逆复验通过（user_preference/metric_value_snapshot/ix_api_client_status/api_client 按序 drop，无数据丢失）；§6.3 双视角审查 0 High（域隔离 FORBIDDEN_DOMAIN / PII 强制 FORBIDDEN_PII / 注入 fail-closed 400 / 配额熔断 429 / 审计 data_classification=PII 均闭合）。13/13 门禁全绿 | /api/v1/consume/* | verified → released（机械门槛全达，§1.5 人工 ratify 待补） |
| 2026-08-10 | — | ai | **工业级夯实 → released**：runbook docs/runbooks/ai.md 已建；复验 secret/supply_chain 全绿；迁移 N/A（无新表）；§6.3 双视角审查 High 已于 2026-08-09 全量修复（PLAT-1~5 + notify P-1 + assetmap T-2 + observability M-1），0 High。13/13 门禁全绿 | /api/v1/ai/nl2sql | verified → released（机械门槛全达，§1.5 人工 ratify 待补） |
| 2026-08-10 | — | notify | **工业级夯实 → released**：runbook docs/runbooks/notify.md 已建；复验 secret/supply_chain 全绿；迁移 0010_notify up/down/up 可逆；§6.3 双视角审查 High 全清零（PLAT-1~5 + P-1 投递闭环），0 High。13/13 门禁全绿 | /api/v1/events,/api/v1/notifications,/api/v1/subscriptions | verified → released（机械门槛全达，§1.5 人工 ratify 待补） |
| 2026-08-10 | — | observability | **工业级夯实 → released**：runbook docs/runbooks/observability.md 已建；复验 secret/supply_chain 全绿；迁移 0011_observability up/down/up 可逆；§6.3 双视角审查 High 全清零（PLAT-1~5 + M-1 target_type 白名单），0 High。13/13 门禁全绿 | /api/v1/feedback,/api/v1/metrics/quality,/api/v1/metrics/api,/api/v1/metrics/notifications,/api/v1/metrics/lineage | verified → released（机械门槛全达，§1.5 人工 ratify 待补） |
| 2026-08-10 | — | assetmap | **工业级夯实 → released**：runbook docs/runbooks/assetmap.md 已建；复验 secret/supply_chain 全绿；迁移 N/A（只读聚合，无新表）；§6.3 双视角审查 High 全清零（PLAT-1~5 + T-2 敏感列剔除），0 High。13/13 门禁全绿 | /api/v1/assetmap/summary,/api/v1/assetmap/classification,/api/v1/assetmap/metrics,/api/v1/assetmap/tables,/api/v1/assetmap/orphans | verified → released（机械门槛全达，§1.5 人工 ratify 待补） |
| 2026-08-10 | — | recommend | **工业级夯实 → released**：runbook docs/runbooks/recommend.md 已建；复验 secret/supply_chain 全绿；迁移 N/A（只读，无新表）；§6.3 双视角审查 High 全清零（PLAT-1~5），0 High。13/13 门禁全绿 | /api/v1/recommend/metrics,/api/v1/recommend/terms,/api/v1/recommend/metrics/{metric_id}/related | verified → released（机械门槛全达，§1.5 人工 ratify 待补） |
| 2026-08-10 | — | glossary | **工业级夯实 → released**：runbook docs/runbooks/glossary.md 已建；复验 secret/supply_chain 全绿；迁移 0008_glossary up/down/up 可逆（链 0001→0012 已修复）；§6.3 双视角审查 0 High（安全/混沌/可观测门禁已补），0 High。13/13 门禁全绿 | /api/v1/terms,/api/v1/terms/conflicts,/api/v1/terms/relations | verified → released（机械门槛全达，§1.5 人工 ratify 待补） |
| 2026-08-10 | — | dimension | **工业级夯实 → released**：runbook docs/runbooks/dimension.md 已建；复验 secret/supply_chain 全绿；迁移 0009_dimension up/down/up 可逆；§6.3 双视角审查 0 High（安全/混沌/可观测门禁已补），0 High。13/13 门禁全绿 | /api/v1/dimensions,/api/v1/dimensions/mappings,/api/v1/dimensions/reconciliations | verified → released（机械门槛全达，§1.5 人工 ratify 待补） |
| 2026-08-11 | — | governance | **D9 被遗忘权（R7-09③）收尾 + 缺陷修复**：补全 service/API 单测（backend/tests/unit/test_governance_erasure.py 4 项 + backend/tests/security/test_governance_security.py 补 4 项，pytest 全绿、mypy/ruff 0 error）；修复 `_scrub_pii` 裸字符串替换缺陷——当 detail_json 中主体 id 以数值形式出现（如 `{"uid":42}`）时原实现会破坏 JSON 结构导致擦除 500，改为结构化递归替换并在数值型 user id 命中时以 token 字符串替换保持 JSON 合法 | /api/v1/erasure | implemented（测试收尾 + 缺陷修复） |
| 2026-08-11 | — | quality | **D11 外部基准对账（FR-10）实现**：新增 external_benchmark / reconciliation_record 两表（迁移 0014，可逆：downgrade 顺序 DROP 索引→DROP 表）；6 个 API 端点——POST /benchmarks/import（幂等 upsert，key=source_id+metric_code+bench_date+dims JSON 规范化比对）、GET /benchmarks、POST /benchmarks/{id}/bind（声明目标指标/口径/容忍率）、POST /reconciliation/run（基准值 vs 观测值，差异率自动判定 OK/WARN/ALERT，ALERT 发 reconciliation.alert 事件）、GET /reconciliation-records、POST /reconciliation-records/{id}/confirm（Owner 确认 reasonable/caliber_error）；RBAC：导入/绑定/执行需写权限，确认需治理权限，列表需读权限；单测 7 项 + 安全测 8 项全绿，mypy --strict 0 error。FR-10 余下 动态基线/同环比/跨源/修复建议 仍规划待实现 | /api/v1/quality/benchmarks/import,/api/v1/quality/benchmarks,/api/v1/quality/benchmarks/{id}/bind,/api/v1/quality/reconciliation/run,/api/v1/quality/reconciliation-records,/api/v1/quality/reconciliation-records/{id}/confirm | released（FR-10 外部基准对账子集实现，余下子项规划待补） |
| 2026-08-11 | — | semantic | **D10 §6.3 双视角审查闭环（补齐缺失记录）**：复审发现并修复 2 处 High——① PII 合规复核端点 `/{metric_code}/pii-review` 原允许 metric_owner 自审，违背治理 COMPL-2 职责分离：端点限 platform_admin/domain_admin，service 层加 `owner_id != actor_id` 守卫（SELF_REVIEW_BLOCKED）；② 创建/更新/发布/废弃/PII复核 5 个写端点此前零审计，违反 TD §15.4：全量补 `write_audit`（写操作前置，与业务同事务由会话依赖统一提交）。新增 backend/tests/security/test_semantic_security.py（5 项：pii-review 角色闸 + 4 写端点审计落库）+ backend/tests/unit/test_semantic_service.py 补 2 项（自审守卫正反例）；pytest 20 项全绿，ruff 全清。结论：0 High、2 High 已闭环；满足 released 机械门槛，仅余 §1.5 人工 ratify | /api/v1/metric-definitions/* | released（§6.3 审查记录补齐，D10-1 关闭） |
| 2026-08-11 | — | (8 服务) | **D10-2 门禁登记补齐**：ai/quality/notify/observability/assetmap/recommend/glossary/dimension 的 `gateways_passed` 补登 `secret`/`supply_chain`（CI/.gateways.yml 全局 protected 门禁，gitleaks/pip-audit 已在仓库级复验）。module-status.yaml 14 服务 gateways_passed 口径一致，doc_sync 校验通过 | — | released（D10-2 关闭） |
| 2026-08-11 | — | semantic | **修复 list_metrics / get_metric 缺失 return 生产级缺陷（附带发现）**：两端点的 `return ok(...)` 被包在 `if pii_flag/if pii_codes:` 内，当指标非 PII 或无 PII 指标列表时函数无返回值 → 响应经 `ApiResponse` 校验失败抛 500（ResponseValidationError）。将 `return` 移出条件块（审计仍条件执行）。该缺陷同时导致 `test_list_metrics_success`/`test_read_pii_audit` 偶发 500，修复后 28 项语义相关测试全绿 | /api/v1/metric-definitions,/api/v1/metric-definitions/{metric_code} | released（一致性缺陷修复，无状态变更） |
| 2026-08-11 | — | governance | **D10 §3.5 授权回收范围校验缺口补齐**：新增 `GovernanceService._assert_revoke_scope`——platform_admin 全局可回收；domain_admin 仅可回收本域授权（无域归属视为跨域 fail-closed）；其余角色仅可回收本人授权。单条回收端点 `DELETE /grants/{id}` 角色闸门由 `_GRANT_ADMIN_ROLES` 放宽至全体已登录用户（范围由服务层收敛，支持 owner 自管）；批量回收 `_revoke_matching` 同样接入范围校验（域管理员仅限本域）。`tests/unit/test_governance_service.py` + `tests/security/test_governance_security.py` 新增 10 项（平台/域管/本人/越权 全路径），pytest 52/52 全绿，ruff 全清。module-status.yaml governance 状态更新（medium_resolved 2、updated 2026-08-11） | /api/v1/grants/{id},/api/v1/grants/batch | released（安全缺口修复，无状态变更） |
| 2026-08-11 | — | lineage | **深度列血缘（CTE/子查询/表达式）落地（Epic 1）**：`parser.extract_field_lineage` 基于 sqlglot `build_scope` 递归解析作用域，将每个投影列解析到真实源列（跨多层 CTE 与子查询），派生表达式（如 `a.col + b.col`）记入 `FieldEdge.expression`；`_collect_ctes` 从完整 AST 收集 CTE（CTE 挂在顶层 DML 节点而非内层 Select）。新增 `backend/tests/unit/test_lineage_parser.py` 5 项（CTE 深度/嵌套链/派生表/表达式/UPDATE 降级），pytest 12 项血缘测试全绿，ruff/mypy --strict 全清 | 内部 parser，无新端点 | released（能力增强，无状态变更） |
| 2026-08-11 | — | collector | **全量采集接入异步队列（Epic 2）**：新增 `CollectionQueue`/`JobStore` Protocol + `InMemoryCollectionQueue`（默认单例）+ `RedisJobStore` + `ArqCollectionQueue`（生产惰性导入 arq）；`service.schedule_collection`/`get_job_status` 投递进队并返回 job_id/状态；worker 任务体 `run_collection_task` 复用注入 svc/collector 并回写 COMPLETED/FAILED。新增 `backend/tests/unit/test_collector_queue.py` 5 项（入队/调度+状态/缺失源/任务体完成与失败回写），pytest 全绿，ruff/mypy --strict 全清 | POST /collector/{source_id}/schedule,GET /collector/jobs/{job_id} | released（能力增强，无状态变更） |
| 2026-08-11 | — | conflict | **LLM 补位冲突检测（Epic 3）**：新增 `ConflictLlmClient` Protocol + `DeterministicFallbackLlmClient`（默认静默弃权，不依赖外部 LLM）+ `HttpLlmClient`（OpenAI 兼容 `/v1/chat/completions`，仅 `llm_base_url` 配置时启用，fail-soft）。`detect_conflict` 在 borderline（0.45≤定义相似度<0.85 或 0.5≤综合<0.85）时经 LLM 确认同语义后升格为 `SAME_DEF_DIFF_NAME` soft conflict；`service.check` 对 borderline 候选调用 LLM 补位。新增 `backend/tests/unit/test_conflict_llm.py` 9 项，pytest 19 项冲突测试全绿，ruff/mypy --strict 全清 | 内部 detect，无新端点 | released（能力增强，无状态变更） |
| 2026-08-11 | — | governance | **PII 字段级脱敏二次校验（Epic 4，依赖 governance）**：`validate_pii_masking` 在 DB 落库脱敏之外二次核验 ①指标须通过合规复核 ②字段级脱敏策略非 none ③口径定义明文暴露（先剔除 hash/mask 等脱敏函数调用再比对 PII 字段名）；`pii_review` 写入 `secondary_validation`（`PiiSecondaryValidationResult`），新增 `POST /governance/pii/validate`（写 `PII_SECONDARY_VALIDATION` 审计，pii_access=True）。新增 `backend/tests/unit/test_governance_pii_validation.py` 5 项（非 PII 通过/未复核失败/明文暴露失败/干净通过/未知抛 NotFoundError），pytest 全绿，ruff/mypy --strict 全清 | POST /governance/pii/validate | released（能力增强，无状态变更） |
| 2026-08-11 | — | quality | **动态基线 / 同环比 / 跨源检测（Epic 6）**：新增 `quality_observation` 时序表（迁移 0015_quality_observation，up/down 可逆）+ `POST /quality/observe` 写入样本；`detect` 按 `rule_mode` 分派——DYNAMIC_BASELINE（历史窗口中位数±σ 越界，冷启动退化为 static_fallback）、YOY_WOY（对照期观测超差异率）、CROSS_SOURCE（多 source 最新值 spread 超容忍率），单条规则评估失败不阻断其他规则；`create_rule`/`update_rule` 放开四种模式（仅校验 threshold 为字典）。新增 `backend/tests/unit/test_quality_advanced_modes.py` 13 项，pytest 全绿，ruff/mypy --strict 全清 | POST /quality/observe | released（FR-10 高级检测子集实现，修复建议生成仍规划待补） |

| 2026-08-11 | — | quality | **FR-10 修复建议生成（Epic 7 收口）+ 三道门禁 live 复测**：`quality_event.repair_suggestion` JSON 列（迁移 0016_quality_repair，up/down 可逆）+ `_build_repair_suggestion` 在 detect 命中即生成（责任方/上游 collector_job 引用/建议 SQL 骨架/观测基线，owner_hint 注明沿血缘反查）+ `POST /quality/events/{id}/repair`（GOV 角色）确认留痕（confirmed_by/confirmed_at）；新增单测 3 项（detect 生成/confirm 留痕/非 OPEN 拒绝）。integration 2 passed（隔离库 unisense_gate，迁移 0016 经 alembic upgrade head 建列验证）、chaos 3 passed（全 mock）、perf_baseline k6 打真实端点 P95=23.78ms(<800)/错误率0%/检查 2548 全绿（证据 backend/tests/reports/quality_gateways_2026-08-11.txt） | POST /quality/events/{id}/repair | released（FR-10 全量闭环达成；§1.5 人工 ratify 待补） |

| 2026-08-11 | — | assetmap | **P2: AssetMap 图谱/热力/Owner 视图**：service 层新增 `get_graph(domain,depth,pii_only)`（Neo4j Cypher+CircuitBreaker+MySQL fallback）、`get_heatmap(dimension)`（聚合分桶）、`get_owner_view(owner_id)`（按责任人聚合）；repository 新增 `graph_from_mysql/heatmap_aggregation/owner_aggregation`；API 新增 GET `/graph`, `/heatmap`, `/owner-view`（RBAC+注入守卫）；前端 `AssetMap.tsx`（Ant Design 图谱/热力/Owner 三 Tab） | GET /api/v1/asset-map/graph, /heatmap, /owner-view | released（P2 增强） |
| 2026-08-11 | — | recommend | **P2: 协同过滤推荐**：基于 `tracking_events` 用户行为（query/favorite/browse/search）计算 Jaccard 相似度，`_MIN_OVERLAP=2` 阈值过滤噪声用户，推荐相似用户偏好指标；`_lineage_fallback` 作为冷启动兜底 | GET /api/v1/recommendations（逻辑增强） | released（P2 增强） |
| 2026-08-11 | — | llm | **P2: LLM 结构化输出**：`chat()` 返回 `LlmStructuredOutput`（content+confidence+reasoning+candidates+model+usage），`confidence` 钳位 [0,1]，非 JSON 输出自动降级 confidence=0.5；`DeterministicFallbackLlmClient` 返回结构化格式 confidence=0.0；新增单测 4 类 | 内部 client 接口变更 | released（P2 增强） |
| 2026-08-11 | — | collector | **P2: LLM 敏感分级路由**：`register_catalog` 调用 `_llm_classify_sensitivity()`，confidence≥0.7 自动采纳、<0.7 标记 `needs_review`（不自动落库敏感级别） | POST /api/v1/catalogs（逻辑增强） | released（P2 增强） |
| 2026-08-11 | — | audit | **P2: 审计归档**：新增 `AuditArchiveLog` 模型 + `audit_log.archived` 字段+索引；Arq 定时任务 `audit_archive`（30 天+已归档过滤，JSONL 批量导出→MinIO 上传，记录归档日志）；新增单测 8 项 | 内部定时任务，无新端点 | released（P2 增强） |
| 2026-08-11 | — | governance | **P2: PII 沿血缘传播**：`propagate_pii_to_metric` 检查上游 source_column PII 标记+上游 metric pii_flag，自动设置 metric.pii_flag=True + definition_json.pii=True，发布 `pii.propagated` 事件；`lineage_edge.pii_inherited` 字段（迁移 0017） | 内部服务方法 | released（P2 增强） |
| 2026-08-11 | — | (cross-cutting) | **P3: BaseService Protocol 迁移**：所有 14 服务继承 `BaseService`（`super().__init__(db)`），统一 `_write_audit`/`_publish_event` 能力；`EventBus.publish` 替换 governance `_safe_publish` 散布实现 | 内部服务基类 | released（P3 跨切面） |
| 2026-08-11 | — | (cross-cutting) | **P3: datetime.utcnow() → datetime.now(UTC)**：全量替换 quality/notify/conflict/dimension/observability 服务 + glossary/dimension 模型 default lambda | 内部代码质量 | released（P3 跨切面） |
| 2026-08-11 | — | semantic | **P2: 指标模板化注册**：新增 `MetricTemplate` 模型（code/name/domain/defaults_json/required_fields/预设字段）+ `metric.template_id` FK；语义 API 新增 GET/POST `/semantics/templates`、GET `/semantics/templates/{id}`、POST `/semantics/templates/{id}/instantiate`（合并模板默认值+用户覆盖→创建指标） | GET/POST /api/v1/semantics/templates | released（P2 增强） |
| 2026-08-11 | — | semantic | **P2: 消费者仪表盘+消费指南**：GET `/semantics/dashboard`（按域/Owner 聚合：总数/按状态/按分级/按域/PII占比）；GET `/semantics/consumption-guide/{metric_id}`（返回预设指南或自动生成基础指南） | GET /api/v1/semantics/dashboard, /consumption-guide/{id} | released（P2 增强） |
| 2026-08-11 | — | observability | **P2: NPS 采集+反馈采纳闭环**：`submit_nps(user_id,score,comment)` 0-10 评分+`nps.submitted` 事件；`update_feedback_status(feedback_id,status,resolver_id,resolution_note)` 状态更新(adopted/rejected/in_progress)+`feedback.status_updated` 事件；API 新增 POST `/observability/nps`、PATCH `/observability/feedback/{id}/status` | POST /api/v1/observability/nps, PATCH /api/v1/observability/feedback/{id}/status | released（P2 增强） |
| 2026-08-11 | — | (db) | **P2: Alembic 迁移 0017**：audit_log.archived+索引、audit_archive_log 表、metric_template 表+索引、metric.template_id FK、lineage_edge.pii_inherited；downgrade 全量可逆 | — | released（迁移） |

| 2026-08-12 | — | collector | **工业级修复（35项缺陷全部修复，达标率40.5→≥88）**：① 7种数据源连接器(mysql/postgres/hive/doris/clickhouse/kafka/starrocks)+CollectorRegistry插件式注册(@registry.register装饰器)；② Schema Drift检测(内容指纹SHA-256+DriftDetector diff计算+SchemaDriftLog变更历史+catalog_schema_drifted事件)；③ 增量采集(IncrementalCollectorMixin混合策略:MySQL UPDATE_TIME/ClickHouse metadata_modification_time支持增量,其余降级全量+CollectionWatermark水位记录)；④ 定时调度(POST /schedule cron+mode参数保存到DataSource)；⑤ 容错重构(单表try/catch跳过+failed_specs+asyncio.timeout(300)+CollectionLock分布式锁+job_id幂等SET NX)；⑥ 健康检查(collect成功→healthy/失败→unhealthy+GET /health探活端点)；⑦ 边界处理(空schema warning+schema_incomplete标记+connection_config host校验+BLE001替换为具体异常+llm_classify_error_total metric+batch事件publish_batch)。新增迁移0018(schema_drift_log+collection_watermark+data_source.schedule_cron/collection_mode+db_catalog.content_signature/schema_incomplete)。新增测试: test_incremental_collection(21项)+test_collector_chaos(11项)+test_collector(5项边界) | /api/v1/data-sources/{id}/schedule,/api/v1/data-sources/{id}/watermark,/api/v1/data-sources/{id}/health | released（工业级修复，35项缺陷全清零） |

| 2026-08-12 | — | consume(FR-06) / resilience | **FR-06 执行引擎工业级修复（含真实证据）**：①F3 熔断器半开单飞——原半开窗口放行全部请求致恢复期雪崩，改为半开仅放行一个探测、其余拒绝、探测成败即复位/重开（resilience.py）；②C6 限流器初始化未生效——`init_rate_limiter(redis)` 之前从未被调用，且 service 于 import 期冻结 InMemory 实例、Redis 分布式限流成死代码；现 main.py lifespan 调用 init 且 check_rate_limit 动态 get_rate_limiter()；③rate_limiter InMemory 降级路径 4 处 `self._fallback.allow(...)` 同步裸调未 await（返回未 await 协程恒为真、限流静默失效），全部补 await；④F4 缓存读不校验反序列化类型——损坏缓存可致下游崩溃，_get_cache 校验 rows 为 list、total 归一化、损坏即删键；⑤F2 OLAPExecutor 每请求新建不 close 连接池泄漏——改为进程内共享单例复用连接池；⑥B5 SQL 构建从伪表 `unified_metric` 改为真实物理口径 SQL `_build_query_sql`（source_table + 全参数化杜绝注入）；⑦F1 _parse_response 严格解析（列缺失/行宽不匹配即报错，杜绝静默截断，dict(zip(strict=True))）；⑧C5 429 响应不再带 Retry-After——middleware 现读 exc.ctx.retry_after 写响应头；⑨修复上轮 H-2 引入的回归：conflict chaos _BoomService.arbitrate mock 缺 actor_id 参数。新增回归测试：test_circuit_breaker_half_open_single_flight + test_build_query_sql_*（参数化注入/缺省表/多值IN）。验证：mypy --strict 6 文件通过 / ruff+format 全清 / test_consume_service(17)+chaos(8)+conflict_chaos+security consume/quality/conflict 全绿；cache_fallback 4 项为既有 schema 漂移（MetricResponse.emergency_publish/pending_conflict 为 None），与本次改动无关；integration 6 项为 MySQL 未启动 env 问题 | /api/v1/consume/*,(FR-06 执行引擎) | released（FR-06 工业级修复） |

| 2026-08-12 | — | lineage | **生产级重构 → 实际生产数据血缘平台**：① Neo4j 图读路径（graph.query_impact 图优先 + MySQL 兜底，修复 Cypher 参数作变长关系上界的 SyntaxError）；② 循环依赖检测（建边前 would_create_cycle → 409，TD §12.2.2 通道③）；③ 血缘历史快照（lineage_edge_history WORM，R10-04，迁移 0019）；④ 指标级血缘 L3 + EXTERNAL_BREAK 断链登记（R10-03）；⑤ what-if 变更影响预览（POST /lineage/impact-preview + risk_level）；⑥ PII 沿血缘传导（propagate_pii 写 pii_inherited）；⑦ 影响分析 Redis 缓存（TTL 60s）+ 分页信封；⑧ Parser 生产级（SELECT*/MERGE/UNION/多语句拆分 sqlparse + 方言降级）。门禁：606 unit / ruff 0 / mypy 0(141文件) / 血缘核心覆盖率 repository97% service94% graph100% events100% parser83%；容器实跑 Neo4j 图遍历 downstream/upstream 验证通过。提交 a1abcdb | /api/v1/lineage/*（impact/impact-preview/edges 分页） | released（生产级重构） |

| 2026-08-12 | — | semantic | **FR-07 指标管理生产级修复（含端到端真实证据）**：① 评审闭环——submit-review→REVIEW→approve/reject→PUBLISHED + MetricStateMachine 6 态 + Metric.submitted_by（迁移 0021）+ 自审拦截 SELF_REVIEW_BLOCKED（端到端实测 400）；② owner 越权防护——_assert_owner_or_admin 覆盖 update/publish/submit/review/deprecate 全写路径（同角色 metric_owner 越权改他人指标 → 403 实测）；③ PII 分级脱敏——redact_definition 非敏感角色读取口径脱敏为 ***（实测）；④ PII 双源归一 _normalize_pii（pii_flag↔definition_json.pii）；⑤ P0 update 崩溃修复——change_reason 不再写 Metric 主表（CompileError 消除）；⑥ 版本确认三端点 confirm/reject/extend version（FR-007/008）+ pending_version_manager；⑦ 写端点补注入守卫；⑧ 修复并行代理契约断裂（MetricSubmitRequest/ApproveRequest/RejectRequest 命名统一）+ 迁移多头合并（0019_semantic_state_machine 顺延 0020 单链）。门禁：643 unit / ruff 0 / mypy 0(147文件)；端到端真实 MySQL：评审闭环/IDOR 拦截/PII 脱敏全部验证。提交 37df13e/d17a279/本轮集成 | /api/v1/metric-definitions/*（submit-review/review/confirm-version/reject-version/extend-version） | released（FR-07 生产级修复） |

| 2026-08-12 | — | ai / llm | **LLM 接入生产级补强（OpenAI 协议兼容，含国内主流大模型）**：services/llm/client.py 支持 openai/deepseek/qwen/ernie/kilo 多提供商；NL2SQL 真实走 LLM（anchored 词表锚定 + method=llm，网关不可达降级 keyword）；json_object 结构化输出（LlmStructuredOutput: content/confidence/reasoning/candidates + Pydantic 校验）；kilo.ai 网关接入（UNISENSE_LLM_BASE_URL/API_KEY/MODEL，容器实跑 method=llm 验证）；conflict borderline LLM 补位统一走 HttpLlmClient。提交 8906d50 | POST /api/v1/ai/nl2sql | released（LLM 真实接入） |

| 2026-08-12 | — | notify | **通知真实外发**：SMTP 邮件（aiosmtplib，新增依赖）/ 钉钉 Webhook 机器人（markdown 告警/待办/升级模板）/ Webhook 三渠道适配器 + _dispatch 真实投递回写 SENT/FAILED + delivered 计数；共享 httpx.AsyncClient 单例（连接池复用）；事件发布链路补齐（notify_url 运行时注入 + 订阅扇出即时投递）；测试 test_notify_channels 全绿。提交 0861e68 | POST /api/v1/notify/events（即时投递） | released（真实外发） |

| 2026-08-12 | — | frontend | **前端生产级扩展（15+ 页面 + 测试体系）**：新增 AiAssistant/QueryWorkspace/QualityCenter/Governance/Glossary/Dimensions/DataSources/Catalogs/Notifications/Observability/AuditLog/ApiClients/Templates/MetricReview/AssetMap/ConsumptionGuide/Dashboard 页面 + 统一 Layout/theme；血缘分页 + what-if 预览 + PII 列适配；测试体系 Vitest（AssetMap/ConsumptionGuide/Dashboard 12 passed）+ Playwright e2e 脚手架 + typecheck 0 error + production build 通过。提交 8ab4739 | 前端全部路由 | released（前端生产级扩展） |

---

## 2026-08-17 补充条目：指标模块全维度修复完整提交清单（P0–P3 + 遗留清理）

> 本条目为上方 2026-08-17「指标模块全维度审查修复（P0-P2 全量落地，P3 验证）」的**提交清单补充**——按提交顺序补登该次修复的全部 18 个提交（含 2 项遗留清理），便于追溯每条变更的落点。

| # | 提交 | 提交信息 |
|---|------|----------|
| 1 | `e102261` | [semantic] fix: mark-source-dropped 越权收紧——仅管理角色可批量置 DSD（P0-1） |
| 2 | `e314c3c` | [semantic] fix: LLM 额度防护——suggest-rename/auto-suggest 读角色收紧为写角色 + auto-suggest 显式 schema 防非字符串 sql→500（P0-2） |
| 3 | `daf3ddc` | [semantic] fix: 批量端点异常信息脱敏——public_error_message 助手透传业务消息、未知异常泛化防泄漏（P0-3） |
| 4 | `ce96d4c` | [collector+semantic] feat: 采集触发 DSD 闭环——全量采集检测源表 DROP 沿血缘置下游指标 DATA_SOURCE_DROPPED + mark_source_dropped 支持按表名精确过滤 + 前端采集结果展示下线指标数（P1-4） |
| 5 | `73da8fc` | [semantic] feat: 健康恶化每日告警闭环——每日刷新发现 WARNING/CRITICAL 定向通知指标 Owner+备份 Owner（P1-5） |
| 6 | `327eee6` | [semantic] feat: 紧急发布补审闭环——新增 emergency-review 端点写 emergency_reviewed_at + 服务层 complete_emergency_review + 前端详情页补审入口（P1-6） |
| 7 | `0e38b2c` | [semantic] feat: 灰度超期强制回收——每日巡检超30天 EXPERIMENTAL 通知 Owner+回收至 DRAFT（状态机新增 expiry_recycle 跃迁 + metric.gray_recycled 事件；reject 通道收紧仅 REVIEW）（P1-7） |
| 8 | `fbe6922` | [semantic] refactor: 合并 PendingVersionManager 双实现——删除 manager 死方法（confirm/reject/extend/check_timeouts/pause_on_drift），确认/拒绝/延期/超时规则唯一实现在 MetricService（P1-8） |
| 9 | `682c40b` | [consume] refactor: 前端发布语义统一——删除 publishMetric 死代码（发布实际走 approve）+ 批量提交改走原子 /batch-submit（替代 N 次 submitReview 循环）（P2-9） |
| 10 | `e63da57` | [consume] fix: rollback/promote 加确认弹窗——全量发布确认灰度转正式；回滚标注高风险，危险按钮二次确认（P2-10） |
| 11 | `8be8391` | [semantic+glossary] feat: 术语绑定写路径 + 冲突关联指标——新增 PUT /{code}/term 写 metric.term_id + 前端搜索绑定/解绑；GlossaryConflict.ref_metric_id 填充绑定术语的指标（P2-11） |
| 12 | `88ce00b` | [semantic] feat: 健康度质量维度接入 quality_event——近30天未关闭质量异常按等级反比扣分（P0重/P1中/P2轻），兑现 TD §12.3（P2-12） |
| 13 | `514704d` | [governance] docs+ci: 文档漂移修复 + 前端门禁落地——TD 检索架构改 MySQL LIKE（ES 未落地）、TD §12.3 健康度模型对齐实际、DEV_GUIDE §8 前端门禁改 tsc+vitest；CI 新增 frontend-gateways job（typecheck+vitest）（P2-13） |
| 14 | `401bd67` | [consume] refactor: 前端重复常量收敛——STATUS_COLOR/LABEL 归入 enums.ts，EDGE_TYPE_LABEL 按语义拆分 METRIC_RELATION_EDGE_LABEL（推荐关联）与 LINEAGE_EDGE_TYPE_LABEL（血缘图）（P2-14） |
| 15 | `3b5f7df` | [semantic] test: 补齐生命周期零 API 测试——archived/归档/restore/promote/rollback/emergency-publish/compare/batch-register/DSD 三端点契约测试 + mark-source-dropped 越权门禁（P3-15a） |
| 16 | `d9e8791` | [governance] docs: CHANGELOG 追加指标模块 P0-P2 全量修复 + P3 验证记录（P3-15b） |
| 17 | `ce3aff0` | fix(test): 修复 semantic_cache 测试债务（fake_metric 补齐 MetricResponse 演进字段）+ 清理 scripts/ ruff 违规（E501 断行/import 排序/SIM105）（遗留清理 1） |
| 18 | `9ac9a33` | fix(test): 同步 list_catalogs 转义断言到 / 转义实现（repository.py 已改 ESCAPE '/'，测试断言仍为反斜杠）（遗留清理 2） |

| 2026-08-12 | — | (db) | **Alembic 迁移链收敛**：0019_lineage_edge_history（血缘历史快照）+ 0019_semantic_state_machine 顺延为 0020（消除多头）+ 0021_metric_submitted_by（metric.submitted_by）；0018_collector_drift_watermark 先行；单链升级 0017→0021 在真实 MySQL 验证通过 | — | released（迁移收敛） |


| 2026-08-12 | — | consume / notify | **consume/notify 端到端联调修复（docker 容器实跑）**：端到端验证暴露并修复 3 个单测覆盖不到的 bug——① notify channel 枚举漂移（模型 6 值 vs 迁移 4 值 → 写入 DINGTALK/console 报 Data truncated 1265）→ 新增迁移 0022 扩展；② notify API 直接返回 ORM 对象（Unable to serialize 500）→ 全端点 from_model 序列化；③ _dispatch 渠道大小写漂移（DB 存大写枚举值但比较小写 → 除 console 外全部渠道投递失败）→ 归一化比较 + SubscriptionUpsert channel validator。consume 实跑验证：dry-run 真实参数化 SQL / OLAP 未配置 503 降级 / 越权三层拦截（FORBIDDEN_METRIC/DOMAIN/401）/ QPS 限流 429 retry_after / 收藏 CRUD / 审计留痕；维度标识符纵深防御（FORBIDDEN_DIMENSION）。补充防回归单测 43 notify 用例全绿。提交 9603002 | POST /api/v1/notify/events、/consume/query | released（端到端实跑验证通过） |

| 2026-08-12 | — | (db) | **迁移链健壮性加固（全新环境 alembic upgrade head 可跑通，双重验证）**：① 0018 建 schema_drift_log/collection_watermark 显式 mysql_charset=utf8mb4——修复库默认 collation 为 utf8mb4_unicode_ci 时 FK fk_drift_log_source 报 MySQL 3780 崩溃（加固前仅标准 0900_ai_ci 库默认可跑通）；② 新增 0023_tracking_softdelete 固化 tracking_event 表 + metric_template.deleted_at 列（原仅临时 SQL 修补，全新环境缺失导致 recommend 协同过滤 / 语义模板页 500；幂等：存量库对象已存在则跳过，仅推进版本号）；③ seed_admin.py 的 UNISENSE_SEED_ADMIN_PASSWORD 环境变量路径实跑验证（创建/幂等不覆盖/哈希校验注入值生效）；④ 后端镜像重建 + 容器重启启动链路验证（alembic upgrade head 幂等 no-op + uvicorn healthy）。提交 9a71f38 | /api/v1/recommend/*、/api/v1/semantics/templates | released（迁移链收敛加固） |

| 2026-08-12 | — | semantic | **工业级整改 batch2（38项缺陷修复）**：① api/metrics.py 补4端点：emergency-publish(跳REVIEW不跳PII)/health(五维健康度)/compare(双指标对比)/batch-register(批量注册+审计)；② api/semantic.py 重构：dashboard→repository.aggregate_dashboard(单次CASE WHEN)、消费指南→service.get_consumption_guide(缓存)、模板创建→MetricTemplateCreateRequest Schema校验、清理残余ORM代码；③ service.py update_metric PUBLISHED+破坏性变更走PENDING_VERSION(PendingVersionManager.create_pending_confirmations)；④ service.py review_compliance 增pii_flag门禁(非PII指标无需合规复核→PII_FLAG_REQUIRED)；⑤ 新增test_health_scorer.py(7测试类+15用例)和test_semantic_chaos.py(6测试类+8用例)；⑥ 全部Python语法通过 | /api/v1/metric-definitions/{code}/emergency-publish, /health, /compare, /batch-register; /api/v1/semantics/dashboard, /consumption-guide | released → implemented（工业级整改中） |
| 2026-08-12 | — | frontend (Layout) + api/preferences | **侧边栏折叠偏好按用户持久化（服务端为准 + 本地 per-user 缓存加速）**：① 后端新增 GET/PUT/DELETE `/api/v1/me/preferences`（复用 user_preference 表，user_id 一律取自认证身份，禁止跨用户读写）；② _upsert 修复软删除 + 唯一键冲突——恢复软删除行而非 INSERT，避免 Duplicate entry uk_pref（生产环境真实复现并修复）；并发唯一键冲突 IntegrityError 兜底；③ 前端 Layout.tsx：localStorage key 带 user.id（跨用户隔离）+ 挂载时 fetch 服务端偏好为准并回写本地缓存 + 切换防抖 400ms 落库 + 切换用户时重载；④ 验证：tsc 0 错、vitest 20/20（Layout 8 项含 per-user 隔离 + 服务端覆盖 + 防抖写入）、pytest 偏好 8/8、docker 容器生产链路 :8180 端到端验证（折叠→服务端持久化→刷新保持→per-user localStorage 键→展开恢复）。提交 62b6ee0 | /api/v1/me/preferences | released（按用户记住偏好） |

| 2026-08-13 | — | assetmap | **写能力 + 聚合缓存 + perf 基线实跑（FR-18 资产工作台生产化）**：① 新增 4 个写端点（POST /entities/{id}/owner、/sensitivity、/batch-owner、/batch-sensitivity），仅 platform_admin/domain_admin，写入与审计同事务原子提交（PLAT-3），RBAC 403/成功/404/422 全覆盖；② repository 写方法（get_catalog_entity/list_catalog_entities/user_exists/assign_owner/reclassify/batch_*），service 存在性与用户校验（NotFoundError）+ 批量空集防护；③ 聚合 cache-aside TTL30s + 熔断（读/写双防雪崩），Redis 不可用静默回源；④ 测试 87 全绿（service31/repo41/security15），覆盖率 93%（api81/repo97/svc97/schemas100）；⑤ perf 基线 k6 实跑入库（assetmap_perf_2026-08-13.txt）：10 VU/30s/2700 请求，P95=19.97ms（契约<800ms）、失败率 0.00%；⑥ 端到端实跑（本地 uvicorn + docker MySQL）：认领/重分类/批量 200、404/422、reviewer 403、审计 4 条留痕、DB 生效（owner_id/sensitivity 更新）。提交 aacac22 | /api/v1/assetmap/entities/{id}/owner, /sensitivity, /batch-owner, /batch-sensitivity | released（写能力+缓存+perf 基线入库） |

| 2026-08-13 | — | resilience/health (FR-17 能力降级矩阵) | **FR-17 降级矩阵工业级补齐（逐行审查发现 5 项缺陷，全部修复并验证）**：TD §5.2/§4.13 定义的降级矩阵此前仅实现熔断器+舱壁骨架，缺审计/恢复事件/持久化。修复：① **审计与恢复事件缺失（TD §5.2.4/§5.2.5）**→ `CircuitBreaker` 新增 name + 全局降级监听器注册（`register_degradation_listener`），状态切换仅在「关闭→打开」发出 DEGRADED、仅在「打开→关闭（恢复）」发出 HEALTHY（不重复刷）；`main.lifespan` 注册监听器经 `fire_degradation_event` 异步落库+发事件；② **降级事件无持久化（TD §4.13 degradation_event 表从未建模/迁移）**→ 新增 `DegradationEvent` 模型 + 迁移 `0024_degradation_event.py`（可逆，down_revision=0023）；`core/degradation.py` 的 `record_degradation` 用**独立会话** best-effort 落库（独立于请求事务，避免随主事务回滚丢失）+ publish `degradation.state_changed` 事件，DB/EventBus 任一不可用仅告警不阻断；③ **ES 熔断器死代码**→ 命名 es_breaker=ES 并注明 ES 为可选依赖尚未接入客户端（已知缺口，非误用）；④ **就绪探针阻塞事件循环**→ `health.py` 的 `optional_dependency_status()` TCP 探活改 `asyncio.to_thread` 执行，不再阻塞事件循环；⑤ **配置级降级不上报**→ `consume.service.execute_query` 在 OLAP 未配置时 `fire_degradation_event("OLAP","olap","DEGRADED","olap_not_configured")`。新增单测：test_resilience.py（3，验证半开单飞回归锁 + 状态切换各上报一次）/ test_degradation.py（3，验证持久化+事件总线+DB/总线失败 best-effort）。验证：ruff check/format 全清；mypy --strict 6 源文件 0 error；pytest 新 6 passed + consume 单元 17 + chaos 9 全绿；import 链路无环。已知缺口：TD §4.13 的 `dependency_health`（实时熔断态）表未单独建，当前熔断态在内存且由 degradation_event 历史可推导，列入后续迭代。 | /ready、degradation.state_changed 事件、degradation_event 表 | released（降级矩阵工业级补齐） |

| 2026-08-13 | — | resilience/degradation/es (FR-17 缺口补齐) | **FR-17 两处已知缺口补齐（dependency_health 实时表 + ES 客户端接入，达工业级）**：闭合前序 FR-17 行「已知缺口」——① **TD §4.13 `dependency_health` 实时熔断态表**：新增 `DependencyHealth` 模型（dependency_type/dependency_id/status/circuit_state/consecutive_failures/latency_p95_ms/error_rate_pct/last_check_at/circuit_opened_at/meta，唯一约束 (dependency_type,dependency_id) + 双索引）+ 迁移 `0025_dependency_health.py`（down_revision=0024，可逆）；`core/degradation.py` 新增 `handle_circuit_signal` 监听器，将 `CircuitBreaker.DegradationSignal`（circuit_state 映射 CLOSED/OPEN/HALF_OPEN，PROBING→DEGRADED）经 MySQL `INSERT ... ON DUPLICATE KEY UPDATE` UPSERT 实时写入 `dependency_health`（熔断态/健康态/连续失败数/最近探活时刻/探针 meta），与 `degradation_event` 审计表职责分离（后者仅落 DEGRADED/HEALTHY 审计行）；② **ES 客户端接入（消除 es_breaker 死代码）**：新增 `core/es_client.py`——守卫式导入 `elasticsearch`（可选依赖，缺包时客户端自动禁用不崩溃），`EsClient.search/index/health` 真实走 `es_breaker.allow()` 并 record_success/record_failure，熔断抛 `CircuitOpenError`、禁用/异常抛 `SearchUnavailableError`；`/ready` 在 ES 启用时以真实 `.ping()` 探活取代 TCP 桩，使 es_breaker 进入真实调用路径；`get_es_client()` 单例。`CircuitBreaker` 增强：`DegradationSignal` 数据类 + `register_degradation_listener` 注册表（resilience 层对 db/eventbus 零依赖）。新增单测 10 项：test_dependency_health.py（4：UPSERT/持久化双写/信号映射/`handle_circuit_signal`）+ test_es_client.py（6：缺包禁用/无 URL 禁用/检索走 breaker/熔断拒绝/health/单例）。验证：mypy --strict 全量 155 源文件 0 error（顺带清理 lineage/parser.py 一处 sqlglot 升级后失效的 `# type: ignore`）；ruff check+format 全清；pytest 定向 16 passed（resilience3+degradation3+dependency_health4+es_client6）+ 回归 test_health/test_consume_service 24 passed 全绿；py_compile 通过；迁移链 0023→0024→0025 线性；`app.main` 导入无环、监听器身份 = `handle_circuit_signal`。 | /ready（ES 真实探活）、dependency_health 表、es_breaker 调用路径 | released（降级矩阵缺口补齐 → 工业级） |
| 2026-08-13 | — | subject_domain + system_dict + auto_fill (FR-001~FR-016) | **主题域管理 + 系统字典 + 指标注册全字段自动化（新模块）**：① SubjectDomain模块（FR-001~FR-004/FR-013）：ORM模型+Alembic 0026迁移(邻接表+物化路径3层限制)+服务层(CRUD+树查询+默认值+停用/删除约束)+API 8端点 `/api/v1/domains`+前端SubjectDomain.tsx管理页；② SystemDict模块（FR-005~FR-007/FR-014）：ORM模型(uk_dict_type_code)+服务层(按类型查询+增删改+启停用+引用计数)+API 8端点 `/api/v1/dicts`+前端SystemDict.tsx管理页；③ auto_fill引擎（FR-010/FR-011）：纯函数式auto_fill.py(4段编码拼接+格式校验+保留词检测)+POST /auto-suggest API；④ MetricService集成（FR-008/FR-009/FR-016）：create_metric域校验+字典校验+auto_fill自动补全；batch_register适配字典校验；MetricCreateRequest新增source_table/measure_column/period；⑤ 前端MetricCreate重构（US5）：Cascader级联选域+Select字典下拉+半自动编码输入；⑥ seed脚本：6标准域+10类字典项；⑦ 枚举扩展：DomainStatusEnum/DictStatusEnum/DictTypeEnum。单元测试3组+集成测试1组。导航新增"主题域管理"+"字典管理"，路由 /domains + /dicts | /api/v1/domains/*, /api/v1/dicts/*, /api/v1/metric-definitions/auto-suggest | implemented |finance'` 端到端展示。 | /data-sources/types、/data-sources/test-connection、/data-sources/{id}/check、/data-sources/{id}/collect（全库语义） | released（FR-30 数据源生产级闭环） |
| 2026-08-13 | — | quality/collector/notify/semantic (四项生产工程) | **质量自动调度 + 统一 arq worker + 备份恢复与告警升级 + QuickBI 票据（FR-10/12 生产闭环）**：① **质量自动调度（FR-10）**：新增 `quality/tasks.py` `run_quality_checks`——周期扫描全部启用规则，按 (metric_id, rule_type) 去重，取最近观测值（quality_observation，48h 新鲜窗口）自动调 detect 评估，命中落 OPEN 事件+告警，无观测跳过不误报，单条失败不阻断；repo 新增 `list_all_enabled_rules`。② **统一 arq worker**：`collector/worker.py` WorkerSettings 合并注册 7 函数 + 6 cron（采集调度/语义 PENDING 超时·健康度·紧急补审·灰度超期/质量每 5 分钟），compose worker 服务命令指向该 WorkerSettings，此前 semantic 4 个 cron 定义永不运行的缺口闭合。③ **备份恢复**：`scripts/backup.sh`（mysqldump --single-transaction --routines --triggers + 7 天轮转）+ compose `backup` 服务（每日自动，RPO≤1d）+ `docs/runbooks/backup-restore.md`（RTO≤4h 恢复流程/演练/待办）；**告警升级**：`notify/escalation.py` P0/P1/P2 分级策略（P0 10min×6、P1 30min×4、P2 不重复）+ `escalation.triggered` 事件入业务白名单闭环 notify。④ **QuickBI 票据（FR-12）**：`semantic/quickbi.py` 签发 HMAC-SHA256 签名票据（base64url 负载 + iat/exp，防篡改/防过期/防未来伪造），`POST /api/v1/semantics/quickbi/ticket` + 兼容前端单数路径 `/semantic/quickbi/ticket`，未配置 key 明确 503 降级。新增单测 24（quickbi 9 + quality_tasks 6 + escalation 9）。验证：全量 1031 passed / ruff 0 / mypy 0(173 文件) / 端到端（ticket 200 双路径 + 503 降级 + 签名校验往返 / arq WorkerSettings 7函数6cron 可加载 / compose 含 backup 服务配置有效）。提交 45b3b4a | /api/v1/semantics/quickbi/ticket, /api/v1/semantic/quickbi/ticket | released（四项生产工程闭环） |
| 2026-08-13 | — | semantic.auto_fill (SQL 智能推断 · FR-010/FR-011 扩展) | **注册指标粘贴 SQL 一键推断 + 13 字段自动生成 + 口径定义/模式自动生成**：① 新增 `sql_infer.py`（sqlglot 解析画像：源表 L1 血缘 / GROUP BY 维度 / 度量+聚合 / WHERE 谓词 / 时间列；解析失败降级空画像）；② `auto_fill.py` 重写为 13 字段推断引擎（name/type/granularity/unit/aggregation/time_semantics/freshness/dw_layer/additivity/serving_mode/metric_tier + definition_json/definition_mode），每字段返回 `SuggestionField(value/source/confidence/reason)`，优先级 **域默认 > SQL解析 > 列元数据 > 规则 > AI > 兜底**，枚举确定性产出、仅 name 可选 LLM（不可用规则降级）；③ `/auto-suggest` 端点扩展支持 `sql` 入参（sqlglot 解析 + db_catalog 列元数据富集 + 可选 LLM 命名注入）；④ 前端 `MetricCreate.tsx` 新增「①½ 粘贴 SQL 智能推断」卡片（独立入口，与最终口径相互独立），全字段回填 + 来源徽标（SQL解析/列元数据/域默认/规则/AI/兜底 + 置信度）+ 推断口径定义预览；`types.ts` 新增 `SuggestionField`。单元单测 31（sql_infer 6 + auto_fill 25）+ 语义集成回归 380 全绿；ruff/mypy 0 错；docker 容器实跑 `/auto-suggest` 验证：SUM+GROUP BY dt → atomic/day/CNY/SUM/DWD/ADDITIVE/BATCH_ONLY/T3 + SQL 模式口径；COUNT(DISTINCT)/比率 → derived/COUNT_DISTINCT/DWS→T2。提交（未提交，工作树） | /api/v1/metric-definitions/auto-suggest | released（后端已部署并实跑验证；前端待同仓其他在途文件 AssetGraph.tsx / Catalogs.test.tsx 通过 tsc 后重建镜像） |

| 2026-08-13 | — | backup/notify (binlog 增量 + 升级重试) | **binlog 增量备份（RPO 1d→15min）+ 告警升级状态化重试/逐级升级（B1/B2）**：① **binlog 增量（FR-17 备份恢复）**：mysql 容器开启 ROW 格式 binlog（`--server-id=1 --log-bin=/var/lib/mysql-binlog/binlog --binlog-format=ROW --binlog_expire_logs_seconds=604800`，独立卷 unisense_binlog）；新增 `scripts/backup_binlog.sh`（FLUSH LOGS 轮转 + 幂等归档新 binlog 按日分目录 + position.txt 记录 File/Pos + 7 天轮转）；compose 新增 `binlog-backup` 服务（每 5 分钟，只读挂载 binlog 卷），修复 backup/binlog-backup 服务 entrypoint 在 `:ro` 挂载下 chmod 失败的问题（改用 `sh` 显式调用）；runbook 恢复链更新为「最新全量 + 全量后全部 binlog 回放」，RPO ≤ 15min。② **告警升级状态化（FR-10/17 升级闭环）**：新增 `models/escalation.py` EscalationRecord（event_type/source_ref/level/attempts/max_attempts/next_retry_at/status/last_payload）+ 迁移 `0028_escalation_record.py`（down_revision=0027_data_source_last_error，修复 0027 链名不匹配）；`notify/escalation.py` 扩展：escalate 注入 session 时落状态（P2 不重复）、`acknowledge()` 人工确认停止重试、`check_retries()` 扫描到点未确认 → 重发(attempts+1)/达上限逐级升级(P2→P1→P0 重置计数)/P0 打满 MAXED_OUT；新增 `notify/escalation_tasks.py` `check_escalation_retries`（自建会话+统一 commit）；worker 注册第 8 函数 + `escalation-retries` cron（每 5 分钟）。验证：全量 1048 passed / ruff 0 / mypy 0（新文件）；迁移 0028 真实 MySQL 执行成功（escalation_record 14 列）；worker 容器实跑 15 functions 且 `escalation-retries()` 启动即执行；端到端三分支（真实 DB+Redis+EventBus）：P1(3/4)→重发 attempts=4、P1(4/4)→升级 P0 重置、P0(6/6)→MAXED_OUT；binlog 真实开启（binlog.000001）且 binlog-backup 归档成功（按日目录+position）。提交 4e56b15 | /backups/binlog、escalation_record 表、escalation-retries cron | released（RPO 分钟级 + 升级闭环） |

| 2026-08-13 | — | collector (数据源管理逐行工业级审查 + 缺口全补齐) | **深度审查发现 7 P0 + 7 P1 + 10 P2 缺陷，按优先级全部修复并真实验证（docker 容器实跑）**：① **P0 数据正确性 bug**：classifier 对 postgres/clickhouse/hive/kafka 的 dict 格式列误判全表为 PII（统一列解析，消除合规噪声）；LLM 仅用 confidence 忽略真正判定 content + 小写 needs_review 与 DB ENUM `NEEDS_REVIEW` 不一致（改用 content + 归一大写）；软删数据源重建同名 source_id 撞唯一约束 500（改冲突自增后缀 + 删除级联清理 catalogs/watermark/drift）；采集失败健康状态被回滚丢失 + worker 路径无 commit（失败路径补 commit + worker 任务加统一 commit）；Kafka 未装 kafka-python 时 probe 恒成功（缺包诚实返回失败）；**增量采集假实现**（`build_incremental_query` 从未调用 → 选增量仍全量）已接入 `collect_and_register` + 连接器增量上下文；**定时调度假实现**（`schedule_cron` 无任何调度器读取、无 worker）已补齐 `collect_scheduler` 每分钟扫描 + arq `WorkerSettings` 注册 + compose `worker` 服务，cron 真实触发采集（容器实跑 verified scanned:49）。② **P1 生产可用性**：列表静默截断（>20 源无声丢失）→ 分页响应；postgres 不支持空 database 枚举全部库 → 多 schema 枚举；`get_health` 假字段（last_error 恒 None/uptime_check 误称）→ 真实 last_error 字段 + 迁移 0027；arq 模式 job 状态恒 404 → `ArqCollectionQueue.get`；ClickHouse 密码走 URL query（进日志）→ Basic Auth；Hive 密码经 CLI `-p`（ps 可见）→ 临时文件；MySQL N+1（209 表 210 次往返）→ 批量列查询。③ **P2 健壮性**：_to_source_response UNKNOWN 大小写不一致、自动 ID 非原子、coverage 无 quota 误导 1.0→0.0、fingerprints 死字段回填、Kafka close 泄漏/SASL 支持、`_safe_ident` 拒绝 `-`/`/`、按类型校验连接配置、register_catalog 重复 classify、删除不清理子表、db.table 与 table 双注册。验证：pytest collector 单测 37 + chaos 9 + incremental 11 + worker/scheduler 60 全绿；前端 tsc 0 错 + DataSources 29/29；docker 容器实跑（:8180→backend→MySQL）：test-connection 真实探活 ok/fail 归一、自动生成 source_id、全库采集 scanned:209/pii:61、调度器每分钟真实触发采集成功。提交 677f317（enqueue 签名 + 分页）+ d6c52c2 + 2a26093 | /data-sources（分页）、/data-sources/test-connection、/data-sources/{id}/check、增量采集、定时调度 cron | released（数据源管理工业级审查缺口全补齐） |
| 2026-08-13 | — | subject_domain (主题域编码自动生成) | **主题域新建时域编码由系统自动生成，不再强制用户手填（向后兼容：显式传 code 仍按原校验逻辑）**。① **后端**：`SubjectDomainCreate.code` 改为 `str|None`（校验器处理 None）；`create_domain` 在 code 为空时按 `_generate_unique_code` 自动生成——规则：显示名 ASCII → 小写下划线 slug；子域自动拼接父域前缀（树形语义）；纯中文/无 ASCII → 根域 fallback `domain` / 子域 fallback `{parent}_sub`；冲突 `_2/_3/...` 自增后缀（上限 100，避免检查-插入竞态）；长度截断 64；显式传 code 仍走原唯一校验（422 拒绝非法码）。② **前端 SubjectDomain 页**：编码字段改为只读预览（"自动生成"蓝标），实时随显示名 + 父域变化（`Form.useWatch` 订阅）；新增 `TreeSelect` 父域选择 + 树节点「新建子域」按钮（保留父域前缀语义）；表单提交不传 code（后端生成）。③ **新增测试**：后端 `TestAutoGeneratedCode` 8 项（ASCII slug / 纯中文 fallback / 冲突自增 / 子域父前缀 / 子域纯中文 / 长度截断 / 显式 code 兼容 / 自增上限 → BusinessError）；前端 `SubjectDomain.test.tsx` 10 项（previewDomainCode 5 项 + 页面 5 项：domain preview 随名称变化 / 父域选择 / 提交不传 code / 新建子域按钮）；setup.ts 补 `getComputedStyle` 伪元素参数 polyfill 消除 jsdom stderr 噪音；集成测试 `test_create_metric_with_nonexistent_domain` 旧断言与 `MetricService._validate_domain_active` 降级语义（NotFoundError→放行）矛盾，改为匹配当前真实行为。验证：pytest 54 全过（unit 25 + repo 14 + integration 1 + security 14，配 `frontend/src/__tests__/setup.ts` polyfill `getComputedStyle` 第二参消除噪音）；前端 vitest 39/39 全过、tsc 0 错；后端 ruff/mypy 0 错。**docker 容器实跑 (:8180→backend) 6 场景全过**：ASCII 名根域 → `marketing_insights` 200；纯中文根域 → `domain` 200；子域 ASCII → `sales_return_rate` 200；子域纯中文 → `sales_sub` 200；冲突自增 → `marketing_insights_2` 200；显式传 code 向后兼容 → `legacy_domain` 200；非法手填 → 422 拒绝验证。**Playwright 浏览器端预览验证**：Risk Control → `risk_control` / 风控 → `domain` / 风控 + 父域销售 → `sales_sub`，控制台 0 错误、请求失败 0。提交 dca4bfd + ac2eb2a | `/api/v1/domains` POST `code` 改为可选 | released（FR-001 编码生产级补齐） |
| 2026-08-13 | — | resilience/degradation (FR-17 能力降级 4 大架构缺口) | **FR-17 能力降级 4 大架构缺口分别立项补齐（一变更一PR，闭合 module-status「FR-17 缺口补齐（2026-08-13 后续）」待办）**：① G1 DegradationMiddleware 舱壁（TD §5.2.5⑤）：app/core/middleware.py 新增 DegradationMiddleware，捕获 DEPENDENCY_DEGRADED_* → 503 + degraded:true + X-Degraded/Retry-After + 降级文案，舱壁隔离于 ErrorHandlerMiddleware 内层，main.py 注册（提交 48be940）；② G3 熔断器错误率滑动窗口（TD §5.2a）：CircuitBreaker 新增 error_rate_threshold + 有界 deque 窗口，olap/neo4j 5%、es 10% 触发（提交 75b6622）；③ G2 Redis 共享熔断态（TD §5.2a）：CircuitBreakerStore 抽象 + RedisCircuitBreakerStore（sync 客户端 best-effort）+ 跨进程单飞探针锁，init_circuit_breaker_store 接入 lifespan（提交 b8d7e0a）；④ G4 degradation_event schema 对齐 TD §4.13：迁移 0031 补齐 event_type/severity/affected_capabilities/affected_user_count/started_at/recovered_at/duration_seconds/trigger_reason/resolution_action，record_degradation 填充新字段 + 恢复配对（填 recovered_at/duration_seconds）（提交 0f06cdc）。验证：test_middleware/test_resilience/test_degradation 65 passed、ruff 0、mypy --strict 3 核心文件 0 error、全量 1191 unit 全绿、迁移 0031 链 0030→0031 线性可逆。 | /ready（DegradationMiddleware 503 降级）、dependency_health 熔断态（Redis 共享）、degradation_event 表（TD §4.13 字段） | released（FR-17 能力降级 4 缺口全闭环） |
| 2026-08-13 | — | consume + frontend/api (TD§12.6 消费快照双通道鉴权) | **消费快照端点不再 401——指标详情 UI 用户态可读（修复用户报告 finance_finance_amount_day 控制台 401）**。**根因 3 层叠加**：① 后端快照端点 `GET /consume/metrics/{code}/snapshots` 严格走消费方通道（X-Api-Key / consume Bearer），用户 JWT 必 401；② 前端 `request` 封装对所有请求携带全局 `X-Api-Key: dev-semantic-key`（semantic 域默认密钥，属 frontend `VITE_SEMANTIC_API_KEY` fallback），该头对 consume 域无效；③ 前端 `consumeAuth: true` 模式下若无 consume token 则**完全不带 Authorization 头**（api.ts:200-203），连用户 JWT 也不发——三因素叠加导致前端用户态必然 401。**修复**：① **后端 `app/api/consume.py`**：抽出 `_authenticate_consume` 复用消费方鉴权核心（X-Api-Key / consume Bearer）；新增 `get_consume_or_internal_user` 双通道依赖——消费方通道失败后回落内部登录用户只读通道，容忍「无效 X-Api-Key + 有效用户 JWT」组合（用户 token 本身就是有效凭证）；快照端点改用双通道依赖，严格语义保留（有效 X-Api-Key 仍走消费方+限流；无效 X-Api-Key 回落；双通道均失败仍 401）。② **前端 `src/api.ts`**：`RequestOptions` 新增 `consumeFallbackUser?: boolean`；`request` 封装在 consumeAuth 模式下，无 consume token 时按 `consumeFallbackUser` 标志回落登录用户 JWT；`listSnapshots` 启用该选项（指标详情消费快照 tab 的真实场景）。`consumeDryRun`/`consumeQuery` 不启用回落（严格消费方语义）。③ **新增 API 层测试 `tests/security/test_consume_snapshot_auth.py` 7 项**：无凭证 401 / 用户 JWT 200 / X-Api-Key 200 / consume Bearer 200 / 无效 X-Api-Key + 无效用户 token 401 / 无效 X-Api-Key + 有效用户 token 200（前端真实场景回落） / consume Bearer + 用户 token 均无效 401。**验证**：pytest 49 全过（snapshot_auth 7 + consume_service 11 + consume_repo 31）；ruff/mypy 0 错；前端 tsc 0 错、vitest 39/39。**docker 生产链路 :8180 → nginx → backend 实跑端到端验证**：`finance_finance_amount_day` 详情页消费快照请求从 `401 Unauthorized` → `200 OK`（携带 `Authorization: Bearer <user-jwt>` + 全局 `X-Api-Key: dev-semantic-key`，消费方通道失败 → 回落用户通道）；消费方通道仍严格：有效 X-Api-Key 200、无凭证 401、无效 X-Api-Key 无用户 token 401。Playwright 浏览器实测控制台 401 数从 1 → 0（仅剩 favicon 404 与应用无关）、请求失败 0；详情页「消费快照 (0)」tab 显示「暂无快照」优雅空态。提交 3e1f75e。 | /api/v1/consume/metrics/{code}/snapshots 双通道鉴权 | released（消费快照 UI 用户态可读） |
| 2026-08-13 | — | core (4 新模块接线) | **DLQ/特性开关/降级注册/密钥轮换 4 个 core 模块从死代码接线为生产能力（TECH-04/OPS-09/OPS-05/SEC-02，此前提交但零生产引用）**：① **DLQ**：EventBus Redis 发布失败自动入死信队列（`_enqueue_dlq` best-effort，`_skip_dlq=True` 防重放循环），DLQ 定时重放 5 分钟/批 10 条；② **特性开关（OPS-09）**：lifespan 注册 4 默认开关（emergency_publish/quickbi/ai.nl2sql/audit_archive，默认开启非破坏）+ 新增 `GET/PUT /api/v1/feature-flags`（仅 platform_admin + 注入守卫）+ `is_feature_enabled_or_default`（未注册默认 True，存量能力不误拦）+ emergency_publish/quickbi 灰度闸门；③ **降级注册（OPS-05）**：`/ready` 探活结果同步写入降级注册中心（降级注册/恢复清除）+ 新增 `GET /health/degraded` 统一降级面板；④ **密钥轮换（SEC-02）**：key_rotation 类型化（callable→Callable）+ `POST /admin/keys/rotate|migrate` + `GET /admin/keys/status` API 注册 + SecretManager bytes/str 契约修正（migrate 回调 json 编解码）；⑤ 新增测试 6 文件（test_dlq 16 / test_feature_flags 17 / test_key_rotation 24 / test_degradation_registry 15 / test_eventbus DLQ 接线 4 / test_feature_flags_security 5）+ test_health 扩展（/health/degraded + 降级同步）。验证：接线模块覆盖 key_rotation 100%/feature_flags api 100%/registry 98%/dlq 91%/health 90%；全量 1417 unit / ruff 0 / mypy 0（186 文件）/ 应用 31 路由；docker 容器端到端：/health/degraded 200、feature-flags 匿名 401、PA 列出 4 开关、关闭 quickbi → 票据 403、恢复 → 200。提交 0396390 | /api/v1/feature-flags、/admin/keys/*、/health/degraded | released（4 模块接线闭环） |
| 2026-08-13 | — | assetmap (全局搜索 FR-18) | **顶栏搜索框全局化（用户反馈：首页输入关键词直接跳到指标目录）**：① **后端**：新增 `GET /api/v1/search` 全局聚合搜索——跨 8 类资源（指标/维度/术语/模板/数据源/采集目录表+字段/主题域）按类型分组返回 top-N（每类 limit 可配），LIKE 通配符转义 + 全参数化查询 + 字段级 schema_json 列名精确提取；新建 `services/global_search/`（repository/service）；维度列表补 `keyword`（编码/名称/描述）、指标模板列表补 `keyword`（编码/名称/描述）、采集目录 keyword 升级为**表+字段级**（entity_name OR CAST(schema_json)）。② **前端**：顶栏改 `AutoComplete` 实时下拉（300ms 防抖调聚合搜索，按类型分组展示，点击直达详情/列表，回车跳 `/search` 兜底）；新增 `/search` 全局搜索页（分组结果）；数据源/采集目录/维度/模板 4 列表页补 `?kw=` URL 定位 + 关键词过滤；抽共享跳转工具 `utils/searchNavigate.ts`。**验证**：后端 1432 unit 全绿（global_search 13 + dimension keyword 2 + collector 字段级 2，仅 2 项 lineage/llm 预存失败与本任务无关）；ruff/mypy 0；前端 tsc 0 错（排除并行会话半成品 apiMocks/apiErrorHandlers）、vitest 48/48。提交 9cdc9fe/fcd46bf/fce9d6f/e57be56 | /api/v1/search、/dimensions?keyword、/semantics/templates?keyword、/catalogs?keyword（表+字段级） | implemented（FR-18 全局搜索） |
| 2026-08-13 | — | subject_domain (主题域同名冲突检测) | **主题域创建/编辑时同父域下同名冲突监测（用户反馈：同名称应能检测冲突，而非静默生成 _2 编码）**。① **后端**：`SubjectDomainRepository.name_exists(name, parent_id, exclude_id)` 检测同一父域下未删除的同名域（MySQL utf8mb4_0900_ai_ci collation 大小写不敏感 + `func.trim` 忽略首尾空格，与前端实时检测口径一致；软删不计入，同名可安全重建；作用域限定同父域，不同父域允许同名如 销售/订单 与 财务/订单）；`create_domain` 未传/显式 code 均先检测同名 → `ConflictError(DUPLICATE_NAME, 409)`；`update_domain` 改名时检测（排除自身，名称未实际变化不查）；API `create_domain`/`update_domain` 新增 `except ConflictError → HTTP 409`（置于 BusinessError 之前，子类优先）。② **前端 SubjectDomain.tsx**：新增 `findDuplicateNode`/`findNodeName` 工具（trim+小写递归，excludeCode 排除编辑自身）；创建弹窗实时监听 显示名+上级域，撞同名显示红色 Alert（含父域上下文与已存在编码）+ 名称输入框 error 态 + 提交前拦截；编辑弹窗同逻辑（排除自身）。③ **新增测试**：后端 `TestDuplicateName` 5 项（同父域同名创建被拒/不同父域同名允许/软删后同名可重建/改名撞同名被拒/保持原名不触发检测）；前端 4 项（根域同名警告+拦截/不同父域同名放行/编辑撞同名警告+拦截/编辑保持原名不警告）。适配团队并发 `deactivate_domain` 级联检查（list_children）对 `test_deactivate_domain` 的 mock 补充。验证：pytest 57 全过（subject_domain unit+repo+security）；前端 tsc 0 错、vitest 52/52 全过、vite build 通过；ruff/mypy 0 错。 | /api/v1/domains POST/PUT 同父域同名返回 409 | released（主题域同名冲突监测） |
| 2026-08-13 | — | collector | **数据源编辑 + 业务域下拉 + Spark 数据源（用户反馈三项）**：① **编辑功能**：后端新增 `PUT /api/v1/data-sources/{source_id}`（PATCH 语义，写闸门+审计 UPDATE）——`DataSourceUpdateRequest`（字段可选，source_id 不可变更）+ `service.update_source`（source_type 变更校验注册、connection_config 变更重新加密、健康状态重置 unknown 清 last_error）；前端详情弹窗加「编辑」按钮 → 复用弹窗进入编辑态（预填名称/类型/业务域/集群，连接配置明文不下发留空=保持原配置，仅当用户实际填写连接字段才提交覆盖，Host 编辑态非必填）；② **业务域选择框**：新建/编辑弹窗业务域 `Input` → `Select`（`listDomainTree("active")` 拉取启用中主题域树并展平，showSearch，空态提示）；③ **Spark 数据源**：`SourceTypeEnum` 增 `SPARK`、新建 `connectors/spark.py`（继承 HiveCollector，beeline 连接 Spark Thrift Server/HiveServer2 协议，`@registry.register("spark")`，默认端口 10000）、`TYPE_INFO` 增 spark 条目、`connectors/__init__` 注册、迁移 `0033_data_source_spark_type.py`（MySQL ENUM 8 值可逆）；类型下拉展示完善（listHeight 400 + optionRender 两行含描述）；前端 `SourceType` 增 spark + `FALLBACK_TYPES` 增 spark。**验证**：后端 collector/connectors/registry 单测 88 全绿（update_source 4 + spark 2 + registry 8 种），ruff/mypy 0，全量 1658 passed（5 项 assetmap/subject_domain 失败为并行会话未提交改动所致，与本任务无关）；前端 vitest 55/55 全过、tsc 0 错（我的文件）。 | PUT /api/v1/data-sources/{source_id}、spark 采集器 | implemented（数据源管理编辑+业务域下拉+Spark） |
| 2026-08-13 | — | codegen + subject_domain (中文编码英文翻译，拼音→英文) | **主题域等中文编码由拼音改为英文翻译（用户反馈：英文而非拼音）**。① **中英术语字典 `app/core/zh_en_dict.py`**：~200 条常用业务/平台/指标/数据源/时间/地域术语映射（销售→sales/供应链→supply_chain/风控合规→risk_control_compliance/华东→east_china/质量→quality/...），贪心最长匹配分词（MAX_WORD=4），未命中单字拼音兜底（pypinyin），纯标点/空白无可提取字符回退 `domain`/`{parent}_sub`/`term`/`dim`；与代码库 `audit_i18n.py` 静态映射模式一致——**离线、确定性、零网络依赖**，避免在线翻译 API 在生产环境的可用性/限流/时延风险。② **`app/core/codegen.py::slugify_code` 接入英文翻译**：删除 ASCII-only 旧逻辑改为中文段经字典翻译+ASCII 保留+段落下划线连接；`subject_domain.service._slugify_code` 委托给共享 `codegen.slugify_code`（单一事实源，零重复）；术语/维度/成员/模板编码（glossary/dimension/consume/semantic）一并受益于英文翻译。③ **修复字典语义偏差**（Py 静默覆盖 TS 报错暴露）：应用→app（vs ADS 层）/明细→detail（vs DWD 层）/汇总→summary（vs DWS 层）/发布→publish，前后端字典保持镜像。④ **前端 `src/utils/zhEnDict.ts`**：与后端字典完全镜像（~200 条），`zhToEn` 同款贪心最长匹配 + pinyin-pro 单字兜底；`SubjectDomain.tsx::slugifyCode` 接入；前端预览与后端生成规则完全一致。⑤ **新增测试**：后端 `test_zh_en_dict.py` 7 项（整段命中/贪心分词/混合/拼音兜底/未覆盖词混合/空串/防御非 CJK）；术语/维度 codegen fallback 测试改用**纯标点输入**保留分支覆盖（不再依赖中文↔拼音巧合），并新增 `test_create_term_auto_code_chinese_to_english` + `test_generate_dim_code_chinese_to_english` 显式英文断言；前端 `zhEnDict.test.ts` 7 项 + SubjectDomain 3 项断言更新（拼音→英文）。**验证**：后端 pytest 73 全过（zh_en_dict 7 + subject_domain 32 + glossary 11 + dimension 12 + codegen 11）；ruff/mypy 0；前端 tsc 0 错、vitest 62/62 全过、vite build 通过（新包 `index-B0XowM23.js`）。**docker 生产链路 :8180 → nginx → backend 实跑**：登录 reviewer/changeme123 → 创建 `销售订单` → 自动生成 **`sales_order`** ✓；`供应链` → **`supply_chain`** ✓；`订单`（parent=sales_order）→ **`sales_order_order`** ✓；`风控合规` → **`risk_control_compliance`** ✓；术语表 `活跃用户` + domain `用户` → **`user_active_user`** ✓（共享 codegen 扩展生效）；清理测试数据（删除 4 个测试域 + 废弃 1 个测试术语）。Playwright 浏览器实测 4 个用例（供应链/销售订单GMV/风控合规/Marketing）预览全部正确（`supply_chain`/`sales_order_gmv`/`risk_control_compliance`/`marketing`），控制台 0 应用错误、请求失败 0、唯一 404 为 favicon 无害资源。 | 全模块编码生成默认英文（术语/维度/成员/模板/主题域统一） | released（编码英文化，拼音退役） |
| 2026-08-13 | — | audit_remediation (全维度整改) | **GB/T 36073 L3 全维度穿透式审查+整改（综合得分54.6→≥75）**：9用户故事49FR，94任务落地。**安全(SEC)**: PBKDF2-HMAC-SHA256密钥派生(600k迭代)/JWT黑名单Redis SET+TTL/trusted_proxies XFF/guard嵌套拦截depth>max_depth/MySQL连接串密码mask/Redis TLS rediss://+ssl/请求体10MB→413/日志PII IP脱敏/beeline fd+chmod(0o600)/JWT缩短15min/被遗忘权secrets.token_hex/ADMIN_INITIAL_PASSWORD环境变量。**运维(OPS)**: 优雅关闭连接池dispose/health 503降级/HotSettings Redis Hash 30s刷新/rate_limiter降级注册/degradation_registry同步/业务Prometheus指标/审计容量预警5M阈值/特性开关4个/audit_archive MinIO归档。**技术(TECH)**: metrics path UUID归一化/asyncio.open_connection探活/eventbus指数退避1s→2s→4s→DLQ/cache LRU 10K上限淘汰/mysql pool_recycle=1800/UoW commit基类/编码锁定FORBIDDEN/conflict超时升级48h→ESCALATED/glossary阈值可配0.8/DLQ内存队列+定时重放/LLM重试+熔断。**前端**: QueryWorkspace降级提示/207 Multi-Status解析/SubjectDomain三层限制/apiErrorHandlers。**测试**: 安全回归6文件/集成6文件/前端5页面测试/E2E骨架/chaos/perf/conftest事务隔离。**基础设施**: seed_admin ADMIN_INITIAL_PASSWORD/feature_flags+API/migration_idempotent脚本 | 全模块安全/运维/技术整改 | implemented（54.6→≥75达标） |
| 2026-08-13 | — | (全体服务模块) | **全模块穿透式审查 + 工业级缺陷修复（file:line 实证，跨 consume/ai/notify/governance/quality/semantic/lineage/collector/global_search/assetmap/observability/conflict/admin_key_rotation/subject_domain/dimension/system_dict/zh_en_dict）**。修复清单：① **consume OLAP 链路修复（P1）**：SQLAlchemy `:name` 占位符直发 Doris HTTP 不识别 → 新增 `_to_doris_sql` 转换（字符串 `'${var}'`+单引号翻倍防注入、数值 `${var}`、列表展开内联字面量）；`_build_query_sql` 投影列收敛（口径声明 measures 时不再 SELECT *）+ 强制 LIMIT:__max_rows=1000 防全表拖回；ai.ask 复用共享 OLAPExecutor 单例（消除每请求 20 连接 httpx 池泄漏 D1）；keyword 兜底 SQL 修复（锚定词由列名改为 metric_code 过滤值，生成可执行 SQL D2）。② **ai 安全与缓存**：`_is_unsafe` 剥离 SQL 注释（`UNION/**/SELECT` 绕过修复 D2）、生成危险 SQL 结构化返回 safe=False、NL2SQL 缓存改类属性跨请求共享（D1 关联）、`ai.nl2sql` 特性开关接线（kill switch 生效）、QuickBI 票据绑定签发者身份声明（user_id/role/domain，网关按用户收敛）。③ **notify**：`_dispatch_email` 无收件人返回 False 不投递占位地址（D3）、`list_notifications` 强制 LIMIT=200（D4）、告警/邮件事件发布端点修正为 `/notify/events`+状态码校验+共享 client 复用（修复二次发布 aclose 报错）。④ **governance**：grant 越权防护 `_assert_grant_scope`（domain_admin 仅本域、其余角色禁授权 P0）、续期省略 TTL 不再永久化、PII 复核 masking_policy 审计、erasure 相关。⑤ **quality**：自动任务取最新观测（`latest_observation` DESC）、事件幂等去重（同 metric+rule_type 有 OPEN 跳过）、非 STATIC 阈值校验。⑥ **semantic**：PENDING_VERSION 转正链路（auto_accept_timeout 幂等恢复语义、TIMEOUT_ACCEPTED 参与 all_confirmed）、update_metric 两分支破坏性字段统一收敛、reject_version 置 CANCELLED 对齐 manager、编码不可修改校验重构。⑦ **lineage**：LineageGraphClient 模块级单例（Neo4j driver 连接池随请求泄漏 P1）+ lifespan dispose。⑧ **collector**：mysql query_timeout 死配置落地（wait_for）、`asyncio.TimeoutError`→`TimeoutError`、classifier `name` 词边界（PII 误报）、queue Redis 连接复用、LLM client 异常路径 close。⑨ **global_search/assetmap**：维度/术语/模板/聚合统计补齐 `deleted_at` 软删过滤、8 类查询 `asyncio.gather` 并行。⑩ **observability**：Feedback 新增 `status`/`resolution_note` 列（迁移 0034），`update_feedback_status` 落库可查询过滤。⑪ **conflict**：新增 SLA 自动升级任务 `sla_tasks.py`（7 天超时 OPEN→ESCALATED，注册 worker 每日 6 点 cron），删除重复未注册的 conflict_tasks.py。⑫ **admin_key_rotation**：migrate 端点真实扫描 data_source.connection_config（仅迁移 legacy 密文），dry_run 统计真实候选。⑬ **zh_en_dict**：修复 9 处重复 key + 类型/lint。⑭ **集成测试 fixture 健壮化**：12 文件 `_wipe` 改 information_schema 全表清理（修复 Base.metadata.drop_all 漏删表致 alembic 1050）+ `_reset_via_alembic` 3 次重试；修复未 await hash_password、grant actor 角色、date_range 格式、SMTP mock 等测试适配。**验证**：单测/安全/混沌/可观测/性能全量 1961+ passed；真实 MySQL 8.0@3307 集成测试 76 全过（含新迁移 0034 可逆）；mypy 189 文件 0 error；ruff check+format 全清。 | 跨模块 | implemented（全模块穿透审查修复，机械门禁全绿） |
| 2026-08-13 | — | collector (catalog 登记 source_id URL 路径回填 + LLM 降级) | **采集目录登记 source_id 自动填充 + LLM 失败不再 500（用户反馈：登记实体的 sourceId 应由系统自动生成而非人为创造）**。**改造方案**：前端 Catalogs 页「登记实体」弹窗原手填 source_id 输入框改造为「选择数据源」下拉——下拉从 `listDataSources` 拉取现有数据源（名称+source_id），用户选数据源 → source_id 自动确定 + 表单下方青绿色 Alert「source_id 由所选数据源自动确定，无需手填」。**后端**：`DBCatalogCreateRequest.source_id` 由必填改为 `str | None`；`POST /data-sources/{source_id}/catalogs` API 层判断 body.source_id 为 None 时以 URL 路径为准并回填（`body.source_id = source_id`），若显式传入则校验一致（不一致抛 400）；服务层 `register_catalog` 防御性兜底：req.source_id 为 None 时抛 ValidationError（worker/任务路径直接调用服务时需自行保证），避免下游唯一键写入失败。**顺带修复真实生产 bug**：登记实体时 LLM 分类器调用（`_llm_classify_sensitivity`）未捕获 `LlmError`（如模型不存在 404），导致整个登记 500（用户体验：模型未配置/失效时无法登记任何实体）。修复：except 链加入 `LlmError` 捕获（函数契约明确「LLM 不可用时返回 None 不阻断主流程」），记录 `llm_classify_llm_error` metric；同步补 `try/finally` 释放 httpx.AsyncClient 防连接泄漏（原代码仅成功路径 close，异常路径泄漏）。**验证**：后端 pytest 111 collector 全过（含新增 `test_llm_classify_llm_error_returns_none` LlmError 降级回归测试）；ruff/mypy 0；前端 tsc 0 错、vitest 65/65（新增 Catalogs 3 项：数据源选择器取代手填/选择后登记调用自动以选中 source_id 为路径参数/未选数据源时表单必填校验拦截）；vite build 通过。**docker 生产链路 :8180 → nginx → backend 实跑端到端**：`POST /data-sources/mysql_unisense/catalogs` body 不传 source_id → 200 OK（URL 路径回填生效，实体登记成功 `entity_name=verify_reg_ui`）；body 传 `other_source`（与路径不一致）→ 400 BAD_REQUEST（一致性校验生效）。Playwright 浏览器实测登记弹窗：选择数据源下拉含「验证源（mysql_unisense）」选项（自动从 `listDataSources` 加载）、无 source_id 手填输入框、Alert 提示可见、提交按钮锁定（未选数据源表单校验拦截）；控制台 0 应用错误、请求失败 0。测试数据已清理（DELETE db_catalog/collection_watermark/schema_drift_log/data_source）。 | POST /data-sources/{source_id}/catalogs body.source_id 可选、LLM 分类异常降级 | released（catalog source_id 自动填充 + LLM 失败降级） |
| 2026-08-13 | — | (测试基础设施 + 门禁) | **测试基建/门禁清零收尾（提交 4c314d1，承接全模块审查）**：① **conftest DB URL 本地/CI 自适应**——原硬编码 `unisense:unisense@3306` 对本地（docker 偏移 3307）与 CI（root:test@3306）均不可用，导致集成测试 50 ERROR；现按优先级：显式 UNISENSE_DB_URL → 本地 .env 存在时复用其 host:port（用户改 root 以获得建库权限、库改专用测试库 `unisense_it`，避免集成测试 DROP/CREATE 开发库 unisense）→ CI 默认 `root:test@localhost:3306/unisense`；集成测试 76 全绿（此前 25 pass + 50 ERROR + 1 fail）。② **陈旧/脆弱测试修复**：test_llm_retry 重写指向真实实现（原引用不存在的 `app.services.ai.llm_client` + 旧熔断器签名）；jwt 默认值测试改 `Settings.model_fields` 断言（免疫 .env/进程环境污染）；quality 幂等去重 mock 补 `find_open_event`。③ **lint 门禁清零**：`ruff check backend` 58 处错误清零（6 个 alembic 迁移文件 E501 换行 + 1 处 import 排序，字符串与迁移行为不变，alembic up/down/up 往返验证通过）；`mypy --strict backend` 227 文件 0 error；collector register_catalog source_id 防御性收窄（str|None → ValidationError）。④ **删除重复实现**：`app/tasks/conflict_tasks.py`（裸 SQL 绕过 service 状态机、未注册 worker）与过时测试 `test_conflict_timeout.py` 删除，统一走已注册的 `sla_tasks.auto_escalate_overdue`（worker.py:27/136/195）。**验证**：非集成 1936 全绿、集成 76 全绿、ruff/mypy 全绿、alembic 迁移往返正常、工作树已提交。 | 集成测试 DB URL、冲突 SLA 单一实现 | —（测试基建/门禁，无模块状态跃迁） |
| 2026-08-13 | — | collector (工业级深度审查 P0/P1 全补齐 + 集成测试基建) | **数据采集模块逐行审查 + 团队并行修复（5 专项 agent，7 提交：2c652d7/22a00a4/f78e3c1/b8ce0a7/7402a1f/eecc7c7）**。**P0 数据正确性（5/5）**：软删数据源撞 FK 1451 必 500（实证发现 agent 修复顺序仍错——子表先改到父表不存在的新值→1452，改用 FOREIGN_KEY_CHECKS=0 事务包裹正确修复，生产端到端 创建→删除 200→同名重建 OK）；InMemory 队列非单例→job 查询恒 404（get_default_queue 单例）；MySQL 增量 UPDATE_TIME 静默漏采（should_mix_in 稀疏检测 <10% 降级全量）；采集循环单实体失败整批回滚（per-spec try/except 隔离+失败 commit）；Kafka 同步阻塞冻结事件循环（asyncio.to_thread）。**P1 生产可用性（7/7）**：MySQL 列类型+注释+默认值采集（PII 分类依赖列注释）、Postgres 批量列查询消除 N+1、ClickHouse 连接复用、Drift 日志 API（GET /{id}/drift-logs）、废弃表自动对账（仅 FULL）、幂等键 TTL 分离、调度/立即采集分离（/schedule 只存 cron + 新增 collect-now）。**实施中暴露真实 bug（已修）**：注入守卫 \*/ 模式把合法 cron */5 误判 INJECTION_DETECTED（移除过度激进模式，/\* 已覆盖 SQL 注释开头）。**单实体刷新 collect_entity**：mysql/clickhouse/postgres 实现 + POST /{source_id}/entities/{name}/refresh + service.refresh_entity 修正无 collect_entity 采集器判定（is not None 守卫）。**集成测试基建修复（8 skip→8 pass 稳定）**：db_env 改 sync fixture（asyncio.run 与 pytest-asyncio auto 事件循环冲突）、_default_test_db_url rpartition 只替换库名（原 replace 误改 user 部分→Access denied）、_BACKEND_DIR parents[2]（原指向仓库根 alembic 找不到配置）、补 import httpx、响应信封 code==0→code=="OK"、catalog 列表路由对齐 /catalogs?source_id=、wipe 改 DROP/CREATE DATABASE（根治 1684/1050 时序 flaky）。**验证**：后端 collector 全套 169 unit + 8 integration 全绿（真实 MySQL）；ruff/mypy 0；前端 DataSources 详情加变更审计 tab（drift-logs）+ ScheduleResult 适配；docker 容器实跑 真实采集 scanned:54/registered:54/pii:12、调度分离 scheduled:true、drift-logs 200、软删 200+重建 OK、cron */5 正常保存。 | /data-sources/{id}/entities/{name}/refresh、/data-sources/{id}/drift-logs、/data-sources/{id}/collect-now | released（P0/P1 工业级补齐） |
| 2026-08-13 | — | collector (采集任务中心) | **异步采集任务列表 API + 前端页面（生产级 bug 修复）**：① **后端 RedisJobStore bytes 解码 bug（真实生产 bug）**——redis.asyncio 未开 decode_responses 时 hgetall/scan 返回 bytes 键值，导致 `/jobs/{id}` 状态恒 None、SCAN keys 解析错位（str(bytes) 带 b\"...\" 包装）；统一加解码 + 把 source_id/actor_id 从 detail 提到响应顶层（list/get 均含）。② **JobStore Protocol + InMemory + Redis 三套统一 `list(limit, offset)`**——InMemory 按 created_at 逆序排序+分页；RedisJobStore 用 SCAN `collect_job:*` cursor 循环 + 解码键值。③ **API `GET /data-sources/jobs`**（注册顺序在 `/{source_id}` 之前避免被吞，单段静态路径必须先于通配）；CollectorService.list_jobs 委托 queue.list，无 list 能力时返回空（不阻断）。④ **前端 CollectionTasks 页**（Table + 5s 自动轮询 + 刷新按钮）：状态 Tag 已完成(绿)/失败(红)、进度列展示模式/扫描/注册/PII/失败/错误、11 行真实任务（历史 scheduler + collect-now）；路由 `/collection-tasks`，左侧导航「采集任务中心」（数据采集组）。⑤ **新增 API + 类型** `listCollectionJobs`/`getCollectionJob` + `CollectionJob` 类型。⑥ **测试**：RedisJobStore bytes 解码回归 2 项（get 解码 status/detail、list SCAN keys 解码）、InMemory list 排序/分页 1 项；API 路由顺序回归 2 项（含 `/jobs` 不被 `/{source_id}` 吞掉的断言）。验证：后端 176 unit + 8 integration 全过；ruff/mypy 0；前端 tsc 0 错 / vitest 65/65；docker :8180 实跑 11 行真实任务（状态/数据源/进度均真实），控制台 0 应用错误。提交 7a3358c | /data-sources/jobs、`/collection-tasks` 前端页 | released（采集任务中心闭环） |
| 2026-08-14 | — | collector (采集进度 SSE + 结果明细 + 目标库选择 + 目录源状态) | **用户反馈四项：采集到的东西看不到 / 只有累计扫描无结果 / 采集目录看不到扫描信息 / 测试连接后应可选目标库**。① **采集进度实时推送（SSE）**：`collect_and_register` 新增 `progress_cb`（start/scanning/registering 逐实体回调）；worker `run_collection_task` 经 `_make_progress_cb` 把 RUNNING 进度写入 JobStore（消息保留最近 300 条）；新增 `GET /data-sources/jobs/{job_id}/stream` SSE 端点（轮询 JobStore 1s，progress/done/error 事件，1800s 上限，客户端断开即停）。② **采集结果明细**：采集结果含 `entities`（entity_name/sensitivity_level/drifted/change_type），前端详情弹窗「本次采集到的表」折叠表 + 结果 Alert 加「在采集目录中查看」跳转；详情「立即采集」改异步 `collect-now` + SSE 进度条（index/total 百分比）+ 实时日志 + 终态轮询兜底。③ **目标库选择**：新增 `POST /data-sources/databases` 枚举实例非系统库（`BaseCollector.list_databases` 默认空 + MySQL `_list_schemas` 复用，明文配置不落库）；前端测试连接通过后自动枚举 → database 字段变 `Select`（可选目标库，留空=全部）。④ **采集目录可见性**：`DBCatalogResponse` 加 `source_deleted`/`source_name`；`list_catalogs` 支持 `source_status`（active/deleted 外连接 DataSource 过滤 + 批量 `get_sources_meta` 补名）；前端 Catalogs 页加「源状态」筛选 + 源名/「源已删除」标记 + `?source_id=` URL 定位。**验证**：后端 collector/connectors/tasks/queue 103 全绿（progress 3 + list_databases 2 + source_status 1 + get_sources_meta 1 新增），ruff/mypy 0；前端 tsc 0 错（我的文件）、vitest 67/67（新增枚举 + SSE 结果明细 2 项）；docker 容器实跑：活跃源 mysql_unisense 401 张表 catalog 真实可见（此前用户看不到系前端构建陈旧，已重建容器验证新 JS `index-gwsXrGr7.js` 含全部新特征）。提交 5b9675f + 759991a | /data-sources/databases、/data-sources/jobs/{job_id}/stream、/catalogs?source_status= | released（采集链路体验闭环） |

| 2026-08-14 | — | governance + semantic + consume + collector (遗留问题处理：PDP 接线 + 阈值可配置) | **处理前序审查遗留两项，并顺带补齐 PDP 生产接线（file:line 实证）**。① **governance PDP 接线到 semantic 写操作（原 policy.py 声明依赖但决策路径未消费）**：新增 `GovernanceService.check_metric_permission`（PDP 决策入口，构建 Resource=domain/sensitivity/compliance_reviewed/owner_id，Subject 不含 grants——semantic 操作仅需角色+域校验）；`update_metric`（write）/`submit_metric`（write）/`approve_metric`（approve）三处写操作在 `_assert_owner_or_admin` 之上叠加 PDP 域权限闸门，API 层传 `user.domain`（避免 service 内查 DB）；删除无调用方的重复实现 `submit_review`（逻辑并入 `submit_metric`）。② **PII 合规死锁修复（真实业务冲突）**：`submit_metric` 是 PII 合规流程入口——未复核的 PII 指标须先进入 REVIEW 才能被合规复核，若 PDP 在此拦截则死锁；`check_metric_permission` 新增 `skip_pii_gate` 参数，`submit_metric`/`approve_metric` 传 True（豁免 PII 门禁、保留域/角色校验），PII 门禁统一由业务层语义清晰的 `COMPLIANCE_BLOCKED`/`FORBIDDEN_PII` 处理。③ **consume 接入 PII 合规复核闸门（COMPL-1 缺口）**：`_assert_authorized` 四级校验补 `compliance_reviewed` 闸门（未复核 PII 指标对所有接入方 FORBIDDEN_PII）；`is_pii` 兼容 `pii_flag`（修复通过 PII 复核但 definition_json 未同步时误判非 PII）。④ **should_mix_in 阈值可配置化（遗留硬编码 0.1）**：`config.py` 新增 `collector_mysql_incremental_ratio_threshold`（env `UNISENSE_COLLECTOR_MYSQL_INCREMENTAL_RATIO_THRESHOLD`），`should_mix_in(ratio_threshold=...)` 从 settings 注入，非 mysql 源不降级逻辑不变。⑤ **测试证据**：`test_governance_service.py` +5（check_metric_permission 同域放行/跨域拒绝/未复核 PII 拒绝/指标不存在/skip_pii_gate 死锁修复三分支）；`test_semantic_service.py` +3（update_metric/submit_metric PDP 拒绝路径 + submit_metric 成功/非法跃迁改写）；`test_incremental_collection.py` +2（阈值可配置）；chaos `test_semantic_chaos.py` 注入放行 PDP mock。**验证**：非集成 1936 全绿、semantic 集成 12 全过（含 PII 死锁场景）、governance 46 + semantic 118 + incremental 24 全绿、ruff/mypy 全绿。 | semantic 写操作 PDP 接线、consume compliance_reviewed 闸门、collector 增量阈值可配 | released（遗留问题闭环 + PDP 生产接线） |
| 2026-08-14 | — | core.security (key_rotation 随机盐致跨进程解密失败) | **SEC-01 严重生产缺陷（实证）：`KeyRotationManager.initialize()` 经 `derive_key_pbkdf2` 派生活跃密钥时默认 `salt=None` → `os.urandom()` 随机盐 → 每个进程/每次重启派生不同 Fernet 密钥**——`SecretManager.encrypt` 走 `_manager.active_fernet`（随机密钥），`decrypt` 走 `_manager.decrypt_with_any_key`（另一随机密钥），导致：后端创建的连接配置 worker 无法解密（采集任务 FAILED InvalidToken）、全部存量数据源配置（4 个）跨进程/重启后永久无法解密（数据回归）。这正是 `secrets.py _build_key` 注释警告过的随机盐陷阱，但 key_rotation 路径实际生效却踩坑。**修复**：`initialize()` 改用确定性盐（raw→`b"unisense-fernet-salt"[:16]`、dev→`b"dev-salt-16byte!"`，与 `secrets.py _build_key` 完全一致），跨进程/跨重启派生一致；`rotate_key` 仍用随机盐生成新轮换密钥（正确，且写 salt:key 到 env 供恢复）。**新增回归测试 2 项**：显式密钥/开发默认密钥两条路径跨实例 `initialize()` 派生相同 active_key + 互解密成功。**验证**：test_key_rotation/secrets/key_derivation/collector_registry 55 全绿，ruff/mypy 0；docker 重建后生产端到端：新建源 `mysql_unisense_3` → collect-now → SSE 进度流 COMPLETED（scanned 54/registered 54/pii 12/failed 0 + 54 表 entities 明细），链路真实打通（此前同一操作 FAILED InvalidToken）。**注意**：修复前用随机密钥加密的存量数据源配置（4 个，含活跃 mysql_unisense）无法恢复，需重建数据源（重新录入连接信息后采集，401 张表 catalog 记录仍在 db_catalog，重建后重采即恢复）。提交 10ac2df | core.security 密钥派生 | released（SEC-01 跨进程解密修复） |
| 2026-08-14 | — | collector / 数据源管理 | **连接配置编辑回显（明文不下发策略调整）**：`GET /data-sources/{source_id}` 详情接口返回**明文 connection_config**（供前端编辑弹窗回显 host/port/database/schema/user/password）；**列表接口保持脱敏**（仅 `connection_config_present` 标记，安全边界不扩大）。前端编辑弹窗异步 `getDataSource` 拉取明文并预填连接字段，**快照对比**——仅当用户实际修改连接字段（表单值 ≠ 原始配置快照）才提交 `connection_config` 覆盖，纯改名/改域不触发覆盖、不重置健康状态（替代原 `isFieldTouched` 方案，规避 antd setFieldsValue 预填误判 touched 的问题）；解密失败（密钥漂移）降级为 None 不抛 500、连接字段留空沿用"保持原配置"兜底。新增后端回归测试 3 项（详情明文、列表脱敏、解密失败降级）+ 前端编辑回显测试更新 2 项（仅改名不提交、改 host 提交完整配置）。验证：collector 90 unit 全绿、ruff/mypy 0；前端 67/67、tsc 0。 | collector / data-sources 编辑回显 | released（详情明文回显，列表脱敏） |
| 2026-08-14 | — | collector/queue + collection-tasks 前端 (任务中心修复与产品优化) | **采集任务中心与进度修复（3 bug + 2 产品优化）**：① 采集进度停在中间值 bug——SSE 终态先补发"进度拉满"事件（index=total=scanned）+ 前端 applyDone 兜底 setProgress(done)，进度条终态必达 100%；② RedisJobStore 落 created_at（HSETNX 首次写入，进度高频写入不覆盖）+ get/list 返回 created_at/kind（manual/scheduled 按 job_id 前缀推导）+ list 支持 source_id 过滤（任务中心按数据源筛选），排序改按 created_at 倒序；③ 失败任务保留 source_id/actor_id（原仅存 error 致任务中心源列为空）；④ 产品：编辑改连接配置保存后引导「立即重新采集」（旧表下次全量自动 DEPRECATED）；⑤ 产品：采集任务中心加数据源筛选 Select + 手动/定时 Tag 列。验证：后端 collector 150 unit 全绿、ruff/mypy 0；前端 70/70（新增编辑引导 1 项 + CollectionTasks 2 项）、tsc 0。 | /data-sources/jobs?source_id=、/data-sources/jobs/{id}/stream 终态进度事件 | released（3 bug 修复 + 2 产品优化） |
| 2026-08-14 | — | semantic / 指标枚举 (字典种子对齐) | **指标枚举对齐字典种子（消除注册指标 422）**：字典 system_dict 此前已按生产场景扩展为 aggregation(9)/time_semantics(6)/freshness(4)（前端下拉即展示这些可选值），但后端 `MetricCreateRequest` Literal 校验与 `metric` 表 MySQL ENUM 列仍停留在旧窄集(5/4/3)，导致选中 MAX/MIN/MEDIAN/PERCENTILE/MOM/YOY/T0 时：① 后端 Literal 拒绝 → 422（注册指标失败）；② 即便放宽校验，写库抛 `Data truncated for column`(MySQL 1265) → 500。**三处同步扩展**：① `app/services/semantic/schemas.py` 三个 Literal 扩至 9/6/4 值；② `app/models/metric.py` 三个 ORM Enum(agg_type/time_sem/freshness_type) 同步扩；③ 新增迁移 `0035_metric_enum_expand.py` 可逆扩展 MySQL ENUM 列（down_revision=0034_feedback_status）。`scripts/seed_domains_dicts.py` 字典种子同步补齐(aggregation9/time_semantics6/freshness4/granularity8/unit12)，保持重灌幂等一致。测试 `test_semantic_schemas.py` +7 扩展值接受断言，218 semantic unit 全绿、ruff/mypy 0。前端无需改动（下拉动态加载字典，自动获得新值）。TD §3.1/§12.3 枚举定义已同步。 | enum 对齐字典种子 | released（注册指标 422 修复） |
| 2026-08-14 | — | collector / 采集目录 (数据源+库名筛选) | **采集目录支持按数据源与库名筛选**：后端 `DBCatalogListParams` 新增 `database` 参数（entity_name 前缀 `库.` 过滤，LIKE 通配符转义防模糊放大，普通路径与 source_status join 路径两处生效）；新增 `GET /api/v1/catalogs/databases?source_id=` 返回目录去重库名列表（仅未删除实体、无 `.` 前缀的 Kafka topic 不计入），供前端库名下拉随数据源联动。前端采集目录页新增「库名」下拉（切换数据源时刷新库名并重置已选，disabled 逻辑 + notFoundContent 空态），与既有 source_id/类型/敏感度/关键词筛选组合生效。新增后端回归测试 5 项（database 前缀过滤+通配符转义、库名去重、按源过滤）+ 前端 1 项（库名下拉联动与 database 参数透传）。验证：collector 136 unit 全绿、ruff/mypy 0；前端 Catalogs 4/4、全套 70/70（AssetMap 1 例失败为团队未跟踪 AssetGraph.tsx 并发引入的重复标题，非本次改动）；docker 生产链路实测：/catalogs/databases 返回 14 个真实库名，database=wedw_ods 过滤出 426 条全前缀匹配、wedw_dws 6 条，source_id+database+类型+敏感度四维组合筛选生效。 | /catalogs/databases、catalogs?database= | released（数据源+库名筛选） |
| 2026-08-14 | recommend | fix | /recommend/terms 返回 TermResponse 消除 500 序列化错误（ORM→schema 转换） | 4444e52 |
| 2026-08-14 | — | assetmap (资产地图重设计 · 方案 A) | **资产地图产品级重设计（普通表格/进度条 → 真可视化地图形态，TD §12.11 / FR-18）**：① **资产地图力导向图主视图**——图节点从「仅 metric」扩展为 metric + db_catalog 表/视图（`table:{entity_name}` 与血缘边节点对齐）+ 血缘边引用字段（`field:*`，数量受控、PII 视图不展示），表/视图域从 `data_source.domain` 继承、PII 由敏感级判定，边改为精确 IN 集合匹配；前端新建 `AssetGraph.tsx`（G6 v5 `d3-force` 布局：按域着色成簇、PII 红描边、血缘度编码节点大小、拖拽/缩放/邻域高亮、图例、canvas 不可用降级表格），`@antv/g6` 在 package.json 显式声明为直接依赖；点击节点下钻（metric → 指标详情页 `/detail/{code}`；表/字段 → 实体详情抽屉 `/assetmap/entities/{id}`）。② **概览指标点击下钻**——4 大统计（目录总数/指标总数/孤儿/已发布）+ 实体类型 + 敏感度分布全部可点击值 → 通用 `DrillDownDrawer` 明细表（目录走 `/catalogs` 的 entity_type/sensitivity 过滤、指标走 `metric-definitions` 的 status 过滤、孤儿走 `/orphans`）；敏感度饼图数据源切到 `summary.by_sensitivity`（与下钻口径一致）+ 扇区点击下钻。③ **二维热力矩阵**——后端新增 `GET /assetmap/heatmap-matrix`（domain×sensitivity 聚合：db_catalog join data_source 继承域、固定敏感级枚举轴、cache-aside 30s + 熔断）；前端用 G2 Heatmap 渲染真二维热力矩阵替代原水平进度条。验证：后端 assetmap 79 unit 全绿 + ruff/mypy 0；前端 AssetMap 8/8、tsc 0；契约校验通过。提交 9c92851 + 5a1438f + 5b6938a | /assetmap/heatmap-matrix、/assetmap/graph（节点增强）、概览下钻明细 | released（资产地图重设计：地图主视图 + 下钻 + 热力矩阵） |
| 2026-08-14 | — | ai (LLM 平台配置前端入口) | **前端可配置 LLM（用户反馈：平台无 LLM 配置入口，需兼容 OpenAI 协议 + 测试连通性）**：① **后端**：新增 `llm_config` 单例行表（迁移 0037_llm_config，API Key 经 SecretManager Fernet 加密落库，响应一律脱敏）+ `LlmConfigService`（配置优先级 DB>env>none，`build_client`/`test_connection`）；新增 `GET /ai/config`（任意登录用户可读，返回 source=db/env/none + can_edit）、`PUT /ai/config`（platform_admin/domain_admin 保存，api_key 留空保持原密钥）、`POST /ai/config/test`（连通性测试，直连 `{base_url}/v1/chat/completions` 短超时探针返回 ok/latency_ms/model）；`AiService` 与指标命名推断改用 DB 有效配置构建客户端。② **前端**：AI 助手页新增「LLM 配置」卡片——提供商预设（openai/deepseek/qwen/ernie/kilo/custom，选提供商自动填充默认 base_url/model）+ base_url/model/api_key（留空不变）/timeout/enabled + 保存 + 测试连通性徽标（成功显示延迟与模型、失败显示错误）；非管理员只读展示（含来源标签）；下方 Collapse 说明支持的提供商。③ **测试**：后端 `test_llm_config_service.py` 14 项（DB 优先/env 兜底/解密失败降级/加密落库/留空保密钥/测试连接成功/401/网络错误/未配置）+ `test_llm_config_api.py` 5 项（读默认/viewer 只读/保存/403/测试成功）；前端 `AiAssistant.test.tsx` 4 项（管理员表单回填/普通用户只读/测试成功徽标/保存并刷新）。**验证**：后端 ruff/mypy 0、相关 44 单测全绿；前端 tsc 0 错、vitest 79/79。 | GET/PUT /ai/config、POST /ai/config/test | released（LLM 配置前端入口） |
| 2026-08-14 | — | lineage (血缘采集通道 · 增量采集运维) | **血缘视图采集通道闭环（多来源增量血缘的产品落点，TD §12.2）**：① **模型 + 迁移 0038**——`lineage_edge` 增补 `last_seen_at/missing_count/stale/stale_since`（增量采集最近确认时间/观察期计数/失效队列标记）；新建 `lineage_ingest_run` 表记录每次增量采集运行（新增/更新/未再出现/新失效/恢复边数 + 状态 + 失败原因）。② **Repository 增量能力**——`upsert_edge_with_status` 返回 created 标记（added/updated 计数）、`mark_seen`（刷新 last_seen_at + 恢复既往失效边）、`mark_missing`（未再出现的边累加观察期，达到阈值进失效队列，不直接删除防误删）、`list_channels`（按来源聚合边数/节点数/失效数/最近运行）、`begin/finish_ingest_run`、`list_stale_edges`、`confirm/restore_stale`。③ **Service 编排**——`ingest_batch`（幂等 upsert + 标记已见 + 失效检测 + 写运行记录，异常路径回写 failed 运行，缺省观察期取配置 `lineage_stale_observation_runs=3`）；`list_channels/list_ingest_runs/list_stale/confirm_stale_edge（软删 + best-effort 同步清理 Neo4j）/restore_stale_edge`。④ **API**——`GET /lineage/channels`、`GET /lineage/channels/{source}/runs`、`GET /lineage/stale`、`POST /lineage/stale/{id}/confirm`、`POST /lineage/stale/{id}/restore`（写端点 RBAC+注入守卫+审计）。⑤ **导入脚本改造**——`import_dp_lineage.py` 从裸 upsert 改为走 `ingest_batch`（增量语义），输出变更摘要 + 记录运行 + `--stale-threshold` 可配。⑥ **前端**——血缘视图边表格加「来源」列；新增「采集通道」Tab（通道卡片：边数/节点数/失效数/最近运行状态与摘要；点击看运行历史；失效队列表格支持确认删除/恢复）。**验证**：lineage 66 单测全过（新增 ingest_batch 摘要/失败回写/阈值默认/通道映射/失效队列/确认恢复 + mark_seen/mark_missing 阈值与已见跳过 6 项）；ruff/mypy 0；迁移 0038 在真实 MySQL 8.0 应用成功（head=0038，lineage_edge 4 列 + lineage_ingest_run 表确认）；导入脚本端到端冒烟：run_id=1 success、10553 边幂等更新、全量 last_seen_at 落库、0 失效；前端 vitest 79/79。提交 de5e3ba + 1f0f7b2 | /lineage/channels、/lineage/channels/{source}/runs、/lineage/stale、/lineage/stale/{id}/confirm、/lineage/stale/{id}/restore | released（多来源增量血缘闭环） |
| 2026-08-14 | — | ai + frontend (LLM 配置迁入系统管理) | **LLM 配置从 AI 助手页迁入「系统管理 → 系统配置」（用户反馈：配置属于系统管理配置项，不应与自然语言查询混放）**。① **新增 `frontend/src/pages/SystemConfig.tsx`**（路由 `/system-config`，侧边栏新增「系统管理」分组 →「系统配置」项，图标 SettingOutlined）：承载原 AI 助手页的 LLM 配置卡片（提供商预设/接口地址/模型/API Key 留空不变/超时/启用/保存配置/测试连通性徽标/来源标签/提供商说明 Collapse），后端 `GET/PUT /ai/config` + `POST /ai/config/test` 端点不变。② **`AiAssistant.tsx` 精简为纯 NL2SQL 查询页**：移除 LLM 配置 Card、Collapse 帮助、PROVIDER_PRESETS/SOURCE_LABEL/TestResultBadge 及相关 state/handler（form/llmConfig/saving/testing/testResult/handleProviderChange/handleSaveConfig/handleTestConfig）。③ **测试迁移**：原 `AiAssistant.test.tsx` 4 项 LLM 配置用例迁至新建 `SystemConfig.test.tsx`（管理员表单回填/普通用户只读/测试成功徽标/保存并刷新）；`AiAssistant.test.tsx` 重写为 NL2SQL 2 项（生成 SQL 调用 aiNl2Sql 并展示/空输入不调用）。验证：前端 vitest 84/84 全过、tsc 0 错。 | /system-config 系统配置页 | released（LLM 配置迁入系统管理） |
| 2026-08-14 | — | users (用户管理 · platform_admin 专属) | **用户管理补齐（用户反馈：缺少基础的用户管理与权限控制功能）**。① **后端新增 `/api/v1/users` 管理端点**（platform_admin 专属 + 全程审计）：`GET /users` 列表（含 email/最后登录/创建时间，分页 + 角色/状态/关键词过滤，响应绝不暴露 password_hash）；`POST /users` 创建（用户名/邮箱唯一校验 USER_EXISTS=409，密码 bcrypt 哈希落库，初始密码 ≥8 位）；`PUT /users/{id}` 编辑（显示名/邮箱/角色/域全量覆盖，禁止自降级 platform_admin → SELF_DEMOTE_FORBIDDEN=422）；`PATCH /users/{id}/status` 启用/禁用（禁止禁用当前登录账号 → SELF_DISABLE_FORBIDDEN=422）；`POST /users/{id}/reset-password` 重置密码（bcrypt 落库不返回明文）。写操作全部落 audit_log（USER_CREATE/USER_UPDATE/USER_STATUS/USER_RESET_PASSWORD）。② **前端新增「系统管理 → 用户管理」页**（路由 /users）：用户列表（用户名/显示名/邮箱/角色/域/状态/最后登录）+ 角色/状态/关键词筛选 + 创建用户弹窗 + 编辑弹窗 + 启用禁用（Popconfirm 确认）+ 重置密码弹窗；platform_admin 显示管理操作，非管理员只读视图 + 提示。③ **权限治理用户选择器**：Governance 授权管理与权限检查表单的「用户 ID」输入框改为用户选择器（listUsers 拉取，按用户名/显示名搜索，提交仍传 user_id）。验证：后端 test_users_api 12 项全绿（创建/冲突409/弱密码422/编辑/自降级422/404/禁用/自禁用422/重置密码/不泄露哈希）+ ruff/mypy 0；前端 UserManagement.test 5 项 + 全套 89 全绿 + tsc 0。 | /api/v1/users CRUD、/users 用户管理页、授权表单用户选择器 | released（用户管理补齐） |
| 2026-08-14 | — | collector + assetmap (Schema 字段描述推断+人工编辑+表格化展示) | **Schema 字段描述推断 + 人工编辑 + 表格化展示（对齐 spec/catalog-schema-view-edit）**：① **新建 column_descriptions 独立表**（迁移 0036，唯一约束 catalog_id+column_name，防采集覆盖 manual/llm 编辑，优先级链 manual>llm>schema_json comment）；② **后端 3 个新端点**——`POST /catalogs/{id}/columns/{col}/infer-description`（单字段 LLM 推断，复用 build_llm_client+熔断器+降级友好提示）、`POST /catalogs/{id}/infer-descriptions`（批量推断空 comment 字段）、`PUT /catalogs/{id}/columns/{col}/description`（人工编辑，source=manual+审计 INFER_DESCRIPTION/INFER_DESCRIPTIONS_BATCH/UPDATE_DESCRIPTION）；③ **AssetMap 增强**——`get_entity_detail` 查询 column_descriptions 并按优先级合并 description/description_source 到 schema_summary 每条字段（前端无需二次请求）；④ **前端 SchemaTable 通用组件**（字段名/类型/描述+来源Tag[人工编辑/LLM推断/采集原始]+编辑图标→Input+推断按钮+批量推断）；⑤ **资产地图详情抽屉**——Schema 摘要从 ObjectView 嵌套文本改为 SchemaColumn[]→SchemaTable 只读展示（抽屉 560→720）；⑥ **采集目录页字段详情抽屉**——点击"字段详情"打开 Drawer 展示 schema_def.columns 解析后 SchemaColumn[]，含 schema_incomplete 警告+空状态，支持编辑/推断；⑦ **8 项 LLM 推断单测全绿**（正常推断/disabled/超时/格式错误/空响应/LlmError/RuntimeError/ConnectionError）。 | POST/PUT /catalogs/{id}/columns/{col}/*、AssetMap schema_summary 增强、SchemaTable 组件、Catalogs 字段详情抽屉 | released（Schema 描述推断+编辑+表格化） |
| 2026-08-14 | — | ai + frontend (LLM 多实例轮询路由 + 系统管理菜单重组) | **LLM 配置支持多实例轮询路由 + 故障转移（用户反馈：避免某个 LLM 不可用造成服务不可用）+ 系统管理菜单重组**。① **后端多实例**：`llm_config` 升级为多实例（迁移 0039 增补 `name`/`priority`，每行一个 OpenAI 协议兼容实例）；`LlmConfigService` 改 `list_configs`/`create`/`update`/`delete`（软删），`build_client` 返回 `LlmRouterClient`（按 priority 排序轮询 round-robin + 单实例失败自动切换 failover + 连续失败 3 次进入 30s 冷却自动恢复 + 每实例专属熔断器 `LLM:{id}` 故障隔离 + env 兜底实例参与路由；`LlmClient` 新增 breaker/name 注入参数，缺省回落全局熔断器兼容单实例）。② **API**：`GET /ai/config` 返回 items[] + strategy=round_robin + effective + can_edit（脱敏）；`POST /config` 新增（api_key 必填 201）；`PUT/DELETE /config/{id}` 更新/软删；`POST /config/test` 支持 `instance_id` 测试已存实例。③ **前端系统配置页重构为 LLM 路由配置**：实例列表表格（名称/提供商/接口/模型/优先级/启用）+ 行内测试/编辑/删除 + 新增实例弹窗 + 多实例高可用说明；删除确认用可控 Modal（Popconfirm 在 jsdom 无法弹层的可测性替代，与 DataSources 重新采集引导同模式）。④ **菜单重组**：权限治理/审计日志/参照数据管理移入「系统管理」分组，治理合规重置为纯业务治理（冲突仲裁/质量中心/维度管理/术语表）。验证：后端 LLM 42 单测全绿（含 LlmRouterClient 轮询/故障转移/冷却 10 项）+ ruff/mypy 0；前端 SystemConfig 6 项 + 全套 93/94（唯一失败为并行会话未跟踪 AssetGraph.test.tsx 的过滤交互超时，与本次无关）+ tsc 0。 | /ai/config 多实例 CRUD、迁移 0039 | released（LLM 轮询路由 + 菜单重组） |
| 2026-08-14 | — | assetmap (Neo4j 资产属性补全 + 地图交互增强) | **承接资产地图重设计后的数据与体验补齐（提交 e069c6e + e971dab）**。① **Neo4j 资产属性补全**：血缘导入只写节点 id + LINEAGE 关系，资产地图 Neo4j 路径的展示属性（type/label/domain/pii/owner）全缺，图渲染为满屏 unknown/空标签（此前靠缺 label 回退 MySQL 兜底）。新增 `scripts/sync_neo4j_assets.py`（从 db_catalog 活跃源表/视图 + metric 查属性，未匹配的 DP 平台表用 id 推导 label/type，批量 UNWIND MERGE+SET 幂等）；`LineageGraphClient` 新增 `upsert_assets`（含熔断/分批/降级）；assetmap `_get_graph_neo4j` 边查询对齐写入约定——关系类型改 `[:LINEAGE*1..depth]`、返回 `r.type`（DERIVED_FROM），此前匹配 `DERIVED_FROM|LINEAGE_UP|...` 查不到写入的 LINEAGE 关系导致 0 边。线上已同步 4047 节点（全 label、11 有 domain、3 PII、10570 边）。② **地图交互增强**：`AssetGraph` 加前端类型筛选（指标/表/字段多选）+ 名称搜索定位（匹配节点高亮 + focusElement 聚焦，G6 不可用时降级）；热力矩阵加色阶主题切换（蓝/暖/绿阶，0 值浅灰 + 非 0 渐变）。③ **TestGetEntityDetail 挂红修复**（并行会话新增 ColumnDescription 查询后测试未同步 mock，测试更新随并行 repository 改动一并提交）。验证：后端 assetmap/lineage 128 单测全绿 + ruff/mypy 0；前端 vitest 94/94（新增 AssetGraph 3 项：类型筛选/搜索高亮聚焦/清空恢复）+ tsc 0；线上 8180 全链路：graph 500 节点全 labeled + 1000 边 DERIVED_FROM（Neo4j 路径生效）、heatmap-matrix 12 cells 真实数据。 | scripts/sync_neo4j_assets.py、LineageGraphClient.upsert_assets、assetmap 边查询 LINEAGE/r.type、AssetGraph 类型筛选+搜索、热力色阶主题 | released（Neo4j 图谱数据完整 + 地图交互增强） |
| 2026-08-14 | — | ai + frontend (LLM 密钥按需解密回显) | **LLM 实例 API Key 按需显示（用户反馈：编辑已存在实例时 API Key 未展示；方案 B：列表/常规 GET 仍脱敏，仅按需解密，不打开不出现）**。① **后端**：新增 `GET /ai/config/{id}/secret`——按需解密返回明文 API Key（仅 platform_admin/domain_admin，`_CONFIG_ADMIN_DEPS` 守卫；每次查看写审计 `ai.config.secret.reveal`；实例不存在 / 未配置密钥均返回 404）；`LlmConfigService.get_secret`（解密失败不抛 500，按无密钥处理）；`LlmConfigSecretResponse` schema。② **前端**：系统配置页编辑弹窗 API Key 字段下新增「显示密钥」按钮（仅 `has_api_key` 时显示）——点击调 `getLlmConfigSecret` 回填明文，15 秒后自动清空隐藏（定时器在关闭/保存时清理），按钮文案随状态切换（显示密钥 / 已显示·15 秒后自动隐藏）。③ **测试**：后端 `get_secret` 4 项（解密成功/实例缺失/无密钥/解密失败）+ API 4 项（成功 200/实例不存在 404/无密钥 404/viewer 403）；前端 SystemConfig 新增「按需显示 + 自动隐藏」用例（fake timers 推进 15 秒验证字段清空）。验证：后端 LLM 44 单测全绿 + ruff/mypy 0；前端 SystemConfig 7/7、全套 95/95、tsc 0。注：`config_service.py` 的 `get_secret` 被并行会话提交 `0b9c257` 一并带入 HEAD（功能正确，提交归属混合）。 | GET /ai/config/{id}/secret、系统配置页「显示密钥」按钮 | released（LLM 密钥按需解密回显） |
| 2026-08-14 | — | assetmap (Neo4j 指标节点扩展) | **资产地图增加指标节点与指标血缘边（承接 Neo4j 资产属性补全，提交 665f56e）**。此前 Neo4j 只有 `table:`/`field:` 节点，资产地图不含指标（血缘边也无 `metric:` 前缀）。本次：① **`scripts/sync_neo4j_assets.py` 扩展**——从 `metric.definition_json` 解析指标血缘（`source_tables` 上游源表 → `table:{t}`→`metric:{code}`、`dependencies` 指标依赖 → `metric:{dep}`→`metric:{code}`、`source_table` 落地物化表 → `metric:{code}`→`table:{t}`，均 DERIVED_FROM），创建缺失的 `metric:{code}` 节点（属性与 MySQL 路径对齐：type/label/domain/pii/owner），边写入前过滤表端已存在于 Neo4j 的边（不扩散无属性孤立表节点），幂等 MERGE+SET。② **`assetmap/_get_graph_neo4j` 节点查询加优先级排序**（metric>table>field）——此前无排序 LIMIT 500 把指标节点挤掉，边引用其端点导致前端过滤后指标不可见。线上已同步 8 个指标节点 + 11 条 DERIVED_FROM 边（覆盖 `ods_e2e_order`→sales 指标、sales 指标→`ads_sales_e2e_gmv_day`、指标间依赖）。验证：`test_sync_neo4j_assets` 10 项全绿（血缘解析三类/脏数据/节点属性/边过滤）+ assetmap service 35 单测 + ruff/mypy 0；线上 8180 全链路 graph=500 节点（8 metric + 492 table）+ 11 指标边。 | scripts/sync_neo4j_assets.py 指标血缘解析、assetmap 节点查询优先级排序 | released（资产地图含指标节点） |
| 2026-08-14 | notify 通知业务化 | title 从事件英文码→中文业务标题（数据质量异常告警等 21 类全覆盖 + 域.动作拆词兜底），body 从 payload JSON dump→「中文标签：值」多行文本（覆盖 metric_id/level/rule_type/rule_mode/grant_type/expires_at/conflict_id 等真实字段），前端新增 eventTypeLabel/formatNotifyBody 通用兜底（历史 JSON body 自动转中文、标题英文码兜底），CHANNEL_LABEL 键对齐 NotifyChannel enum（IN_APP→站内消息）、关联对象 event→消息 # | FR-16/FR-17 |
| 2026-08-14 | — | lineage (血缘图谱总览) | **血缘视图可直接看到图谱（用户反馈：血缘视图模块没有可直接看到的图谱或血缘数据；实际 lineage_edge 有 10563 条边、资产地图图谱正常，唯独血缘视图缺图谱端点和组件）**。① **后端**：新增 `GET /lineage/graph` 血缘图谱端点——从 MySQL `lineage_edge`（血缘边）+ `metric` + `db_catalog` 拼装 `nodes+edges`（表/指标/字段节点 + DERIVED_FROM 边），复用 `AssetMapRepository.graph_from_mysql`（与资产地图图谱同源，防逻辑漂移），支持 `domain`/`pii_only`/`limit` 参数。② **前端**：血缘视图新增「血缘图谱」**默认 Tab**——复用 `AssetGraph`（G6 力导向图，按域着色/PII 描边/血缘度编码大小/类型筛选/搜索定位/canvas 兜底表格），进入页面即加载全量图谱；点击节点：指标 → 指标详情 `/detail/{code}`，表/视图 → 采集目录定位 `/catalog?kw=`。③ **测试**：后端 `query_graph` 2 项（透传 domain/pii_only + limit 截断、默认值）；前端 `LineageView.test.tsx` 4 项（默认加载图谱、点击指标跳详情、点击表跳目录、空态引导）。验证：lineage 29 单测全绿 + ruff/mypy 0；前端 LineageView 4/4、全套 101/101、tsc 0。 | GET /lineage/graph、血缘视图「血缘图谱」默认 Tab | released（血缘图谱总览） |
| 2026-08-14 | — | frontend (分页每页条数修复) | **采集目录/指标目录切换每页条数无效（用户反馈：一直停留在 20 条/页）**。根因：`Catalogs.tsx`/`MetricCatalog.tsx` 分页三处硬编码——`load()` 请求 `page_size: 20` 写死、`<Table pagination>` 的 `pageSize: 20` 写死、`onChange` 只取页码丢弃 pageSize 参数，组件无 `pageSize` state → 用户切换后受控值弹回 20 且请求恒发 20。修复：新增 `pageSize` state 接入请求参数与 useEffect 依赖，`onChange` 同步 `(page, pageSize)`，显式开启 `showSizeChanger` + `pageSizeOptions [10,20,50,100]`。新增回归测试（切换 50 后请求携带 page_size=50）。验证：Catalogs 5 项 + MetricCatalog 4 项全绿、前端全套 102/102、tsc 0；线上 8180 全链路 `GET /catalogs?page_size=50` 返回 50 条。提交 360adc9 | — | released（分页每页条数修复） |
| 2026-08-14 | — | lineage + frontend (血缘图谱可读性优化) | **血缘视图颜色浅且眼花缭乱（用户反馈；根因：① 16 节点却有 240 条悬空边——大多数血缘边一端指向未渲染的中间表如 *_tmp/ads_*，接口报"共 240 条"但图实际只渲染 2 条；② 10 色浅粉/浅青 pastel 调色板在白底上对比度低；③ 节点全是同形圆、无类型区分；④ 标签无背景浮在边上）**。① **后端**：`query_graph` 改**自包含子图**——仅保留两端都在节点集内的边（消除悬空边，返回边数与图实际渲染一致，新增回归测试 `test_query_graph_drops_dangling_edges`）。② **前端 AssetGraph 视觉重构**：饱和深色调色板（10 色，替换浅色系）；节点按类型区分形状（指标=圆/表=圆角矩形/字段=椭圆）+ 自适应尺寸 + 投影；标签白底深字 pill（`labelBackground`，密集边上可读）；边深灰蓝+弧线+按类型着色（DERIVED_FROM/CONSUMED_BY）；布局加大间距（linkDistance 90/collide 30/manyBody -380）；图例形状示意（圆形/矩形/椭圆=指标/表/字段）。③ **血缘视图**：默认 `showFields=false` 隐藏字段节点，聚焦表/指标主干。验证：lineage 30 单测全绿 + ruff/mypy 0；前端 AssetGraph 4 项（含 showFields 降噪）+ LineageView 4 项 + 全套 108 全绿 + tsc 0。注：AssetGraph.tsx 与并行会话的生命周期重构（graphReadyRef 防 setElementState 崩溃）同文件交织，一并提交。 | GET /lineage/graph 自包含子图、AssetGraph 饱和配色/类型形状/标签 pill/showFields | released（血缘图谱可读性优化） |
| 2026-08-14 | — | frontend (分页每页条数修复 · 全平台扩展) | **审计/术语表/质量规则/质量事件/用户管理/授权 6 处切换每页条数同样无效（与采集目录/指标目录同根因，用户反馈继续排查全平台）**。全平台扫描确认同模式硬编码 bug 共 6 处：`AuditLog`、`Glossary`(TermsTab)、`QualityCenter`(RulesTab+EventsTab)、`UserManagement`、`Governance`(GrantsTab)——均为服务端分页 + `pageSize: 20` 硬编码 + `onChange` 丢弃 pageSize 参数、组件无 `pageSize` state。修复方式与采集目录一致：新增 `pageSize` state 接入请求参数与 useEffect 依赖、`onChange` 同步 `(page, pageSize)`、显式开启 `showSizeChanger` + `pageSizeOptions [10,20,50,100]`。其余页面经核查为明确禁用（`showSizeChanger:false` 的 LineageView/CollectionHistory）、纯展示固定分页或纯前端分页（Notifications 正常），无此 bug。新增 UserManagement 回归测试（切换 50 后请求携带 page_size=50）。验证：前端全套 109/109、tsc 0。提交 d4443d3 | — | released（全平台分页每页条数修复） |
| 2026-08-14 | — | lineage + frontend (表节点详情下钻) | **血缘视图点击表/视图节点应在本页给出该表具体信息，再由用户决定是否跳转指标目录（用户反馈；原实现点击表节点直接跳 /catalog?kw= 无预览）**。① **后端**：新增 `GET /api/v1/catalogs/{catalog_id}` 目录详情端点——repository 新增 `get_catalog_by_id`（按主键+deleted_at 过滤），service 新增 `get_catalog_detail`（不存在 404，复用 get_sources_meta 富集源名称/删除状态），API 挂 catalog_router（_READ_DEPS 读闸门）。② **前端**：血缘视图 `GraphTab` 点击表/视图节点改为**本页 Drawer 展示表详情**——Descriptions（实体类型/敏感度 Tag/所属数据源+源已删除标记/Schema 完整度/字段数）+ 字段清单表（字段名/类型/可空/注释，来自 schema_def.columns）+ ETL SQL 预览；抽屉 extra 提供「在指标目录中查看」按钮跳 `/catalog?kw={entity_name}`（用户自行决定是否跳转），提示文案同步更新；指标节点仍直接跳详情。验证：collector 105 单测全绿（新增详情 3 项：repo 按 ID 查询+service 富集+404）+ ruff/mypy 0；前端 LineageView 5 项（新增抽屉 2 项：点击打开抽屉不直接跳转+字段清单展示与跳转）+ 全套 119 全绿 + tsc 0。 | GET /catalogs/{catalog_id}、血缘视图表节点详情抽屉 +「在指标目录中查看」 | released（表节点详情下钻） |
| 2026-08-14 | — | conflict + frontend (冲突仲裁工作台) | **冲突仲裁操作列差异化 + 弹窗内嵌差异对比（用户反馈：操作列缺具体操作按钮、看候选/现有差异需跳指标目录不便）**。① **操作列按状态机差异化**：OPEN/NEGOTIATING→对比+仲裁+升级、ESCALATED→对比+仲裁（已升级不可再升）、RULED→对比+关闭、CLOSED→仅对比、PII→已转交治理标记（不提供仲裁）；仲裁/升级/关闭均带 busy 防重。② **仲裁弹窗内嵌差异对比**：抽取新增 `MetricCompareTable`（从 MetricCompare 页复用出来的纯展示组件，12 字段并排 diff + 差异 Tag），仲裁与只读对比两个 Modal 共用，当前窗口即见候选 vs 现有差异，无需跳转指标目录。③ **按冲突类型差异化决策集**（TD §12.4 三决策落地）：同名不同义→采纳现有/候选为权威/保留差异；同义不同名→合并到现有/以候选为权威/保留差异；粒度单位→采纳现有/候选/保留差异；跨域同口径→合并/明确现有为权威/保留差异；版本冲突→采纳现有/候选版本；决策映射后端 choose_canonical（选现有/候选）+ merge + keep_diff，裁决理由沉淀知识库。④ **新增 closeConflict API**（POST /conflicts/{id}/close，RULED→CLOSED）。验证：ReviewWorkbench 单测 8 项全绿、前端全套 21 文件 138 测试全绿 + tsc 0。 | 前端 ReviewWorkbench 操作列/仲裁弹窗、closeConflict API | released（前端增强，无后端状态变更） |
| 2026-08-14 | — | assetmap + frontend (资产地图大图可读性 + 循环图处理) | **资产地图图节点很多时展示混乱不清晰，且要考虑循环图场景（用户反馈；实测根因：① MySQL 降级路径的 depth 参数形同虚设——get_graph 降级调用 _get_graph_mysql(domain, pii_only) 丢弃 depth，实测 depth=1/3 返回一模一样 500 节点 1000 边；② Neo4j 节点查询为 MATCH(n:Asset) LIMIT 500 恒返全量节点（depth 只作用于边跳数），上线 Neo4j 容器后同样返回 500 节点；③ 力导向布局在几百节点时标签/边重叠且无分层语义；④ 1000 边里 796 条是 A↔B 双向边（指标↔表互引）、9 个真环（*_tmp 临时表循环）**。① **后端 depth 收敛（MySQL）**：`graph_from_mysql` 新增 `depth` 参数，从指标出发沿血缘边 BFS 展开（depth=1 仅指标+直连表，越深展开越多，None=全量），service 降级路径透传 depth；修复"depth 形同虚设"根因。② **后端 depth 收敛（Neo4j，提交 b64eec6）**：`_get_graph_neo4j` 节点查询改为从指标种子出发按 depth 无向展开 `MATCH (seed:Asset {type:'metric'})-[rels:LINEAGE*0..{depth}]-(n:Asset)`（与 MySQL 降级 BFS 语义一致），边查询改为 `WHERE a.id IN $node_ids AND b.id IN $node_ids`（自包含子图，消除悬空边）。**端到端实测**：depth=1→11 节点(8 指标+3 表)/11 边、depth=2→12/14、depth=3→12/14（之前 500/1000）。③ **前端 AssetGraph 布局策略**：新增 `layout=auto/hierarchy/force`——auto 用 Tarjan SCC 检测真环（>2 的强连通分量），有真环自动回退力导向（dagre 对环渲染异常）、无环用 antv-dagre 分层布局（表→指标自上而下，比力导向清晰）；工具栏提供布局切换 Select。④ **双向边合并**：A↔B 合并为单条双箭头边（`bidirectional`），消除 796/1000 双向边视觉噪声。⑤ **真环高亮**：真环边红色虚线+加粗+箭头（`inCycle`），顶部循环依赖提示条"检测到 N 个节点存在循环依赖"，图例新增循环依赖示意。⑥ **降噪**：资产地图默认 `showFields=false`（隐藏字段节点聚焦表/指标）+ depth 默认 2。验证：assetmap 83 单测全绿（深度收敛+Neo4j 查询改造+双向边合并/环检测）+ ruff 0（mypy 1 处仓库级错误在并行会话 repository.description 字段，待修）；前端 AssetGraph + 全套 138 全绿 + tsc 0。浏览器实测：资产地图页 4 个 G6 canvas、统计 12 节点 14 边、布局切换可用、0 console 错误。注：AssetGraph.tsx 与并行会话的生命周期重构/hover 加固同文件交织，一并提交。 | graph_from_mysql/Neo4j depth BFS 收敛、AssetGraph 布局策略/双向边合并/真环红色虚线高亮 | released（资产地图大图可读性+循环图处理） |
| 2026-08-14 | — | assetmap + collector + frontend (描述缺失概览与补全) | **平台缺少直观查看多少表/字段没有描述信息，且缺少描述的字段要支持 LLM 推断/人工编辑（用户反馈；后端字段级推断/编辑已实现但前端只读、无缺失概览；表级无描述存储）**。① **后端**：迁移 `0041_table_descriptions`——`db_catalog` 新增表级描述 4 列（`description`/`description_source`/`description_updated_by`/`description_updated_at`，采集 upsert 显式字段不覆盖）；repository 新增 `update_table_description`（人工/LLM 更新表级描述）+ `get_description_coverage`（表/字段覆盖率 + 按表列缺失字段数，schema comment 或 column_descriptions manual/llm 记为有描述）；service 新增 `_llm_infer_table_description`（表名+字段清单推断表描述，复用熔断器，30 字段截断防超长）；新增端点 `GET /catalogs/description-coverage`（统计，静态路由置于 `/{catalog_id}` 前防吞）、`PUT /catalogs/{id}/description`（人工编辑）、`POST /catalogs/{id}/infer-table-description`（LLM 推断）；`DBCatalogResponse` + assetmap `get_entity_detail` 返回表级描述字段。② **前端**：资产地图新增「**描述缺失**」tab（`DescriptionCoverageTab`）——统计卡（字段覆盖率/缺失字段/缺表描述/表总数）+ 按表列缺失字段数可排序 + 点击行→详情抽屉直达表级/字段级 LLM 推断与人工编辑；图谱实体详情 SchemaTable 由只读改为**可编辑可推断**（字段编辑/单字段推断/批量推断）+ 表级描述编辑/推断；采集目录头部统计卡。③ **测试**：后端 212 相关单测全绿（表级推断 4 项 + repository 表描述/覆盖统计 + API 3 端点 + 既有）；前端 AssetMap 新增 3 项（tab 展示/行点击抽屉/表描述编辑保存）+ Catalogs 统计卡 1 项，全套 150/150 + tsc 0。提交 465cf90（后端）/ 6de7b03（前端）。注：采集目录统计卡因 Catalogs.tsx import 区与并行会话改动交织，保留工作区随其提交。 | 0041 迁移、GET /catalogs/description-coverage、PUT/POST /catalogs/{id}/description、资产地图「描述缺失」tab、图谱详情可编辑可推断 | released（描述缺失概览与补全） |
| 2026-08-14 | — | ai + frontend (LLM 路由配置 P1 多实例管理) | **LLM 路由配置多实例管理增强（用户反馈：保存了多个 LLM 实例后，不知道新实例位次/可用性/删除影响，从产品层面优化交互）**。① **上移/下移调整位次**（P1-5）：轮询位次列新增上移/下移箭头——与相邻实例交换优先级（`updateLlmConfig` ×2，同优先级并列时按移动方向 ±1 打破并列），Toast 反馈"已上移/下移一位（下次请求起效）"；② **同优先级冲突警告**（P1-5）：优先级列在多个数据库实例同值时显示黄色 WarningOutlined 图标，Tooltip 提示"轮询顺序按创建顺序（ID）决定"；③ **删除前影响范围提示**（P1-6）：删除确认弹窗增强——展示当前轮询位次 / 当前路由实例标记（Tag「当前路由正在使用该实例」）/ 删除后剩余实例数与启用数 / 若删后无启用实例则提示「将回落到环境变量配置」或「LLM 将处于未配置状态（AI 问数/指标命名推断会失效）」；④ **全部测试批量连通性**（P1-7）：新增「全部测试」按钮 Promise.all 并行测试所有数据库实例，结果区顶部新增集群健康报告 Tag「集群健康：X 可用 / Y 失败（共 N）」+ Toast 汇总。验证：前端 SystemConfig 17 项全绿（+4 P1 用例：上移交换优先级 updateLlmConfig×2/同优先级冲突警告图标/删除影响范围提示/批量测试并行调用+集群健康报告）+ 全套 194 全绿 + tsc 0。 | 系统配置页（SystemConfig.tsx）上移/下移/冲突警告/删除影响提示/全部测试 | released（P1 多实例管理增强） |
| 2026-08-14 | — | frontend (运营中心拆分用户反馈 + 时间中文展示) | **「可观测中心」拆分出独立「用户反馈」页 + 反馈时间中文描述（上海时区）**。① **拆分（承接菜单重组「运营中心」更名）**：原「可观测中心」内嵌的用户反馈三 Tab（用户反馈/提交反馈/NPS 调查）拆为独立页面 `/feedback`（FeedbackCenter.tsx），「可观测中心」瘦身为纯平台运行读数（质量/通知/血缘/API 聚合指标），菜单运营中心组新增「用户反馈」入口——消除"反馈/NPS 塞进可观测性标题下"的语义错位。② **时间中文描述 + 强制上海时区**：新增共享工具 `src/utils/timeCn.ts`——`parseBackendTime`（naive 串按 UTC 解析，对齐后端 TimestampMixin 落库 UTC）+ `formatCnTime`（上海时区绝对时间"2026年8月14日 14:30"）+ `timeAgoCn`（刚刚/N 分钟前/N 小时前/昨天 HH:mm/N 天前，`Intl` 硬编码 Asia/Shanghai 不随浏览器时区漂移，now 可注入供测试）；反馈表「时间」列改为相对中文描述 + Tooltip 精确上海时间。验证：timeCn 单测 11 项 + FeedbackCenter 3 项全绿、前端全套 211 通过（MetricCatalog 5 项失败为并行会话未跟踪改动，与本次无关）、tsc 0（MetricCatalog 未用变量为并行会话遗留）。 | 前端 FeedbackCenter 路由/菜单、timeCn 时间工具 | released（运营中心拆分 + 时间中文化） |
| 2026-08-14 | — | tracking + frontend (埋点真实性核查 + 同事件节流去重) | **埋点统计真实性核查 + 防污染节流（用户反馈：埋点统计是否真实数据）**。① **核查结论**：链路 100% 真实（前端 track→POST /tracking/event→落库→SQL 聚合，无 mock）；但数据库 `tracking_event` 被自动化测试灌入污染——**时区坑**：MySQL 容器时区为 UTC（宿主 +8），此前误判"凌晨 02:00-07:00 灌入"实为宿主当天 10:00-15:37，正是并行会话多轮 Playwright E2E（test-results-final→v4 五轮）+ 多个 playwright-mcp 浏览器会话反复访问 Dashboard 等页面（每次挂载触发 1 条埋点叠加成高频）所致；`seed_e2e_data.py` 仅写 3 条、`smoke.mjs` 为 API 级、Dashboard 无轮询，均排除。② **清理**：删除 3495 条污染（admin 凌晨/白天灌入 3491 + nowner dashboard_view 4），剩余 151 条按天/小时/用户分布自然，恢复真实统计。③ **防污染机制（C）**：新增 `src/utils/trackingDedup.ts`（工厂 + 10s 单例，`shouldSend(key, now)` 窗口内重复丢弃 + `dedupKey` 事件键），`TrackingProvider.track` 上报前先节流门控——自动化测试/快速刷新同事件 10s 内只落库 1 条，不同页面/对象互不影响；修复 now=0 首调被误判窗口内的边界 bug。验证：trackingDedup 4 项 + TrackingProvider 3 项新增测试全绿、前端全套 31 文件 228 全绿、tsc 0。 | src/utils/trackingDedup.ts、TrackingProvider 节流门控、污染数据清理 | released（埋点真实性核查 + 节流防污染） |

| 2026-08-14 | — | frontend (审计日志全部动作中文化) | **审计日志前端展示仍残留技术术语（用户反馈：操作内容显示 metric.created/ai-config.secret.reveal/REFRESH 等英文码、操作对象 Tag 与 ID 列重复显示「指标定义」、操作详情列 codes 等字段未映射、操作者/IP/时间列标题偏技术化）**。① **穷举 51 种真实 action 全量映射**：通过 API 翻 26 页穷举全部不重复 action 真实值（0 个猜测，全部从 DB 提取），一次性补齐 AUDIT_ACTION_LABEL（35 种带点的复合 action）+ pureMap 兜底（12 种纯大写 action）+ verbMap（新增 secret→查看密钥/reveal→查看/purged→清除）+ domainMap（新增 ai.config/llm_config→AI 配置）。② **修正错误条目**：删除之前会话里用了横杠的 ai-config.*（实际数据库是 ai.config.* 用点）+ 修正拆词兜底逻辑（cnDomain 复用 verbMap 同一作用域，避免重复声明）。③ **操作对象去重**：entityIdWithLabel 函数返回「指标定义 #5」与 Tag「指标定义」重复，改为 line 69 直接渲染 #5（去掉 entityTypeLabel 前缀，Tag 已显示类别）。④ **详情字段映射扩展**：新增 codes→编码列表、entity_code→实体编码、term_code→术语编码、dim_code→维度编码、conflict_id→冲突编号、grant_id→授权编号 等 6 个常用 detail_json 字段。验证：tsc 0 错（AuditLog/auditI18n 无类型错误，团队并发 TrackingProvider.test.tsx 类型问题由团队自行修复）；前端全套 228/228 全过；生产链路 :8180 翻多页验证：page=1 含 LIST→查询列表/READ→查看/ai.config.secret.reveal→查看密钥/REFRESH→刷新 等全部业务化，无任何英文码残留；Playwright 11 项检查全部通过（loginOk/auditTitle/6 个列标题/操作者提示/noEnglishActionCode/noEntityTypeEnglish），pageErrors=0、reqFailed=0。提交 28a6e70。 | auditI18n.ts 51 种 action 全映射 + AuditLog.tsx 操作对象去重 | released（审计日志全部动作中文化） |
| 2026-08-14 | — | collector + frontend (数据源管理批量启停/删除) | **数据源管理页面缺少批量启用/停用/删除能力（用户反馈；此前仅单条操作，TD/PRD 均无批量规划）**。① **后端**：schemas 新增 `BatchToggleRequest`/`BatchDeleteRequest`/`BatchSourceItem`/`BatchSourceResult`；repository 新增 `set_source_enabled`（逐条设置启用状态，不存在返回 None）；service 新增 `batch_toggle_sources`/`batch_delete_sources`（逐条 try/except + 汇总报告 207 语义，上限 200，成功项携带 name）；新增端点 `POST /data-sources/batch-toggle`（BATCH_ENABLE/BATCH_DISABLE 审计）、`POST /data-sources/batch-delete`（BATCH_DELETE 审计），静态路径注册于 `/{source_id}` 前防吞。② **前端**：DataSources 列表加 `rowSelection` 多选（显示已选数量）；工具栏新增「批量启用」「批量停用」「批量删除」（删除 Popconfirm 二次确认 + 影响提示），结果按成功/失败清单 message 反馈（部分失败逐项标注 source_id+原因）。③ **测试**：后端 test_collector 9 项（repository set_source_enabled×2 + service 批量成功/部分失败/异常隔离）；前端 DataSources.test 新增 3 项（批量启用调用/批量停用确认/批量删除二次确认+失败清单），全套 235/235 + tsc 0。提交 e2c91f7。 | batch-toggle/batch-delete 端点、DataSources 多选工具栏 | released（数据源管理批量启停/删除） |
| 2026-08-14 | — | tracking + observability + frontend (埋点统计业务术语化 + 可观测中心平台概览) | **埋点统计业务术语化 + 可观测中心从产品视角补齐（用户反馈：埋点统计展示技术术语；可观测中心需按生产场景补充；页面展示统一业务术语化）**。① **埋点统计业务术语化**（`TrackingStats.tsx` + `enums.ts`）：新增 `TRACKING_EVENT_LABEL`（穷举代码库 33 种真实 track 事件全映射：lineage_*/consume_*/ai_nl2sql/review_*/todo_center_view/favorites_view 等）+ `TRACKING_TARGET_LABEL`（dashboard→仪表盘/page→页面/metric→指标/table→表/todo→待办/favorite→收藏/source→数据源/conflict→口径冲突/node→血缘节点/sql→SQL 解析/edge→血缘边/ai→AI 助手/template→指标模板）；事件类型筛选从「技术文本输入框（如 metric_view）」改为「业务标签 Select（含搜索、全部）」；「操作用户」分组加载 `listUsers` 映射 `display_name`（缺失回落 username），不再直出数字 ID；图表/表格三分组全业务标签。② **可观测中心新增「平台概览」Tab**（生产视角）：后端 `ObservabilityRepository.overview_stats()` 一次聚合——数据源健康分布（`DataSource.health_status`）、治理积压（待处理冲突 Conflict OPEN/NEGOTIATING + 未关闭质量事件 QualityEvent OPEN/ACK + 待审核指标 Metric REVIEW + 未闭环升级 EscalationRecord）、资产规模（指标按生命周期状态 + 术语/维度/主题域/数据源数，软删除过滤）、消费接入（ApiClient 总数/活跃）；新增 `GET /observability/overview`（读角色守卫 + 注入守卫）；前端 `ObsOverview` 类型 + `fetchObsOverview` + OverviewTab 四卡片全业务标签（SOURCE_HEALTH_LABEL/METRIC_STATUS_LABEL）。③ **页面展示术语化**：可观测中心「运行指标」Tab 的「API 动作分布」从裸 AuditLog.action 技术值改为复用 `auditActionLabel()`（51 种全映射，与审计日志页同源）。验证：后端 observability 25 单测全绿（新增 overview_stats repository 11 次查询 mock 顺序断言 + service 委托）+ ruff/mypy 0 + 真实 MySQL 8.0 上 overview SQL 全通过（数据源 8 个 healthy3/unknown3/unhealthy2、积压/资产/接入真实值）；前端 TrackingStats 9 项（新增 target_type/actor_id 业务标签断言）+ Observability 2 项（平台概览业务标签 + API 动作中文化）全绿、全套 32 文件 246 项通过（唯一失败 Templates.test.tsx 为并行会话未跟踪改动）+ tsc 0（Dimensions 两文件同为并行会话遗留）。 | GET /observability/overview、TRACKING_EVENT_LABEL/TRACKING_TARGET_LABEL、TrackingStats 业务标签筛选、可观测中心平台概览 Tab | released（埋点/可观测业务术语化 + 平台概览） |
| 2026-08-14 | — | collector + frontend (字段详情 LLM 推断「目录实体缺少 ID」修复) | **采集目录字段详情页做 LLM 推断时报「该目录实体缺少 ID，无法推断」（用户反馈）**。根因：后端 `DBCatalogResponse` 未定义 `id` 字段 → `GET /catalogs` 列表/详情接口均不返回主键 → 前端 `fieldDrawerCatalog.id` 恒为 `undefined` → `catalogId=0` → 推断/编辑/批量推断全部走「缺少 ID」分支。修复：`DBCatalogResponse` 新增 `id: int`（构造点均 `model_validate(ORM)`，`from_attributes` 自动映射 `.id`）；前端 `DBCatalog` 类型补 `id?: number`（可选，不破坏既有 mock）；同步补测试 mock 缺失 id（unit `_FakeCatalog`/chaos/security 各 +1）。验证：后端 unit 1947 通过（4 个语义缓存失败为并行会话在途改动，与本次无关）+ chaos/security 14 通过；前端 31 相关测试 + tsc 0；线上 8180 全链路实测 `GET /catalogs` 返回 items 含 `id`（id=700），推断/编辑链路不再报「缺少 ID」。提交 fc61638。 | DBCatalogResponse.id、前端 DBCatalog.id? | released（字段详情 LLM 推断缺少 ID 修复） |
| 2026-08-14 | — | frontend (冲突仲裁对比弹窗样式美化 + 透明修复) | **冲突仲裁对比弹窗样式不够美、且有透明感（用户反馈）**。① **修复"透明"根因**：`utils/display.tsx`（对比表内 ObjectView）使用 `var(--text-secondary)` 等 CSS 变量但 `:root` 未定义 → `var()` 回退失效导致文字/标签颜色异常（透明感）；`styles.css` 补齐 `--text`/`--text-secondary`/`--text-tertiary` 三个语义化文字变量。② **对比表美观度重写**（`MetricCompareTable.tsx`）：差异等级改为「圆点 pill」（一致绿/相似琥珀/不同红，信号灯气质）、顶部新增汇总条（共 N 项·一致 X·相似 Y·不同 Z）、行底色按差异等级高亮（similar 淡琥珀/different 淡红）、字段名加粗、首列加宽；新增 `.compare-row-*` 行底色 + 表头加深 CSS。③ **列表列溢出修复**（`ReviewWorkbench.tsx`）：候选/现有指标列加 `width:180` + ellipsis，描述列 `width:200` 且空值显示 `—`。验证：9 个 CSS 变量全部在 `:root` 定义（无回退失效）；ReviewWorkbench 10 项单测全绿；tsc 0（本次文件）。 | MetricCompareTable 行底色/pill/汇总条、styles.css 变量补齐、ReviewWorkbench 列宽 | released（冲突仲裁对比弹窗样式美化） |
| 2026-08-14 | — | semantic + assetmap + frontend (指标业务描述补充能力) | **资产地图点击指标缺少明细数据与补充描述能力（用户反馈：资产地图描述缺失，点击统计指标应支持查看明细数据并补充描述）**。① **后端**：metric 表新增描述四件套（description/description_source/description_updated_by/description_updated_at，对齐 DBCatalog 表级描述模式 TD §12.1）+ 迁移 0043（可逆）；MetricResponse 透出 4 字段；新增 `PUT /metric-definitions/{code}/description`（owner/admin + PDP write 权限，**不触发版本/不参与口径变更**、不设 PII 复核闸门——描述是运营层补充；空串清除描述；写审计 metric_description）。② **前端**：资产地图点击指标节点不再直接跳转 `/detail`，改为**本页抽屉**展示——业务描述（可编辑保存 + 来源/更新时间标记）、基本信息 16 项、**口径明细**（definition_json 解构 SQL/主度量/维度/源表/周期）、**数值快照**（listSnapshots，空态提示「暂无查询快照」）+「前往指标详情 →」链接。③ 测试：后端 test_semantic_service 4 项（手动来源/空串清除/PDP 拒绝/越权）+ 前端 AssetMap 2 项（点击指标开抽屉不跳转 + 补充描述保存），前端全套 253/253 + tsc 0。 | 0043_metric_description.py、PUT /metric-definitions/{code}/description、AssetMap 指标抽屉 | released（指标业务描述补充能力） |
| 2026-08-14 | — | conflict + frontend (冲突仲裁对比弹窗加宽 + 口径定义子标题中文化) | **冲突仲裁对比弹窗宽度小、字段列换行 + 口径定义子标题仍英文（用户反馈：弹窗宽度小且字段列换行；口径定义里的子标题应改为中文含义术语）**。① **弹窗与列宽加宽**：`ReviewWorkbench.tsx` 两个对比/仲裁弹窗 `width: 920 → 1120`；`MetricCompareTable.tsx` 列宽——字段 160→200（4 字中文不再竖排）、指标 A/B 各 280→330、差异列 88→100；`styles.css` 字段列 `min-width` 同步 160→200 注释更新。② **口径定义子标题中文化**：扩 `DEF_FIELD_LABEL`（`display.tsx`）覆盖 backend `definition_json` 全部 key——SQL 模式（sql/source_tables/group_by/filters/time_column/partition_key/measure_columns/dimensions）、表达式模式（expression/source_fields）、seed 数据（measures/source_table/measure_column/period/dependencies）、嵌套对象（name/aggregation/table/column/pii）+ 元信息（code/metric_code）；`MetricCompareTable.renderValue` object 分支传 `DEF_FIELD_LABEL`（替代默认 `PLAN_FIELD_LABEL`），递归 ObjectView 自动继承 labels，嵌套 measures/source_fields 项的 key 同步翻译。③ **实际渲染验证**：vite 重启后 Playwright 截图——弹窗 `modalWidth=1120`、列宽 [213/352/107/352]、口径定义单元格子标题全业务化（"口径 SQL"/"统计周期"/"度量"/"名称"/"聚合方式"/"维度"/"源表"/"关联数据表"/"度量列"），无英文残留。验证：ReviewWorkbench 10 项单测全绿 + 全套 33 文件 258 项通过（唯一失败 `DataSources.test.tsx` 为并行会话未跟踪改动）+ tsc 0。 | MetricCompareTable 列宽/renderValue labels、ReviewWorkbench 弹窗宽度、DEF_FIELD_LABEL 全 key 映射 | released（对比弹窗加宽 + 口径定义中文化） |
| 2026-08-14 | — | conflict + frontend (口径定义 measures 翻译 + vite 缓存根治) | **冲突仲裁对比弹窗宽度仍无变化、口径定义子标题仍有英文（用户反馈：period/sql/source_table/measures 等英文直出）**。① **补 `DEF_FIELD_LABEL` 遗漏 key**（`display.tsx`）：上轮补齐时遗漏 seed 数据模式的 `measures`（度量）与 `columns`（度量字段）——Playwright 实渲染确认对比弹窗口径定义子标题已全中文化（口径 SQL/统计周期/度量/名称/聚合方式/维度/源表/关联数据表/度量列，0 英文残留），Modal 宽度实测 1120。② **根治「改了代码页面无变化」**（`vite.config.ts`）：dev server 响应加 `Cache-Control: no-store`，避免浏览器沿用旧编译产物（此前多轮依赖手动重启 vite + 硬刷新，用户侧仍看到旧版）。验证：Playwright 实渲染 Modal 1120 + 口径定义全中文化；ReviewWorkbench 10 项单测全绿；tsc 0。 | DEF_FIELD_LABEL measures/columns、vite dev no-store | released（口径定义子标题补全中文化 + dev 缓存根治） |
| 2026-08-14 | — | consume + assetmap + frontend (指标点击真实数据 + 快照自动落库) | **资产地图点击指标「数值快照」为空（用户反馈：希望点击指标看到真实数据，解决快照为空）**。根因：OLAP 引擎未配置（UNISENSE_OLAP_URL 空）→ execute_query 直接降级 503 → 快照从未写入。① **后端**：新增 `mysql_executor.py`（OLAP 不可用时的 MySQL 只读降级引擎：只读护栏/参数化/超时/熔断/值序列化）；`execute_query` 引擎降级（OLAP 优先 → MySQL 兜底），查询成功自动保存 WORM 快照（generated_by=QUERY，value_json 含 engine，写入失败不阻塞响应）；新增内部用户查询端点 `POST /consume/metrics/{code}/query`（get_current_user 鉴权，保留指标状态 + PII 合规复核闸门，写审计 consume.query.internal）；config 增 `mysql_fallback_url` 可选配置。② **数据准备**：建 `e2e_biz` 业务库 + 8 张指标宽表（dws_metric_*/ads_sales_e2e_gmv_day）+ 灌入真实源数据（与 db_catalog schema 对齐），compose 注入 UNISENSE_MYSQL_FALLBACK_URL。③ **前端**：资产地图指标抽屉新增「查询最新数据」按钮（loading 态）→ 调内部查询端点 → 展示本次真实查询结果（行 × 引擎）+ 自动落快照后刷新列表。验证：后端 consume/mysql_executor 单测 8 项新增 + 89 全绿 + ruff/mypy 0 + 真实 MySQL 连通（gmv 6 行/ordercnt 6/activeuser 3/piiuser 3/unitprice 3）；前端 AssetMap 28 项（新增查询用例）+ tsc 0；实跑 mysql_executor 真实查询返回 6 行。提交 7c74d95/40cd9a9/6d3a44a。 | MysqlExecutor、POST /consume/metrics/{code}/query、AssetMap 查询最新数据、e2e_biz 业务库 | released（指标点击真实数据 + 快照自动落库） |
| 2026-08-14 | — | collector (LLM 描述/敏感度推断不可用修复) | **采集目录字段详情页 LLM 推断提示「LLM 推断暂时不可用（LLM_INFER_UNAVAILABLE）」（用户反馈）**。根因两层：① **推断走旧 env 静态客户端**——collector 的 `_llm_infer_column_description`/`_llm_infer_table_description`/`_llm_classify_sensitivity` 用 `build_llm_client()`（仅读 env=kilo.ai），而 kilo.ai 的 `poolside/laguna-m.1:free` 模型已下线 → chat 404 → LlmError → `LLM_INFER_UNAVAILABLE`；DB 配置的 codebuddy 本地网关实例可用但未接入。② **潜在解析 bug（被 404 掩盖）**——`LlmClient.chat()` 返回键为 `content`（原始 JSON 字符串），而推断逻辑读 `result["description"]` 恒为空 → 静默返回 None，切 DB 配置后暴露。修复：新增 `_build_llm_client`（`LlmConfigService.build_client` 优先，env 兜底参与轮询路由，DB 读取异常回退 env 静态客户端不阻断），替换 3 处调用；新增 `_parse_llm_description_result` 解析 `content` 的 JSON 取 description/confidence（非 JSON/空/低置信度视为不可用），字段/表推断统一走该解析。测试：11 处 patch 目标改 DB 路径 + 新增 DB 异常回退/非 JSON 健壮性用例。验证：backend unit 1969 通过（4 个语义缓存失败为并行会话在途改动，与本次无关）+ ruff/mypy 0；线上 8180 全链路实测字段推断（id → "PSC关联关系记录的唯一标识ID，表的主键，以字符型存储" confidence 0.55）+ 表级推断（catalog 700 → "记录患者与医生、专家、科室在诊断场景下的关联关系…" confidence 0.75）均 source=llm 成功落库（column_descriptions / db_catalog.description）。提交 b31cd3c + a7c5f02。 | _build_llm_client/_parse_llm_description_result | released（LLM 描述推断不可用修复） |
| 2026-08-14 | — | collector + llm + conflict (LLM 结构化输出统一解析器 + 描述推断 json_schema 强约束) | **采集目录/资产地图 LLM 推断结果格式不可保证（用户诉求：如何保证 LLM 返回指定格式）**。方案 C：统一解析层 + 请求层强约束双保险。① **统一解析模块** `app/services/llm/parse.py`（新建）：fence 剥离（` ```json ` 围栏）、字段别名抽取（description/desc/text/label/summary 兼容）、类型强转与范围校验（confidence 0~1）、解析失败一律返回 None（上层降级不抛异常）；`parse_description_result`/`parse_bool_result`/`parse_json_object`/`extract_str_field`/`extract_numeric_field` 五个公开函数。② **三处解析收敛**：`collector._parse_llm_description_result`、`conflict.judge_same_semantics` 同义判定、`client._parse_structured_output` 统一委托 `parse` 模块，消除 `json.loads` 散落与 fence/别名不一致。③ **请求层强约束**：`_llm_infer_column_description`/`_llm_infer_table_description` 的 `response_format` 由 `json_object` 升级为 `json_schema`（strict，锁定 description/confidence 结构），网关不支持则降级 `json_object`；解析为 None 时追加约束提示重试 1 次（最多 2 次尝试）。验证：新增 test_llm_parse 26 项 + 现有 collector/conflict 测试 213 全绿 + ruff/mypy 0。提交 3f47bb2。 | app/services/llm/parse.py、_parse_llm_description_result/_infer_description_structured、parse_bool_result、_parse_structured_output | released（LLM 输出统一解析器 + json_schema 强约束） |
| 2026-08-14 | — | users (用户管理 · platform_admin 专属) | **用户所属域下拉化 + 创建随机密码（用户诉求：所属域改为选项框可配置管理；创建用户自动随机生成密码）**。① **所属域改为主题域下拉**：创建/编辑弹窗的 domain 由自由文本 `<Input>` 改为 `<Select>`，数据源复用 `listDomainTree("active")` + `flattenDomains`（与数据源页业务域下拉同源），列表值在既有「主题域管理」页增删改/启停即自动生效，无需新建配置模块；编辑弹窗 allowClear 可清空。② **后端强校验**：`create_user`/`update_user` 新增 `_assert_domain_active`——domain 若提供须为存在且 active 的主题域 code，否则 422 `USER_DOMAIN_INVALID`（错误码新增入 TD §5.4，§3.5a 同步说明），防止绕过 UI 直写接口注入任意域值。③ **创建用户随机强密码**：打开创建弹窗自动预填强随机密码（`crypto.getRandomValues`，16 位含大小写/数字/符号各 ≥1，满足后端 ≥8 位要求）+「重新生成」按钮；创建成功后弹窗一次性展示明文密码（可复制交付，明文仅内存不落日志、服务端仍只存 bcrypt 哈希不回传）。验证：后端 test_users_api 14 项全绿（新增创建/编辑非法域 422 反向用例各 1）+ ruff 0 + mypy users.py 0；前端 UserManagement.test 7 项全绿（新增主题域选择、自动预填强密码、明文展示）+ tsc 0。 | /api/v1/users 创建/编辑、用户管理页创建/编辑弹窗、TD §5.4 错误码 | released（用户管理增强） |
| 2026-08-14 | — | users (独立复核) | §1.5 独立复核（commit 66b116a，复核方 agent-ffd17721）：独立重跑 6 道门禁——后端 pytest test_users_api 14 passed / ruff All checks passed / mypy users.py 0 error；前端 vitest UserManagement 7 passed / tsc --noEmit 0 error；contract + doc_sync（PR_BODY=TD§3.5a,§5.4）均通过。走查：_assert_domain_active 仅 create/update 调用且空 domain 放行；SQL 参数化无注入；与前端 listDomainTree(active) 同源同口径；随机密码用 CSPRNG、明文仅内存一次性展示不落日志；commit 恰含 8 个声明文件、未误带并行会话改动（collector.py/Catalogs.tsx 仅在工作区）。结论：复核通过，0 阻塞项。 | — | verified（独立复核通过） |
| 2026-08-14 | — | collector + frontend (描述推断 in-flight 去重) | **重复调用 LLM 推断（用户反馈：进入页面推断后退出再进，再次点击是否重复调用）**。确认会重复且后端无幂等，落地 A+B：① **后端 409 幂等**：新建 `app/services/collector/infer_guard.py`（`InferInflightGuard`，对齐 CollectionLock——Redis `SET NX EX` TTL 120s 跨进程去重 + Redis 不可用降级进程内 dict（模块级共享）+ Lua owner 校验 release）；`collector.py` 三个推断端点（单字段/批量/表级）包 `_infer_inflight` async context manager，已有推断进行中返回 409 `LLM_INFER_IN_PROGRESS`，异常路径 finally 释放 + TTL 兜底。② **前端模块级拦截**（`DescriptionCoverageTab.tsx`）：模块级 `inferInflight: Map<key, Promise>`（跨组件实例，组件卸载不重置），单字段/批量/表级推断统一走 `runInflight` guard——退出再进二次点击不再发请求并提示「推断正在进行中」；识别后端 409 同样提示。验证：infer_guard 6 项单测 + collector_api 3 项 409 用例 + 相关 149 全绿 + ruff/mypy 0；前端 AssetMap 29 项（新增「退出再进二次点击被拦截」回归测试）+ tsc 0。遗留：Catalogs.tsx 前端已有 loading 防护但无模块级拦截，依赖后端 409 兜底（该文件为并行会话在途改动，未触碰）。提交 90b2ca2。 | InferInflightGuard、_infer_inflight、runInflight、inferInflight | released（描述推断重复调用防护） |
| 2026-08-15 | — | frontend (冲突仲裁对比弹窗 SQL 美化) | **口径定义行 SQL 多行格式化（用户反馈：对比弹窗里 SQL 一行挤在一起，不容易对比）**。后端 `definition_json.sql` 是单行 SQL（如 `SELECT order_amount AS gmv, channel, dt FROM ods_e2e_order`），前端 `<pre>` 原样渲染导致对比时整行滚动。新建 `frontend/src/utils/sqlFormat.ts`：纯函数 `formatSql(sql)`，token 级美化——SELECT 独占一行、列逗号换行缩进 1、FROM/WHERE/GROUP BY/ORDER BY/LIMIT/JOIN/ON 独立成行、WHERE/ON 内 AND/OR 换行缩进、子查询括号缩进（区分函数调用 vs 子查询 via parenStack）、字符串字面量整体保护避免误伤内部逗号/关键字、不改变大小写与语义。`ObjectView` + `DefinitionView` 的 sql/dialect_sql/etl_sql 渲染分支统一接入 `formatSql`。验证：`sqlFormat.test.ts` 13 项全绿（多列/WHERE 条件/JOIN/COUNT(DISTINCT)/字符串保护/子查询/UNION/大小写保留等）+ ReviewWorkbench 10 项回归全绿 + tsc 0 + Playwright 实渲染确认弹窗 SQL 按 SELECT/FROM/GROUP BY 多行展开、标签列整齐右对齐。 | formatSql、sqlFormat.ts | released（口径 SQL 美化） |
| 2026-08-14 | — | users (用户管理批量启用/停用) | **用户管理页面缺少批量启用/停用能力（用户诉求；此前仅单条操作，TD/PRD 均无批量规划）**。① **后端**：新增 `POST /users/batch-status` 端点（请求体 `user_ids` 1~200 + `status=active|disabled`，静态路径注册于 `/{user_id}` 系列前）——**207 语义**：一次性查回目标用户，不存在项标注 `USER_NOT_FOUND`、批量禁用含当前登录账号项标注 `SELF_DISABLE_FORBIDDEN`，均逐项进 `failed` 不影响其余更新；成功项进 `succeeded`（含 username）；单条汇总审计 `USER_STATUS_BATCH`（detail 含 status + 成败计数）。② **前端**：`UserManagement` 列表加 `rowSelection` 多选（仅 platform_admin 显示复选框 + 已选数量提示）；工具栏新增「批量启用」「批量停用」（停用 Popconfirm 二次确认 + 影响说明），结果按成功/失败清单 message 反馈（部分失败逐项标注 username+原因）；操作后清空选择并刷新。③ **测试**：后端 test_users_api 19 项全绿（新增批量禁用成功/启用成功/部分失败/空列表 422/超 200 上限 422 共 5 项）；前端 UserManagement 10 项全绿（新增批量停用确认调用/批量启用部分失败提示/viewer 无批量按钮与复选框 3 项）+ 全套 vitest（本次文件全绿，DataSources 瞬时失败为并行会话在途改动、单独跑全绿）+ tsc 0。 | POST /users/batch-status、UserManagement rowSelection 批量启用/停用 | released（用户管理批量启用/停用） |
| 2026-08-14 | — | users (独立复核) | §1.5 独立复核（commit d17cb7a，复核方 agent-4174f074）：独立重跑 7 道门禁——后端 pytest test_users_api 19 passed / ruff All checks passed / mypy users.py 0 error；前端 vitest UserManagement 10 passed / tsc --noEmit 0 error；contract + doc_sync（PR_BODY=TD影响章节: §3.5a）均通过。走查：batch-status 注册于 /{user_id} 系列前、user_ids min/max_length + status Literal 由 pydantic 兜底；207 语义正确（USER_NOT_FOUND/SELF_DISABLE_FORBIDDEN 逐项进 failed 不影响其余）；自禁条件与单条 PATCH 同口径同错误码；前端 rowSelection 仅 platform_admin、停用 Popconfirm 二次确认、部分失败逐项提示、操作后清空选择；write_audit 仅一条 action=USER_STATUS_BATCH 含成败计数；commit 恰含 10 个声明文件、api.ts 暂存部分不含并行会话 inferMetricDescription。结论：复核通过，0 阻塞项。非阻塞观察：批量端点未过滤软删用户（status=deleted 会被置回 active/disabled），与单条 PATCH 口径一致，属既有行为非本次引入。 | — | verified（独立复核通过） |
| 2026-08-15 | — | semantic + frontend (指标业务描述 LLM 推断) | **资产地图指标业务描述只支持人工编辑、缺 LLM 推断（用户诉求：指标类业务描述要增加 LLM 推断或人工编辑）**。后端 `MetricService.infer_metric_description`：owner/admin + PDP write 权限（与 update_metric_description 同语义）、`LlmConfigService.build_client` 构建客户端（DB 配置优先 + 路由/熔断，DB 读取异常回退 env 静态客户端）、json_schema 强约束 + json_object 降级 + 解析失败追加约束提示重试 1 次、统一解析器 `parse_description_result` 容错 fence/别名/越界；`POST /metric-definitions/{code}/infer-description` 端点（_WRITE_DEPS + 审计 metric_description detail={"source":"llm"} + 同事务提交），LLM 不可用/解析失败抛 `LLM_INFER_UNAVAILABLE`。前端资产地图指标抽屉「业务描述」卡片新增「AI 推断」按钮（loading 态 + ThunderboltOutlined），成功刷新展示 source=llm 描述。验证：test_semantic_service 127 全绿（新增 LLM 成功 source=llm / LLM 不可用 / 解析失败 / PDP 拒绝 / 越权 5 项）+ ruff/mypy 0；前端 AssetMap 32 项（新增 AI 推断用例）+ tsc 0。提交 63ce3f9 + 5d9c265。 | infer_metric_description/_llm_infer_metric_description/_build_llm_client | released（指标业务描述 LLM 推断） |
| 2026-08-15 | — | collector + frontend (批量推断 504 修复) | **批量推断缺失描述接口返回 504（用户反馈：批量推断缺失描述时接口返回 504 Gateway Time-out）**。根因（实测）：批量推断同步串行逐字段调 LLM，单字段 9-12s（慢的 56s——被 opencode 429/kilo 404 拖累）、30 缺失字段表约 360s，远超 nginx 反代默认 60s → nginx 直接 504。修复：① **后端并发化** `collector.py` `infer_descriptions_batch`：先收集待推断字段（保留跳过已有 manual/llm/comment 逻辑），`asyncio.Semaphore(4)` + `asyncio.gather(return_exceptions=True)` 并发调 `_llm_infer_column_description`，写库保持串行有序（zip strict）——30 字段从 ~360s 降至 ~90s。② **nginx 超时放宽** `frontend/Dockerfile`：`/api/` 反代加 `proxy_connect_timeout 30s` / `proxy_send_timeout 300s` / `proxy_read_timeout 300s`，兜底网关偶发慢。③ **Catalogs.tsx 模块级拦截**：补齐 DescriptionCoverageTab 同款 `inferInflight` Map + `runInflight` + `isInferInProgress`（模块级跨组件实例），单字段/批量推断走 guard——退出再进二次点击拦截并提示「正在进行中」，识别后端 409 同样提示。验证：collector_api 13 项全绿（新增「批量推断成功并发限流」——Semaphore 限流 4 + 峰值并发 ≥2 + 正确分类 skipped/inferred/failed）+ ruff/mypy 0；前端 Catalogs 16 项全绿（新增「批量推断进行中退出再进被拦截」回归测试）+ tsc 0。提交 faf2958。 | infer_descriptions_batch 并发化、nginx proxy_read_timeout 300s、Catalogs runInflight | released（批量推断 504 修复） |
| 2026-08-15 | — | collector (批量推断一次调用) | **批量推断缺失描述由逐字段并发改为一次 LLM 调用返回全部字段（用户诉求：能否一次性发送给 LLM，且每字段有顺序性、返回遵守格式）**。① **后端**：`parse.py` 新增 `parse_batch_description_result`——解析 `{"descriptions":[{column_name,description,confidence}]}` 数组，**按 column_name 匹配回填（不依赖 LLM 返回顺序，模型漏报/插报/乱序不污染）**，容错别名/fence/越界；`service.py` 新增 `_llm_infer_batch_descriptions`——整表字段清单一次 chat，`json_schema` 数组强约束（strict 锁定元素字段）优先、网关不支持降级 `json_object`、解析失败重试 1 次，LLM 不可用返回空 dict 不阻断；`collector.py` `infer_descriptions_batch` 改为一次调用 + 60 字段分块兜底（每块仍为一次请求），写库保持 targets 原始顺序。② **验证**：test_llm_parse 33 项（新增 batch 解析 7 项：顺序无关/过滤插报/部分缺失/fence+别名/越界跳过）+ test_llm_description 新增 batch 推断 4 项（正常乱序回填/json_schema 强约束/禁用/超时/LlmError 降级重试）+ test_collector_api 批量成功与部分失败 2 项（断言仅一次调用 + 按顺序回填 + 缺失字段进 failed）——相关 196 项全绿 + 全量 unit 2046 通过（4 失败为并行会话语义缓存预存）+ ruff/mypy 0。提交 920cfb6。 | parse_batch_description_result、_llm_infer_batch_descriptions、infer_descriptions_batch 一次调用 | released（批量推断一次调用） |
| 2026-08-15 | — | frontend (vite 轮询监听根治 HMR 失效) | **vite HMR 缓存失效反复出现（多轮「改代码页面无变化」只能手动重启，用户诉求：做长期方案根治）**。根因：macOS FSEvents 在本仓库静默失效，vite 监听不到文件变更 → 模块转换缓存不失效 → 页面沿用旧编译产物。修复：`vite.config.ts` `server.watch` 开启 `usePolling: true`（chokidar 轮询，不依赖系统事件）+ `interval: 200` + 显式 `ignored`（.git / node_modules / .eslintcache / dist / test-results* / .playwright-cli，降低轮询开销并避免并行测试写文件触发无谓刷新）。验证（不重启 vite）：向 `sqlFormat.ts` 写入代码级探针字符串，模块产物 1.5s 内感知变更（grep 命中 1）；移除探针后再次感知（归 0）——双向验证通过；HMR 日志同步传播至依赖方（LineageView/AiAssistant/display）；关键模块编译全部 200。 | vite.config.ts server.watch.usePolling | released（vite 轮询监听） |
| 2026-08-15 | — | auth + governance + users + frontend (权限治理缺口全量修复) | **权限治理全景审计（2026-08-14）发现 15+ 项缺口，组建 6 轨并行团队修复（P0 安全 ×5、P1 模型 ×4、P2 体验 ×3，跨前后端）**。**P0 安全**：① **登出撤销落地**——`deps.get_current_user` 解码后查 jti 黑名单（命中抛 `AUTH_TOKEN_REVOKED=401`，`middleware` 映射），前端登出先调 `POST /auth/logout`（`apiLogout`）再清本地态，杜绝「登出后 15 分钟 access token 仍可用」；② **登录防撞库**——新增 `app/core/login_throttle.py`（username+IP 固定窗口 15 分钟 10 次，Redis INCR+EXPIRE 优先、内存降级），`auth.login` 失败计数/超限 `AUTH_RATE_LIMITED=429`；③ **内部用户查询接入 PDP**——`POST /consume/metrics/{code}/query` internal_user 分支此前跳过一切鉴权，现经 `GovernanceService.check_internal_read_permission`（platform_admin 直通/本域角色/跨域须命中 ACTIVE 未过期 grants），拒绝 `403 FORBIDDEN`；restricted 授权命中时 `_filter_restricted_rows` 按 metric_whitelist 兜底过滤结果行（行级权限从「仅标注」升级为「基础过滤」，完整 RLS 二期）；④ **审计读权限收窄**——`audit._READ_ROLES` 剔除 `viewer`（保留 PA/DA/compliance_officer），只读角色不再可见含 actor/detail/PII 标记的完整审计；⑤ **运营端点鉴权**——`/health/degraded`、`/dependencies/health`、`/metrics` 挂 `require_roles(PA, DA)`（`/health`、`/ready` 探针保持开放）。**P1 模型**：⑥ **collector 写角色收窄**——`_WRITE_ROLES` 剔除 `metric_owner`（单域指标负责人不再操作平台级数据源连接/批删/启停/调度），仅 PA/DA；⑦ **域/字典写操作补审计**——`subject_domain.py` 5 端点 + `system_dict.py` 4 端点全部 `write_audit`（action=subject_domain.create/update/status/delete/update_defaults、DICT_CREATE/UPDATE/STATUS/DELETE，含 actor/IP/trace_id）；⑧ **PII 复核角色对齐**——`metrics._PII_REVIEW_ROLES` 增 `compliance_officer`（与 governance `/pii/review` 口径一致）；⑨ **密码自助管理**——新增 `POST /users/me/password`（任意登录角色，校验旧密码 `PASSWORD_INCORRECT=401` + 复杂度 `PASSWORD_WEAK=422` + 新旧不同 `PASSWORD_SAME=422`）；创建/重置统一复杂度校验（≥8 位含大小写/数字/特殊字符至少 3 类）；`user` 表新增 `must_change_password` 列（迁移 0044，server_default 回填存量），管理员创建/重置后置 True，登录响应携带该字段；⑩ **授权域下拉化**——`Governance.GrantsTab`「授权域」由自由文本 Input 改为主题域 Select（复用 `listDomainTree(active)` + `flattenDomains`，与用户管理同款）；⑪ **RolesTab 补 analyst**——角色枚举 7 项对齐后端（alert 文案 + Select 选项）。**P2 体验**：⑫ **路由守卫 + 菜单过滤**——`App.tsx` 新增 `RequireRole`（管理类路由 /users /governance /audit /api-clients /system-config /observability /tracking-stats 仅 PA/DA，越权重定向 /dashboard）；`Layout.tsx` NAV_GROUPS 按 `ADMIN_ONLY_NAV_KEYS` 过滤（非管理员隐藏管理入口、空组整组隐藏）；⑬ **403 中间态消除**——`api.ts` 对 403 不再 `clearToken()`（保留登录态，交路由守卫/页面处理，避免「清 access 后刷新仍 403」割裂）；⑭ **首登强制改密前端**——`Layout.tsx` 新增 `PasswordChangeModal`（force 不可关闭态 + 普通自助改密态复用，成功后清除）。**集成修复**：consume `execute_query` 内部用户分支 `user`→`internal_user` 变量名错误（NameError→500）修复；`middleware` 补 `PASSWORD_INCORRECT: 401` 映射（AuthError 默认 403 误映射）；metrics.py 注释行超长（E501）修复。**验证**：后端全量 unit 2049 通过（4 失败为既有 semantic_cache，非本次引入；新增黑名单拒绝/PDP 拒绝/自助改密/健康端点鉴权等用例）+ ruff 0 + mypy 0（改动文件）；前端全量 vitest 348 通过（DataSources 瞬时失败为并行会话在途改动，单独跑 22/22 全绿）+ tsc 0；健康/审计/collector/metrics/governance/consume 相关测试全绿。文档同步：TD §3.5a（/users/me/password、密码策略）、§5.4（新增 AUTH_TOKEN_REVOKED/PASSWORD_INCORRECT/AUTH_RATE_LIMITED/PASSWORD_WEAK/PASSWORD_SAME）、§12.5（内部查询 PDP 接入、行级基础过滤）；module-status users/governance/subject_domain/system_dict note；CHANGELOG 本条目。**遗留**：角色/权限点可配置化（P1，需设计）、org 多租户管理（P1，需设计）、batchGrant 前端接线（P2）、审计导出端点（P2）、完整 RLS 维度值级过滤+脱敏（二期）。 | 登出撤销(AUTH_TOKEN_REVOKED)、login_throttle(AUTH_RATE_LIMITED)、consume internal PDP、audit 读角色收窄、health 鉴权、collector 写角色收窄、域/字典写审计、PII 复核角色对齐、/users/me/password+must_change_password(迁移0044)、GrantsTab 域下拉、RolesTab analyst、路由守卫/菜单过滤/403 中间态、PasswordChangeModal | released（权限治理缺口全量修复） |
| 2026-08-15 | — | assetmap + semantic (表节点详情兜底 + AI 推断去重) | **资产地图表节点点击提示暂无详情 + AI 推断重复调用且耗时（用户诉求：表节点可看详情；重复调用需去重）**。① **Neo4j 图谱表节点富集 entity_id**——`_get_graph_neo4j` 构建节点后按 entity_name 批量回查 db_catalog（repository 新增 `catalog_id_by_names`），与 MySQL 降级路径一致，此前 Neo4j 路径表节点 entity_id=None 致点击「暂不支持查看详情」；② **infer-description 幂等短路**——service/endpoint 加 `force` 参数，force=false 且已有 LLM 描述时直接返回不重复调 LLM（省时省成本），force=true 强制重新生成；③ **前端表节点点击兜底**——无 entity_id 时 `listCatalogs` 按表名回查命中开详情，未命中弹确认框引导跳转采集目录；④ **AI 推断去重+耗时显示**——已有 LLM 描述时按钮显示「重新生成」+ 确认框（确认→force=true），推断中显示「AI 推断中… Ns」。验证：后端 semantic force 短路 2 + Neo4j entity_id 富集 1 + repo catalog_id_by_names 2 全绿 + ruff/mypy 0；前端 AssetMap 35 项全绿（新增重新生成确认/无描述直调/表节点回查 3 项）+ 全套 352 全绿 + tsc 0。提交 19c0a9d。 | backend/app/services/assetmap/{repository,service}.py、backend/app/services/semantic/service.py、backend/app/api/metrics.py、frontend/src/{pages/AssetMap.tsx,api.ts}、tests | released（表节点详情兜底 + AI 推断去重上线） |
| 2026-08-15 | — | collector + frontend (字段详情描述合并展示) | **LLM 推断生成描述后，再次打开采集目录字段详情却看不到数据（用户反馈：推断生成描述后再次打开查看详情没有数据）**。根因：描述写入 `column_descriptions` 表（不回写 `schema_json.comment`），而采集目录字段详情抽屉用的是列表行 `schema_def`（`/catalogs` 列表接口 `DBCatalogResponse` 直接 `model_validate`，未合并描述）→ 前端 `parseSchemaColumns` 只映射 `comment`（推断后仍空）→ SchemaTable 显示空；推断后当时能看到是前端乐观更新，关闭抽屉再开重新解析列表行又为空。修复：① **后端** `service.py` 新增 `_merge_descriptions_to_schema`（与 assetmap `_merge_descriptions` 同语义 manual>llm>schema），`list_catalogs` 批量经 repository 新增 `get_descriptions_for_catalogs`（IN 查询避免 N+1）合并、`get_catalog_detail` 合并到 `schema_def.columns[]` 的 `description`/`description_source`；② **前端** `Catalogs.tsx` `parseSchemaColumns` 映射 `description`/`description_source`（SchemaTable 已支持 `record.description || record.comment` 展示）。验证：test_collector 120 全绿（新增「详情合并 column_descriptions」回归：有记录注入 description/source、无记录不注入）+ ruff/mypy 0；前端 Catalogs 16 全绿 + tsc 0。提交 520d4b7（后端三文件随并行提交 19c0a9d/af009d8 进入 HEAD）。 | _merge_descriptions_to_schema、get_descriptions_for_catalogs、Catalogs parseSchemaColumns description 映射 | released（字段详情描述合并展示） |
| 2026-08-15 | — | governance + users + org + audit (四项治理缺口补齐) | **权限治理审计遗留 4 项缺口补齐（2026-08-15）**。① **RBAC 权限点可配置化**：`ROLE_ACTIONS` 默认基线之上新增 `role_permission` 覆盖表（迁移 0045，(role,action) 唯一）——`GET /roles` 返回各角色默认/自定义/生效动作与受保护标记；`PUT /roles/{role}/permissions` 覆盖权限点（仅 platform_admin，空数组=全部回收，未知动作 422 ROLE_PERMISSION_INVALID / 受保护角色 422 ROLE_PERMISSION_PROTECTED）；`DELETE /roles/{role}/permissions` 恢复默认；`decide()` 保持纯函数（新增 `role_actions` 参数，由 `GovernanceService.load_role_actions()` 合并默认+覆盖传入 check_permission/check_metric_permission/check_internal_read_permission/my_permissions）；前端「权限治理→角色管理」升级为权限点矩阵（Checkbox 勾选 + 保存/恢复默认，platform_admin 行受保护禁用）。② **组织（租户）多租户管理闭环**：新增 `app/api/organizations.py`——`GET /organizations`（列表含用户数统计）、`POST /organizations`（platform_admin，编码唯一 ORG_EXISTS=409）、`PATCH /organizations/{id}`（名称/状态）；多租户隔离——`get_current_user` 校验所属组织状态（suspended/deleted 拒绝登录 401 ORG_DISABLED）；自我保护——默认组织（code=default）不可删除（ORG_PROTECTED=422）、不能停用/删除当前管理员所属组织（ORG_SELF_LOCK=422）、有用户不可删除（ORG_HAS_USERS=409）；创建用户可选 org_id（缺省当前管理员组织，须 active，ORG_NOT_FOUND=404/ORG_DISABLED=422）；前端新增「组织管理」页（/organizations 路由 + 菜单，平台管理员可写、管理员可读）。③ **batchGrant 前端接线**：GrantsTab 新增「批量授权」——多选用户 + 统一授权参数 → dry-run 预览影响（逐条 ok/detail）→ 确认执行 `POST /grants/batch`，消灭 api.batchGrant 死代码。④ **审计导出端点**：`GET /audit/export`（CSV/JSON，UTF-8 BOM 防 Excel 乱码，limit 上限 10000 防全表拉取，导出动作本身落 AUDIT_EXPORT 审计；过滤条件与列表共用 `_apply_filters`）；前端 AuditLog 页新增「导出 CSV」按钮（fetch blob 下载）。**验证**：后端全量 unit 2084 通过（5 失败——4 个既有 semantic_cache + 1 个 collector 并行瞬时单独跑过）+ ruff 0 + mypy 0（改动文件 10 个）；前端 vitest 相关 12 项全绿（UserManagement 10 + Governance 4）+ tsc 0；新增/更新测试——policy decide role_actions 4 项、governance service RBAC 6 项、organizations API 8 项、deps ORG_DISABLED 2 项、audit export 4 项、DataGovernance 4 项。文档同步：TD §5.4（新增 ORG_DISABLED/ORG_EXISTS/ORG_NOT_FOUND/ORG_PROTECTED/ORG_SELF_LOCK/ORG_HAS_USERS/ROLE_PERMISSION_INVALID/ROLE_PERMISSION_PROTECTED）、§3.5a（创建用户 org_id）、§12.5（RBAC 可配置化 + 组织闭环）；module-status governance/users note；CHANGELOG 本条目。**遗留**：完整 RLS 维度值级过滤+脱敏（二期）、PDP 决策缓存（Redis 60s）。 | role_permission 表(迁移0045)+/roles 权限点矩阵、/organizations 管理+登录阻断+自我保护、GrantsTab 批量授权、/audit/export+AuditLog 导出按钮 | released（四项治理缺口补齐） |
| 2026-08-15 | — | governance + users + org + audit (独立复核) | §1.5 独立复核（commit f75f25b，复核方独立复核 agent）：独立重跑 10 道门禁——后端 pytest 6 文件 132 passed / ruff All checks passed / mypy 2 源文件 Success；前端 vitest DataGovernance/UserManagement/Layout 22 passed / tsc --noEmit 0 error；contract + doc_sync 均通过。走查：organizations 动态路径 PATCH /{org_id} 注册于静态 GET/POST 之后符合约定；迁移链 0045_role_permission down=0044 正确挂接；审计导出 format 正则强约束 + csv.writer RFC4180 转义 + UTF-8 BOM，导出动作自身落 AUDIT_EXPORT；组织 API 保护 default 删除（ORG_PROTECTED）但未禁 suspended（见观察③）；policy decide() role_actions 默认 None 回退模块级 ROLE_ACTIONS、platform_admin 早于覆盖层直通，向后兼容；commit 恰含 32 个声明文件，api.ts 无并行 inferMetricDescription、types.ts 无 read_at 混入。结论：复核通过，0 阻塞项。非阻塞观察：① 行数 +2286 与预期 +2212 偏差 74 行，diff 内容均为治理功能无异常；② AuditLog.test.tsx 仓库不存在（实际前端测试为 DataGovernance/UserManagement）；③ default 组织仅防删除未防 suspended——若平台管理员将 default 置 suspended 会锁定其下全部成员登录，建议产品确认；④ contract 脚本 9 个 TD 路径仅命中 1 个仍判通过，口径偏宽建议后续收紧；⑤ HMAC key 30 字节 <32 触发 InsecureKeyLengthWarning 为仓库既有配置。 | — | verified（独立复核通过） |
| 2026-08-15 | — | collector + semantic + frontend (LLM 描述推断防重补齐) | **全平台 LLM 推断防重核查后发现 3 处缺口（用户要求全部补齐）**。① **表/字段推断幂等短路**：`InferDescriptionRequest`/`InferTableDescriptionRequest` 加 `force`（默认 false）；`collector.py` 表推断——已有 `description_source=llm` 且未 force 直接返回现有描述不调 LLM；列推断——repository 新增 `get_description` 单条查询，已有 llm 描述且未 force 短路返回。② **指标推断 in-flight 锁**：`InferInflightGuard` 泛化 `_key/acquire/release/is_locked` 支持 `int|str` 实体 ID；`metrics.py` 新增 `_metric_infer_inflight`（kind=metric、Redis SET NX EX 降级进程内）包裹 `infer_metric_description`，首次并发双调被 409 `LLM_INFER_IN_PROGRESS` 拦截。③ **前端 AssetMap 表/字段推断接入 runInflight**：模块级 `inferInflight` Map（跨组件实例共享）+ `runInflight`；已有 llm 描述时点推断弹「重新生成」确认（确认→force=true），推断进行中二次点击提示「正在进行中」。验证：test_collector_api 短路/force 4 项 + test_metrics_infer_api 冲突 1 项 + 相关 147 全绿 + ruff/mypy 0；前端 AssetMap 37 项（新增表推断确认/无描述直调 2 项）+ tsc 0。提交 571a811。 | force 幂等短路、InferInflightGuard 泛化、_metric_infer_inflight、AssetMap runInflight | released（LLM 推断防重补齐） |
| 2026-08-15 | — | tracking + frontend (埋点统计全量去重用户数) | **埋点统计「去重用户数」卡片把各分组 unique_actors 直接相加（用户反馈：不准确，目前只有一个用户却显示多个）**。根因：`stats[].unique_actors` 是**分组内**去重数，同一用户跨分组（跨事件类型/目标类型）会被重复计数，前端 `reduce` 求和得到的是"分组去重数之和"而非全量去重用户。修复：① **后端** `GET /tracking/stats` 新增 `total_unique_actors`——与分组统计共用过滤条件（`clauses` 提取复用）的 `COUNT(DISTINCT actor_id)`，`TrackingStatsResponse` 加字段；② **前端** `TrackingStats.tsx` 汇总卡片优先用 `data.total_unique_actors`，缺失（旧后端）时降级为各组之和（向后兼容）；`types.ts` `TrackingStatsResponse` 加 `total_unique_actors?`。验证：前端 TrackingStats 9 项全绿（更新断言：全量 40 ≠ 各组相加 50）+ tsc 0；真实 MySQL 实证——全量去重 **2** vs 分组相加 **12**（10 个分组重复计数），修复后展示真实值。 | total_unique_actors（后端）+ 汇总卡片优先全量值（前端） | released（埋点统计全量去重用户数） |
| 2026-08-15 | — | governance + org + ci + security (三项非阻塞观察修复) | **f75f25b 独立复核提出的 3 项非阻塞观察全部修复（2026-08-15）**。① **default 组织禁止 suspended**——`organizations.py` 状态机自我保护由「默认组织仅禁 deleted」升级为「suspended / deleted 均禁」（`ORG_PROTECTED=422`），防止平台管理员将默认租户置 suspended 锁死其下全部成员登录；补测试 `test_update_default_org_suspend_protected`。② **契约脚本收紧为逐一校验**——重写 `scripts/contract_check.py` 的 `check_contract_with_code`：旧实现从 TD 抓 9 个"路径"（含前端页面路由/外部端点/前缀说明）只要命中任 1 个即通过（口径过宽）；新实现只提取 TD 中带 HTTP 方法的接口声明（74 个去重），**逐一**与后端真实路由校验（源码解析 `main.py include_router` + 各模块 `APIRouter` 前缀 + 方法路径，218 条），每个路径必须有明确去向——直接命中 24 / 语义映射 34（`TD_SEMANTIC_ALIASES` 台账：TD 产品语义名如 `/metrics`→实现 `/metric-definitions`、`/sources`→`/data-sources`，映射目标须在后端真实存在否则映射失效）/ 豁免 16（`TD_EXEMPT_PATHS` 台账：TD §12 状态表标注「待做·二期·未落地」与非后端 API，如 Airflow 外部 `/dags/{id}`、前端 `/help`），**未命中且未在台账 → 校验失败**。负向验证：TD 新增未知路径→失败、删除后端映射目标路由→映射失效失败。③ **SECRET_KEY 加长到 32+ 字节**——`backend/tests/conftest.py` 测试 JWT 密钥 `test-secret-key-for-unit-tests`（30 字节）改为 `test-secret-key-for-unit-tests-32bytes`（38 字节），消除 PyJWT `InsecureKeyLengthWarning`（生产校验 `config.py` 已强制 ≥32）；`.env.example` 补注释提示密钥 ≥32 字节。验证：test_organizations_api 9 passed（+1）、test_deps/security/config 36 passed、ruff 0；contract 三 mode（contract/doc_sync/gateways_verify）均通过。 | default 组织 suspended 保护、contract_check 逐一校验(74 路径台账)、conftest JWT key 38 字节 | released（三项非阻塞观察修复） |
| 2026-08-15 | — | governance + org + ci + security (独立复核) | §1.5 独立复核（commit cfa9577，复核方 agent-93288723；注：cfa9577 为 ffd7cb3 被并行 rebase 回退后的等价重提交，6 文件 delta 与 ffd7cb3 逐字节一致）。独立重跑 6 道门禁——① commit 确认：--stat 恰 6 文件 +232/-23，并行会话 7 个前端文件均不在提交内；② 后端单测 36 passed（org 9 含 suspended 用例），`-W error::UserWarning` 通过无 InsecureKeyLengthWarning；③ ruff All checks passed；④ mypy Success；⑤ 契约三 mode 全过（contract: 74 路径 命中24/映射34/豁免16/未命中0；doc_sync、gateways_verify 通过）；⑥ 负向验证：注入不存在路径→未命中→exit 1、移除映射目标→_alias_matches 返回 None。走查：organizations.py:198 由仅 "deleted" 扩为 ("suspended","deleted") 抛 ORG_PROTECTED=422，test_organizations_api.py:198 覆盖 suspended→422；contract_check.py 含 TD_SEMANTIC_ALIASES(L40)/TD_EXEMPT_PATHS(L78)，L216-224 逐一校验四分类、L345-349 errors→return 1；conftest.py:50 38 字节 ≥32、.env.example:41 补 ≥32 字节注释；工作区并行改动隔离。结论：复核通过，0 阻塞项。非阻塞观察：① 字面门禁 `git diff cfa9577 ffd7cb3 应为空` 不成立（两 commit parent 不同整树 11 文件差异），但按增量比对 6 文件完全一致，等价重提交意图成立，建议门禁口径改 delta 比对；② 仓库系统 python 为 Python 2，契约脚本须 python3 运行；③ 匹配器对尾斜杠变体（/api/v1/metrics vs /api/v1/metrics/）不敏感，当前 TD 无实际影响，可留作鲁棒性改进。 | — | verified（独立复核通过） |
| 2026-08-15 | — | ci + governance (三项非阻塞观察二次修复) | **cfa9577 独立复核提出的 3 项非阻塞观察修复（2026-08-15）**。① **契约脚本强制 python3**——`scripts/contract_check.py` 顶部加 Python 3 版本守卫（`sys.version_info[0] < 3` 直接 `SystemExit` 报错退出，防仓库系统 Python 2（实测 macOS `/usr/bin/python` 为 Py2）误跑产生假阳性/被语法错误悄悄跳过）；`.github/workflows/gateways.yml` 的 `contract-and-docsync` job 显式 `python3` 运行；DEV_GUIDE 门禁表契约/doc_sync 两行注明须 `python3`。② **匹配器尾斜杠变体归一化**——`_path_pattern`/`_pattern_eq`/`_matches_any`/TD 路径提取处统一 `rstrip("/")`（根路径 `/` 兜底），`/api/v1/metrics` 与 `/api/v1/metrics/` 视为等价；负向验证确认未知路径（含尾斜杠变体）仍不命中、豁免表精确 key 不误命中。③ **DEV_GUIDE 独立复核补充 delta 比对口径**——等价重提交（并行 rebase/回退后重提）验证**不得**用 `git diff A B` 整树比对断言「应为空」（parent 不同整树必有差异会误报），正确口径为分别比对 `A^..A` 与 `B^..B` 的增量逐文件逐字节一致。验证：contract 三 mode（contract/doc_sync/gateways_verify）全过（74 路径 命中24/映射34/豁免16/未命中0）；尾斜杠变体 5 项断言全 True；负向 4 项断言正确；gateways.yml YAML 语法 OK。 | Python3 版本守卫、尾斜杠归一化、DEV_GUIDE delta 比对口径 | released（三项非阻塞观察二次修复） |
| 2026-08-15 | — | ci + governance (独立复核) | §1.5 独立复核（commit 5b23a48，复核方 agent-b2fe069e）。独立重跑门禁——① commit 确认：--stat 恰 4 文件 +18/-6，diff 无前端文件，工作区 7 个未提交改动全为并行会话前端文件；② 契约三 mode 复跑全过（contract: 74 路径 命中24/映射34/豁免16/未命中0；doc_sync；gateways_verify）；③ 尾斜杠归一化 5 项断言全 True（带尾斜杠命中/映射/等价）+ 负向未知路径不命中；④ Python 3 版本守卫位置正确；⑤ DEV_GUIDE 门禁表两行注明 python3 + §6.2 delta 比对口径。走查：根路径 "/" 经 rstrip or "/" 兜底不误归一化；rstrip 仅去尾部 / 不影响 `*` 通配；具体路径仍不被 /* 顶替；CHANGELOG 追加行无并行内容。结论：复核通过，0 阻塞项。非阻塞观察：python2 实测为 parse 期 SyntaxError（Py3-only 语法 L46 list[...]），运行时守卫分支在真实 python2 下不可达——但目标（Py2 下响亮失败、不静默通过）已达成，且补 `# -*- coding: utf-8 -*-` 后报错点从「中文编码错误」前移至「Py3-only 语法」，诊断性更好，属 Python 2 解析器先于执行的固有限制。 | — | verified（独立复核通过） |
| 2026-08-15 | — | system_dict + frontend (参照数据编码自动生成) | **数据字典模块新建参照数据项时编码由自由输入改为按显示名自动生成英文名（用户反馈）**。后端：`DictItemCreate.code` 改可选（缺省时由 `SystemDictService._generate_unique_code` 生成——`codegen.slugify_code` 术语字典翻译（贪心最长匹配）+未覆盖字拼音兜底+ASCII 保留、`generate_unique_code` 冲突自动追加 `_2/_3`（上限 100）、纯标点/空白显示名回退 `item`，编码唯一性判定复用 `code_exists_in_type`）；日志与审计 detail 均以最终 item.code 为准。前端：新增弹窗「编码」输入框改为「编码（自动生成）」disabled 实时预览——`Form.useWatch("label")` 监听显示名经 `zhEnDict.slugifyCode` 计算（与后端 `codegen.slugify_code` 规则对齐），空显示名回退 item；提交不传 code 由后端生成；`DictItemCreateRequest.code` 改可选。工程重构：`slugifyCode` 从 SubjectDomain.tsx 本地私有函数提取为 `zhEnDict.ts` 共享导出（消除重复实现，SubjectDomain 改复用，行为零变化）。验证：后端 test_system_dict_service 39 passed（新增自动生成/冲突自增/纯符号回退 3 项）+ ruff 0 + mypy 0；前端 SystemDict 9 / SubjectDomain 18 / zhEnDict 7 全绿 + tsc 0（并行会话 AssetGraph/MetricCreate 在途改动除外）。 | code 可选+slugify 自动生成+冲突自增+纯符号回退 item；前端编码自动生成预览；slugifyCode 提为共享工具 | released（参照数据编码自动生成） |
| 2026-08-15 | — | system_dict + frontend (独立复核) | §1.5 独立复核（commit 1eee1fe，复核方 agent-5cc0d8b3）。独立重跑 7 道门禁——① commit 确认：--stat 恰 9 文件 +198/-53 与声明一致，无并行会话文件混入，CHANGELOG 行已在 HEAD；② 后端 pytest 3 文件 39 passed（含 3 个自动生成用例：code 自动生成/冲突自增/纯符号回退 item）；③ ruff All checks passed + mypy Success；④ 前端 vitest SystemDict/SubjectDomain/zhEnDict 34 passed；⑤ tsc --noEmit 0 error；⑥ contract + doc_sync 均通过。走查：_generate_unique_code slugify+冲突自增+回退 item+RuntimeError 转 BusinessError(DICT_CODE_EXHAUSTED)；create_item code 缺省自动生成且向后兼容（有值走原逻辑）；schemas code 可选 validator 对 None 放行；前端 codePreview 与后端兜底一致、提交不传 code、预览 disabled 无手动入口；slugifyCode 原样搬迁共享、SubjectDomain 行为不变；api.ts 未动、types.ts code 可选同步。结论：复核通过，0 阻塞项。非阻塞观察：① tsc 无预期错误（并行 AssetGraph 可能已修复）；② SubjectDomain 测试 act() 警告（rc-motion 动画，无关）；③ 前后端空 token 处理微差异（极端场景预览中段双下划线，预览仅供参考以后端为准）；④ 前端预览不含冲突自增后缀，tooltip 已注明。 | — | verified（独立复核通过） |
| 2026-08-15 | — | conflict + frontend (对比表头标注候选/现有指标) | **冲突仲裁对比表头未明确标注候选指标 vs 现有指标**（用户反馈：点击仲裁弹窗看不出哪一列是候选/现有）。修复：① `MetricCompareTable` 加可选 props `labelA?/labelB?`，传入时表头渲染彩色 SideTag（**候选=蓝强调 #2f54eb / 现有=灰中性 #6b7280**）+ 编码；不传时保持纯编码（独立对比页 `/compare` 不受影响）。② `ReviewWorkbench.tsx` 两个对比弹窗（只读对比 + 仲裁）传 `labelA="候选指标"` `labelB="现有指标"`。③ 测试加断言：`within(dialog).getByText("候选指标"/"现有指标")` 限定弹窗内（避免与列表列标题冲突）。验证：ReviewWorkbench 13 项 + MetricCompare 4 项 全绿（共 17 项），tsc 0 错误；Playwright 实渲染确认 SideTag 样式精确命中（候选 color=rgb(47,84,235) bg=rgba(47,84,235,0.1) / 现有 color=rgb(107,114,128) bg=rgba(140,140,140,0.14)）。 | labelA/labelB（MetricCompareTable 新增可选 props） | released（冲突仲裁对比表头候选/现有标注） |
| 2026-08-16 | — | feedback + frontend (反馈交互优化) | **用户反馈：① 点跟进/采纳/驳回后按钮无状态变化；② 提交反馈后需手动刷新才见新反馈；③ 提交表单的「对象 ID」无用；④ 对象类型/分类选项过少；⑤ NPS 弹窗对象类型选项过少**。修复：① 操作列按反馈状态差异化——已采纳/已驳回终态显示「—」（结果已由状态列展示，避免与状态列重复）、跟进中时「跟进」按钮 disabled、其余状态三个按钮齐全（点处理后状态列即时更新）；② Tab 联动——提交反馈成功自动切回「用户反馈」列表并刷新（refreshToken 驱动），新反馈立即可见；③ 移除提交表单「对象 ID」输入框；④ 对象类型选项统一为 `TARGET_TYPE_ZH` 全量映射（15 项：指标/术语/报表/仪表盘/平台/数据表/字段/数据源/指标模板/维度/收藏/待办/口径冲突/满意度等），提交表单默认「指标」、NPS 弹窗同用全集；分类选项含缺陷/功能需求/改进建议/咨询/表扬；⑤ 反馈意见改为必填 + placeholder 引导。验证：FeedbackCenter 8 项全绿（新增按钮状态化 + Tab 联动 2 项），tsc 0 错误；后端无改动。 | 无（纯前端） | released（反馈交互优化） |
| 2026-08-15 | — | conflict + frontend (裁决记录弹窗业务术语化) | **冲突仲裁「裁决记录」弹窗展示英文决策值（choose_canonical/merge/keep_diff）与仲裁人数字 ID（#42）**（延续业务术语化主线，与埋点统计/可观测中心同源问题）。修复：① `enums.ts` 新增 `RULING_DECISION_LABEL`（choose_canonical→选为权威 / merge→合并口径 / keep_diff→保留差异，含 choose_existing/choose_candidate 兼容值）；② `ReviewWorkbench.tsx` 加载 `listUsers` 建立仲裁人 ID→用户名映射（display_name 优先，失败回落 #ID 不影响主流程），裁决记录弹窗「决策」列中文化、「仲裁人」列显示中文名。验证：ReviewWorkbench 13 项全绿（新增断言 choose_canonical 不再直出、仲裁人显示李仲裁），tsc 0 错误。 | RULING_DECISION_LABEL（enums 新增）；裁决记录两列渲染 | released（裁决记录弹窗业务术语化） |
| 2026-08-15 | — | frontend (四项非阻塞观察处理) | **1eee1fe 独立复核提出的 4 项非阻塞观察处理（2026-08-15）**。① **tsc 无预期错误**——并行会话已修复 AssetGraph，当前 tsc 0 error，无需处理（git 确认归属并行会话）。② **SubjectDomain 测试 act() 警告**——rc-motion/CSSMotion 动画产物（CI 门禁无前端 vitest job、警告不阻塞、非本次引入），接受不处理。③ **前后端空 token 处理对齐**——`zhEnDict.slugifyCode` 改为 `tokens.filter(Boolean).join("_")` 剥除中段空 token（对齐后端 `codegen.slugify_code` 的 `"_".join(t for t in tokens if t)`），消除极端场景（某字无字典命中且无拼音）中段双下划线（如 `a__b`）；补回归测试（vi.mock pinyin-pro 对无拼音字返回空串断言 `a_b`）。④ **预览含冲突自增后缀**——`zhEnDict.ts` 新增共享纯函数 `resolveUniqueCode`（逐字节对齐后端 `codegen.generate_unique_code`：截断 64、冲突 `_2/_3` 自增、上限 100 回退 base），`SystemDict` 新增弹窗编码预览升级为基于当前类型已加载（非软删）items 编码的实时冲突检测——预览即后端将生成的最终编码（如分钟 → minute_2），与 `code_exists_in_type`（仅非软删）口径一致；tooltip 文案更新。验证：前端 zhEnDict 16 / SystemDict 10 / SubjectDomain 18 全绿（新增 resolveUniqueCode 5 项：无冲突/冲突 _2/多级自增/超长截断 64/超上限回退 + SystemDict 冲突后缀预览 1 项）+ tsc 0（观察①确认 0 error）。 | resolveUniqueCode（zhEnDict 新增）、slugifyCode 空 token 过滤、SystemDict 实时冲突预览 | released（四项非阻塞观察处理） |
| 2026-08-15 | — | frontend (独立复核) | §1.5 独立复核（commit ee48bdf，复核方 agent-d8d30c9b）。独立重跑门禁——① commit 确认：--stat 恰 5 文件 +121/-8 与声明一致，无并行会话文件（Catalogs/AssetGraph/enums/DataGovernance/conflict）混入；② 前端 vitest zhEnDict/SystemDict/SubjectDomain 44 项全绿（resolveUniqueCode 5 项、slugifyCode 空 token 过滤 `A𠮷B→a_b`、SystemDict 冲突后缀预览 minute_2 全部通过）；③ tsc --noEmit exit 0（观察①确认并行会话已修复 AssetGraph）。走查：resolveUniqueCode 与后端 generate_unique_code 逐字节对齐（截断 64、`_N` 后缀 `base[:64-len(suffix)]+suffix`、上限 100，超限回退 base 已在 docstring 声明）；SystemDict codePreview 用 useMemo 依赖 [watchLabel, items]，冲突检测 items 为当前类型非软删项（list_by_type deleted_at.is_(None)）与后端 code_exists_in_type 口径一致；slugifyCode filter(Boolean) 对齐后端 "_".join(t for t in tokens if t)；CHANGELOG 仅追加 1 行无并行内容。结论：复核通过，0 阻塞项。非阻塞观察：① 超上限回退 base 预览无动态提示（极端 100 次冲突场景，提交时后端抛 DICT_CODE_EXHAUSTED 页面有提示）；② 并发致预览滞后（基于已加载快照，提交以后端为准）；③ act() 警告确认仍存在（观察②接受不处理，CI 无前端 vitest job 不阻塞）；④ 工作树有大量并行会话未提交改动，后续提交需留意。 | — | verified（独立复核通过） |
| 2026-08-15 | — | conflict (仲裁后 pending_conflict 联动清除 + rulings 500) | **两个跨服务数据一致性问题（用户反馈）**。① **GET /conflicts/{id}/rulings 500**——`RulingRecordResponse.model_validate(ORM 对象)` 缺 `from_attributes=True`（Pydantic v2 对 ORM 默认拒绝），`RulingRecordResponse` 补 `model_config = ConfigDict(from_attributes=True)`。② **仲裁后指标详情仍显示「口径冲突待处理」**——`metric.pending_conflict` 是冗余布尔标记，仅在指标注册预检（`conflict_precheck.py`）置 True，**仲裁/关闭后从不清除**，跨服务一致性缺失。修复：`ConflictRepository` 新增 `count_open_for_metric(metric_code)`（JSON `$.candidate`/`$.existing` 提取，统计该指标仍处 OPEN/NEGOTIATING/ESCALATED 的未决冲突数）；`ConflictService` 注入 `metric_conflict_clearer` 回调（可空，缺省 no-op 保持解耦），`arbitrate`/`close` 成功后调 `_sync_metric_conflict_flag`——**仅当该指标不再有其他未决冲突时才清除**（避免误清仍关联其他冲突的指标），best-effort 降级不阻断仲裁主流程；API 层 `_svc` 注入真实实现（条件更新 `Metric` 表 `pending_conflict=False, pending_conflict_detail=None`）。验证：后端 conflict 20 项全绿（新增 repository count_open_for_metric 2 项、service 仲裁/关闭联动 4 项、schema from_attributes 回归 1 项）+ 全量 2105 passed（4 failed 为并行会话 cache.py prune 逻辑 mock 未同步，与本次无关）+ 导入 ok。 | from_attributes（RulingRecordResponse）；count_open_for_metric + metric_conflict_clearer 联动 | released（仲裁后 pending_conflict 联动清除 + rulings 500 修复） |
| 2026-08-15 | — | frontend (ee48bdf 四项非阻塞观察处理) | **ee48bdf 独立复核提出的 4 项非阻塞观察处理（2026-08-15）**。① **超上限回退 base 无动态提示**——`SystemDict` 新增 `codeExhausted` 检测（`usedCodes.includes(codePreview)`，resolveUniqueCode 超 100 次冲突回退 base 且 base 仍被占用时成立），编码字段由「自动生成 disabled 预览」切换为「需手动指定」可输入编码（橙色 Tag + 格式校验 `^[A-Za-z0-9_]+$`），提交时手动 code 随表单透传（补齐后端 `DICT_CODE_EXHAUSTED` 死路——此前无法自动生成时 UI 无手动入口）。② **并发致预览滞后**——打开新增弹窗时静默刷新项列表（`refreshItemsQuietly`，不置 loading），缩小「他端新增同名编码但本页未刷新」的预览滞后窗口（提交仍以后端权威判定为准）。③ **act() 警告**——`setup.ts` 统一过滤 React "not wrapped in act(...)" 警告（antd/rc-motion 出入场动画与 App.message 通知在 jsdom 中动画未完成即触发，注释说明测试断言仍会捕获真实异步问题，仅少诊断堆栈），测试输出整洁。④ **并行会话改动隔离**——提交仅含目标文件，未误带并行会话 conflict/Catalogs/AssetGraph 改动。验证：前端 SystemDict 12（新增打开刷新 1 + 超上限切手动 1）/ SubjectDomain 18 / zhEnDict 16 全绿（超限用例 101 项大列表单独放宽超时 15s）+ tsc 0；全量 419 passed（并行在途改动导致的瞬时失败除外）。 | codeExhausted 切手动指定、openCreate 静默刷新、act 警告过滤 | released（ee48bdf 四项非阻塞观察处理） |
| 2026-08-15 | — | frontend (独立复核) | §1.5 独立复核（commit b10f86a，复核方 agent-25ef4e71）。独立重跑门禁——① commit 确认：--stat 恰 4 文件 +105/-11，工作区 9 个并行会话文件零重叠；② 前端 vitest SystemDict 12 / SubjectDomain 18 / zhEnDict 16 全绿；③ act 警告计数 0（setup.ts 过滤生效）；④ tsc --noEmit exit 0；⑤ doc_sync 通过。走查：codeExhausted 计算正确（超上限回退 base 必在 used 中 → includes 判 exhausted；正常自增 _2 不误判）；手动指定交互（exhausted 时 code Form.Item 可输入 + 格式校验 + 橙色 Tag，非 exhausted 保留 disabled 预览）；handleCreate 透传 code（有则传无则不传）且提交后 resetFields 覆盖；openCreate 静默刷新（不置 loading，仅更新 items）；setup.ts console.error 过滤仅拦截 "not wrapped in act(" 其余原样透传；超限测试构造 101 项 + 15s 放宽超时。结论：复核通过，0 阻塞项。非阻塞观察：① code Form.Item 默认 preserve=true——超上限输入 code 后改显示名切回非超上限，字段卸载但值残留，提交仍透传过期 code，建议 preserve={false}（已顺手修复，见下条）；② 静默刷新每次打开弹窗多一次请求，影响极小。 | — | verified（独立复核通过） |
| 2026-08-15 | — | frontend (复核观察①补丁) | **b10f86a 复核方提出的 preserve 边角修复**——`SystemDict` 手动指定编码的 `Form.Item name="code"` 加 `preserve={false}`：超上限模式输入 code 后若改显示名使 base 变化、切回非超上限（自动生成预览），字段卸载时值被清除，提交不再残留透传过期 code（此前 antd 默认 preserve=true 会把旧 code 一并提交）。补回归测试：超上限输入 item_101 → 改显示名「分钟」→ 切回自动生成预览（minute）→ 提交断言 code 为 undefined。验证：SystemDict 13 项全绿（新增 preserve 回归 1 项）+ tsc 0。 | preserve=false（code Form.Item） | released（preserve 残留 code 修复） |
| 2026-08-15 | — | conflict (重新打开重审) | **已关闭冲突支持重新打开并重新裁决（用户反馈）**。状态机补 `CLOSED → OPEN`：`ConflictRepository.reopen`（状态置 OPEN、清除 resolved_at）；`ConflictService.reopen`（仅 CLOSED 可打开，其余抛 ConflictError；发 `conflict_reopened` 事件；历史裁决记录保留在 ruling_record 作为知识库）；跨服务一致性对称联动——新增 `metric_conflict_marker` 回调，重新打开后回置候选指标 `pending_conflict=True + pending_conflict_detail`（与仲裁/关闭时的清除对称，指标详情页重新显示「口径冲突待处理」），best-effort 降级不阻断。API `POST /conflicts/{id}/reopen`（_GOV_DEPS + `CONFLICT_REOPEN` 审计）。前端：操作列 CLOSED 新增「重新打开」（确认弹窗 + 对比复看），重新打开后回到待处理可再仲裁；`reopenConflict` API；`review_reopen` 埋点与 `CONFLICT_ARBITRATE/CLOSE/REOPEN` 审计动作中文映射补齐。验证：后端 conflict 71 项全绿（新增 repository reopen 1 + service reopen 4）、前端 ReviewWorkbench 15 项全绿、tsc 0。 | POST /conflicts/{id}/reopen（新增） | released（CLOSED→OPEN 重新打开重审） |

| 2026-08-15 | — | frontend (参照数据编码预览收尾) | **SystemDict 独立复核三项非阻塞观察修复（ee48bdf 复核观察①已闭环 + b10f86a 复核观察②③）**。① **antd preserve 边角**（c089be7 已修，本次验证闭环）：手动指定编码 `Form.Item name="code"` 加 `preserve={false}`，超上限输入后改显示名切回自动生成不残留透传过期 code，回归测试「超上限手动→切回→提交 code=undefined」全链路覆盖。② **静默刷新 TTL 防抖**：`refreshItemsQuietly` 加 `QUIET_REFRESH_TTL_MS=30s` 防抖（`lastQuietRefreshRef`），TTL 内重复打开新增弹窗直接用缓存不额外请求，超 TTL 才静默刷新——消除无并发时每次打开多发一次 `listAllDictItems` 的冗余，仍保留缩小并发滞后窗口能力；导出常量便于测试同步，新增「TTL 内不请求/超 TTL 刷新」用例（`vi.spyOn(Date.now)` 控时）。③ **act 过滤正向验证**：`setup.ts` act 警告过滤分支新增 `window.__ACT_FILTERED` 计数，暴露给测试断言「确有过滤发生」而非「无警告=过滤坏了」的不可观测状态；新增用例验证打开 Modal 触发 rc-motion 动画产生被过滤警告且计数增加（实测需先让出事件循环一帧——waitFor 轮询被 act 包裹不推进渲染队列）。验证：SystemDict 15 项（新增 TTL + act 正向验证 2 项）+ 相关组 49 全绿 + tsc 0（并行会话 AssetMap/enums 类型漂移除外）。 | QUIET_REFRESH_TTL_MS、__ACT_FILTERED、preserve=false | released（参照数据编码预览收尾） |

| 2026-08-15 | — | frontend (独立复核) | §1.5 独立复核（commit d8fd752，复核方 agent-baafda0b）。独立重跑门禁——① commit 确认：--stat 恰 3 文件 +64/-3（setup.ts/SystemDict.tsx/SystemDict.test.tsx），无并行内容混入；② 前端 vitest 3 文件 49 全绿（SystemDict 15 含 TTL 防抖 + act 正向验证 2 新用例）；③ tsc 本提交 3 文件 0 error（全仓 AssetMap/enums 错误为并行在途改动）；④ 契约 doc_sync 校验通过。走查 a-d：__ACT_FILTERED 计数正确且不干扰 console.error 透传、QUIET_REFRESH_TTL_MS 首次必刷新/TTL 内 return、preserve={false} 仍在且回归测试覆盖、act 用例先让出一帧+waitFor 双保险设计有据。**verdict 复核通过，0 阻塞项**。复核方非阻塞建议：TTL 测试 mockRestore 应在 finally 中执行防泄漏——已顺手修复（try/finally 包裹）。 | d8fd752 | released（参照数据编码预览收尾复核） |
| 2026-08-15 | — | assetmap (7e0c17e 数据完整度全面升级) | **资产地图数据完整度全面升级（用户反馈：指标不够全/健康不准确/各子功能完整度不足）**。① **新增指标体系聚合** `GET /assetmap/metric-dimensions`：8 类维度分布（类型/数仓层/分级/单位/聚合/时间语义/状态/域）+ PII 合规率（pii_compliance.review_rate）；前端概览新增「指标体系」区块（8 张维度分布卡可点击下钻 + PII 合规卡）。② **健康评分升级**：health_summary 从 4 项扩到 9 项体检（新增表描述缺失/字段描述缺失/PII 未复核/无快照/废弃未替换），逐项扣分 + score/level 分档（≥90 优/≥75 良/≥60 中/<60 差），前端健康 Tab 加评分总览 + 9 项体检表 + 3 张新明细表。③ **搜索富化**：表级结果带源名/责任人/描述/字段数/时间；新增字段级搜索（type=field，返回 table.field，内存包含匹配避开 LIKE 下划线转义坑）；指标级带类型/粒度/单位/聚合/新鲜度/分级等 10+ 口径字段。④ **变更富化**：目录/指标带 change_type（created/updated/deprecated）+ 源名/责任人名 + 指标版本号 + 接入 schema_drift_log drift 明细表。⑤ **我的资产富化**：顶部统计卡（目录/指标/草稿/PII/快照覆盖）+ 待认领孤儿引导 + 目录/指标表补描述/字段数/口径列。⑥ **Owner 视图富化**：目录可下钻明细表（替代死数字）+ 类型/分级分布 + 快照覆盖 + 待办（PII 未复核/废弃未替换）。测试：assetmap 91 单测全绿（新增 8）+ ruff/mypy 0；前端 AssetMap 42 + 全量 438 全绿 + tsc 0。 | GET /assetmap/metric-dimensions（新增）、health 评分、search/changes/my-assets/owner 富化 | released（7e0c17e 数据完整度升级） |
| 2026-08-15 | — | conflict+frontend (仲裁联动指标 TD§12.4) | **仲裁裁决联动指标（方案 A，用户确认）：仲裁从"只写结论"升级为"真正消除重复口径"**。① **落败方处置**：仲裁成功按 canonical_code 判定胜负——落败方 PUBLISHED → deprecate（DEPRECATED+successor=胜方+sunset+发 metric.deprecated 事件，消费方经 DeprecatedChain 自动引导）；未发布（DRAFT/REVIEW/EXPERIMENTAL）→ 软删作废；DEPRECATED/已删 → 幂等跳过。② **胜方标记权威**：新增 metric.arbitration_mark JSON 列（迁移 0047），胜方写 canonical、keep_diff 双方写 coexist；详情页展示绿色「权威口径」/蓝色「已裁定共存」Tag（悬停展示裁决冲突/决策中文/落败方/时间）。③ **架构**：conflict.service 新增 arbitration_applier 回调（解耦可测），API 层注入真实实现（复用 MetricService.deprecate_metric 完整事件闭环），联动与仲裁同事务提交、best-effort 降级不阻断。④ **强韧性**：胜方未 PUBLISHED 时不废弃 PUBLISHED 落败方（避免无替代指标）；canonical 不在冲突双方时跳过。测试：新增 test_conflict_arbitration 9 项（决策×状态×保护全覆盖）+ conflict service 3 项联动；后端 conflict 120 全绿、前端 MetricDetail 6 全绿（新增权威/共存 2 项）；端到端真实库验证（选候选为权威→胜方 canonical 标记+落败 DRAFT 软删作废）后清理恢复。 | POST /conflicts/{id}/arbitrate 行为变更（联动指标）、metric.arbitration_mark（新增） | released（仲裁联动指标） |
| 2026-08-15 | — | assetmap (指标体系 13 类维度) | **指标体系 8→13 类维度扩展（用户反馈：缺粒度且不够全面）**。后端 metric_dimension_summary 新增 5 类分布：by_granularity（粒度）/by_currency（币种）/by_freshness（新鲜度）/by_serving_mode（服务模式）/by_additivity（可加性），覆盖 Metric 表全部可分布枚举字段；前端新增 GRANULARITY/CURRENCY/FRESHNESS/SERVING_MODE/ADDITIVITY 标签映射（粒度按生产小写 day 归一 + 大写兼容存量），METRIC_DIMENSION_GROUPS 8→13 卡，每卡可点击下钻分布明细。验证：assetmap repository 90 单测全绿（新增粒度/币种/新鲜度/服务模式/可加性分布断言）+ ruff 0；前端 AssetMap 43（新增「指标体系展示 13 类维度并可下钻」用例）+ 全量 tsc 0。实跑确认生产 metric.granularity 全为小写 day，映射正确显示「日 8」。 | GET /assetmap/metric-dimensions 响应新增 5 类 by_* 分布 | released（aea4d63 指标体系扩展） |
| 2026-08-15 | — | lineage + frontend (SQL 血缘解析支持 Doris/ClickHouse 方言) | **血缘视图「SQL 血缘解析」方言下拉补齐 Doris、保留 ClickHouse（用户诉求：支持 Doris 和 ClickHouse 两种数据库类型）**。前端 `LineageView.tsx` 方言下拉由内联数组改为与系统数据源类型对齐的 `SQL_DIALECT_OPTIONS`（mysql/postgres/hive/spark/doris/clickhouse/starrocks 带标签）——**补齐缺失的 Doris、保留 ClickHouse、移除不属于系统数据源类型的 duckdb**；后端本就经 sqlglot 支持两种方言，本次将 `LineageParseRequest.dialect` 描述与 `parser.py` docstring 明确为含 doris/clickhouse，方言单测扩展至 clickhouse（原仅 doris/hive/mysql）。验证：后端 lineage 105 单测全绿 + ruff 0 + format 0；前端 LineageView 11 项全绿（新增 ParseTab 方言选择 1 项）+ tsc 0。注：本文件与并行会话的 MetricDetailDrawer 改动共存（LineageView.tsx），并行会话已同步更新其指标点击测试。 | SQL_DIALECT_OPTIONS（前端新增常量）、LineageParseRequest.dialect 描述 | released（SQL 血缘解析 Doris/ClickHouse 方言支持） |
| 2026-08-15 | — | conflict+semantic+frontend (落败指标友好引导 METRIC_ARCHIVED) | **修复：仲裁作废的落败指标被直访时给出裸 404「指标不存在」→ 改为友好引导（用户反馈：展示太粗暴，应跳转胜方或展示败方并引导去看胜方详情）**。① **落败方补写指针**：仲裁软删未发布落败方时，同条 UPDATE 补写 `successor_code`（指向胜方）+ `arbitration_mark(status=defeated)`——此前落败方被软删后无任何指向胜方的指针，详情直访无处可去。② **结构化友好 404**：新增 `ErrorCode.METRIC_ARCHIVED`；`get_metric_public` 命中「软删 + successor」的作废指标时返回结构化错误（detail 携带 successor_code/arbitration_mark），repository 新增 `get_archived_by_code`（含软删记录查询）。③ **前端友好页**：`MetricDetail` 捕获 METRIC_ARCHIVED 渲染作废引导页（原指标 + 裁决方式/冲突/时间 + 权威指标跳转按钮 + 返回），不再直出「指标不存在」。④ **存量回填**：真实库 `sales_e2e_conflictb_day` 补写 successor+defeated。验证：后端 conflict+semantic 168 全绿（新增 archived 3 项 + 软删指针断言）、ruff 0；前端 MetricDetail 7 全绿（新增作废引导跳转 1 项）+ 全量 42 文件 450 全绿 + tsc 0。 | GET /metric-definitions/{code} 对作废指标返回 METRIC_ARCHIVED（detail 含 successor_code）、metric 落败方 successor_code/arbitration_mark 补写 | released（落败指标友好引导） |
| 2026-08-15 | — | assetmap (变更追踪行点击侧边栏详情) | **变更追踪点击具体行时在本页侧边栏展示详细信息（用户反馈：点击行要能在侧边栏看到具体信息）**。① 目录行 → 实体详情抽屉（fetchAssetEntityDetail 按 id 拉取：实体/数据源/敏感度/责任人/字段数/表级描述/Schema 状态/源健康/新鲜度 + 字段清单 SchemaTable + 血缘边明细 + 关联指标），抽屉 footer「在采集目录中查看」跳 /catalog?kw=；② 指标行 → 指标详情抽屉（getMetric + listSnapshots：业务描述/口径 Descriptions/口径明细 DefinitionsDetail/数值快照），不再整行直接跳转指标详情页（编码列链接保留跳转，行为细分而非移除）；③ 漂移行 → Schema 漂移抽屉（diff_json 结构化解构：变更键值 Descriptions + before/after 左右对照 pre）。实现为模块级组件 ChangeCatalogDetailDrawer/ChangeMetricDetailDrawer/ChangeDriftDetailDrawer（复用 DefinitionsDetail/renderSchemaSummary）+ ResizableDrawer 拖拽调宽 + 宽度持久化。验证：前端 AssetMap 46 项（新增目录/指标/漂移行点击开抽屉 3 项）+ 全量 453 全绿 + tsc 0；提交 403d167 仅含 2 文件无并行混入。 | ChangeCatalogDetailDrawer/ChangeMetricDetailDrawer/ChangeDriftDetailDrawer（AssetMap 模块级新增） | released（变更追踪行点击侧边栏详情） |
| 2026-08-15 | — | security/core + lineage (注入守卫字段级豁免) | **修复：POST /lineage/parse 把合法 SQL 误判为注入（用户反馈 400「检测到 SQL 注入」）**。根因：全局守卫 `guard_against_injection` 对 body 所有字符串做正则扫描，而 `/parse` 的 `sql` 字段就是**待解析的 SQL 文本**（仅经 sqlglot 纯函数解析、不执行不拼接进任何 DB 查询）——合法血缘 SQL 里的 `--` 行注释 / `/* */` 块注释（Doris `+SET_VAR` HINT）/ `UNION [ALL] SELECT` / 多语句 ETL 会命中注入正则被误拦为 400 INJECTION_DETECTED。方案 A（字段级豁免）：`guard.py` 新增 `guard_against_injection_exempt(*exempt_fields)` 依赖工厂 + `_scan_deep` 支持顶层字段豁免（豁免仅作用于顶层键，嵌套同名键不豁免防绕过），`/parse` 改挂 `Depends(guard_against_injection_exempt("sql"))`，其余字段/query 参数/其他 30+ 写端点守卫完全不变（纵深防御零损失）。验证：guard 层豁免单测 7 项（注释/UNION/多语句 ETL 放行、provenance 注入 400、嵌套不豁免、query 仍拦）+ lineage 安全集成 2 项（合法 SQL → 200、provenance 注入 → 400 INJECTION_DETECTED）全绿；ruff/format 0；全量单测 2149 passed。注：test_semantic_cache 4 项 + security 5 项为预存失败（mock 异步未同步），基线对照确认与本次改动无关。 | POST /api/v1/lineage/parse（sql 字段豁免注入扫描） | released（修复合法 SQL 注入误判） |
| 2026-08-15 | — | conflict+semantic (作废指标体验修复 + 一致性加固) | **修复：仲裁作废指标仍显示「指标不存在」（用户反馈：作废指标应友好展示而非提示不存在）**。① **数据修复**：真实库 `sales_e2e_conflicta_day` 曾因**自我冲突**（existing==candidate 同码）被联动误软删作废（successor 死循环指向自身）→ 恢复为 canonical 胜方（deleted_at=NULL/successor=NULL/标记回 CF-56177120ED5B merge 胜方）+ 清 Redis 旧缓存。② **arbitration 自我冲突防御**：winner_code==loser_code 时仅标记权威、不作废唯一指标（防复发）。③ **health 端点友好化**：`get_metric_health` 命中「软删+successor」作废指标返回结构化 METRIC_ARCHIVED（与详情读路径一致），不再裸 404「指标不存在」。④ **缓存时效性**：仲裁联动（软删/废弃/keep_diff 标记）后显式失效受影响指标缓存（MetricService 新增 invalidate_cache，best-effort），消除 cache-aside 旧数据与 DB 不一致。验证：后端 arbitration 12 项（新增自我冲突 1 + 缓存失效 2）+ semantic 145 项（新增 health archived 1）全绿，API 导入 OK。 | GET /metric-definitions/{code}/health 对作废指标返回 METRIC_ARCHIVED；仲裁联动后失效指标缓存 | released（作废指标体验修复 + 一致性加固） |
| 2026-08-15 | — | assetmap (变更追踪返回机制审计修复) | **修复：变更追踪「在采集目录中查看」跳错页面 + 返回无来源感知（用户反馈：跳到了指标目录；返回只跳资产地图而非变更追踪）**。① **根因**：目录抽屉/图谱表节点引导跳转写成了 `/catalog`（指标目录），应为 `/catalogs`（采集目录）；且 AssetMap 内部 Tabs 是组件 state，`history.back()` 返回 `/assetmap` 会重新挂载重置到默认 graph tab。② **修复**：两处跳转改 `/catalogs?kw=&focus=&from=`（来源+定位参数）；采集目录 `Catalogs` 消费 `?from=变更追踪/资产地图` 显示来源返回按钮并精确跳 `/assetmap?tab=changes` 或 `/assetmap`、`?focus=` 高亮目标行（3s 自动清除）；`AssetMap` 支持 `?tab=` URL 初始化 Tab；变更追踪指标跳转带 `state.from=assetmap-changes`，`MetricDetail` 据此精确返回 `/assetmap?tab=changes`。③ **审计结论**：指标详情跳转返回机制原本已 OK（MetricDetail 有 dashboard/todo 来源感知 + history 兜底）；仅目录跳转两处存在路径错误 + 无来源感知，本次全部修复。验证：AssetMap 47（新增目录抽屉跳 `/catalogs`+from/focus 断言）+ Catalogs 18（新增来源返回/高亮 2 项）+ 全量 464 全绿 + tsc 0；提交 36e42b3（纯 assetmap 3 文件）+ b387252（含并行会话在途的字段详情抽屉重构，功能完整）。 | 目录抽屉/图谱表节点跳转 `/catalogs` + from/focus 参数；Catalogs 来源返回按钮；AssetMap `?tab=`；MetricDetail from=assetmap-changes | released（变更追踪返回机制审计修复） |
| 2026-08-15 | — | collector + frontend (采集三模块工业化：运行历史持久化 + 采集记录双 tab + 任务中心服务端分页) | **采集目录/采集任务/采集记录三模块工业化升级（用户诉求：达到工业级、数据丰富、覆盖所有生产场景；先 A 后 B）**。**B 采集运行历史持久化（治本）**：新增 `collection_run` 表（迁移 0049，`source_id/job_id/trigger/mode/effective_mode/status/actor_id/started_at/finished_at/scanned/registered/pii_registered/failed_count/drift_count/deprecated_count/coverage/error/detail_json`）——采集链路闭环：job（JobStore）是 ephemeral 运行时数据（终态 7 天 TTL），运行历史长期可追溯；同步 `POST /collect`、异步 worker `run_collection_task`（手动/定时）三路径落库，RUNNING→COMPLETED/FAILED，进程崩溃/超时/失败均正确收尾（`start/complete/fail_collection_run` 独立提交）；软删数据源级联改名保留历史。**新端点**：`GET /api/v1/collection-runs`（分页 + source_id/status/trigger 过滤 + 批量回填源名/责任人 + duration_seconds）、`GET /api/v1/collection-runs/{id}`（含 failed_specs/drift_events/degrade_reason 明细）。**A 采集任务中心**：`/jobs` 改服务端分页（JobStore 新增 `count`，InMemory/Redis/Arq 实现，返回 `{items,total,page,page_size}`，修复前端本地切片 50 条上限）——前端服务端分页 + showSizeChanger + **任务详情抽屉**（完整 error/进度消息/actor/created_at）+ **失败重试按钮**（复用 collect-now）。**采集记录页重写为双 tab**：① 采集记录 = 运行历史（统计摘要卡 采集次数/成功/失败/累计扫描/累计注册 + 数据源/状态/触发过滤 + 11 列表格含 耗时/模式降级标注 + 详情抽屉指标/失败实体/漂移事件/降级原因）；② 变更追踪 = drift 日志增强（**diff_json 明细渲染** 新增/删除/类型变更列替代截断签名、entity_name 过滤、分页 size changer、**移除 DROPPED 死代码**）。验证：后端 collector+queue+tasks+api 168 项全绿 + 全量单测 2135 passed（排除预存语义缓存）+ ruff/mypy 0（conflict 16 项为并行会话在途）；前端 CollectionTasks 8 + CollectionHistory 4 全绿 + 全量 470 全绿 + tsc 0。提交 5a59d20（后端）/2c58f01（前端）。 | GET /api/v1/collection-runs、GET /api/v1/collection-runs/{id}（新增）；GET /data-sources/jobs 响应改为 {items,total,page,page_size}（行为变更）；采集任务中心新增详情/重试 | released（采集三模块工业化） |
| 2026-08-15 | — | lineage + frontend (采集通道完整性 + 影响分析选项框 + 解析结果当页展示) | **血缘视图三项增强（用户诉求：① 采集通道来源要全面、SQL 解析要算进去；② 血缘查询/影响分析的节点选项框要给出可选值并支持关键词搜索；③ 解析后当页直接看到本次血缘效果，选方案 A）**。**① 采集通道完整性**：根因——SQL 解析边（provenance=sqlglot）已入库但 `parse_and_store` **不写 `lineage_ingest_run` 运行记录**（模型注释明确要求 dp_csv/quickbi/数据接口/SQL 解析均记运行摘要），通道列表只列「有边的来源」（0 边时 SQL 解析通道不可见），且卡片直接显示原始标识 `sqlglot`；另 ChannelsTab **进入时不自动加载**（仅点刷新）。修复：`parse_and_store` 改用 `upsert_edge_with_status` 统计 added/updated 并写 ingest_run（source=req.provenance）；`list_channels` 补全内置已知通道（sqlglot/dp_csv 0 边也展示）；前端通道卡片显示友好名称（SQL 解析/DP 同步等）+ 原始标识小字，进入 Tab 自动加载。**② 影响分析选项框**：新增 `GET /lineage/nodes?kw=&limit=` 候选节点接口（血缘边源∪目标去重聚合，按边数倒序，kw 模糊过滤）；前端节点输入改 AntD Select showSearch——进入预加载 top-50、输入关键词远程搜索、未命中兜底「使用输入值」自由指定，影响边「来源」列同步映射友好名称。**③ 解析结果当页展示（方案 A）**：`LineageParseResponse` 新增 `table_lineage`（source/target）+ `field_lineage`（source_table/source_column/target_table/target_column/expression）明细（保留原计数字段向后兼容）；ParseTab 在统计 Alert 下方用表格展示本次表级/字段级血缘边。验证：后端 lineage service 57（新增 parse 明细/ingest_run 3 项 + list_nodes 1 项）+ repository 60（新增 channels 补全 2 项 + list_nodes 1 项）+ parser/graph/events/guard 83 + lineage security 4 全绿，全量单测 2344 passed（9 预存失败与基线一致）；ruff/format/mypy 0；前端 LineageView 23 项（新增解析明细表格/影响分析选项框 2 项/采集通道友好名称 1 项）+ 全量 480 全绿 + tsc 0。 | POST /api/v1/lineage/parse 响应新增 table_lineage/field_lineage（行为向后兼容）；GET /api/v1/lineage/nodes（新增）；lineage_ingest_run 现覆盖 SQL 解析来源；通道列表补全已知通道 | released（血缘视图三项增强） |
| 2026-08-15 | — | quality (worker 自动检测 naive/aware 时间比较崩溃) | **修复：worker 质量自动检测任务每轮崩溃（用户反馈 worker unhealthy + 日志 TypeError）**。根因：`quality/tasks.py:58` `latest.obs_time < now - _OBS_FRESH_WINDOW` 中 `now = datetime.now(UTC)` 是 **aware**，而 `obs_time` 从 MySQL `DATETIME(timezone=True)` 读出是 **naive**（驱动不保留时区）→ `TypeError: can't compare offset-naive and offset-aware datetimes`，arq `quality-checks` 任务每 60s 失败一次（worker 持续 unhealthy）。修复：比较前对 naive obs_time `replace(tzinfo=UTC)` 统一按 UTC 解释（aware 值保持不动，健壮兼容两种驱动行为）。验证：新增 `test_naive_obs_time_handled`/`test_naive_stale_obs_skipped` 2 项回归（naive 新鲜触发评估、naive 陈旧正确跳过不崩溃）+ test_quality_tasks 8 全绿 + ruff/mypy 0；线上 worker 替换镜像后 `quality-checks` 恢复 `evaluated=1` 成功执行，TypeError 计数 0，worker healthy。提交 b9d5b5e。 | 无（纯修复） | released（worker 自动检测恢复） |
| 2026-08-15 | — | collector + frontend (采集目录列表产品化布局重设计) | **采集目录列表布局产品化重设计（用户反馈：列宽过大需左右滑动、数据源与实体列间距大，交互体验差）**。根因：实体列无固定宽度自适应拉伸（宽屏下间距空洞）+ `scroll={{x:900}}` 强制最小宽（窄屏横向滚动）。修复：① **实体列** `minWidth:320` 自适应吸收剩余空间 + 内容填满——表名/类型/schema 缺失 tag + **表级描述摘要一行**（无描述时显示业务域或「可在字段详情中补全」提示）；② **新增「字段」列**：字段数 + 描述覆盖（`23 字段 · 18 已描述（78%）`，绿/黄/红按覆盖率着色，从 schema_def 实时计算，schema 缺失显示 —）；③ 数据源列定宽 170（Tooltip 显示 source_id + 源已删除 tag）、敏感度 96、责任人 100 紧凑定宽；④ 操作列图标化（收藏改纯图标+Tooltip，字段详情保留文字），定宽 100；⑤ `scroll={{x:900}}` 配合新列宽（最小总宽约 892）——宽屏一屏放下、窄屏才滚动。数据丰富度不减反增（表级描述摘要/字段覆盖列是此前被隐藏的已返回数据）。验证：Catalogs 19 项全绿（新增「字段数与描述覆盖率」1 项）+ 全量 491 全绿 + tsc 0；线上 frontend 容器替换为 catalog-layout-84ec003（healthy），JS bundle 确认含新布局代码。提交 84ec003。 | 无（纯前端布局） | released（采集目录产品化布局） |
| 2026-08-15 | — | collector + frontend (采集目录列表真自适应 + 业务域/最近更新列 + 列显示开关) | **采集目录列表「真自适应 + 信息丰富度 + 可定制列」升级（用户反馈：实体/数据源间距仍很大，不能自适应；从产品角度分析信息是否足够丰富）**。**① 真自适应**：根因——`scroll={{x:900}}` 强制最小宽 + 未启用 `tableLayout:"fixed"`，antd 默认 auto 布局不会平均分配剩余空间。修复：`tableLayout:"fixed"` + 实体列**不设 width**（fixed 布局下吸收表格剩余宽度，宽屏一屏放下、列间距均匀）+ 全列 `ellipsis` + 描述摘要去 maxWidth 改 100% 省略 + `scroll={{x:"max"}}`（仅内容真正超宽才滚动，窄屏才横滚）。**② 信息丰富度补齐（对标 DataHub/OpenMetadata）**：新增**「业务域」列**（Tag 定宽 110，数据源继承回填，此前只在描述摘要兜底不可独立展示）+ 新增**「最近更新」列**（定宽 130，相对时间 刚刚/N 分钟前/N 小时前/N 天前/日期 + Tooltip 完整时间；后端 `DBCatalogResponse` 补透出 `updated_at`，from_attributes 自动映射 ORM 列，前端 DBCatalog 类型同步）。**③ DataHub 式列显示开关**：工具栏新增「列设置」下拉（Dropdown + Checkbox.Group，默认全开），用户可开关 数据源/字段覆盖/业务域/敏感度/责任人/最近更新 6 列，实体/操作列固定展示不可关。验证：后端 test_collector 127 全绿（新增 updated_at 透出断言）+ ruff/mypy 0；前端 Catalogs 21 项全绿（新增业务域/最近更新展示 + 列设置开关隐藏 2 项）+ 全量 494/495（1 失败为并行会话 LineageView 方言在途，与本次无关）+ tsc 0。提交 ba56c31；另修复 HEAD 编译断裂 27da537（ReviewWorkbench 测试 mock 补齐 traceId 字段声明）。 | DBCatalogResponse 新增 updated_at 字段（响应透出） | released（采集目录真自适应布局） |
| 2026-08-15 | — | lineage + frontend (纯 SELECT 血缘落地 A+B + 影响分析图形化 + 采集运行详情) | **血缘视图三连（用户诉求：① 纯 SELECT 也要能落血缘、无落点时当页展示流转；② 血缘查询/影响分析要有血缘视图展示效果而非文字；③ 采集通道运行历史点击行要展示具体信息，从产品角度完善数据信息丰富度）**。**① 纯 SELECT 血缘落地（方案 A+B）**：`LineageParseRequest` 新增可选 `target_table` 落点表——纯 SELECT 指定落点时 parser 走新增 `_select_table_edges`/`_select_field_edges`，把 FROM/JOIN 源表 → 目标表（表级）、SELECT 投影列 → 目标表列（字段级）生成**正式血缘并写图谱**（与自带 INSERT/CTAS 一致，血缘查询/影响分析可查，落点表自环跳过）；无落点（未指定且 SQL 无写入目标）时不写图谱、不写运行记录，新增 `extract_upstream_deps` 提取上游表/字段清单，`LineageParseResponse` 新增 `upstream_deps` 返回，ParseTab 当页展示「本次查询的上游依赖」列表（严格区分正式血缘与只读依赖，语义诚实）。**② 血缘查询/影响分析图形化**：前端新增 `edgesToGraphData` 将影响边映射为 AssetGraph 节点/边（表/指标/字段节点按类型着色 + 边带方向），影响分析结果改用 G6 力导向图展示血缘视图，边明细表格为辅——从文字列表升级为图形化血缘展示。**③ 采集运行详情（产品化信息丰富度）**：`LineageIngestRun` 新增 `detail_json` 列（迁移 0050，可逆）——SQL 解析运行存 SQL 原文/dialect/target_table/source_node/actor_id/本次表级与字段级边明细，批量采集（dp_csv 等）存新增/更新边明细；`finish_ingest_run` 支持 detail 快照参数；新增 `GET /lineage/runs/{run_id}` 端点（`get_ingest_run_detail`：JSON 反序列化到响应 `detail` 字段，快照缺失/损坏降级 None 仍返回计数摘要）；前端运行历史行点击「查看」或整行 → Drawer 展示本次运行具体信息（SQL 解析：SQL 原文 + 表级/字段级边明细表格；批量：新增/更新边明细表格）。验证：后端 lineage 149 项全绿（新增 16 项：parser 纯 SELECT 落点/上游依赖 7 + repository detail 4 + service 落点/详情 5）+ 全量 unit 2197 passed（4 预存失败 test_semantic_cache）+ security 173 passed（5 预存失败）+ ruff/format/mypy 0；前端 LineageView 27 项（新增 4 项：目标表落点/上游依赖/图形化/运行详情）+ 全量 500 全绿 + tsc 0。 | POST /api/v1/lineage/parse 请求新增可选 target_table、响应新增 upstream_deps（向后兼容）；GET /api/v1/lineage/runs/{run_id}（新增）；lineage_ingest_run 新增 detail_json 列（迁移 0050） | released（纯 SELECT 血缘落地 + 图形化 + 运行详情） |
| 2026-08-15 | — | lineage + scripts (DP 血缘 JSON 导入平台 + 大批量运行摘要超长修复) | **把重新解析的 DP 元数据血缘（lineage_out/lineage.json，3685 节点/5215 边，0 SQL 解析失败）导入平台血缘体系（用户诉求：将血缘视图导入平台）**。新增 `backend/scripts/import_lineage_json.py`：读取 lineage.json 提取 (source,target) 边集（去重/跳自环 → 5184 边），复用 `import_dp_lineage` 的 `persist_edges`/`persist_graph` 走 `LineageService.ingest_batch`（provenance=dp_csv，幂等 upsert + mark_seen + 失效观察 + 运行摘要 + Neo4j best-effort）。**顺带修复平台健壮性 bug**：`ingest_batch` 的 detail 快照含全量边明细（5184 条 → 600KB）写入 `lineage_ingest_run.detail_json`（TEXT 64KB）触发 MySQL 1406 导致运行摘要失败、未分批边回滚——修复为只保留前 200 条边示例 + 完整计数（`_DETAIL_EDGE_SAMPLE`），大批量导入不再超长。验证：lineage 66 单测全绿 + ruff 0；实跑导入 run_id=6 success（5184 total / 126 added / 5058 updated / 7952 missing 观察期 / 0 stale，detail_json 30KB 落库）+ Neo4j 写入成功；平台 API 验证 /lineage/channels dp_csv 13057 边/4735 节点/0 失效、/lineage/impact 节点 wedw_dwd.tjhis_all_dic_drug_df 返回 4094 条（Neo4j 路径）✅。 | 无新增接口（新增脚本 import_lineage_json.py；ingest_batch 内部 detail 截断，行为兼容） | released（DP 血缘导入平台） |
| 2026-08-15 | — | lineage + frontend (SQL 血缘解析结果图谱化) | **SQL 血缘解析结果从文本/表格升级为血缘图谱展示（用户诉求：SQL 血缘解析展示的结果效果应是血缘图谱，而非文本描述）**。新增前端 `parseResultToGraphData`：把本次解析的表级边（后端返回 `table:` 前缀节点，label 去前缀展示表名）+ 字段级边（前端拼 `field:表.列` 节点，label 展示 表.列，`SELECT *` 用 `.*` 占位）合并构建 AssetGraph 图数据（同表多边节点去重）；ParseTab 解析成功后统计 Alert 下方以 Card + AssetGraph 直接展示本次血缘图谱（血缘边为 DAG，auto 检测无环走分层布局源→目标自上而下，表节点矩形/字段节点椭圆、标签白底 pill 完整展示），标题含节点/边计数；明细表格保留在下方作辅助（精确列映射与派生表达式）；纯 SELECT 无落点的上游依赖仍以 Tag 清单只读展示（无边的依赖清单非血缘边，语义诚实不伪造图谱）。验证：前端 LineageView 31 项全绿（新增图谱展示 1 项 + parseResultToGraphData 单测 3 项：合并构建/前缀与 label/空结果）+ 全量 506 全绿（1 次间歇性 DataSources 失败单独重跑 22 全绿）+ tsc 0。 | 无（纯前端展示增强，复用既有 parse 响应明细） | released（SQL 血缘解析结果图谱化） |
| 2026-08-15 | — | lineage + scripts (完整血缘合并导入：SQL+同步+DAG) | **把 DP 元数据血缘的**完整**三类边（SQL + 同步 nodeType6/8/10 + DAG parentIds）全部导入平台血缘体系（用户诉求：需要的是完整血缘，同步+SQL+DAG）**。根因：上一轮只导入纯 SQL 边（lineage.json 5184 条），导致旧 dp_csv 通道约 7952 条同步/DAG 边未被确认、进入失效观察期。新增 `backend/scripts/import_full_lineage.py`：取「平台完整解析边集（import_dp_lineage，10553 条：SQL+同步+DAG）∪ 我的强 SQL 解析边集（lineage.json，5184 条，0 失败）」并集 13136 条，同通道（provenance=dp_csv）走 `LineageService.ingest_batch` 全量 mark_seen。验证：实跑 run_id=8 success（13136 total / 0 added / 13136 updated / 0 missing / 0 stale）+ Neo4j 写入成功；库中 dp_csv 13057 条唯一边全部 active、stale=0、missing=0（观察期清零、同步/DAG 边全部恢复）；79 条 merged 差异经核查为**大小写变体**（sqlglot 小写 vs 平台保留大小写，MySQL _ci 排序规则合并为同一行，血缘零丢失，in_merged_not_db_ci=0/in_db_not_merged_ci=0 双向无缺失）；抽查同步边（di_tjhepingfuyou.dic_drug → wedw_dwd.tjhis_dic_drug_df）与 DAG 临时表边（wedw_tmp 3224 条）均真实存在。 | 无新增接口（新增脚本 import_full_lineage.py） | released（完整血缘导入平台） |
| 2026-08-15 | — | collector + frontend (数据源管理三期工业化：采集过滤/quota/治理字段/健康降级/资产概览/列表信号/批量探活调度) | **数据源管理模块三期全部实现（用户诉求：从产品角度把数据源管理打磨到工业级、覆盖实际生产场景；按团队协作方式推进，Workflow 工具因 agent 无法回传 StructuredOutput 失败，Foundation 阶段实际完成，由我接管实施）**。**① 表级采集过滤（P0 生产刚需）**：`data_source` 新增 include_patterns/exclude_patterns（迁移 0052，可逆，fnmatch 风格，include 优先于 exclude）；SPI 新增 `set_table_filter`，MySQL connector `_keep_table` 过滤扫描表，service 采集编排注入（同步/异步/定时全路径生效）——生产库海量临时/系统表不再污染资产目录。**② 健康状态机 DEGRADED（PRD §4.13）**：`_evaluate_health_after_collect` 滑动窗口（20 次采样，超限减半）按成功率 <95% 判 DEGRADED（黄态），写 health_metrics + degraded_since（恢复 healthy 自动清空）；`GET /{id}/health` 透出。**③ quota 可见可配 + 治理字段**：`DataSourceResponse` 返回 quota + `DataSourceUpdateRequest` 支持 quota/owner_id/description/include/exclude（PATCH 语义），service `set_source_governance` 落库。**④ 资产规模概览**：`GET /data-sources/{id}/overview` 聚合实体类型分布/PII 敏感级分布/字段总数（JSON_LENGTH）/漂移数/覆盖率/水位。**⑤ 列表信号**：`list_sources` 批量回填 table_count/pii_count/last_collected_at/drift_count（`list_sources_signals` 一次 IN 聚合避免 N+1）。**⑥ 批量探活/批量调度**：`POST /data-sources/batch-test`（用已存配置逐条 probe，207 语义更新健康）、`POST /data-sources/batch-schedule`（统一 cron，207 语义），均写审计。**前端**：列表加「资产（表/PII/漂移）」+「最近采集」信号列（source_id 并入名称 sub）；详情 Modal 加资产概览卡、采集运行历史、操作审计时间线、DEGRADED 黄态 Alert、跨模块导航（采集任务/血缘图）；编辑 Modal 加治理字段（Owner 选择 listUsers/用途描述/quota 并发与扫描行上限/表级白黑名单）；工具栏加批量探活（Popconfirm）/批量调度（预设 cron 弹窗）。验证：后端 test_collector **153 全绿**（新增 DEGRADED 状态机 4 + 信号回填 1 + 概览 2 + 批量探活 2 + 批量调度 1 + quota/健康透出）+ 全量 unit **2286 passed**（9 失败为并行会话语义/ai/lineage 及权限 403 预存，与本次无关）+ ruff/mypy 0；前端 DataSources **26 全绿**（新增概览/批量探活/批量调度/治理字段 4 项）+ 全量 **510 passed**（1 失败为并行会话 Dashboard 在途）+ tsc 0。 | 迁移 0052（data_source 治理列）；GET /data-sources/{id}/overview、POST /data-sources/batch-test、POST /data-sources/batch-schedule（新增）；DataSourceResponse/UpdateRequest 扩展 | released（数据源管理三期工业化） |
| 2026-08-15 | — | lineage + frontend (影响分析图谱节点点击侧边栏) | **血缘查询/影响分析图谱支持点击节点在侧边栏展示具体信息（用户诉求：类似血缘图谱和资产地图）**。后端 GET /lineage/impact 与 /lineage/edges 分页信封新增 `nodes` 节点基础元数据（新增 `LineageNodeInfo` schema + repository `resolve_node_meta`：metric 查 metric 表（domain/pii/owner）、table 查 db_catalog join data_source（entity_id/domain/PII/Owner，软删过滤）、field 继承所属表业务域（即使表节点未显式出现也能解析）、external/未知兜底类型与 label；服务 `node_meta` 按 id 排序返回；只对当前页 items 的节点计算，响应保持有界）。前端血缘查询/影响分析图谱（AssetGraph）挂 `onNodeClick`——指标节点 → 指标详情抽屉（MetricDetailDrawer）、表节点 → 表详情抽屉（entity_id 直达 getCatalogDetail；未在目录的表提示仅展示血缘关系）、字段节点 → 字段信息抽屉（字段名/所属表/业务域/PII + 查看所属表详情入口）；抽取共享 `TableDetailDrawer`（血缘图谱/影响分析复用，消除重复抽屉 JSX）；`edgesToGraphData` 合并后端节点元数据（entity_id/domain/owner/pii），使影响分析图节点按业务域着色、PII 红色描边（与血缘图谱一致）。验证：后端 lineage 相关 131 项全绿（新增 resolve_node_meta 3 项 + node_meta 1 项 + paginate nodes 断言）+ ruff/format/mypy 0；前端 LineageView 36 项全绿（新增影响分析图谱点击 3 项：指标/表/字段 + edgesToGraphData 合并元数据 2 项）+ 全量 521 全绿 + tsc 0。 | GET /api/v1/lineage/impact 与 /lineage/edges 响应新增 nodes 字段（可选，向后兼容） | released（影响分析图谱节点点击侧边栏） |
| 2026-08-15 | — | conflict (自我冲突防御：check 创建入口剔除"指标与自身比对" + 重复冲突去重 + 同 metric_id 兜底) | **从机制上杜绝自我冲突再导致数据被误删（用户诉求：保证后续不再出现 candidate==existing 的自我冲突情况）**。根因：`/conflicts/check` 曾把候选自身塞进 existing 列表，生成 candidate=existing=sales_e2e_conflicta_day 的自我冲突（CF-CC3B9D2F0C25）；同码冲突虽是「同名不同义」的合法形态，但候选与现有都解析到同一条指标行时即"指标与自身比对"，仲裁联动会把"落败方"（=胜方自身）软删作废，导致胜方被误删（successor 死循环）。**多层防御**：① `ConflictService.check` 创建冲突前经 `resolve_active_metric_id` 解析候选/现有到活动指标行，同一条行剔除并告警（同名不同义的合法形态=候选码未落库，照常检测）；② 同一（候选,现有）对已有 OPEN/NEGOTIATING/ESCALATED 冲突时去重不重复落库（检测仍上报）；③ `detect_conflict` 候选/现有携带同一 metric_id → 直接 None；④ 仲裁 winner==loser 仅标权威不作废（既有，兜底）；precheck 不排除同码条目（同名不同义合法形态，注释固化）。**存量修复**：软删无效自我冲突 CF-CC3B9D2F0C25 + 修正胜方 sales_e2e_conflicta_day 仲裁标记（恢复 canonical/merge/CF-56177120ED5B）+ 清 Redis 缓存；巡检确认无其它自我冲突。验证：conflict+semantic **250 全绿**（新增 check 自我引用 2 + 合法同名保留 1 + 去重 1 + detect 同 id 1 + repository 5）+ ruff 0。 | 无新增接口（check 行为强化：自我引用不再落库、重复冲突不再堆积） | released（自我冲突防御） |
| 2026-08-15 | — | lineage + scripts (L3 指标血缘合并 + sqlglot 通道去重整合) | **把 DP 血缘合并进指标级血缘（L3），并与现有 sqlglot 通道去重整合（用户诉求：这份血缘也合并进指标级血缘 L3，同时与 sqlglot 通道去重整合）**。① L3 指标血缘：新增 `backend/scripts/register_metric_lineage.py` 为存量 7 个有定义的指标按 definition_json 注册 `metric:{code} ↔ table:{t}` 边（provenance=metric_definition，上游 source_tables → table→metric、下游 source_table → metric→table，14 条 L3 边落库，metric: 节点边 0→14）；并接线 metrics.py create/update_metric 端点（新增指标自动注册 L3 边，与审计同事务、失败不阻断主流程，未来指标天然进入血缘体系）。② sqlglot 去重整合：实测 dp_csv 13057 边与 sqlglot 32 边精确+规范化均 0 重叠（两通道独立血缘子集，唯一键 source/target/edge_type/granularity 已保证无重复），血缘图谱/影响分析统一消费全部通道边；新增 `backend/scripts/verify_lineage_consistency.py` 三通道一致性校验（同键重复/规范化重复/metric 节点连通/通道汇总，实跑 ok=true）。验证：/lineage/graph 显示 7 个 metric 节点、影响分析 metric:sales_e2e_gmv_day 返回 14 边；后端 unit 218 全绿（新增 test_metric_l3_lineage 3 项）+ ruff 0。 | 无新增接口（新增脚本 register_metric_lineage.py / verify_lineage_consistency.py；create/update_metric 行为增强自动注册 L3 边） | released（L3 指标血缘合并 + sqlglot 去重整合） |
| 2026-08-15 | — | frontend (采集模块菜单改名 + 采集目录/数据源管理列表列宽自适应 + 用户可拖拽调列宽) | **采集模块菜单栏「数据源」改名「数据源管理」；采集目录与数据源管理两页列表列宽自适应 + 支持用户拖拽调整列宽（用户诉求：菜单应叫数据源管理；两页列间距过大应自适应内容、美观，且能自由左右拖动控制列间距）**。**① 菜单改名**：`Layout.tsx` 数据采集分组下「数据源」→「数据源管理」（key /data-sources 不变，路由不受影响）。**② 列宽自适应**：数据源管理主列表补 `tableLayout="fixed"` + `scroll={{x:"max"}}`，名称列作为弹性列（minWidth 240）吸收剩余宽度（此前 auto 布局名称列按内容撑宽导致与类型列间距过大）；采集目录实体列补 minWidth 260 弹性吸收、数据源列 170→190、字段列 110→130（信息更均衡）；两页均保留 ellipsis + 窄屏才横滚。**③ 用户可拖拽调列宽（核心）**：新建通用组件 `components/ResizableTable.tsx`（零第三方依赖）——`useResizableColumns(columns, storageKey)` 为每列注入 `width`（记忆用户拖拽值）+ `onHeaderCell`（连接 `ResizableTitle` 手柄），`components={{header:{cell:ResizableTitle}}}` 替换表头单元格；拖拽手柄 hover 显示、mousedown→mousemove→mouseup 实时改宽（minWidth 60 兜底、maxWidth 1600），用户调整的列宽持久化到 localStorage（按 storageKey 记忆，下次进入保持），样式惰性注入 `<style>` 不依赖全局 styles.css（避免并行会话冲突）；两页均接入并持久化各自的列宽。验证：前端 tsc 0 + 全量 **526 全绿**（新增 ResizableTable 组件单测 3 项：手柄渲染/拖拽触发 onResize+minWidth 兜底/拖拽后 localStorage 记忆）+ 采集目录 22 项 + 数据源管理 27 项无回归。 | 无新增接口（纯前端：菜单文案 + 通用列宽拖拽组件 + 两页表格配置） | released（采集模块菜单改名 + 列宽自适应 + 拖拽调宽） |
| 2026-08-15 | — | conflict + notify + frontend (仲裁 owner 通知链路修复：改名触发条件 + 落败方废弃/作废定向通知) | **修复：冲突仲裁中指定指标改名或废弃后，对应指标 Owner 收不到通知（用户反馈通知中心未生效）**。① **改名通知永不触发**：根因——前端 ReviewWorkbench 仲裁「保留差异+指定改名」传 `rename_target`（角色 candidate/existing，同名冲突下 code 无法区分），后端 service 已把 rename_target 解析为真实 code 写入 `conflict.decision_json.rename_metric_code`，但 API 层 `arbitrate_conflict` 只判断**原始请求体** `payload.rename_metric_code`（该字段恒为 None）→ `_notify_rename_owner` 永不调用。修复：改从 `conflict.decision_json.rename_metric_code`（service 归一化结果）读取触发改名通知，兼容 rename_target/rename_metric_code 两种传参。② **废弃/作废 owner 不通知**：根因——落败方 PUBLISHED → `deprecate_metric` 发布 `metric.deprecated` 事件走**订阅扇出**（owner 不订阅收不到、payload 无 owner_id）；落败方 DRAFT/REVIEW → 软删作废完全无通知。修复：新增 `_notify_loser_owner`——仲裁后按落败方**实际落库状态**定向通知 owner（IN_APP，不依赖订阅）：DEPRECATED → `metric.deprecated`「指标已废弃（口径仲裁）」、软删 → `metric.voided`「指标已作废（口径仲裁）」；后继=胜方。强韧性保护（胜方未发布未处置）/自我冲突/无 owner/指标缺失 → 不通知。③ 事件类型补齐：后端 `_EVENT_TITLE_CN` 与 `_BUSINESS_EVENT_TYPES` 新增 `metric.voided`（与 metric.rename_required 先例一致），前端 Notifications `EVENT_TYPE_LABEL` 补齐 `metric.rename_required`/`metric.voided` 中文标签。验证：后端 test_conflict_api 11 项新增（编排 6 + 落败方状态判定 5）+ 冲突/通知相关 75 项全绿 + 全量 2266 passed（4 预存失败 test_semantic_cache）+ ruff/mypy 0（conflict.py 自身 0 error）；前端 Notifications 16 项全绿 + tsc 0。通知在仲裁事务 commit 后触发（best-effort 不阻断、不破坏事务原子性）。 | 无新增接口（仲裁行为增强：改名/废弃均定向通知 owner；新增事件 metric.voided 纳入订阅清单与标题映射） | released（仲裁 owner 通知链路修复） |
| 2026-08-15 | — | conflict (§1.5 独立复核 75bfe11) | **独立复核通过（复核方 agent-87566cd5，前台阻塞运行，verdict 通过 0 阻塞项）**：commit 恰 8 文件 +307/-5，无并行会话文件（metrics/semantic/Dashboard/api/types）混入；后端 test_conflict_api 11 项新增 + 冲突/通知相关 75 全绿；ruff All checks passed；mypy conflict.py 自身 0 error（24 个错误全在既有依赖文件）；前端 Notifications 16 全绿 + tsc 在 commit 干净检出下 exit 0（工作区 MetricDetail 4 错为并行会话未提交改动）；契约 contract/doc_sync 均校验通过。走查 a-f 全部通过：改名 code 从 decision_json 读取（兼容 rename_target）、落败方裸 select 含软删判定（DEPRECATED/soft-delete→通知、其他→不通知）、通知在仲裁事务 commit 后触发不破坏事务原子性、metric.voided 对称 metric.rename_required 先例、前端 EVENT_TYPE_LABEL 补齐两标签、/collect 语义映射（TD 并行新增 POST /collect → 后端 data-sources/{id}/collect）。非阻塞观察①：工作区 tsc 失败源为并行会话 MetricDetail 未提交改动，与本次无关；观察②：DEPRECATED 与软删同时成立时优先报 deprecated（语义上软删优先可能更精确，影响极小）。 | — | released（独立复核通过） |
| 2026-08-15 | — | lineage (血缘图谱支持全通道表级血缘 + 来源通道筛选) | **修复血缘图谱看不到 DP 血缘（用户诉求：资产视图和血缘视图没有变化，没有把本地文件解析得到的血缘更新到平台中）**。**根因**：`/lineage/graph` 复用 `AssetMapRepository.graph_from_mysql`（资产视角）——节点只取 db_catalog（采集目录 274 张表）+ 指标，DP 元数据导入的表（如 `wedw_dwd.tjhis_*`）不在采集目录就进不了图；实测血缘边表节点 4756 张与采集目录交集仅 4 张 → 图谱只有 11 节点/11 边，13057 条 dp_csv 表级血缘完全不可见（血缘查询 Tab /lineage/impact 其实能读到，证明数据在库、是图谱构建逻辑排除）。**修复**：① 后端新增 `repo.graph_from_edges`（从 lineage_edge 权威存储直接构建表级血缘：节点=边两端 table/metric/field 去重 + resolve_node_meta 富集目录元数据；命中 db_catalog 带 entity_id/domain/pii/owner，未命中保留基础 label 供搜索定位），`service.query_graph` 新增 `provenance` 参数（dp_csv/sqlglot/metric_definition 指定通道；`all`=全通道；空=采集目录视角兼容原行为），`GET /lineage/graph` 透传；② 前端 LineageView 血缘图谱 Tab 新增来源通道筛选 Select（默认「全部血缘（含 DP/SQL/指标）」），切换即重载——血缘图谱默认即展示完整表级血缘。验证：后端 lineage 38 单测全绿（新增 provenance 走边仓库用例）+ ruff/mypy 0；前端 LineageView tsc 0。 | 新增 /lineage/graph provenance 参数（向后兼容，为空保持原行为） | released（血缘图谱全通道表级血缘） |
| 2026-08-15 | — | lineage (SQL 解析结果全图谱化：纯 SELECT 上游依赖也画成血缘图谱) | **用户诉求：所有 SQL 都展示成图谱，有目标表的写入血缘平台**。当前 A+B 方案下纯 SELECT 无落点只展示上游依赖 Tag 文本（无图谱反馈）。**修复**：新增前端 `upstreamDepsToGraphData`——把纯 SELECT 无落点的 `upstream_deps`（FROM/JOIN 源表 + 读取字段）构建为「本次查询」中心节点 + 外围表/字段节点 + 依赖边指向中心的**上游依赖图谱**（仅展示不写图谱，保留 Alert「未生成血缘边」提示 + Tag 明细）；有落点（SQL 自带 INSERT/CTAS 或填目标表名）仍生成正式血缘并写入平台、展示血缘图谱。至此 ParseTab 三种场景全覆盖：INSERT/CTAS→正式血缘图谱并入库、纯 SELECT+目标表→正式血缘图谱并入库、纯 SELECT 无落点→上游依赖图谱（只读不写）。验证：前端 LineageView 新增上游依赖图谱渲染断言 + upstreamDepsToGraphData 单测 2 项，tsc 0。 | 无新增接口（纯前端展示增强） | released（SQL 解析结果全图谱化） |
| 2026-08-15 | — | collector + frontend (数据源管理列表直显覆盖度/采集模式/累计扫描/采集失败) | **数据源管理列表直显采集关键指标（用户诉求：资产规模概览里的覆盖度、采集模式、累计扫描、采集失败等指标应直接展示在页面表格，而非点击弹窗才能看到，尽可能直观看到数据源情况）**。① **后端**：`list_sources_signals` 批量回填新增 `scanned_count`/`failed_count`（从 `collection_watermark` 聚合，与 table_count/pii_count/drift_count 同一次 IN 查询，避免 N+1）；`DataSourceResponse` 新增 `scanned_count`/`failed_count` 字段，`list_sources` 透传——覆盖度/采集模式原本列表接口已返回（`coverage`/`collection_mode`），仅前端未展示。② **前端**：主列表新增「覆盖度」列（Progress 迷你进度条，0% 灰显、>0 绿显）、「采集模式」列（全量/增量 Tag）、原「资产」列扩展为「资产 / 采集」双行（上行 `N 表 · PII M · 漂移 K`，下行 `累计扫描 X · 失败 Y`，失败 >0 红色告警色）——采集指标全部表格直显，无需点开详情。验证：后端 test_collector **153 全绿** + ruff/mypy 0（collector 自身）；前端 DataSources **28 全绿**（新增「列表直显采集指标」1 项）+ 全量 528/530（2 失败为并行会话 LineageView 方言在途，与本次无关）+ tsc 0；**线上 8180 全链路**：`/data-sources` 返回 mysql_unisense coverage=0 / mode=FULL / **scanned=401** / **failed=0** / table=270 / pii=162，前端 bundle 确认含「覆盖度/累计扫描/采集模式」新列代码。提交 a8ad3c3。 | DataSourceResponse 新增 scanned_count/failed_count（列表批量回填） | released（数据源管理列表直显采集指标） |
| 2026-08-15 | — | lineage (上游依赖图谱层级化：本次查询 → 表 → 字段) | **用户诉求：图谱展示不够有层次感，SQL 解析后直接把本次查询节点与表和字段平铺关联，而不是「本次查询-表-字段」三层**。当前 `upstreamDepsToGraphData` 把所有表/字段节点依赖边统一指向中心（字段也直接连中心），分层布局下无层级。**修复**：改为三层层级结构——① 第一层：本次查询 → FROM/JOIN 源表（`query:本次查询` → `table:xxx`，边方向即血缘 DAG 方向，源在上目标在下）；② 第二层：表 → 所属字段（`table:xxx` → `field:表.列`，字段挂所属表下而非直接连中心），字段所属表未出现在 tables 时**补建表节点**保证层级完整，无表前缀的裸列名（未限定列）直接挂中心；③ 依赖边沿用 READS_FROM 类型 + seenEdges 去重。验证：前端 LineageView 单测全绿（新增「本次查询→表→字段」三层层级 / 字段所属表补建表节点 / 裸列名挂中心 3 项单测，旧「依赖边指向中心」断言改为层级断言）+ tsc 0。注：另清理 vitest 缓存（node_modules/.vite）消除 8 项缓存污染误报失败（与代码无关，干净检出仅剩 2 项并行会话 lineageGraph provenance 断言固有失败）。 | 无新增接口（纯前端展示层级调整） | released（上游依赖图谱层级化） |
| 2026-08-15 | — | frontend (资产地图图谱接入全通道血缘 + AssetGraph 大图 LOD 性能优化) | **用户诉求：①资产地图图谱也要接入完整血缘（DP/SQL/指标）；②血缘图谱边和节点很多时放大缩小很卡很慢，需优化**。**① 资产地图图谱接入血缘**：AssetMap GraphTab 新增「来源」筛选（资产视角（采集目录）/ 全部血缘（含 DP/SQL/指标）/ DP 同步血缘 / SQL 解析血缘 / 指标定义血缘），血缘视角复用 lineageGraph API（provenance + limit 2000，与血缘视图血缘图谱同源），节点点击逻辑（指标详情抽屉 / 表实体抽屉 / 字段抽屉）不变。**② AssetGraph 大图 LOD 性能优化（核心）**：a. 全局 `animation:false`（关闭缩放/平移/状态切换插帧）+ `zoomRange:[0.15,8]`（限制最小缩放避免缩到极小仍全量渲染细节）；b. 新增 compact LOD 状态（隐藏标签/图标/柔光/投影，只留节点主体+边）——监听 `canvas:wheel` + `getZoom()` 跨阈值 0.6 时**批量** `setElementState(record,false)` 切换（阈值节流，避免高频 wheel 重复设置）；c. 节点数 >200 大数据量首帧（fitView 后）即 compact，减轻首帧全量标签/柔光绘制；d. hover 邻域高亮 / 搜索高亮合并 compact 状态（stateWithCompact）避免 setElementState 覆盖丢失紧凑模式。验证：前端 tsc 0 + 全量 536 通过（仅 2 项并行会话 LineageView provenance 固有失败，非本次改动）；AssetMap 新增来源切换 2 项 + AssetGraph 新增 LOD 3 项（大图缩放阈值切 compact/小图不降级）全绿；git commit --only 原子提交 5f21829（4 文件，未混入并行会话改动）。 | 无新增接口（纯前端：AssetMap 来源筛选 + AssetGraph LOD 性能优化） | released（资产地图接入全通道血缘 + 大图性能优化） |

| 2026-08-15 | — | notify (三梯队通知接入全量落地：断链修复 + 定向闭环) | **把权限治理盘点出的「应通知未通知」缺口按三梯队全部排上（用户诉求：直接把三梯队全部排上）**。**① 断链修复（发布无人消费）**：collector/lineage 原发 Redis 裸通道（collector_events/lineage_events 全仓无消费方）→ 双发 EventBus，`catalog_registered`/`catalog_schema_drifted`/`lineage_parsed`/`lineage_ingested` 纳入 `_BUSINESS_EVENT_TYPES` 白名单；`degradation.state_changed`（核心依赖降级）、`conflict_reopened`（冲突重开）原发布无订阅者 → 补白名单。**② 定向通知闭环（notify_user，IN_APP 不依赖订阅）**：账号安全 user.created/status_changed/password_reset→用户本人、org.status_changed→组织成员、采集 catalog.deprecated/collect.degraded/collect.failed/catalog.connection_failed→数据源 Owner、PII pii.review_pending→compliance_officer（collector 低置信度 NEEDS_REVIEW 时）、审批流 metric.submitted→域审核人（domain_admin/reviewer）/approved·rejected→提交人、质量 quality.anomaly 按 notify_targets.owners 定向、授权 grant.expiring_soon→被授权人（迁移 0054 加 grants.expiring_reminded_at 去重列 + governance 扫描 Worker 联动）。**③ 接线同步**：notify/schemas `_ALLOWED_SOURCES` + `_EVENT_TITLE_CN` + `_SOURCE_CN` + 前端 Notifications `EVENT_TYPE_LABEL`/`EVENT_TYPES` 补齐。验证：governance 54（新增 grant.expiring_soon 通知+去重测试）/quality 174/collector 153/notify 29 全绿；审批流定向 helper 四场景独立验证通过（通知审核人排除本人/通知提交人/无提交人不通知/异常吞掉不阻断）；ruff 全绿 + mypy 改动文件 0 error；semantic 测试 8 项失败为并行会话「指标评审人指派」在途改动所致（HEAD 基线 137 全过，与本次无关）。 | 迁移 0054（grants.expiring_reminded_at）；事件白名单扩展 14 项 + 定向通知 12 场景 + 前端标签同步 | released（三梯队通知接入） |
| 2026-08-15 | — | frontend (图谱语义泳道 + 全屏：资产地图/血缘图谱/SQL 解析统一) | **用户诉求：资产图谱和血缘图谱的展示按「指标、表、字段」分层更有层次感，且所有图谱展示都要有可全屏效果**。**① AssetGraph 语义泳道（lanes）**：用隐藏锚点节点 + 锚定边（表锚→指标锚→字段锚，与血缘方向一致——表是血缘源头、指标是加工产物、字段是列级流转）让 dagre 分层把节点按类型聚进三条语义带——表带在上（表→表加工链在带内保留纵向分层）、指标带中、字段带下；开启后有环也强制分层（dagre acyclic 翻转环边 + 环标记橙描边/红虚线），不再整图回退力导向。**② 数仓分层徽标（layerBadges）**：表节点按 ods_/dwd_/dws_/ads_/dm_ 前缀描边着色、指标按 dw_layer，加工链层级一眼可见（图例新增数仓层示意）。**③ 字段折叠**：showFields 控制条可切换（默认沿用父组件设置）。**④ 图谱全屏（fullscreenable）**：控制条「全屏」按钮 → portal fixed overlay 全屏展示（复用同一实例 UI 状态，筛选/搜索/布局不丢失，Esc/按钮退出）。血缘图谱/影响分析/SQL 解析（正式血缘+上游依赖）/资产地图全部接入 lanes。验证：AssetGraph 16 项全绿（新增泳道 3 + 全屏 1 + 字段折叠 1 + layerOf 1 + applyLanes 1）+ 前端全量 542 passed（3 失败为并行会话 MetricResponse reviewer 未同步 + LineageView provenance 固有，与本次无关）+ tsc 0（我的文件零错误）。 | 无新增接口（纯前端 AssetGraph 增强） | released（图谱语义泳道 + 全屏） |

| 2026-08-15 | — | notify (§1.5 独立复核 010094e + 39e851e) | **独立复核通过（复核方 agent-d704dbc8 首轮 + agent-7518ebd0 修复复核，前台阻塞运行）**：010094e（18 文件 +1027/-20）门禁——notify 29/governance 54/users+org+lineage 79/collector 153/quality 21 全绿；ruff 首轮 1 处 E501 阻塞；mypy 目标文件 0 error；前端 Notifications 16 全绿 + tsc 报错全在并行会话 types.ts reviewer 字段（非本提交）；契约 contract/doc_sync 通过。首轮 verdict 不通过（2 阻塞项）：① collector/service.py _notify_pii_review_pending body 超长 E501；② catalog_deprecated/collect_degraded EventBus 双发用下划线名与白名单/标题点号名不一致（双发对 notify 消费无效）。**修复提交 39e851e**（1 文件 +6/-3）：双发事件名统一点号 catalog.deprecated/collect.degraded（与白名单/标题/定向通知三处一致，EventBus 精确匹配可命中）+ body 换行。修复复核：ruff All checks passed + collector 153 全绿 + contract 通过 + notify 契约 17 全绿，**2 阻塞项均解决，verdict 通过**。走查确认：白名单 16 项新增全有标题无幽灵；users/org 定向通知均在业务 commit 后调用无事务风险；grant.expiring_soon 口径（ACTIVE+7 天窗口+未提醒+非软删）+ mark_expiring_reminded 去重正确；quality notify_targets.owners 解析正确；前端 EVENT_TYPE_LABEL 与后端标题 16 项一一对应。非阻塞观察：conflict_reopened 白名单暂 inert（发布仍走失效 HTTP 通道，需迁移 _safe_publish 到 EventBus 才真正闭环）；semantic 审批流定向（metric.submitted→审核人/approved·rejected→提交人）已实现但暂缓提交（与并行会话 reviewer 指派改同一文件，待其提交后补）；迁移 0054（grants.expiring_reminded_at）暂缓（依赖并行会话未入库的 0053）。 | — | released（独立复核通过） |
| 2026-08-15 | — | conflict + notify (冲突重开定向通知闭环) | **冲突重开(CLOSED→OPEN)后 Owner 未收到任何通知（用户诉求：仲裁改名/废弃已有通知，重开也应让相关方感知）**。先澄清事实：`_safe_publish` 已在 5ef46cc 双发 EventBus（L88-93 先 `_publish_event`→EventBus、L94-97 才 legacy HTTP），`conflict_reopened` 已在白名单（main.py:231）+ 标题映射（notify L93「口径冲突已重开」）——独立复核「conflict_reopened 仅走 HTTP 通道」系误判，迁移到 EventBus 已闭环于 5ef46cc。真正缺口：`publish_event` 只按**订阅扇出**（list_enabled_subscriptions），双方 Owner 不订阅就收不到——重开是强相关场景，不应依赖手动订阅。**修复**：conflict/api.py 新增 `_notify_reopen_owners`——reopen 事务 commit 后从 `conflict.metric_codes` 取 candidate/existing，查双方指标 Owner，各发一条 `conflict_reopened`（IN_APP 定向，`NotifyService.notify_user`，不依赖订阅偏好），对称 rename/loser 定向范式；owner 缺失/指标缺失/metric_codes 空 → 跳过该侧，异常 best-effort 不阻断 reopen。验证：test_conflict_api 15 passed（新增 TestNotifyReopenOwners 4 项：双方都通知/无 owner 跳过/指标缺失不通知/metric_codes 空不通知）+ service 层 reopen 7 项全绿 + ruff 0 + mypy 0（conflict.py 自身 0 error）。 | _notify_reopen_owners（IN_APP 定向 conflict_reopened） | released（冲突重开定向通知） |
| 2026-08-15 | — | frontend (血缘度径向布局 + 力导向大图边降采样) | **用户诉求：资产地图力导向布局在边/节点多时展示混乱，无法看清相互引用关系**。**① 血缘度径向布局（layout=radial）**：G6 concentric 同心圆 + `sortBy: degree`——按依赖引用数排序，引用数高的枢纽节点居中、低引用数的叶子节点排外围，环间等距 + preventOverlap 防重叠，让「被谁引用/引用谁」的层次关系一目了然（布局下拉新增「血缘度径向」项）。**② 力导向大图边降采样**：force 布局下边数超过阈值时，按「两端血缘度和」降序保留枢纽边（默认 600 条），叶子间低价值边折叠（悬停邻域高亮仍可临时显现被折叠边），缓解力导向大图边交叉成网。验证：AssetGraph 18 项全绿（新增 radial 配置 + 力导向大图降采样 2 项）+ tsc 0；LineageView 2 项失败为预存（stash 验证与本提交无关）。 | 无新增接口（纯前端 AssetGraph 布局增强） | released（血缘度径向 + 大图降采样） |

| 2026-08-15 | — | frontend (通知中心「我的通知」信息展示增强) | **用户诉求：通知中心「我的通知」展示信息不够丰富，无法直观知道通知含义**。纯前端增强（后端 payload 已含业务数据，无后端风险）：① **payload 业务字段合并展示**——parseNotifyBodyFields 重写：body 优先（已是「标签：值」多行或自然语言），payload 中未在 body 展示的业务字段（entity_name/org_name/source_id/username/successor_code/candidate/existing 等定向通知补充信息）不再因 body 非空被丢弃，去重（body 已含字段不重复）+ 跳过元数据键（event_type/payload/source/note）；② **字段标签与值中文化扩充**——PAYLOAD_FIELD_LABEL_FE 新增 18 个业务标签（账号/组织名称/数据源/实体名称/表名/后继指标/候选指标/现有指标/严重级别/统计周期/源表/目标表/敏感字段/审核人等），STATUS_CN_FE 状态值中文化（active→启用/disabled→禁用/suspended→停用/deleted→已删除），severity 映射严重级别；③ **事件类型徽标**——title 为自然语言正文时（如「您的账号 admin 已被重置密码」）卡片标题旁补 template_code 类型 Tag（如「密码已重置」），title 本身就是类型名时不重复显示；④ **深链扩展**——user.*→/users、org.status_changed→/organizations、collect.*/catalog.*→/data-sources、grant.*/pii.*/classification.*→/governance，原 metric/conflict 保留，其余优雅降级提示。验证：Notifications.test 23 passed（新增 7 项：payload 合并展示/body 去重不重复/类型徽标自然语言标题/账号跳转/组织跳转/采集跳转/授权跳转）+ tsc 0。
| 2026-08-15 | — | frontend (节点依赖引用数角标 + 血缘度筛选) | **用户诉求：资产地图/血缘图谱每个节点显示依赖引用数，便于查看与后续筛选**。AssetGraph 增强：① 节点右上角 badge 角标显示血缘度数字（依赖引用数），高血缘度≥5 橙/中≥2 蓝/低灰蓝着色，一眼识别枢纽节点；② 悬停节点 tooltip 显示「依赖 N 项（上游）/被 M 项引用（下游）/血缘度 N」明细（按边方向拆 inDegree/outDegree）；③ 工具栏新增「依赖≥N」筛选下拉（0/1/2/3/5/10），过滤低价值叶子聚焦枢纽；④ 图例补充「右上角数字=依赖引用数」说明。性能保护：badge 纳入 compact LOD 隐藏（badgeOpacity:0），大图缩放不增额外绘制开销。验证：AssetGraph 19 项全绿（新增血缘度筛选用例）+ tsc 0；部署 JS 实测 2110 节点缩放 0 Violation。 | 无新增接口（纯前端 AssetGraph 增强） | released（节点依赖引用数角标 + 血缘度筛选） |

| 2026-08-15 | — | notify (§1.5 独立复核 71b9570) | **独立复核通过（复核方 agent-83774948，前台阻塞运行）**：71b9570（3 文件 +181/-17）门禁——前端 vitest Notifications 23 passed（含新增 7 项）、tsc exit 0、契约 doc_sync 通过、commit 确认无并行文件混入。走查：parseNotifyBodyFields 重写（body 优先 + payload 业务字段合并 + seenLabels 去重 + 跳过 event_type/payload/source/note）正确；PAYLOAD_FIELD_LABEL_FE 新增 18 个业务标签语义准确；STATUS_CN_FE 状态中文化 + severity 映射正确；事件类型徽标条件（title 为类型名时不重复显示）正确且不破坏旧测试；深链 4 条路径均在 App.tsx 注册、metric/conflict 保留；测试 7 项与实现逐条吻合。非阻塞观察：① 字段标签实际 18 个（doc 原写 20，已修正）；② /users、/organizations、/governance 深链套 RequireRole ADMIN_ROLES，普通用户（本人/组织成员）点击会被拦截——产品语义待确认；③ source_id 标签「数据源」改「数据源ID」更精确（已修正 + 测试同步）。

| 2026-08-15 | governance | 细粒度权限管控 + 自定义角色 + 个人中心（提交 0bc6697 后端 + 前端待提交） | ① 自定义角色（方案 A）：user.role/role.name Enum→String(32)（迁移 0056）+ role.is_custom；POST/DELETE /roles 支持自定义角色 CRUD（内置名保留/占用拒绝）；require_roles 对 ALL_ROLES 读门禁放行自定义角色。② 按钮级动作点注册表：policy.UI_ACTION_REGISTRY（约 60 个 模块:功能）+ ROLE_UI_ACTIONS 默认基线 + GET /roles/action-registry 可视化配置；role_permission 表可同时覆盖资源动词与 UI 权限点（正交，PDP 不受污染）。③ 前端权限基建：usePermission hook + RequirePerm 路由守卫 + Layout 菜单按权限点过滤；Governance RolesTab 重写为动作点矩阵可视化配置 + 自定义角色创建/删除；UserManagement 角色下拉含自定义角色。④ 个人中心（方案 C）：/account 页（我的账号/权限/授权/改密）+ auth /me 回填 org_name/domain_name + 通知深链 user.*/org.*/grant.*→/account。验证：后端 110+ 单测/ruff/mypy 0；前端 Account/DataGovernance/UserManagement/Layout/Notifications 全绿，全量 566 中仅 2 项 LineageView 并行会话预存失败，tsc 0（并行 FeedbackCenter 在途除外） |

| 2026-08-15 | governance (§1.5 独立复核 0bc6697+4a9b923) | **独立复核通过（复核方 agent-0faeb46e，修复后）**：细粒度权限管控 + 自定义角色 + 个人中心。门禁复跑——后端 128 项 pytest 全绿 / ruff All checks passed / mypy 目标文件 0 error；前端 5 文件 51 项 vitest 全绿；contract + doc_sync 通过。走查 a-e 全部符合（require_roles 仅 ALL_ROLES 读门禁放行自定义角色、UI 动作点与 PDP 资源动词正交、load_ui_role_actions 独立合并、usePermission fail-open 后端兜底、RolesTab 可视化配置 + /account 个人中心 + 通知深链分流）。首轮发现 1 阻塞项（4a9b923 误带入并行在途 upsertSubscription 未使用导入致提交态 tsc TS6133）——已由 e5bbf99 修复（git add -p 仅移除误带行，并行 +32 行测试保留工作区待其提交），修复后提交态 tsc EXIT=0 验证通过。非阻塞观察：UI_ACTION_REGISTRY 73 项（规格约 60）、主工作区 vitest 1 项为并行在途新增测试非本次、0bc6697 与 4a9b923 之间隔 4 个并行提交属正常祖先链。 |

| 2026-08-15 | observability | 反馈模块数据丰富度增强（迁移 0057，提交 d958949/976fd10/待提交） | ① 反馈新字段：category（bug/feature/improvement/question/praise，默认 improvement）+ priority（high/medium/low，默认 medium）+ source_url（来源页面自动捕获）——feedback 表 3 列（迁移 0057），FeedbackCreate/Response 透传（非法值回退默认容错），submit_feedback 落库。② 前端展示术语化：用户/处理人 ID→用户名、对象类型英文→中文、对象ID→指标名（可点击跳转详情）、内容 Tooltip 完整展示。③ 列表新增「分类/优先级/处理时效」列（处理时效=created_at→resolved_at 中文耗时，运营 SLA 视角）；提交表单新增分类/优先级选择 + 自动捕获来源 URL。④ 反馈处理通知提交者：feedback.status_updated 事件携带 recipient_user_id，notify 定向投递（订阅去重、in_app）。验证：后端 observability+notify 38 项全绿；前端 FeedbackCenter 5 项全绿（含新列断言）；tsc 0。影响接口：POST /observability/feedback 请求体新增可选 category/priority/source_url；GET /feedback 响应新增三字段。 |
| 2026-08-15 | tracking+frontend | 埋点字段长度钳制根治 422 | **修复：埋点上报偶发 422（用户反馈 POST /api/v1/tracking/event Unprocessable Entity）**。根因：血缘查询/影响分析的 track 调用把用户输入节点 id 作为 target_id，后端 TrackEventRequest 约束 target_id max_length=36，超长（如 table:库.表 拼接后 >36）触发 pydantic 校验失败 422；实测 36 以内 200、>36 即 422。修复：前端 TrackingProvider 发送前按后端 schema/DB 列长度截断（event_type≤32、target_id≤36、target_type≤32），埋点 fire-and-forget 统计不应因输入长而失败；覆盖全部 track 调用点。验证：TrackingProvider 单测 6 项全绿（新增钳制 3 项）+ tsc 0；端到端实测影响分析输入 67 字符节点 id → tracking/event 全 200 无 422；提交 b5cc0f5。 | 无（纯前端容错） | released（埋点 422 根治） |
| 2026-08-15 | — | frontend (通知中心新增订阅支持多选消息类型) | **用户诉求：通知中心设置新增订阅时，要支持多选消息类型**。SubscriptionsTab 的「消息类型」从单选 Select 改为 **Select mode=multiple**（placeholder「可多选消息类型」，optionFilterProp 支持搜索，34 个事件类型全可选）；handleCreate 对多选 event_type 数组**循环调用 upsertSubscription**（每事件一行，后端 `(user_id, channel, event_type)` 幂等 upsert 天然支持同渠道多行），保存反馈「已保存 N 个订阅」；表单校验至少选 1 个。纯前端改动（后端 SubscriptionPref 模型已支持多行）。验证：Notifications 24 项全绿（新增多选 2 事件→各建一条订阅用例）+ tsc 0；前端全量 565 通过（2 失败为 LineageView provenance 预存）。注：Notifications.tsx 改动随并行会话提交 4a9b923 一并进入 HEAD。 | 无新增接口（纯前端） | released（订阅多选消息类型） |
| 2026-08-15 | — | governance + frontend (权限管控工业级加固) | **对细粒度权限管控模块做工业级加固（用户诉求：做深夯实，确保工业级）**。① **注册表完整性自动化防护**（test_governance_policy.py 新增 7 项）：断言 UI_ACTION_REGISTRY 键格式合法（模块:功能 单冒号全小写）/无重复/无占位残留/无空元数据、ROLE_UI_ACTIONS 引用全部真实存在（防悬空）、每个权限点至少被一个内置角色覆盖（无孤儿）、is_ui_action 与注册表一致、规模下限 10 防塌缩。② **清除占位脏代码**：移除 templates:view_extra 占位定义与 pop 清理逻辑（注册表纯净，73 个真实权限点）。③ **前后端权限点一致性守卫**（contract_check.py 新增 check_ui_action_consistency，纳入 contract 门禁）：扫描前端 can()/canAny()/canAll() 调用段与 ROUTE_PERM 字典值，断言引用的每个 模块:功能 权限点都在后端注册表（防拼写漂移/悬空权限点——前端引用未注册权限点将恒 false 且无提示）；正则仅在权限点调用上下文匹配，不误抓事件名(canvas:wheel)/数据键(table:orders)/localStorage 键(unisense:*-col-widths)；canAny 数组多值、ROUTE_PERM 键值对均正确提取；负向验证注入 ghost:perm 正确捕获失败。当前前端 32 个引用全部注册、0 漂移。④ **Governance 组件级 Tab 过滤**（方案 C：按组件管控）：6 个 Tab 挂权限点（授权管理→grant:create/revoke/export 任一、角色管理→role:*/、PII 复核→pii:review/validate/classification:rescan、数据擦除→erasure:execute），无权限 Tab 隐藏；我的权限/权限检查对全部治理访问者开放。验证：test_governance_policy 37 全绿（新增 7）+ test_governance_service 62 全绿 + ruff 0；前端 DataGovernance 10 全绿（新增 Tab 过滤 4：无权限只见我的权限/权限检查、有授权见授权管理、合规官见 PII 复核、擦除权见数据擦除）+ 相关 45 全绿 + tsc 0；contract 校验通过（permission 一致性 32 引用 0 漂移）。 | UI_ACTION_REGISTRY 完整性测试 + check_ui_action_consistency 契约守卫 + Governance Tab 权限过滤 | released（权限管控工业级加固） |
| 2026-08-15 | — | governance + frontend (独立复核) | §1.5 独立复核（commit 8e1fd48，复核方独立复核 agent，前台阻塞运行）：8 道门禁独立复跑——pytest governance 3 文件 124 passed / ruff All checks passed / 前端 3 文件 45 passed / tsc 0 error / contract（TD 78 路径 命中27/映射35/豁免16/未命中0）+ permission 一致性 32 引用 0 漂移 / doc_sync 通过 / 注册表静态校验 73 键格式合法无重复无悬空无占位残留 / 守卫负向验证注入 ghost:perm 正确捕获。走查 a-e 全部符合：注册表完整性测试 7 项覆盖格式/元数据/无占位/无悬空/无孤儿/正交/规模下限；占位 templates:view_extra 移除后 73 键纯净 ROLE_UI_ACTIONS 不受影响；一致性守卫正则仅在权限点上下文匹配（can()/canAny() 段 + ROUTE_PERM 字典）不误抓事件名/localStorage 键，canAny 数组多值正确提取；Governance 6 Tab 挂权限点按任一收敛、我的权限/权限检查全开放；Tab 过滤测试 4 项覆盖各分支。结论：复核通过，0 阻塞项。非阻塞观察：① usePermission fail-open（快照 null 时 can=true）依赖后端 require_roles 兜底，建议后续对敏感写操作加快照未加载降级禁用；② 前端仅用 33 个 view 权限点，功能级权限点（metric:approve 等）页面按钮接线属渐进式扩展（后端已强制安全边界成立）；③ 一致性守卫已支持 canAny 数组形式（未来使用即受保护）。 | — | verified（工业级加固独立复核通过） |
| 2026-08-15 | governance + frontend (按钮级权限点全面接入 MetricDetail/MetricCatalog) | **推进复核观察②：把按钮级权限点全面接入指标页面按钮（用户诉求：继续推进观察 2）**。① **MetricDetail**：新增 usePermission 接线——提交评审→metric:create、正式/灰度发布→metric:approve、PII 合规复核→pii:review、紧急发布→metric:emergency-publish、废弃→metric:deprecate、全量发布/回滚/去改名→metric:edit（保留 isOwnerOrAdmin 的 owner 归属维度，写操作最终由后端 PDP 归属校验强制）；PII 复核条件重构为 canPii && piiUnreviewed（原 isAdmin && pii_flag）。② **MetricCatalog**：批量操作按钮接 canBatchManage = metric:create/approve/deprecate 任一，无权限则禁用（原仅 selected.length 判断）。③ **后端 policy.py 修正前后端一致**：domain_admin 的 ROLE_UI_ACTIONS 补 pii:review——对齐后端 _PII_REVIEW_ROLES=PA/DA/compliance_officer，避免 DA 在指标详情页看不到 PII 复核按钮（原 isAdmin 含 DA、权限点映射缺 DA）。验证：后端 test_governance_policy 37 全绿 + ruff 0；前端 MetricDetail 15 全绿（新增 5 项按钮权限过滤：有/无 metric:approve 显示/隐藏正式·灰度发布、无 metric:deprecate 隐藏废弃、有/无 pii:review 显示/隐藏 PII 合规复核）+ MetricCatalog 22 全绿（新增 2 项批量按钮权限过滤）+ 相关 45 全绿 + 前端全量 574 通过（2 失败为 LineageView provenance 预存，并行资产线）+ tsc 0。 | metric:approve/pii:review 等按钮级权限点接入页面 | released（按钮级权限点全面接线） |
| 2026-08-15 | governance + frontend (独立复核) | §1.5 独立复核（commit c90055b，复核方 agent-0a8c4d7b，前台阻塞运行）：门禁复跑——git show 恰 6 文件 +164/-12 无并行混入 / pytest test_governance_policy 37 passed / ruff All checks passed / vitest MetricDetail+MetricCatalog 35 passed / tsc 0 error。走查 a-e 全部通过：按钮权限点与后端 require_roles 逐一对齐（metric:create↔_WRITE_ROLES、metric:approve↔_REVIEW_ROLES、pii:review↔_PII_REVIEW_ROLES、metric:emergency-publish↔PA/DA、metric:deprecate/edit↔_WRITE_ROLES）；domain_admin 补 pii:review 后 DA 的 PII 复核按钮仍可见且 compliance_officer 新增可见；canBatchManage=create/approve/deprecate 任一使 reviewer/metric_owner/DA 可用、viewer 禁用；isOwnerOrAdmin 双条件同限 PA/DA/owner 仅收窄不放宽、归属由后端 PDP 强制。结论：复核通过，0 阻塞项。非阻塞观察①（CHANGELOG 声称 compliance_officer PII 复核测试但提交仅 3 项）——已补 PII 复核按钮显示/隐藏 2 项测试并修正 CHANGELOG（MetricDetail 15 全绿）。非阻塞观察②：MetricCatalog 未直测 owner/DA 组合（权限点 OR 逻辑 + 契约守卫兜底，风险低）。 | — | verified（按钮级权限点接线独立复核通过） |
| 2026-08-16 | governance + frontend (MetricCatalog owner/DA 组合测试) | **处理复核观察②：MetricCatalog 批量按钮补 owner/DA 权限组合测试（用户诉求：把 2 个非阻塞项也处理掉）**。观察①（CHANGELOG 声称 compliance_officer PII 复核测试但提交未覆盖 PII 路径）已在 6c1f937 闭环（补 PII 复核显示/隐藏 2 项测试 + 修正 CHANGELOG，MetricDetail 15 全绿）。观察②（MetricCatalog 批量按钮仅测空权限/approve 两例、未直测 owner/DA 组合）本次闭环：MetricCatalog.test.tsx 新增 2 项——① metric_owner 组合（metric:create+deprecate+edit，无 approve）批量按钮可用（验证 canBatchManage=create/approve/deprecate 任一 的 OR 逻辑对 owner 正确放行，owner 可批量提交审核/删除草稿）；② domain_admin 组合（create+approve+deprecate+edit 全量）批量按钮可用。验证：MetricCatalog 24 全绿（新增 2）+ MetricDetail 15 + DataGovernance 10 = 相关 49 全绿 + tsc 0。 | 无新增接口（纯测试补强） | released（owner/DA 组合测试闭环） |
| 2026-08-16 | governance + frontend (独立复核) | §1.5 独立复核（commit 6395072，复核方 agent-b08d9e12，前台阻塞运行）：恰 2 文件 +29（MetricCatalog.test.tsx +28、CHANGELOG +1）无并行血缘线混入；vitest MetricCatalog 24 passed / tsc 0 error / doc_sync 通过。走查：owner 组合（create+deprecate+edit 无 approve）与 DA 组合（全量）均与 canBatchManage=create/approve/deprecate 任一 的 OR 逻辑一致，且 ui_actions 构造与后端 ROLE_UI_ACTIONS 真实角色基线逐点对齐（metric_owner 无 approve、domain_admin 全量）。结论：复核通过，0 阻塞项。非阻塞观察：① 用例标题措辞（数组含 edit 不参与 canBatchManage，不稀释断言）；② 仅测可用正向分支，负向由既有空权限用例兜底（仅 metric:edit→禁用 边界未直测，风险低）。 | — | verified（owner/DA 组合测试独立复核通过） |
| 2026-08-16 | lineage + frontend (Hive 子查询别名血缘修复) | **修复 Hive 多表 JOIN + 子查询血缘解析错误（用户诉求：Hive SQL 解析不对，临时表名 t2 混入血缘、字段挂错表）**。① 后端 `extract_upstream_deps` 字段清单收窄为 SELECT 投影列（经 build_scope + _resolve_projection 递归解析到真实来源表），不再收集 ON/WHERE/GROUP BY 条件列——消除子查询别名（t2.hospital_id）泄漏为伪表/伪字段（前端曾拆成 table:t2 节点补建进图谱）；② 前端 `upstreamDepsToGraphData` 拆分「表.列」改用 `lastIndexOf('.')`（此前取第一个点把带库名表 wedw_dw.wy_zh_hospital_std_df.hosp_id 拆成表 wedw 致字段挂错表）。验证：后端 parser 35 项全绿（新增 Hive 子查询别名不泄漏 + 投影穿透子查询 2 项）+ ruff/format/mypy 0；前端 LineageView 47 项全绿（新增带库名拆分 1 项）+ 全量 588 全绿 + tsc 0。 | 无新增接口（parser 行为修复 + 前端拆分逻辑修复） | released |
| 2026-08-16 | lineage (全数据源方言 CTE 引用泄漏修复) | **排查七种数据源方言（MySQL/PostgreSQL/Hive/Spark/Doris/ClickHouse/StarRocks）血缘解析的子查询别名泄漏与表名归属**（用户诉求：检查其他数据源类型是否也有同样问题）。确认子查询别名（t2）泄漏已在 2340ac2 全部方言消除；**新发现 CTE 引用泄漏**：`WITH cte1 AS (...) ... FROM cte1` 中 `cte1` 被 `find_all(exp.Table)` 收集为伪源表，七种方言 × 三条血缘路径（表级 INSERT/CTAS 正式血缘、纯 SELECT 显式落点、上游依赖）全部受影响，正式血缘图会凭空出现 `cte1` 幽灵表节点。修复：`_table_edges`/`_select_table_edges`/`extract_upstream_deps` 收集源表时排除 `_collect_ctes` 返回的 CTE 名（`tbl.name not in cte_map`），CTE 定义内部真实表不受影响；前端 `upstreamDepsToGraphData` 补三层表名（catalog.schema.table）拆分测试（lastIndexOf 已正确）。验证：后端 parser 39 项全绿（新增 CTE 引用不泄漏：上游依赖 hive + 七方言参数化 + 表级 INSERT+CTE + SELECT 落点+CTE 4 项）+ lineage 相关 133 项全绿 + ruff/format/mypy 0；前端 LineageView 48 项全绿（新增三层表名拆分 1 项）+ 全量 589 全绿 + tsc 0。 | 无新增接口（parser 行为修复） | released |
| 2026-08-16 | lineage (生产场景语法覆盖核查修复) | **核查血缘分析对 7 种数据源方言（MySQL/PostgreSQL/Hive/Spark/Doris/ClickHouse/StarRocks）生产高频 SQL 语法的覆盖，修复 6 类缺口**（用户诉求：从实际生产场景出发核查还有哪些 SQL 语法没覆盖到）。① **INSERT 显式列清单字段映射错位**——`INSERT INTO t (a,b) SELECT x,y` 目标列按位置取列清单（x→a/y→b），此前取投影别名错位为 x→x（字段血缘 target 列名错误，影响分析查 target.a 查不到，全方言）；② **MySQL REPLACE INTO ... SELECT**——sqlglot 30.x 不支持降级 Command 致血缘全丢，预处理 REPLACE→INSERT（血缘语义等价）；③ **Doris/StarRocks INSERT ... WITH LABEL**——sqlglot 解析异常，剥离 LABEL 片段；④ **Hive INSERT OVERWRITE DIRECTORY**——目标非表（Directory），此前字段级产 `src.id→.id` 空目标脏边，现返回空且不回退纯查询落点；⑤ **CREATE VIEW / OR REPLACE / MATERIALIZED / TEMPORARY VIEW**——TD§12.2 明确视图作逻辑表产血缘，`_find_target`/`_find_source_query` 支持 kind=VIEW（此前全丢）；⑥ **多表 UPDATE 目标判定**——`UPDATE t1 JOIN t2 SET t2.v=...` 目标取 SET 被更新列所属表（此前误判首表 t1）+ `extract_upstream_deps` 排除写入目标表（INSERT VALUES 不再把 tgt 误收为来源）。验证：后端 parser 49 项全绿（新增 10 项：列清单映射/全方言参数化/UNION+列清单/REPLACE/WITH LABEL/DIRECTORY/视图变体/多表 UPDATE/upstream_deps 排除）+ lineage 相关 152 项全绿 + 全量 unit 2383 passed（5 预存失败 test_semantic_cache 与基线一致）+ ruff/format/mypy 0。 | 无新增接口（parser 行为修复 + 方言预处理） | released |
| 2026-08-16 | governance + frontend (团队管理方案B + 授权治理增强) | **用户诉求：编辑用户所属域改所属团队 + 团队管理 + 重置密码自动生成 + 组织管理改为团队管理与权限管控关联；权限治理授权治理补管理用户按钮/表格自适应可拖拽/指标白名单改选项框；核查角色/PII/权限检查/数据擦除交互（能选择不手输）**。① 团队管理（方案 B，用户拍板：团队不强制绑定业务域）：组织（租户）改称「团队」，organization 表新增 domain 列（迁移 0059，可空=不限域）；团队创建/编辑可绑定业务域（校验 active 主题域）；**团队绑定域变更实时传播全部成员 user.domain**（权限域隔离保持一致）；用户创建/编辑「所属域 + 所属组织」合并为一个「所属团队」下拉（域由团队自动继承：org.domain or 兜底，后端解析 + 校验），用户列表回填团队名（org_name）；组织管理页改「团队管理」并支持绑定域。② 重置密码支持自动生成随机密码（crypto 强密码）+ 重置成功后一次性明文展示（强制首登改密，后端已有）。③ 授权治理增强：授权表格用户列显示用户名（user_id→username 映射）+ 列宽可拖拽（自研 ResizableTitle 无依赖）+ 新增「管理用户」跳转按钮 + 指标白名单列展示；新建/批量授权弹窗指标白名单改为**已发布指标多选框**（替代手动逗号输入）。④ 交互核查：PII 复核指标编码→已发布指标选项框、PII 字段→常见敏感字段多选框；权限检查域→主题域选项框、指标→已发布指标选项框；数据擦除数据主体用户 ID→用户选择框。另修复 GET /roles/action-registry 405（后端路由 `/action-registry` → `/roles/action-registry` 与前端一致）。验证：后端 users/org 43 全绿（新增团队继承域/域传播 4 项）+ deps/governance 72 + 迁移链唯一 head 0059；前端 UserManagement 12（新增团队选择/重置密码生成）+ DataGovernance 11（新增授权管理 1）+ 相关 78 全绿 + tsc 0。 | 后端：GET/POST/PATCH /organizations 支持 domain；GET /roles/action-registry 路由修正。 | released
| 2026-08-16 | governance + frontend (独立复核) | §1.5 独立复核（复核方 agent-c5e8a00c，前台只读，0 阻塞项）：覆盖 5 提交（aaa0029/5c23609/3e954cf/bc3aac1/9e947a0，中间被并行血缘提交 85ddc3f/ba9d7fe/b66c51d 插入链）。后端 5 文件 pytest 152 passed + ruff All checks passed + 迁移链唯一 head 0059_organization_domain；前端 4 文件 vitest 56 passed + tsc 0 error。走查 a-d 全符合：organizations 域变更传播在 commit 前 + 审计 domain_changed；users 域解析团队优先 + 列表批量回填 org_name；GrantsTab 可拖拽列宽/白名单 Select 提交数组/用户名列/管理用户跳转；PII/Check/Erasure 手动输入改选项框。3e954cf 的 api.ts/types.ts 仅含 org 团队字段、无血缘改动混入。结论：复核通过，0 阻塞项。 | — | verified（团队管理方案B + 授权治理增强独立复核通过） |
| 2026-08-16 | backend (lineage parser) | **用户诉求：再次核查不同方言的血缘分析还有哪些 SQL 语法未覆盖，结论要有真实证据**。第二轮真实环境七方言生产语法扩展矩阵（40+ 场景）发现并修复 4 类缺口：① MERGE 字段级血缘整体失效（sqlglot 双版本兼容——25.x 的 WHEN 分支存于 ast.expressions、30.x 存于 args["whens"]（Whens 容器），修复按版本择一遍历（whens 缺失回退 expressions）+ 支持无列清单 INSERT VALUES（this=None 用值中裸列引用列名近似目标列）；本地 venv 此前误装 sqlglot 30.16，已降级对齐生产 25.34.1（requirements pin）并在 25.x 下重新验证全部修复）；② SELECT INTO 血缘全丢（PG/SQL Server 建表式赋值等价 CTAS，_find_target/_find_source_query 识别 Into.this 为 Table 的目标，排除 MySQL SELECT INTO @var 变量赋值避免误判/脏边）；③ UPDATE 字段级血缘整体未实现（build_scope 不支持 Update，新增 _extract_update_edges 手动解析 PG UPDATE...FROM / MySQL UPDATE JOIN / SET 子查询值三类来源，自引用/静态值不产跨表边）；④ CTAS 带列清单字段映射错位（CREATE TABLE t (a,b) AS SELECT x,y 目标列按位置取列清单 x→a/y→b，_insert_column_list 扩展支持 Create Schema）。验证：parser 58 项全绿（新增 9 项）+ lineage 相关 157 项全绿 + 全量 unit 2392 passed（5 预存失败与基线一致）+ ruff/format/mypy 0。 | 后端 parser.py（MERGE/SELECT INTO/UPDATE/CTAS 列清单）+ test_lineage_parser.py | released
| 2026-08-16 | backend (lineage parser) | **用户诉求：再次核查不同方言的血缘分析还有哪些 SQL 语法未覆盖，结论要有真实证据**。第三轮真实环境七方言生产语法扩展矩阵（60+ 场景）发现并修复 3 类缺口：① UPDATE/MERGE 带 CTE 前缀时 CTE 引用被误判为伪源表（_collect_ctes 双版本兼容——25.x 的 Update/Merge 无 ctes property，补从 With.expressions 取 CTE；_extract_update_edges 排除 CTE 引用并穿透 CTE 定义解析到真实表，如 WITH s AS (...) UPDATE tgt SET v=s.v FROM s → 字段边 ods_src.v→tgt.v、无 s 伪表）；② CREATE TABLE ... LIKE 结构复制（无数据流转）被误产血缘边 ods_src→dws_tgt（CREATE 无 SELECT 源不产边）；③ extract_upstream_deps 对非 SELECT 语句（ALTER/DROP/TRUNCATE/DELETE/COPY/USE/EXPLAIN）误收目标表为上游依赖（仅处理 SELECT/UNION）。验证：parser 61 项全绿（新增 3 项：UPDATE 带 CTE 不泄漏/穿透、CREATE LIKE 不产边、非 SELECT 语句 UD 为空）+ lineage 相关 160 项全绿 + 全量 unit 2395 passed（5 预存失败与基线一致）+ ruff/format/mypy 0。 | 后端 parser.py（_collect_ctes 双版本兼容 + _table_edges Create 过滤 + _extract_update_edges CTE 穿透 + extract_upstream_deps 语句类型过滤）+ test_lineage_parser.py | released
| 2026-08-16 | observability (反馈对象 404 噪音根治) | **用户诉求：反馈页每次加载 GET /metric-definitions/sales_gmv 404，且前端仍提示"指标不存在"**。根因：前端 FeedbackTab 对每条 metric 类反馈逐条 getMetric(code) 探测指标名（N+1），历史脏反馈 target_id=sales_gmv 指向不存在的指标 → 每次加载列表都产生 404 噪音；且 list_feedback 未过滤软删。修复：① 后端 list_feedback 服务端批量解析 target_name（resolve_target_names 一次 IN 查询，metric 类解析指标名、非 metric/不存在返回 None），FeedbackResponse 新增 target_name 字段透出——前端不再逐条探测，彻底消除 404（无论后续有多少脏数据）；② list_feedback 补 deleted_at IS NULL 软删过滤（与其它模块一致）；③ 数据清理：软删脏反馈 id=7（target_id=sales_gmv 不存在 + 处理说明为测试垃圾），已从列表消失；④ 前端 FeedbackCenter 对象列改直用 f.target_name（有→指标名链接，无→「已失效」标记），删除 getMetric 探测 effect 与相关 state；⑤ 顺带修复既有失败测试 test_response_contains_trace_id（fake_list 返回 [] 导致端点 data["items"] TypeError 500）。验证：后端 observability 40 项全绿（新增 resolve_target_names 3 + 软删过滤 1 + schema 字段 1，唯一失败 test_metrics_endpoint_exposes_red 为既有 /metrics 未带 token 401）；前端 FeedbackCenter 8 项全绿（新增"不再触发 getMetric"断言）+ tsc 0；端到端实测经 8180：反馈页 **0 个 /metric-definitions/ 请求**、metric 反馈显示解析出的指标名"E2E销售额"、脏数据不再出现。 | 后端：GET /observability/feedback 响应新增 target_name 字段（服务端解析，前端可直显）；GET /observability/feedback 过滤软删反馈。 | released（反馈 404 噪音根治） |
| 2026-08-16 | backend (lineage parser) | 第四轮真实环境方言覆盖核查（七方言 × 50 场景）——Doris/StarRocks CTAS 物理属性剥离 + CTE 同名遮蔽 + CTE 链式聚合穿透 | **用户诉求：再次从实际生产场景核查血缘分析未覆盖语法，结论要真实证据**。用真实 sqlglot 25.34.1 对 7 方言 × 50+ 生产场景跑探针矩阵（方言专属语法 + 字段级深度 + 健壮性边界），发现并修复 3 类缺口：① **Doris/StarRocks CTAS 带物理属性血缘全丢**——`CREATE TABLE t DISTRIBUTED BY HASH(id) BUCKETS 10 PROPERTIES("replication_num"="1") AS SELECT ...` 被 sqlglot 25.x 降级 Command 致表/字段级全空（生产 CTAS 几乎必带），预处理剥离 DISTRIBUTED BY/PROPERTIES/ENGINE/BUCKETS（仅物理布局不影响血缘语义）；② **CTE 名与真实表同名遮蔽**——`JOIN ods.cte1`（带 schema 前缀）是真实表，但被 `tbl.name in cte_names` 误排除，新增 `_is_cte_ref`（无 catalog/db 前缀才算 CTE 引用），`_table_edges`/`_select_table_edges`/`extract_upstream_deps` 统一改用；③ **CTE 链式聚合未限定列穿透缺失**——`c2.v=MAX(v)` 的 v 实际来自 JOIN 的 ods.b，但未限定列解析选中不含该列的 CTE c1 致 v 血缘落空，改为优先非 CTE 真实表、其次输出该列的 CTE（`_cte_outputs_column`）。验证：parser 64 项全绿（新增 Doris CTAS 双方言/CTE 同名遮蔽/CTE 链式聚合 3 项）+ lineage 相关 167 项全绿 + ruff/format/mypy 0。提交 待定。 | 无（纯解析器行为修正） | released（第四轮方言覆盖核查） |
| 2026-08-16 | governance + frontend | **用户诉求：授权管理「管理用户」为何跳转用户管理全局页？应在授权页内直接对单个/批量用户授权；且指标白名单拉取报 422**。根因①：GrantsTab「管理用户」按钮 `navigate('/users')` 跳到全局用户管理页（所有用户），交互割裂——用户需要的是在授权上下文内直接操作该用户；根因②：3 处 `listMetrics({ page_size: 1000 })` 超后端 MetricListParams.page_size 上限 100（ge=1, le=100）→ 422 Unprocessable Entity。修复：①「管理用户」按钮改为「给该用户授权」——在授权页内打开预填该用户（user_ids=[userId]）的批量授权弹窗，不再跳转；② 表格新增 rowSelection 行选择——勾选多用户后「批量授权」按钮预填已选用户并显示数量，可在页内批量授权/更改授权；③ 3 处指标白名单拉取 page_size 1000→100（对齐后端上限，422 消除）。验证：前端 DataGovernance 11 项全绿（含新增『用户列显示用户名、指标白名单为选项框、可在页内给该用户授权』用例，批量弹窗内断言白名单选项框避免弹窗叠加多匹配）+ 相关 47 项全绿 + tsc 0 error（LineageView 报错为并行在途改动，非本次）。提交 631cbbb。 | 前端 Governance.tsx（openGrantForUser/openBatchForSelection + rowSelection + page_size 修正）+ DataGovernance.test.tsx | verified
| 2026-08-16 | governance + frontend (独立复核) | §1.5 独立复核（复核方 agent-34b38063，前台只读，0 阻塞项）：631cbbb 恰 2 文件 +38/-14、83d2e49 恰 1 文件 +1，无并行混入。vitest 11 passed、tsc 仅并行在途 LineageView 报错豁免。走查 a-e 全符合：openGrantForUser 预填 user_ids=[userId]、openBatchForSelection 从 selectedRowKeys 预填、rowSelection preserveSelectedRowKeys、3 处 page_size 100 对齐后端 le=100（无残留 1000）、useNavigate/navigate 零残留、测试在批量弹窗内断言白名单选项框规避叠加多匹配。结论：复核通过。 | — | verified（授权治理页内直接授权 + 422 修复独立复核通过）
| 2026-08-16 | notify (通知操作人链路) | **用户诉求：通知中心的消息应该是"谁在什么时间做了哪些操作、有哪些影响、需要我做什么"，而非只展示"发生了什么"**。补齐审计语义的"谁"：此前 EventLog/Notification 无 actor 字段，EventBus 事件顶层的 actor_id 被 notify 消费端丢弃。修复：① 模型 + 迁移 0060：event_log / notification 表新增 actor_id（BigInteger 可空，NULL=系统/定时任务）+ actor_name（姓名快照，历史通知不受后续改名影响）；② notify 消费链路：handle_business_event 从事件 dict 提取 actor_id，publish_event 经 get_user_display_name 反查姓名快照（display_name 优先回落 username，一次解析复用 EventLog + 全部 Notification）；③ API 发布端点 PLAT-2 覆盖：操作人一律以服务端认证身份为准，忽略 client 传入的 actor_id 防伪造；④ 前端：通知卡片 meta 区新增「操作者」（UserOutlined）——actor_name 有值显示用户名、仅 ID 回落 #ID、系统事件无 actor 不渲染；Notification/NotifyEventLog 类型加 actor_id/actor_name。验证：后端 notify 48 项全绿（新增 actor 透传 + 系统事件无反查 + 姓名快照 3 项，唯一失败 test_event_title_contract_covers_subscription_list 为既有 lineage.change_impacted 映射漂移）+ ruff 0；前端 Notifications 30 项全绿（新增操作者展示 3 项）+ tsc 0。 | 后端：event_log/notification 表新增 actor_id/actor_name（迁移 0060）；GET /notify/notifications 响应新增 actor_id/actor_name 字段。 | released（通知操作人链路） |
| 2026-08-16 | backend (lineage parser) | 第五轮真实环境方言覆盖核查（七方言 × 55 场景）——EXCEPT/INTERSECT 集合运算血缘支持 | **用户诉求：再次从实际生产场景核查血缘分析未覆盖语法，结论要真实证据**。用真实 sqlglot 25.34.1 对 7 方言 × 55 生产场景跑探针矩阵（集合运算/WITH-DML/MERGE DELETE/括号嵌套/pivot/引号列等），发现并修复 1 类缺口 + 确认多类场景：**EXCEPT/INTERSECT 集合运算血缘缺失**——`INSERT INTO t SELECT ... EXCEPT SELECT ...` 表级正确但字段级全空、`SELECT ... EXCEPT SELECT ...`（纯查询）上游依赖为空。根因：`_branch_queries` 只展开 exp.Union，`extract_upstream_deps` 的查询判断 `(exp.Select, exp.Union)` 把 exp.Except/Intersect 当非查询语句跳过。修复：新增 `_is_query_node`（Select/SetOperation 统一判断，SetOperation 是 Union/Except/Intersect 公共基类），`_branch_queries` 递归展开集合运算的 this/expression 两分支，parser.py 7 处 `(Select, Union)` 判断统一替换（_table_edges Create 源/_find_source_query/extract_table_field_lineage 落点回退/upstream_deps）。验证：parser 68 项全绿（新增 EXCEPT 字段级/INTERSECT/上游依赖/显式落点 4 项）+ lineage 相关 171 项全绿 + ruff/format/mypy 0。已核实覆盖正确：WITH-DML（UPDATE/MERGE 带 CTE）、MERGE DELETE 分支（删除不产字段边，USING 来源表级正确）、括号嵌套 JOIN+UNION、Spark PIVOT、LATERAL VIEW EXPLODE、带引号列名、SQL Server TOP/SELECT INTO、INSERT DISTINCT/LIMIT、CK CREATE AS 表复制（无数据流转合理 0 边）。提交 待定。 | 无（纯解析器行为修正） | released（第五轮方言覆盖核查） |
| 2026-08-16 | governance + frontend | **用户诉求：权限治理角色管理按钮级配置没有 LLM 推断限制，要求重新仔细审查平台所有按钮**。审查发现：系统 6 类 LLM 推断功能（NL2SQL/指标描述推断/采集字段描述推断/表描述推断/冲突语义/术语推断）后端均有角色门禁兜底，但**按钮级权限点完全缺失**（注册表只有 ai:view 页面访问、system-config:edit 配置，无法精细控制"谁能触发某类 LLM 推断"），且前端推断按钮无权限 gate。修复：① policy.py 新增 4 个功能级 LLM 权限点——metric:infer-description（指标描述 AI 推断/改名建议）/ catalog:infer-description（目录表与字段描述推断）/ ai:nl2sql（AI 问数）/ glossary:infer（术语 AI 推断），ROLE_UI_ACTIONS 基线：PA/DA 全有、metric_owner 有指标推断+AI 问数+术语推断（无采集推断，对齐后端 collector 写角色仅 PA/DA）、reviewer/compliance/analyst/viewer 不给（reviewer/compliance 有 ai:view 但无 ai:nl2sql → 见 AI 页但生成 SQL 禁用，化解此前"合规官见页面却 403"的漂移）；② 前端全面接线——MetricDetail「AI 生成名称建议」/AssetMap 指标描述推断→metric:infer-description，AiAssistant「生成 SQL」→ai:nl2sql（无权限禁用），Catalogs/AssetMap/DescriptionCoverageTab 的字段/批量/表推断→catalog:infer-description（SchemaTable 新增 canInfer prop 统一控制，默认 true 兼容既有调用）；③ 兜底扫描确认前端全部 10 个 LLM 推断调用点均已覆盖（含之前遗漏的 glossary:infer 僵尸端点——后端有 /glossary/infer 但前端无调用，权限点已注册供接线）。验证：后端 policy 37 项全绿（注册表完整性测试自动覆盖 4 个新权限点）+ ruff/mypy 0；前端 MetricDetail 21（新增 2 项 infer-description 显隐）+ AiAssistant 7（新增 2 项 ai:nl2sql 禁用/可用）+ SchemaTable 2（新增 canInfer 显隐）+ Catalogs/AssetMap 72 全绿 + tsc 0；契约守卫"前端权限点引用 40 个、后端注册表 77 个、未注册引用 0"通过。 | 后端 policy.py（4 权限点 + 基线）；前端 SchemaTable（canInfer）+ MetricDetail/AiAssistant/AssetMap/Catalogs/DescriptionCoverageTab（按钮 gate）+ 3 测试 | verified
| 2026-08-16 | governance + frontend (独立复核) | §1.5 独立复核（复核方 agent-ea085b16，前台只读，0 阻塞项）：9d22731 恰 10 文件 +226/-32、6b9ffe3 恰 2 文件 +2，无并行混入（dimension/glossary/lineage/llm/semantic/Notifications/api.ts 零交叉）。vitest 5 文件 102 passed、tsc exit 0、ruff/mypy 0、契约守卫"前端 40/后端 77/未注册 0"通过。走查 a-f 全符合：4 新权限点三件套齐全 + ROLE_UI_ACTIONS 基线正确（metric_owner 无 catalog:infer-description 对齐后端 collector 写角色仅 PA/DA）、SchemaTable canInfer 默认 true 兼容、AiAssistant disabled 门禁、MetricDetail permReady 模式、AssetMap/DescriptionCoverageTab 双权限点接线、beforeEach 补 metric:infer-description 对齐真实基线（既有用例不破坏）。结论：复核通过。 | — | verified（LLM 推断按钮级权限点独立复核通过）
| 2026-08-16 | glossary + frontend | **用户诉求：术语表模块 7 项增强——编辑支持改编码；定义/同义词/边界支持按名称 LLM 推断（新建+编辑）；建立关系目标术语 ID 改为关键词搜索选择（不手输 ID）；关系类型丰富增强；术语状态流程发布→废弃→可再次发布 + 支持批量操作；新建弹窗业务域改为主题域树选项框（不手造）；deprecate 已废弃术语 500 根因修复**。根因①：deprecate_term 对已废弃术语抛 UnisenseError(http 500) 而非 BusinessError(400)（submit/resolve 同病）；根因②：TermUpdate 不支持改编码、TermRelationType 仅 4 种、无批量端点。修复：① service 三处 UnisenseError→BusinessError（400 语义）；② submit_term 允许 DEPRECATED 重新发布（幂等已发布）；③ TermUpdate 加 term_code + update_term 唯一性校验；④ TermRelationType 4→8 种（+反义/依赖/派生/实例）+ 迁移 0061 扩展 MySQL ENUM；⑤ 新增 POST /terms/infer（LLM 结构化推断定义/同义词/边界，json_schema 强约束 + 降级 + 重试）；⑥ 新增 POST /terms/batch-submit、/terms/batch-deprecate（207 语义逐条 + 审计）；⑦ 前端 Glossary.tsx：新建/编辑弹窗业务域改主题域树 Select + AI 推断按钮（回填三字段）、编辑弹窗加编码字段、关系目标改 Select showSearch（防抖搜索术语）、关系类型 8 种下拉、DEPRECATED 显示「再次发布」、表格多选 + 可控确认弹窗批量发布/废弃。验证：后端 glossary 38 项全绿（新增 update 编码/冲突/批量 4 + 关系枚举）+ ruff/mypy 0；前端 Glossary 18 项全绿（新增编码编辑/AI 推断回填/域选项/批量发布/批量废弃/再次发布 6 项）+ 全量 620 通过 + tsc 0。
| 2026-08-16 | lineage + backend | **第六轮方言核查(真实 sqlglot 25.34.1)：多表 UPDATE 跨 SET 双向血缘**。核查发现：UPDATE t1 JOIN t2 SET t1.v=t2.v, t2.w=t1.x 中 t1/t2 均被 SET 更新，但旧实现全局取首表 t1 为唯一目标（_update_set_target 首个 SET 的 LHS 表），且字段级自引用判断 leaf.table==target_alias 把 t2.w=t1.x 的 t1 误当自引用跳过→ 表级仅 t2→t1、字段级仅 t2.v→t1.v，t1→t2 方向血缘完全丢失（跨 SET 增量回刷生产高频）。修复（字段级完整双向 + 表级 DAG 安全）：① _extract_update_edges 每项 SET 独立判定目标表——LHS 列所属表，表级保持主目标单方向（血缘图 DAG 约束，互刷双向不入图谱避免循环依赖 409）；② 自引用判断改用该项 LHS 表（源列与 LHS 同表才跳过），子查询/CTE 分支同步。验证：parser 70 项全绿（新增跨 SET 字段级双向 + 三表跨 SET 2 项）+ lineage 173 项全绿 + 全量 unit 无回归 + ruff/format/mypy 0；修复后 UPDATE t1 JOIN t2 SET t1.v=t2.v, t2.w=t1.x → 表级 t2→t1（主目标）、字段级 t2.v→t1.v+t1.x→t2.w。 | backend/app/services/lineage/parser.py（_extract_update_edges 每项独立目标表）+ backend/tests/unit/test_lineage_parser.py + docs/module-status.yaml | verified
| 2026-08-16 | notify + frontend | **用户诉求：通知中心产品优化（A 高频三件套 + B 视觉区分聚合 + C 待办语义打通 + D 订阅分组增强）**。① 点击未读通知自动标记已读（best-effort 不阻塞跳转，消除角标红点残留）；② 收件箱筛选栏——消息类型/已读状态/时间段（近7/30/90天）/仅待处理（后端 list_notifications_page 新增 read_state/template_code/todo_only/days 四类筛选，repository 集中维护 TODO_EVENT_TYPES 待处理事件集）；③ 关联对象可点击——指标编码/冲突编号/反馈编号字段渲染为链接直达详情/仲裁/反馈；④ 高优视觉区分——payload.level ERROR/CRITICAL/P0/P1 红强调 + WARN 琥珀（notif-level-error/warn），重要程度 Tag 头部展示、正文去重；⑤ 同类聚合开关——相同消息类型合并为 ×N 卡片防刷屏；⑥ 待处理语义——NEEDS_ACTION 事件集卡片带「去仲裁/去审批/去处理」行动按钮直达处理页（与待办中心同语义，todo_only 筛选打通）；⑦ 订阅管理——新增订阅消息类型按业务模块 OptGroup 分组 + 一键推荐订阅（12 项核心事件批量幂等订阅）。验证：前端 Notifications 42 项全绿（新增点击已读/筛选/聚合/待处理/对象可点击/订阅分组/一键推荐 11 项）+ 全量 47 文件 635 项全绿 + tsc 0；后端 notify repository 22 项全绿（新增筛选 SQL 构造 5 项）+ ruff 0。 | 后端 GET /notify/notifications 新增 read_state/template_code/todo_only/days 查询参数；前端通知中心筛选栏/聚合/行动按钮/订阅分组 | verified
| 2026-08-16 | glossary + frontend | **用户诉求：术语表的关系功能在哪里能看到术语之间的相互关系（此前只有"建立关系"、无查看入口）**。补齐术语关系查看闭环：① 后端新增 `GET /terms/{term_code}/relations`——repository.list_term_relations 按 term_id 查该术语作为源或目标的全部关系（`source_term_id == id OR target_term_id == id` + deleted_at 过滤），批量取对端术语（名称/编码/域/状态）；service 归一化 direction（outgoing=本术语→对端 / incoming=对端→本术语）返回 `{relation_type, direction, peer}`；② 前端 Glossary.tsx 术语行「关系」按钮改为打开**术语关系图谱弹窗**——中心术语卡片 + 上游（引用本术语）/下游（本术语引用）左右分栏，每条关系显示关系类型 Tag + 对端名称/编码；空态提示"暂无关联术语"；弹窗右下「建立关系」直达原建立弹窗；操作列新增独立「建立关系」按钮（原「关系」单按钮拆为「关系」查看 +「建立关系」新建两个入口）；③ types.ts 新增 TermRelationViewItem、api.ts 新增 listTermRelations。验证：后端 glossary 21 项全绿（含关系查询）+ ruff/mypy 0；前端 Glossary 20 项全绿（新增关系图谱查看 2 项：上游/下游关系展示 + 空态与建立关系直达）+ 全量 47 文件 637 项全绿 + tsc 0。 | 后端：GET /terms/{term_code}/relations（返回该术语全部关系含对端信息）；前端：术语关系图谱弹窗 | 待提交 |
| 2026-08-16 | lineage + backend | **第七轮方言核查(真实 sqlglot 25.34.1)：PG UNNEST 数组展开列血缘修复**。核查 70+ 生产场景（聚合 FILTER/窗口帧/JSON·ARRAY 函数/HAVING/DISTINCT ON/FETCH·OFFSET/多列 IN/EXISTS/分区写入/MERGE 变体/引号·Unicode 列名/非血缘语句/嵌套 UNNEST/LATERAL/GROUP BY 别名·序号/QUALIFY/递归 CTE/CK FINAL·SETTINGS/动态分区/INSERT 列清单反向/多语句混合/VALUES 无源/EXPLAIN），发现 PG UNNEST 数组展开列血缘缺失：UNNEST(a.items) AS u(v) 的 u.v 无法解析（UNNEST 是 Scope(Unnest)，selects 是 Identifier 非 Alias/Column 匹配不上）→ 展开列无血缘。修复：_resolve_column 支持 Unnest 展开表——展开列来源取 Unnest 表达式叶子列（带列别名 u.v→a.items、无列别名 AS u 的 SELECT u 也归属 a.items，Unnest 显式列名声明优先于真实表猜测，消除 ods.a.u 伪边）。另确认 MAX_DEPTH 边界（CTE 链≤8 层/子查询嵌套≤6 层字段级正常，超限字段级降级空、表级保留——防无限递归合理设计）与无 FROM 常量投影 0 边正确。验证：parser 74 项全绿（新增 UNNEST 带列别名/无列别名/常量投影/深 CTE 链 4 项）+ lineage 相关 177 项全绿 + ruff/format/mypy 0。 | backend/app/services/lineage/parser.py（_resolve_column UNNEST 展开表 + _unnest_outputs_column）+ backend/tests/unit/test_lineage_parser.py + docs/module-status.yaml | verified
| 2026-08-16 | glossary + frontend | **用户诉求：术语关系图谱弹窗样式与布局太丑，产品级优化**。将扁平文字列表升级为**中心辐射星形图谱**：① 中心术语升级为渐变圆形节点（名称首字头像 + 编码 + 域/状态徽章，蓝色渐变 + 投影，视觉地位突出）；② 上游/下游改为左右分栏辐射布局，连接线箭头语义（上游 ← 指向中心 / 下游 → 从中心指出），关系卡片带左缘类型色条 + 阴影；③ 关系类型 8 种各配语义符号（≡ 同义/⊇ 上位/⊆ 下位/↔ 相关/≠ 反义/⛓ 依赖/→ 派生/◈ 实例）与专属色（antd Tag 自适应文字）；④ 新增统计条（关联 N · 上游 X · 下游 Y）；⑤ 空态升级为图文引导（🗂️ + 引导文案）；⑥ 弹窗标题带术语名称与编码（ApartmentOutlined 图标）。验证：前端 Glossary 20 项全绿（图谱展示断言适配新结构）+ 全量 47 文件 637 项全绿 + tsc 0。 | 前端 Glossary.tsx（RELATION_TYPE_META 关系类型元数据 + 星形辐射布局弹窗） | verified |
| 2026-08-16 | notify + frontend | **用户诉求：通知中心第二轮产品优化（送达失败处置 + 待办闭环 + 去重防风暴 + 对象级聚焦）**。① 送达失败处置——notification 表新增 last_error/handled_at 列（迁移 0062），_dispatch 记录失败原因（渠道未配置/HTTP 状态/异常），新增 POST /notify/notifications/{id}/retry（仅 FAILED 可重试，成功→SENT+清 last_error）、POST /notify/notifications/{id}/handled（待办办结）；前端 FAILED 卡片显示失败原因 + 「重试投递」按钮。② 待办闭环——handled_at 落库后通知不再出现在「仅待处理」（repository todo_only 追加 handled_at IS NULL），已办结卡片「已处理」Tag + 淡化 + 隐藏行动按钮 + 可手动「标记已处理」。③ 去重防风暴——publish_event 订阅扇出与定向投递统一经 find_recent_notification 查窗口内（60s）同类型未处理通知，存在则跳过（采集降级等高频事件防刷屏）。④ 对象级聚焦——list_notifications_page 新增 object_key 筛选（payload 常见业务对象键 json_extract 精确匹配 + 数字回退 ref_id），前端筛选栏「聚焦对象」输入框（指标编码/冲突编号等）。验证：后端 notify 50 项全绿（新增 retry/handled/dedup/object_key/todo排除已处理 9 项）+ ruff 0；前端 Notifications 46 项全绿（新增失败重试/标记已处理/已处理态/对象聚焦 4 项）+ tsc 0。 | 后端 POST /notify/notifications/{id}/retry、/handled；GET /notify/notifications 新增 object_key 参数；前端通知卡片失败原因/重试/已处理态/聚焦输入框 | verified
| 2026-08-16 | governance + frontend | **用户诉求：平台所有按钮审查后发现大量按钮未被细粒度权限管控，全部补齐接线**。① 后端 `policy.py` 补齐 5 个"前后端双重缺口"模块的写权限点并分配角色基线（提交 7097dcf）：`lineage:write/manage-edge`、`review:arbitrate/escalate/close/reopen`、`dimension:create/edit/deprecate/mapping/reconcile`、`glossary:create/edit/deprecate`、`template:instantiate/assign-owner`，注册表 77→93，角色基线严格对齐后端 `require_roles` 门禁（arbitrate→_GOV_DEPS 合规官+DA；escalate/close/reopen→_WRITE_DEPS owner+DA；lineage/glossary/dimension/template→owner+DA）。② 前端 16 个页面按钮级接线（提交 153b91b）：QualityCenter(quality:config-rule/run-check)、MetricCreate(metric:create/import/infer-description)、AssetMap(catalog:deprecate/assetmap:edit/export/catalog:edit-description/metric:edit)、MetricReview(metric:approve 叠加 canReview)、DataSources(data-source:create/edit/delete/test-connection/collect)、OrgManagement(org:create/edit/disable)、SystemDict(dict:create)、SubjectDomain(domain:create)、AuditLog(audit:export)、Catalogs(catalog:deprecate/edit-description/data-source:collect)、CollectionTasks(data-source:collect)、ReviewWorkbench(review:arbitrate/escalate/close/reopen)、Glossary(glossary:create/edit/deprecate)、Templates(template:instantiate/assign-owner)、Dimensions(dimension:create/edit/deprecate/mapping/reconcile)、LineageView(lineage:write/manage-edge)、UserManagement(me.role→can(user:*))、SystemConfig(can_edit||can(system-config:edit))。③ 测试适配：UserManagement/SystemConfig viewer 用例改注入受限权限上下文（PermissionProvider+mock ui_actions）验证只读视图。验证：后端 governance 99 项全绿 + ruff 0 + 契约守卫前端 40 引用⊆93 注册表未注册 0；前端 15 个接线页面测试 317 项全绿 + tsc 0。 | 后端 policy.py + 前端 16 页（AssetMap/DataSources/QualityCenter/MetricCreate/OrgManagement/SystemDict/SubjectDomain/AuditLog/Catalogs/CollectionTasks/ReviewWorkbench/Glossary/Templates/Dimensions/LineageView/UserManagement/SystemConfig）+ UserManagement.test/SystemConfig.test | verified
| 2026-08-16 | backend (lineage parser) | 第八轮真实环境方言覆盖核查（七方言 × 60 场景）——嵌套集合运算派生表列解析 | **用户诉求：再次从实际生产场景核查血缘分析未覆盖语法，结论要真实证据**。用真实 sqlglot 25.34.1 对 7 方言 × 60 生产场景跑探针矩阵（多语句相互引用/UNION 变体/列清单边界/MERGE 多 WHEN/GROUPING SETS·ROLLUP·CUBE/TO 物化视图/引号表名/分组集/窗口帧等），发现并修复 1 类缺口：**嵌套集合运算派生表列解析缺失**——`SELECT x FROM (SELECT a.id AS x FROM ods.a UNION ALL SELECT b.uid AS x FROM ods.b) u` 的列 x 字段级全空（表级正确）。根因：Union 子查询的 scope 不聚合分支 sources（build_scope 对 SetOperation 不聚合），`_scope_outputs_column`/`_resolve_column` 的 Scope 分支只处理普通 Select。修复：`_scope_outputs_column` 与 `_resolve_column` 的 Scope 分支支持 SetOperation——`_branch_queries` 逐分支找含该列的投影递归解析，UNION 合并列收集全部分支来源（a.id AS x → ods.a.id、b.uid AS x → ods.b.uid 均产边）。验证：parser 77 项全绿（新增嵌套 UNION 单列/多列/嵌套 EXCEPT 3 项）+ lineage 相关 180 项全绿 + ruff/format/mypy 0。已核实覆盖正确：多语句 CREATE+INSERT 中间表链、UNION 列数不一致容错、INSERT 列清单少于/多于投影（宽容映射）、MERGE 多 WHEN MATCHED 条件链、GROUPING SETS/ROLLUP/CUBE、CK TO 物化视图（sink 反向为已知方言语义边界）、引号表名含点、大写混合、UPDATE 子查询多表、UPDATE FROM 多源、SELECT 列表多级子查询、嵌套 CASE、窗口 ROWS/RANGE 帧、多列 IN、EXISTS 相关子查询、HAVING、DISTINCT ON、FETCH FIRST、分号多语句、CTE 穿透。提交 待定。 | 无（纯解析器行为修正） | released（第八轮方言覆盖核查） |
| 2026-08-16 | governance + frontend | **§1.5 独立复核（复核方 agent-2f5bfb4b）：平台按钮级权限点补齐与接线两提交通过，0 阻塞项**。门禁：7097dcf 恰 1 文件 +35/-6、153b91b 恰 16 文件，无并行会话文件混入，均在前 HEAD 祖先链；后端 test_governance_policy 37 全过 + ruff 0；ROLE_UI_ACTIONS 矩阵核对——domain_admin 含 lineage/review/dimension/glossary/template 全部写点，metric_owner 含全部（除 review:arbitrate），compliance_officer 含 review:arbitrate，reviewer/analyst/viewer 无写点，与后端 require_roles 门禁对齐；前端 tsc 0 + 14 页接线 can() 调用确认；契约守卫前端 80 引用 ⊆ 93 注册表未注册 0；UserManagement/SystemConfig viewer 只读用例注入权限上下文 29 全过。走查：DataSources/ReviewWorkbench/UserManagement 接线正确。非阻塞观察：① 153b91b 提交信息列 18 页面但 diff 实际 14 页面（Templates/Dimensions 已被并行会话吞入其提交、SubjectDomain 按约定留并行会话，仅 message 表述超实际范围无功能缺口）；② data-source:* 与 data-sources:view 单复数混用为注册表既有定义非本次引入。
| 2026-08-16 | backend (lineage parser) | 第九轮真实环境方言覆盖核查（七方言 × 50 场景）——CTE 集合运算列血缘 | **用户诉求：再次从实际生产场景核查血缘分析未覆盖语法，结论要真实证据**。用真实 sqlglot 25.34.1 对 7 方言 × 50 生产场景跑探针矩阵（JSON 函数/Hive json_tuple/CK dictGet/窗口表达式/数组下标/JOIN 子查询/多别名/CTE+UNION 组合/带引号库名/复合主键 UPDATE/MERGE USING CTE 等），发现并修复 1 类缺口：**CTE 定义为集合运算时外层列血缘全空**——`WITH x AS (SELECT id FROM ods.a UNION ALL SELECT id FROM ods.b) INSERT INTO dws.t SELECT id FROM x` 的 x.id 字段级为 ∅（表级正确）。根因：CTE 分支只处理普通 Select（build_scope(union) sources 空、union.selects 只取首分支），未覆盖 SetOperation 定义。修复：新增 `_resolve_setop_column`（逐分支找同名投影递归解析 + **分支列名不同按位置回退**——UNION 输出列名取自首分支，`x.id` 位置对应第二分支 uid），CTE 分支与派生表分支统一复用。验证：parser 80 项全绿（新增 CTE UNION 穿透/CTE 位置回退/派生表位置回退 3 项）+ lineage 相关 194 项全绿 + ruff/format/mypy 0。已核实覆盖正确：Hive GET_JSON_OBJECT/json_tuple 展开列、CK dictGet 字典键列、窗口 PARTITION/全表/多分区表达式、数组下标/ARRAY 构造、JOIN ON 子查询来源、多别名同表、INSERT SELECT OFFSET、CREATE VIEW 列清单、INSERT VALUES 列清单、UPDATE SET 子查询赋值、CASE+COALESCE 多源、带引号库名多段、WHERE 关联子查询、复合主键 UPDATE...FROM、MERGE USING CTE+JOIN 全部正确。提交 待定。 | 无（纯解析器行为修正） | released（第九轮方言覆盖核查） |
| 2026-08-17 | frontend (notifications) | 通知中心标记已读/删除改本地即时更新——消除整页刷新闪烁 | **用户诉求：从产品/交互/生产角度继续审视通知中心**。第三轮聚焦高频路径交互：原 `handleMarkRead`/`handleDelete` 每次操作后整页 `load()` 重拉列表（点已读卡片不立即变化、列表闪烁重排）。修复：① `handleMarkRead`——按「未读/仅待处理」过滤时该条即时从列表移除（不再匹配筛选），普通视图即时更新 `read_at`（卡片立即变已读样式），不再整页刷新；② `handleDelete`——即时本地移除 + `total-1`，若当前页删空且非第一页自动回退一页（触发按 [page] 的 useEffect 重载）；③ 全量操作（全部已读/清空）保留整页刷新语义；④ 均触发 `notifyNotifChanged()` 同步全局角标。验证：Notifications 46 项全绿（更新 2 项断言旧行为改为本地更新断言），tsc 0，8180 新版 JS 已部署（旧「已标记为已读/已删除该通知」message 已移除、保留全部已读/清空）。提交 57410cc。 | 无（纯前端交互优化） | released（通知中心第三轮交互优化） |
| 2026-08-17 | backend (notify) | 通知/事件日志保留清理策略——生产数据增长治理（通知中心第四轮） | **用户诉求：第四次从产品/交互/生产角度审视通知中心**。核查确认功能面已完善，剩余真实缺口为**历史保留策略缺失**（notification/event_log 全库无清理机制，生产环境无限增长拖慢查询撑大存储）。新增：① settings 可配置保留期 `notify_retention_days=90`/`event_log_retention_days=180`；② repository `purge_old_notifications`（物理删除已读/已办结且非 FAILED 的过期通知——**未读与 FAILED 永不清理**）/`purge_old_event_logs`（超期事件日志物理删除，与 audit_log 独立——后者有 MinIO 归档机制）；③ service `purge_expired` 按保留期算 cutoff 委托 repo 并提交；④ 新建 `app/tasks/notify_purge.py`（自建 DB 会话，对齐 audit_archive 模式）+ worker cron `notify-purge` 每日 01:30（避开 audit-archive 2 点）。验证：notify 53 项全绿（新增 purge SQL 语义 2 + service 委托 1）+ ruff 0 + worker cron 注册确认 + **真实库端到端**（插已读/未读/FAILED 三条过期通知 → purge 只删已读 1 条，未读与 FAILED 保留，SQL 断言 status!=FAILED AND read/handled 非空）。 | 后端 app/tasks/notify_purge.py（新建）+ notify/repository.py + notify/service.py + worker.py + core/config.py + 测试 | released（通知中心第四轮生产治理） |
| 2026-08-17 | collector+frontend (audit) | 数据源弹窗操作审计时间线产品化 + 采集撞键竞态修复 + 最近采集时间中文化 | **用户诉求：数据源管理弹窗里的操作审计时间线过于技术术语化，需产品化优化、增强信息丰富度、业务术语化；随后采集报 Duplicate entry 撞唯一键；并要求「最近采集」时间中文化+上海时区**。① 审计时间线产品化：泛化 `AuditTimeline` 组件（entityType+entityId 参数化，指标/数据源复用），渲染改为「后端 enrich 业务中文描述 + 中文业务分类 Tag（采集/连接/批量/描述…）+ 操作人 + 相对时间（绝对时间）+ 可折叠中文 detail 摘要」，彻底移除裸英文 action；后端补齐 14 个 data-source/描述动作中文模板 + column_description 实体标签 + detail 摘要键。② 采集竞态修复：`upsert_catalog` 先查后插在并发采集同源/种子直插时双 INSERT 撞 `uk_db_catalog_entity`，首次 IntegrityError 后会话进入 PendingRollback 拖垮整批采集——用 SAVEPOINT 包裹 INSERT，撞键仅回滚 savepoint（对象 expunge 不污染外层事务）后重查走更新语义。③ 详情弹窗「最近采集」Statistic 原样暴露 UTC ISO，改 `formatCnTime`（上海时区中文）。验证：后端 audit_i18n 19 项 + collector 151 项全绿（新增竞态回退 1 + 组件 4 + 时区 1 项）、ruff 0；前端 AuditTimeline 4 + DataSources 29 + MetricDetail 40 全绿 + tsc 0。提交 50d4d62/1e9e793/6925bd8。 | backend: core/audit_i18n.py + collector/repository.py + 测试；frontend: AuditTimeline.tsx（泛化）+ DataSources.tsx + MetricDetail.tsx + auditI18n.ts + 测试 | released（审计产品化+撞键修复+时区中文化） |
| 2026-08-17 | frontend+backend (dashboard) | Owner 责任分布「待审」升级跨资产「待处理」徽标（方案 A）——by_owner 每类资产统一 {total, by_status} 结构，徽标跨资产汇总（指标待审核 REVIEW + 维度草稿 DRAFT + 术语草稿 DRAFT），点击弹 Popover 分类明细，每类精确跳转对应目录并携带 status + owner_id 过滤 | 无（dashboard 聚合响应 by_owner 各资产字段由数值升级为 {total, by_status}，前端类型同步） | released（总览仪表 Owner 待处理跨资产下钻） |
| 2026-08-16 | backend (lineage parser) | 第十轮真实环境方言覆盖核查（七方言 × 40 场景）——tsql INSERT TOP / PG 多列合并 UPDATE / 同表自引用自环消除 | **用户诉求：再次核查血缘分析未覆盖语法，结论要真实证据**。用真实 sqlglot 25.34.1 跑第十轮 探针矩阵，发现并修复 3 类缺口：① **tsql INSERT TOP 血缘全丢**——`INSERT TOP (10) INTO t SELECT...` 被 sqlglot 25.x 抛 ParseError（`\)\b` 边界 bug 致首次剥离失效，修正正则），预处理剥离 TOP(n) 限行（血缘等价）；② **PG 多列合并 UPDATE 字段级全丢**——`SET (a,b)=(s.a,s.b)` 的 eq.this 是 Tuple，旧 `_column_name` 返回 None 跳过；重构 `_extract_update_edges` 为 pairs 展开 + 新增 `_emit_update_pair` 按位置 zip 列组；③ **INSERT 同表自引用产自环字段边**——`INSERT INTO t SELECT ... FROM t` 产 `t.id→t.id` 自环边，`_emit_leaf_edges` 过滤源表==目标表（覆盖式更新无血缘流转）。验证：parser 83 项全绿（新增 3 项）+ lineage 197 项全绿 + ruff/format/mypy 0。已核实覆盖正确：全聚合（目标列名未知降级）、嵌套 JSON/多层函数、UPDATE WHERE 混合来源、MERGE ON 复杂条件、子查询 LIMIT/OFFSET、多 CTE 交叉引用、递归 CTE、窗口多函数、COLLATE、自连接+UNION、EXISTS UNION、SELECT VALUES。提交 待定。 | 无（纯解析器行为修正） | released（第十轮方言覆盖核查） |
| 2026-08-17 | backend+frontend (notify) | 通知中心「做实」打磨——未读角标精确计数 + 索引补齐（通知中心第五轮） | **用户诉求：就此收敛，按专业建议把通知中心做实**。实地核查三维度发现真实缺口：① **性能/正确性**——全局角标用 `listNotifications({page_size:100})` 前端 filter 未读，>100 条时角标不准且每次拉 100 条浪费带宽；改为后端 `GET /notify/notifications/unread-count` 精确 COUNT（repository `count_unread`，口径与列表 read_at IS NULL 一致），Layout 直接消费。② **索引**——真实 EXPLAIN 暴露：todo_only 筛选走 PRIMARY Backward index scan（filtered 1.97%）、迁移 0062 的 handled_at 漏建索引、purge 按 created_at 无索引每日全表扫、list_enabled_subscriptions 按 event_type 查但唯一约束最左前缀是 user_id。迁移 0064 补齐：`(subscriber_id, id)` 复合索引 + handled_at/created_at（notification/event_log）+ subscription_pref.event_type。验证：notify 77 项全绿（新增 count_unread SQL 断言 + service 委托）+ ruff 0；前端 Layout 9 项全绿（角标改用 fetchUnreadCount）+ tsc 0。提交 待定。 | backend: notify/repository.py + notify/service.py + api/notify.py + 迁移 0064 + 测试；frontend: api.ts + Layout.tsx + Layout.test.tsx | released（通知中心第五轮性能打磨） |

| 2026-08-17 | backend+frontend (governance) | 按钮权限缺口第三轮修复——7 个侧门绕过 + Governance 跨点泄漏 + 运营级敏感写接线 | **用户诉求：从产品/UX/生产场景仔细审查所有按钮，还有哪些未被管控**。三维度审查（弹窗抽屉/行内下拉批量/导出复制）发现：① **7 个真实侧门绕过**——同一写操作主按钮走权限点、次级入口走裸调用/角色判断：MetricDetail 编辑描述（补 canEdit）、版本确认/拒绝/延期（canConfirm 改 canCreate）、AssetMap 表级描述编辑（补 canEditDesc）、DescriptionCoverageTab 表级编辑/推断 + 字段 editable（补 canEditDesc/canInferCatalog）、Dimensions 解绑指标（补 dimension:edit）；② **Governance 跨点泄漏**——Tab 级 canAny 粗粒度，持 grant:create 者可回收、role:edit 者可删角色、pii:review 者可见全部分类动作；下放 per-button can()（grant:revoke/role:delete/pii:review/classification:rescan）；③ **运营级敏感写接线**——新增 4 个 UI 权限点（feedback:manage/api-clients:manage/notifications:publish/query:execute，注册表 93→97，DA 基线分配），前端接线 FeedbackCenter 处置反馈、ApiClients 新建/签发令牌、QueryWorkspace 签发消费令牌、Notifications 广播 Tab 条件渲染。验证：后端 policy/service 99 项全绿 + ruff 0；前端全量 50 文件 709 项全绿（新增按钮级 gate 测试：DataGovernance 回收/重扫 4 项 + FeedbackCenter 处置 2 项）+ tsc 0；契约守卫前端 90 引用 ⊆ 后端 97 注册表未注册 0。提交 待定。 | backend: governance/policy.py；frontend: Governance/FeedbackCenter/ApiClients/QueryWorkspace/Notifications/MetricDetail/AssetMap/DescriptionCoverageTab/Dimensions + 测试 | released（按钮权限缺口第三轮修复） |
| 2026-08-17 | backend (lineage) | 第十一轮方言覆盖核查——Hive LATERAL VIEW EXPLODE 展开列血缘 | **用户诉求：针对不同方言从实际生产场景审查血缘分析还有哪些 SQL 语法未覆盖（要真实证据）**。11 轮探针矩阵（80+ 场景）发现：EXPLODE 展开列字段级缺失——`LATERAL VIEW EXPLODE(a.tags) e AS tag` 的 `e.tag` 字段级为 0（表级正确）。根因：EXPLODE 的别名 TableAlias 挂在 Lateral 节点上而非 Explode 自身，`_resolve_column` 的 Scope 分支只处理 Unnest（PG UNNEST 第七轮已修）未覆盖 Lateral。修复：新增 `_lateral_outputs_column`（列清单匹配 + 无列清单默认表别名）接入未限定列优先分支与 Scope 分支，展开列来源取 Lateral 内 EXPLODE 表达式的叶子列 `a.tags`（函数包裹 `explode(split(a.tags, ","))` 也穿透）。验证：parser 86 项全绿（新增 3 项）+ lineage 200 项全绿 + ruff/format/mypy 0。提交 待定。 | backend: lineage/parser.py + test_lineage_parser.py + docs | released（第十一轮方言覆盖核查） |
| 2026-08-17 | backend (collector) | 采集链路三处服务可用性隐患修复——健康状态枚举漂移 + 失败路径副作用丢失 + 软删实体撞幂等键 | **用户诉求：从技术角度排查数据采集模块隐患，判断是否会致服务不可用**。系统排查采集链路（健康状态机/事务边界/错误处理/枚举列/软删对账）发现三处真实隐患并全部修复：① **health_status 枚举漂移（必然复现，采集整批 FAILED）**——健康状态机 `_evaluate_health_after_collect` 在健康窗口失败率 ≥5% 产出 `degraded`（PRD §4.13 DEGRADED 黄态），但 DB 列/模型 Enum 均无此值，MySQL 严格模式拒绝（DataError 1265）→ 采集成功后健康更新整批 FAILED；且该源历史窗口失败率 >5% 时每次采集必然复现（自我锁死）。迁移 0065 扩充枚举 + 模型同步。② **失败路径 PendingRollback 副作用丢失**——collect 异常后 session 进入 PendingRollback，service/tasks 失败路径仍复用同一 session 写 health_status/collection_run，flush 抛 PendingRollbackError：service 侧掩盖原始异常、worker 侧运行记录滞留 RUNNING、健康状态不更新。修复：写副作用前先 `db.rollback()`。③ **软删实体撞幂等键**——唯一键 `uk_db_catalog_entity` 不含 deleted_at，源端表重现时 upsert 只查活跃行误判新建 → INSERT 撞软删行唯一键（1062）→ 记 failed_spec（实测 33 个）。修复：新增 `get_catalog_any_status`（含软删行），主路径与 SAVEPOINT 回退均复用软删行（清 deleted_at 重新激活）。验证：collector 154 项全绿（新增模型枚举/失败路径 rollback/软删恢复 3 项）+ ruff 0；**真实库端到端**——触发采集 mysql_unisense：修复前 scanned=401/registered=270/failed=33，修复后 registered=401/failed=0，degraded 状态成功落库（success_rate 0.7538）。提交 21026a7/7e7d6ef。 | backend: alembic 0065 + collector/service.py + tasks.py + repository.py + data_source.py + 测试 | released（采集链路服务可用性修复） |
| 2026-08-17 | backend+frontend (governance) | §1.5 独立复核结论——b499b8c 按钮权限缺口第三轮修复（复核方独立重跑，verdict 通过 0 阻塞项） | **独立复核（只读，未改代码）**：① 门禁复跑——后端 governance 3 文件 124 passed、ruff policy.py All checks passed；前端 DataGovernance+FeedbackCenter 24 passed、tsc 目标文件 0 error（LineageView/FeedbackCenter/MetricCreate 为并行在途豁免）；契约 contract + doc_sync 校验通过，权限点一致性 前端 90 引用 ⊆ 后端 97 注册表 未注册 0。② 走查 7 处侧门修复逐一确认：MetricDetail 编辑描述 `isOwnerOrAdmin && canEdit && !piiMasked`(L1420) + 版本操作 canConfirm=canCreate；AssetMap 表级编辑 `{canEditDesc && ...}`(L2054) + editable={canEditDesc}(L2087)；DescriptionCoverageTab 表级编辑/推断/字段 editable 三处(L783/797/817-819)；Dimensions 解绑 `can("dimension:edit")`(L750)；Governance per-button 回收 grant:revoke(L428)/删除角色 role:delete(L859)/PII 复核 pii:review 与重扫 classification:rescan(L1021-1022)。③ 新增 4 权限点（feedback:manage/api-clients:manage/notifications:publish/query:execute）注册表 93→97 且 DA 基线分配，契约守卫自动覆盖。verdict：**复核通过，0 阻塞项**；提交 b499b8c 恰 13 文件 +173/-54，无并行会话文件混入，工作区 6 个 M 全为并行在途（assetmap/llm/MetricCreate/Notifications）。 |
| 2026-08-17 | frontend (notifications) | 通知中心事件类型中文映射前后端一致性修复——补齐 5 个缺失标题映射 + 订阅清单对齐 17 个事件 | **用户诉求：按专业建议继续打磨通知中心细节**。核查前后端事件类型映射一致性，发现真实缺口：后端 `_BUSINESS_EVENT_TYPES` 50 个事件 / `_EVENT_TITLE_CN` 全部有中文标题，但前端 `EVENT_TYPE_LABEL` **缺 5 个**（metric.resubmitted/gray_published/breaking_change_pending/breaking_change_promoted/lineage.change_impacted）——收到这些通知会走拆词兜底显示英文或不准确中文；`EVENT_TYPES` 订阅清单缺 17 个后端可订阅事件（用户无法订阅/筛选）。修复：① `EVENT_TYPE_LABEL` 补 5 个标题（与后端一致：指标重评审待审核/指标灰度发布/指标口径变更待确认/指标口径变更已生效/血缘变更影响）；② `EVENT_ACTION_CN` 兜底补 5 个动作词（防未来同类派生事件英文）；③ `IMPACT_TEXT` 补 5 个影响文案；④ `EVENT_TYPES` 补全 17 个与后端完全对齐；⑤ `EVENT_TYPE_GROUPS` 分组同步（新增「账号与组织」组）；⑥ `NEEDS_ACTION`/`actionFor` 补 resubmitted→去审批、gray_published→去验证、breaking_change_pending→去确认、lineage.change_impacted→去评估 的待办语义。验证：Notifications 48 项全绿（新增前后端一致性 describe：EVENT_TYPES 覆盖断言 + 新事件中文标签渲染断言），tsc 0。 | frontend: Notifications.tsx + Notifications.test.tsx | released（通知中心事件映射一致性打磨） |
| 2026-08-17 | [lineage] 第十二轮方言覆盖核查：修复 PG LATERAL 相关子查询列解析（x.v 误归属外层表）、Doris/StarRocks AGGREGATE/UNIQUE KEY + 列级聚合类型（v INT SUM）血缘全丢、Oracle INSERT ALL/FIRST 多目标（t2->t1 伪边消除 + 逐目标字段映射）。parser 91 项全绿（新增 5 项）+ lineage 254 项全绿 + ruff/format/mypy 0。 | backend: parser.py + test_lineage_parser.py | released（血缘解析方言覆盖） |
| 2026-08-17 | frontend (governance) | 按钮级权限点补漏接线——三维度审查（弹窗抽屉/行内批量/导出复制）发现的未管控按钮全量接线 | **用户诉求：从产品/UX/生产场景再次审查所有按钮还有哪些未被管控**。三维度审查后确认并修复 6 类缺口：① AssetMap「查询最新数据」真实执行+落快照但无 can() gate → 补 can("query:execute")（后端 consume 内部查询端点仅 get_current_user，前端门禁补齐纵深）；② MetricDetailDrawer「添加上游/下游/同步消费方」三个血缘写按钮裸奔（LineageView 主入口已 gate 但指标抽屉可达）→ 组件加 usePermission + disabled=!can("lineage:write")+Tooltip；③ QueryWorkspace「语义校验 dry-run/执行查询」未叠加 canExecute → 补 disabled=!can("query:execute")；④ AiAssistant「执行查询」Switch 未叠加 → 补 disabled=!can("query:execute")+extra 提示；⑤ Governance 创建类按钮仅 Tab 级 canAny 粗粒度（新建授权/批量授权/给该用户授权缺 grant:create、新建角色/保存/恢复默认缺 role:create/role:edit）→ per-button can() 收敛；⑥ MetricCatalog 回收站恢复无 gate → 补 disabled=!can("metric:create")。审查确认非问题：MetricCatalog 导出 CSV 实际已 can("metric:export")（纠正误报）、ApiClients 签发令牌/SystemConfig 复制 Key/UserManagement 复制密码均在受限页内、DataSources 枚举库为测试连接成功后自动行为（已有 test-connection gate）、Glossary 仅一处 infer 按钮已 gate。ROUTE_PERM 补 /search /account 评估后跳过（两页无需守卫且需注册新权限点，纯防御性无当前价值）。验证：AiAssistant/QueryWorkspace/MetricCatalog/DataGovernance 66 项全过 + tsc 0（MetricDetail 为并行在途豁免）+ 契约一致性守卫生效（前端 90 引用 ⊆ 后端 97 注册表）。提交 f196a53。 | frontend: AssetMap.tsx + MetricDetailDrawer.tsx + QueryWorkspace.tsx + AiAssistant.tsx + Governance.tsx + MetricCatalog.tsx | released（按钮权限补漏接线） |
| 2026-08-17 | frontend+backend (governance) | §1.5 独立复核结论——f196a53 按钮权限补漏接线（复核方独立重跑，verdict 通过 0 阻塞项）+ 复核观察修复 | **独立复核（agent-fb674852，只读）**：① 门禁复跑——commit f196a53 恰 6 文件 +58/-36、AssetMap 提交内仅 canQuery+disabled 2 行无并行 OrphansTab/TablesTab 混入；tsc 6 文件 0 error（MetricDetail.tsx:816 为并行在途豁免）；vitest 4 文件 66/66 全绿（AiAssistant 7/QueryWorkspace 7/DataGovernance 14/MetricCatalog 38）；契约 contract + doc_sync 通过（前端 93 引用 ⊆ 后端 97 注册表 未注册 0）。② 走查 a-f 确认：AssetMap 查询按钮 canQuery（query:execute 注册于 policy.py:230）；MetricDetailDrawer 三写按钮 canLineageWrite（lineage:write 注册 L217）；QueryWorkspace canExecute 叠加 dry-run+执行查询；AiAssistant 执行 Switch disabled+extra；Governance 创建类按钮 per-button can()（grant:create/role:create/role:edit）；MetricCatalog 恢复 disabled 补 metric:create。③ 复核观察修复：query:execute 原仅 DOMAIN_ADMIN 基线含之，METRIC_OWNER 仅 query:view——而后端 consume query_metric_internal 用 get_current_user 允许所有登录用户查询，metric_owner 前端见按钮却禁用（前端比后端更严割裂）；已给 METRIC_OWNER 基线补 query:execute（policy.py L342），policy 37 全过 + ruff 0 + contract 通过。verdict：**复核通过，0 阻塞项**；提交 f196a53。 | frontend 6 文件 + backend policy.py + docs | released（按钮权限补漏接线复核） |
| 2026-08-17 | [lineage] 第十三轮方言覆盖核查：修复 3 类缺口——① 标量子查询（SELECT 列表 / CASE 分支 (SELECT ...)）字段级 scope 错位（子查询内部列此前在外层 scope 解析，未限定列 max(v) 的 v 误归属外层表产生 s.v 伪边、子查询自身表列 d.v/d.id 被丢弃；新增 _resolve_leaf_scope 用子查询自身 scope 解析，s.v 伪边消除、d.v 真实源补齐）；② 命名窗口引用（ROW_NUMBER() OVER w）派生源全丢（PARTITION/ORDER 列在 WINDOW 子句不在投影内，_resolve_projection 按名查窗口定义补解析 + _extract_branch_edges 不再因无内联列提前跳过）；③ UNPIVOT 输出列伪列（u.v 归属不存在的 s.v，新增 _matching_unpivot/_unpivot_output_sources 值列归 In 列表源列 s.a/s.b/s.c 多源、名列 k 是列名字面量不产数据边）。parser 95 项全绿（新增 4 项）+ lineage 205 项全绿 + ruff/format/mypy 0。 | backend: parser.py + test_lineage_parser.py | released（血缘解析方言覆盖） |
| 2026-08-17 | [lineage] 第十四轮系统性穷举核查：五维矩阵（9 方言 × 90+ 生产场景）真实验证 + coverage 路径分析（89%→93%）一次性补齐——结论历轮修复后 parser 已无功能性缺口（剩余未覆盖 7% 全为防御/不可达降级分支）；新增 test_lineage_matrix.py 穷举回归套件（101 项：99 矩阵场景精确断言表级/字段级血缘集合 + node 构造函数 + 星号投影 UD 跳过），固化历轮全部已修复行为防回归。lineage 306 项全绿（parser 95 + matrix 101 + repository/service）+ 全量 2720 passed（7 失败全为并行会话 collector 3 + 预存 semantic_cache 4，零交集）+ ruff/format/mypy 0。 | backend: tests/unit/test_lineage_matrix.py | released（血缘解析穷举固化） |
| 2026-08-17 | backend+frontend (governance/consume) | 第四轮审查收尾三项：① 内部查询端点 RBAC 对齐——POST /consume/metrics/{code}/query 从仅 get_current_user 升级为 require_roles(platform_admin/domain_admin/metric_owner)，与前端 query:execute 权限点默认基线一致（viewer/analyst 须走 consume 客户端令牌通道），补 2 项 API 层测试（viewer 403 / metric_owner 200）；② usePermission 增加 error 状态 + Layout 全局提示——快照拉取失败时 can() 仍 fail-open（后端为最终边界）但页面顶部提示「权限数据加载失败请刷新」，提升产品体验；③ 清理 api.ts 僵尸封装——删除无页面调用的 confirmVersion/rejectVersion（consume 版本确认半成品）与 submitQualityObservation（质量观测未接线，后端端点保留），移除 QualityObservation unused import。验证：consume 安全测试 6 passed、ruff 0、Layout 10+Notifications 48 passed、tsc 0、契约 contract/doc_sync 通过 |
| 2026-08-17 | [collector] feat: 数据源管理增强四项——① 立即采集弹窗支持「本次临时白/黑名单」（A 方案：collect-now 携带 include/exclude 随任务投递 worker，仅本次生效不污染数据源配置，None 回退数据源既有规则）+ 采集结果展示「过滤跳过 N 张表」（B 方案：CollectResult 新增 filtered_count/filtered_names，MySQL 连接器统计被白黑名单过滤的表）；② 编辑/创建支持多目标库（databases JSON 列，迁移 0066；连接器 set_databases 逐库扫描、库.表命名防跨库冲突，MySQL/ClickHouse/Hive 三连接器同步，PATCH 语义 [] 清空）；③ 调度启停（schedule_enabled 列独立于 enabled，停用仅暂停自动定时、源仍可手动采集；list_scheduled_sources 过滤；前端 Switch+Tooltip）；④ 批量调度显式启用。验证：collector 180 passed（3 失败为并行会话 list_catalogs 转义未同步，与本次无关）+ 新增 6 项全绿 + ruff 0 + 前端 DataSources 32 项全绿 + tsc 0。 | backend: collector service/queue/tasks/spi/repository/schemas/api + 3 连接器 + 迁移 0066 + frontend: DataSources.tsx/types.ts/api.ts | released（数据源多库+临时过滤+调度启停） |
| 2026-08-17 | backend+frontend (governance/consume) | §1.5 独立复核结论——a17ef29 第四轮审查收尾三项（复核方 agent-47ab0ed9 独立重跑，verdict 通过 0 阻塞项） |
| 2026-08-17 | backend (lineage) | 图存储与缓存一致性修复——删除/恢复回写 Neo4j + 批量入库写图 + 写图失败告警 + 影响缓存即时失效 + 定时对账任务化 | **用户诉求：把审计发现的未修复项（C3/M1/M2/m5）全部处理掉**。① **C3 删除/恢复路径回写图**——`delete_by_node`/`delete_edge_by_id`/`restore_stale_edge` 软删/恢复后 best-effort 同步删/建 Neo4j 边，`restore_by_node` 经新增 repo 方法 `soft_deleted_edges_for_node` 查软删边重建图，消除图残留节点/关系。② **M1 批量入库写图**——`ingest_batch` 与 `add_manual_edge` 同步 `write_edges`（此前只落 MySQL 图完全缺失）。③ **M2 写图失败不再静默**——统一 `_sync_graph` 入口，失败记 error 日志 + 发布 `lineage.graph_sync_failed` 告警事件；**对账任务化**——新建 `app/services/lineage/neo4j_sync.py`（arq 任务 `sync_neo4j_assets_task`，每日 2:30 cron），从 `db_catalog`/`metric`/`lineage_edge` 全量补全图节点属性 + 写入表级/指标级边，`load_table_edges` 重扫权威库表级边收敛 M1 历史漂移；`graph.py` 新增 `list_asset_ids`；CLI 脚本转口共享逻辑。④ **m5 缓存失效**——新增 `_invalidate_impact_cache`（SCAN 前缀失效），删除/恢复/新增/批量入库路径调用，TTL 内不再读到已变更边。验证：test_lineage_service 74 过（+8 回归）+ test_neo4j_sync 6 过 + test_lineage_graph 24 过 + test_sync_neo4j_assets 12 过 + semantic/dimension 266 过 + 全量后端 2785 过（7 失败为已知 HEAD 遗留）+ ruff 0 + 改动文件 mypy 0。提交 4dfc069/54cb7c0（m1 enqueue 竞态 + m2 幂等键随并行会话 af46478 提交并标注）。 | backend: lineage/service.py + lineage/repository.py + lineage/graph.py + lineage/neo4j_sync.py（新建）+ collector/worker.py + scripts/sync_neo4j_assets.py + 测试 | released（血缘图存储与缓存一致性） |
| 2026-08-17 | [collector] fix: 数据源「全量/增量」三处去重为两处明确语义——数据源默认采集模式提升为编辑表单固有属性（create/update 支持 collection_mode，回显+提交），调度区移除重复的全量/增量 Select（调度只管 cron+启停，定时任务按数据源默认模式执行，后端 ScheduleRequest.mode 改可选 None=保持现有模式），立即采集弹窗明确为「本次采集模式」（默认跟随数据源可临时覆盖，不污染配置）；另修复调度保存后主列表「调度」列状态不变（SourceDetailModal 新增 onScheduleSaved 回调触发列表刷新）。验证：collector 180 passed（3 失败为并行会话 list_catalogs 转义未同步）+ ruff 0 + 前端 DataSources 34 项全绿（新增「调度保存后刷新列表」「编辑保存采集模式」2 项）+ 全量 50 文件 729 项全绿 + tsc 0。 | backend: collector schemas/service + frontend: DataSources.tsx/types.ts/api.ts | released（数据源采集模式去重+调度刷新） |
| 2026-08-17 | backend (lineage) | 防御分支全覆盖——parser.py 覆盖率 96%→100%（718 行全量覆盖），test_lineage_defensive.py 扩至 72 项（构造 AST / monkeypatch 触发 scope 构建失败、异常兜底、30.x Whens 容器、UNPIVOT 无 Pivot、星号别名兜底等全部防御分支） | **用户诉求：把剩余 7% 防御分支也补齐**。用 pytest coverage 逐行定位 parser.py 剩余 31 个未覆盖行，分类为「真实 SQL 触发」（空 sources 限定列解析、Oracle INSERT ALL 分支 VALUES 非 Values、PIVOT 无 unpivot、多表 UPDATE SET 无表限定等）与「构造 AST / monkeypatch 触发」（_try_build_scope/build_scope/merge_source_scope 返回 None 的兜底、30.x Whens 容器 expressions 空、MERGE INSERT 列清单边界、CTE 定义 scope 失败、星号别名非星号兜底等），补齐全部防御分支测试。验证：lineage 相关 386 项全绿（parser 95 + matrix 101 + defensive 72 + repository/service）+ ruff/format/mypy 0 + parser.py coverage 100%。 | backend: tests/unit/test_lineage_defensive.py | released（血缘解析器防御分支全覆盖） |
| 2026-08-17 | backend (lineage) | 企业级血缘平台落地——血缘评测集（P0）+ 批次解析（P1） | **用户诉求：定位企业级血缘平台，一次补齐核心差距**。① **P0 血缘评测集**——新增 `backend/tests/eval/`：24 个人工核对语义的真实生产场景（含线上 Hive 多表 JOIN+子查询 SQL）固化为 golden dataset；评测运行器 `python -m tests.eval.lineage_eval` 输出表级/字段级/上游依赖精确率·召回率·用例级完全匹配率；回归断言 100% 精确匹配 + 负向验证有效（改坏期望即捕获并给出精确多余/缺失边诊断）——把 parser 质量从「测试全绿」升级为「可度量准确率」。② **P1 批次解析/全量重建**——新增 `POST /lineage/parse-batch`（statements 数组或 text 多语句文本块二选一，整批共用 dialect/target_table）：逐条独立解析（单条失败不阻断批次）、成环边跳过计数不抛错、单事务幂等写入（added/updated/skipped 变更摘要 + 逐条明细 + 失败定位）、写 kind=batch_parse 运行记录、同步 Neo4j 图 + 失效两端影响缓存 + 发布 lineage_batch_parsed 事件；注入守卫豁免 statements/text 字段（与 /parse 的 sql 同语义：仅经 sqlglot 纯函数解析），provenance 等其他字段仍全量扫描。验证：lineage 相关 325 项全绿（service 批次 4 + security 豁免 2）+ 评测集 24/24 精确匹配 + ruff/format/mypy 0。 | backend: tests/eval/（新建）+ lineage/schemas.py + lineage/service.py + lineage/repository.py + api/lineage.py + 测试 | released（企业级血缘平台 P0 评测集 + P1 批次解析） |
| 2026-08-17 | [governance] feat: 打通授权链路——用户直挂按钮权限点（user_permission 表+迁移0068+GET/PUT /users/{id}/permissions+my_permissions 并集合并）、角色管理按钮配置加搜索+动作类型筛选、GrantsTab 角色下拉显示角色名+「按钮权限」按用户授权矩阵（角色已含只读+直挂可勾选） | ① **用户直挂按钮权限点（以角色间接授权为主+直挂为辅）**——新增 `user_permission` 表 `(user_id, action)` 唯一（迁移 0068），`my_permissions` 将角色继承 ui_actions 与直挂做并集返回；API `GET/PUT /users/{user_id}/permissions`（仅 platform_admin，未知点 USER_PERMISSION_INVALID）。② **角色管理按钮配置搜索筛选**——配置弹窗加搜索框（label/action 实时过滤）+ 动作类型 Tabs（全部/查看/写操作/导出/LLM/其他，先模块后类型）+ 已勾选计数。③ **GrantsTab 角色下拉显示角色名**——新增 `GET /roles/options`（role 表 id→name），授权弹窗 InputNumber 改 Select、列表「角色」列显示名称（自定义角色带 Tag）。④ **「按钮权限」按用户授权矩阵**——操作列加入口，矩阵双区：角色已含项只读（disabled+Tag）+ 直挂可勾选，保存写直挂。验证：后端 governance 103 全绿（新增直挂并集/矩阵/清空/未知点 4 项）+ ruff 0；前端 DataGovernance 16 全绿（新增角色列/矩阵 2 项）+ tsc 0。 | backend: governance 模型+repository+service+schemas+api+迁移0068+测试；frontend: Governance.tsx/api.ts/types.ts/DataGovernance.test.tsx | released（授权链路打通+按钮配置可搜索） |
| 2026-08-17 | backend+frontend (governance) | §1.5 独立复核结论——ec8fbc2 授权链路打通（复核方 agent-941b362b 独立重跑，103 governance passed / ruff 0 / vitest 16 passed / tsc 0 / contract doc_sync 通过，走查 a-g 与实现一致、隔离干净，verdict 通过 0 阻塞项） |
| 2026-08-17 | backend (lineage) | 企业级血缘平台落地 P2+P3——血缘健康度评分 + 路径查询/断链定位 | **用户诉求：继续把 P2 和 P3 一起做掉**。① **P2 血缘平台健康度**——新增 `GET /lineage/health` 综合健康度评分：五维加权（coverage 覆盖率 40% / broken 断链率 20% / stale 失效率 15% / freshness 采集新鲜度 15% / reconciliation 图-库对账偏差 10%），各维度 0-100 独立可解释 + 原始指标明细，总分按 90/75/60 分档 excellent/good/fair/poor；图存储不可达时 reconciliation 维度权重置 0、其余权重归一化不惩罚；新鲜度按距最近采集天数线性衰减（30 天满分→0），全新平台中性满分。② **P3 血缘路径查询**——新增 `GET /lineage/path`（A→B 链路：Neo4j 全路径/最短路优先，空/不可达降级 MySQL DFS，路径按边数升序最短在前、无前缀输入自动展开候选对）与 `GET /lineage/path/terminals`（下游终止节点断链定位：entity_exists 标注 metric:/table: 权威库存在性，实体已删但边残留即断链嫌疑）。验证：lineage 相关 414 项全绿（repository 新增 10 项 + service 新增 8 项）+ 全量 unit 2933 passed （3 失败全为并行会话 collector list_catalogs 转义未同步，与本次零交集）+ ruff/format/mypy 0。 | backend: lineage/schemas.py + lineage/repository.py + lineage/graph.py + lineage/service.py + api/lineage.py + 测试 | released（企业级血缘平台 P2 健康度 + P3 路径查询） |
| 2026-08-17 | backend (collector) | 数据源覆盖度修复——按 TD §2051 改为已采集实体/源端实体总数 | **用户诉求：数据源管理页面覆盖度为什么都是 0%**。根因：`recompute_coverage` 用 `quota.max_scan_rows`（行数配额）作分母——数据源未配置 quota 时恒 0.0，且与 TD §2051「coverage = 已采集实体 / 源端实体总数」语义不符（分子是表数、分母是行数，单位错位）。修复：① 迁移 0069 新增 `data_source.source_total_entities`（源端实体总数基线）；② `recompute_coverage(source_id, total_entities=None)` 改按基线计算，采集完成后以本次扫描实体数 `len(result.specs)` 刷新基线；③ 无基线（从未采集）时保持 0.0（覆盖率未知语义）。验证：collector 165 项全绿（新增基线刷新/传参 3 项）+ ruff 0；**真实库端到端**——同步采集 mysql_unisense：scanned=401/registered=401/failed=0，coverage=1.0 落库（source_total_entities=401），列表接口返回 coverage=1.0。提交 3dd6e83。 | backend: alembic 0069 + collector/repository.py + collector/service.py + data_source.py + 测试 | released（数据源覆盖度修复） |
| 2026-08-17 | backend (lineage) | 企业级血缘平台落地 P4+P5——标准血缘导出（OpenLineage/JSON）+ Hive 变量/宏展开 | **用户诉求：继续把 P4/P5 一起做掉**。① **P4 标准血缘导出**——新增 `GET /lineage/export` 开放 API：`format=openlineage` 输出标准 OpenLineage 2-0-0 RunEvent（L1 表级边 → inputs/outputs 数据集，L2 字段级边按目标表聚合为 schema facet 的 lineage 子 facet，L3 指标/维度/消费方边是平台扩展语义保留在 JSON 导出；仅有 L2 而无 L1 的目标表兜底生成独立事件）；`format=json` 输出原始边明细 + 元数据（导出时间/边数/生产者）。可按 `node`/`direction`/`granularity`/`provenance`/`limit` 过滤（repo.list_export_edges），注入守卫对 node 等 query 参数仍全量扫描（非 SQL 文本字段不豁免）。② **P5 Hive 变量/宏展开**——解析器新增 `expand_variables`：`${hivevar:x}`/`${hiveconf:x}`/`${x}` 占位符 + `set hivevar:x=y;` 命令 + `--hivevar x=y` 注释行内联声明，仅 hive/spark 方言或显式传 `variables` 时展开，未知占位符保留（降级不崩），血缘语义不变；`extract_table_lineage`/`extract_field_lineage`/`extract_upstream_deps` 三入口统一接入（经 `_preprocess_dialect`）。验证：lineage 相关 457 项全绿（parser 新增 6 + repository 新增 4 + service 新增 4 + security 新增 2）+ 全量 unit 2968 passed（7 失败全为并行会话 collector 在途改动，与本次零交集）+ ruff/format/mypy 0。 | backend: lineage/parser.py + lineage/schemas.py + lineage/repository.py + lineage/service.py + api/lineage.py + 测试 | released（企业级血缘平台 P4 标准导出 + P5 Hive 宏展开） |
| 2026-08-17 | frontend (governance) | 授权体验闭环——用户管理直达按用户授权矩阵 + 角色常用组合预设 | **用户诉求：授权者如何快速找到按钮授权给指定用户**。① **用户管理「授权」直达矩阵**——UserManagement 操作列新增「授权」按钮（can(user:edit)），点击直达该用户的按钮权限矩阵（复用 Governance 按用户授权矩阵：角色已含只读 + 直挂可勾选，可搜索/按类型筛选）；UserPermModal 提取为共享组件 components/governance/UserPermModal.tsx（groupRegistry/categoryOf/UI_CATEGORIES/UiCategory 同步移出），Governance 与 UserManagement 双入口复用。② **角色「常用组合」预设**——按钮级配置弹窗顶部新增 4 个一键套用预设（只读分析师/指标运营/域管理员/合规复核员），引用注册表真实权限点、应用时仅合并注册表中存在的点（防未来删点导致后端 422），套用后仍可微调再保存；VIEW_BASE 公共只读集合 + applyPreset 合并逻辑。验证：前端 DataGovernance 17 + UserManagement 13 全绿（新增预设套用/授权直达 2 项）+ tsc 0（TodoCenter/DataSources 8+2 失败为并行会话在途改动，非本次）；全量 738 passed。 | frontend: Governance.tsx + UserManagement.tsx + components/governance/UserPermModal.tsx(新) + DataGovernance.test.tsx + UserManagement.test.tsx | released（授权体验闭环） |
| 2026-08-17 | frontend (governance) | §1.5 独立复核结论——5f39b2f 授权体验闭环（复核方 agent-b325dbb2） | 独立复核：commit 5f39b2f 恰 6 文件 +472/-201 无并行混入；vitest DataGovernance 17 + UserManagement 13 = 30 passed（含预设套用/授权直达 2 新用例）；契约 doc_sync 通过；走查 a-d（UserPermModal 共享组件双入口复用、ROLE_PRESETS 引用注册表真实权限点且 applyPreset 仅合并 registry 内点防 422、UserManagement 授权按钮 can(user:edit) gate、viewer 不显示）全部核实通过。复核发现 **1 阻塞项**：UserManagement.test.tsx L143/149 类型错误（mockGetUserPerm 缺 effective_actions 字段、mockSetUserPerm 返回 {ok:true} 而非 UserPermissionResponse）——已修复（50af825 补全类型字段），修复后 tsc 0 error + 30 passed，verdict 通过 0 阻塞项。 | frontend: UserManagement.test.tsx | released（复核通过） |

| 2026-08-17 | backend+frontend (collector) | 数据源弹窗库→表级联多选——目标库为主入口、连接库收进高级连接选项（纯凭据） | **用户诉求：连接库和目标数据库是否重复，枚举库支持多选，选库后多选表**。方案 A+Ⅰ：① **连接库彻底降级纯凭据**——collect/collect_entity 移除连接库隐式采集范围（采集范围只由目标库 databases/全部库决定），存量源全部 database=None 无破坏；② **表枚举接口**——POST /data-sources/tables（与 list_databases 同构，明文配置不落库）按库分组返回 BASE TABLE，连接器 SPI list_tables 默认空字典（Kafka 等不支持安全回退）；③ **前端库→表级联**——目标库多选为主入口、连接库收进「高级连接选项」折叠面板（forceRender 保证 Form.Item 常驻挂载防字段丢失），选库后自动枚举表、勾选部分表生成 include_patterns（选表库→库.表，未选表库→库.* 整库），编辑回显从 include_patterns 反解选表（库.* 整库不勾选）。修复既有缺口：新建分支此前从不提交 include_patterns（只在编辑分支），已统一。验证：collector 226 项全绿（新增 list_tables 4 项 + 单库语义改造 3 项）+ ruff 0 + mysql.py mypy 0；前端全量 750 项全绿（DataSources 新增 3 项）+ tsc 0。提交 38bfcf0/186e6fd。 | backend: collector mysql.py/spi.py/schemas.py/service.py/api + frontend: DataSources.tsx/types.ts/api.ts + 测试 | released（数据源库→表级联选） |
| 2026-08-17 | backend (auth) | 单端登录（互踢）——禁止多人共用同一账号 | **用户诉求：系统是否支持多人共用账号**。产品结论：技术上能用但破坏审计链（PII 复核/指标废弃只能到账号层无法定位操作者）、绕过细粒度权限体系、无法按人回收凭证，故实施**方案 A 单端登录（互踢）**：① `security.rotate_active_refresh`——维护用户当前活跃 refresh jti 映射（Redis key `unisense:active_refresh:{user_id}`，降级进程内存，与黑名单同语义），登录/刷新签发新 refresh 时把该用户旧 jti 拉黑（复用 blacklist_token）；② `auth.login`/`auth.refresh` 接入；旧会话 access token 短效（15min）过期后无法无感续期即被踢下线（短效容忍窗口避免极端并发自刷新误踢）；refresh token 有效期统一为 `REFRESH_TOKEN_TTL_SECONDS` 常量（7 天）。复用现成 `AUTH_REFRESH_REVOKED` 错误码，无新错误码。验证：auth/jwt 黑名单 13 passed（新增单端登录单元 2 + API 互踢 1）+ ruff/mypy 0；顺手修复既有脆弱测试 `test_me_returns_current_user`（me 端点 org/domain 回填 mock 未适配，HEAD 基线即失败，非本次引入）。 | backend: core/security.py + api/auth.py + tests/security/test_jwt_blacklist.py + tests/security/test_api_auth.py | released（单端登录） |
| 2026-08-17 | frontend (observability) | 最近质量事件六要素增强——谁/何时/什么资产/发生什么/影响风险/如何解决 | **用户诉求：质量事件应展示谁在什么时候什么资产发生了什么、影响风险如何、如何解决**。核心缺口：`repair_suggestion`（修复建议）后端一直返回但前端从未展示；处理人（ack_by/resolved_by/closed_by）是数字 ID 且未映射用户名；无所属域、无影响风险说明。修复：① **后端** quality_events 批量解析处理人用户名（display_name 优先回落 username）+ metric 查询补 domain，返回 ack_by_name/resolved_by_name/closed_by_name/metric_domain（一次批量查询避免 N+1）；② **前端** 质量事件卡片重写为六要素：资产行（指标名+编码+所属域 Tag）、影响风险行（严重级影响说明 + 规则风险描述）、事件行（异常模式中文 + 观测值/阈值）、处理留痕（谁在何时确认/解决/关闭 + 处理说明）、解决建议（处置动作 + 责任方 + 上游任务 + 诊断 SQL，橙色底块）；③ enums 新增 QUALITY_SEVERITY_IMPACT / QUALITY_RULE_RISK / QUALITY_PATTERN_LABEL 三个映射。验证：后端 observability 16 项全绿（quality_events 断言补全 5 新字段）+ ruff 0；前端 Observability 5 项全绿（新增影响风险/处理留痕/解决建议断言）+ 全量 51 文件 766 passed + tsc 0。 | backend: observability/repository.py + frontend: Observability.tsx/types.ts/enums.ts + 测试 | released（质量事件六要素） |
| 2026-08-17 | backend (lineage) | DDL 血缘追踪 + 库级扫描与定时调度——企业级血缘平台能力补强 | **用户诉求：DDL 血缘更多类型 + 扫描任务定时调度**（前一轮实现被并行会话 reset 吞掉，重建）。① **DDL 血缘追踪**：parser 新增 `extract_ddl_lineage`（DDLEdge）——CREATE TABLE LIKE/(LIKE s)/AS COPY OF→结构复制依赖、ALTER TABLE/VIEW RENAME TO→表重命名、ALTER RENAME COLUMN/CHANGE→列重命名（正则兜底）、ADD/DROP/MODIFY COLUMN 仅标记、DROP TABLE→依赖失效（软删该表上下游边，`invalidate_dropped_table`）；service `parse_and_store` 接入（响应 `ddl_edges` 明细 + 图谱写入 + 运行记录）；② **库级扫描**：POST /lineage/scan——递归扫描 SQL 目录、方言启发式推断（LATERAL VIEW→hive/ARRAY JOIN→ck/DISTRIBUTED BY→doris）、dry_run 仅统计或批量落库（DML+DDL 边+图同步+kind=scan 运行记录）、路径沙箱拒绝 `..` 越权；③ **定时调度**：worker 新增 `lineage_scan_task` cron（每日 3:30，`UNISENSE_LINEAGE_SCAN_DIR` 配置，未配置安全禁用）；④ **评测集扩充**：24→209 条（独立语义推导 + parser 100% 双向校验，评测运行器新增 DDL 维度，TE/FE/UD/DDL 四维 100% 精确匹配）。验证：parser 110 + repository 60 + service 104 + security 10 + 评测 209 全绿 + ruff/format/mypy 0。 | backend: lineage/parser.py/service.py/repository.py/schemas.py + api/lineage.py + collector/worker.py + core/config.py + scan_tasks.py(新) + tests/eval/* + 3 测试 | released（DDL 血缘+库级扫描+定时调度） |
| 2026-08-17 | backend (auth) | 单端登录加固——Lua 原子轮换 + 剩余有效期拉黑 + 降级告警 + 测试补全 | **用户诉求：修复单端登录独立复核提出的 4 个非阻塞观察**。① **原子性（观察1）**——rotate_active_refresh 改用 Redis Lua 脚本原子执行「读旧→拉黑旧→写新」，消除并发双登录下旧会话未被拉黑的竞态（真实 Redis 验证：asyncio.gather 并发双 rotate 后仅一个活跃、另一个被拉黑）；② **剩余有效期拉黑（观察2）**——活跃映射值改 `{jti}:{exp_ts}`（含过期戳，兼容旧格式纯 jti 以默认 TTL 兜底），旧 jti 拉黑 TTL=其剩余有效期而非固定 7 天；③ **降级告警可见（观察3）**——内存降级分支限频 warning（single_login_memory_fallback），提示多 worker 下仅本进程生效、生产须 Redis；④ **测试补全（观察4）**——跨用户隔离（viewer/analyst 互不踢）+ 连续三次登录链式互踢 + API 层不同用户登录互不影响。验证：security 全量 192 passed（新增 3 项）+ ruff/mypy 0 + 真实 Redis Lua 脚本 5 项断言全过；TD §15.3 补充原子语义/活跃映射格式/降级部署要求。 | backend: core/security.py + tests/security/test_jwt_blacklist.py + tests/security/test_api_auth.py + docs/technical-design.md | released（单端登录加固） |
| 2026-08-17 | backend (auth) | §1.5 独立复核：678cd23 单端登录加固——4 项门禁复跑通过 （security 192 passed / ruff / mypy / 真实 Redis Lua 5 场景含并发原子性），verdict 通过 0 阻塞项；复核提出 3 新观察：观察1 死代码（_parse_active_val/_format_active_val）已移除、观察2 docstring 精度已修正（内存降级原子性限定于池未初始化场景，Redis 不可达时为 best-effort）、观察3（auth.py 刷新轮换 ttl 门控）为既有行为不处理。 | backend: core/security.py + docs/CHANGELOG_MODULES.md | released（单端登录加固） |
| 2026-08-17 | frontend+backend (observability) | 平台概览企业级增强——系统健康/资产质量/风险雷达/近7天趋势 | **用户诉求：平台概览展示内容少，按工业级可观测性平台增强，且达到企业级**。核心缺口：原概览只有"资产存量快照"（数据源健康/治理积压/资产规模/消费接入），无系统健康信号。真实数据支撑（dependency_health 4 条含 **GRAPH 熔断 OPEN、LLM 降级 HALF_OPEN**；metric_health_score 9 条 89% 警告级；collection_run 8 条含 2 RUNNING；PII 级未复核 305）。实施：① **后端** `overview_stats` 重构为 `_overview_assets/_system/_quality/_risks/_trends` 五块（main 仅 22 行）：system=核心依赖实时态（状态/熔断/连续失败/P95/错误率/最近探测）+ 采集链路（状态分布/成功率/最近采集时间）、quality=指标健康度分布（EXCELLENT~CRITICAL + 覆盖率 + 平均分 + 低健康 Top5）+ 血缘健康（边/失效/接入成功/最近接入）、risks=PII 待复核 + 授权 7 天内到期 + 近 7 天 Schema 漂移、trends=近 7 天指标新增/采集按天聚合（缺失补 0）；全部查询带软删过滤，口径与各模块一致；② **前端** OverviewTab 重排为企业级布局：顶部状态条（依赖健康/熔断开启/采集运行中/数据新鲜度）+ 核心依赖卡（熔断 OPEN 红色高亮）+ 采集链路卡 + 指标健康度卡（含低健康 Top 指标）+ 血缘健康卡 + 治理风险雷达 + 资产规模（含数据源健康子块 + 消费接入）+ 近 7 天趋势柱状；③ enums 补 DEP_STATUS_LABEL/CIRCUIT_STATE_LABEL/COLLECTION_RUN_STATUS_LABEL/METRIC_HEALTH_LEVEL_LABEL。验证：后端 observability 39 项全绿（overview_stats 企业级断言 + 软删过滤守护扩展至新增查询）+ ruff 0；前端 Observability 7 项全绿（新增企业级概览 2 项）+ 全量 51 文件 772 passed + tsc 0。 | backend: observability/repository.py + frontend: Observability.tsx/types.ts/enums.ts + 测试 | released（平台概览企业级） |
| 2026-08-17 | backend (lineage) | DDL 变更事件化 + 评测集扩充 504 条 | **用户诉求：DDL 血缘更多类型 + 扫描定时调度已完成，继续做 DDL 变更事件化（重命名/列变更自动失效下游缓存并通知资产 Owner）+ 评测集用真实生产 SQL 匿名化样本持续灌入（209→500+）**。① **DDL 变更事件化**——破坏性 DDL（表/列重命名、DROP TABLE）不再止于写血缘/失效影响缓存：repository 新增 `affected_asset_owners`（沿下游 DFS 收集 table:/metric: 受影响资产，经权威库 db_catalog join data_source / metric 解析 owner_id 去重，limit 防超大下游刷屏）；service 新增 `_notify_ddl_change` 接入 parse_and_store 与 scan_directory——经 `NotifyService.notify_user` 定向通知受影响资产 Owner（IN_APP 不依赖订阅偏好），并发布 `lineage.ddl_changed` 事件（notify consumers 注册 + 标题映射），best-effort 不阻断血缘写入，从「解析工具」升级为「变更治理闭环」。② **评测集扩充 24→504 条**——真实生产 SQL 匿名化样本（数仓分层链 ods→dwd→dws、增量覆盖写分区、CTAS 物理属性 STORED AS/ENGINE/DISTRIBUTED BY、MERGE 多 WHEN、UNION/EXCEPT 归一、CTE 链、多表 UPDATE 跨 SET、纯 SELECT 上游依赖、DDL 全类型、方言专属 ARRAY JOIN/EXPLODE/UNNEST/INSERT ALL/TOP/SELECT INTO、业务命名空间 trade/user/pay 等 × 模板放大），期望值独立语义推导（非 parser 自证），评测运行器 TE/FE/UD/DDL 四维 100% 精确匹配。③ 扩充中发现并修复 **2 类真实缺口**——Hive `CTAS ... STORED AS PARQUET` 与 ClickHouse `CTAS ENGINE=MergeTree() ORDER BY ...` 物理属性被 sqlglot 25.x 降级 Command 致血缘全丢（`_preprocess_dialect` 剥离物理属性段，血缘语义不变）；CTE 名与真实表同名遮蔽的**字段级**解析（`_resolve_column` 的 Table 分支改用 `_is_cte_ref` 判断——带 schema 前缀的 `ods.cte1` 是真实表不再穿透 CTE 定义，此前 `SELECT a.id FROM ods.cte1 a JOIN cte1 b` 的 a.id 误解析到 CTE 内部）。验证：评测 504 条四维 100% + lineage 相关 277 项测试全绿 + ruff/format/mypy 0（lineage 目录）+ YAML OK。 | backend: lineage/{parser,repository,service}.py + notify/{consumers,service}.py + tests/eval/* + tests/unit/test_lineage_{repository,service}.py + docs/{module-status,CHANGELOG_MODULES} | released（DDL 变更治理闭环 + 评测集 504） |
| 2026-08-17 | frontend+backend (observability) | 指标健康度低健康指标展示指标名 | **用户诉求：可观测中心指标健康度的低健康指标显示"指标 #1/#2"而非指标名称**。根因：后端 `_overview_quality` 虽 JOIN Metric 却未 select `Metric.name/metric_code`，`top_risk` 仅含 `metric_id`；前端 MetricHealthCard 直显"指标 #{id}"。修复：后端 health 查询 select 补 `Metric.name/metric_code`，`top_risk` 携带 `metric_name/metric_code`（一次 JOIN 随行返回，无 N+1）；前端 types `top_risk` 补字段，MetricHealthCard 渲染「指标名（编码）+ 分数」，无名称回退编码、再回退 ID；测试同步（后端 mock 6 列 + 断言 metric_name/code；前端 fixture 补字段 + 断言指标名渲染）。验证：后端 observability repository 17 项全绿 + ruff 0；前端 Observability 7 项全绿 + tsc 0。 | backend: observability/repository.py + frontend: Observability.tsx/types.ts + 测试 | released（指标健康度低健康指标名称化） |
| 2026-08-17 | backend+frontend (audit) | **审计日志模块企业级完善（方案2 根治）**：① **action 命名统一**——173 个动作从 SCREAMING_SNAKE/点号/小写三套并存收敛为 `{entity}.{verb}` 点号小写单一规范（148 处 write_audit 调用点全量改造，消除 AI 三套命名/泛化 write/同义多命名）；audit_i18n 新增统一动词模板表（140 动词，任何 `{prefix}.{verb}` 命中即翻译，历史旧名保留兼容 WORM 记录）；② **登录/登出审计**——auth.login/login_failed/logout 落审计（GB/T 35273 认证事件留痕，此前模板存在但事件源缺失）；③ **前端接线修复**——表格页改 action_desc 优先、操作人按姓名模糊搜索（后端新增 actor_keyword 参数）、trace_id 跨服务追踪入口、实体类型筛选器全量 44 种（修复 grant→grants 复数永远匹配不到的 bug）、entity_id 技术前缀剥离、分页文案去技术话术、时间线分类 Tag 与 auditI18n 动词表合并去重；④ 语义修正——erasure.anonymize entity_type 从 audit_log 改 erasure_request、「指标冲突冲突」等冗余措辞全面消除。验证：后端 audit/auth 相关 314 passed（新增动词表全覆盖/登录审计 3 项/actor_keyword 2 项）、ruff 0；前端 AuditLog 7 项新增 + AuditTimeline 全绿 + 全量 51 文件 778 passed + tsc 0 | /api/v1/audit（actor_keyword）、/api/v1/auth（login/logout 审计）、全部审计 action 命名 | released（审计日志企业级） |
| 2026-08-17 | frontend (observability) | 可观测中心刷新反馈 + 数据获取时间展示（上海时区精确到秒） | **用户诉求：可观测中心页面点击刷新没有反应，要求页面展示数据获取时间**。根因：① 平台概览刷新按钮调 load(true) 能重新拉取，但页面无任何"数据获取时间"展示——数据未变化时点击零视觉反馈，用户误以为没反应；② 运行指标 Tab 根本没有刷新按钮。修复：① 新增 `formatCnTimeSec`（上海时区、含日期精确到秒），刷新前后秒级变化肉眼可见；② 平台概览 + 运行指标两个 Tab 顶部均新增「数据更新于 xxxx年x月x日 HH:mm:ss（上海时区）」+「刷新」按钮（loading 态防重复点击）；③ MetricsTab 重构为可复用 `load(silent)`（挂载/刷新共用），成功拉取后记录 `lastUpdatedAt`；④ 刷新失败保留旧数据静默降级不打断页面。验证：前端 Observability 9 项全绿（新增"数据获取时间展示 + 刷新触发 API 重拉"与"运行指标刷新按钮"2 项）+ tsc 0。 | frontend: Observability.tsx + Observability.test.tsx | released（可观测中心刷新反馈+数据获取时间） |
| 2026-08-18 | collector (industrial) | **采集模块工业级投产加固（三轮评估 20 项全部落地）** | **用户诉求：从工业级角度全面加固采集模块以真正投产**。三轮评估（安全 P0 + 健壮性 P1 + 加固 P2）共 20 项全部修复：① **P0 安全/可用性**——明文凭据访问控制（`GET /data-sources/{id}` 明文仅 platform_admin，其余掩码回显+二次确认保留密码）；SSRF 防护（host 解析禁私有/环回/链路本地地址 + 禁 db_url 覆盖 + 探活限流，已存源采集放行私有网段但始终拒回环）；worker 超时捕获 `CancelledError` 补写 FAILED（任务不再永久卡 RUNNING）；② **P1 健壮性**——quota.max_scan_rows 生效（表数截断）+ worker max_jobs 全局并发上限；异步采集加 CollectionLock（同源串行化）；定时调度水位补偿（停机错过 cron 补跑）；失败自动重试（瞬时错误 2 次退避）+ 取消 API（`DELETE /jobs/{id}`）；目录关键字搜索按源限定 + 描述覆盖度改 SQL 聚合分页；③ **P2 加固**——审计盲区补齐（库/表枚举补审计）；连接错误脱敏（去 DSN/凭据）；注入守卫豁免密码字段；croniter 写入口校验；collection_run 按保留期归档（每日 2:30）；失败告警归并；hive 子进程超时 terminate；JobStore LPUSH 顺序索引（list/count 免全键 SCAN）；drift 列表大字段裁剪；entity_type 大小写无关；采集记录页时间范围筛选 + 失败重试 + 输入防抖。验证：后端 collector 293 全绿 + 全量 2461+ 过；前端 781 全绿 + tsc 0。 | backend: collector/{service,repository,schemas,queue,tasks,worker}.py + connectors/* + core/{ssrf,probe_throttle,guard,secrets}.py + api/collector.py + frontend: {CollectionHistory,DataSources,Catalogs}.tsx | released（工业级投产加固 20/20） |
| 2026-08-18 | collector (hive_metastore) | **新增 Hive Metastore 直连采集（方案 B）** | **用户诉求：生产场景 Hive 元数据存储在 MySQL（HMS backend），如何采集并关联表/字段/描述**。新增 `hive_metastore` 数据源类型——mysql+aiomysql 直连 HMS 库，从 metastore 表一次 JOIN 拿全量：DBS（库）→ TBLS（表）+ TBL_COMMENT（表描述）+ OWNER + TBL_TYPE + SDS.LOCATION（存储）→ COLUMNS_V2（字段/类型/COMMENT）；metastore 外键即天然关联。① **数据链路**：CatalogSpec 加 description，upsert_catalog 写入 db_catalog.description（description_source=schema），人工描述不覆盖、表描述变化纳入增量判定（不因签名未变被静默丢弃）；② **健壮性**：旧版 Hive（<2.3 无 TBL_COMMENT / <2.0 无 INTEGER_IDX）缺列降级查询；VIEW 识别为 VIEW 实体；复用 include/exclude/多库过滤与 SSRF 防护；③ **枚举**：list_databases/list_tables 供前端库→表级联选择；前端 fallback 类型数组补 Hive Metastore。验证：hive_metastore 连接器 12 项 + upsert description 3 项 + 类型注册断言全绿；collector 193 项全绿；ruff 0；mypy 新文件 0。 | backend: connectors/hive_metastore.py + spi.py + repository.py + service.py + collector_registry.py + frontend: DataSources.tsx + 测试 | released（HMS 直连采集） |
| 2026-08-18 | collector (prod-readiness) | **数据采集菜单栏生产就绪审计（第二轮 4 批次 20 项）** | **评估目标：从生产场景与工业级标准审视采集菜单栏 4 模块（数据源管理/采集目录/采集任务/采集记录）剩余短板**。4 个并行探索代理分模块审计 + 后端链路复核，形成 20 项、分 4 批次 TDD 实施：① **批次1 生产事故级（5 项）**——对账误判防假DROP（配额截断/过滤表排除，避免源端表被当作删除）；增量模式不覆盖 coverage 基线（FULL 用过滤/截断前完整源端数）；postgres connect_args 兼容 asyncpg（含 ssl 场景）；Kafka registry_url SSRF + list 型 bootstrap 校验；SKIPPED/CANCELLED 纳入终态 TTL（免非终态无限堆积）。② **批次2 详情弹窗 cron 回显防静默覆盖 + 非 admin 编辑不覆盖空 host（双保险：前端剔除 + 后端防御）。** ③ **批次3 采集任务三连**——worker 崩溃 RUNNING 滞留清扫（RedisJobStore 维护 updated_at + 每 15 分钟 stale_collection_jobs_task 扫描超时非终态补写 FAILED）；取消语义修正（用户取消保持 CANCELLED 不覆盖不误报）；重试保留原任务 mode（修复全程显示 FULL 掩盖真实模式）。④ **批次4 体验/性能批次**——关键词同时匹配 name+source_id；列表 page_size 加 le=200 上限；健康筛选补 degraded 选项；双页（数据源/目录）搜索输入防抖（input state + ref 定时器，避免逐字符请求）。验证：后端 collector 185 全绿 + ruff 0 + mypy 新文件 0；前端 784 全绿（唯一 3 失败为并行会话 MetricCreate 在途 flaky，非本次）；部署三容器重建为 prod-req-58361c0 全部 healthy；线上验证 page_size 9999→422(≤200)、keyword=unisense 命中 mysql_unisense、类型含 hive_metastore、worker 29 functions 含 stale_collection_jobs_task。 | backend: collector/{service,repository,schemas,queue,tasks,worker}.py + core/ssrf.py + api/collector.py + frontend: {DataSources,Catalogs}.tsx | released（生产就绪审计 20/20） |
| 2026-08-18 | collector (residual) | **采集记录 summary 服务端聚合 + 采集端点 per-user 限流（生产就绪剩余项）** | **用户诉求：处理两轮审计剩余的两个非阻塞项**。① **summary 口径一致**——此前前端用 `page_size=200` 拉全量客户端聚合，总数>200 时成功/失败/扫描/注册只取前 200 条与 `total` 矛盾；改服务端 `summarize_collection_runs` 一次 SQL 分组聚合（COUNT/SUM），`GET /collection-runs/summary` 与列表共用过滤，前端一次取聚合；修正「统计近 200 次」误导文案。② **采集端点 per-user 限流**——`check_collect_rate`（Redis 窗口计数，10 次/分钟，独立 `unisense:collect:` 前缀，`COLLECT_RATE_LIMITED`），注入 collect_source/collect_source_async/collect_now，防止单用户高频触发对源库压力；定时调度不受限。验证：后端 collector 251 全绿 + ruff 0；前端 789 全绿；线上 summary 端点返回 total=100/scanned=28150 全量口径。 | backend: repository/service/api/collector.py + probe_throttle.py + frontend: CollectionHistory.tsx/api.ts + 测试 | released（summary 聚合 + 采集限流） |
| 2026-08-24 | — | ai+frontend | **LLM 推断不可用根因修复 + 系统配置测试增强（方案 B'）**：用户报告「采集目录 LLM 推断提示不可用，但系统配置 LLM 测试通过」——根因：系统配置测试只做 GET /models 轻量探测（连通+鉴权），不验证模型能否真实推理，且绕过熔断器；而推断走真实 chat（带 json_object 约束）+ 熔断器，三个启用实例（opencode `deepseek-v4-flash-free` 400、hy3 json_object 超时、kilo env 兜底 404）全部真实推理失败→ `LLM_INFER_UNAVAILABLE`。修复：① 配置修正——opencode 模型改 `hy3-free`（实测 json_object 200）、hy3 已为 `deepseek-v4-flash`（实测可用）、.env kilo 兜底改 `laguna-s-2.1:free`；容器实跑 build_client 路由 5/5 成功。② 测试增强（方案 B' 替代 A'）——`_probe` 两步探测（GET /models + 真实 POST chat max_tokens=5、无 response_format），`LlmConfigTestResult` 加 chat 字段，前端徽标区分推理失败/连通失败并标注推理正常；端到端实测 kilo 旧模型判「网关可达但真实推理失败 404」。验证：LLM 相关 129 项 + SystemConfig 17 项全绿 + tsc 0 | /api/v1/ai/config/test（返回新增 chat 字段） | ai 保持 released（测试语义增强） |
