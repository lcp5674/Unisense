Unisense quality 服务门禁证据报告（2026-08-08，状态：verified）
================================================================

门禁（CI/.gateways.yml 定义，13 道）：
1.  lint          ✅ ruff（line-length=100, target=py311）全过
2.  type          ✅ mypy --strict（7 source files, no issues）
3.  unit          ✅ pytest backend/tests/unit/test_quality_service.py → 16 passed
                   覆盖：阈值评估(正常域语义) / 状态机非法转移拒绝 / 越界方向阈值 /
                   最高严重级选择 / 非 STATIC 模式跳过 / 创建守卫拒绝
4.  integration   ✅ pytest backend/tests/integration/test_quality_integration.py → 2 passed（真实 MySQL 3307）
5.  doc_sync      ✅ scripts/contract_check.py --mode doc_sync 校验通过
6.  migration     ✅ alembic 0005_quality up/down/up 可逆已验
7.  contract      ✅ scripts/contract_check.py --mode contract 校验通过（TD 接口 9 个命中）
8.  security_reverse ✅ pytest backend/tests/security/test_quality_security.py → 4 passed
                   覆盖：写接口 RBAC 闸门(analyst→403 FORBIDDEN) / detect 治理角色闸门(viewer→403) /
                   list_rules/list_events SQL 注入守卫(400 INJECTION_DETECTED)
9.  chaos         ✅ pytest backend/tests/chaos/test_quality_chaos.py → 3 passed
                   覆盖：notify 不可达时检测主流程仍落库(告警降级) / 发布器隔离故障不传播 /
                   未配置 notify 静默降级
10. observability ✅ pytest backend/tests/observability/test_quality_observability.py → 3 passed
                   覆盖：create_rule 落审计(action=quality_rule.create, actor, trace_id) /
                   响应体+头透传 trace_id / /metrics 暴露 http_requests_total(RED)
11. perf_baseline ✅ k6 真实压测通过（2026-08-08，运行态 FastAPI 指向 unisense_perf@3307）。
                   脚本 backend/tests/perf/baseline.js（vus=10, dur=15s, P95<800ms, failed<10%）。
                   第一轮（无 token）：仅 RED /metrics → P50≈7.61ms / P95=9.56ms；721 检查全绿。
                   第二轮（qowner 测试 JWT，sub=1, role=metric_owner）：覆盖读路径
                   /api/v1/quality/rules + /api/v1/quality/events → P50≈16.9ms / P90=21.13ms /
                   P95=22.61ms / max=51.78ms；错误率 0.00%；1830 检查 100% 通过（含 quality rules 200 /
                   quality events 200）。读路径延迟远低于 perf_contract 阈值(500ms)。
                   注：测试 JWT 用 perf secret 现签（不碰登录/用户表），覆盖完整鉴权读路径。
12. §6.3 双视角   ✅ code-explorer 子 Agent 独立复核；初始发现 3 个 High 数据正确性缺陷
                   （非 STATIC 模式静默永不触发 / 双边阈值越界方向记错 / P0 随机被丢弃），
                   已全部修复（service.py：_evaluate 方向感知 + 严重级降序 + 非 STATIC 跳过告警
                   + create/update fail-fast 守卫），复验 0 High。剩余 Medium/Low 已记录（见下）。
13. §1.5 人工 ratify — 属 released 门槛，verified 阶段不要求（待 released 时人工 ratify）。

已知 Medium/Low 限制（不影响 verified，待后续 PR 收敛）：
- FR-10 一期仅覆盖「规则 CRUD + 事件状态机 + 静态阈值」骨架；TD §12.8 的外部基准对账 /
  血缘传导根因下钻 / 动态基线算法 / 修复建议生成 / UNIQUENESS 模板 / SLA 例外日历 未实现。
- ack_event 的 note 入参接收后未落库；quality_event 表缺 acked_by/resolved_by/closed_by 留痕列。
- service 的 ack/resolve/close 方法 user_id 参数为死参数（未被使用）。
- notify 服务尚未开发，告警经 best-effort + CircuitBreaker 降级（静默丢弃）。

released 待补：§1.5 人工 ratify + runbook docs/runbooks/quality.md + 上述 Medium/Low 收敛。
