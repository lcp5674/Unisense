# 后端 API 基础地址（含 /api/v1）。留空则开发态走 Vite 代理（/api → localhost:8000）。
# 示例：VITE_API_BASE_URL=http://localhost:8000/api/v1
VITE_API_BASE_URL=

# Semantic API 网关鉴权 Key（后端 X-Api-Key 校验，见 backend/app/api/deps.py）。
# 默认值仅适用于本地开发；生产环境通过环境变量注入真实 key。
VITE_SEMANTIC_API_KEY=dev-semantic-key
